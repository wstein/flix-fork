use Sys.Console

/// Prints a prompt, reads the user's name, and greets them.
/// `readln` returns `None` at the end of input, so the line has to be unwrapped.
/// The Console effect has a default handler, so no explicit `runWithIO` is needed.
def main(): Unit \ Console =
    Console.print("What is your name? ");
    match Console.readln() {
        case Some(name) => Console.println("Hello ${name}!")
        case None       => Console.println("No name given.")
    }
