{"files":[{"additions":39,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Fsrc%2Flib.rs","changes":55,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-macros%2Fsrc%2Flib.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":16,"filename":"trussbars/crates/trussbars-macros/src/lib.rs","patch":"@@ -183,7 +183,7 @@ fn expand(input: TokenStream) -> Result<TokenStream, String> {\n     // The optional trailing clauses — `helpers = [a, b]` (F3) and `truthiness = Mode`\n     // (spec §7) — in any order.\n     let mut helpers = Vec::new();\n-    let mut mode = trussbars_template::TruthMode::NonEmpty;\n+    let mut mode = trussbars_template::TruthPolicy::default();\n     for group in &groups[3..] {\n         match clause_key(group).as_deref() {\n             Some(\"helpers\") => helpers = parse_helpers(group)?,\n@@ -258,24 +258,47 @@ fn clause_key(group: &[TokenTree]) -> Option<String> {\n     }\n }\n \n-/// Parse the optional `truthiness = Mode` clause (spec §7) into the policy marker.\n-/// `Mode` is a single ident — `NonEmpty` (default), `Liquid`, or `Handlebars`.\n-fn parse_truthiness(group: &[TokenTree]) -> Result<trussbars_template::TruthMode, String> {\n-    match group {\n-        [\n-            TokenTree::Ident(kw),\n-            TokenTree::Punct(eq),\n-            TokenTree::Ident(mode),\n-        ] if kw.to_string() == \"truthiness\" && eq.as_char() == '=' => {\n-            let name = mode.to_string();\n-            trussbars_template::TruthMode::from_ident(&name).ok_or_else(|| {\n+/// Parse the optional `truthiness = Mode` clause (spec §7, `docs/16`) into the policy.\n+/// `Mode` is either a built-in policy ident — `NonEmpty` (default), `Liquid`, or\n+/// `Handlebars` — or a *host-defined* policy named by a Rust type path (`self::MyMode`,\n+/// `crate::policies::Foo`), emitted as the marker of `truthy_in::<Path, _>`. A lone\n+/// unknown ident is reported as a typo of a built-in (the common case); anything longer\n+/// than one identifier is taken as a host policy path.\n+fn parse_truthiness(group: &[TokenTree]) -> Result<trussbars_template::TruthPolicy, String> {\n+    use trussbars_template::{TruthMode, TruthPolicy};\n+    let rest = match group {\n+        [TokenTree::Ident(kw), TokenTree::Punct(eq), rest @ ..]\n+            if kw.to_string() == \"truthiness\" && eq.as_char() == '=' && !rest.is_empty() =>\n+        {\n+            rest\n+        }\n+        _ => {\n+            return Err(\n+                \"truss!: `truthiness = Mode` needs a built-in mode ident or a host policy path\"\n+                    .into(),\n+            );\n+        }\n+    };\n+    // A lone identifier selects a built-in policy — and a wrong one gets the located\n+    // \"expected …\" hint rather than a confusing downstream trait error. Anything longer\n+    // (a `::`-qualified path or generics) is a host-defined marker, emitted verbatim.\n+    if let [TokenTree::Ident(mode)] = rest {\n+        let name = mode.to_string();\n+        return TruthMode::from_ident(&name)\n+            .map(TruthPolicy::Builtin)\n+            .ok_or_else(|| {\n                 format!(\n-                    \"truss!: unknown truthiness mode `{name}` (expected NonEmpty, Liquid, or Handlebars)\"\n+                    \"truss!: unknown truthiness mode `{name}` (expected NonEmpty, Liquid, or \\\n+                     Handlebars; write a host-defined policy as a path, e.g. `self::MyMode`)\"\n                 )\n-            })\n-        }\n-        _ => Err(\"truss!: `truthiness = Mode` needs a single mode identifier\".into()),\n+            });\n     }\n+    // Reconstruct the path via a `TokenStream` rather than `join(\" \")`: token-wise\n+    // joining would split `::` into `: :` (each colon is a separate `Punct`), so the\n+    // stream's own `Display` — which respects `Punct` spacing/jointness — is the correct\n+    // way to render a path/generic back to valid Rust.\n+    let path = rest.iter().cloned().collect::<TokenStream>().to_string();\n+    Ok(TruthPolicy::Custom(path))\n }\n \n /// Parse the optional `helpers = [name, name, …]` clause into the allow-list of","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Fsrc%2Flib.rs","sha":"cd70e95497d8b07c088cec18d2007a6a48ed1328","status":"modified"},{"additions":39,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Frender.rs","changes":39,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Frender.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":0,"filename":"trussbars/crates/trussbars-macros/tests/render.rs","patch":"@@ -186,3 +186,42 @@ fn block_host_helpers_drive_the_body() {\n     // `repeat` drives the body closure 3× — the power a pre-rendered body wouldn't have.\n     assert_eq!(repeated(&g), \"AdaAdaAda\");\n }\n+\n+// A *host-defined* truthiness policy, selected through the macro (docs/16 §2). Because\n+// the mode is a type parameter, the host writes `impl TruthyIn<NonBlank>` for its own\n+// rule — here \"a whitespace-only string is falsy\", which no built-in policy offers — and\n+// selects it with `truthiness = self::NonBlank` (a path, so a lone built-in typo still\n+// gets the located \"expected NonEmpty, …\" hint).\n+struct NonBlank;\n+impl trussbars_core::TruthyIn<NonBlank> for String {\n+    fn truthy(&self) -> bool {\n+        !self.trim().is_empty()\n+    }\n+}\n+struct Banner {\n+    title: String,\n+}\n+truss!(\n+    banner,\n+    Banner,\n+    \"{{#if title}}show{{else}}hide{{/if}}\",\n+    truthiness = self::NonBlank\n+);\n+\n+#[test]\n+fn host_defined_policy_selected_through_the_macro() {\n+    assert_eq!(banner(&Banner { title: \"Hi\".into() }), \"show\");\n+    // Whitespace-only is falsy under NonBlank — a rule no built-in policy can express.\n+    assert_eq!(\n+        banner(&Banner {\n+            title: \"   \".into()\n+        }),\n+        \"hide\"\n+    );\n+    assert_eq!(\n+        banner(&Banner {\n+            title: String::new()\n+        }),\n+        \"hide\"\n+    );\n+}","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Frender.rs","sha":"8140e9fb63d8fa3a6213df881af9b8e6c2a1ed32","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Fui%2Funknown_truthiness.stderr","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Fui%2Funknown_truthiness.stderr?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":1,"filename":"trussbars/crates/trussbars-macros/tests/ui/unknown_truthiness.stderr","patch":"@@ -1,4 +1,4 @@\n-error: truss!: unknown truthiness mode `Mustache` (expected NonEmpty, Liquid, or Handlebars)\n+error: truss!: unknown truthiness mode `Mustache` (expected NonEmpty, Liquid, or Handlebars; write a host-defined policy as a path, e.g. `self::MyMode`)\n  --> tests/ui/unknown_truthiness.rs:5:1\n   |\n 5 | truss!(render, str, \"{{#if name}}x{{/if}}\", truthiness = Mustache);","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-macros%2Ftests%2Fui%2Funknown_truthiness.stderr","sha":"00fbad0d10d16eb324ff2f9241b3ebc4d27f288f","status":"modified"},{"additions":26,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fast.rs","changes":26,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fast.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":0,"filename":"trussbars/crates/trussbars-template/src/ast.rs","patch":"@@ -39,6 +39,32 @@ impl TruthMode {\n     }\n }\n \n+/// The truthiness policy the **AOT emitter** compiles a template under: either a built-in\n+/// [`TruthMode`] marker, or a *host-defined* policy named by its Rust type path\n+/// (`truss!(…, truthiness = self::MyMode)`), emitted verbatim as the type argument of\n+/// `truthy_in::<Path, _>`. This richer form is AOT-only — a host `TruthyIn<Mode>` impl is\n+/// resolved at compile time, so the dynamic VM keeps the closed [`TruthMode`] enum\n+/// (`docs/16-truthiness-modes.md`).\n+#[derive(Clone, Debug, PartialEq, Eq)]\n+pub enum TruthPolicy {\n+    /// One of the three built-in, `trussbars-core`-provided policy markers.\n+    Builtin(TruthMode),\n+    /// A host-defined marker, named by its Rust type path (e.g. `self::NonBlank`).\n+    Custom(String),\n+}\n+\n+impl Default for TruthPolicy {\n+    fn default() -> Self {\n+        Self::Builtin(TruthMode::NonEmpty)\n+    }\n+}\n+\n+impl From<TruthMode> for TruthPolicy {\n+    fn from(mode: TruthMode) -> Self {\n+        Self::Builtin(mode)\n+    }\n+}\n+\n /// A literal value.\n #[derive(Debug, Clone, PartialEq)]\n pub enum Value {","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fast.rs","sha":"ca595469d3cd2f811773ec6a6471032024115ec4","status":"modified"},{"additions":39,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","changes":57,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":18,"filename":"trussbars/crates/trussbars-template/src/emit.rs","patch":"@@ -6,27 +6,28 @@\n use std::collections::{BTreeMap, BTreeSet};\n use std::rc::Rc;\n \n-use crate::ast::{Case, Cond, Each, Expr, HelperBlock, Node, TruthMode, Value, With};\n+use crate::ast::{Case, Cond, Each, Expr, HelperBlock, Node, TruthMode, TruthPolicy, Value, With};\n use crate::parse::parse;\n use crate::span::Span;\n \n-impl TruthMode {\n+impl TruthPolicy {\n     /// The truthiness call for this policy over an already-referenced expression\n-    /// (`&(expr)` or a borrowed binding). `NonEmpty` emits the bare `truthy(…)` —\n-    /// byte-identical to the v1 emitter, so the conformance corpus is unaffected; a\n-    /// non-default policy routes through the monomorphized `truthy_in::<Mode, _>(…)`.\n+    /// (`&(expr)` or a borrowed binding). The default `NonEmpty` emits the bare\n+    /// `truthy(…)` — byte-identical to the v1 emitter, so the conformance corpus is\n+    /// unaffected; every other policy (built-in or host-defined) routes through the\n+    /// monomorphized `truthy_in::<Mode, _>(…)`.\n     /// (Defined here, in the std-only emit module, since it builds codegen strings; the\n-    /// policy enum itself lives in [`crate::ast`] so the `no_std` VM can name it.)\n-    fn call(self, ref_expr: &str) -> String {\n-        match self {\n-            Self::NonEmpty => format!(\"trussbars_core::truthy({ref_expr})\"),\n-            Self::Liquid => {\n-                format!(\"trussbars_core::truthy_in::<trussbars_core::Liquid, _>({ref_expr})\")\n+    /// policy types live in [`crate::ast`] so the `no_std` VM can name [`TruthMode`].)\n+    fn call(&self, ref_expr: &str) -> String {\n+        let marker = match self {\n+            Self::Builtin(TruthMode::NonEmpty) => {\n+                return format!(\"trussbars_core::truthy({ref_expr})\");\n             }\n-            Self::Handlebars => {\n-                format!(\"trussbars_core::truthy_in::<trussbars_core::Handlebars, _>({ref_expr})\")\n-            }\n-        }\n+            Self::Builtin(TruthMode::Liquid) => \"trussbars_core::Liquid\",\n+            Self::Builtin(TruthMode::Handlebars) => \"trussbars_core::Handlebars\",\n+            Self::Custom(path) => path,\n+        };\n+        format!(\"trussbars_core::truthy_in::<{marker}, _>({ref_expr})\")\n     }\n }\n \n@@ -55,8 +56,9 @@ pub fn emit_named(\n     ctx_type: &str,\n     src: &str,\n     helpers: &[String],\n-    mode: TruthMode,\n+    mode: impl Into<TruthPolicy>,\n ) -> Result<String, String> {\n+    let mode = mode.into();\n     // A host cannot declare a built-in block head (`if`/`each`/`case`/…) as a helper —\n     // it is shadowed by the parser, so accepting it would silently never fire (docs/12 §5.2).\n     if let Some(name) = helpers\n@@ -128,7 +130,7 @@ struct Env {\n     /// functions. Shared (cheap clone) across the recursive emit.\n     helpers: Rc<BTreeSet<String>>,\n     /// The truthiness policy this template compiles under (spec §7).\n-    mode: TruthMode,\n+    mode: TruthPolicy,\n }\n \n impl Env {\n@@ -1138,7 +1140,7 @@ fn rust_str(s: &str) -> String {\n \n #[cfg(test)]\n mod tests {\n-    use super::{TruthMode, emit, emit_named};\n+    use super::{TruthMode, TruthPolicy, emit, emit_named};\n \n     fn e(src: &str) -> String {\n         emit(\"Ctx\", src).unwrap_or_else(|err| panic!(\"{src:?}: {err}\"))\n@@ -1219,6 +1221,25 @@ mod tests {\n         );\n     }\n \n+    #[test]\n+    fn custom_policy_emits_the_host_marker_path() {\n+        // A host-defined policy is emitted verbatim as the `truthy_in::<Path, _>` marker,\n+        // so the host's `impl TruthyIn<MyMode>` governs the condition (docs/16 §2).\n+        let out = emit_named(\n+            \"render\",\n+            \"Ctx\",\n+            \"{{#if flag}}x{{/if}}\",\n+            &[],\n+            TruthPolicy::Custom(\"self :: NonBlank\".into()),\n+        )\n+        .unwrap();\n+        assert!(\n+            out.contains(\"trussbars_core::truthy_in::<self :: NonBlank, _>(&(\"),\n+            \"{out}\"\n+        );\n+        assert!(!out.contains(\"trussbars_core::truthy(&(\"), \"{out}\");\n+    }\n+\n     #[test]\n     fn output_escaped_and_raw() {\n         assert!(e(\"{{name}}\").contains(\"trussbars_core::esc(&(ctx.name), &mut out)\"));","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","sha":"f25e2bf341f5b15bec54750244c4de8be70a96f0","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":1,"filename":"trussbars/crates/trussbars-template/src/lib.rs","patch":"@@ -36,7 +36,7 @@ pub mod parse;\n pub mod parse_expr;\n pub mod span;\n \n-pub use ast::{Case, Cond, Each, Expr, Node, TruthMode, Value, With};\n+pub use ast::{Case, Cond, Each, Expr, Node, TruthMode, TruthPolicy, Value, With};\n #[cfg(feature = \"std\")]\n pub use emit::{emit, emit_named};\n pub use lex::{Lexeme, Sigil, lex};","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs","sha":"d516009d00b3db2741de2915b375b1cc5abbfe86","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fdocs%2F16-truthiness-modes.md","changes":22,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fdocs%2F16-truthiness-modes.md?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":7,"filename":"trussbars/docs/16-truthiness-modes.md","patch":"@@ -72,14 +72,21 @@ policy (`Option` defers to its inner type *under the same policy*, so `Some(\"\")`\n ## 4. Selecting a policy\n \n ```rust\n-truss!(card, Card, \"{{#if tags}}…{{/if}}\", truthiness = Liquid);\n+truss!(card, Card, \"{{#if tags}}…{{/if}}\", truthiness = Liquid);        // a built-in policy\n+truss!(note, Note, \"{{#if body}}…{{/if}}\", truthiness = self::NonBlank); // a host policy\n ```\n \n-The `truthiness = Mode` clause sits alongside `helpers = [..]` (any order). The default is\n-`NonEmpty`. An unknown mode is a located class-A `compile_error!` naming the valid modes\n-(pinned by `tests/ui/unknown_truthiness.{rs,stderr}`). The choice is per-template and visible\n-in the source — the same template compiled with and without the clause renders `[]` as truthy\n-or falsy respectively, so a divergence is never silent.\n+The `truthiness = Mode` clause sits alongside `helpers = [..]` (any order); the default is\n+`NonEmpty`. `Mode` is either a **built-in ident** (`NonEmpty` / `Liquid` / `Handlebars`) or a\n+**host policy named by its Rust type path** (`self::NonBlank`, `crate::policies::Foo`): the\n+emitter drops the path straight into `truthy_in::<Path, _>`, so a host's own\n+`impl TruthyIn<Mode>` (the §2 orphan-rule extension) governs the condition at zero cost. The\n+path requirement is the disambiguator — a *lone* identifier must be a built-in, so a typo\n+(`truthiness = Mustache`) stays a located class-A `compile_error!` naming the valid modes\n+(pinned by `tests/ui/unknown_truthiness.{rs,stderr}`), while a host marker is written as a\n+path. The choice is per-template and visible in the source — the same template with and\n+without the clause renders `[]` as truthy or falsy respectively, so a divergence is never\n+silent.\n \n ## 5. Conformance posture\n \n@@ -90,7 +97,8 @@ construction**, exactly like custom helpers (`docs/01` §11). They are gated ins\n \n - `trussbars-core` `TruthyIn<Mode>` unit tests (every policy, including a host-defined marker\n   proving the orphan-rule extension);\n-- the `truss!(…, truthiness = …)` end-to-end render test;\n+- the `truss!(…, truthiness = …)` end-to-end render tests — built-in *and* host-selected-by-path;\n+- the emitter unit test that a custom path becomes the `truthy_in::<Path, _>` marker;\n - the unknown-mode `trybuild` golden.\n \n ## 6. Alternatives rejected","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fdocs%2F16-truthiness-modes.md","sha":"25e0974e3c195dd25432809e97d63f2384d23ba6","status":"modified"},{"additions":30,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Flib.rs","changes":35,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Flib.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":5,"filename":"trussbars/examples/truthiness/src/lib.rs","patch":"@@ -85,11 +85,11 @@ pub fn empty_cart() -> Value {\n }\n \n // ── 3. A host-defined policy over a foreign type (the orphan-rule extension) ───────────\n-// `NonBlank` is a *local* marker, so impl'ing the foreign trait `TruthyIn` for the foreign\n-// type `str` is allowed — a policy the built-ins don't offer: a whitespace-only string is\n-// falsy. This is the \"define your own rule, even for std types\" capability; it is a\n-// library-level (`truthy_in`) facility, not a `truss!` macro clause (which is limited to\n-// the built-in policy idents).\n+// `NonBlank` is a *local* marker, so impl'ing the foreign trait `TruthyIn` for foreign\n+// types (`str`/`String`) is allowed — a policy the built-ins don't offer: a whitespace-only\n+// string is falsy. This is the \"define your own rule, even for std types\" capability, and\n+// it is usable at every layer: the library `truthy_in` (below) *and* — selected by its type\n+// path — the `truss!(…, truthiness = self::NonBlank)` AOT clause.\n \n /// A host policy in which a string is truthy only when it has non-whitespace content.\n pub struct NonBlank;\n@@ -100,9 +100,34 @@ impl TruthyIn<NonBlank> for str {\n     }\n }\n \n+// The owned-`String` impl too, so a context *field* (serde deserializes strings as `String`)\n+// can sit in a boolean position under this policy via the macro.\n+impl TruthyIn<NonBlank> for String {\n+    fn truthy(&self) -> bool {\n+        !self.trim().is_empty()\n+    }\n+}\n+\n /// Whether `s` is truthy under the [`NonBlank`] host policy (`\"   \"` is falsy, unlike the\n /// built-in policies where any non-empty string is truthy).\n #[must_use]\n pub fn non_blank(s: &str) -> bool {\n     truthy_in::<NonBlank, _>(&s)\n }\n+\n+/// A one-field context, to show the host policy governing a real template condition.\n+pub struct Note {\n+    /// The note body — blank (empty *or* whitespace-only) takes the `{{else}}` arm under\n+    /// [`NonBlank`], where any non-empty string would be truthy under the built-ins.\n+    pub body: String,\n+}\n+\n+// The host policy selected through the macro by its type path — the capability the AOT\n+// `truthiness` clause gained: not just the three built-in idents, but any `TruthyIn<Mode>`\n+// a host defines. A lone unknown ident still gets the located \"expected NonEmpty, …\" hint.\n+truss!(\n+    note_nonblank,\n+    Note,\n+    \"{{#if body}}content{{else}}blank{{/if}}\",\n+    truthiness = self::NonBlank\n+);","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Flib.rs","sha":"462acd8ce4c9c49b04268e6347419ca9777549c2","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Fmain.rs","changes":14,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Fmain.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":2,"filename":"trussbars/examples/truthiness/src/main.rs","patch":"@@ -3,8 +3,8 @@\n \n use trussbars_vm::TruthMode;\n use truthiness_example::{\n-    Cart, cart_default, cart_liquid, count_handlebars, count_liquid, empty_cart, non_blank,\n-    vm_render,\n+    Cart, Note, cart_default, cart_liquid, count_handlebars, count_liquid, empty_cart, non_blank,\n+    note_nonblank, vm_render,\n };\n \n fn main() {\n@@ -30,4 +30,14 @@ fn main() {\n     println!(\"\\n== Library: a host-defined policy over a foreign type (str) ==\");\n     println!(\"  non_blank(\\\"hi\\\")  = {}\", non_blank(\"hi\")); // true\n     println!(\"  non_blank(\\\"   \\\") = {}\", non_blank(\"   \")); // false (whitespace-only)\n+\n+    println!(\"\\n== AOT: that same host policy, selected in the macro by its path ==\");\n+    println!(\n+        \"  truthiness = self::NonBlank, body=\\\"hi\\\"  -> {}\",\n+        note_nonblank(&Note { body: \"hi\".into() })\n+    ); // content\n+    println!(\n+        \"  truthiness = self::NonBlank, body=\\\"   \\\" -> {}\",\n+        note_nonblank(&Note { body: \"   \".into() })\n+    ); // blank (whitespace-only is falsy)\n }","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Fsrc%2Fmain.rs","sha":"a4e96b77c35a925c717b0c96348d486bc3292ce2","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/flatbars/blob/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Ftests%2Fpolicies.rs","changes":17,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Ftruthiness%2Ftests%2Fpolicies.rs?ref=8c6ff4888d218865ba8018252ccaa124d2426024","deletions":2,"filename":"trussbars/examples/truthiness/tests/policies.rs","patch":"@@ -3,8 +3,8 @@\n \n use trussbars_vm::TruthMode;\n use truthiness_example::{\n-    Cart, cart_default, cart_liquid, count_handlebars, count_liquid, empty_cart, non_blank,\n-    vm_render,\n+    Cart, Note, cart_default, cart_liquid, count_handlebars, count_liquid, empty_cart, non_blank,\n+    note_nonblank, vm_render,\n };\n \n fn empty() -> Cart {\n@@ -57,3 +57,16 @@ fn host_defined_policy_over_a_foreign_type() {\n     assert!(!non_blank(\"   \"));\n     assert!(!non_blank(\"\"));\n }\n+\n+#[test]\n+fn host_defined_policy_selected_through_the_macro() {\n+    // The same policy, chosen by its path in the `truss!` clause (not just a built-in ident).\n+    assert_eq!(note_nonblank(&Note { body: \"hi\".into() }), \"content\");\n+    assert_eq!(note_nonblank(&Note { body: \"   \".into() }), \"blank\");\n+    assert_eq!(\n+        note_nonblank(&Note {\n+            body: String::new()\n+        }),\n+        \"blank\"\n+    );\n+}","raw_url":"https://github.com/wstein/flatbars/raw/8c6ff4888d218865ba8018252ccaa124d2426024/trussbars%2Fexamples%2Ftruthiness%2Ftests%2Fpolicies.rs","sha":"7e08b5b693e832d3d0867a5f4ff3669a05a0255c","status":"modified"}]}