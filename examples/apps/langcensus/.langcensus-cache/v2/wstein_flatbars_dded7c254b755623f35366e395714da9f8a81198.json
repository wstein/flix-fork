{"files":[{"additions":19,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Findex.html","changes":203,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":184,"filename":"lab/index.html","patch":"@@ -703,14 +703,6 @@\n         font-weight: 700; color: var(--tok-key);\n         box-shadow: inset 0 -1px 0 var(--accent-soft);\n       }\n-      /* ST4 transpile-error marker: distinct amber border so the\n-         `<! transpile-error: ... !>` comments are visible at a glance,\n-         with the same hover-link affordance as normal segments. */\n-      #output-text .seg-error {\n-        background: var(--danger-soft, rgba(220, 90, 60, 0.10));\n-        box-shadow: inset 0 0 0 1px var(--danger, #d97706);\n-        color: var(--danger, #b25500);\n-      }\n       /* The full-segment hover/caret link highlight is applied as a JS decoration\n          (.cm-out-linked) over the run's whole range, so a run that wraps across\n          output lines lights up on every line — not just the fragment under the\n@@ -1726,9 +1718,7 @@\n         hasFeature,\n         mergeDataOverlays,\n         partialNameAt,\n-        scanTruthinessPragma,\n         tabVisibleUnder,\n-        transpileSt4,\n         validateOverlayName,\n         vendoredVerdict,\n         vendoredWorkspace,\n@@ -2005,9 +1995,6 @@\n       // Compiler & runtime options (ADR-0016).\n       // `standalone`    — strip standalone-tag lines at compile time (ADR-0016, default on).\n       let standalone      = true;\n-      // `st4Mode`        — force the `{{! @truthiness: st4 }}` gate for ST4 export\n-      //                    even when the pragma is absent (ST4 Preview / emit).\n-      let st4Mode         = false;\n       // `allowList`      — ADR-0013 positive allow-list: when an array, only\n       //                    listed transformer names are callable at render\n       //                    time (the production \"close unused ports\" lever).\n@@ -2223,32 +2210,6 @@\n         },\n       });\n \n-      // Highlighter for the ST4 Preview pane (Phase 3 live transpiler).\n-      // Recognises ST4's `<...>` attribute references and the few control\n-      // keywords (`if`/`else`/`endif`/`elseif`/`delimiters`), plus quoted\n-      // option values and the `format`/`null`/`separator` option names the\n-      // four ST4-compatible transformers emit.\n-      const ST4_KEYWORDS = /^(?:if|elseif|else|endif|delimiters)\\b/;\n-      const ST4_OPTIONS = /^(?:format|null|separator|wrap|anchor|indent)\\b/;\n-      const st4SourceLang = StreamLanguage.define({\n-        token(stream) {\n-          if (stream.match(/<!.*?!>/)) return \"comment\";\n-          if (stream.eatSpace()) return null;\n-          if (stream.match(/\"(?:[^\"\\\\]|\\\\.)*\"/)) return \"string\";\n-          if (stream.peek() === \"<\") {\n-            stream.next();\n-            return \"punctuation\";\n-          }\n-          if (stream.match(ST4_KEYWORDS)) return \"keyword\";\n-          if (stream.match(ST4_OPTIONS)) return \"propertyName\";\n-          if (stream.match(/[A-Za-z_]\\w*(?:\\.[A-Za-z_]\\w*)*/)) return \"variableName\";\n-          if (stream.match(/-?\\d+/)) return \"number\";\n-          if (stream.match(/::=|=|;|,|>|\\||{|}|\\(|\\)|\\.|!/)) return \"operator\";\n-          stream.next();\n-          return null;\n-        },\n-      });\n-\n       // Coverage decoration (Panel #7: branch coverage in the editor).\n       // The dock's Coverage tab lists every `if`/`unless`/`each`/`with` block\n       // and whether it would execute against the current data. This field\n@@ -2568,7 +2529,7 @@\n         const tmplActive = activeView === \"tmpl\" && templateMode === \"tmpl\";\n         const xfActive   = activeView === \"tmpl\" && templateMode === \"transform\";\n         const hlpActive  = activeView === \"tmpl\" && templateMode === \"helpers\";\n-        const cfgNonDefault = !!escapeMode || !standalone || st4Mode || allowList !== null;\n+        const cfgNonDefault = !!escapeMode || !standalone || allowList !== null;\n \n         const groups = [\n           { key: \"template\", label: \"TEMPLATE\", files: [\n@@ -2637,7 +2598,7 @@\n           ]}]),\n           { key: \"config\", label: \"CONFIG\", files: [\n             { icon: \"cfg\", name: \"config.yaml\", active: activeView === \"config\", dirty: cfgNonDefault,\n-              title: \"render config — escape · trim · ST4 · allow-list · i18n locale · MinBars truthiness\",\n+              title: \"render config — escape · trim · allow-list · i18n locale · MinBars truthiness\",\n               on: () => selectConfigTab() },\n           ]},\n         ];\n@@ -2854,7 +2815,7 @@\n             const isActive = activeView === \"config\";\n             const t = document.createElement(\"div\");\n             t.className = \"tab tab-transform\" + (isActive ? \" active\" : \"\");\n-            t.title = \"render config — escape · trim · ST4 · allow-list\";\n+            t.title = \"render config — escape · trim · allow-list\";\n             t.append(document.createTextNode(\"config.yaml\"));\n             t.addEventListener(\"click\", () => selectConfigTab());\n             t.append(tabCloseBtn(() => closeEditor(\"config\")));\n@@ -2979,7 +2940,7 @@\n         const v = res.value;\n         escapeMode = v.escape; byId(\"escape-select\").value = v.escape;\n         standalone = v.trim;   byId(\"standalone-cb\").checked = v.trim;\n-        st4Mode = v.st4; allowList = v.allow; minbarsCompat = v.mustacheJs; labLocale = v.locale;\n+        allowList = v.allow; minbarsCompat = v.mustacheJs; labLocale = v.locale;\n         updateRenderStatus(); renderTabs();\n         scheduleRun(); saveHash();\n       }\n@@ -3460,16 +3421,6 @@\n         if (outputView === \"bytecode\") {\n           lastSegViews = [];\n           setOutputDoc(lastProgram ? disassemble(lastProgram) : \"\", Decoration.none);\n-        } else if (outputView === \"st4\") {\n-          // Live ST4 source emit driven by `transpileSt4` against the\n-          // current parsed AST. Re-renders on each compile cycle alongside\n-          // the Bytecode disasm tab. Provenance segments wire the hover/\n-          // click handlers so a `<! transpile-error: ... !>` comment jumps\n-          // straight to the offending Stem source span.\n-          const { output, segments } = renderSt4Preview();\n-          const { segViews, deco } = buildSt4Decorations(segments);\n-          lastSegViews = segViews;\n-          setOutputDoc(output, deco);\n         } else if (outputView === \"data\") {\n           let text = \"\";\n           if (lastData != null) {\n@@ -3566,14 +3517,6 @@\n           outputEditor.dispatch({ effects: outLangComp.reconfigure([bytecodeLang]) });\n           return;\n         }\n-        if (outputView === \"st4\") {\n-          // ST4 source grammar — highlights `<...>` references, ST keywords\n-          // (`<if(...)>`/`<endif>`/etc.), option names (`format`/`null`/\n-          // `separator`), quoted option values, and `<! transpile-error: ... !>`\n-          // comments emitted for ❌ rows.\n-          outputEditor.dispatch({ effects: outLangComp.reconfigure([st4SourceLang]) });\n-          return;\n-        }\n         if (outputView === \"compiled\") {\n           outputEditor.dispatch({ effects: outLangComp.reconfigure([jsLang]) });\n           return;\n@@ -3998,77 +3941,6 @@\n       // the engine's advertised features so existing call sites stay arg-free.\n       const visibleViews = () => ovVisibleViews(engineFeatures);\n \n-      // Phase 3 transpiler is live (`transpileSt4` in playground_utils.mjs).\n-      // The ST4 Preview pane shows the current template emitted as `.stg`\n-      // group source against the latest parsed AST. The walker mirrors the\n-      // BEAM `Stem.ST4.Transpile` for the matrix's ✅/⚠️ rows and renders\n-      // ❌ rows inline as `<! transpile-error: ... !>` ST4 comments so the\n-      // preview is never empty (the canonical walker raises; the preview\n-      // displays). The empty-AST placeholder shown before a first compile:\n-      const ST4_PREVIEW_EMPTY =\n-        \"// Stem → ST4 source transpiler. Edit the template to see the\\n\" +\n-        \"// emitted `.stg` group source here.\\n\" +\n-        \"//\\n\" +\n-        \"// Templates intended for ST4 export must declare\\n\" +\n-        \"//     {{! @truthiness: st4 }}\\n\" +\n-        \"// at the top. Without it, the preview shows the source emit but\\n\" +\n-        \"// flags the truthiness gate failure as a comment — Stem.ST4.Transpile\\n\" +\n-        \"// (the canonical walker used by `mix stem.st4.emit` and the parity\\n\" +\n-        \"// gate) raises Stem.ST4.TranspileError on the same condition.\\n\";\n-\n-      // Build the live ST4 preview from the current parsed AST. Returns\n-      // `{ output, segments }` where `segments` carries `{outBegin, outEnd,\n-      // file, start, end, kind}` per emitted run — fed to buildSt4Decorations\n-      // so the playground's hover/click handlers can localise both Stem\n-      // constructs and `<! transpile-error: ... !>` comments back to the\n-      // template source. With no parsed AST yet, returns the empty-hint\n-      // placeholder and no segments.\n-      function renderSt4Preview() {\n-        if (!lastAst || !lastAst.main) {\n-          return { output: ST4_PREVIEW_EMPTY, segments: [] };\n-        }\n-        const main = lastAst.main;\n-        const truthiness = scanTruthinessPragma(templateSourceFor(\"main\")) || (st4Mode ? \"st4\" : \"stem\");\n-        const partialsAst = {};\n-        for (const [name, nodes] of Object.entries(lastAst)) {\n-          if (name !== \"main\") partialsAst[name] = nodes;\n-        }\n-        const hasPartials = Object.keys(partialsAst).length > 0;\n-        return transpileSt4(\n-          main,\n-          { truthiness, map: true },\n-          hasPartials ? partialsAst : null,\n-        );\n-      }\n-\n-      // Build CodeMirror mark decorations + the segViews shape the source-\n-      // view hover/click handlers consume. ST4 segments are already char-\n-      // indexed (the JS transpiler tracks JS string positions), so we don't\n-      // need the UTF-8 byte-to-char dance `buildProvenance` does for the\n-      // wasm-emitted source-map.\n-      function buildSt4Decorations(segments) {\n-        const segViews = [];\n-        const ranges = [];\n-        for (const seg of segments) {\n-          const from = seg.outBegin;\n-          const to = seg.outEnd;\n-          if (from >= to) continue;\n-          const i = segViews.length;\n-          segViews.push({\n-            from, to,\n-            file: seg.file || \"main\",\n-            kind: seg.kind || \"emit\",\n-            start: typeof seg.start === \"number\" ? seg.start : null,\n-            end: typeof seg.end === \"number\" ? seg.end : null,\n-          });\n-          ranges.push(Decoration.mark({\n-            class: \"seg\" + (seg.kind === \"error\" ? \" seg-error\" : seg.kind === \"emit\" ? \" seg-expr\" : \"\"),\n-            attributes: { \"data-i\": String(i) },\n-          }).range(from, to));\n-        }\n-        return { segViews, deco: ranges.length ? Decoration.set(ranges, true) : Decoration.none };\n-      }\n-\n       function templateSourceFor(file) {\n         if (file === \"main\") return tabs[0]?.source || \"\";\n         return partialsMap()[file] || \"\";\n@@ -4094,7 +3966,7 @@\n \n       function setOutputView(view) {\n         outputView = view;\n-        // Text views (Plain Text / Render Data / Bytecode / ST4 / Compiled JS /\n+        // Text views (Plain Text / Render Data / Bytecode / Compiled JS /\n         // Truthiness) paint into the editor; \"rendered\"/\"markdown\" use the iframe.\n         // `applyView` (lab/output-view.mjs) toggles the panes and reports the kind.\n         const isText =\n@@ -4113,7 +3985,7 @@\n         caretLinkKey = null; syncOutputToCaret();\n         // Re-render the dock (Coverage panel) and the inspector pane (Context\n         // Inspector) so their render-only placeholders show when switching\n-        // into ST4 Preview and revert to live data on switch back.\n+        // into a render-only view and revert to live data on switch back.\n         renderDock();\n         renderDataInspect();\n       }\n@@ -4133,11 +4005,6 @@\n         const file = sv.file;\n         const isExpr = sv.kind === \"emit\";\n         const isError = sv.kind === \"error\";\n-        // ST4 Preview is a source-to-source transpile, not a render. The\n-        // segments map ST4 tokens back to the Stem source they were transformed\n-        // from, so the tooltip uses \"transpiled from\" to make the domain\n-        // explicit and prevent confusion with render-time provenance.\n-        const isTranspile = outputView === \"st4\";\n         segTip.textContent = \"\";\n \n         // Header forms:\n@@ -4149,9 +4016,7 @@\n         kindEl.textContent = isError\n           ? \"transpile error\"\n           : isExpr ? \"expression\" : \"literal\";\n-        const connector = isError\n-          ? \" at \"\n-          : isTranspile ? \" transpiled from \" : \" from \";\n+        const connector = isError ? \" at \" : \" from \";\n         head.append(kindEl, document.createTextNode(connector));\n         const fileEl = document.createElement(\"span\");\n         fileEl.className = \"seg-tip-file\";\n@@ -4260,10 +4125,9 @@\n       let userMovedCaret = false;\n \n       function syncOutputToCaret() {\n-        // Suppressed mid-render (offsets stale), when neither the Plain Text\n-        // nor ST4 Preview view is showing, and on the default boot caret\n-        // before any user interaction.\n-        if (renderPending || (outputView !== \"source\" && outputView !== \"st4\") || !userMovedCaret) {\n+        // Suppressed mid-render (offsets stale), when the Plain Text view is\n+        // not showing, and on the default boot caret before any user interaction.\n+        if (renderPending || outputView !== \"source\" || !userMovedCaret) {\n           outputEditor.dispatch({ effects: setOutLink.of(Decoration.none) });\n           caretLinkKey = null;\n           return;\n@@ -4308,7 +4172,7 @@\n       // Keyboard parity: Enter on the output editor jumps from the run under the\n       // cursor to its source (arrows/Home/End navigate via the default keymap).\n       function jumpToSourceAtCursor(view) {\n-        if ((outputView !== \"source\" && outputView !== \"st4\") || !lastSegViews.length)\n+        if (outputView !== \"source\" || !lastSegViews.length)\n           return false;\n         const pos = view.state.selection.main.head;\n         const sv = lastSegViews.find((s) => pos >= s.from && pos < s.to)\n@@ -4343,9 +4207,9 @@\n           const sv = svOf(e.target);\n           if (!sv) return;\n           openSourceLocation(sv.file, sv.start, sv.end);\n-          // Context Inspector reads scope at a Plain-Text output span; ST4 (and\n-          // future non-source views) share the seg/click model only for caret\n-          // navigation, so the dock auto-switch would be a surprise here.\n+          // Context Inspector reads scope at a Plain-Text output span; other\n+          // views share the seg/click model only for caret navigation, so the\n+          // dock auto-switch would be a surprise here.\n           if (outputView === \"source\") setContextTarget(sv);\n         });\n       })();\n@@ -4575,7 +4439,6 @@\n         escapeMode = \"\";\n         byId(\"escape-select\").value = \"\";\n         standalone = true;\n-        st4Mode = false;\n         minbarsCompat = true; // default mustacheJs (ADR-029)\n         allowList = null;\n         syncStandaloneUI();\n@@ -4642,7 +4505,6 @@\n         escapeMode = \"\";\n         byId(\"escape-select\").value = \"\";\n         standalone = true;\n-        st4Mode = false;\n         minbarsCompat = true; // default mustacheJs (ADR-029)\n         allowList = null;\n         syncStandaloneUI();\n@@ -4698,7 +4560,6 @@\n           ...(dataOverlays.length ? { do: dataOverlays.map((o) => ({ n: o.name, s: o.source })) } : {}),\n           e: escapeMode, v: outputView,\n           sa: standalone ? 1 : 0,       // ADR-0016 standalone flag\n-          ...(st4Mode ? { s4: 1 } : {}),            // ST4 truthiness gate\n           ...(allowList !== null ? { al: allowList } : {}), // ADR-0013 transformer allow-list\n           av: activeView, ai: activeTabIdx,\n           // MinBars truthiness toggle (ADR-022 S2); omitted when off so links stay compact.\n@@ -4768,7 +4629,6 @@\n           escapeMode = s.e ?? \"\";\n           byId(\"escape-select\").value = escapeMode;\n           standalone   = s.sa !== 0;  // default true; explicit 0 opts out\n-          st4Mode = s.s4 === 1;\n           minbarsCompat = s.mj !== 0; // default mustacheJs; explicit mj:0 = spec (ADR-029)\n           allowList = Array.isArray(s.al) ? s.al.map(String) : null;\n           syncStandaloneUI();\n@@ -5792,7 +5652,7 @@\n \n       // ── Panel #5: Capabilities / escape provenance ────────────────────\n       // The security profile is per-engine (ADR-0020): Stem renders its\n-      // render-time host policy (allow-list, eval gate, escape modes, ST4);\n+      // render-time host policy (allow-list, eval gate, escape modes);\n       // Handlebars renders its own — compile-time helper whitelist, prototype-\n       // access hardening, and the global escaping model — never remapping\n       // Stem's controls onto it (no false parity, no \"N/A\" shown as green). The\n@@ -5854,7 +5714,6 @@\n         pol(\"Engine\", `${engineMeta.version} · ${engineMeta.builtins.length} built-ins · ${engineMeta.bcVersion}`, \"ok\");\n         pol(\"Escape mode\", escapeMode, \"ok\");\n         pol(\"Trim whitespace\", standalone ? \"ON (ADR-0016 standalone-tag stripping)\" : \"off\", \"ok\");\n-        pol(\"ST4 mode\", st4Mode ? \"ON (forces {{! @truthiness: st4 }} gate)\" : \"off (pragma-driven)\", st4Mode ? \"warn\" : \"ok\");\n         pol(\n           \"Allow-list\",\n           allowList === null\n@@ -5942,17 +5801,6 @@\n       // how many iterations.\n       function renderCoverage(body) {\n         // Coverage counts conditional / iteration hits during the render.\n-        // ST4 Preview only transpiles the Stem source — no branches are\n-        // executed — so the counts from the last render don't apply here.\n-        if (outputView === \"st4\") {\n-          const section = makeEl(\"div\", { class: \"cv-section\" });\n-          section.append(makeEl(\"div\", { class: \"cv-empty\" },\n-            \"Coverage counts conditional / iteration hits during render. \" +\n-            \"ST4 Preview only transpiles the Stem source; no branches are \" +\n-            \"executed. Switch to Plain Text to see coverage.\"));\n-          body.append(section);\n-          return;\n-        }\n         const rows = lastCoverage;\n         const section = makeEl(\"div\", { class: \"cv-section\" });\n         section.append(makeEl(\"div\", { class: \"cv-head\" }, [\n@@ -6083,17 +5931,6 @@\n       function renderContext(body) {\n         const pane = makeEl(\"div\", { class: \"ctx-pane\" });\n         // Context Inspector reads render-time scope at a Stem source span.\n-        // ST4 Preview is a source-to-source transpile (no render), so there is\n-        // no scope to inspect — surface a placeholder instead of stale data\n-        // from the last Plain Text click.\n-        if (outputView === \"st4\") {\n-          pane.append(makeEl(\"div\", { class: \"ctx-hint\" },\n-            \"Context Inspector reads render-time scope. ST4 Preview transpiles \" +\n-            \"the Stem source to ST4 source without executing it, so no scope \" +\n-            \"exists at the clicked span. Switch to Plain Text to inspect render-time scope.\"));\n-          body.append(pane);\n-          return;\n-        }\n         if (!ctxTarget || !lastProgram) {\n           pane.append(makeEl(\"div\", { class: \"ctx-hint\" },\n             \"Click an expression in the Plain Text output to inspect its scope here. \" +\n@@ -6554,11 +6391,11 @@\n       // ── Render Config (config.yaml popover) ────────────────────────\n       // One home for the per-render options that were scattered across the\n       // output strip (escape, trim whitespace) plus two that had no GUI at all\n-      // (ST4 mode, ADR-0013 transformer allow-list). Edited as a small YAML\n+      // (the ADR-0013 transformer allow-list). Edited as a small YAML\n       // doc with inline descriptions. `escape-select` / `standalone-cb` stay\n       // the canonical backing state (share links, examples, restore all read\n       // them), so Save syncs them and the rest of the app is unchanged.\n-      const RENDER_CFG_DEFAULTS = { escape: \"\", trim: true, st4: false, allow: null };\n+      const RENDER_CFG_DEFAULTS = { escape: \"\", trim: true, allow: null };\n       const VALID_ESCAPE = [\"\", \"html\", \"xml\", \"json\", \"none\"];\n \n       // Serialise current state to the editable YAML (values inlined into a fixed\n@@ -6568,7 +6405,7 @@\n       function cfgLine(kv, comment) {\n         return kv + \" \".repeat(Math.max(1, 26 - kv.length)) + \"# \" + comment;\n       }\n-      function buildConfigYaml(v = { escape: escapeMode, trim: standalone, st4: st4Mode, allow: allowList }) {\n+      function buildConfigYaml(v = { escape: escapeMode, trim: standalone, allow: allowList }) {\n         const esc = v.escape ? v.escape : '\"\"';\n         // `null` = unconstrained (the default); a real array (even empty)\n         // locks the surface to that positive list. Mirrors the engine's\n@@ -6580,7 +6417,6 @@\n           \"\",\n           cfgLine(`escape: ${esc}`, 'html · xml · json · none · \"\" (as-compiled)'),\n           cfgLine(`trim_whitespace: ${v.trim}`, \"strip standalone tag-only lines (ADR-0016)\"),\n-          cfgLine(`st4_mode: ${v.st4}`, \"force {{! @truthiness: st4 }} gate for ST4 export\"),\n           \"\",\n           \"transformers:\",\n           cfgLine(`  allow: ${allow}`, \"ADR-0013 positive list — ~ = unconstrained (default); [eval] = only listed names render\"),\n@@ -6608,7 +6444,6 @@\n           return { ok: false, error: `escape: must be html, xml, json, none, or \"\" — got \"${esc}\"` };\n \n         const trim = cfg.trim_whitespace !== false; // default on\n-        const st4 = cfg.st4_mode === true;\n         // MinBars truthiness (ADR-029): default mustacheJs (true); only \"spec\" opts out.\n         const minbars = cfg.minbars && typeof cfg.minbars === \"object\" ? cfg.minbars : {};\n         const mustacheJs = minbars.truthiness !== \"spec\";\n@@ -6628,7 +6463,7 @@\n           if (unknown.length)\n             return { ok: false, error: `transformers.allow: unknown transformer${unknown.length > 1 ? \"s\" : \"\"} — ${unknown.join(\", \")}` };\n         }\n-        return { ok: true, value: { escape: esc, trim, st4, allow, mustacheJs, locale } };\n+        return { ok: true, value: { escape: esc, trim, allow, mustacheJs, locale } };\n       }\n \n       // Render config changed → refresh the explorer (the config.yaml row's","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Findex.html","sha":"b40c8cf2f8bea9846e9c850d960be0154e0d3bcb","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Foutput-view.mjs","changes":5,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Foutput-view.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":3,"filename":"lab/output-view.mjs","patch":"@@ -21,7 +21,6 @@ export const VIEW_TABS = [\n   { view: \"source\", label: \"Plain Text\" },\n   { view: \"rendered\", label: \"HTML Preview\" },\n   { view: \"markdown\", label: \"Markdown Preview\" },\n-  { view: \"st4\", label: \"ST4 Preview\", requires: [\"st4-modes\"] },\n   { view: \"data\", label: \"Render Data\" },\n   { view: \"bytecode\", label: \"Bytecode\", requires: [\"bytecode-wire\"] },\n   { view: \"compiled\", label: \"Compiled JS\", requires: [\"compile-js\"] },\n@@ -40,10 +39,10 @@ export function validView(view, features) {\n }\n \n // Whether a view paints into the read-only text editor (vs the preview iframe).\n-// The text views: Plain Text, Render Data, Bytecode, ST4, Compiled JS, Migrated.\n+// The text views: Plain Text, Render Data, Bytecode, Compiled JS, Migrated.\n export function viewKind(view) {\n   const text =\n-    view === \"source\" || view === \"data\" || view === \"bytecode\" || view === \"st4\" ||\n+    view === \"source\" || view === \"data\" || view === \"bytecode\" ||\n     view === \"compiled\" || view === \"migrated\";\n   return text ? \"text\" : \"preview\";\n }","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Foutput-view.mjs","sha":"776466ee38cf6cb7afcdb7fe0301bfb8e45cfccf","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Foutput-view.test.mjs","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Foutput-view.test.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":1,"filename":"lab/output-view.test.mjs","patch":"@@ -28,7 +28,7 @@ const NO_COMPILE = [\"surface-dialect\", \"core-dialect\"];\n // ── Layer 1: decision ───────────────────────────────────────────────────────\n \n test(\"viewKind: text views paint the editor, previews use the iframe\", () => {\n-  for (const v of [\"source\", \"data\", \"bytecode\", \"st4\", \"compiled\", \"migrated\"]) {\n+  for (const v of [\"source\", \"data\", \"bytecode\", \"compiled\", \"migrated\"]) {\n     assert.equal(viewKind(v), \"text\", `${v} should be a text view`);\n   }\n   for (const v of [\"rendered\", \"markdown\"]) {","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Foutput-view.test.mjs","sha":"17b89bf1cd2994d799609c4e4a58bc8d93e0d153","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Fplayground_utils.mjs","changes":410,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fplayground_utils.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":408,"filename":"lab/playground_utils.mjs","patch":"@@ -116,8 +116,8 @@ export function disassemble(program) {\n   // program references any). `INVOKE_PARTIAL <name>` in the main listing is a\n   // pointer into that registry, so dumping each body inline — under a\n   // `; partial: <name>` section header — gives the user the whole program in\n-  // one scrollable view, mirroring how the ST4 Preview lists the main template\n-  // and every partial as siblings in a single `.stg` group file. Partials are\n+  // one scrollable view, the main template and every partial as siblings.\n+  // Partials are\n   // resolved by name at render time, so the listing order doesn't have to\n   // match invocation order; sorting alphabetically gives a stable scan.\n   const partials = (program && program.partials) || {};\n@@ -234,412 +234,6 @@ function inspectLiteral(value) {\n   return String(value);\n }\n \n-// ── ST4 source-text transpiler (Phase 3, JS port for the playground) ─────\n-//\n-// Mirrors `Stem.ST4.Transpile` (BEAM) but operates on the AST-wire JSON\n-// the WASM `parseAst` returns rather than the BEAM tuple AST. The playground\n-// uses this to render the live \"ST4 Preview\" pane against the current\n-// template; it is **not** a security boundary and **not** a full\n-// implementation of every matrix row (each ❌ row renders as an inline\n-// `<! transpile-error: ... !>` ST4 comment so the preview is never empty,\n-// rather than throwing the way the BEAM walker does). For the canonical\n-// transpiler — used by `mix stem.st4.emit` and the parity gate — see\n-// `lib/stem/st4/transpile.ex`.\n-\n-// The Stem `{{! @truthiness: <mode> }}` pragma scanner from the source.\n-// Returns `\"st4\"` | `\"elixir\"` | `\"stem\"` | null per match. Mirrors the\n-// BEAM `Stem.Parser.pragmas/1` for the single key the transpiler reads.\n-export function scanTruthinessPragma(source) {\n-  let rest = (source || \"\").replace(/^\\s+/, \"\");\n-  while (rest.length > 0) {\n-    let open, close;\n-    if (rest.startsWith(\"{{!--\")) {\n-      open = \"{{!--\"; close = \"--}}\";\n-    } else if (rest.startsWith(\"{{!\")) {\n-      open = \"{{!\"; close = \"}}\";\n-    } else {\n-      return null;\n-    }\n-    rest = rest.slice(open.length);\n-    const idx = rest.indexOf(close);\n-    if (idx < 0) return null;\n-    const body = rest.slice(0, idx).trim();\n-    rest = rest.slice(idx + close.length).replace(/^\\s+/, \"\");\n-    const m = body.match(/^@truthiness:\\s*(\\w+)\\s*$/);\n-    if (m) return m[1];\n-  }\n-  return null;\n-}\n-\n-// Transpile a Stem AST (the JSON shape `parseAst` returns) to ST4 source\n-// text. Without partials it produces bare `.st` text; with `partials`\n-// (a `{name: [...nodes]}` map) it produces a `.stg` group file.\n-//\n-// `opts.truthiness` may be `\"st4\"` (clean emit) or any other string\n-// (emits a leading comment noting the gate requires `:st4`).\n-//\n-// When `opts.map === true`, returns `{output, segments}` instead of a\n-// bare string. Each segment maps a `[outBegin, outEnd)` character range\n-// in the emitted output to its originating Stem source span:\n-//\n-//   { outBegin, outEnd, file, start, end, kind }\n-//\n-// `file` is `\"main\"` for nodes from the main template and the partial\n-// name for nodes from a partial AST. `start`/`end` are byte offsets in\n-// that file's source (matching the AST's `src` shape). `kind` is one of\n-// `\"text\"`, `\"emit\"`, `\"if\"`, `\"each\"`, `\"partial\"`, `\"error\"`, or\n-// `\"header\"` so the playground can colour them per category. Error\n-// segments cover the `<! transpile-error: ... !>` comment and point at\n-// the offending node — Click-to-localise wires through these.\n-export function transpileSt4(nodes, opts = {}, partials = null) {\n-  const truthiness = opts.truthiness || \"stem\";\n-  const map = opts.map === true;\n-  const state = { out: \"\", segs: [], file: \"main\" };\n-\n-  if (truthiness !== \"st4\") {\n-    const msg =\n-      `<! Stem→ST4 transpile gate: source must declare {{! @truthiness: st4 }} (got \"${truthiness}\") !>`;\n-    const begin = state.out.length;\n-    state.out += msg + \"\\n\";\n-    state.segs.push({\n-      outBegin: begin,\n-      outEnd: state.out.length,\n-      file: \"main\",\n-      start: 0,\n-      end: 0,\n-      kind: \"error\",\n-    });\n-  }\n-\n-  if (partials && Object.keys(partials).length > 0) {\n-    pushHeader(state, 'delimiters \"<\", \">\"\\n\\n');\n-    emitGroupTemplate(state, \"main\", nodes);\n-    for (const name of Object.keys(partials).sort()) {\n-      pushHeader(state, \"\\n\");\n-      const saved = state.file;\n-      state.file = name;\n-      emitGroupTemplate(state, name, partials[name]);\n-      state.file = saved;\n-    }\n-  } else {\n-    emitNodes(state, nodes, defaultCtx());\n-  }\n-\n-  if (map) return { output: state.out, segments: state.segs };\n-  return state.out;\n-}\n-\n-function defaultCtx() {\n-  return { inEach: false };\n-}\n-\n-function pushHeader(state, text) {\n-  const begin = state.out.length;\n-  state.out += text;\n-  state.segs.push({\n-    outBegin: begin,\n-    outEnd: state.out.length,\n-    file: state.file,\n-    start: 0,\n-    end: 0,\n-    kind: \"header\",\n-  });\n-}\n-\n-function pushText(state, text, src, kind) {\n-  if (!text) return;\n-  const begin = state.out.length;\n-  state.out += text;\n-  state.segs.push({\n-    outBegin: begin,\n-    outEnd: state.out.length,\n-    file: state.file,\n-    start: src && typeof src.start === \"number\" ? src.start : 0,\n-    end: src && typeof src.end === \"number\" ? src.end : 0,\n-    kind: kind || \"emit\",\n-  });\n-}\n-\n-function pushError(state, message, src) {\n-  pushText(state, `<! transpile-error: ${message} !>`, src, \"error\");\n-}\n-\n-function emitGroupTemplate(state, name, nodes) {\n-  const args = deriveArgs(nodes).join(\", \");\n-  pushHeader(state, `${name}(${args}) ::= <<\\n`);\n-  emitNodes(state, nodes, defaultCtx());\n-  pushHeader(state, `\\n>>\\n`);\n-}\n-\n-function emitNodes(state, nodes, ctx) {\n-  if (!Array.isArray(nodes)) return;\n-  for (const node of nodes) emitNode(state, node, ctx);\n-}\n-\n-function emitNode(state, node, ctx) {\n-  if (!node) return;\n-  switch (node.t) {\n-    case \"text\":\n-      pushText(state, node.text || \"\", node.src, \"text\");\n-      return;\n-    case \"emit\":\n-      emitExprNode(state, node, ctx);\n-      return;\n-    case \"if\":\n-      emitIfNode(state, node, ctx, false);\n-      return;\n-    case \"unless\":\n-      emitIfNode(state, node, ctx, true);\n-      return;\n-    case \"each\":\n-      emitEachNode(state, node, ctx);\n-      return;\n-    case \"with\":\n-      pushError(state, \"{{#with}} is not supported by ST4\", node.src);\n-      return;\n-    case \"partial\":\n-      emitPartialNode(state, node, ctx);\n-      return;\n-    case \"yield\":\n-      pushError(state, \"{{yield}} is not supported by ST4\", node.src);\n-      return;\n-    case \"region\":\n-      return; // regions are inlined at yield sites; skip in the preview\n-    case \"partial_scope\":\n-      pushError(\n-        state,\n-        \"partial with rebinding body is not supported by ST4\",\n-        node.src,\n-      );\n-      return;\n-    default:\n-      return;\n-  }\n-}\n-\n-function emitIfNode(state, node, ctx, negated) {\n-  const condText = emitValue(node.cond, ctx);\n-  const body = node.then || [];\n-  const elseBody = node.else || [];\n-  const prefix = negated ? \"<if(!\" : \"<if(\";\n-  pushText(state, `${prefix}${condText})>`, node.src, \"if\");\n-  emitNodes(state, body, ctx);\n-  if (elseBody.length > 0) {\n-    pushText(state, \"<else>\", node.src, \"if\");\n-    emitNodes(state, elseBody, ctx);\n-  }\n-  pushText(state, \"<endif>\", node.src, \"if\");\n-}\n-\n-function emitEachNode(state, node, ctx) {\n-  const body = node.body || [];\n-  const elseBody = node.else || [];\n-\n-  if (elseBody.length > 0) {\n-    pushError(\n-      state,\n-      \"{{else}} on {{#each}} is not supported by ST4 — wrap in {{#if}}…{{else}}…{{/if}}\",\n-      node.src,\n-    );\n-    return;\n-  }\n-  const list = emitValue(node.subject, ctx);\n-\n-  pushText(state, `<${list}:{ it | `, node.src, \"each\");\n-  emitNodes(state, body, { inEach: true });\n-  pushText(state, \"}>\", node.src, \"each\");\n-}\n-\n-function emitPartialNode(state, node, ctx) {\n-  const name = node.name;\n-  const hash = node.hash || {};\n-  const ctxArg = node.context;\n-  const hashKeys = Object.keys(hash);\n-  let text;\n-\n-  if (!ctxArg && hashKeys.length === 0) text = `<${name}()>`;\n-  else if (ctxArg && hashKeys.length === 0) text = `<${name}(${emitValue(ctxArg, ctx)})>`;\n-  else if (!ctxArg && hashKeys.length > 0) {\n-    const args = hashKeys.map((k) => `${k}=${emitValue(hash[k], ctx)}`).join(\", \");\n-    text = `<${name}(${args})>`;\n-  } else {\n-    pushError(state, \"{{> name ctx k=v}} is not supported by ST4\", node.src);\n-    return;\n-  }\n-  pushText(state, text, node.src, \"partial\");\n-}\n-\n-function emitExprNode(state, node, ctx) {\n-  const expr = node.expr;\n-  const escape = node.escape || \"html\";\n-  const src = node.src;\n-\n-  if (expr && expr.t === \"lit\") {\n-    // Top-level literal expressions emit as inline text (matrix row A).\n-    const v = expr.value;\n-    if (v === null || v === undefined) return;\n-    pushText(state, String(v), src, \"emit\");\n-    return;\n-  }\n-  if (expr && expr.t === \"call\") {\n-    emitTransformerCall(state, expr, escape, src, ctx);\n-    return;\n-  }\n-  const base = emitValue(expr, ctx);\n-  pushText(state, wrapEscape(base, escape), src, \"emit\");\n-}\n-\n-function wrapEscape(base, escape) {\n-  switch (escape) {\n-    case \"none\":\n-      return `<${base}>`;\n-    case \"html\":\n-    case \"default\":\n-      return `<${base}; format=\"stem.escape.html\">`;\n-    case \"json\":\n-      return `<${base}; format=\"stem.escape.json\">`;\n-    case \"uri\":\n-      return `<${base}; format=\"stem.escape.uri\">`;\n-    default:\n-      return `<${base}>`;\n-  }\n-}\n-\n-const ST4_TRANSFORMER_MAP = {\n-  upcase: { option: \"format\", value: \"upper\" },\n-  downcase: { option: \"format\", value: \"lower\" },\n-};\n-\n-function emitTransformerCall(state, call, _escape, src, ctx) {\n-  const name = call.name;\n-  const args = (call.args || []).filter((a) => a.kind === \"positional\").map((a) => a.value);\n-\n-  const mapped = ST4_TRANSFORMER_MAP[name];\n-  if (mapped && args.length === 1) {\n-    const base = emitAttrBase(args[0], ctx);\n-    if (base.error) {\n-      pushError(state, base.error, src);\n-      return;\n-    }\n-    pushText(state, `<${base.value}; ${mapped.option}=\"${mapped.value}\">`, src, \"emit\");\n-    return;\n-  }\n-  if (name === \"default\" && args.length === 2 && args[1].t === \"lit\" && typeof args[1].value === \"string\") {\n-    const base = emitAttrBase(args[0], ctx);\n-    if (base.error) {\n-      pushError(state, base.error, src);\n-      return;\n-    }\n-    pushText(state, `<${base.value}; null=\"${escapeSt4String(args[1].value)}\">`, src, \"emit\");\n-    return;\n-  }\n-  if (name === \"join\" && args.length === 2 && args[1].t === \"lit\" && typeof args[1].value === \"string\") {\n-    const base = emitAttrBase(args[0], ctx);\n-    if (base.error) {\n-      pushError(state, base.error, src);\n-      return;\n-    }\n-    pushText(state, `<${base.value}; separator=\"${escapeSt4String(args[1].value)}\">`, src, \"emit\");\n-    return;\n-  }\n-  pushError(\n-    state,\n-    `transformer \\`${name}\\` has no ST4 equivalent — use upcase / downcase / default / join`,\n-    src,\n-  );\n-}\n-\n-function emitAttrBase(expr, ctx) {\n-  if (!expr) return { error: \"missing transformer subject\" };\n-  if (expr.t === \"identifier\") return { value: expr.name };\n-  if (expr.t === \"path\") {\n-    return { value: (expr.segments || []).join(\".\") };\n-  }\n-  if (expr.t === \"context\" && expr.kind === \"this\" && ctx.inEach) {\n-    const segments = expr.path || [];\n-    return { value: [\"it\"].concat(segments).join(\".\") };\n-  }\n-  if (expr.t === \"call\") return { error: \"format options do not compose in ST4\" };\n-  return { error: `transformer subject must be a plain attribute reference` };\n-}\n-\n-function emitValue(expr, ctx) {\n-  if (!expr) return \"\";\n-  switch (expr.t) {\n-    case \"lit\":\n-      return expr.value === null || expr.value === undefined ? \"\" : String(expr.value);\n-    case \"identifier\":\n-      return expr.name;\n-    case \"path\":\n-      return (expr.segments || []).join(\".\");\n-    case \"index\":\n-      return ctx.inEach ? \"i0\" : \"\";\n-    case \"index1\":\n-      return ctx.inEach ? \"i\" : \"\";\n-    case \"key\":\n-    case \"first\":\n-    case \"last\":\n-      return `(transpile-error: @${expr.t} has no ST4 analogue)`;\n-    case \"context\": {\n-      const segments = expr.path || [];\n-      if (expr.kind === \"this\") {\n-        if (ctx.inEach) return [\"it\"].concat(segments).join(\".\");\n-        return segments.length === 0 ? \"this\" : segments.join(\".\");\n-      }\n-      if (expr.kind === \"root\") return \"(transpile-error: @root has no ST4 walk)\";\n-      if (expr.kind === \"parent\") return \"(transpile-error: @parent has no ST4 walk)\";\n-      return segments.join(\".\");\n-    }\n-    case \"call\":\n-      return `(transpile-error: nested transformer ${expr.name})`;\n-    default:\n-      return \"\";\n-  }\n-}\n-\n-function st4Comment(msg) {\n-  return `<! ${msg} !>`;\n-}\n-\n-function escapeSt4String(s) {\n-  return String(s).replace(/\\\\/g, \"\\\\\\\\\").replace(/\"/g, '\\\\\"');\n-}\n-\n-// Conservative argument-list derivation for `.stg` group templates: the\n-// top-level identifier and attribute names the body references. Mirrors\n-// `Stem.ST4.Transpile.derive_args/1` (BEAM).\n-function deriveArgs(nodes) {\n-  const set = new Set();\n-  function walkNode(node) {\n-    if (!node) return;\n-    switch (node.t) {\n-      case \"emit\": walkExpr(node.expr); break;\n-      case \"if\":\n-        walkExpr(node.cond);\n-        (node.then || []).forEach(walkNode);\n-        (node.else || []).forEach(walkNode);\n-        break;\n-      case \"each\":\n-        walkExpr(node.subject);\n-        (node.body || []).forEach(walkNode);\n-        break;\n-      case \"partial\":\n-        if (node.context) walkExpr(node.context);\n-        Object.values(node.hash || {}).forEach(walkExpr);\n-        break;\n-      default: break;\n-    }\n-  }\n-  function walkExpr(e) {\n-    if (!e) return;\n-    if (e.t === \"identifier\") set.add(e.name);\n-    else if (e.t === \"path\" && (e.segments || []).length > 0) set.add(e.segments[0]);\n-    else if (e.t === \"call\") (e.args || []).forEach((a) => walkExpr(a.value));\n-  }\n-  (nodes || []).forEach(walkNode);\n-  return Array.from(set).sort();\n-}\n-\n // ── Share-link state codec ────────────────────────────────────────────────\n // The URL fragment carries the whole workspace. We raw-DEFLATE the JSON via the\n // browser-native CompressionStream (no dependency — brotli isn't available for","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Fplayground_utils.mjs","sha":"b21a88a7811b7254846cf8064dd48bb9bb03bc85","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Fplayground_utils.test.mjs","changes":193,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fplayground_utils.test.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":189,"filename":"lab/playground_utils.test.mjs","patch":"@@ -22,9 +22,7 @@ import {\n   mergeDataOverlays,\n   stemTruthy,\n   partialNameAt,\n-  scanTruthinessPragma,\n   tabVisibleUnder,\n-  transpileSt4,\n   validateOverlayName,\n   vendoredWorkspace,\n   vendoredVerdict,\n@@ -38,7 +36,7 @@ import {\n const STEM_VECTOR = [\n   \"escape-modes\", \"eval-opt-in\", \"partials\", \"source-map\", \"standalone\",\n   \"context-inspect\", \"bytecode-wire\", \"catalog\", \"used-transformers\",\n-  \"required-assigns\", \"partial-graph\", \"st4-modes\", \"stem-allow-list\",\n+  \"required-assigns\", \"partial-graph\", \"stem-allow-list\",\n ];\n const HBS_VECTOR = [\n   \"partials\", \"catalog\", \"used-transformers:approximate\",\n@@ -74,15 +72,14 @@ test(\"tabVisibleUnder gates panels per the engine vector\", () => {\n   // inert under Stem (the Phase 1 proof).\n   const PANEL_REQUIRES = [\n     [\"catalog\"], [\"required-assigns\"], [\"standalone\"], [\"partial-graph\"],\n-    [\"stem-allow-list\"], [\"st4-modes\"], [\"bytecode-wire\"],\n+    [\"stem-allow-list\"], [\"bytecode-wire\"],\n   ];\n   for (const requires of PANEL_REQUIRES) {\n     assert.equal(tabVisibleUnder(STEM_VECTOR, requires), true, requires.join());\n   }\n   // Under the thin Handlebars vector the Stem-only panels drop out, while the\n   // shared ones (catalog, partial-graph at approximate fidelity) stay.\n   assert.equal(tabVisibleUnder(HBS_VECTOR, [\"bytecode-wire\"]), false);\n-  assert.equal(tabVisibleUnder(HBS_VECTOR, [\"st4-modes\"]), false);\n   assert.equal(tabVisibleUnder(HBS_VECTOR, [\"stem-allow-list\"]), false);\n   assert.equal(tabVisibleUnder(HBS_VECTOR, [\"standalone\"]), false);\n   assert.equal(tabVisibleUnder(HBS_VECTOR, [\"catalog\"]), true);\n@@ -711,8 +708,8 @@ test(\"disassemble dumps each compiled partial body after the main listing\", () =\n   // each compiled body alongside `instructions` as `partials: {name: [...]}`.\n   // The playground's Bytecode view dumps them inline so the user can see what\n   // each `INVOKE_PARTIAL` resolves to without leaving the disasm — sibling\n-  // sections similar to how the ST4 Preview emits a `.stg` group file with\n-  // one entry per partial. Names are listed alphabetically for a stable scan.\n+  // sections, one entry per partial. Names are listed alphabetically for a\n+  // stable scan.\n   const program = {\n     version: \"stem-bc/v1\",\n     instructions: [\n@@ -744,188 +741,6 @@ test(\"disassemble omits the partials section when none are referenced\", () => {\n   assert.equal(out, '; stem-bc/v1\\nEMIT_TEXT \"hi\"\\n');\n });\n \n-// ── scanTruthinessPragma + transpileSt4 (playground live preview) ────────\n-\n-test(\"scanTruthinessPragma reads {{! @truthiness: st4 }} at the top\", () => {\n-  assert.equal(scanTruthinessPragma(\"{{! @truthiness: st4 }}\\nhi\"), \"st4\");\n-  assert.equal(scanTruthinessPragma(\"{{! @truthiness: elixir }}foo\"), \"elixir\");\n-  assert.equal(scanTruthinessPragma(\"{{!-- @truthiness: stem --}}\"), \"stem\");\n-});\n-\n-test(\"scanTruthinessPragma returns null when absent or not at the top\", () => {\n-  assert.equal(scanTruthinessPragma(\"Hello {{name}}!\"), null);\n-  assert.equal(scanTruthinessPragma(\"hello {{! @truthiness: st4 }}\"), null);\n-  assert.equal(scanTruthinessPragma(\"\"), null);\n-});\n-\n-test(\"scanTruthinessPragma does not match plain comments without @\", () => {\n-  assert.equal(scanTruthinessPragma(\"{{! truthiness: st4 }}<x>\"), null);\n-});\n-\n-test(\"transpileSt4 emits plain text verbatim\", () => {\n-  const nodes = [{ t: \"text\", text: \"Hello, world!\" }];\n-  assert.equal(transpileSt4(nodes, { truthiness: \"st4\" }), \"Hello, world!\");\n-});\n-\n-test(\"transpileSt4 emits {{{name}}} as <name>\", () => {\n-  const nodes = [\n-    { t: \"emit\", expr: { t: \"identifier\", name: \"name\" }, escape: \"none\" },\n-  ];\n-  assert.equal(transpileSt4(nodes, { truthiness: \"st4\" }), \"<name>\");\n-});\n-\n-test(\"transpileSt4 emits {{name}} as <name; format=stem.escape.html>\", () => {\n-  const nodes = [\n-    { t: \"emit\", expr: { t: \"identifier\", name: \"name\" }, escape: \"html\" },\n-  ];\n-  assert.equal(\n-    transpileSt4(nodes, { truthiness: \"st4\" }),\n-    '<name; format=\"stem.escape.html\">',\n-  );\n-});\n-\n-test(\"transpileSt4 emits {{#if cond}}A{{else}}B{{/if}} → <if(cond)>A<else>B<endif>\", () => {\n-  const nodes = [\n-    {\n-      t: \"if\",\n-      cond: { t: \"identifier\", name: \"cond\" },\n-      then: [{ t: \"text\", text: \"A\" }],\n-      else: [{ t: \"text\", text: \"B\" }],\n-    },\n-  ];\n-  assert.equal(\n-    transpileSt4(nodes, { truthiness: \"st4\" }),\n-    \"<if(cond)>A<else>B<endif>\",\n-  );\n-});\n-\n-test(\"transpileSt4 emits {{#each items}}-{{{@this}}}{{/each}} → <items:{ it | -<it>}>\", () => {\n-  const nodes = [\n-    {\n-      t: \"each\",\n-      subject: { t: \"identifier\", name: \"items\" },\n-      body: [\n-        { t: \"text\", text: \"-\" },\n-        { t: \"emit\", expr: { t: \"context\", kind: \"this\", path: [] }, escape: \"none\" },\n-      ],\n-      else: [],\n-    },\n-  ];\n-  assert.equal(\n-    transpileSt4(nodes, { truthiness: \"st4\" }),\n-    \"<items:{ it | -<it>}>\",\n-  );\n-});\n-\n-test(\"transpileSt4 emits {{upcase name}} → <name; format=upper>\", () => {\n-  const nodes = [\n-    {\n-      t: \"emit\",\n-      expr: {\n-        t: \"call\",\n-        name: \"upcase\",\n-        args: [{ kind: \"positional\", value: { t: \"identifier\", name: \"name\" } }],\n-      },\n-      escape: \"none\",\n-    },\n-  ];\n-  assert.equal(\n-    transpileSt4(nodes, { truthiness: \"st4\" }),\n-    '<name; format=\"upper\">',\n-  );\n-});\n-\n-test(\"transpileSt4 flags an incompatible transformer as <! transpile-error: ... !>\", () => {\n-  const nodes = [\n-    {\n-      t: \"emit\",\n-      expr: {\n-        t: \"call\",\n-        name: \"filter\",\n-        args: [{ kind: \"positional\", value: { t: \"identifier\", name: \"items\" } }],\n-      },\n-      escape: \"none\",\n-    },\n-  ];\n-  const out = transpileSt4(nodes, { truthiness: \"st4\" });\n-  assert.match(out, /<! transpile-error: transformer `filter`/);\n-});\n-\n-test(\"transpileSt4 flags missing truthiness pragma with a leading comment\", () => {\n-  const nodes = [{ t: \"text\", text: \"hi\" }];\n-  const out = transpileSt4(nodes, { truthiness: \"stem\" });\n-  assert.match(out, /^<! Stem→ST4 transpile gate: source must declare/);\n-  assert.match(out, /hi/);\n-});\n-\n-test(\"transpileSt4 with {map:true} returns segments mapping output ranges back to source spans\", () => {\n-  // Stem source: `{{name}}{{filter items}}` (paths/spans are illustrative).\n-  const nodes = [\n-    {\n-      t: \"emit\",\n-      expr: { t: \"identifier\", name: \"name\" },\n-      escape: \"none\",\n-      src: { start: 0, end: 8 },\n-    },\n-    {\n-      t: \"emit\",\n-      expr: {\n-        t: \"call\",\n-        name: \"filter\",\n-        args: [{ kind: \"positional\", value: { t: \"identifier\", name: \"items\" } }],\n-      },\n-      escape: \"none\",\n-      src: { start: 8, end: 24 },\n-    },\n-  ];\n-  const result = transpileSt4(nodes, { truthiness: \"st4\", map: true });\n-\n-  assert.equal(typeof result.output, \"string\");\n-  assert.ok(Array.isArray(result.segments));\n-\n-  // Two segments — one emit + one error — each covering its own\n-  // contiguous range of the output and carrying its original source span.\n-  assert.equal(result.segments.length, 2);\n-\n-  const [s1, s2] = result.segments;\n-  assert.equal(s1.kind, \"emit\");\n-  assert.equal(s1.start, 0);\n-  assert.equal(s1.end, 8);\n-  assert.equal(result.output.slice(s1.outBegin, s1.outEnd), \"<name>\");\n-\n-  assert.equal(s2.kind, \"error\");\n-  assert.equal(s2.start, 8);\n-  assert.equal(s2.end, 24);\n-  assert.match(result.output.slice(s2.outBegin, s2.outEnd), /^<! transpile-error: /);\n-});\n-\n-test(\"transpileSt4 map mode tags partials with file=<partial-name>\", () => {\n-  const main = [{ t: \"partial\", name: \"row\", context: null, hash: {}, src: { start: 0, end: 9 } }];\n-  const partials = {\n-    row: [{ t: \"text\", text: \"- item\", src: { start: 5, end: 11 } }],\n-  };\n-  const result = transpileSt4(main, { truthiness: \"st4\", map: true }, partials);\n-\n-  const fileTags = new Set(result.segments.map((s) => s.file));\n-  assert.ok(fileTags.has(\"main\"));\n-  assert.ok(fileTags.has(\"row\"));\n-\n-  const rowSeg = result.segments.find((s) => s.file === \"row\" && s.kind === \"text\");\n-  assert.equal(rowSeg.start, 5);\n-  assert.equal(rowSeg.end, 11);\n-});\n-\n-test(\"transpileSt4 wraps partials into a .stg group file\", () => {\n-  const main = [{ t: \"partial\", name: \"row\", context: null, hash: {} }];\n-  const partials = { row: [{ t: \"text\", text: \"- item\" }] };\n-  const out = transpileSt4(main, { truthiness: \"st4\" }, partials);\n-  assert.match(out, /^delimiters \"<\", \">\"/);\n-  assert.match(out, /main\\(\\) ::= <</);\n-  assert.match(out, /<row\\(\\)>/);\n-  assert.match(out, /row\\(\\) ::= <</);\n-  assert.match(out, /- item/);\n-});\n-\n test(\"vendoredWorkspace maps a fixture to a Lab workspace payload\", () => {\n   const fx = {\n     id: \"mustache/partials/context\",","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Fplayground_utils.test.mjs","sha":"56c86afd95531af3642ee24aa900dbcda65d2aeb","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Fsmoke.mjs","changes":1,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fsmoke.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":1,"filename":"lab/smoke.mjs","patch":"@@ -76,7 +76,6 @@ const { compile, render, parseAst, usedTransformers, requiredAssigns, partialGra\n // thinner adapter exists.\n const features = engineInfo().features;\n const PANEL_REQUIRES = {\n-  \"view:ST4 Preview\": [\"st4-modes\"],\n   \"view:Bytecode\": [\"bytecode-wire\"],\n   \"dock:Transformers\": [\"catalog\"],\n   \"dock:Data Access\": [\"required-assigns\"],","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Fsmoke.mjs","sha":"4485bf9f9fdb960a7e403fbe9b708ee39249d46b","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Fstem.mjs","changes":1,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fstem.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":1,"filename":"lab/stem.mjs","patch":"@@ -50,7 +50,6 @@ const STEM_FEATURES = [\n   \"used-transformers\",\n   \"required-assigns\",\n   \"partial-graph\",\n-  \"st4-modes\",\n   \"stem-allow-list\",\n ];\n ","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Fstem.mjs","sha":"874fa275371cbec24fb91b6bb836e6b9ff328453","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/flatbars/blob/dded7c254b755623f35366e395714da9f8a81198/lab%2Ftest%2Fbrowser_smoke.mjs","changes":53,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Ftest%2Fbrowser_smoke.mjs?ref=dded7c254b755623f35366e395714da9f8a81198","deletions":53,"filename":"lab/test/browser_smoke.mjs","patch":"@@ -260,58 +260,6 @@ try {\n   // Capture an overlays screenshot for the docs.\n   await page.screenshot({ path: join(SCREENSHOT_DIR, \"playground-overlays.png\") });\n \n-  // Output-view check — the \"ST4 Preview\" tab sits to the right of Markdown\n-  // Preview. The Stem→ST source transpiler is Phase 3 of the export-only\n-  // interop effort, so for now this pane shows a placeholder explaining\n-  // what will land here. Confirm the tab exists, clicks paint the\n-  // placeholder, and the tab order is markdown → ST4 Preview → data.\n-  const st4Clicked = await page.evaluate(() => {\n-    const tabs = Array.from(document.querySelectorAll(\"#view-tabs .view-tab\"));\n-    const tab = tabs.find((b) => (b.textContent || \"\").trim() === \"ST4 Preview\");\n-    if (!tab) return false;\n-    tab.click();\n-    return true;\n-  });\n-  check(\"ST4 Preview output-view tab exists and was clicked\", st4Clicked);\n-  if (st4Clicked) {\n-    await new Promise((r) => setTimeout(r, 200));\n-    const st4Body = await page.evaluate(() => {\n-      const cm = document.querySelector(\"#output-text .cm-content\");\n-      return cm ? cm.textContent || \"\" : \"\";\n-    });\n-    // The live transpiler emits actual ST4 source for the current Cheat\n-    // Sheet template. The fixture template is rich enough that the output\n-    // always contains at least one ST4 attribute reference (`<name>`) or\n-    // a control-form keyword (`<if(...)>`). Fall back to the\n-    // empty-AST/loading placeholder text if no AST has parsed yet.\n-    check(\n-      \"ST4 Preview shows live transpiler output (or its empty-AST hint)\",\n-      /<\\w/.test(st4Body) || /Edit the template/.test(st4Body),\n-      st4Body.slice(0, 80),\n-    );\n-    const tabOrder = await page.evaluate(() =>\n-      Array.from(document.querySelectorAll(\"#view-tabs .view-tab\")).map((b) =>\n-        (b.textContent || \"\").trim(),\n-      ),\n-    );\n-    const idxMarkdown = tabOrder.indexOf(\"Markdown Preview\");\n-    const idxSt4 = tabOrder.indexOf(\"ST4 Preview\");\n-    check(\n-      \"ST4 Preview sits immediately to the right of Markdown Preview\",\n-      idxSt4 === idxMarkdown + 1 && idxMarkdown >= 0,\n-      `tabs: ${tabOrder.join(\", \")}`,\n-    );\n-    await page.screenshot({ path: join(SCREENSHOT_DIR, \"playground-st4-preview.png\") });\n-    console.log(`  saved ${join(SCREENSHOT_DIR, \"playground-st4-preview.png\")}`);\n-    // Restore the source view so the overview screenshot stays canonical.\n-    await page.evaluate(() => {\n-      const tabs = Array.from(document.querySelectorAll(\"#view-tabs .view-tab\"));\n-      const tab = tabs.find((b) => (b.textContent || \"\").trim() === \"Plain Text\");\n-      tab?.click();\n-    });\n-    await new Promise((r) => setTimeout(r, 200));\n-  }\n-\n   // Renaming an *existing* file is keyboard-driven now (no double-click): focus\n   // its Explorer row and press the rename key (F2 on Windows/Linux, Enter on\n   // macOS; F2 also works on macOS). Assert F2 on the focused overlay row opens\n@@ -427,7 +375,6 @@ try {\n     capturedAt: new Date().toISOString(),\n     panels: PANELS.map((p) => ({ id: p.id, label: p.label, file: `screenshots/dock-${p.id}.png` })),\n     overview: \"screenshots/playground-overview.png\",\n-    outputViews: { st4: \"screenshots/playground-st4-preview.png\" },\n   };\n   await writeFile(\n     join(SCREENSHOT_DIR, \"manifest.json\"),","raw_url":"https://github.com/wstein/flatbars/raw/dded7c254b755623f35366e395714da9f8a81198/lab%2Ftest%2Fbrowser_smoke.mjs","sha":"23f075bc2c10ef9f9f8485e5512b8eba59ef3a0b","status":"modified"}]}