{"files":[{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Findex.html","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=0745fe95a5b2d000b98e61f20f72e9f428c6565c","deletions":1,"filename":"lab/index.html","patch":"@@ -1656,7 +1656,7 @@\n       // Cache-bust query (like playground_utils.mjs): bump on any change to the\n       // engine adapters so a reload never pairs a fresh index.html / example\n       // file with a stale cached adapter module.\n-      import { createRenderer } from \"./renderer.mjs?v=2\";\n+      import { createRenderer } from \"./renderer.mjs?v=3\";\n       // Engine-derived INNER-TOKEN highlighting (ADR-014 amendment): the shared\n       // painter + the lexer's own tokenizer, in place of a hand-written tag regex.\n       import { flatbarsHighlight } from \"./cm-flatbars.mjs?v=7\";","raw_url":"https://github.com/wstein/flatbars/raw/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Findex.html","sha":"2af3d5cff025e50ac988f4e35c37ec02873b32b1","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/flatbars/blob/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Frenderer.mjs","changes":43,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Frenderer.mjs?ref=0745fe95a5b2d000b98e61f20f72e9f428c6565c","deletions":6,"filename":"lab/renderer.mjs","patch":"@@ -304,14 +304,45 @@ function flatbarsRenderer(activeDialect, _opts) {\n   }\n \n   // required-assigns (EXACT): the root of every `{t:\"path\"}`. Helpers are explicit\n-  // calls and block params are `(param)` calls, so neither leaks in.\n+  // calls and ClassicBars block params lower to calls, so neither leaks in. The\n+  // MaxBars binding surfaces, however, carry their bound name into the lowered AST\n+  // AS A PATH: `{% for x in y %}` is a `for` node with raw [x, \"in\", y] path args\n+  // (`each … in` / `with … as` lower correctly and don't), and `{% let n = v %}`\n+  // names `n` in a @hash and references it as a path in the body. So we collect the\n+  // for/let bound names — and skip the for-header `in` keyword — and exclude them,\n+  // leaving only the iterable / value / real reads. (Other dialects have no\n+  // for/let nodes, so `bound` stays empty and behaviour is unchanged.)\n   function requiredAssigns(program) {\n+    const nodes = nodesOf(program);\n+    const bound = new Set();\n+    walk(nodes, (node) => {\n+      if (node.t === \"for\" && node.args && node.args[0]) {\n+        const b = node.args[0].value;\n+        if (b && b.t === \"path\" && b.segments && b.segments.length) bound.add(b.segments[0]);\n+      } else if (node.t === \"let\" && Array.isArray(node.args)) {\n+        for (const a of node.args) {\n+          const h = a.value;\n+          if (h && h.t === \"call\" && h.name === \"@hash\" && h.args && h.args[0]) {\n+            const nameLit = h.args[0].value;\n+            if (nameLit && nameLit.t === \"lit\") bound.add(String(nameLit.value));\n+          }\n+        }\n+      }\n+    });\n     const out = new Set();\n-    walk(nodesOf(program), (node) =>\n-      eachExpr(node, (e) => walkExpr(e, (x) => {\n-        if (x.t === \"path\" && x.segments && x.segments.length) out.add(x.segments[0]);\n-      })),\n-    );\n+    const collect = (e) => walkExpr(e, (x) => {\n+      if (x.t === \"path\" && x.segments && x.segments.length && !bound.has(x.segments[0])) out.add(x.segments[0]);\n+    });\n+    walk(nodes, (node) => {\n+      if (node.t === \"for\" && Array.isArray(node.args)) {\n+        // Skip the binding (args[0]) and the `in` keyword (args[1]); only the\n+        // iterable (args[2..]) reads data. Body reads are visited via walk() and\n+        // filtered by `bound`.\n+        for (const a of node.args.slice(2)) collect(a.value);\n+      } else {\n+        eachExpr(node, collect);\n+      }\n+    });\n     return [...out].sort();\n   }\n   // used-transformers: block-helper node types + every `{t:\"call\"}` head.","raw_url":"https://github.com/wstein/flatbars/raw/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Frenderer.mjs","sha":"c93f32355cbde482b7f5a34e80a2e39d72104fd9","status":"modified"},{"additions":18,"blob_url":"https://github.com/wstein/flatbars/blob/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Frenderer.test.mjs","changes":18,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Frenderer.test.mjs?ref=0745fe95a5b2d000b98e61f20f72e9f428c6565c","deletions":0,"filename":"lab/renderer.test.mjs","patch":"@@ -625,3 +625,21 @@ test(\"MinBars compileToJs seeds the chosen truthiness rule\", async () => {\n   assert.ok(compat.ok, compat.error);\n   assert.match(compat.value, /truthyHandlebars/);\n });\n+\n+test(\"requiredAssigns excludes MaxBars for/let bound names + the for-header `in` keyword\", async () => {\n+  const r = await createRenderer(\"maxbars\");\n+  const ra = (src) => r.requiredAssigns({ dialect: \"maxbars\", source: src, partials: {} });\n+  // `{% for x in y %}` parses to a for node with raw [x, \"in\", y] path args, and\n+  // body refs to x are paths — none of x/in/(nested bindings) are data assigns.\n+  assert.deepEqual(ra(\"{% for x in items %}{{x}}{% endfor %}\"), [\"items\"]);\n+  assert.deepEqual(\n+    ra(\"{% for node in outline %}{{node.name}}{% for child in node.children %}{{child}}{% endfor %}{% endfor %}\"),\n+    [\"outline\"], // not node/child/in — those are loop bindings + the keyword\n+  );\n+  // `{% let n = v %}` names n in a @hash and references it as a path in the body.\n+  assert.deepEqual(ra(\"{% let n = total %}{{n}}{% endlet %}\"), [\"total\"]);\n+  assert.deepEqual(ra(\"{% let n = 5 %}{{n}}{% endlet %}\"), []);\n+  // already-correct forms stay correct (engine lowers each/with bindings to calls)\n+  assert.deepEqual(ra(\"{% each x in items %}{{x}}{% endeach %}\"), [\"items\"]);\n+  assert.deepEqual(ra(\"{{user.name}} {{title}}\"), [\"title\", \"user\"]);\n+});","raw_url":"https://github.com/wstein/flatbars/raw/0745fe95a5b2d000b98e61f20f72e9f428c6565c/lab%2Frenderer.test.mjs","sha":"cc8bee5c2e8e419c7540606041f595af72957816","status":"modified"}]}