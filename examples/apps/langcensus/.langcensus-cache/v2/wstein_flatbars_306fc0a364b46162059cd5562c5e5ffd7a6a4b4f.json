{"files":[{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fnav.adoc","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fnav.adoc?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":0,"filename":"docs/modules/ROOT/nav.adoc","patch":"@@ -29,6 +29,8 @@\n * xref:adr-0024-let-and-dict-literal.adoc[ADR-024: `let` & dict literal (proposed)]\n * xref:adr-0025-list-literal.adoc[ADR-025: list literal & `list` helper (proposed)]\n * xref:adr-0026-editor-capability-matrix.adoc[ADR-026: editor capability matrix]\n+* xref:adr-0027-cross-template-scope-graph.adoc[ADR-027: cross-template scope graph (proposed)]\n+* xref:adr-0028-hybrid-template-lsp.adoc[ADR-028: hybrid host+FlatBars LSP (proposed)]\n * xref:adr-final-review.adoc[Final ADR review (2026-06-01)]\n \n .Specification","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fnav.adoc","sha":"b6b359fa9428297fb9b53bee04a3a3f29c566ed2","status":"modified"},{"additions":8,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0026-editor-capability-matrix.adoc","changes":11,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0026-editor-capability-matrix.adoc?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":3,"filename":"docs/modules/ROOT/pages/adr-0026-editor-capability-matrix.adoc","patch":"@@ -105,9 +105,14 @@ here turns the absence into a feature.\n * **Verifier**: `check:operations-paint` (added alongside this ADR) keeps the\n   catalog / painter parity honest, so the helper-name highlighting stays\n   trustworthy even as the prelude grows.\n-* **Future**: Adding a cross-template scope graph (a real \"module system\" for\n-  FlatBars) would unlock `definition`, `references`, `rename`. That is a\n-  language-level change behind a fresh ADR, not an editor follow-up.\n+* **Future**: Adding a cross-template scope graph would unlock `definition`,\n+  `references`, `rename`, and `documentHighlight`. Designed in\n+  xref:adr-0027-cross-template-scope-graph.adoc[ADR-027] as an editor-side\n+  workspace indexer over the engine's `tokenize`, no language-level changes\n+  needed. Adding full LSP intelligence inside hybrid host+FlatBars templates\n+  (`*.java.xbars` and friends) is designed in\n+  xref:adr-0028-hybrid-template-lsp.adoc[ADR-028] via VS Code's\n+  `EmbeddedLanguageInfoProvider` API plus a virtual-document shim.\n \n == Status\n ","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0026-editor-capability-matrix.adoc","sha":"9cc82b76df9dddcdf672dcbf184ad0c3455931dd","status":"modified"},{"additions":103,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0027-cross-template-scope-graph.adoc","changes":103,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0027-cross-template-scope-graph.adoc?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":0,"filename":"docs/modules/ROOT/pages/adr-0027-cross-template-scope-graph.adoc","patch":"@@ -0,0 +1,103 @@\n+= ADR-027 — Cross-template scope graph for the LSP\n+:adr-id: 0027\n+:status: Proposed\n+:date: 2026-06-04\n+\n+== Context\n+\n+xref:adr-0026-editor-capability-matrix.adoc[ADR-026] fixed the LSP capability\n+matrix and listed four capabilities the server deliberately does NOT ship —\n+`textDocument/definition`, `textDocument/references`, `textDocument/rename`,\n+`textDocument/documentHighlight` — on the grounds that the engine has no\n+cross-template scope graph: partials and helpers are looked up at runtime\n+through a host-supplied registry the engine never indexes. Without an editor-\n+side index, those four capabilities would either return nothing or fall back\n+to text-shaped string search (strictly worse than the editor's built-in word\n+highlight).\n+\n+This record proposes the missing scope graph as a *separate, future work\n+item* outside the editor surface — an engine-level addition that, once\n+shipped, would unlock the four LSP capabilities almost mechanically.\n+\n+== Decision\n+\n+A cross-template scope graph is a workspace-scoped index built and maintained\n+by the language server, structured around the two named references FlatBars\n+templates actually carry:\n+\n+* **Partial names** — every `{{> name}}` (and `{{#> name}}`) is a reference;\n+  every `{{#*inline \"name\"}} … {{/inline}}` block (FullBars) is a definition.\n+* **Helper / operation names** — every identifier the painter resolves\n+  through `editors/operations.json` is a reference to the catalogue entry;\n+  user-defined helpers registered via `registerHelper` (ADR-018) are\n+  definitions only the host knows about (out of editor scope until the\n+  registry abstraction lands).\n+\n+The index is workspace-rooted: the LSP scans every file whose URI extension\n+maps to a dialect (`dialectForUri` returns non-null), parses each into the\n+token stream, and records `(name, defLocation)` and `(name, refLocation)`\n+tuples. Two collections per workspace:\n+\n+* `partialDefinitions: Map<name, Location[]>` — the locations of inline\n+  declarations.\n+* `partialReferences: Map<name, Location[]>` — the locations of `{{> name}}`\n+  invocations.\n+\n+Capability wiring:\n+\n+* `textDocument/definition` on a partial-name token (in either an inline\n+  declaration or an invocation) returns the matching definitions.\n+* `textDocument/references` returns all matching invocations (plus the\n+  definition when `includeDeclaration` is set).\n+* `textDocument/rename` proposes a `WorkspaceEdit` rewriting both the\n+  definition and every invocation.\n+* `textDocument/documentHighlight` (the in-file slice) returns the\n+  definitions and references that share the file with the cursor.\n+\n+The index is *eventually consistent*: built on `workspace/didChangeWatchedFiles`,\n+re-indexed per-file on `textDocument/didChange`, garbage-collected on\n+`textDocument/didClose` when the URI leaves the workspace. The LSP serves\n+stale answers between batches rather than blocking on a full re-scan — the\n+worst stale answer is \"no result\" which the four capabilities tolerate.\n+\n+== Phased plan\n+\n+. **Phase 1 — read-only scanner.** Add `editors/lsp/src/index.mjs` that\n+  consumes the engine's `tokenize` over every workspace file and builds the\n+  two maps. No LSP wiring yet; expose `definitionsOf(name)` /\n+  `referencesOf(name)` for tests.\n+. **Phase 2 — `textDocument/definition` + `documentHighlight`.** Wire the\n+  first two capabilities into `server.mjs`; advertise both in the\n+  `InitializeResult`. The single-buffer documentHighlight degrades to the\n+  word-bounded set of references in the active file when the index is cold.\n+. **Phase 3 — `textDocument/references`.** Same shape as definition; honours\n+  `includeDeclaration`.\n+. **Phase 4 — `textDocument/rename`.** Returns a `WorkspaceEdit` with one\n+  `TextEdit` per occurrence. A pre-check rejects renames whose target collides\n+  with an existing partial name.\n+. **Phase 5 — helper / operation references.** Treat catalogue identifiers as\n+  references too; the catalogue entries themselves are the definitions\n+  (synthetic locations into `operations.json`).\n+\n+Tests at each phase: per-fixture corpus under `editors/lsp/test/fixtures/`\n+asserting the index's contents, the capability's response, and at least one\n+multi-file scenario.\n+\n+== Consequences\n+\n+* Unblocks the four ADR-026 omissions without requiring a language-level\n+  module system. The engine itself stays unchanged — the LSP owns the index.\n+* Adds workspace scanning to server startup; on large workspaces (10K+\n+  templates) the initial scan needs streaming and progress notifications.\n+  Implement with `workspace/configuration` so users can cap the scope.\n+* `registerHelper`-style host-defined helpers remain unreachable until the\n+  registry abstraction in ADR-018 grows an editor-visible projection.\n+* User-defined inline partials in non-FullBars dialects are unreachable as\n+  written — RawBars/MaxBars do not have `{{#*inline}}`. Either the partial\n+  registry is host-provided (unindexable today) or partials become a\n+  filesystem convention (one `.fullbars` file per partial name).\n+\n+== Status\n+\n+Proposed 2026-06-04. Implementation is gated on a small `tokenize`-based\n+scanner; no language-level changes are required.","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0027-cross-template-scope-graph.adoc","sha":"2ac9a0175f26b3e93d2bb79d350666a023d25ad4","status":"added"},{"additions":104,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0028-hybrid-template-lsp.adoc","changes":104,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0028-hybrid-template-lsp.adoc?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":0,"filename":"docs/modules/ROOT/pages/adr-0028-hybrid-template-lsp.adoc","patch":"@@ -0,0 +1,104 @@\n+= ADR-028 — Full LSP intelligence in hybrid host+FlatBars templates\n+:adr-id: 0028\n+:status: Proposed\n+:date: 2026-06-04\n+\n+== Context\n+\n+xref:adr-0017-editor-support-lsp.adoc[ADR-017] amended on 2026-06-04 added an\n+injection grammar (`editors/flatbars-injection.tmLanguage.json`) so a hybrid\n+file like `test.java.xbars` — opened as `java` via `files.associations` —\n+gets Java syntax highlighting from the host grammar AND FlatBars tag\n+highlighting layered on top via the injection. Hybrid templates work for\n+*syntax highlighting* immediately, with zero per-file setup beyond the file\n+association.\n+\n+What hybrid templates do NOT get is LSP intelligence on the FlatBars regions:\n+the LSP attaches to the *host* language (Java's LSP), not to FlatBars. Hover,\n+completion, diagnostics, the canonicalisation quick-fix, the new\n+documentSymbol / foldingRange / formatting capabilities — none of these fire\n+on `{{ … }}` tags in a hybrid file.\n+\n+The path forward is VS Code's `vscode.languages.registerEmbeddedLanguageInfoProvider`\n+API (introduced 1.86) plus an `EmbeddedDocumentProvider` style scheme: extract\n+the embedded FlatBars regions into a synthetic virtual document, forward LSP\n+requests across the two languages, and map positions back through the\n+extraction. This record proposes the design.\n+\n+== Decision\n+\n+The VS Code extension adds an `EmbeddedLanguageInfoProvider` for every host\n+language listed in `editors/flatbars-injection.tmLanguage.json` `injectTo`\n+(currently 26 hosts). For each FlatBars region in a hybrid file the provider\n+exposes a *virtual document* the FlatBars LSP attaches to; the extension\n+shims LSP requests between the user's editor (which targets the physical\n+file) and the LSP (which sees the virtual file).\n+\n+Concretely:\n+\n+* **Region extraction.** A new helper in `editors/vscode/src/embedded.js`\n+  scans the physical document for `{{ … }}` runs (the same regex the\n+  injection grammar uses, in a JS port to keep them in lock-step). For each\n+  run it records `(physicalRange, virtualOffset)`.\n+* **Virtual document.** The hybrid document is reflected at a `flatbars-embedded:`\n+  URI; its content is ` `-padded host text with the FlatBars tags spliced\n+  in at their original offsets so column numbers match the physical document\n+  exactly. The padding glyph is a non-breaking space that the FlatBars\n+  tokenizer treats as content (never a tag).\n+* **Request forwarding.** The extension intercepts every LSP request on the\n+  physical document whose position falls inside an extracted region and\n+  re-issues it against the virtual URI, then translates result ranges back.\n+  Capabilities forwarded: hover, completion, codeAction, foldingRange,\n+  documentSymbol, formatting. Diagnostics flow the other way — the LSP\n+  publishes onto the virtual URI and the extension forwards them onto the\n+  physical URI with translated ranges.\n+* **Capability gating.** Requests whose position falls OUTSIDE every region\n+  pass through to the host language unchanged. This is what makes \"Java\n+  intelligence on Java code, FlatBars intelligence on `{{ … }}` tags\"\n+  work simultaneously in one buffer.\n+\n+JetBrains follow-up: IntelliJ has an analogous concept (the LSP customization\n+hook plus the `MultiHostLanguage` API). Wiring it is a separate ticket\n+because the JetBrains Marketplace plugin currently consumes the LSP through\n+the platform's stock client, which does not expose the multi-host hook.\n+\n+== Phased plan\n+\n+. **Phase 1 — extraction + virtual document.** Land `embedded.js` with a\n+  unit test exercising every host's region extraction. No LSP wiring.\n+. **Phase 2 — hover + diagnostics on virtual.** Wire the smallest pair to\n+  prove the round-trip; gate behind a `flatbars.experimental.hybridLsp`\n+  setting until the rough edges shake out.\n+. **Phase 3 — completion + codeAction.** Adds the request types that mutate\n+  the document (codeAction's `WorkspaceEdit` needs physical-URI ranges).\n+. **Phase 4 — foldingRange + documentSymbol + formatting.** Same wiring as\n+  Phase 3; documentSymbol's nested children need physical ranges.\n+. **Phase 5 — drop the experimental gate.** Flip the default; document in\n+  the editor README.\n+\n+Tests per phase: hybrid fixture corpus under `editors/vscode/test/embedded/`,\n+one per host language, asserting position mapping in both directions\n+(physical → virtual and virtual → physical).\n+\n+== Consequences\n+\n+* Unlocks every LSP capability inside hybrid templates without changing the\n+  injection grammar or the LSP server. The complexity is purely\n+  client-side wiring.\n+* Performance: a hybrid file holds two LSP sessions simultaneously. The\n+  virtual-document representation should be ephemeral (live in extension\n+  memory, not the workspace) to keep watchers and indexers from doubling.\n+* The padding strategy means line numbers in the virtual document always\n+  match the physical document, which the LSP can stay oblivious to —\n+  position translation is identity on column except across newlines. We\n+  accept the small wasted bytes (` ` per host character) as the price\n+  of zero column-math edge cases.\n+* JetBrains side stays syntax-only until the multi-host hook ships there or\n+  we add a custom integration. Communicate this in the JetBrains README so\n+  Ultimate users aren't surprised.\n+\n+== Status\n+\n+Proposed 2026-06-04. Implementation gated on\n+`vscode.languages.registerEmbeddedLanguageInfoProvider` (1.86+) and a small\n+client-side region extractor; no engine or LSP server changes are required.","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0028-hybrid-template-lsp.adoc","sha":"fd4f53782b11b39c070d03041906b66be80f9640","status":"added"},{"additions":79,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/editors%2Flsp%2Fsrc%2Ftokens.mjs","changes":99,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/editors%2Flsp%2Fsrc%2Ftokens.mjs?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":20,"filename":"editors/lsp/src/tokens.mjs","patch":"@@ -119,31 +119,90 @@ export function dialectDiagnostics(text, dialect) {\n     if (s.kind === \"set-delimiter\") continue; // {{=A B=}} legitimately has internal space\n     const body = bodyOfTag(text, s);\n     if (!body) continue;\n-    // Subexpression: a `(` outside any string literal.\n-    const subexpAt = findOutsideStrings(body.text, \"(\");\n-    if (subexpAt >= 0) {\n-      out.push({\n-        start: body.from + subexpAt,\n-        end: body.from + subexpAt + 1,\n-        message: `Subexpressions \\`(…)\\` are not valid in ${displayName(dialect)} — only a single path is allowed in a tag.`,\n-      });\n-      continue; // one finding per tag keeps the output focused\n-    }\n-    // Helper-args: any whitespace between non-string tokens in the body. A\n-    // legitimate Mustache tag body is a single path with optional leading/\n-    // trailing whitespace; `{{lookup x}}` packs two tokens and is invalid.\n-    const helperGapAt = findHelperArgsGap(body.text);\n-    if (helperGapAt >= 0) {\n-      out.push({\n-        start: body.from + helperGapAt,\n-        end: body.from + helperGapAt + 1,\n-        message: `Helper invocations with arguments are not valid in ${displayName(dialect)} — a tag body is a single path, not a function call.`,\n-      });\n+    // Most-specific rules first, so the diagnostic message matches the actual\n+    // construct rather than the generic \"helper invocation\" catch-all. Each rule\n+    // returns the diagnostic offset in body.text (or -1); we stop at the first\n+    // hit per tag so squiggle spam stays manageable.\n+    const finders = [\n+      findSubexpression,\n+      findBlockParam,\n+      findPartialHashArg(s.kind),\n+      findOperator,\n+      findHelperArgs,\n+    ];\n+    for (const finder of finders) {\n+      const hit = finder(body.text);\n+      if (hit) {\n+        out.push({\n+          start: body.from + hit.at,\n+          end: body.from + hit.at + hit.len,\n+          message: hit.msg(displayName(dialect)),\n+        });\n+        break;\n+      }\n     }\n   }\n   return out;\n }\n \n+function findSubexpression(body) {\n+  const at = findOutsideStrings(body, \"(\");\n+  if (at < 0) return null;\n+  return {\n+    at, len: 1,\n+    msg: (d) => `Subexpressions \\`(…)\\` are not valid in ${d} — only a single path is allowed in a tag.`,\n+  };\n+}\n+\n+function findBlockParam(body) {\n+  // ` as |x|` or ` as |x y|`. The leading space is mandatory so we don't false-\n+  // positive on identifiers that happen to start with `as`.\n+  const m = /\\sas\\s*\\|/.exec(body);\n+  if (!m) return null;\n+  return {\n+    at: m.index, len: m[0].length,\n+    msg: (d) => `Block parameters (\\`as |…|\\`) are not valid in ${d} — sections take only a single name.`,\n+  };\n+}\n+\n+function findPartialHashArg(kind) {\n+  return (body) => {\n+    if (kind !== \"partial\") return null;\n+    // `name=value` anywhere in the body outside strings; `=` outside a string\n+    // is the simplest signal. Skip the partial sigil (`>`/`>*`) plus the\n+    // partial name — those don't contain `=`.\n+    const at = findOutsideStrings(body, \"=\");\n+    if (at < 0) return null;\n+    return {\n+      at, len: 1,\n+      msg: (d) => `Partial hash arguments (\\`name=value\\`) are not valid in ${d} — only the partial name is allowed.`,\n+    };\n+  };\n+}\n+\n+function findOperator(body) {\n+  // MaxBars operators that the engine's tokenize doesn't classify as `error`\n+  // when the dialect doesn't support them — `??` / `==` / `!=` / `<=` / `>=` /\n+  // `<-` / `&&` / `||`. Single-char operators (`<`/`>`/`+`/`*`/`%`/`!`/`|`/`-`)\n+  // are too noisy to flag at this layer because identifier punctuation often\n+  // overlaps; the helper-args rule catches them via the whitespace-gap test.\n+  const multi = body.match(/\\?\\?|==|!=|<=|>=|<-|&&|\\|\\|/);\n+  if (!multi) return null;\n+  return {\n+    at: multi.index, len: multi[0].length,\n+    msg: (d) => `Operator \\`${multi[0]}\\` is a MaxBars feature, not valid in ${d}.`,\n+  };\n+}\n+\n+function findHelperArgs(body) {\n+  const at = findHelperArgsGap(body);\n+  if (at < 0) return null;\n+  return {\n+    at, len: 1,\n+    msg: (d) => `Helper invocations with arguments are not valid in ${d} — a tag body is a single path, not a function call.`,\n+  };\n+}\n+\n function bodyOfTag(text, s) {\n   // Skip braces, sigils, ~ at both ends; the rest is the body.\n   let from = s.from;","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/editors%2Flsp%2Fsrc%2Ftokens.mjs","sha":"57e2337df7e24cfb59f035e66b405768d4216393","status":"modified"},{"additions":33,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/editors%2Flsp%2Ftest%2Ftokens.test.mjs","changes":33,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/editors%2Flsp%2Ftest%2Ftokens.test.mjs?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":0,"filename":"editors/lsp/test/tokens.test.mjs","patch":"@@ -283,6 +283,39 @@ t(\"dialectDiagnostics — FullBars and MaxBars accept both shapes\", () => {\n   assert.deepEqual(dialectDiagnostics(\"{{ a ?? b }}\", \"maxbars\"), []);\n });\n \n+t(\"dialectDiagnostics — MinBars flags block parameters\", () => {\n+  const ds = dialectDiagnostics(\"{{#each items as |item|}}{{/each}}\", \"minbars\");\n+  assert.equal(ds.length, 1);\n+  assert.match(ds[0].message, /Block parameters/);\n+});\n+\n+t(\"dialectDiagnostics — MinBars flags partial hash args\", () => {\n+  const ds = dialectDiagnostics('{{> p name=\"v\"}}', \"minbars\");\n+  assert.equal(ds.length, 1);\n+  assert.match(ds[0].message, /Partial hash arguments/);\n+});\n+\n+t(\"dialectDiagnostics — MinBars flags MaxBars-only operators\", () => {\n+  for (const [op, src] of [\n+    [\"??\", \"{{ a ?? b }}\"],\n+    [\"==\", \"{{ a == b }}\"],\n+    [\"!=\", \"{{ a != b }}\"],\n+    [\"<=\", \"{{ a <= b }}\"],\n+    [\"<-\", \"{{ a <- b }}\"],\n+    [\"&&\", \"{{ a && b }}\"],\n+  ]) {\n+    const ds = dialectDiagnostics(src, \"minbars\");\n+    assert.ok(ds.length >= 1, `${op} flagged`);\n+    assert.match(ds[0].message, new RegExp(`Operator.*${op.replace(/[?+*]/g, \"\\\\$&\")}.*MaxBars`));\n+  }\n+});\n+\n+t(\"dialectDiagnostics — RawBars matches MinBars on the extended rules\", () => {\n+  assert.equal(dialectDiagnostics(\"{{#each xs as |i|}}{{/each}}\", \"rawbars\").length, 1);\n+  assert.equal(dialectDiagnostics('{{> p k=\"v\"}}', \"rawbars\").length, 1);\n+  assert.equal(dialectDiagnostics(\"{{ a ?? b }}\", \"rawbars\").length, 1);\n+});\n+\n t(\"parseDiagnostics merges parser and dialect findings\", () => {\n   // The user's exact case from the session: opens cleanly as fullbars, fires two\n   // diagnostics in minbars (the subexpression + the helper invocation later in","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/editors%2Flsp%2Ftest%2Ftokens.test.mjs","sha":"0b52650d20857042ce107a9f4aff88eba7601c8a","status":"modified"},{"additions":23,"blob_url":"https://github.com/wstein/flatbars/blob/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/lab%2Fvendor%2Fflatbars-engine.mjs","changes":46,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fvendor%2Fflatbars-engine.mjs?ref=306fc0a364b46162059cd5562c5e5ffd7a6a4b4f","deletions":23,"filename":"lab/vendor/flatbars-engine.mjs","patch":"@@ -14035,20 +14035,33 @@ var lookup8 = /* @__PURE__ */ lookup2(ordString);\n var union6 = /* @__PURE__ */ union(ordString);\n var map24 = /* @__PURE__ */ map(functorEither);\n var mapFlipped3 = /* @__PURE__ */ mapFlipped(functorEither);\n+var minMeta = {\n+  runtimeVersion,\n+  preamble: \"\",\n+  seed: \"rt.mseed(data, rt.truthyMustache)\"\n+};\n+var minLexConfig = /* @__PURE__ */ (function() {\n+  return {\n+    open: defaultLexConfig.open,\n+    close: defaultLexConfig.close,\n+    keepLongComments: defaultLexConfig.keepLongComments,\n+    mustacheDelims: true\n+  };\n+})();\n var minOptions = /* @__PURE__ */ (function() {\n   return {\n     parseExpr: defaultParseOptions.parseExpr,\n     parseHead: defaultParseOptions.parseHead,\n     rawBlockHash: defaultParseOptions.rawBlockHash,\n     standaloneSeps: defaultParseOptions.standaloneSeps,\n     lexOptions: defaultParseOptions.lexOptions,\n-    lexConfig: defaultParseOptions.lexConfig,\n     extras: true,\n     inheritance: true,\n     decorators: false,\n     partialBlocks: false,\n     rawBlockHbs: false,\n-    trimStandalone: false\n+    trimStandalone: false,\n+    lexConfig: minLexConfig\n   };\n })();\n var parseMinWith = function(cfg) {\n@@ -14085,27 +14098,14 @@ var renderCoreWith = function(cfg) {\n             return new Right(v1.value0);\n           }\n           ;\n-          throw new Error(\"Failed pattern match at MinBars (line 137, column 7 - line 139, column 31): \" + [v1.constructor.name]);\n+          throw new Error(\"Failed pattern match at MinBars (line 143, column 7 - line 145, column 31): \" + [v1.constructor.name]);\n         }\n         ;\n-        throw new Error(\"Failed pattern match at MinBars (line 131, column 39 - line 139, column 31): \" + [v.constructor.name]);\n+        throw new Error(\"Failed pattern match at MinBars (line 137, column 39 - line 145, column 31): \" + [v.constructor.name]);\n       };\n     };\n   };\n };\n-var minMeta = {\n-  runtimeVersion,\n-  preamble: \"\",\n-  seed: \"rt.mseed(data, rt.truthyMustache)\"\n-};\n-var minLexConfig = /* @__PURE__ */ (function() {\n-  return {\n-    open: defaultLexConfig.open,\n-    close: defaultLexConfig.close,\n-    keepLongComments: defaultLexConfig.keepLongComments,\n-    mustacheDelims: true\n-  };\n-})();\n var parseMin = /* @__PURE__ */ parseMinWith(minLexConfig);\n var renderCore = /* @__PURE__ */ renderCoreWith(minLexConfig);\n var renderMinWith = function(partialSrcs) {\n@@ -14121,7 +14121,7 @@ var renderMinWith = function(partialSrcs) {\n           return new Right(new Tuple(v2.value0, desugar2(v1.value0.nodes)));\n         }\n         ;\n-        throw new Error(\"Failed pattern match at MinBars (line 112, column 35 - line 114, column 58): \" + [v1.constructor.name]);\n+        throw new Error(\"Failed pattern match at MinBars (line 118, column 35 - line 120, column 58): \" + [v1.constructor.name]);\n       };\n       var v = traverse7(compilePartial)(partialSrcs);\n       if (v instanceof Left) {\n@@ -14132,7 +14132,7 @@ var renderMinWith = function(partialSrcs) {\n         return renderCore(fromFoldable9(v.value0))(src)(dat);\n       }\n       ;\n-      throw new Error(\"Failed pattern match at MinBars (line 108, column 3 - line 110, column 57): \" + [v.constructor.name]);\n+      throw new Error(\"Failed pattern match at MinBars (line 114, column 3 - line 116, column 57): \" + [v.constructor.name]);\n     };\n   };\n };\n@@ -14153,7 +14153,7 @@ var expansionIndent = function(indent) {\n       return new Left(new DisallowedShape(\"intrinsic block indentation with a non-static default (MinBars compile)\", 0));\n     }\n     ;\n-    throw new Error(\"Failed pattern match at MinBars (line 260, column 1 - line 260, column 66): \" + [indent.constructor.name, body.constructor.name]);\n+    throw new Error(\"Failed pattern match at MinBars (line 266, column 1 - line 266, column 66): \" + [indent.constructor.name, body.constructor.name]);\n   };\n };\n var blockIndent = function(args) {\n@@ -14186,7 +14186,7 @@ var inline = function(partials) {\n                   return inline(partials)(overrides)(new Cons(v[\"value1\"][\"value1\"][0].value0.value0, chain))(depth)(indentTemplate(v[\"value1\"][\"value1\"][1].value0.value0)(v1.value0));\n                 }\n                 ;\n-                throw new Error(\"Failed pattern match at MinBars (line 217, column 22 - line 219, column 99): \" + [v1.constructor.name]);\n+                throw new Error(\"Failed pattern match at MinBars (line 223, column 22 - line 225, column 99): \" + [v1.constructor.name]);\n               }\n               ;\n             }\n@@ -14210,7 +14210,7 @@ var inline = function(partials) {\n                   return inline(partials)(union6(overrides)(harvestBlocks(v.value4)))(chain)(depth + 1 | 0)(indentTemplate(v[\"value3\"][1].value0.value0)(v1.value0));\n                 }\n                 ;\n-                throw new Error(\"Failed pattern match at MinBars (line 226, column 22 - line 230, column 45): \" + [v1.constructor.name]);\n+                throw new Error(\"Failed pattern match at MinBars (line 232, column 22 - line 236, column 45): \" + [v1.constructor.name]);\n               }\n               ;\n             }\n@@ -14240,7 +14240,7 @@ var inline = function(partials) {\n                   });\n                 }\n                 ;\n-                throw new Error(\"Failed pattern match at MinBars (line 235, column 36 - line 242, column 37): \" + [v2.constructor.name]);\n+                throw new Error(\"Failed pattern match at MinBars (line 241, column 36 - line 248, column 37): \" + [v2.constructor.name]);\n               }\n               ;\n               return new Left(new DisallowedShape(\"malformed block override (MinBars compile)\", 0));","raw_url":"https://github.com/wstein/flatbars/raw/306fc0a364b46162059cd5562c5e5ffd7a6a4b4f/lab%2Fvendor%2Fflatbars-engine.mjs","sha":"1416dafcffc5815bfbb84bdf7933b48b9bce1d9a","status":"modified"}]}