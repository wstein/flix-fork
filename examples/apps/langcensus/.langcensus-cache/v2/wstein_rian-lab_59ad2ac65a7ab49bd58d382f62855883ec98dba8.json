{"files":[{"additions":15,"blob_url":"https://github.com/wstein/rian-lab/blob/59ad2ac65a7ab49bd58d382f62855883ec98dba8/docs%2Fadr%2F0049-backend-target-roadmap.md","changes":26,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0049-backend-target-roadmap.md?ref=59ad2ac65a7ab49bd58d382f62855883ec98dba8","deletions":11,"filename":"docs/adr/0049-backend-target-roadmap.md","patch":"@@ -164,17 +164,21 @@ tagged **array** `[\"ok\", v]`. The repr is parity-locked across `Rian.JS` and the\n purs `JS.purs` (`js`-parity), with\n `compile_types` emitting the matching discriminated object union.\n \n-**Field keys are positional `_0`/`_1`** today. A *labeled-object* refinement — use\n-the variant's field labels where present (`{ $: \"Circle\", radius: r }`), `_n` for\n-anonymous — is the agreed next step but **deferred**: the prototype showed the IR\n-that dominates real Rian is ~90% anonymous variants (so labels help a small, mostly\n-user-facing subset, and structs already cover named fields), while the cost is a\n-ctor→label resolution pass (the `EVariant`/`Rian.Lower.resolve_variants` machinery)\n-wired through three emitters. A **hybrid** (labeled→object, anonymous→array) was\n-**rejected**: it reintroduces the variant≠list ambiguity for the anonymous majority,\n-makes the runtime shape depend on whether the author wrote field names, has no\n-coherent answer for *partially*-labeled variants (`Foo(x Int, Int)`), and degrades\n-the `.d.mts` from a discriminated-object union to literal tuples.\n+**Field keys are the variant's labels where present, `_0`/`_1` otherwise** —\n+`Circle(radius Float64)` → `{ $: \"Circle\", radius: r }` (matched `v.radius`), while an\n+anonymous `Add(Expr, Expr)` stays `{ $: \"Add\", _0: a, _1: b }`. A reflective pre-emit\n+pass (`Rian.JS.bake_variants`) resolves a construction to an `EVariant` carrying its\n+`{label｜nil, value}` pairs (handling positional **and** named `Circle(radius: …)`\n+construction) and tags a `PCtor` with its `labels`, so `expr_js`/`pat_match` read the\n+names off the node — no type registry threaded into the deep emit. `compile_types`\n+emits the matching named union (`{ $: \"Circle\", radius: number } | …`). The prototype\n+that drove the design: IR is ~90% anonymous variants, so the named-field win is\n+concentrated in user-facing types (and structs already cover named fields) — but the\n+one-model object form carries it cleanly. A **hybrid** (labeled→object,\n+anonymous→array) was **rejected**: it reintroduces the variant≠list ambiguity for the\n+anonymous majority, makes the runtime shape depend on whether the author wrote field\n+names, has no coherent answer for *partially*-labeled variants (`Foo(x Int, Int)`),\n+and degrades the `.d.mts` from a discriminated-object union to literal tuples.\n \n ### 4. Relationship to the bootstrap stages (ADR-0031)\n ","raw_url":"https://github.com/wstein/rian-lab/raw/59ad2ac65a7ab49bd58d382f62855883ec98dba8/docs%2Fadr%2F0049-backend-target-roadmap.md","sha":"657a5df29a1ac3ad841c52a8f0be611b16b6bf28","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/59ad2ac65a7ab49bd58d382f62855883ec98dba8/lib%2Frian%2Fcore.ex","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcore.ex?ref=59ad2ac65a7ab49bd58d382f62855883ec98dba8","deletions":2,"filename":"lib/rian/core.ex","patch":"@@ -126,9 +126,14 @@ defmodule Rian.Core do\n   end\n \n   defmodule PCtor do\n-    @moduledoc \"A sum-variant pattern `Ctor(args…)` (nullary when `args == []`).\"\n+    @moduledoc \"\"\"\n+    A sum-variant pattern `Ctor(args…)` (nullary when `args == []`). `labels` is an\n+    optional per-field name list (`[\"radius\"]`, `nil` for anonymous), populated by the\n+    JS emitter's `bake_variants` so it can bind `v.radius` instead of `v._0`; other\n+    backends ignore it.\n+    \"\"\"\n     @enforce_keys [:ctor]\n-    defstruct ctor: nil, args: [], type: nil\n+    defstruct ctor: nil, args: [], type: nil, labels: nil\n   end\n \n   defmodule PAs do","raw_url":"https://github.com/wstein/rian-lab/raw/59ad2ac65a7ab49bd58d382f62855883ec98dba8/lib%2Frian%2Fcore.ex","sha":"dd3f55abfed33b264ca6ea23c586c7e35212c457","status":"modified"},{"additions":105,"blob_url":"https://github.com/wstein/rian-lab/blob/59ad2ac65a7ab49bd58d382f62855883ec98dba8/lib%2Frian%2Fjs.ex","changes":109,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fjs.ex?ref=59ad2ac65a7ab49bd58d382f62855883ec98dba8","deletions":4,"filename":"lib/rian/js.ex","patch":"@@ -107,6 +107,7 @@ defmodule Rian.JS do\n     ENum,\n     EStr,\n     EStruct,\n+    EVariant,\n     ELabel,\n     ETuple,\n     EUnary,\n@@ -202,6 +203,10 @@ defmodule Rian.JS do\n       Check.program_ic(prog)\n       |> Map.put(:consts, MapSet.new(consts, & &1.name))\n       |> Map.put(:js_reg, reg)\n+      # ctor → {enum, named, labels} for `bake_variants`: a sum construction becomes\n+      # an `EVariant` (named fields where the variant has labels, `_n` otherwise) and\n+      # a ctor pattern gains its `labels`, so emit can spell `v.radius` not `v._0`.\n+      |> Map.put(:js_vmeta, variant_meta(prog))\n \n     const_js = Enum.map_join(consts, \"\\n\", &const_js(&1, i53, ic))\n     fn_js = Enum.map_join(funcs, \"\\n\\n\", &function_js(&1, i53, ic))\n@@ -356,7 +361,9 @@ defmodule Rian.JS do\n         body =\n           fs\n           |> Enum.with_index()\n-          |> Enum.map_join(\", \", fn {f, i} -> \"_#{i}: #{ts_type(f.type, known, tvars)}\" end)\n+          |> Enum.map_join(\", \", fn {f, i} ->\n+            \"#{Map.get(f, :label) || \"_#{i}\"}: #{ts_type(f.type, known, tvars)}\"\n+          end)\n \n         \"{ $: #{tag}, #{body} }\"\n     end\n@@ -536,6 +543,7 @@ defmodule Rian.JS do\n       |> Pratt.parse_body()\n       |> resolve_consts(Map.get(ic, :consts, MapSet.new()))\n       |> Check.annotate(%{}, ic)\n+      |> bake_variants(Map.get(ic, :js_vmeta, %{}))\n \n     val =\n       case Rian.Shadow.dedup(stmts, [], &js_fresh/2) do\n@@ -902,9 +910,11 @@ defmodule Rian.JS do\n   # *inside* the structural test so a nested field access (`a0[1][1]`) only runs\n   # once the shape is known; a `when` guard, written in the bound names, follows.\n   defp clause_js(%{pats: pats, body: body, guard: guard}, i53, params, ic) do\n+    vm = Map.get(ic, :js_vmeta, %{})\n+\n     {tests, binds} =\n       pats\n-      |> Enum.map(&Core.from_pat/1)\n+      |> Enum.map(&(&1 |> Core.from_pat() |> bake_variants(vm)))\n       |> Enum.with_index()\n       |> Enum.reduce({[], []}, fn {p, i}, {ts, bs} ->\n         {t, b} = pat_match(p, \"a#{i}\", i53)\n@@ -989,12 +999,12 @@ defmodule Rian.JS do\n     {[\"#{acc}.length === #{length(es)}\" | ts], bs}\n   end\n \n-  defp pat_match(%PCtor{ctor: ctor, args: args}, acc, i53) do\n+  defp pat_match(%PCtor{ctor: ctor, args: args, labels: labels}, acc, i53) do\n     {ts, bs} =\n       args\n       |> Enum.with_index()\n       |> Enum.reduce({[], []}, fn {p, i}, {ts, bs} ->\n-        {t, b} = pat_match(p, \"#{acc}._#{i}\", i53)\n+        {t, b} = pat_match(p, \"#{acc}.#{field_key(labels, i)}\", i53)\n         {ts ++ t, bs ++ b}\n       end)\n \n@@ -1078,10 +1088,89 @@ defmodule Rian.JS do\n       |> bake_union_disc(Map.get(ic, :js_reg, %{sums: %{}, structs: MapSet.new()}))\n       |> resolve_consts(Map.get(ic, :consts, MapSet.new()))\n       |> Check.annotate(tenv, ic)\n+      # resolve sum constructions to `EVariant` (labeled fields) + label ctor\n+      # patterns in `case` arms, on the typed Core (ADR-0049 §3b).\n+      |> bake_variants(Map.get(ic, :js_vmeta, %{}))\n \n     block_return(Rian.Shadow.dedup(stmts, params, &js_fresh/2), i53)\n   end\n \n+  # ctor name → `%{enum, ctor, named, labels}` for every sum variant (top level +\n+  # every `mod`); `labels` is the per-field `label | nil` list.\n+  defp variant_meta(prog) do\n+    types = Map.get(prog, :types, []) ++ for(m <- Map.get(prog, :mods, []), t <- m.types, do: t)\n+\n+    for t <- types, v <- t.variants, into: %{} do\n+      labels = Enum.map(v.fields, &Map.get(&1, :label))\n+      named = v.fields != [] and Enum.all?(labels, & &1)\n+      {v.ctor, %{enum: t.name, ctor: v.ctor, named: named, labels: labels}}\n+    end\n+  end\n+\n+  # Reflective Core walk: a sum construction (`Ctor(args)` / nullary `Ctor`) becomes\n+  # an `EVariant` carrying `{label | nil, value}` pairs, and a ctor pattern gains its\n+  # `labels` — so emit spells named fields where present and `_n` otherwise, off the\n+  # node (no registry threaded into `expr_js`/`pat_match`). Non-variant nodes recurse\n+  # generically; a non-sum `Ctor(...)` (a struct/function call) is left untouched.\n+  defp bake_variants(%EVariant{} = n, _vm), do: n\n+\n+  defp bake_variants(%ECall{fun: %EId{name: c}, args: args} = n, vm) do\n+    case Map.get(vm, c) do\n+      nil ->\n+        bake_struct(n, vm)\n+\n+      info ->\n+        %EVariant{\n+          enum: info.enum,\n+          ctor: info.ctor,\n+          named: info.named,\n+          pairs: variant_pairs(info, args, vm)\n+        }\n+    end\n+  end\n+\n+  defp bake_variants(%EId{name: c} = n, vm) do\n+    case Map.get(vm, c) do\n+      %{labels: []} = info ->\n+        %EVariant{enum: info.enum, ctor: info.ctor, named: info.named, pairs: []}\n+\n+      _ ->\n+        n\n+    end\n+  end\n+\n+  defp bake_variants(%PCtor{ctor: c, args: args} = n, vm) do\n+    labels = if info = Map.get(vm, c), do: info.labels, else: nil\n+    %PCtor{n | labels: labels, args: Enum.map(args, &bake_variants(&1, vm))}\n+  end\n+\n+  defp bake_variants(%_struct{} = n, vm), do: bake_struct(n, vm)\n+  defp bake_variants(l, vm) when is_list(l), do: Enum.map(l, &bake_variants(&1, vm))\n+\n+  defp bake_variants(t, vm) when is_tuple(t),\n+    do: t |> Tuple.to_list() |> Enum.map(&bake_variants(&1, vm)) |> List.to_tuple()\n+\n+  defp bake_variants(x, _vm), do: x\n+\n+  defp bake_struct(%mod{} = n, vm) do\n+    struct(mod, n |> Map.from_struct() |> Map.new(fn {k, v} -> {k, bake_variants(v, vm)} end))\n+  end\n+\n+  # the JS object key for a variant's i-th field: its declared label, else `_i`.\n+  defp field_key(labels, i), do: (labels && Enum.at(labels, i)) || \"_#{i}\"\n+\n+  # `{label | nil, value}` pairs in declared field order — positional args zip onto\n+  # the labels; all-named args (`Circle(radius: 1.0)`) are placed by name (mirrors\n+  # `Rian.Lower.variant_pairs`).\n+  defp variant_pairs(info, args, vm) do\n+    if args != [] and Enum.all?(args, &match?(%ELabel{}, &1)) do\n+      given = Map.new(args, fn %ELabel{name: l, expr: e} -> {l, bake_variants(e, vm)} end)\n+      Enum.map(info.labels, fn l -> {l, Map.fetch!(given, l)} end)\n+    else\n+      Enum.zip(info.labels, Enum.map(args, &bake_variants(&1, vm)))\n+    end\n+  end\n+\n   # Bake a value-union type-pattern's discriminator (ADR-0083) into the surface so\n   # `pat_match` (which threads no type registry) can emit a sum's tag / a struct's\n   # `__struct__` JS test. `map_node` deliberately skips arm PATTERNS, so a `case`\n@@ -1356,6 +1445,18 @@ defmodule Rian.JS do\n     \"{ __struct__: #{inspect(to_string(name))}#{if fields == \"\", do: \"\", else: \", \" <> fields} }\"\n   end\n \n+  # a resolved sum-variant construction (ADR-0049 §3b) -> a tagged object\n+  # `{ $: \"Ctor\", radius: r }` (a labeled field) or `{ $: \"Ctor\", _0: a, _1: b }`\n+  # (anonymous). `bake_variants` produces this from a `Ctor(args)` call.\n+  defp expr_js(%EVariant{ctor: ctor, pairs: pairs}, i53) do\n+    fields =\n+      pairs\n+      |> Enum.with_index()\n+      |> Enum.map_join(\"\", fn {{label, v}, i} -> \", #{label || \"_#{i}\"}: #{expr_js(v, i53)}\" end)\n+\n+    \"{ $: #{inspect(ctor)}#{fields} }\"\n+  end\n+\n   # bare field access `value.field` (a remote call `Mod.fun(…)` is handled above\n   # as an `ECall` over an `EDot`, so a standalone `EDot` here is field access)\n   defp expr_js(%EDot{head: head, name: field}, i53), do: \"#{expr_js(head, i53)}.#{field}\"","raw_url":"https://github.com/wstein/rian-lab/raw/59ad2ac65a7ab49bd58d382f62855883ec98dba8/lib%2Frian%2Fjs.ex","sha":"fca444acea061d9e66518124cf9a4f2d243b0d58","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/59ad2ac65a7ab49bd58d382f62855883ec98dba8/site%2Fsrc%2Fdata%2Ftour.json","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fdata%2Ftour.json?ref=59ad2ac65a7ab49bd58d382f62855883ec98dba8","deletions":2,"filename":"site/src/data/tour.json","patch":"@@ -37,7 +37,7 @@\n       \"id\": \"types\",\n       \"panes\": {\n         \"ex\": \"@type shape :: {:circle, float()} | {:square, float()}\\ndef area({:circle, r}) do 3.14159 * r * r end\\ndef area({:square, s}) do s * s end\",\n-        \"js\": \"function area(a0) {\\n  { if (a0.$ === \\\"Circle\\\") { const r = a0._0; return ((3.14159 * r) * r); } }\\n  { if (a0.$ === \\\"Square\\\") { const s = a0._0; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\",\n+        \"js\": \"function area(a0) {\\n  { if (a0.$ === \\\"Circle\\\") { const r = a0.radius; return ((3.14159 * r) * r); } }\\n  { if (a0.$ === \\\"Square\\\") { const s = a0.side; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\",\n         \"jvm\": \"sealed interface Shape\\ndata class Circle(val f0: Double) : Shape\\ndata class Square(val f0: Double) : Shape\\n\\nprivate fun area(a0: Shape): Double {\\n  if (a0 is Circle) { val r = a0.f0; return ((3.14159 * r) * r) }\\n  if (a0 is Square) { val s = a0.f0; return (s * s) }\\n  throw RuntimeException(\\\"area: no clause matched\\\")\\n}\",\n         \"rs\": \"#[derive(Clone, Debug, PartialEq)]\\nenum Shape {\\n    Circle { radius: f64 },\\n    Square { side: f64 },\\n}\\n\\nfn area(s: &Shape) -> f64 {\\n    match s {\\n        Shape::Circle { radius: r } => 3.14159 * r * r,\\n        Shape::Square { side: s } => s * s,\\n    }\\n}\"\n       },\n@@ -89,7 +89,7 @@\n       \"id\": \"capabilities\",\n       \"panes\": {\n         \"ex\": \"@type shape :: {:circle, float()} | {:square, float()}\\ndef area({:circle, r}) do 3.14159 * r * r end\\ndef area({:square, s}) do s * s end\\ndef consume(s) do area(s) end\",\n-        \"js\": \"function area(a0) {\\n  { if (a0.$ === \\\"Circle\\\") { const r = a0._0; return ((3.14159 * r) * r); } }\\n  { if (a0.$ === \\\"Square\\\") { const s = a0._0; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\\n\\nfunction consume(s) { return area(s); }\",\n+        \"js\": \"function area(a0) {\\n  { if (a0.$ === \\\"Circle\\\") { const r = a0.radius; return ((3.14159 * r) * r); } }\\n  { if (a0.$ === \\\"Square\\\") { const s = a0.side; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\\n\\nfunction consume(s) { return area(s); }\",\n         \"jvm\": \"sealed interface Shape\\ndata class Circle(val f0: Double) : Shape\\ndata class Square(val f0: Double) : Shape\\n\\nprivate fun area(a0: Shape): Double {\\n  if (a0 is Circle) { val r = a0.f0; return ((3.14159 * r) * r) }\\n  if (a0 is Square) { val s = a0.f0; return (s * s) }\\n  throw RuntimeException(\\\"area: no clause matched\\\")\\n}\\n\\nprivate fun consume(a0: Shape): Double {\\n  val s = a0; return area(s)\\n}\",\n         \"rs\": \"#[derive(Clone, Debug, PartialEq)]\\nenum Shape {\\n    Circle { radius: f64 },\\n    Square { side: f64 },\\n}\\n\\nfn area(s: &Shape) -> f64 {\\n    match s {\\n        Shape::Circle { radius: r } => 3.14159 * r * r,\\n        Shape::Square { side: s } => s * s,\\n    }\\n}\\n\\nfn consume(s: Shape) -> f64 {\\n    match s {\\n        s => area(s),\\n    }\\n}\"\n       },","raw_url":"https://github.com/wstein/rian-lab/raw/59ad2ac65a7ab49bd58d382f62855883ec98dba8/site%2Fsrc%2Fdata%2Ftour.json","sha":"4dc6d78179fa6c8a1ac34ae700e06d01accb86ea","status":"modified"},{"additions":11,"blob_url":"https://github.com/wstein/rian-lab/blob/59ad2ac65a7ab49bd58d382f62855883ec98dba8/test%2Frian%2Ftour_test.exs","changes":11,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ftour_test.exs?ref=59ad2ac65a7ab49bd58d382f62855883ec98dba8","deletions":0,"filename":"test/rian/tour_test.exs","patch":"@@ -40,6 +40,17 @@ defmodule Rian.TourTest do\n       assert basics[\"panes\"][\"js\"] == \"function twice(n) { return (n * 2); }\"\n     end\n \n+    test \"a labeled sum variant lowers to a named-field object (`a0.radius`, ADR-0049 §3b)\", %{\n+      data: data\n+    } do\n+      js = Enum.find(data[\"cells\"], &(&1[\"id\"] == \"types\"))[\"panes\"][\"js\"]\n+      # `Circle(radius Float64)` binds by the field name, not a positional `_0`\n+      assert js =~ ~s|a0.$ === \"Circle\"|\n+      assert js =~ \"const r = a0.radius;\"\n+      assert js =~ \"const s = a0.side;\"\n+      refute js =~ \"a0._0\"\n+    end\n+\n     test \"the reachability strip is the honest, inferred matrix (ADR-0057)\", %{data: data} do\n       by_name = Map.new(data[\"reachExamples\"], &{&1[\"name\"], &1[\"reach\"]})\n ","raw_url":"https://github.com/wstein/rian-lab/raw/59ad2ac65a7ab49bd58d382f62855883ec98dba8/test%2Frian%2Ftour_test.exs","sha":"2e1eee2e64cc0458d7a9f1d6f35f5fdb7346d860","status":"modified"}]}