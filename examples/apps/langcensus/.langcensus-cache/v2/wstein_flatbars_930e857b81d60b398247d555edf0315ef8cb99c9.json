{"files":[{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0006-maxbars-loop-variables.mdx","changes":3,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0006-maxbars-loop-variables.mdx?ref=930e857b81d60b398247d555edf0315ef8cb99c9","deletions":1,"filename":"spec/src/content/docs/adr/adr-0006-maxbars-loop-variables.mdx","patch":"@@ -35,7 +35,8 @@ scope is scoped helpers). The cost is that bare names can shadow data fields.\n    an *error*.\n \n 1. *v1 outer-loop limit (documented).* Only the outer element/index/key are reachable, via block\n-   params `as |user uidx|`. Outer `first`/`last`/`length`/`rindex` are *not* reachable — labelled loops\n+   params (`as user uidx` in current MaxBars — drop-pipes, [ADR-021](/adr/adr-0021-maxbars-variable-model/)\n+   amendment). Outer `first`/`last`/`length`/`rindex` are *not* reachable — labelled loops\n    are future work. The companion spec's claim \"compute from a named outer length\" is wrong: block\n    params bind only the item plus index/key, so the outer length cannot be named.\n ","raw_url":"https://github.com/wstein/flatbars/raw/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0006-maxbars-loop-variables.mdx","sha":"ccbf30fb93da4185580e31e12195332f9164c04f","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/flatbars/blob/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0013-labelled-loops.mdx","changes":5,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0013-labelled-loops.mdx?ref=930e857b81d60b398247d555edf0315ef8cb99c9","deletions":2,"filename":"spec/src/content/docs/adr/adr-0013-labelled-loops.mdx","patch":"@@ -12,7 +12,8 @@ the full `this`/`loop`/`root`/`parent`/`outer` model (`outer` = a `label NAME`)\n \n ## Context\n \n-MaxBars v1 reaches only the *outer element/index/key*, via block params `{{#each users as |user uidx|}}`.\n+MaxBars v1 reaches only the *outer element/index/key*, via block params `{{#each users as user uidx}}`\n+(the drop-pipes spelling — [ADR-021](/adr/adr-0021-maxbars-variable-model/) amendment).\n The outer loop's `first`/`last`/`length`/`rindex` are unreachable, and the companion spec's \"compute from a\n named outer length\" idea was shown wrong: block params bind only the item plus index/key, so the\n outer length cannot be named. Reaching full outer-loop state needs a *naming/scoping* construct, not\n@@ -37,7 +38,7 @@ not scope-declared. So a label must be a **scope-declared binding**, exactly lik\n    single-sourced in the surface desugar beside `extractBlockParams` (it adds `NAME` to `scope` and has\n    `each` bind the frame-object). Rejected alternatives: a static `loop=\"outer\"` hash (a hash\n    value is a runtime expression, but a label is a *static* scope fact — special-casing a string-literal\n-   hash is magic); an `as |user ^outer|` sigil (MaxBars deliberately dropped sigils, ADR-006).\n+   hash is magic); an `as user ^outer` sigil (MaxBars deliberately dropped sigils, ADR-006).\n 1. *The frame-object field set is normative* —\n    `{ this, index0, index1, rindex0, rindex1, first, last, length, key }`, reusing ADR-006's fixed\n    arithmetic (`rindex0 = length − 1 − index0`, `first = index0 == 0`, …). It is an **immutable","raw_url":"https://github.com/wstein/flatbars/raw/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0013-labelled-loops.mdx","sha":"d23d31748622edcad37da7c25de4f6292b0d21e0","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/flatbars/blob/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0019-operation-vocabulary.mdx","changes":19,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0019-operation-vocabulary.mdx?ref=930e857b81d60b398247d555edf0315ef8cb99c9","deletions":9,"filename":"spec/src/content/docs/adr/adr-0019-operation-vocabulary.mdx","patch":"@@ -177,16 +177,17 @@ register and is exactly the two-register model applied at the boundary, not just\n   `registerOperation(name, fn[, arity])` affordance, mirroring `registerHelper`). RawBars is the\n   desugared core surface: it has *no surface sugar*, so a block head emits no `@hash`/`@param` markers — a\n   RawBars block operation therefore gets `options.fn`/`options.inverse` (and `options.fn(ctx, { data })`\n-  for scoped vars) but *no* `options.hash` and no `as |…|` block params (there is no surface to write\n+  for scoped vars) but *no* `options.hash` and no `as` block-param clause (there is no surface to write\n   them). Resolution stays *strict* — an unknown operation is `UnknownHelper`, never `blockHelperMissing`\n   (RawBars has no implicit-section policy; ADR-020's lenient resolve is FullBars/MaxBars only).\n * *MaxBars* — `renderWithOperations(...)` over the MaxBars surface (infix/pipes/loop vars). It desugars the\n   same `@hash`/`@param` markers, so a MaxBars block operation gets the *full* surface: `fn`/`inverse`/`hash`\n   + `options.fn(ctx, { data, blockParams })`, and the lenient `blockHelperMissing` resolve it inherits.\n-  Block params (`as |a b|`) parse because the MaxBars head grammar omits its top-level *pipe* rung, leaving\n-  a bar in head position to serve as the block-parameter delimiter — exactly as FullBars and the core\n-  parser already treat it. The only head-position consequence: to *pipe* a block argument, parenthesise it\n-  (`{{#each (xs | f) as |x|}}`), which re-enters the full expression grammar.\n+  Block params use the MaxBars **drop-pipes** spelling (`as a b`; the head grammar omits its top-level\n+  *pipe* rung and a bar in a head is a parse error — [ADR-021](/adr/adr-0021-maxbars-variable-model/)\n+  amendment), and `each` binds a third name to the 1-based index (`as a i0 i1`). The only head-position\n+  consequence: to *pipe* a block argument, parenthesise it (`{{#each (xs | f) as x}}`), which re-enters the\n+  full expression grammar.\n \n ### Mechanism — reuse, do not rebuild\n \n@@ -202,10 +203,10 @@ machinery:\n   `flatbars-js` facade.\n * `Ctl.hash`/`blockParams` are populated by the shared `blockArgs` seam regardless of dialect; RawBars sees\n   `Nothing`/`[]` (no markers), MaxBars sees both — so the *same* marshaller yields the per-dialect surface\n-  above with no per-dialect branching. (MaxBars producing `@param` markers required one dialect-local parser\n-  change — the head ladder dropping its pipe rung so `as |…|` survives as structure; see\n-  [the MaxBars page](/engine/maxbars/). No core or desugar change: `as` stays single-sourced in the surface\n-  desugar's `extractBlockParams`.)\n+  above with no per-dialect branching. (MaxBars producing `@param` markers required only the dialect-local\n+  drop-pipes head grammar — `as a b`, a bar in a head being a parse error ([ADR-021](/adr/adr-0021-maxbars-variable-model/)\n+  amendment); see [the MaxBars page](/engine/maxbars/). No core change: `as` stays single-sourced in the\n+  surface desugar's `extractBlockParams`, which selects the pipe vs drop-pipes spelling by dialect.)\n \n ### Gating\n ","raw_url":"https://github.com/wstein/flatbars/raw/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0019-operation-vocabulary.mdx","sha":"2de08b2a3e1f9a2fba15fdacdc18fc32f16d6309","status":"modified"},{"additions":35,"blob_url":"https://github.com/wstein/flatbars/blob/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0024-let-and-dict-literal.mdx","changes":43,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0024-let-and-dict-literal.mdx?ref=930e857b81d60b398247d555edf0315ef8cb99c9","deletions":8,"filename":"spec/src/content/docs/adr/adr-0024-let-and-dict-literal.mdx","patch":"@@ -4,7 +4,10 @@ sidebar:\n   label: \"ADR-024 `let` & dict literal\"\n ---\n \n-*Status:* Proposed (revised 2026-06-04 after team review — see *Resolved during review* below; the list\n+*Status:* **Partially accepted — the dict literal is implemented (2026-06-08); `{{#let}}` remains\n+proposed (not yet implemented).** The implemented dict literal uses **JSON-style** `{name: expr, …}`\n+(a `:` separator, comma-separated pairs), *revising* the proposed `{name=expr name=expr}` (`=`,\n+whitespace-separated) of §2 below — see the *Amendment*. Revised 2026-06-04 after team review (the list\n literal was split out to [ADR-025](/adr/adr-0025-list-literal/)).\n *Date:* 2026-06-04. Applies to *MaxBars and RawBars* only; *FullBars is unchanged*\n (Handlebars-faithful — see [ADR-009](/adr/adr-0009-fullbars/) and the conformance line at\n@@ -17,6 +20,20 @@ dict literal (which reuses the existing `dict` helper). The *list literal* needs\n in the shared prelude, a different blast radius, so it is its own decision —\n [ADR-025](/adr/adr-0025-list-literal/). The two ADRs share this Context and the lexing/dialect model.\n \n+*Amendment (2026-06-08, implementation).* The dict literal shipped as **`{name: expr, …}`** — a `:`\n+key/value separator, **comma**-separated pairs, keys a bare identifier or a quoted string — *not* the\n+proposed `{name=expr name=expr}` (§2). The reason is consistency with the [list\n+literal](/adr/adr-0025-list-literal/): a list **must** be comma-separated because MaxBars juxtaposition is\n+*application* (`[a b]` would parse as the single element `(a b)`), so the §\"JSON-style literals\" negative's\n+premise — that `:`/`,` could be avoided — does not hold once lists exist. With the list forced to commas,\n+a `=`/whitespace dict would split the two literals' surfaces gratuitously; one comma-separated family\n+(`[a, b]` / `{k: v, w: z}`) is the smaller surprise and the JS/JSON author's expectation. The ADR's *key*\n+decision is unchanged and upheld: distinct `{…}` (dict) vs `[…]` (list) shapes, so the empty-collection\n+case (`{}` vs `[]`) stays unambiguous. The `:` and `,` interior tokens are added behind\n+`LexOptions.collectionLiterals` (MaxBars-only). One delimiter caveat: a dict abutting the tag close needs a\n+space — `{{#with {a: 1} }}` — since `}}}` would otherwise close the tag early. The desugar target (the\n+`dict` helper) and the dialect scope (MaxBars sugar; RawBars writes `(dict …)`) are exactly as below.\n+\n ## Context\n \n Template authors recurringly need *template-local constants* — a forge-name → URL lookup table, a\n@@ -92,19 +109,21 @@ Semantics:\n * *Does not re-root context.* `{{#let u=user}}{{name}}{{/let}}` resolves `name` against the *root*, not\n   against `user`. To re-root, use `{{#with}}` (which stays — see below).\n \n-### 2. Dict literal `{name=expr name=expr …}` — MaxBars expression sugar\n+### 2. Dict literal `{name: expr, …}` — MaxBars expression sugar\n \n-A brace-delimited list of hash-arg pairs in expression position is sugar for `(dict …)`. The interior\n-syntax is FlatBars-native — *bare* keys, `=` separator, *whitespace*-separated pairs — no JSON\n-conventions are imported.\n+> *Implemented form (see the Amendment above).* A brace-delimited list of `key: value` pairs in\n+> expression position is sugar for `(dict …)`. Keys are a bare identifier or a quoted string; values are\n+> full expressions; pairs are comma-separated. *(The original proposal was `{name=expr name=expr}` —\n+> `=`-separated, whitespace-separated; revised to the JSON-style form for consistency with the\n+> comma-separated list literal.)*\n \n ```handlebars\n-{{#let forges={codeberg=\"…\" github=\"…\" gitlab=\"…\"}}}\n+{{#let forges={codeberg: \"…\", github: \"…\", gitlab: \"…\"} }}\n   …\n {{/let}}\n ```\n \n-Desugars at the MaxBars parser stage to `(dict codeberg=\"…\" github=\"…\" gitlab=\"…\")`. The `dict`\n+Desugars at the MaxBars parser stage to `(dict \"codeberg\" \"…\" \"github\" \"…\" \"gitlab\" \"…\")`. The `dict`\n helper already lives in the shared prelude ([prelude](/engine/prelude/)); *no kernel or prelude change* — this is\n the property that lets the dict literal ship ahead of the list literal.\n \n@@ -119,7 +138,15 @@ delimiter widths (`{{ }}` / `{{{ }}}` / `{{{{ }}}}`) intact: width detection run\n tag span; only then does the interior tokenizer see the literal braces. (ADR-025 extends the same\n principle to `[`/`]`; the rule is identical.)\n \n-The one consequence for the surface: inside an already-delimited tag, the interior tokenizer matches `{`\n+> *Implemented behavior (2026-06-08).* The structure lexer was kept **fully untouched** — it gained no\n+> brace-depth width detection — so the proposed \"authors never insert a space\" claim does **not** hold for a\n+> dict whose `}` abuts the tag close: `{{#with {a: 1}}}` would have the structure lexer match the *first*\n+> `}}` and split the dict. The fix is a single space — `{{#with {a: 1} }}` — and the simpler, smaller change\n+> (no structural-scanner surgery) was preferred over width detection. A `[…]` list needs no space (its `]`\n+> is not a tag-delimiter char). The original proposal below is retained for the rationale; the space caveat\n+> is the one deviation.\n+\n+The proposed consequence for the surface: inside an already-delimited tag, the interior tokenizer matches `{`\n against its closing `}` by nesting depth, so a literal collection's own closing brace is consumed before\n the tag close. `{{#let x={a=1}}}` therefore reads as `#let x=` · `{a=1}` · `}}` (dict-close, then\n tag-close), and the nested `{{#let x={a={b=1}}}}}` as `{a={b=1}}` · `}}` — no ambiguity, because","raw_url":"https://github.com/wstein/flatbars/raw/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0024-let-and-dict-literal.mdx","sha":"31d760bcb36838cf7013dd5f719bd14c6390e858","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/flatbars/blob/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0025-list-literal.mdx","changes":22,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0025-list-literal.mdx?ref=930e857b81d60b398247d555edf0315ef8cb99c9","deletions":6,"filename":"spec/src/content/docs/adr/adr-0025-list-literal.mdx","patch":"@@ -4,12 +4,22 @@ sidebar:\n   label: ADR-025 list literal\n ---\n \n-*Status:* Proposed. *Date:* 2026-06-04. The list-literal half of the collection-literal design,\n-*split from* [ADR-024](/adr/adr-0024-let-and-dict-literal/) during the 2026-06-04 team review.\n-Applies to *MaxBars* (surface sugar) and the *shared prelude* (a new constructor). FullBars's surface is\n-*unchanged*; the prelude addition is value-only and Handlebars-compatible (gated by\n+*Status:* **Accepted — implemented 2026-06-08.** *Date:* 2026-06-04. The list-literal half of the\n+collection-literal design, *split from* [ADR-024](/adr/adr-0024-let-and-dict-literal/) during the\n+2026-06-04 team review. Applies to *MaxBars* (surface sugar) and the *shared prelude* (a new constructor).\n+FullBars's surface is *unchanged*; the prelude addition is value-only and Handlebars-compatible (gated by\n `check:hbs-conformance`).\n \n+*Amendment (2026-06-08, implementation).* The list literal shipped as **comma**-separated —\n+`[e1, e2, …]`, *not* the whitespace-separated `[expr expr …]` of §2. This **resolves the *Open question*'s\n+tokenizer spike** in the only way the MaxBars grammar allows: juxtaposition is *application* there\n+(`{{ f a b }}` ⇒ `(f a b)`), so a whitespace-separated `[a b]` would be the single element `(a b)`, not two\n+elements — a separator is mandatory. The comma matches the [dict literal](/adr/adr-0024-let-and-dict-literal/),\n+which the same review revised to JSON-style for the same consistency. The `[`/`.[`/`]` disambiguation\n+holds as in §3: a *leading* `[` is unconditionally a list-open and a *mid-identifier* `[seg]` (the `a.[k]`\n+path-bracket, consumed inside `readIdent`) is a path index — the two never compete. The `list` helper and\n+the dialect scope (§1, §4) ship exactly as specified.\n+\n This is a separate ADR from [ADR-024](/adr/adr-0024-let-and-dict-literal/) for one reason: the\n list literal is *not* pure MaxBars surface. Its desugar target does not exist, so it ships a new helper\n into the *shared* prelude — a different blast radius (catalog regen, conformance re-run, presence in\n@@ -51,13 +61,13 @@ a *shared-prelude* change, with the catalog + conformance consequences in *Gates\n A bracket-delimited list of positional expressions in expression position is sugar for `(list …)`.\n \n ```handlebars\n-{{#let langs=[\"PureScript\" \"TypeScript\" \"Rust\"]}}\n+{{#let langs=[\"PureScript\", \"TypeScript\", \"Rust\"]}}\n   {{#each langs}} — {{this}}{{/each}}\n {{/let}}\n ```\n \n Desugars at the MaxBars parser stage to `(list \"PureScript\" \"TypeScript\" \"Rust\")`. Empty: `[]` is\n-the empty list, lowers to `(list)`. The sugar is MaxBars-only; RawBars writes `(list …)` directly;\n+the empty list, lowers to `(list)`. (Implemented form: comma-separated — see the *Amendment* above.) The sugar is MaxBars-only; RawBars writes `(list …)` directly;\n FullBars has neither the sugar nor (by convention) uses it, though the `list` helper is *present* in its\n prelude.\n ","raw_url":"https://github.com/wstein/flatbars/raw/930e857b81d60b398247d555edf0315ef8cb99c9/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0025-list-literal.mdx","sha":"f8b1a06551ded7dd677bcadfcf3963c191437d14","status":"modified"}]}