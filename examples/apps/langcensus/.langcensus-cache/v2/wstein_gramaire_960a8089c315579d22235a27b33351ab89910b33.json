{"files":[{"additions":5,"blob_url":"https://github.com/wstein/gramaire/blob/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala","changes":6,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala?ref=960a8089c315579d22235a27b33351ab89910b33","deletions":1,"filename":"core/src/main/scala/gramaire/BackendAntlr.scala","patch":"@@ -5,6 +5,9 @@ package gramaire\n // becomes a parser rule, each token class a lexer rule. The reverse\n // direction (ANTLR -> Gramaire, ConvertAntlr) is the hard half.\n // Ported from src/Gramaire/Backend/Antlr.purs.\n+//\n+// Also consumes `IRNonterminal.comment` (ADR D39) to reconstruct each parser rule's own leading\n+// `/* … */` doc comment — mirrors `BackendBison`'s own use of the same field exactly.\n object BackendAntlr:\n   val backend: Backend = Backend(\n     name = \"antlr\",\n@@ -125,12 +128,13 @@ object BackendAntlr:\n \n     def parserRule(nt: IRNonterminal): String =\n       val alts = ir.grammar.rules.filter(_.lhs == nt.id)\n-      alts.map(altText) match\n+      val body = alts.map(altText) match\n         case Vector() => s\"${ruleName(nt.name)} : /* (no productions) */ ;\\n\"\n         case texts =>\n           s\"${ruleName(nt.name)}\\n  : ${texts.head}\" + texts.tail\n             .map(b => s\"\\n  | $b\")\n             .mkString + \"\\n  ;\\n\"\n+      nt.comment.map(c => s\"/* $c */\\n\").getOrElse(\"\") + body\n \n     def lexerRule(lx: IRLexer, tid: Int): Option[String] =\n       for","raw_url":"https://github.com/wstein/gramaire/raw/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala","sha":"ddcd2a3171d6b7ca11677c1730948099e1375b56","status":"modified"},{"additions":86,"blob_url":"https://github.com/wstein/gramaire/blob/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala","changes":105,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala?ref=960a8089c315579d22235a27b33351ab89910b33","deletions":19,"filename":"core/src/main/scala/gramaire/ConvertAntlr.scala","patch":"@@ -10,6 +10,14 @@ package gramaire\n // The parser is a small hand-written recursive descent over a `.g4`\n // token stream.\n // Ported from src/Gramaire/Convert/Antlr.purs.\n+//\n+// Also carries ADR D39's comment round-trip (mirroring `ConvertBison`'s own): a `/* ... */` or\n+// `//` comment immediately preceding a parser rule's own `name :` head (no other real token in\n+// between since the previous rule's own `;`) becomes that rule's own leading prose in the\n+// rendered `.gram.md` — `Lr.docCommentsOf` reads it back out on re-parse. ANTLR itself has no\n+// distinct doc-comment token of its own — a `/** ... */` is lexically just an ordinary block\n+// comment — so plain `//`/`/* */` capture, identical to what `ConvertBison` already does, is\n+// sufficient here too.\n \n /** The result of an import: the rendered `.gram.md` and any features that could not be represented\n   * and were dropped.\n@@ -31,6 +39,13 @@ object ConvertAntlr:\n     case TArrow // -> lexer command\n     case TEq // = / += element-label binders\n     case TComma\n+    // `blankBefore`: whether a blank line (or start-of-input) precedes this comment — the ADR\n+    // D39 doc-comment heuristic (see `leadingDoc`) that a comment is a rule's own LEADING prose\n+    // only when set off from whatever came before by a blank line; otherwise it's a TRAILING\n+    // note on the previous rule (e.g. `r : 'x' ; // note`), which must not migrate onto the next\n+    // rule just because the token stream drops the `;` between them.\n+    case TLineComment(s: String, blankBefore: Boolean) // // … (inner text, trimmed)\n+    case TBlockComment(s: String, blankBefore: Boolean) // /* … */ (inner text, trimmed)\n \n   import Tok.*\n \n@@ -62,18 +77,37 @@ object ConvertAntlr:\n       case ',' => Some(TComma)\n       case _   => None\n \n+    // Stops AT the newline itself (not past it) — `TLineComment`'s own slice excludes it, and\n+    // `.trim` mops up whatever whitespace remains; mirrors `ConvertBison.lexBison`'s own\n+    // `lineEnd`. `case None => j` matches EOF (an unterminated `//` running to end-of-file).\n     def lineEnd(j: Int): Int = at(j) match\n-      case Some('\\n') => j + 1\n+      case Some('\\n') => j\n       case Some(_)    => lineEnd(j + 1)\n       case None       => j\n-    def blockEnd(j: Int): Int = at(j) match\n-      case Some('*') if at(j + 1) == Some('/') => j + 2\n-      case Some(_)                             => blockEnd(j + 1)\n+    // Stops AT the `*` of the closing `*/` (not past it) — mirrors `ConvertBison.lexBison`'s\n+    // own `blockCommentEnd`; the caller advances past the `*/` itself separately.\n+    def blockCommentEnd(j: Int): Int = at(j) match\n+      case Some('*') if at(j + 1) == Some('/') => j\n+      case Some(_)                             => blockCommentEnd(j + 1)\n       case None                                => j\n     def identEnd(j: Int): Int = at(j) match\n       case Some(d) if isIdentPart(d) => identEnd(j + 1)\n       case _                         => j\n \n+    // Scans BACKWARD from `j` (a comment's own start offset) through pure whitespace, counting\n+    // newlines — true if it hits a blank line (2+ newlines) or the start of the input before\n+    // any non-whitespace character. See `Tok.TLineComment`/`TBlockComment`'s own doc comment,\n+    // and `ConvertBison.lexBison`'s identical `blankLineBefore`, for why this matters.\n+    def blankLineBefore(j: Int): Boolean =\n+      def back(k: Int, newlines: Int): Boolean =\n+        if k < 0 then true\n+        else\n+          src.charAt(k) match\n+            case '\\n'              => back(k - 1, newlines + 1)\n+            case ' ' | '\\t' | '\\r' => back(k - 1, newlines)\n+            case _                 => newlines >= 2\n+      back(j - 1, 0)\n+\n     def go(i: Int, acc: Vector[Tok]): Either[String, Vector[Tok]] =\n       if i >= len then Right(acc)\n       else\n@@ -110,9 +144,13 @@ object ConvertAntlr:\n           go(identEnd(j), acc :+ TId(slice(start, identEnd(j))))\n \n         at(i) match\n-          case Some(c) if isSpace(c)               => go(i + 1, acc)\n-          case Some('/') if at(i + 1) == Some('/') => go(lineEnd(i + 2), acc)\n-          case Some('/') if at(i + 1) == Some('*') => go(blockEnd(i + 2), acc)\n+          case Some(c) if isSpace(c) => go(i + 1, acc)\n+          case Some('/') if at(i + 1) == Some('/') =>\n+            val e = lineEnd(i + 2)\n+            go(e, acc :+ TLineComment(slice(i + 2, e).trim, blankLineBefore(i)))\n+          case Some('/') if at(i + 1) == Some('*') =>\n+            val e = blockCommentEnd(i + 2)\n+            go(math.min(e + 2, len), acc :+ TBlockComment(slice(i + 2, e).trim, blankLineBefore(i)))\n           case Some('\\'')                          => str(i + 1, StringBuilder())\n           case Some('[')                           => setLit(i + 1, StringBuilder())\n           case Some('{')                           => action(i + 1, 1, StringBuilder())\n@@ -164,7 +202,8 @@ object ConvertAntlr:\n       name: String,\n       lexer: Boolean,\n       alts: Vector[Vector[Elem]],\n-      skip: Boolean\n+      skip: Boolean,\n+      doc: Option[String] = None\n   )\n   private final case class Parsed(name: String, rules: Vector[G4Rule], warnings: Vector[String])\n \n@@ -301,16 +340,43 @@ object ConvertAntlr:\n       case TId(\"import\") :: tail => skipPrequel(dropThrough(TSemi, tail))\n       case _                     => ts\n \n-  private def rulesOf(ts: List[Tok], acc: Vector[G4Rule]): Either[String, Vector[G4Rule]] =\n-    skipPrequel(ts) match\n-      case Nil => Right(acc)\n-      case ts2 =>\n-        ts2 match\n-          case TId(\"mode\") :: tail => rulesOf(dropThrough(TSemi, tail), acc)\n-          case _ =>\n-            parseRule(ts2) match\n-              case Left(e)             => Left(e)\n-              case Right((rule, rest)) => rulesOf(rest, acc :+ rule)\n+  // A rule's own leading doc-comment (ADR D39, mirroring `ConvertBison.leadingDoc` exactly):\n+  // comments immediately preceding its name, back to (and including) the last one set off from\n+  // whatever came before by a blank line. A comment with NO blank line before it — right after\n+  // the previous rule's own `;` — is that PREVIOUS rule's trailing note, not this rule's\n+  // heading, and every comment before the last blank-line-set-off one is discarded rather than\n+  // misattributed to this rule.\n+  private def leadingDoc(pending: Vector[(String, Boolean)]): Option[String] =\n+    pending.lastIndexWhere(_._2) match\n+      case -1 => None\n+      case i  => Some(pending.drop(i).map(_._1).mkString(\"\\n\").trim).filter(_.nonEmpty)\n+\n+  private def rulesOf(ts0: List[Tok], acc0: Vector[G4Rule]): Either[String, Vector[G4Rule]] =\n+    def go(\n+        ts: List[Tok],\n+        pendingDoc: Vector[(String, Boolean)],\n+        acc: Vector[G4Rule]\n+    ): Either[String, Vector[G4Rule]] = ts match\n+      case Nil                             => Right(acc)\n+      case TLineComment(s, blank) :: tail  => go(tail, pendingDoc :+ (s, blank), acc)\n+      case TBlockComment(s, blank) :: tail => go(tail, pendingDoc :+ (s, blank), acc)\n+      case _ =>\n+        val stripped = skipPrequel(ts)\n+        // A prequel construct (options/tokens/channels/@header/import) actually consumed here\n+        // is decl-like, not a rule — any pending comment run belonged to IT (or nothing), never\n+        // to whatever real rule eventually follows, so it's discarded here exactly as\n+        // `ConvertBison.parseDecls` unconditionally discards every comment in its own\n+        // declarations section.\n+        if stripped != ts then go(stripped, Vector.empty, acc)\n+        else\n+          stripped match\n+            case TId(\"mode\") :: tail => go(dropThrough(TSemi, tail), Vector.empty, acc)\n+            case _ =>\n+              parseRule(stripped) match\n+                case Left(e) => Left(e)\n+                case Right((rule, rest)) =>\n+                  go(rest, Vector.empty, acc :+ rule.copy(doc = leadingDoc(pendingDoc)))\n+    go(ts0, Vector.empty, acc0)\n \n   private def dropToGrammar(ts: List[Tok]): Option[List[Tok]] = ts match\n     case TId(\"grammar\") :: tail => Some(tail)\n@@ -581,7 +647,8 @@ object ConvertAntlr:\n         )\n \n     def ruleSection(r: G4Rule): String =\n-      s\"## ${r.name}\\n\\n```gramaire\\n${r.name}\\n  : \" + r.alts\n+      val doc = r.doc.map(d => d + \"\\n\\n\").getOrElse(\"\")\n+      s\"## ${r.name}\\n\\n$doc```gramaire\\n${r.name}\\n  : \" + r.alts\n         .map(renderTopAlt)\n         .mkString(\"\\n  | \") + \"\\n  ;\\n```\\n\"\n ","raw_url":"https://github.com/wstein/gramaire/raw/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala","sha":"ef1f364724a325fc110fd59ff2c6fe8ff95e3a8e","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/gramaire/blob/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala","changes":15,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala?ref=960a8089c315579d22235a27b33351ab89910b33","deletions":0,"filename":"core/src/test/scala/gramaire/BackendAntlrSuite.scala","patch":"@@ -23,6 +23,21 @@ class BackendAntlrSuite extends munit.FunSuite:\n         )\n   }\n \n+  test(\"IRNonterminal.comment is rendered as a leading /* ... */ block comment above its rule\") {\n+    IR.buildIR(Method.Canonical, \"Tiny\", tiny) match\n+      case Left(_) => fail(\"tiny grammar should build\")\n+      case Right(ir0) =>\n+        val ir = ir0.copy(grammar =\n+          ir0.grammar.copy(nonterminals =\n+            ir0.grammar.nonterminals.map(n =>\n+              if n.name == \"S\" then n.copy(comment = Some(\"The start rule.\")) else n\n+            )\n+          )\n+        )\n+        val g4 = BackendAntlr.emit(ir)\n+        assert(g4.contains(\"/* The start rule. */\\ns\\n\"), s\"expected a leading comment on s:\\n$g4\")\n+  }\n+\n   test(\"regex→ANTLR keeps char classes, negates with ~, strips (?:, quotes literals\") {\n     assertEquals(BackendAntlr.regexToAntlr(\"[0-9]+\"), \"[0-9]+\")\n     assertEquals(BackendAntlr.regexToAntlr(\"[ \\t\\r\\n]+\"), \"[ \\t\\r\\n]+\")","raw_url":"https://github.com/wstein/gramaire/raw/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala","sha":"c383399a49830cdb2bd0e01fa43e784ea4e53b92","status":"modified"},{"additions":74,"blob_url":"https://github.com/wstein/gramaire/blob/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala","changes":74,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala?ref=960a8089c315579d22235a27b33351ab89910b33","deletions":0,"filename":"core/src/test/scala/gramaire/ConvertAntlrSuite.scala","patch":"@@ -18,6 +18,23 @@ class ConvertAntlrSuite extends munit.FunSuite:\n       |WS     : [ \\t\\r\\n]+ -> skip ;\n       |\"\"\".stripMargin\n \n+  // Mirrors `calcG4` exactly, plus a leading doc comment (ADR D39) on two of its three parser\n+  // rules — one `/* … */` block comment, one `//` line comment — to exercise the ANTLR\n+  // converter's own comment round-trip (mirroring `ConvertBisonSuite.calcY`).\n+  private val calcDocG4: String =\n+    \"\"\"grammar CalcDoc;\n+      |\n+      |/* An expression: a sum or difference of terms. */\n+      |expr   : expr '+' term | expr '-' term | term ;\n+      |\n+      |// A term: a product or quotient of factors.\n+      |term   : term '*' factor | term '/' factor | factor ;\n+      |\n+      |factor : '(' expr ')' | NUMBER ;\n+      |NUMBER : [0-9]+ ;\n+      |WS     : [ \\t\\r\\n]+ -> skip ;\n+      |\"\"\".stripMargin\n+\n   // A grammar exercising an action: no Core equivalent, always dropped.\n   private val flaggedG4: String =\n     \"\"\"grammar P;\n@@ -107,6 +124,63 @@ class ConvertAntlrSuite extends munit.FunSuite:\n                   case Right(imp2) => assertEquals(imp2.markdown, imp.markdown)\n   }\n \n+  test(\"convert: a rule's own leading comment (// or /* */) survives the full round trip\") {\n+    ConvertAntlr.importAntlr(calcDocG4) match\n+      case Left(e) => fail(s\"calcDoc.g4 should import: $e\")\n+      case Right(imp) =>\n+        assert(\n+          imp.markdown.contains(\"An expression: a sum or difference of terms.\"),\n+          \"the block comment becomes expr's own leading prose\"\n+        )\n+        assert(\n+          imp.markdown.contains(\"A term: a product or quotient of factors.\"),\n+          \"the line comment becomes term's own leading prose\"\n+        )\n+        Lr.parse(imp.markdown) match\n+          case Left(e) => fail(s\"imported markdown should parse: $e\")\n+          case Right(g) =>\n+            val g2 = Lr.withDocComments(g, imp.markdown)\n+            IR.buildIRWithTokens(defsOf(imp.markdown), Method.Canonical, \"CalcDoc\", g2) match\n+              case Left(_) => fail(\"imported grammar should build an IR\")\n+              case Right(ir) =>\n+                val g4b = BackendAntlr.emit(ir)\n+                assert(\n+                  g4b.contains(\"/* An expression: a sum or difference of terms. */\"),\n+                  \"the comment survives re-export as an ANTLR block comment above the rule\"\n+                )\n+                ConvertAntlr.importAntlr(g4b) match\n+                  case Left(e) => fail(s\"re-export should re-import: $e\")\n+                  case Right(imp2) =>\n+                    assert(\n+                      imp2.markdown.contains(\"An expression: a sum or difference of terms.\"),\n+                      \"the comment survives a full round trip, not just one direction\"\n+                    )\n+  }\n+\n+  test(\n+    \"convert: a trailing comment right after a rule's `;` stays with that rule, not the next one\"\n+  ) {\n+    val g4 =\n+      \"\"\"grammar P;\n+        |foo : 'x' ;  // trailing note about foo, not about bar\n+        |\n+        |/* bar's own real leading doc */\n+        |bar : 'y' ;\n+        |\"\"\".stripMargin\n+    ConvertAntlr.importAntlr(g4) match\n+      case Left(e) => fail(s\"should import: $e\")\n+      case Right(imp) =>\n+        assert(\n+          !imp.markdown.contains(\"trailing note about foo\"),\n+          \"a same-line trailing comment on the previous rule must not be carried anywhere, \" +\n+            \"and specifically must not migrate onto the next rule as its own leading doc\"\n+        )\n+        assert(\n+          imp.markdown.contains(\"bar's own real leading doc\"),\n+          \"the genuinely blank-line-separated leading comment for `bar` still survives\"\n+        )\n+  }\n+\n   test(\"convert: an action with no Core equivalent is flagged and dropped, not invented\") {\n     ConvertAntlr.importAntlr(flaggedG4) match\n       case Left(e) => fail(s\"flagged.g4 should import: $e\")","raw_url":"https://github.com/wstein/gramaire/raw/960a8089c315579d22235a27b33351ab89910b33/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala","sha":"8a358bef52770708790ae3211213a9cb4d9c9784","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/960a8089c315579d22235a27b33351ab89910b33/docs%2Fmulti-backend-implementation-plan.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=960a8089c315579d22235a27b33351ab89910b33","deletions":1,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -735,7 +735,7 @@ everything target-specific is independent.\n | D36 | Raw .gram projection       | **`.gram.md` is the SINGLE source of truth. `.gram` is a DERIVED, FENCE-FREE projection (`gramaire strip`), ANTLR-style: ALL-CAPS lexer + Mixed-case parser rules told apart by case, `%left`/`%right` kept. Prose → `/** */` banner + `///` doc-comment lines holding Markdown (skipped on parse); headings, errors, diagrams & tables dropped. Author only in `.gram.md`. `parse(strip(x)) == parse(x)`.**                                                                                                                                                                                       | Gramaire's differentiator is that the grammar IS lint-clean Markdown rendering as docs — a second editable format is the sync/trust nightmare the bet avoids. But the ecosystem (editors, linguist, generators, review) expects a plain grammar file that does NOT read as Markdown, so a one-way EXPORT serves it. Fence-free and marker-free because the ```gramaire fences made `.gram` look like Markdown and Gramaire, like ANTLR, tells lexer from parser rules by case. Prose is preserved as doc comments — a `/** */` banner for the title/intro, `///` per section — carrying the Markdown verbatim (a Sphinx site consumes it via the MyST-Parser extension; no Markdown→RST translation), so the docs travel with the projection; `toFenced` skips them, and only sections carrying a gramaire block survive. Rejected: raw as co-canonical / canonical input; reStructuredText `..` comment markers (tried in a since-reverted commit — the delimiters are doc comments, the content stays Markdown); yacc `%%` sections; `$N` positional actions. The reverse (`.gram` comments → `.gram.md` prose) is a future scaffold. |\n | D37 | LR precedence + bans       | **Shift/reduce conflicts resolved the yacc/LR way: `## Precedence`'s `%left`/`%right`/`%nonassoc` set terminal precedence (later binds tighter), a production's is its last terminal's; on a tie `%left` reduces, `%right` shifts. HARD RULE: a conflict no declaration covers — incl. reduce/reduce — still surfaces. `%prec` per-rule override deferred.**                                                                                                                                                                                                                                      | Gramaire is LR, so precedence resolution is native and proven (the yacc family); ANTLR's precedence-climbing is an LL technique foreign to a bottom-up engine. Stratification (expr/term/factor) is O(levels) and unreadable past a few; `%left` is one line per level. The yacc footgun — SILENT conflict resolution — is closed by keeping `explain-conflict` watching: declared precedence resolves, undeclared ambiguity throws (buildTablesForP; Test.Conflict). REJECTED borrows (so they aren't relitigated): ANTLR structural precedence (alt-order/climbing, LL-only); yacc `%%` section structure (Markdown `##` sections already do this); `$N` positional actions (unreadable, untyped, an injection temptation — `{% %}` lambdas and `# Label` visitors stay).                                                                                                                                                                                                                                                                                                                                                          |\n | D38 | Bison `.y` interop         | **Shipped. `gramaire import <file.y\\|.yy>` (`ConvertBison.importBison`) and `gramaire emit --backend bison` (`BackendBison`), mirroring the ANTLR converter pair; `gramaire import` dispatches by file extension. Precedence reuses D37 (`%left`/`%right`/`%nonassoc` on quoted literals only — a bare token name in one of those lines is dropped and flagged, matching `Table.parsePrecedence`'s own quoted-literal-only grammar); the prologue/epilogue (`%{ … %}`) and `$N` actions are flagged and dropped, exactly as the ANTLR import drops predicates/actions; `%prec` overrides are likewise dropped and flagged (D37 defers per-rule overrides). `%start` reorders the named rule to lead the output, since Gramaire has no start declaration of its own. Because Bison's file is not uniformly lexable in one pass the way ANTLR's `.g4` is — the epilogue after the second bare `%%` is arbitrary, unconstrained C — the importer first runs a raw character-level pre-scan to find the two bare `%%` section boundaries (skipping over comments/quoted strings/`%{…%}` blocks), and only tokenizes the declarations and rules sections. Verified via a full round trip (`.y → import → parse → IR (real precedence) → emit → re-import`) reaching a byte-identical fixed point with zero warnings on re-import, plus a golden test (`test/golden/calc.y`).**                                                                                                                                                                       | Bison/yacc is the other grammar lingua franca; a converter widens Gramaire's reach and, alongside the ANTLR one, makes cross-format round-tripping possible. It rides existing seams: the IR is the interchange, precedence is already yacc-shaped (D37), and the \"skip the untranslatable prelude, flag it\" import strategy is proven on ANTLR (D3/D4). Full parity (union types, error productions, `%glr-parser`) is out of scope; the goal is the Core — rules, alternatives, `%`-precedence — round-tripping cleanly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |\n-| D39 | Cross-format comments      | **Shipped, first concrete step (ANTLR carry-through remains a future extension). A new optional `Rule.doc: Option[String]` field (Core) threads through to `IRNonterminal.comment` (IR stays version 0 — additive, omitted when empty). Populated only via an explicit opt-in (`Lr.docCommentsOf`/`Lr.withDocComments`) — never by `Lr.parseWith`/`parse` themselves, to protect `SelfHostSuite`'s exact-equality bootstrap check. `ConvertBison`'s import attaches each Bison rule's leading `//`/`/* */` comment as its Gramaire section's leading prose; `BackendBison.emit` renders `IRNonterminal.comment` back as a leading `/* … */` block comment. A rule's doc comment survives the full round trip: import → parse → `withDocComments` → IR → re-export → re-import, unchanged.**                                                                                                                    | Comments died at the IR boundary before this (the IR carried no prose), so every conversion was documentation-lossy. One optional prose field, plus the explicit-opt-in design, lets docs survive `.y` ↔ `.gram` round trips — the \"best-practice conversion\" goal — without risking the exact-equality self-host bootstrap test or the byte-stability of existing goldens (the field is omitted whenever absent). Rejected: a second, format-specific comment channel per target (the sync nightmare D36 avoids). Not yet done: carrying ANTLR import/export comments through the same field (D38's Bison converter was the first consumer; ANTLR's converter doesn't populate `Rule.doc` yet).                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n+| D39 | Cross-format comments      | **Shipped for both converter pairs (the ANTLR carry-through landed as a direct follow-up to the Bison-first rollout, closing the gap this row used to flag). A new optional `Rule.doc: Option[String]` field (Core) threads through to `IRNonterminal.comment` (IR stays version 0 — additive, omitted when empty). Populated only via an explicit opt-in (`Lr.docCommentsOf`/`Lr.withDocComments`) — never by `Lr.parseWith`/`parse` themselves, to protect `SelfHostSuite`'s exact-equality bootstrap check. `ConvertBison`'s import attaches each Bison rule's leading `//`/`/* */` comment as its Gramaire section's leading prose; `BackendBison.emit` renders `IRNonterminal.comment` back as a leading `/* … */` block comment. `ConvertAntlr`/`BackendAntlr` now mirror this exactly for `.g4`: `lexG4` emits `TLineComment`/`TBlockComment` tokens (each carrying a `blankBefore` flag, same backward blank-line scan as Bison's), a `leadingDoc` helper attributes a comment run to the FOLLOWING rule only when set off by a blank line (otherwise it's the PREVIOUS rule's trailing note), `G4Rule.doc` carries it through `rulesOf`, and `BackendAntlr.emit` renders `IRNonterminal.comment` back as a leading `/* … */` above the parser rule. A rule's doc comment survives the full round trip in EITHER format: import → parse → `withDocComments` → IR → re-export → re-import, unchanged.**                                                                                                                    | Comments died at the IR boundary before this (the IR carried no prose), so every conversion was documentation-lossy. One optional prose field, plus the explicit-opt-in design, lets docs survive `.y`/`.g4` ↔ `.gram` round trips — the \"best-practice conversion\" goal — without risking the exact-equality self-host bootstrap test or the byte-stability of existing goldens (the field is omitted whenever absent). Rejected: a second, format-specific comment channel per target (the sync nightmare D36 avoids). ANTLR import/export comments now carry through the same field, same as Bison's (D38's Bison converter was the first consumer; ANTLR's followed, reusing the identical lexer/heuristic shape).                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n | D40 | GLR + ALL(\\*) coexist      | **Both retained, not merged. LR(1)/LALR/IELR is the self-hosted core. `Gramaire.Glr` is the generalized recognizer + diagnostics engine: `forest` enumerates all parses (proving ambiguity) and `explainP` powers `explain-conflict` (LALR-artifact vs resolved vs genuine). `Gramaire.Ll` is the ALL(\\*) `ll-star` strategy: top-down ATN-driven prediction that commits to one alternative, and the shared ATN substrate for ANTLR (and future Bison) interop.**                                                                                                                                  | Answers \"drop GLR for ALL(\\*)?\": no — they solve different problems. ALL(\\*) *resolves* ambiguity (picks one alternative by prediction, like ANTLR); GLR *enumerates* it (the forest), which is what proves a grammar ambiguous and what the conflict differential needs. Replacing GLR with ALL(\\*) would silently drop ambiguity reporting and gut `explain-conflict`, while ALL(\\*) still earns its keep as the opt-in top-down strategy and the ATN foundation the foreign-format converters share. The one lateral move considered — shrinking GLR to an Earley diagnostic recognizer — is deferred, not a replacement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |\n | D41 | Context-sensitivity        | **Post-parse CST fold is canonical for symbol-table/declared-before-use checks (extends D7/D24/D25's action-as-catamorphism model); ANTLR-style semantic predicates, if built, are scoped exclusively to the `ll-star` ATN strategy (never Core/LR, never the scanner) and gated on an IR-level effect declaration so they survive backend export; the Bison/Yacc \"lexer hack\" (parser-fed scanner state) is rejected outright.**                                                                                                                                                                 | The verified LR(1)/LALR/IELR core and the self-host lexer oracle (`Test.LexerSelfHost`) depend on tokenization being parse-independent — a parser-driven scanner would invalidate that guarantee for a narrow C-typedef-style win that `%external` (D33) already covers for genuinely non-regular lexis. A pure fold over the finished CST reuses the existing action/Visitor machinery (D24/D25) with zero engine risk and no interaction with incremental reparse (D18/D22), so it is the default answer. Predicates are real (ANTLR's mechanism, and the ATN substrate already exists per D40) but only make sense where prediction happens — top-down ALL(\\*) — and must declare their symbol-table effects in the IR before Phase 3's `.g4` import work relies on them, or ANTLR-imported predicates silently vanish on re-export.                                                                                                                                                                                                                                                                                             |\n | D42 | IR predicate effect        | **`rules[].predicate: { reads: string[], writes: string[] }` — optional, additive (irVersion stays 0). Presence flags the rule's action an ALL(\\*) semantic predicate (D-predicates) rather than a value-building one; the declared keys are author-chosen and opaque to the core (no built-in symbol-table mechanism exists) — they exist purely so a consumer can reason about or invalidate a predicate without evaluating its opaque body. Ships as inert plumbing: no front-end producer populates it yet (`{%? %}` stays unparsed), built ahead of that wiring, same as `atn`/`glr` were.** | Unblocks two dependents stuck waiting on \"design it first\": the ANTLR importer's predicate carry-through (`all-star-port-plan.md` Phase 3 — the field existing doesn't mean the importer populates it yet, that wiring is still separate) and `incremental-spec.md`'s R22/R23 predicate-invalidation rules, which need `reads`/`writes` to reference as something concrete rather than a promise. Mirrors the additive-optional-field pattern already used for `extras`/`recovery`/`glr`/`atn`/`lexer` (`IR.scala`, `IRDecode.scala`, `IRValidate.scala`, `ir-schema.json`) exactly — encode omits the key when absent, decode/validate are presence-guarded, and the committed goldens (`test/golden/*.ir.json`) stay byte-unchanged (`IRGoldenSuite`).                                                                                                                                                                                                                                                                                                                                                                            |","raw_url":"https://github.com/wstein/gramaire/raw/960a8089c315579d22235a27b33351ab89910b33/docs%2Fmulti-backend-implementation-plan.md","sha":"82a92a4d49d1436b64f9cdd1ecdf2db8d75efaff","status":"modified"}]}