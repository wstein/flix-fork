{"files":[{"additions":70,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDiagnosticsGoldenSuite.scala","changes":71,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDiagnosticsGoldenSuite.scala?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":1,"filename":"core/.jvm/src/test/scala/gramaire/DiagnosticsGoldenSuite.scala","patch":"@@ -71,7 +71,9 @@ class DiagnosticsGoldenSuite extends munit.FunSuite:\n         )\n   }\n \n-  test(\"parse error: a missing rule-head newline is located, with an expected-token note\") {\n+  test(\n+    \"parse error: two bare identifiers with no ':' at all is located, with an expected-token note\"\n+  ) {\n     val md = \"\"\"# Broken\n       |\n       |## Foo\n@@ -94,6 +96,20 @@ class DiagnosticsGoldenSuite extends munit.FunSuite:\n         )\n   }\n \n+  test(\"a single-line rule head (no newline before ':') is no longer a parse error (D59)\") {\n+    val md = \"\"\"# Ok\n+      |\n+      |## Foo\n+      |\n+      |```gramaire\n+      |Foo : 'x' ;\n+      |```\n+      |\"\"\".stripMargin\n+    Lr.parseWith(Method.Canonical, md) match\n+      case Left(diags) => fail(s\"expected a clean parse, got: $diags\")\n+      case Right(g)    => assertEquals(g.rules.map(_.name), Vector(\"Foo\"))\n+  }\n+\n   test(\"undefined nonterminal: one diagnostic per reference site, with a did-you-mean\") {\n     val md = \"\"\"# Calc\n       |\n@@ -137,6 +153,59 @@ class DiagnosticsGoldenSuite extends munit.FunSuite:\n         )\n   }\n \n+  test(\n+    \"undefined token class: an ALL-CAPS reference not declared in the document's own Tokens block (D60)\"\n+  ) {\n+    val md = \"\"\"# Calc\n+      |\n+      |## Tokens\n+      |\n+      |```gramaire\n+      |NUMBER : /[0-9]+/ ;\n+      |```\n+      |\n+      |## Factor\n+      |\n+      |```gramaire\n+      |Factor\n+      |: NUMBR\n+      |;\n+      |```\n+      |\"\"\".stripMargin\n+    Lr.parseWith(Method.Canonical, md) match\n+      case Right(_) => fail(\"expected an undefined-token-class diagnostic\")\n+      case Left(diags) =>\n+        assertEquals(diags.length, 1)\n+        assertEquals(\n+          Diagnostic.render(diags.head, \"calc.gram.md\", Lr.toFenced(md)),\n+          \"\"\"error: undefined token class `NUMBR`\n+            |  --> calc.gram.md:13:3\n+            |    : NUMBR\n+            |      ^^^^^\n+            |  note: an ALL-CAPS name must be declared in this document's own `## Tokens` block\n+            |  help: did you mean the token class `NUMBER`?\"\"\".stripMargin\n+        )\n+  }\n+\n+  test(\n+    \"an ALL-CAPS reference with no ## Tokens block at all is unchecked (whole-grammar external mode)\"\n+  ) {\n+    val md = \"\"\"# Calc\n+      |\n+      |## Factor\n+      |\n+      |```gramaire\n+      |Factor\n+      |: NUMBR\n+      |;\n+      |```\n+      |\"\"\".stripMargin\n+    Lr.parseWith(Method.Canonical, md) match\n+      case Left(diags) =>\n+        fail(s\"expected a clean parse (no local Tokens block to check against), got: $diags\")\n+      case Right(g) => assertEquals(g.rules.map(_.name), Vector(\"Factor\"))\n+  }\n+\n   test(\"shift/reduce conflict: points at the reducing rule's own head\") {\n     val md = \"\"\"# Ambiguous\n       |","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDiagnosticsGoldenSuite.scala","sha":"5cfadda47d34ce16d5a4e093ff2b336789fe14b9","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":1,"filename":"core/.jvm/src/test/scala/gramaire/ScalaSelfHostSuite.scala","patch":"@@ -55,7 +55,13 @@ class ScalaSelfHostSuite extends munit.FunSuite:\n             Desugar.desugar(g) match\n               case Left(e) => fail(s\"desugar failed: $e\")\n               case Right(dg) =>\n-                Diagnostics.checkDefined(dg) match\n+                val declaredTokens =\n+                  Tokens\n+                    .parseTokens(Bootstrap.lrTokensSource)\n+                    .getOrElse(Vector.empty)\n+                    .map(_.name)\n+                    .toSet\n+                Diagnostics.checkDefined(dg, declaredTokens = declaredTokens) match\n                   case Right(desugared) => assertEquals(desugared, Bootstrap.bootstrapGrammar)\n                   case Left(e)          => fail(s\"check failed: $e\")\n           case Right(_) => fail(\"parse should yield a Grammar\")","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala","sha":"2aafb18ed94a9e3aa4aca32dc0e9eb8235bd2d37","status":"modified"},{"additions":50,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FDiagnostics.scala","changes":75,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FDiagnostics.scala?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":25,"filename":"core/src/main/scala/gramaire/Diagnostics.scala","patch":"@@ -16,17 +16,26 @@ object Diagnostics:\n     */\n   private def isNonterminalName(name: String): Boolean = name.toUpperCase != name\n \n-  /** Names referenced as nonterminals but never defined by a rule. */\n-  def undefinedNonterminals(g: Grammar): Vector[String] =\n+  /** Names referenced as nonterminals but never defined by a rule, plus (when the document declares\n+    * at least one token class of its own) ALL-CAPS names referenced but never declared in its `##\n+    * Tokens` block. `declaredTokens` is the real, parsed set of that block's own names — ground\n+    * truth, not a spelling guess. An empty `declaredTokens` means the document has no `## Tokens`\n+    * block at all (lexer-spec.md §6's \"block-level external mode\": the whole scanner, ALL-CAPS\n+    * names included, is supplied out of band), so an ALL-CAPS reference can't be checked against\n+    * anything local and is left alone, exactly as before.\n+    */\n+  def undefinedNonterminals(g: Grammar, declaredTokens: Set[String] = Set.empty): Vector[String] =\n     val defined = Table.nontermSet(g)\n-    def undefined(name: String): Boolean = isNonterminalName(name) && !defined.contains(name)\n+    def undefined(name: String): Boolean =\n+      if isNonterminalName(name) then !defined.contains(name)\n+      else declaredTokens.nonEmpty && !declaredTokens.contains(name)\n     g.rules.flatMap(_.alts.flatMap(_.syms.flatMap(Sym.refs))).filter(undefined).distinct\n \n-  // Every ALL-CAPS name referenced anywhere in the grammar — a proxy for \"declared token classes\"\n-  // (the grammar has no separate token-class list of its own; `checkDefined` only ever sees the\n-  // parsed `Grammar`, not the `## Tokens` block). Used for the case-insensitive did-you-mean: an\n-  // author who typed `Number` almost certainly meant the token class `NUMBER`, and `NUMBER` being\n-  // ALL-CAPS means it was never flagged as undefined itself.\n+  // Every ALL-CAPS name referenced anywhere in the grammar — the pre-D60 proxy for \"declared token\n+  // classes\", kept only as the did-you-mean fallback for a document with no `## Tokens` block at all\n+  // (so no real declared set exists to suggest from). An author who typed `Number` almost certainly\n+  // meant the token class `NUMBER`, and `NUMBER` being ALL-CAPS means it was never flagged as\n+  // undefined itself.\n   private def terminalLikeRefs(g: Grammar): Vector[String] =\n     g.rules\n       .flatMap(_.alts.flatMap(_.syms.flatMap(Sym.refs)))\n@@ -59,37 +68,53 @@ object Diagnostics:\n       .headOption\n       .map(_._1)\n \n-  // A `help:` suggestion for an undefined name: a case-insensitive match against a token class\n-  // first (the casing-convention footgun — `Number` meaning `NUMBER`), else the closest defined\n-  // rule name within a tight edit-distance budget.\n+  // A `help:` suggestion for an undefined name. A bad mixed-case name (undefined nonterminal) tries\n+  // a case-insensitive token-class match first (the casing-convention footgun — `Number` meaning\n+  // `NUMBER`), else the closest defined rule name; a bad ALL-CAPS name (undefined token class, only\n+  // reachable when the document declares at least one token of its own) suggests the nearest\n+  // declared token instead — a rule name would never be the right suggestion there.\n   private def didYouMean(\n       bad: String,\n       ruleNames: Vector[String],\n       termNames: Vector[String]\n   ): Option[String] =\n-    termNames.find(_.equalsIgnoreCase(bad)) match\n-      case Some(t) => Some(s\"help: did you mean the token class `$t`? (token classes are ALL-CAPS)\")\n-      case None    => nearestMatch(bad, ruleNames).map(n => s\"help: did you mean `$n`?\")\n-\n-  /** Reject a grammar that references an undefined nonterminal — one diagnostic per reference site\n-    * (via `spans`, if given), each naming the symbol and, where a close match exists, suggesting\n-    * one.\n+    if isNonterminalName(bad) then\n+      termNames.find(_.equalsIgnoreCase(bad)) match\n+        case Some(t) =>\n+          Some(s\"help: did you mean the token class `$t`? (token classes are ALL-CAPS)\")\n+        case None => nearestMatch(bad, ruleNames).map(n => s\"help: did you mean `$n`?\")\n+    else nearestMatch(bad, termNames).map(n => s\"help: did you mean the token class `$n`?\")\n+\n+  /** Reject a grammar that references an undefined nonterminal, or (when `declaredTokens` is\n+    * non-empty) an ALL-CAPS token class never declared in the document's own `## Tokens` block —\n+    * one diagnostic per reference site (via `spans`, if given), each naming the symbol and, where a\n+    * close match exists, suggesting one.\n     */\n   def checkDefined(\n       g: Grammar,\n-      spans: SpanIndex = SpanIndex.empty\n+      spans: SpanIndex = SpanIndex.empty,\n+      declaredTokens: Set[String] = Set.empty\n   ): Either[Vector[Diagnostic], Grammar] =\n-    undefinedNonterminals(g) match\n+    undefinedNonterminals(g, declaredTokens) match\n       case Vector() => Right(g)\n       case bad =>\n         val ruleNames = Table.nontermSet(g).toVector.sorted\n-        val termNames = terminalLikeRefs(g)\n+        val termNames =\n+          if declaredTokens.nonEmpty then declaredTokens.toVector.sorted else terminalLikeRefs(g)\n         def diagsFor(name: String): Vector[Diagnostic] =\n-          val notes = Vector(\n-            \"note: a mixed-case name must be defined by some rule (an ALL-CAPS name is a lexer token class)\"\n-          ) ++ didYouMean(name, ruleNames, termNames).toVector\n+          val (msg, baseNote) =\n+            if isNonterminalName(name) then\n+              (\n+                s\"undefined nonterminal `$name`\",\n+                \"note: a mixed-case name must be defined by some rule (an ALL-CAPS name is a lexer token class)\"\n+              )\n+            else\n+              (\n+                s\"undefined token class `$name`\",\n+                \"note: an ALL-CAPS name must be declared in this document's own `## Tokens` block\"\n+              )\n+          val notes = Vector(baseNote) ++ didYouMean(name, ruleNames, termNames).toVector\n           val occurrences = spans.identSpans.getOrElse(name, Vector.empty)\n-          val msg = s\"undefined nonterminal `$name`\"\n           if occurrences.isEmpty then Vector(Diagnostic.error(Stage.Resolve, msg, None, notes))\n           else occurrences.map(sp => Diagnostic.error(Stage.Resolve, msg, Some(sp), notes))\n         Left(bad.flatMap(diagsFor))","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FDiagnostics.scala","sha":"92c2a880fcd6b91980cd0322fe786c88114d9ea6","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala","changes":18,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":8,"filename":"core/src/main/scala/gramaire/Lexer.scala","patch":"@@ -126,13 +126,12 @@ object Lexer:\n     * A rule head may also be written with NO newline at all before its `:` (`Foo : 'x' ;`,\n     * ANTLR/Bison-style) — `;` already makes \"the token right after a `;`, or the very first token\n     * in the document\" an unambiguous rule-start position, the same way it disambiguates consecutive\n-    * rules above, so a missing head `NL` there is synthesized (zero-width, ADR D59)\n-    * rather than left for the grammar to reject. This keeps `Rule`'s own two productions — and\n-    * every production id after them, in `CodegenScala.lrActionsScala` and `Lr.reduce` alike —\n-    * untouched: a rule head always presents an `IDENT NL ':'` shape to the parser, real or\n-    * synthesized. A `name:Sym` field (`IDENT ':' Sym`, always mid-`Body`, never right after a\n-    * `;`/at the start) is never mistaken for a head, because synthesis only ever fires at a\n-    * position a field can't occupy.\n+    * rules above, so a missing head `NL` there is synthesized (zero-width, ADR D59) rather than\n+    * left for the grammar to reject. This keeps `Rule`'s own two productions — and every production\n+    * id after them, in `CodegenScala.lrActionsScala` and `Lr.reduce` alike — untouched: a rule head\n+    * always presents an `IDENT NL ':'` shape to the parser, real or synthesized. A `name:Sym` field\n+    * (`IDENT ':' Sym`, always mid-`Body`, never right after a `;`/at the start) is never mistaken\n+    * for a head, because synthesis only ever fires at a position a field can't occupy.\n     */\n   def normalizeNewlines(toks: Vector[Token]): Vector[Token] =\n     normalizeNewlinesGeneric[Token](toks, _.terminal, (prev, term, txt) => Token(term, txt))\n@@ -173,8 +172,11 @@ object Lexer:\n       termOf(toks, i) == Some(\"IDENT\") &&\n         (isBoundary(i - 1) || (termOf(toks, i - 1) == Some(\"ATTR\") && isBoundary(i - 2)))\n \n+    // Synthesize only where it can matter: a head `IDENT` directly followed by `:`, with no real\n+    // `NL` already between them. `Foo Bar` (no `:` at all) is left alone — synthesizing there would\n+    // be pointless, since `decide` below would drop the synthetic `NL` anyway.\n     val withSynthesizedHeads: Vector[T] = toks.zipWithIndex.flatMap { case (t, i) =>\n-      if isHeadIdent(i) && termOf(toks, i + 1) != Some(\"NL\") then Vector(t, mk(t, \"NL\", \"\"))\n+      if isHeadIdent(i) && termOf(toks, i + 1) == Some(\":\") then Vector(t, mk(t, \"NL\", \"\"))\n       else Vector(t)\n     }\n ","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala","sha":"d1c04a09f4f2da8ad5bbe658b0a13edd12237f17","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","changes":17,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":3,"filename":"core/src/main/scala/gramaire/Lr.scala","patch":"@@ -883,12 +883,22 @@ object Lr:\n       case Left(diags) => Left(diags)\n       case Right((g, normalized)) =>\n         val spans = SpanIndex.build(normalized)\n+        // The document's own declared token classes (ADR D60) — real ground truth for\n+        // `checkDefined`'s undeclared-token check, not a spelling guess. Empty when the document has\n+        // no `## Tokens` block at all (whole-grammar external-mode lexing, lexer-spec.md §6), which\n+        // leaves an ALL-CAPS reference unchecked exactly as before.\n+        val declaredTokens: Set[String] =\n+          tokensContentOf(md)\n+            .flatMap(Tokens.parseTokens(_).toOption)\n+            .getOrElse(Vector.empty)\n+            .map(_.name)\n+            .toSet\n         Desugar.desugar(g) match\n           case Left(msg) =>\n             Left(\n               Vector(Diagnostic.error(Stage.Desugar, msg, SpanIndex.spanFromMessage(msg, spans)))\n             )\n-          case Right(g2) => Diagnostics.checkDefined(g2, spans)\n+          case Right(g2) => Diagnostics.checkDefined(g2, spans, declaredTokens)\n \n   /** Parse using canonical LR(1) tables, rendering any diagnostics to plain text — the stable\n     * `Either[String, Grammar]` shape most callers (the conformance suite, self-hosting loop, CLI\n@@ -982,8 +992,9 @@ object Lr:\n           )\n \n   // A declared, non-`-> skip` token class no rule ever references by name — almost always a typo'd\n-  // reference (the intended rule then silently resolves the misspelled name as a phantom terminal,\n-  // per `Diagnostics.checkDefined`'s own ALL-CAPS carve-out) or a leftover declaration.\n+  // reference (paired with `checkDefined`'s own complementary \"undefined token class\" diagnostic on\n+  // the misspelled reference itself, ADR D60, once the document declares at least one token) or a\n+  // leftover declaration.\n   private def unusedTokenWarnings(md: String, g: Grammar): Vector[Diagnostic] =\n     tokensContentOf(md) match\n       case None => Vector.empty","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","sha":"a3fff6e2c9633fa2757a1a9e240b293b82ca1444","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/docs%2Fmulti-backend-implementation-plan.md","changes":1,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":0,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -759,6 +759,7 @@ everything target-specific is independent.\n | D57 | Railroad node navigation is shared code (`site/src/lab/railroadNav.ts`), bound to the renderer's own `data-rr-*` grouping | **Both the Lab and the Notebook island now bind nonterminal-node hover/click/keyboard navigation through one shared `bindRailroadNodeNav` helper, querying the `<g class=\"rr-node rr-node-nonterm\" data-rr-kind=\"...\" data-rr-label=\"...\">` grouping `Railroad.scala`'s `renderSvg` already emits (railroad-renderer-roadmap's phase 3, \"semantic affordances\" — this is the \"built from that structure\" half that hadn't landed yet). The Lab's own hover/click cross-highlight (pre-existing) was migrated off querying `rect.rr-nonterm` plus its text sibling separately (two listener targets per node, because the text painted on top of the rect) onto one listener on the `<g>` itself. The Notebook, which had no diagram interactivity at all, gets the same click/keyboard \"jump to that rule's cell\" affordance the outline sidebar already had (`jumpToRule`, resolving a label to a block index the same way `jumpToOutlineEntry` does). Every click/keydown handler calls `stopPropagation()`, since both islands nest the diagram inside a larger click/keydown-handling container (a cell's own \"click to edit\" surface, in the Notebook's case) — activating a nonterminal reference must navigate, never also trigger the ancestor's own activation.** | The metadata already existed (per-node `<title>`/`data-rr-*`, shipped earlier) but nothing consumed it — the Lab's interactivity predated that metadata and was never migrated, and the Notebook never got equivalent interactivity built at all. One shared module rather than two islands growing their own subtly-diverging DOM-query logic; verified end-to-end against the real Scala.js engine (not mocked), including that navigating never also opens the source cell it navigated from. |\n | D58 | Real `---` YAML frontmatter for `.gram.md`, dual-read migration, threaded through lock + site nav | **A leading, `---`-delimited frontmatter block (`name:`/`lang:`, room for future keys) replaces the embedded fenced `` ```gramaire `` \"General settings\" block as the recommended way to declare a grammar's own metadata — chosen over extending the existing fence (the lower-migration-cost default a design debate recommended first) because the user explicitly wanted genuine frontmatter: free extensibility (lists/maps/quoting), standard tooling recognition (every markdown-aware editor/linter/static-site generator already special-cases a leading `---` block), and discoverability (metadata at byte offset 0, not buried in the Nth fence). Parsed by a new, deliberately minimal hand-rolled subset (`core/src/main/scala/gramaire/Frontmatter.scala`: flow scalars, quoted strings with backslash escapes, flow lists — no anchors, tags, multi-document, or block scalars) rather than a general YAML library, since `core` cross-builds to Scala.js (a JVM-only library like SnakeYAML can't) and the schema this project actually needs never exceeds this subset. `Frontmatter.strip` blanks the stripped lines to SAME-LENGTH space-padded text (not merely the same line count), so every downstream `SrcSpan`/diagnostic offset computed against the remaining document stays byte-for-byte correct with no caller-side adjustment. `Lr.scala` dual-reads for one release: a leading frontmatter block wins when present; otherwise the old fence still works, with a deprecation warning pointing at `gramaire fmt`. `gramaire fmt` gained a migration codemod (`GramaireCheck.migrateToFrontmatter`) that lifts `name:`/`lang:` out of the old fence into a new leading block and removes the fence (run once across every shipped example/grammar file in this same change). `Lock` gained an optional `name` field (populated from `Lr.nameOf`), following the exact omit-when-default provenance convention D56 established for `diagramView` — neither `checkStructure` nor `checkDrift` key on it. Sitewide, `parseMarkdownLite` (the one parser shared by the Notebook prose cell, Paper view, and PDF export) gained a frontmatter-skip step alongside its existing `<details>`/HTML-comment housekeeping skips, so the leading block never renders as a literal \"--- name: X lang: Y ---\" paragraph — this was the original, concretely-reported bug that motivated the whole change. `LabResponse` gained a `name` field (`Lr.nameOf`, read independent of whether the grammar notation parses) so the Notebook's download-source/download-PDF filename comes from the real frontmatter-aware name instead of a stale client-side `%name` regex that matched neither the old fence nor the new frontmatter format and always fell back to `\"gramaire-notebook\"`.** | Closes a concretely reported bug (frontmatter rendering as literal prose in Paper/PDF) and a design question (\"how do I add real frontmatter, and how do the values reach the rest of the toolchain\") in one pass, since fixing the render bug properly meant giving frontmatter a real parser and a real place in the pipeline rather than a narrow one-off text-skip. The dual-read window (one release, per this document's own migration-safety convention) means no `.gram.md` in the wild breaks the moment this ships; removing the old-fence fallback and its `knownSettingDirectives` machinery entirely is an explicit follow-up, not bundled here. Full YAML support was explicitly rejected as scope creep — the fixed, small schema Gramaire actually needs (`name`, `lang`, room to grow) never justifies anchors/tags/multi-document/block-scalar support, and a general library would break the Scala.js cross-build this project already depends on. |\n | D59 | A rule head's `NL` before `:` is optional — single-line, ANTLR/Bison-style rules now parse | **`Lexer.normalizeNewlines`/`normalizeNewlinesSpanned` now synthesize a zero-width head `NL` whenever a rule can legitimately start — right after a `;` (the previous rule's own terminator) or at the very start of the document — and the real one is missing, so `Foo : 'x' ;` parses identically to the traditional `Foo\\n  : 'x' ;`. `Rule`'s own two productions in `bootstrapGrammar`/`grammar/Productions.gram.md` are untouched: the parser still only ever sees an `IDENT NL ':'` shape, real or synthesized, so every downstream production id in `CodegenScala.lrActionsScala` and `Lr.reduce`, every golden IR/DOT/`.g4` fixture, and `Productions.gram.md`'s own railroad diagrams and `.gram.lock` stay byte-for-byte unchanged. A `name:Sym` field (`IDENT ':' Sym`) is never mistaken for a head, because synthesis only fires at a position a field can never occupy — mid-`Body`, a field's `:` is never immediately preceded by a `;` or the document start.** | ANTLR handles single-line rules because it never reuses `:` for two jobs (`=`/`+=` bind element labels, `:` only ever separates a rule head); Gramaire reuses `:` for both a rule's own head and a `name:Sym` field, which is why the newline convention existed in the first place (fmt-output-contract.md's \"Option A\"). The mandatory trailing `;` already made \"the token right after a `;`, or the document's first token\" an unambiguous rule-start position (that's what let a prior revision drop the *boundary* NL between consecutive rules, D-numbered implicitly by that revision); this reuses the exact same boundary to also drop the *requirement* that a head's own `NL` be present in the source, without touching the grammar itself. Synthesizing in the lexer rather than adding new `Rule` alternatives was a deliberate choice: `Rule` is nonterminal #3 of the self-hosting bootstrap grammar, so any new alternative shifts every later production's global id, cascading through three independently hand-synchronized tables (`CodegenScala.lrActionsScala`, `Lr.reduce`, and every golden fixture keyed to the `lr` corpus) for no behavioral gain a lexer-level fix doesn't already deliver. |\n+| D60 | An ALL-CAPS reference is checked against the document's real declared token set, not just its spelling | **`Diagnostics.undefinedNonterminals`/`checkDefined` gain a `declaredTokens: Set[String]` parameter — the document's own `## Tokens` block, parsed for real by `Tokens.parseTokens` and threaded through from `Lr.parseWith` (which already has the raw markdown to hand). When `declaredTokens` is non-empty, an ALL-CAPS reference that isn't among them is now a new, real diagnostic — `` undefined token class `X` `` — with a did-you-mean against the other declared tokens, mirroring the existing undefined-nonterminal check exactly. When `declaredTokens` is empty (the document has no `## Tokens` block at all — lexer-spec.md §6's whole-grammar \"external mode\", where the entire scanner is supplied out of band, e.g. `examples/readme.gram.md`'s `WORD`), an ALL-CAPS reference is left unchecked, exactly as before D60. The pre-existing spelling-based `terminalLikeRefs` heuristic survives only as the did-you-mean fallback for that external-mode case, where no real declared set exists to suggest from.** | Closes a real, previously-silent gap: a typo'd ALL-CAPS reference in a grammar that already declares its own tokens locally (the common case — most shipped examples) was never checked against anything and was silently treated as \"presumably a valid terminal\", the exact opposite of the mixed-case undefined-nonterminal check two lines above it in the same function, which has always been a hard error. `unusedTokenWarnings` (a declared-but-unreferenced token, the other direction of the same typo) already existed; this adds its missing complement. Scoped to only fire when the document has actually declared something locally, rather than unconditionally, because `## Tokens` is genuinely optional (whole-grammar external mode) and a real, checked-in example (`readme.gram.md`) depends on that leniency. |\n \n ## Platform & tooling roadmap (D43)\n ","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/docs%2Fmulti-backend-implementation-plan.md","sha":"273b42208b90a777a529e1b98ba234adf77de971","status":"modified"},{"additions":9,"blob_url":"https://github.com/wstein/gramaire/blob/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=b15930ab08815dbb9a5bf039180f78ccb66fc3c0","deletions":4,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -83,10 +83,15 @@ Inside an `lr` productions block the lexer emits these token classes:\n \n A name is a **nonterminal** exactly when it appears as some rule's left-hand\n side; any other mixed-case name referenced but never defined is a build error\n-(not a silent terminal). An ALL-CAPS name is always a token class. Input\n-tokenization itself is per-language: the `lr` lexer reads the grammar; a separate\n-lexer turns _input_ strings into tokens whose `terminal` names match the\n-grammar's terminals.\n+(not a silent terminal). An ALL-CAPS name is always a token class, and — when\n+the document declares at least one token of its own in a `## Tokens` block —\n+must be one of the names declared there; an ALL-CAPS name referenced but\n+undeclared is a build error too, the same way an undefined nonterminal is. A\n+document with no `## Tokens` block at all relies entirely on an externally\n+supplied lexer (lexer-spec.md §6), so its ALL-CAPS references can't be checked\n+against anything local. Input tokenization itself is per-language: the `lr`\n+lexer reads the grammar; a separate lexer turns _input_ strings into tokens\n+whose `terminal` names match the grammar's terminals.\n \n ## 3. Rule/token termination and newline normalization (Option A)\n ","raw_url":"https://github.com/wstein/gramaire/raw/b15930ab08815dbb9a5bf039180f78ccb66fc3c0/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"43a47bc0671c9f7bb8f46b7265bf2bf2cb12e6f4","status":"modified"}]}