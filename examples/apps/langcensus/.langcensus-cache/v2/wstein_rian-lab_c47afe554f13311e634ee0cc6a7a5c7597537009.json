{"files":[{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/c47afe554f13311e634ee0cc6a7a5c7597537009/SELFHOST.md","changes":7,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=c47afe554f13311e634ee0cc6a7a5c7597537009","deletions":1,"filename":"SELFHOST.md","patch":"@@ -184,7 +184,12 @@ construction, and cons-list building all compose and lower correctly.\n    appear, and **needed zero backend changes**: an optimizer over a *sum* IR\n    destructures with variant patterns (long supported), not struct patterns.\n    Slotted before the codegen, it shrinks output — `(2 + 3) * 4` emits a single\n-   `Push 20` instead of five instructions.\n+   `Push 20` instead of five instructions. The codegen also handles **variables\n+   and `let`** via load/store **slots**: it threads a compile-time `name → slot`\n+   environment and the VM threads a slot store (`Map(Int64, Int64)`), so\n+   `let x = 5 in x + 1` lowers to `[Push 5, Store 0, Load 0, Push 1, IAdd]` and\n+   lexical shadowing resolves to distinct slots — still **zero backend changes**\n+   (maps suffice).\n \n ## Conclusion of the spike series\n ","raw_url":"https://github.com/wstein/rian-lab/raw/c47afe554f13311e634ee0cc6a7a5c7597537009/SELFHOST.md","sha":"b373e07720987261da4077d0a5b97cc40daf9d37","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/c47afe554f13311e634ee0cc6a7a5c7597537009/examples%2Frian%2FREADME.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2FREADME.md?ref=c47afe554f13311e634ee0cc6a7a5c7597537009","deletions":3,"filename":"examples/rian/README.md","patch":"@@ -58,9 +58,11 @@ compile + run on real BEAM bytecode:\n   increment (a struct value is a tagged map, read by field access).\n - [selfhost_codegen.rian](selfhost_codegen.rian) — a **code generator + stack\n   VM**: it compiles the `Expr` sum to a post-order list of `Instr` and executes\n-  them on a stack (`Vec(Int64)`). The full `lex → parse → codegen → run` pipeline\n-  runs on `.beam`; it hits **no** backend wall (sum ASTs + list patterns, both\n-  already supported).\n+  them on a stack (`Vec(Int64)`). It handles **variables and `let`** via\n+  load/store **slots** — the generator threads a compile-time `name → slot`\n+  environment and the VM threads a slot store (`Map(Int64, Int64)`) — so\n+  `let x = 5 in x + 1` lowers to `[Push 5, Store 0, Load 0, Push 1, IAdd]`. The\n+  full `lex → parse → codegen → run` pipeline runs on `.beam`; no backend wall.\n - [selfhost_opt.rian](selfhost_opt.rian) — an **optimizer** (constant folding +\n   algebraic identities: `2 + 3 → 5`, `x * 1 → x`, `x * 0 → 0`). It matches IR\n   nodes by shape with nested variant and literal-in-variant patterns","raw_url":"https://github.com/wstein/rian-lab/raw/c47afe554f13311e634ee0cc6a7a5c7597537009/examples%2Frian%2FREADME.md","sha":"aac301cd7427cfe2fb36e9cc90a64b58e5220e78","status":"modified"},{"additions":48,"blob_url":"https://github.com/wstein/rian-lab/blob/c47afe554f13311e634ee0cc6a7a5c7597537009/examples%2Frian%2Fselfhost_codegen.rian","changes":77,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_codegen.rian?ref=c47afe554f13311e634ee0cc6a7a5c7597537009","deletions":29,"filename":"examples/rian/selfhost_codegen.rian","patch":"@@ -1,51 +1,70 @@\n # ===========================================================================\n # SELF-HOSTING SPIKE — a Rian code generator + stack VM, written in Rian\n # ===========================================================================\n-# Goal (ADR-0027/0031): the layer after the type-checker. Compile the `Expr`\n-# sum to a flat list of **stack-machine instructions** (post-order), then run\n-# those instructions on a tiny stack VM — both written in Rian. This is the\n-# \"back end\" of the self-hosting toolchain: `Expr -> Vec(Instr) -> Int64`.\n-# Evidence for SELFHOST.md.\n+# Goal (ADR-0027/0031): the \"back end\" of the self-hosting toolchain. Compile\n+# the `Expr` sum to a flat list of **stack-machine instructions** (post-order),\n+# then run them on a tiny VM — both written in Rian. `Expr -> Vec(Instr) -> Int64`.\n #\n-#   gen(Add(Num(1), Num(2)))  =  [Push(1), Push(2), IAdd]\n-#   run([Push(1), Push(2), IAdd])  =  3\n+# This version handles **variables and `let`** via load/store **slots**: the\n+# code generator threads a compile-time environment (name -> slot index) and the\n+# VM threads a slot store (`Map(Int64, Int64)`) alongside the operand stack.\n+#\n+#   gen(Let(\"x\", Num(5), Add(Var(\"x\"), Num(1))))\n+#     = [Push(5), Store(0), Load(0), Push(1), IAdd]\n+#   run(that)  =  6\n # ---------------------------------------------------------------------------\n \n mod SelfhostCodegen do\n   type Expr := Num(Int64)\n+             | Var(String)\n              | Add(Expr, Expr)\n              | Sub(Expr, Expr)\n              | Mul(Expr, Expr)\n              | Div(Expr, Expr)\n+             | Let(String, Expr, Expr)\n+\n+  # Push a literal, load/store a slot, or pop two / push one.\n+  type Instr := Push(Int64) | Load(Int64) | Store(Int64) | IAdd | ISub | IMul | IDiv\n \n-  # A stack-machine instruction set: push a literal, or pop two / push one.\n-  type Instr := Push(Int64) | IAdd | ISub | IMul | IDiv\n+  # Public entry: compile in an empty environment at slot depth 0.\n+  pub def gen(e Expr) Vec(Instr) := g(e, %{}, 0)\n \n-  # Compile an expression to a post-order instruction list: emit the operands,\n-  # then the operator (so the operator finds them on top of the stack).\n-  pub def gen(e Expr) Vec(Instr)\n-  pub def gen(Num(n)) := [Push(n)]\n-  pub def gen(Add(a, b)) := bin(a, b, IAdd)\n-  pub def gen(Sub(a, b)) := bin(a, b, ISub)\n-  pub def gen(Mul(a, b)) := bin(a, b, IMul)\n-  pub def gen(Div(a, b)) := bin(a, b, IDiv)\n+  # `env` maps a bound name to its slot; `depth` is the next free slot (= the\n+  # lexical `let`-nesting depth — sibling subtrees safely reuse a slot once it\n+  # has been loaded, since scoping forbids a binding escaping its body).\n+  def g(e Expr, env Map(String, Int64), depth Int64) Vec(Instr)\n+  def g(Num(n), _, _) := [Push(n)]\n+  def g(Var(name), env, _) := [Load(Map.get(env, name))]\n+  def g(Add(a, b), env, d) := bin(a, b, env, d, IAdd)\n+  def g(Sub(a, b), env, d) := bin(a, b, env, d, ISub)\n+  def g(Mul(a, b), env, d) := bin(a, b, env, d, IMul)\n+  def g(Div(a, b), env, d) := bin(a, b, env, d, IDiv)\n \n-  def bin(a Expr, b Expr, op Instr) Vec(Instr) := concat(gen(a), concat(gen(b), [op]))\n+  def g(Let(name, rhs, body), env, d)\n+    # evaluate `rhs`, store it into slot `d`, then compile `body` with `name`\n+    # bound to slot `d` and the next free slot at `d + 1`\n+    concat(g(rhs, env, d), concat([Store(d)], g(body, Map.put(env, name, d), d + 1)))\n+  end\n+\n+  def bin(a Expr, b Expr, env Map(String, Int64), d Int64, op Instr) Vec(Instr)\n+  def bin(a, b, env, d, op) := concat(g(a, env, d), concat(g(b, env, d), [op]))\n \n   # List concatenation, written in Rian (there is no `++` operator).\n   def concat(xs Vec(Instr), ys Vec(Instr)) Vec(Instr)\n   def concat([], ys) := ys\n   def concat([h | t], ys) := [h | concat(t, ys)]\n \n-  # Execute an instruction list against a stack (a `Vec(Int64)`), returning the\n-  # value left on top. Binary ops pop two operands (`[b, a | s]`) and push one.\n-  pub def run(prog Vec(Instr)) Int64 := exec(prog, [])\n-\n-  def exec(prog Vec(Instr), stack Vec(Int64)) Int64\n-  def exec([], [top | _]) := top\n-  def exec([Push(n) | rest], stack) := exec(rest, [n | stack])\n-  def exec([IAdd | rest], [b, a | s]) := exec(rest, [a + b | s])\n-  def exec([ISub | rest], [b, a | s]) := exec(rest, [a - b | s])\n-  def exec([IMul | rest], [b, a | s]) := exec(rest, [a * b | s])\n-  def exec([IDiv | rest], [b, a | s]) := exec(rest, [a div b | s])\n+  # Execute against an operand stack (`Vec(Int64)`) and a slot store\n+  # (`Map(Int64, Int64)`), returning the value left on top of the stack.\n+  pub def run(prog Vec(Instr)) Int64 := exec(prog, [], %{})\n+\n+  def exec(prog Vec(Instr), stack Vec(Int64), slots Map(Int64, Int64)) Int64\n+  def exec([], [top | _], _) := top\n+  def exec([Push(n) | r], st, sl) := exec(r, [n | st], sl)\n+  def exec([Load(i) | r], st, sl) := exec(r, [Map.get(sl, i) | st], sl)\n+  def exec([Store(i) | r], [v | st], sl) := exec(r, st, Map.put(sl, i, v))\n+  def exec([IAdd | r], [b, a | st], sl) := exec(r, [a + b | st], sl)\n+  def exec([ISub | r], [b, a | st], sl) := exec(r, [a - b | st], sl)\n+  def exec([IMul | r], [b, a | st], sl) := exec(r, [a * b | st], sl)\n+  def exec([IDiv | r], [b, a | st], sl) := exec(r, [a div b | st], sl)\n end","raw_url":"https://github.com/wstein/rian-lab/raw/c47afe554f13311e634ee0cc6a7a5c7597537009/examples%2Frian%2Fselfhost_codegen.rian","sha":"75400eed32c2e3cd6ba41601fb01a8819aa35324","status":"modified"},{"additions":24,"blob_url":"https://github.com/wstein/rian-lab/blob/c47afe554f13311e634ee0cc6a7a5c7597537009/test%2Frian%2Fbeam_test.exs","changes":24,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fbeam_test.exs?ref=c47afe554f13311e634ee0cc6a7a5c7597537009","deletions":0,"filename":"test/rian/beam_test.exs","patch":"@@ -168,6 +168,30 @@ defmodule Rian.BeamTest do\n       assert cg.gen({:add, {:num, 1}, {:num, 2}}) == [{:push, 1}, {:push, 2}, :i_add]\n     end\n \n+    test \"the codegen handles variables and `let` via load/store slots\" do\n+      {:ok, cg} = Beam.load(File.read!(\"examples/rian/selfhost_codegen.rian\"), :rian_beam_cg_vars)\n+\n+      run = fn e -> cg.run(cg.gen(e)) end\n+\n+      # `let x = 5 in x + 1` -> a slot is stored then loaded\n+      let1 = {:let, \"x\", {:num, 5}, {:add, {:var, \"x\"}, {:num, 1}}}\n+      assert cg.gen(let1) == [{:push, 5}, {:store, 0}, {:load, 0}, {:push, 1}, :i_add]\n+      assert run.(let1) == 6\n+\n+      # nested lets occupy distinct slots\n+      nested =\n+        {:let, \"x\", {:num, 10},\n+         {:let, \"y\", {:num, 4}, {:mul, {:add, {:var, \"x\"}, {:var, \"y\"}}, {:num, 2}}}}\n+\n+      assert run.(nested) == 28\n+\n+      # lexical shadowing: the inner `x` takes its own slot, the outer survives\n+      shadow =\n+        {:let, \"x\", {:num, 1}, {:add, {:let, \"x\", {:num, 2}, {:var, \"x\"}}, {:var, \"x\"}}}\n+\n+      assert run.(shadow) == 3\n+    end\n+\n     test \"the self-hosting optimizer constant-folds + simplifies, shrinking codegen output\" do\n       {:ok, opt} = Beam.load(File.read!(\"examples/rian/selfhost_opt.rian\"), :rian_beam_opt)\n       {:ok, cg} = Beam.load(File.read!(\"examples/rian/selfhost_codegen.rian\"), :rian_beam_opt_cg)","raw_url":"https://github.com/wstein/rian-lab/raw/c47afe554f13311e634ee0cc6a7a5c7597537009/test%2Frian%2Fbeam_test.exs","sha":"e1c94633e2aef60fb520d33ed264f5c6b59ad23f","status":"modified"}]}