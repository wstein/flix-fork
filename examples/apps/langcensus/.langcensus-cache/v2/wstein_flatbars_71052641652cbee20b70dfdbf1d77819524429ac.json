{"files":[{"additions":29,"blob_url":"https://github.com/wstein/flatbars/blob/71052641652cbee20b70dfdbf1d77819524429ac/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc","changes":29,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc?ref=71052641652cbee20b70dfdbf1d77819524429ac","deletions":0,"filename":"docs/modules/ROOT/pages/adr-0029-i18n-helper.adoc","patch":"@@ -247,3 +247,32 @@ Consistent with that default, the share link omits the truthiness field for `mus\n the spec opt-out (`mj=0`), keeping the common link compact. (One consequence: a pre-2026-06-05 MinBars link\n that relied on the *old* default-`spec` — no `mj` field — now renders under `mustacheJs`; such links predate\n the i18n/config work and are vanishingly rare. Add `minbars.truthiness: spec` to restore the old reading.)\n+\n+== Amendment (2026-06-05): i18n is a first-class host seam, not a `registerHelper` shadow\n+\n+The original Decision wired the host's brain by *registering* `t`/`number`/`date` as a user operation\n+(the `renderWith` bag / `rt.register`) that *shadows* the prelude fallback. That worked, but it left the\n+engine unable to distinguish \"a translator is wired\" from \"nobody wired one\" — so the promised *flag*\n+(\"fallback-and-flag\") could not be made precise, and the claim to \"adopt the truthiness pattern (ADR-022)\"\n+was aspirational. The implementation is now the *first-class seam* the Decision described:\n+\n+* *The seam.* `Kernel.Value.Translator` (`pass:[String -> Array Value -> Maybe String]`) is seeded\n+  per-engine — `RefEnv.translator` via `withTranslator` (interpreter, the i18n analogue of `withTruthy`),\n+  and `rt.registerTranslator` (compiled runtime). `Nothing` means \"no host\", which the engine *knows*.\n+* *All five blessed ops are seam-driven* (`t`/`number`/`date`/`selectPlural`/`relative`): a `seamed name\n+  fallback` wrapper consults the translator and runs the op's pure fallback when it is absent or declines.\n+  So the *seam*, not the op identity, decides translated-vs-fallback, and interpreter ≡ compiled (the\n+  fallback path is gated by `test:compile`; the wired path by `check:i18n`).\n+* *The flag is now precise (ADR-029 #2).* Analyse renders with no translator seeded, so it reports every\n+  i18n op that fell back (\"no translator wired — register one or ship keys\") as an *awareness note* — never\n+  a CI-gating lint, so it cannot false-positive a wired project.\n+* *Consumers use the seam.* The `flatbars-js` facade exposes `renderSurfaceI18n` (host translator as a JS\n+  `pass:[(name, args) => string | null]`); the Lab's `TryI18n` island, `check:i18n`, and the `/localize`\n+  host recipe all seed a translator (`registerTranslator`), not `registerHelper`. FullBars carrying these\n+  ops is recorded as an explicit exception in xref:adr-0009-fullbars.adoc[ADR-009] (no faithfulness-by-omission).\n+\n+Open follow-ups (tracked, non-blocking): the Lab's combined worker (`helpers-worker.mjs`) still builds an\n+i18n bag because it *merges* i18n with ADR-018 custom helpers in one render — it needs a combined\n+translator+helpers entry (it still works: a registered helper shadows the seamed prelude op); and a\n+*wired*-translator `test:compile` case (the compiled≡interpret wired path — the fallback path is already\n+gated).","raw_url":"https://github.com/wstein/flatbars/raw/71052641652cbee20b70dfdbf1d77819524429ac/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc","sha":"197489d9e701c876a27c2dba32fd6543b74cbeae","status":"modified"},{"additions":11,"blob_url":"https://github.com/wstein/flatbars/blob/71052641652cbee20b70dfdbf1d77819524429ac/tutorials%2Fsrc%2Fpages%2Flocalize.astro","changes":17,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fpages%2Flocalize.astro?ref=71052641652cbee20b70dfdbf1d77819524429ac","deletions":6,"filename":"tutorials/src/pages/localize.astro","patch":"@@ -32,7 +32,7 @@ const intro = sections[0].cells[0];\n \n <Reference\n   title=\"Localization\"\n-  subtitle=\"FlatBars ships no i18n — it is the host's job (ADR-029). This runnable guide is the Lab acting as the host: a `t` helper wired to the browser's native Intl, so plurals and number formatting come from the platform, not a vendored library.\"\n+  subtitle=\"FlatBars ships no i18n — it is the host's job (ADR-029). Until you wire a host translator, the i18n ops render their key/value verbatim — you ship untranslated strings (analyse mode flags exactly this). This guide is the Lab acting as the host: one translator seeded onto the engine's i18n seam, backed by the browser's native Intl, so plurals and number formatting come from the platform, not a vendored library.\"\n   groups={groups}\n   dialect=\"fullbars\"\n >\n@@ -69,11 +69,16 @@ const intro = sections[0].cells[0];\n       in. FlatBars <em>does</em> ship a blessed <code>t</code> operation\n       (<a href=\"https://github.com/wstein/flatbars/blob/main/docs/modules/ROOT/pages/adr-0029-i18n-helper.adoc\">ADR-029</a>):\n       <code>{`{{t \"key\"}}`}</code> is catalogued, validated, and editor-painted, and on\n-      its own it simply returns the key. What it does <b>not</b> ship is the i18n\n-      <em>brain</em> — that is the <b>host's</b> job. The host registers <code>t</code>\n-      to look a key up in its catalogue, pick the right plural form, and fill in the\n-      placeholders; that registration shadows the fallback. The template never\n-      changes — <code>{`{{t \"key\" count=n}}`}</code>, like any other operation.\n+      its own it simply returns the key — <b>so a template that uses <code>t</code>\n+      with no host wired ships its keys verbatim</b> (analyse mode flags this). What\n+      FlatBars does <b>not</b> ship is the i18n <em>brain</em> — that is the\n+      <b>host's</b> job. The host seeds <em>one</em> translator onto the engine's\n+      first-class i18n seam (<code>registerTranslator</code> in the compiled runtime,\n+      <code>withTranslator</code> in PureScript — the i18n analogue of the truthiness\n+      seam, <b>not</b> <code>registerHelper</code>); it drives <code>t</code> /\n+      <code>number</code> / <code>date</code> together — looking a key up, picking the\n+      plural form, filling the placeholders. The template never changes —\n+      <code>{`{{t \"key\" count=n}}`}</code>, like any other operation.\n     </p>\n     <div class=\"lz-flow\" aria-label=\"key to output\">\n       <span class=\"box\">{`{{t \"key\" count=n}}`}</span><span class=\"arr\">→</span>","raw_url":"https://github.com/wstein/flatbars/raw/71052641652cbee20b70dfdbf1d77819524429ac/tutorials%2Fsrc%2Fpages%2Flocalize.astro","sha":"a3640cdab0658b839e20c15cfa2ca4982b53bda7","status":"modified"}]}