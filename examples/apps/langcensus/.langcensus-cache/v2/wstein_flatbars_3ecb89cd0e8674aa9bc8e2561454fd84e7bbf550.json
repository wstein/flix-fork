{"files":[{"additions":55,"blob_url":"https://github.com/wstein/flatbars/blob/3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550/trussbars%2Fcrates%2Ftrussbars-import%2Fsrc%2Fbin%2Ftruss-import.rs","changes":59,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-import%2Fsrc%2Fbin%2Ftruss-import.rs?ref=3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550","deletions":4,"filename":"trussbars/crates/trussbars-import/src/bin/truss-import.rs","patch":"@@ -4,7 +4,7 @@\n //! extension unless `--dialect` overrides it:\n //!\n //! ```text\n-//! truss-import [--dialect <name>] [--data d.json] <file|->   # migrate → idiomatic .truss\n+//! truss-import [--dialect <name>] [-o <dir|file>] <file|->   # migrate → idiomatic .truss\n //! truss-import --metrics <file|->                            # Mustache idiom metrics\n //! truss-import --ast <file|->                                # dump the dialect AST ({:#?})\n //! ```\n@@ -40,6 +40,7 @@ fn main() -> ExitCode {\n     let mut faithful_truthiness = true;\n     let mut compact = false;\n     let mut data: Option<String> = None;\n+    let mut out: Option<String> = None;\n     let mut report_json = false;\n \n     while let Some(arg) = args.next() {\n@@ -58,6 +59,13 @@ fn main() -> ExitCode {\n             other if other.starts_with(\"--data=\") => {\n                 data = Some(other[\"--data=\".len()..].to_string());\n             }\n+            \"--out\" | \"-o\" => match args.next() {\n+                Some(v) => out = Some(v),\n+                None => return usage(\"`--out` needs a folder or file path\"),\n+            },\n+            other if other.starts_with(\"--out=\") => {\n+                out = Some(other[\"--out=\".len()..].to_string());\n+            }\n             \"--ast\" => mode = Mode::Dump,\n             \"--metrics\" => mode = Mode::Metrics,\n             \"--to-truss\" => mode = Mode::ToTruss,\n@@ -106,11 +114,13 @@ fn main() -> ExitCode {\n         Mode::Metrics => run_metrics(dialect, &src),\n         Mode::ToTruss => run_to_truss(\n             dialect,\n+            &file,\n             &src,\n             ternary,\n             faithful_truthiness,\n             compact,\n             data.as_deref(),\n+            out.as_deref(),\n             report_json,\n         ),\n     }\n@@ -169,13 +179,16 @@ fn run_metrics(dialect: Dialect, src: &str) -> ExitCode {\n     ExitCode::SUCCESS\n }\n \n+#[allow(clippy::too_many_arguments)]\n fn run_to_truss(\n     dialect: Dialect,\n+    input_file: &str,\n     src: &str,\n     ternary: bool,\n     faithful_truthiness: bool,\n     compact: bool,\n     data: Option<&str>,\n+    out: Option<&str>,\n     report_json: bool,\n ) -> ExitCode {\n     // The shape oracle: a `--data` JSON sample, else the heuristic.\n@@ -214,10 +227,23 @@ fn run_to_truss(\n     match migrated {\n         Ok(m) => {\n             // Readable (block tags on their own lines) by default; `--compact` is verbatim.\n-            if compact {\n-                print!(\"{}\", m.truss);\n+            let rendered = if compact { m.truss.clone() } else { m.pretty() };\n+            // Write to a folder/file with `--out`, else stdout.\n+            if let Some(out) = out {\n+                let target = resolve_out(out, input_file);\n+                if let Some(parent) = target.parent()\n+                    && let Err(e) = std::fs::create_dir_all(parent)\n+                {\n+                    eprintln!(\"error: failed to create `{}`: {e}\", parent.display());\n+                    return ExitCode::from(2);\n+                }\n+                if let Err(e) = std::fs::write(&target, &rendered) {\n+                    eprintln!(\"error: failed to write `{}`: {e}\", target.display());\n+                    return ExitCode::from(2);\n+                }\n+                eprintln!(\"wrote {}\", target.display());\n             } else {\n-                print!(\"{}\", m.pretty());\n+                print!(\"{rendered}\");\n             }\n             if report_json {\n                 eprintln!(\"{}\", migrate::report_json(&m.report, src));\n@@ -234,6 +260,28 @@ fn run_to_truss(\n     }\n }\n \n+/// Resolve the `--out` value to a target file path. A folder (ends with `/`, or an\n+/// existing directory) gets `<input-stem>.truss` appended (stem `out` for stdin); any\n+/// other value is taken as the file path verbatim.\n+fn resolve_out(out: &str, input_file: &str) -> std::path::PathBuf {\n+    let as_folder = out.ends_with('/') || out.ends_with(std::path::MAIN_SEPARATOR) || {\n+        let p = Path::new(out);\n+        p.is_dir() || p.extension().is_none()\n+    };\n+    if as_folder {\n+        let stem = if input_file == \"-\" {\n+            \"out\".to_string()\n+        } else {\n+            Path::new(input_file)\n+                .file_stem()\n+                .map_or_else(|| \"out\".to_string(), |s| s.to_string_lossy().into_owned())\n+        };\n+        Path::new(out).join(format!(\"{stem}.truss\"))\n+    } else {\n+        std::path::PathBuf::from(out)\n+    }\n+}\n+\n fn severity_str(s: Severity) -> &'static str {\n     match s {\n         Severity::Info => \"info\",\n@@ -303,6 +351,9 @@ fn print_help() {\n     println!(\n         \"  --compact                 verbatim output (preserve source whitespace; no re-flow)\"\n     );\n+    println!(\n+        \"  -o, --out <path>          write to a folder (<stem>.truss) or file instead of stdout\"\n+    );\n     println!(\"  --data <f.json>           disambiguate sections from a JSON data sample\");\n     println!(\"  --report-json             emit the migration report as JSON on stderr\");\n     println!();","raw_url":"https://github.com/wstein/flatbars/raw/3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550/trussbars%2Fcrates%2Ftrussbars-import%2Fsrc%2Fbin%2Ftruss-import.rs","sha":"1080bc970e4c7e4c540aa25617866ea82a3bcb87","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/flatbars/blob/3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550/trussbars%2Fdocs%2F15-migration-import.md","changes":5,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fdocs%2F15-migration-import.md?ref=3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550","deletions":1,"filename":"trussbars/docs/15-migration-import.md","patch":"@@ -166,7 +166,10 @@ Output is **readable by default** — each block tag (`{{#each}}`, `{{#if}}`, `{\n `{{/…}}`) on its own indented line, content re-flowed to trimmed lines; since block-tag\n lines are standalone (the engine trims them) the render is preserved for\n whitespace-insensitive content. Use `--compact` for the verbatim rendering (preserves\n-source whitespace exactly — preferable for `<pre>`/code-generation).\n+source whitespace exactly — preferable for `<pre>`/code-generation). `-o/--out <path>`\n+writes to disk instead of stdout: a folder (or a path with no extension) receives\n+`<input-stem>.truss` (parents created as needed), any other path is taken as the output\n+file.\n (The library `LowerOptions::default()` stays conservative — all idioms off — so a host opts\n in explicitly; the CLI is the opinionated front-end.)\n ","raw_url":"https://github.com/wstein/flatbars/raw/3ecb89cd0e8674aa9bc8e2561454fd84e7bbf550/trussbars%2Fdocs%2F15-migration-import.md","sha":"8216f7f7b727a8799ffb7a9590816a3323dbfc9d","status":"modified"}]}