{"files":[{"additions":30,"blob_url":"https://github.com/wstein/bin/blob/48384354dafff50519b4e82463c7e769d604c4c5/histlog","changes":43,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=48384354dafff50519b4e82463c7e769d604c4c5","deletions":13,"filename":"histlog","patch":"@@ -300,7 +300,8 @@ module Histlog\n     def build_paths_option_parser(options)\n       OptionParser.new do |opts|\n         opts.banner = \"Usage: histlog paths [--files] [--dirs] [SEARCHTERM] [--regex] [--count] [--asc|--desc] [--sort-by FIELD]\"\n-        opts.on(\"--session [ID]\", \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n+        opts.on(\"--session [ID]\",\n+                \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n           options[:session] = true\n           options[:session_id] = id.to_i if id\n         end\n@@ -335,7 +336,8 @@ module Histlog\n         opts.on(\"--limit N\", Integer, \"Limit number of commands displayed\") { |n| options[:limit] = n }\n         opts.on(\"--regex\", \"Interpret searchterm as a Ruby regex\") { options[:regex] = true }\n         opts.on(\"--fuzzy\", \"Enable fuzzy (fzf-like) matching for the search term\") { options[:fuzzy] = true }\n-        opts.on(\"--session [ID]\", \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n+        opts.on(\"--session [ID]\",\n+                \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n           options[:session] = true\n           options[:session_id] = id.to_i if id\n         end\n@@ -844,7 +846,8 @@ module Histlog\n           # Determine if this command_text has any executions in the current session\n           if env_session_id && env_session_id > 0\n             begin\n-              chk_s = db.get_first_value(\"SELECT COUNT(*) FROM commands WHERE cmd_text_id = ? AND session_id = ?\", [r['cmd_text_id'], env_session_id])\n+              chk_s = db.get_first_value(\"SELECT COUNT(*) FROM commands WHERE cmd_text_id = ? AND session_id = ?\",\n+                                         [r['cmd_text_id'], env_session_id])\n               r['_same_session'] = chk_s.to_i > 0 ? 1 : 0\n             rescue SQLite3::Exception\n               r['_same_session'] = 0\n@@ -855,7 +858,11 @@ module Histlog\n \n           # Determine if this command_text was executed in the current working directory\n           begin\n-            chk_c = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE c.cmd_text_id = ? AND p.path = ?\", [r['cmd_text_id'], cwd])\n+            chk_c = db.get_first_value(\n+              \"SELECT COUNT(*) FROM commands c JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE c.cmd_text_id = ? AND p.path = ?\", [\n+                r['cmd_text_id'], cwd\n+              ]\n+            )\n             r['_same_cwd'] = chk_c.to_i > 0 ? 1 : 0\n           rescue SQLite3::Exception\n             r['_same_cwd'] = 0\n@@ -995,7 +1002,8 @@ module Histlog\n           # Header: Count, Last Used, SW flag, Command (user requested column order)\n           # Use the same SW coloring as the data columns for consistency.\n           header_flag = s_flag_col + 'S' + w_flag_col + 'W' + reset_col\n-          printf(\"%s%6s%s %s%-19s%s %s%s%s %s\\n\", count_col, \"Count\", reset_col, timestamp_col, \"Last Used\", reset_col, flag_col, header_flag, reset_col, \"Command\")\n+          printf(\"%s%6s%s %s%-19s%s %s%s%s %s\\n\", count_col, \"Count\", reset_col, timestamp_col, \"Last Used\", reset_col,\n+                 flag_col, header_flag, reset_col, \"Command\")\n           puts \"--------------------------------------------------------------------------\"\n         rescue Errno::EPIPE\n           return\n@@ -1580,13 +1588,13 @@ module Histlog\n         # Print summary after execution\n         db.close if db\n         puts \"\\n=== Maintenance Summary ===\"\n-        old_size = defined?(original_size) ? original_size : File.size(db_path)\n-        new_size = File.size(db_path)\n+        old_size = defined?(original_size) && original_size ? original_size : (File.size(db_path) rescue 0)\n+        new_size = File.size(db_path) rescue 0\n         puts \"Database size before: #{Histlog::Utils.format_bytes(old_size)}\"\n         puts \"Database size after:  #{Histlog::Utils.format_bytes(new_size)}\"\n-        savings = old_size - new_size\n-        percentage = savings > 0 ? (savings.to_f / old_size * 100).round(2) : 0\n-        if savings > 0\n+        savings = (old_size || 0) - (new_size || 0)\n+        if old_size && old_size > 0 && savings > 0\n+          percentage = (savings.to_f / old_size * 100).round(2)\n           puts \"Space saved:         #{Histlog::Utils.format_bytes(savings)} (#{percentage}%)\"\n         else\n           puts \"Space saved:         0 (already optimized)\"\n@@ -1834,7 +1842,8 @@ module Histlog\n         opts.banner = \"Usage: histlog init [--install] [--force] [--rc-file PATH] <shell>\"\n         opts.on(\"--install\", \"Install the init snippet into the shell rc file (appends)\") { options[:install] = true }\n         opts.on(\"--force\", \"Overwrite existing histlog snippet in rc file if present\") { options[:force] = true }\n-        opts.on(\"--rc-file PATH\", String, \"Install to a specific rc file instead of the default for the shell\") do |path|\n+        opts.on(\"--rc-file PATH\", String,\n+                \"Install to a specific rc file instead of the default for the shell\") do |path|\n           options[:rc_file] = File.expand_path(path)\n         end\n         opts.on(\"-h\", \"--help\", \"Show this help\") do\n@@ -2323,14 +2332,22 @@ module Histlog\n                   same_cwd = 0\n                   begin\n                     if env_session_id && env_session_id > 0\n-                      chk = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id WHERE ct.command = ? AND c.session_id = ?\", [command_text, env_session_id])\n+                      chk = db.get_first_value(\n+                        \"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id WHERE ct.command = ? AND c.session_id = ?\", [\n+                          command_text, env_session_id\n+                        ]\n+                      )\n                       same_session = chk.to_i > 0 ? 1 : 0\n                     end\n                   rescue SQLite3::Exception\n                     same_session = 0\n                   end\n                   begin\n-                    chk2 = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE ct.command = ? AND p.path = ?\", [command_text, cwd])\n+                    chk2 = db.get_first_value(\n+                      \"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE ct.command = ? AND p.path = ?\", [\n+                        command_text, cwd\n+                      ]\n+                    )\n                     same_cwd = chk2.to_i > 0 ? 1 : 0\n                   rescue SQLite3::Exception\n                     same_cwd = 0","raw_url":"https://github.com/wstein/bin/raw/48384354dafff50519b4e82463c7e769d604c4c5/histlog","sha":"905c630726cd4cc93775b5afb2cf0bc50bb0c1a2","status":"modified"}]}