{"files":[{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/SELFHOST.md","changes":19,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=dd0a084ed43e29f40dc41cddc9a1116045afb5c1","deletions":3,"filename":"SELFHOST.md","patch":"@@ -353,9 +353,22 @@ the `length` composite) lowers fully. The lexer is also **callable cross-module*\n on Rust now: a type named in a `pub` function's signature is emitted `pub` (a\n `pub enum` / `pub struct`, fields public too), so `pub fn tokenize(…) ->\n Vec<Token>` no longer exposes a private `Token` — external code can name\n-`selfhost_lexer::Token` and call `tokenize`. The one Rust item still open is the\n-partial **parser**: it needs total clauses (Rust enforces exhaustiveness; the\n-gate is the same one Rian enforces — by design, not an emitter bug).\n+`selfhost_lexer::Token` and call `tokenize`.\n+\n+**The parser is now total**, so it clears the exhaustiveness gate too: an\n+unexpected/leftover token yields an `EErr` node rather than a missing clause, and\n+the parser lowers to both targets (`parse([]) = EErr` on the BEAM; the emitted\n+Rust carries `Expr::EErr`). That was the gate the user flagged — Rust requires\n+exhaustive `match`es, the same totality Rian enforces, so the fix was total\n+clauses, by design.\n+\n+Lowering the parser surfaced the **next** Rust item (a separate, real codegen\n+feature, not exhaustiveness): a **recursive ADT** — `type Expr := Add(Expr, Expr)`\n+→ `enum Expr { Add(Expr, Expr) }` — has infinite size in Rust and must be\n+`Box`-indirected (`Add(Box<Expr>, Box<Expr>)`, `Box::new(...)` at construction,\n+deref at use). This affects every tree-shaped ADT (parser/optimizer/evaluator/\n+checker), which run fine on BEAM/JS (boxing is implicit there) but need the Box\n+pass to `rustc`-compile. It is the consolidated next Rust increment.\n \n **Rust gets cons.** A Rian `Vec(T)` param lowers to a `&[T]` slice, so cons\n patterns become **Rust slice patterns** — `[h | t]` → `[h, t @ ..]` — matched","raw_url":"https://github.com/wstein/rian-lab/raw/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/SELFHOST.md","sha":"476afe15c3c6f90e0cac71f87e8fae4af7e844fc","status":"modified"},{"additions":9,"blob_url":"https://github.com/wstein/rian-lab/blob/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/examples%2Frian%2Fselfhost_parser.rian","changes":11,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_parser.rian?ref=dd0a084ed43e29f40dc41cddc9a1116045afb5c1","deletions":2,"filename":"examples/rian/selfhost_parser.rian","patch":"@@ -18,12 +18,16 @@ mod SelfhostParser do\n   # The token stream this parser consumes (same shape the lexer emits).\n   type Token := TNum(Int64) | TPlus | TMinus | TStar | TSlash | TLParen | TRParen\n \n-  # The parser's own typed output — a sealed expression tree (ADR-0034).\n+  # The parser's own typed output — a sealed expression tree (ADR-0034). `EErr`\n+  # makes the parser **total**: an unexpected/leftover token yields `EErr` rather\n+  # than a missing clause, so every `def` is exhaustive (and lowers to Rust,\n+  # whose `match` must be total — there is no partial path).\n   type Expr := Num(Int64)\n              | Add(Expr, Expr)\n              | Sub(Expr, Expr)\n              | Mul(Expr, Expr)\n              | Div(Expr, Expr)\n+             | EErr\n \n   # One parse step yields the node built so far and the unconsumed tokens. A\n   # single-constructor sum is the pair `(Expr, Vec(Token))` without tuple types.\n@@ -79,12 +83,15 @@ mod SelfhostParser do\n   end\n   def parse_term_tail(left, ts) := P(left, ts)\n \n-  # factor := TNum | '(' expr ')'  — a literal, or a parenthesized subexpression.\n+  # factor := TNum | '(' expr ')'  — a literal, or a parenthesized subexpression;\n+  # any other (or missing) token is a parse error (`EErr`), keeping it total.\n   def parse_factor(ts Vec(Token)) Parse\n   def parse_factor([TNum(n) | rest]) := P(Num(n), rest)\n   def parse_factor([TLParen | rest])\n     case parse_expr(rest) do\n       P(e, [TRParen | rest2]) -> P(e, rest2)\n+      P(_, other) -> P(EErr, other)\n     end\n   end\n+  def parse_factor(ts) := P(EErr, ts)\n end","raw_url":"https://github.com/wstein/rian-lab/raw/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/examples%2Frian%2Fselfhost_parser.rian","sha":"8f5e51d3b6048e6e5a502f39947498a1af84ec7f","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/rian-lab/blob/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/test%2Frian%2Fbeam_test.exs","changes":14,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fbeam_test.exs?ref=dd0a084ed43e29f40dc41cddc9a1116045afb5c1","deletions":0,"filename":"test/rian/beam_test.exs","patch":"@@ -128,6 +128,20 @@ defmodule Rian.BeamTest do\n       # left-associativity of same-precedence operators\n       assert parser.parse(lexer.tokenize(\"10 - 3 - 2\")) ==\n                {:sub, {:sub, {:num, 10}, {:num, 3}}, {:num, 2}}\n+\n+      # the parser is TOTAL: malformed/empty input yields `EErr`, not a crash —\n+      # so every clause is exhaustive (and the parser clears the dual-target gate)\n+      assert parser.parse([]) == :e_err\n+      assert parser.parse(lexer.tokenize(\")\")) == :e_err\n+    end\n+\n+    test \"the total parser clears the exhaustiveness gate and lowers to both targets\" do\n+      # (was rejected by `Lower.check!` as partial before `EErr` made it total)\n+      out = Rian.Decl.compile(File.read!(\"examples/rian/selfhost_parser.rian\"))\n+      rust = Enum.map_join(out, \"\\n\", fn {_, %{rust: r}} -> r end)\n+      assert rust =~ \"enum Expr\"\n+      # the parse-error fallback is present in the emitted Rust\n+      assert rust =~ \"Expr::EErr\"\n     end\n \n     test \"the self-hosting evaluator folds the Expr sum with a map symbol table\" do","raw_url":"https://github.com/wstein/rian-lab/raw/dd0a084ed43e29f40dc41cddc9a1116045afb5c1/test%2Frian%2Fbeam_test.exs","sha":"b2a12f9495ff08907fd6f5974978091617da4831","status":"modified"}]}