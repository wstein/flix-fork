{"files":[{"additions":67,"blob_url":"https://github.com/wstein/bin/blob/2da2ec332abb1135d80bf050cc42c955ae9ba964/2clone","changes":68,"contents_url":"https://api.github.com/repos/wstein/bin/contents/2clone?ref=2da2ec332abb1135d80bf050cc42c955ae9ba964","deletions":1,"filename":"2clone","patch":"@@ -61,6 +61,70 @@ Trim() {\n     printf '%s' \"${v%/}\"\n }\n \n+# Strip query strings, fragments, and common web UI suffixes so browser URLs\n+# clone the underlying repository root.\n+#\n+# $1 - repository path without protocol/host\n+# returns - normalized repository path\n+NormalizeRepoPath() {\n+    path=$1\n+    path=${path%%\\?*}\n+    path=${path%%\\#*}\n+\n+    case \"$path\" in\n+        */-/*)\n+            path=${path%%/-/*}\n+            ;;\n+        */tree/*)\n+            path=${path%%/tree/*}\n+            ;;\n+        */blob/*)\n+            path=${path%%/blob/*}\n+            ;;\n+        */src/*)\n+            path=${path%%/src/*}\n+            ;;\n+    esac\n+\n+    printf '%s' \"${path%/}\"\n+}\n+\n+# Build a normalized clone URL while preserving the original transport when\n+# possible (e.g. SSH stays SSH).\n+#\n+# $1 - original URL\n+# $2 - normalized host\n+# $3 - normalized repository path\n+# returns - clone URL\n+BuildCloneURL() {\n+    original=$1\n+    host=$2\n+    path=$3\n+\n+    case \"$original\" in\n+        *://*)\n+            scheme=${original%%://*}\n+            rest=${original#*://}\n+            case \"$rest\" in\n+                *@*)\n+                    userinfo=${rest%%@*}\n+                    printf '%s://%s@%s/%s' \"$scheme\" \"$userinfo\" \"$host\" \"$path\"\n+                    ;;\n+                *)\n+                    printf '%s://%s/%s' \"$scheme\" \"$host\" \"$path\"\n+                    ;;\n+            esac\n+            ;;\n+        *@*:* )\n+            ssh_host=${original%%:*}\n+            printf '%s:%s' \"$ssh_host\" \"$path\"\n+            ;;\n+        *)\n+            printf 'https://%s/%s' \"$host\" \"$path\"\n+            ;;\n+    esac\n+}\n+\n # Parse a repository URL into host and path components.\n #\n # $1 - URL (possibly including protocol)\n@@ -86,6 +150,7 @@ ParseURL() {\n     fi\n     # normalise host\n     host=$(printf '%s' \"$host\" | sed -e 's/^www\\.//')\n+    path=$(NormalizeRepoPath \"$path\")\n     # strip trailing .git\n     path=$(printf '%s' \"$path\" | sed -e 's/\\.git$//')\n     if [ -z \"$host\" ] || [ -z \"$path\" ]; then\n@@ -122,11 +187,12 @@ Main() {\n     fi\n     host=${parsed%% *}\n     path=${parsed#* }\n+    clone_url=$(BuildCloneURL \"$url\" \"$host\" \"$path\")\n \n     target=\"$HOME/$host/$path\"\n \n     Println \"Cloning $orig → $target\" >&2\n-    cmd=\"git clone --depth '$depth' --recurse-submodules '$orig' '$target'\"\n+    cmd=\"git clone --depth '$depth' --recurse-submodules '$clone_url' '$target'\"\n     if [ \"$dryrun\" -eq 1 ]; then\n         Println \"$cmd\" >&2\n         Return \"$target\"","raw_url":"https://github.com/wstein/bin/raw/2da2ec332abb1135d80bf050cc42c955ae9ba964/2clone","sha":"509e183ea872e2ec0d2339d6b0908860f0c97457","status":"modified"}]}