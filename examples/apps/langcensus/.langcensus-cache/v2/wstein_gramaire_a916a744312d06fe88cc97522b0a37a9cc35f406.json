{"files":[{"additions":25,"blob_url":"https://github.com/wstein/gramaire/blob/a916a744312d06fe88cc97522b0a37a9cc35f406/lab%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Flab%2FLabApiSuite.scala","changes":25,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/lab%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Flab%2FLabApiSuite.scala?ref=a916a744312d06fe88cc97522b0a37a9cc35f406","deletions":0,"filename":"lab/.jvm/src/test/scala/gramaire/lab/LabApiSuite.scala","patch":"@@ -581,6 +581,31 @@ class LabApiSuite extends munit.FunSuite:\n         assert(p.message.exists(_.span.isDefined), \"expected the reject diagnostic to carry a span\")\n   }\n \n+  test(\n+    \"evaluate: strategy \\\"ll-star\\\" trailing input after a complete parse never leaks the internal EOF sentinel\"\n+  ) {\n+    // \"1+2\" is a complete, valid parse on its own; the trailing \" 3\" is what triggers\n+    // LlError.expected == Vector(\"$\") — the internal marker for \"the walk finished early, input\n+    // remained\" — which diagnosticForLlError must never surface verbatim as an expected terminal.\n+    val resp =\n+      LabApi.evaluate(LabRequest(calcMd, Some(\"1+2 3\"), Method.Canonical, strategy = \"ll-star\"))\n+    resp.parse match\n+      case None => fail(\"expected a parse result\")\n+      case Some(p) =>\n+        assert(!p.accepted)\n+        p.message match\n+          case None => fail(\"expected a located reject diagnostic\")\n+          case Some(m) =>\n+            assert(\n+              m.message.contains(\"after a complete parse\"),\n+              s\"expected the complete-parse message, got: ${m.message}\"\n+            )\n+            assert(\n+              m.notes.forall(!_.contains(\"`$`\")),\n+              s\"must not leak the internal EOF sentinel as an expected terminal, got: ${m.notes}\"\n+            )\n+  }\n+\n   test(\"evaluate: strategy \\\"lr\\\" never populates llTrace, even on an accepted parse\") {\n     val resp = LabApi.evaluate(LabRequest(calcMd, Some(\"1+2*3\"), Method.Canonical))\n     assert(resp.parse.exists(_.accepted))","raw_url":"https://github.com/wstein/gramaire/raw/a916a744312d06fe88cc97522b0a37a9cc35f406/lab%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Flab%2FLabApiSuite.scala","sha":"c1b417a05173a0d98da152f1b36647b7e7a9e254","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/gramaire/blob/a916a744312d06fe88cc97522b0a37a9cc35f406/lab%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Flab%2FLabApi.scala","changes":6,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/lab%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Flab%2FLabApi.scala?ref=a916a744312d06fe88cc97522b0a37a9cc35f406","deletions":1,"filename":"lab/src/main/scala/gramaire/lab/LabApi.scala","patch":"@@ -559,8 +559,12 @@ object LabApi:\n   // means the walk completed a full parse but input remained — everywhere else, `expected` names\n   // the terminal(s) that would have continued the parse at `pos`.\n   private def diagnosticForLlError(err: LlError, spanned: Vector[Spanned]): Diagnostic =\n+    // `expected == Vector(\"$\")` is the internal EOF sentinel (the walk completed a full parse but\n+    // input remained) — the message below already says so in plain language; a \"note: expected\n+    // one of: `$`\" alongside it would leak that sentinel to the user instead of naming a real\n+    // terminal, which is what this note means everywhere else.\n     val expectedNote =\n-      if err.expected.isEmpty then Vector.empty\n+      if err.expected.isEmpty || err.expected == Vector(\"$\") then Vector.empty\n       else Vector(\"note: expected one of: \" + err.expected.map(t => s\"`$t`\").mkString(\", \"))\n     val message = spanned.lift(err.pos) match\n       case Some(s) if err.expected == Vector(\"$\") =>","raw_url":"https://github.com/wstein/gramaire/raw/a916a744312d06fe88cc97522b0a37a9cc35f406/lab%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Flab%2FLabApi.scala","sha":"f4a7e1e0af82f616e6f985c100e139ee663801bf","status":"modified"}]}