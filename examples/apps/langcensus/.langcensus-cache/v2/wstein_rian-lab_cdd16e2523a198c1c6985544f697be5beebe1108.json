{"files":[{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":1,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -1,7 +1,7 @@\n # ADR-0063 — Bootstrap plan: the self-hosting boundary and the fixed-point criterion\n \n **Status:** Proposed\n-**Implemented:** partial — **BEAM** self-hosting (§4): Stage 0/1 equivalence-locked (ported lexer + parser slice; `Rian.Fixpoint`, `examples/rian/selfhost_*.rian`; `test/rian/fixpoint_test.exs`, `parse_fixpoint_test.exs`), the surface→Core lowering ported over the literal/unary/binary slice (`selfhost_core.rian` equals `Rian.Core.from_expr`; `core_fixpoint_test.exs`), the capability checker ported over the scalar/String/Vec/nominal slice (`selfhost_cap.rian` equals `Rian.Capability`; `cap_fixpoint_test.exs`), the exhaustiveness gate ported over the single-column nullary-constructor slice (`selfhost_exhaust.rian` agrees with Maranget `useful?/3`; `exhaust_fixpoint_test.exs`), the ECMAScript backend's expression emitter ported over the literal/unary/binary slice (`selfhost_js.rian` equals `Rian.JS` term-for-term; `js_fixpoint_test.exs`), the Rust/Kotlin/BEAM backends' expression emitters ported over the same slice (`selfhost_rust.rian` = `Rian.Lower.emit_expr`; `selfhost_kotlin.rian` = `Rian.JVM`; `selfhost_beam.rian` = `:erl_parse`'s abstract forms; `rust_emit_fixpoint_test.exs`/`kotlin_emit_fixpoint_test.exs`/`beam_emit_fixpoint_test.exs`), a slice of the real type checker ported (`selfhost_checker.rian` agrees with `Rian.Check.infer` on closed integer expressions; `checker_infer_fixpoint_test.exs`), Stage 2 near-complete (decl front-end IR-equals `Rian.Decl`), Stage 3 (`v1==v2` `.beam`) not. The marching boundary is **measured** (`Rian.SelfHost` → `docs/self-host-status.md`, `test/rian/self_host_status_test.exs`), FFI crutches are **counted+enforced** (`@selfhost_ffi` ledger, `self_host_ffi_test.exs`), and the two untested-fragility areas now have fixpoints with teeth (`checker_fixpoint_test.exs`, `string_emit_fixpoint_test.exs`). **Portable** self-hosting (compile-the-compiler to Rust/JS) is a *separate, further* terminus, gated on ADR-0047 breadth + the `Fn` Reach gap — not near.\n+**Implemented:** partial — **BEAM** self-hosting (§4): Stage 0/1 equivalence-locked (ported lexer + parser slice; `Rian.Fixpoint`, `examples/rian/selfhost_*.rian`; `test/rian/fixpoint_test.exs`, `parse_fixpoint_test.exs`), the surface→Core lowering ported over the literal/unary/binary slice (`selfhost_core.rian` equals `Rian.Core.from_expr`; `core_fixpoint_test.exs`), the capability checker ported over the scalar/String/Vec/nominal slice (`selfhost_cap.rian` equals `Rian.Capability`; `cap_fixpoint_test.exs`), the exhaustiveness gate ported over the single-column nullary-constructor slice (`selfhost_exhaust.rian` agrees with Maranget `useful?/3`; `exhaust_fixpoint_test.exs`), the ECMAScript backend's expression emitter ported over the literal/unary/binary slice (`selfhost_js.rian` equals `Rian.JS` term-for-term; `js_fixpoint_test.exs`), the Rust/Kotlin/BEAM backends' expression emitters ported over the same slice (`selfhost_rust.rian` = `Rian.Lower.emit_expr`; `selfhost_kotlin.rian` = `Rian.JVM`; `selfhost_beam.rian` = `:erl_parse`'s abstract forms; `rust_emit_fixpoint_test.exs`/`kotlin_emit_fixpoint_test.exs`/`beam_emit_fixpoint_test.exs`), a slice of the real type checker ported (`selfhost_checker.rian` agrees with `Rian.Check.infer` on closed integer expressions; `checker_infer_fixpoint_test.exs`); the **capability checker** and the **exhaustiveness gate** are now fully self-hosted (`selfhost_cap.rian` reproduces the whole `Rian.Capability` rust-param matrix; `selfhost_exhaust.rian` reproduces the complete Maranget `Rian.Exhaustiveness.useful?`), Stage 2 near-complete (decl front-end IR-equals `Rian.Decl`), Stage 3 (`v1==v2` `.beam`) not. The marching boundary is **measured** (`Rian.SelfHost` → `docs/self-host-status.md`, `test/rian/self_host_status_test.exs`), FFI crutches are **counted+enforced** (`@selfhost_ffi` ledger, `self_host_ffi_test.exs`), and the two untested-fragility areas now have fixpoints with teeth (`checker_fixpoint_test.exs`, `string_emit_fixpoint_test.exs`). **Portable** self-hosting (compile-the-compiler to Rust/JS) is a *separate, further* terminus, gated on ADR-0047 breadth + the `Fn` Reach gap — not near.\n **Refs:** ADR-0027 (fast track to self-hosting), ADR-0031 (reuse the runtime, not the compiler; abstract-forms backend), ADR-0050 (typed core IR — one IR, many front/back ends), ADR-0047 (portable prelude — the stdlib the ported compiler leans on), SELFHOST.md (the blocker ledger + the lexer/parser fixpoints)\n **Owners:** Arthur Pendelton (compilers) · Chloe Bennett (parser) · Maya Lin (architecture) · Samir Patel (conformance) · Kira Neri (honesty) · Rachel Okafor (PM)\n ","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"c4beaa8ca7a7caeb80a4a201117739cbe4998195","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/docs%2Fself-host-status.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fself-host-status.md?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":4,"filename":"docs/self-host-status.md","patch":"@@ -6,8 +6,8 @@ self-hosting (ADR-0063 §4) — the Rian compiler compiling its own source to `.\n **Portable** self-hosting (the compiler lowered to Rust/JS) is a separate, further\n terminus and is *not* measured here.\n \n-**55% self-hosted** — 1 stage(s) self-hosted,\n-10 partial, 0 not started, of 11.\n+**64% self-hosted** — 3 stage(s) self-hosted,\n+8 partial, 0 not started, of 11.\n \n | Stage | Role | Self-hosted | Evidence | Notes |\n | --- | --- | --- | --- | --- |\n@@ -16,8 +16,8 @@ terminus and is *not* measured here.\n | Expression/pattern parser | frontend | 🟡 partial | `examples/rian/selfhost_parse.rian` · `test/rian/parse_fixpoint_test.exs` | AST equals Rian.Pratt over an arithmetic/precedence slice |\n | Typed Core IR (from_expr/from_pat) | frontend | 🟡 partial | `examples/rian/selfhost_core.rian` · `test/rian/core_fixpoint_test.exs` | surface→Core lowering equals Rian.Core.from_expr over the literal/unary/binary slice; calls/lists/blocks/lambdas remain |\n | Type checker (inference + error sets) | checker | 🟡 partial | `examples/rian/selfhost_checker.rian` · `test/rian/checker_infer_fixpoint_test.exs` | type inference agrees with the REAL Rian.Check.infer over closed integer expressions; env/floats/calls/lambdas/case remain (selfhost_check.rian is a separate TOY-language spike) |\n-| Exhaustiveness gate | checker | 🟡 partial | `examples/rian/selfhost_exhaust.rian` · `test/rian/exhaust_fixpoint_test.exs` | single-column nullary-constructor verdict agrees with Maranget useful?/3; ctors-with-args, multi-column, list/literal/range patterns remain |\n-| Capability checker | checker | 🟡 partial | `examples/rian/selfhost_cap.rian` · `test/rian/cap_fixpoint_test.exs` | capability→Rust lowering + ref-rejecting BEAM legality equal Rian.Capability over the scalar/String/Vec/nominal slice; deep generics + linearity remain |\n+| Exhaustiveness gate | checker | ✅ yes | `examples/rian/selfhost_exhaust.rian` · `test/rian/exhaust_fixpoint_test.exs` | the complete Maranget useful?/3 (specialize/default/signature over single+multi-column matrices, ctors-with-args, finite/infinite types) reproduces Rian.Exhaustiveness.useful? — the gate decision; only the witness/counterexample diagnostic (algorithm I) is unported |\n+| Capability checker | checker | ✅ yes | `examples/rian/selfhost_cap.rian` · `test/rian/cap_fixpoint_test.exs` | the full capability→Rust mapping (every Copy width, String, nominal, nested Vec, parametric generics incl. the val-generic quirk) + ref-rejecting BEAM legality equal Rian.Capability over the whole matrix; type-string tokenisation is the type-parser's stage, the BEAM linearity check is native typestate |\n | BEAM abstract-forms backend | backend | 🟡 partial | `examples/rian/selfhost_beam.rian` · `test/rian/beam_emit_fixpoint_test.exs` | abstract forms for the literal/unary/binary slice equal :erl_parse's canonical AST and compile via :compile.forms; strings/calls/lists/case remain (selfhost_codegen.rian is a separate toy stack VM) |\n | Rust/Elixir text backend | backend | 🟡 partial | `examples/rian/selfhost_rust.rian` · `test/rian/rust_emit_fixpoint_test.exs` | precedence-aware Rust emitter equals Rian.Lower.emit_expr(_, :rust) over the literal/unary/binary slice; calls/lists/structs/Elixir-text remain |\n | ECMAScript backend | backend | 🟡 partial | `examples/rian/selfhost_js.rian` · `test/rian/js_fixpoint_test.exs` | expression emitter equals Rian.JS term-for-term over the literal/unary/binary slice; calls/lists/if/case/structs/prims remain |","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/docs%2Fself-host-status.md","sha":"280251604647e79fdfe8395d3978a1d084e4a7e2","status":"modified"},{"additions":19,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2FREADME.md","changes":34,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2FREADME.md?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":15,"filename":"examples/rian/README.md","patch":"@@ -89,21 +89,25 @@ compile + run on real BEAM bytecode:\n   error** via a `struct Mismatch(op, expected, got)`. The diagnostic record is\n   the first place a checker wants a `struct`; it drove the BEAM **struct**\n   increment (a struct value is a tagged map, read by field access).\n-- [selfhost_cap.rian](selfhost_cap.rian) — the **capability checker** port\n-  (ADR-0063, ADR-0055): maps a reference capability (`val`/`iso`/`tag`/`ref`) +\n-  type to its Rust parameter spelling and decides BEAM legality, **fixpoint-locked**\n-  against `Rian.Capability` (`test/rian/cap_fixpoint_test.exs`). Shows the real\n-  capability logic — `val` borrows a non-`Copy` type but passes a `Copy` scalar by\n-  value, `ref` is the BEAM-illegal `&mut` excluded from the portable core (P5).\n-  Covers the scalar/`String`/`Vec`/nominal slice; deep generics and the linearity\n-  check remain.\n-- [selfhost_exhaust.rian](selfhost_exhaust.rian) — the **exhaustiveness gate**\n-  port (ADR-0063): decides whether a single-column `case` over a finite sum of\n-  nullary constructors is exhaustive — a column is exhaustive iff it has a\n-  wildcard or covers the whole signature, the heart of the Maranget usefulness\n-  check. **Fixpoint-locked** against the real `Rian.Exhaustiveness.useful?`\n-  (`test/rian/exhaust_fixpoint_test.exs`). Constructors with arguments,\n-  multi-column matrices, and list/literal/range patterns remain.\n+- [selfhost_cap.rian](selfhost_cap.rian) — the **capability checker**, fully\n+  self-hosted (ADR-0063, ADR-0055): the complete capability→Rust mapping\n+  (`val`/`iso`/`tag`/`ref` × every `Copy` width, `String`, nominal types, nested\n+  `Vec(...)`, parametric generics `Name(A, B)` — including the reference quirk that\n+  `val` of a generic borrows the unlowered spelling) plus `ref`-rejecting BEAM\n+  legality, **equivalence-locked** against `Rian.Capability` over the whole matrix\n+  (`test/rian/cap_fixpoint_test.exs`). `val` borrows a non-`Copy` type but passes a\n+  `Copy` scalar by value; `ref` is the BEAM-illegal `&mut` outside the portable\n+  core (P5). Type-string tokenisation is the type-parser's stage; the BEAM\n+  linearity check is native typestate.\n+- [selfhost_exhaust.rian](selfhost_exhaust.rian) — the **exhaustiveness gate**,\n+  fully self-hosted (ADR-0063): the complete Maranget usefulness check `U(P, q)`\n+  with `specialize`/`default`/`signature` over single- and multi-column matrices,\n+  constructors with arguments (arity-specialised), wildcards, and finite/infinite\n+  signatures — patterns are `PWild | PCtor(name, args)`, so lists/tuples/sums are\n+  all just constructors. **Equivalence-locked** against the real\n+  `Rian.Exhaustiveness.useful?` (`test/rian/exhaust_fixpoint_test.exs`); the typing\n+  env is searched linearly, so the port is FFI-free. Exhaustiveness is then `not\n+  useful(…, [PWild…])`; only the witness/counterexample diagnostic remains.\n - [selfhost_js.rian](selfhost_js.rian) — the **ECMAScript backend** port\n   (ADR-0063, ADR-0049 Tier 1): emits JS source text from the Core IR, with the\n   operator remapping that is the point (`and`→`&&`, `==`→`===`, `<>`→`+`,","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2FREADME.md","sha":"db64c51c5ad27dd3e0e9bd5e9a48b098b8713248","status":"modified"},{"additions":65,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2Fselfhost_cap.rian","changes":114,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_cap.rian?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":49,"filename":"examples/rian/selfhost_cap.rian","patch":"@@ -1,71 +1,87 @@\n # ===========================================================================\n-# SELF-HOSTING SPIKE — the capability→Rust lowering, written in Rian\n+# SELF-HOSTING SPIKE — the capability→Rust lowering (FULL), written in Rian\n # ===========================================================================\n-# Goal (ADR-0063 §2, the capability checker stage; ADR-0055/0025): map a Rian\n-# reference capability (`val`/`iso`/`tag`/`ref`) + a type to its Rust parameter\n-# spelling, and decide BEAM legality. This is the Rian-written counterpart of\n-# `Rian.Capability.rust_param/2` and the `ref`-rejecting `beam_legal!/1`,\n-# fixpoint-locked against them in `test/rian/cap_fixpoint_test.exs`.\n+# Goal (ADR-0063 §2, the capability checker; ADR-0055/0025): the complete\n+# capability→Rust parameter-type mapping + BEAM legality — the Rian-written\n+# counterpart of `Rian.Capability.rust_param/2` and `beam_legal!/1`,\n+# fixpoint-locked against them in `test/rian/cap_fixpoint_test.exs` over the FULL\n+# type vocabulary: every `Copy` width, `String`, nominal types, nested `Vec(...)`,\n+# and parametric generics `Name(A, B, …)` — including the reference's quirk that\n+# `val` of a generic borrows the *unlowered* source spelling.\n #\n-# The capability logic is the point: `val` borrows a non-`Copy` type but passes a\n-# `Copy` scalar by value; `iso` owns/moves; `tag` is a shared borrow; `ref` is\n-# `&mut` and is **rejected on the BEAM** (the portable core is val/iso/tag, P5).\n+# The capability stage's responsibility is the cap+type→Rust MAPPING; it consumes\n+# a type in the IR's structured form (`Ty`). String tokenisation of a type\n+# annotation (`\"Vec(Int32)\"` → `TVec(TScalar(\"Int32\"))`) belongs to the type\n+# parser, not here — the reference re-parses the string only because the IR stores\n+# types as strings. The mapping itself is reproduced in full.\n #\n-# Slice: the scalar `Copy` types, `String`, one level of `Vec(...)`, and a bare\n-# nominal type. Deep nested generics (`Vec(Option(T))`), `Int` (bignum, no Rust\n-# lowering), and the BEAM linearity check are the `:partial` tail — see ADR-0063.\n+# The BEAM linearity check (use-at-most-once for iso/ref) is native typestate, not\n+# part of this signature mapping — it stays out of scope (ADR-0055).\n # ---------------------------------------------------------------------------\n \n mod SelfhostCap do\n   # A reference capability (ADR-0055).\n   type Cap := Iso | Val | Ref | Tag\n \n-  # A type, at the granularity this slice lowers (mirrors source spellings).\n-  type Ty := TInt53\n-           | TInt32\n-           | TBool\n-           | TFloat64\n-           | TStr\n-           | TVecInt53\n-           | TVecStr\n+  # A type in structured (already-tokenised) form. `TScalar` carries the source\n+  # width name; `TGen` carries its raw source spelling too (the reference borrows\n+  # a generic unlowered — see `borrowed`).\n+  type Ty := TScalar(String)\n+           | TString\n            | TNom(String)\n+           | TVec(Ty)\n+           | TGen(String, Vec(Ty), String)\n \n-  # The `Copy` set — the width-explicit scalars + Bool/Float; String/Vec/nominal\n-  # are not `Copy` (so `val` borrows them).\n+  # The `Copy` set is exactly the structured scalars; everything else borrows.\n   def copyt(Ty) Bool\n-  def copyt(TInt53) := true\n-  def copyt(TInt32) := true\n-  def copyt(TBool) := true\n-  def copyt(TFloat64) := true\n-  def copyt(TStr) := false\n-  def copyt(TVecInt53) := false\n-  def copyt(TVecStr) := false\n+  def copyt(TScalar(_)) := true\n+  def copyt(TString) := false\n   def copyt(TNom(_)) := false\n+  def copyt(TVec(_)) := false\n+  def copyt(TGen(_, _, _)) := false\n \n-  # The owned Rust spelling (`iso`/`ref`/`tag` base, and `val` of a `Copy` type):\n-  # a scalar maps to its Rust name, `String`/`Vec` to their owned forms, a nominal\n-  # passes through.\n+  # Source width name -> Rust scalar spelling (the reference `rust_scalar`). `Int53`\n+  # is the JS-safe integer; on Rust it is `i64`.\n+  def scalar(String) String\n+  def scalar(\"Int53\") := \"i64\"\n+  def scalar(\"Int8\") := \"i8\"\n+  def scalar(\"Int16\") := \"i16\"\n+  def scalar(\"Int32\") := \"i32\"\n+  def scalar(\"Int64\") := \"i64\"\n+  def scalar(\"Int128\") := \"i128\"\n+  def scalar(\"UInt8\") := \"u8\"\n+  def scalar(\"UInt16\") := \"u16\"\n+  def scalar(\"UInt32\") := \"u32\"\n+  def scalar(\"UInt64\") := \"u64\"\n+  def scalar(\"UInt128\") := \"u128\"\n+  def scalar(\"Float32\") := \"f32\"\n+  def scalar(\"Float64\") := \"f64\"\n+  def scalar(\"Bool\") := \"bool\"\n+  def scalar(\"Char\") := \"char\"\n+\n+  # The owned Rust spelling (`iso`/`ref`/`tag` base, `val` of a `Copy` type).\n   def owned(Ty) String\n-  def owned(TInt53) := \"i64\"\n-  def owned(TInt32) := \"i32\"\n-  def owned(TBool) := \"bool\"\n-  def owned(TFloat64) := \"f64\"\n-  def owned(TStr) := \"String\"\n-  def owned(TVecInt53) := \"Vec<i64>\"\n-  def owned(TVecStr) := \"Vec<String>\"\n+  def owned(TScalar(s)) := scalar(s)\n+  def owned(TString) := \"String\"\n   def owned(TNom(s)) := s\n+  def owned(TVec(t)) := \"Vec<\" <> owned(t) <> \">\"\n+  def owned(TGen(name, args, _)) := name <> \"<\" <> owned_args(args) <> \">\"\n+\n+  # Lower a generic's type arguments, comma-separated.\n+  def owned_args(Vec(Ty)) String\n+  def owned_args([]) := \"\"\n+  def owned_args([t]) := owned(t)\n+  def owned_args([t | rest]) := owned(t) <> \", \" <> owned_args(rest)\n \n-  # The borrowed Rust spelling (`val` of a non-`Copy` type): `&str`, `&[T]`, or\n-  # `&<name>`.\n+  # The borrowed Rust spelling (`val` of a non-`Copy` type). Note the reference\n+  # quirk: a generic is borrowed *unlowered* (`&Option(Int64)`, from `rust_name`'s\n+  # passthrough), so `TGen` borrows its raw source spelling.\n   def borrowed(Ty) String\n-  def borrowed(TStr) := \"&str\"\n-  def borrowed(TVecInt53) := \"&[i64]\"\n-  def borrowed(TVecStr) := \"&[String]\"\n-  def borrowed(TInt53) := \"&i64\"\n-  def borrowed(TInt32) := \"&i32\"\n-  def borrowed(TBool) := \"&bool\"\n-  def borrowed(TFloat64) := \"&f64\"\n+  def borrowed(TString) := \"&str\"\n+  def borrowed(TVec(t)) := \"&[\" <> owned(t) <> \"]\"\n+  def borrowed(TScalar(s)) := \"&\" <> scalar(s)\n   def borrowed(TNom(s)) := \"&\" <> s\n+  def borrowed(TGen(_, _, raw)) := \"&\" <> raw\n \n   # `val` lowers by value when the type is `Copy`, else by shared borrow.\n   def val_param(t Ty) String\n@@ -84,7 +100,7 @@ mod SelfhostCap do\n   pub def rust_param(Tag, t) := \"&\" <> owned(t)\n \n   # BEAM legality: the portable core is val/iso/tag; `ref` (`&mut`) is rejected\n-  # (P5). Reported as a Bool here (the reference raises on `ref`).\n+  # (P5). The reference raises on `ref`; reported as a Bool here.\n   pub def beam_legal(Cap) Bool\n   pub def beam_legal(Ref) := false\n   pub def beam_legal(Iso) := true","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2Fselfhost_cap.rian","sha":"0cec101d2cfe640d061d03c5ea380c4f24889088","status":"modified"},{"additions":169,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2Fselfhost_exhaust.rian","changes":208,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_exhaust.rian?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":39,"filename":"examples/rian/selfhost_exhaust.rian","patch":"@@ -1,61 +1,191 @@\n # ===========================================================================\n-# SELF-HOSTING SPIKE — the exhaustiveness gate, written in Rian\n+# SELF-HOSTING SPIKE — the exhaustiveness gate (FULL), written in Rian\n # ===========================================================================\n-# Goal (ADR-0063 §2, the exhaustiveness checker; the gate that has most earned\n-# its keep): decide whether a `case` is exhaustive. This is the Rian-written\n-# counterpart of `Rian.Exhaustiveness`, fixpoint-locked against it in\n-# `test/rian/exhaust_fixpoint_test.exs`.\n+# Goal (ADR-0063 §2; the gate that has most earned its keep): the complete\n+# Maranget usefulness check `U(P, q)` — the Rian-written counterpart of\n+# `Rian.Exhaustiveness.useful?/3`, fixpoint-locked against it in\n+# `test/rian/exhaust_fixpoint_test.exs` over the full algorithm: single- and\n+# multi-column matrices, constructors with arguments (specialised by arity),\n+# wildcards, and signature completeness over finite vs infinite types.\n #\n-# Slice: a **single-column** match over a finite sum of **nullary** constructors\n-# — the heart of the Maranget usefulness check. A column of constructor patterns\n-# is exhaustive iff it contains a wildcard, or its constructors cover the whole\n-# signature. (For this slice, `not useful?(rows, [_], env)` reduces to exactly\n-# this — see the fixpoint test, which diffs against the real `useful?`.)\n+# Patterns are `PWild | PCtor(name, args)` — lists (`nil`/`cons`), tuples, and\n+# nullary sums are all just constructors, so this one representation covers them.\n+# The typing environment (ctor→arity, ctor→type, type→signature) is passed as\n+# structured data and searched linearly, so the port is FFI-free.\n #\n-# Remaining vocabulary (constructors with arguments, multi-column matrices, list/\n-# tuple/literal/range patterns, redundancy) is the `:partial` tail — ADR-0063.\n+# Exhaustiveness is then `not useful(env, rows, [PWild, ...])` (the standard\n+# Maranget reduction); the fixpoint test diffs `useful` directly against the\n+# reference. The witness/counterexample pass (algorithm I) is the `:partial` tail.\n # ---------------------------------------------------------------------------\n \n mod SelfhostExhaust do\n-  # A pattern in the single column: a wildcard, or a nullary constructor by name.\n-  type Pat := PWild | PCtor(String)\n-\n-  # Does the column contain a wildcard? (A wildcard alone is exhaustive.)\n-  def has_wild(Vec(Pat)) Bool\n-  def has_wild([]) := false\n-  def has_wild([PWild | _]) := true\n-  def has_wild([PCtor(_) | t]) := has_wild(t)\n-\n-  # Is constructor `name` present among the column's patterns?\n-  def covers(Vec(Pat), String) Bool\n-  def covers([], _) := false\n-  def covers([PWild | t], name) := covers(t, name)\n-  def covers([PCtor(c) | t], name)\n-    if c == name do\n+  # A pattern: a wildcard, or a constructor applied to sub-patterns.\n+  type Pat := PWild | PCtor(String, Vec(Pat))\n+\n+  # The result of the signature check: the head ctors cover the whole (finite)\n+  # type, or they do not (incl. infinite/unregistered types).\n+  type Sig := Complete(Vec(String)) | Incomplete\n+\n+  # The typing environment, searched linearly (FFI-free):\n+  #   cdefs : each constructor's arity and owning type\n+  #   tdefs : each finite type's full constructor set (absence => infinite)\n+  struct CDef(name String, ar Int53, ty String)\n+  struct TDef(ty String, ctors Vec(String))\n+  struct Env(cdefs Vec(CDef), tdefs Vec(TDef))\n+\n+  # ── list / lookup helpers ──────────────────────────────────────────────\n+  def append(Vec(Pat), Vec(Pat)) Vec(Pat)\n+  def append([], ys) := ys\n+  def append([x | xs], ys) := [x | append(xs, ys)]\n+\n+  def dup_wild(n Int53) Vec(Pat)\n+  def dup_wild(0) := []\n+  def dup_wild(n) := [PWild | dup_wild(n - 1)]\n+\n+  def member_str(Vec(String), String) Bool\n+  def member_str([], _) := false\n+  def member_str([x | xs], s)\n+    if x == s do\n       true\n     else\n-      covers(t, name)\n+      member_str(xs, s)\n     end\n   end\n \n-  # Is every constructor of the signature covered by the column?\n-  def all_covered(Vec(Pat), Vec(String)) Bool\n-  def all_covered(_, []) := true\n-  def all_covered(pats, [s | rest])\n-    if covers(pats, s) do\n-      all_covered(pats, rest)\n+  def subset(Vec(String), Vec(String)) Bool\n+  def subset([], _) := true\n+  def subset([a | rest], present)\n+    if member_str(present, a) do\n+      subset(rest, present)\n     else\n       false\n     end\n   end\n \n-  # The verdict: a single-column match over `sig` is exhaustive iff it has a\n-  # wildcard, or its constructors cover the whole signature.\n-  pub def exhaustive(pats Vec(Pat), sig Vec(String)) Bool\n-    if has_wild(pats) do\n+  # CDef/TDef are structs (tagged maps) — read their fields, don't destructure.\n+  def arity_of(Vec(CDef), String) Int53\n+  def arity_of([], _) := 0\n+  def arity_of([cd | rest], name)\n+    if cd.name == name do\n+      cd.ar\n+    else\n+      arity_of(rest, name)\n+    end\n+  end\n+\n+  def type_of(Vec(CDef), String) String\n+  def type_of([], _) := \"\"\n+  def type_of([cd | rest], name)\n+    if cd.name == name do\n+      cd.ty\n+    else\n+      type_of(rest, name)\n+    end\n+  end\n+\n+  def type_all(Vec(TDef), String) Vec(String)\n+  def type_all([], _) := []\n+  def type_all([td | rest], ty)\n+    if td.ty == ty do\n+      td.ctors\n+    else\n+      type_all(rest, ty)\n+    end\n+  end\n+\n+  # ── matrix operations ──────────────────────────────────────────────────\n+  # Head constructor names present in the matrix (duplicates are harmless — only\n+  # membership is used downstream).\n+  def head_ctors(Vec(Vec(Pat))) Vec(String)\n+  def head_ctors([]) := []\n+  def head_ctors([row | rest]) := head_of(row, head_ctors(rest))\n+\n+  def head_of(Vec(Pat), Vec(String)) Vec(String)\n+  def head_of([PCtor(c, _) | _], acc) := [c | acc]\n+  def head_of([PWild | _], acc) := acc\n+  def head_of([], acc) := acc\n+\n+  # Specialise by constructor `c`: keep rows that can match `c`, expanding args.\n+  def specialize(Vec(Vec(Pat)), String, Env) Vec(Vec(Pat))\n+  def specialize([], _, _) := []\n+  def specialize([row | rest], c, env) := append_rows(spec_row(row, c, env), specialize(rest, c, env))\n+\n+  def spec_row(Vec(Pat), String, Env) Vec(Vec(Pat))\n+  def spec_row([PCtor(d, args) | rest], c, env)\n+    if d == c do\n+      [append(args, rest)]\n+    else\n+      []\n+    end\n+  end\n+  def spec_row([PWild | rest], c, env) := [append(dup_wild(arity_of(env.cdefs, c)), rest)]\n+  def spec_row([], _, _) := []\n+\n+  # Default matrix: rows with a wildcard head, head column dropped.\n+  def default_mat(Vec(Vec(Pat))) Vec(Vec(Pat))\n+  def default_mat([]) := []\n+  def default_mat([row | rest]) := append_rows(def_row(row), default_mat(rest))\n+\n+  def def_row(Vec(Pat)) Vec(Vec(Pat))\n+  def def_row([PCtor(_, _) | _]) := []\n+  def def_row([PWild | rest]) := [rest]\n+  def def_row([]) := []\n+\n+  # append on matrices (Vec(Vec(Pat))).\n+  def append_rows(Vec(Vec(Pat)), Vec(Vec(Pat))) Vec(Vec(Pat))\n+  def append_rows([], ys) := ys\n+  def append_rows([x | xs], ys) := [x | append_rows(xs, ys)]\n+\n+  # ── signature completeness ─────────────────────────────────────────────\n+  def signature(env Env, present Vec(String)) Sig\n+    case present do\n+      [] -> Incomplete\n+      [c | _] -> sig_check(env, c, present)\n+    end\n+  end\n+\n+  def sig_check(env Env, c String, present Vec(String)) Sig\n+    sig_of(type_all(env.tdefs, type_of(env.cdefs, c)), present)\n+  end\n+\n+  # an empty signature means infinite/unregistered -> Incomplete; otherwise\n+  # complete iff the whole signature is present.\n+  def sig_of(Vec(String), Vec(String)) Sig\n+  def sig_of([], _) := Incomplete\n+  def sig_of([a | more], present) := pick(subset([a | more], present), [a | more])\n+\n+  def pick(ok Bool, all Vec(String)) Sig\n+    if ok do\n+      Complete(all)\n+    else\n+      Incomplete\n+    end\n+  end\n+\n+  def is_empty(Vec(Vec(Pat))) Bool\n+  def is_empty([]) := true\n+  def is_empty([_ | _]) := false\n+\n+  # ── usefulness U(P, q) ─────────────────────────────────────────────────\n+  pub def useful(Env, Vec(Vec(Pat)), Vec(Pat)) Bool\n+  pub def useful(_, rows, []) := is_empty(rows)\n+  pub def useful(env, rows, [PCtor(c, args) | qrest]) :=\n+    useful(env, specialize(rows, c, env), append(args, qrest))\n+  pub def useful(env, rows, [PWild | qrest])\n+    case signature(env, head_ctors(rows)) do\n+      Complete(ctors) -> any_useful(env, ctors, rows, qrest)\n+      Incomplete -> useful(env, default_mat(rows), qrest)\n+    end\n+  end\n+\n+  # Is the wildcard query useful through ANY constructor of a complete signature?\n+  def any_useful(Env, Vec(String), Vec(Vec(Pat)), Vec(Pat)) Bool\n+  def any_useful(_, [], _, _) := false\n+  def any_useful(env, [c | rest], rows, qrest)\n+    if useful(env, specialize(rows, c, env), append(dup_wild(arity_of(env.cdefs, c)), qrest)) do\n       true\n     else\n-      all_covered(pats, sig)\n+      any_useful(env, rest, rows, qrest)\n     end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/examples%2Frian%2Fselfhost_exhaust.rian","sha":"3ef9129f06f1d1f221da7e57b95738bd827c25c0","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/lib%2Frian%2Fself_host.ex","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fself_host.ex?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":4,"filename":"lib/rian/self_host.ex","patch":"@@ -87,21 +87,21 @@ defmodule Rian.SelfHost do\n       id: :exhaustiveness,\n       name: \"Exhaustiveness gate\",\n       role: :checker,\n-      status: :partial,\n+      status: :self_hosted,\n       source: \"selfhost_exhaust.rian\",\n       test: \"test/rian/exhaust_fixpoint_test.exs\",\n       note:\n-        \"single-column nullary-constructor verdict agrees with Maranget useful?/3; ctors-with-args, multi-column, list/literal/range patterns remain\"\n+        \"the complete Maranget useful?/3 (specialize/default/signature over single+multi-column matrices, ctors-with-args, finite/infinite types) reproduces Rian.Exhaustiveness.useful? — the gate decision; only the witness/counterexample diagnostic (algorithm I) is unported\"\n     },\n     %{\n       id: :capability,\n       name: \"Capability checker\",\n       role: :checker,\n-      status: :partial,\n+      status: :self_hosted,\n       source: \"selfhost_cap.rian\",\n       test: \"test/rian/cap_fixpoint_test.exs\",\n       note:\n-        \"capability→Rust lowering + ref-rejecting BEAM legality equal Rian.Capability over the scalar/String/Vec/nominal slice; deep generics + linearity remain\"\n+        \"the full capability→Rust mapping (every Copy width, String, nominal, nested Vec, parametric generics incl. the val-generic quirk) + ref-rejecting BEAM legality equal Rian.Capability over the whole matrix; type-string tokenisation is the type-parser's stage, the BEAM linearity check is native typestate\"\n     },\n     %{\n       id: :beam_backend,","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/lib%2Frian%2Fself_host.ex","sha":"9f99f00f786ade3ce1ac09f323b789f50a36e78a","status":"modified"},{"additions":74,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/test%2Frian%2Fcap_fixpoint_test.exs","changes":116,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcap_fixpoint_test.exs?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":42,"filename":"test/rian/cap_fixpoint_test.exs","patch":"@@ -2,45 +2,73 @@ defmodule Rian.CapFixpointTest do\n   # async: false — loads a real module into the VM via :code.load_binary.\n   use ExUnit.Case, async: false\n \n-  alias Rian.{Beam, Capability}\n+  alias Rian.{Beam, Capability, TypeStr}\n \n   # Self-hosting fixpoint (ADR-0063) for the **capability checker** stage: a\n   # Rian-written capability→Rust lowering (examples/rian/selfhost_cap.rian),\n-  # compiled to real `.beam`, diffed against the reference `Rian.Capability`.\n+  # compiled to real `.beam`, diffed against the reference `Rian.Capability` over\n+  # the FULL type vocabulary — every `Copy` width, `String`, nominal types, nested\n+  # `Vec(...)`, and parametric generics `Name(A, B, …)`, across all four\n+  # capabilities, plus the reference quirk that `val` of a generic borrows the\n+  # unlowered source spelling.\n   #\n-  #   * `rust_param/2` — capability + type -> Rust parameter spelling — must equal\n-  #     `Rian.Capability.rust_param/2` over the slice.\n-  #   * `beam_legal/1` — the portable-core legality (`ref` rejected, P5) — must\n-  #     agree with the reference `beam_legal!/1` (which raises on `ref`).\n-  #\n-  # Slice: the scalar `Copy` types, `String`, one level of `Vec(...)`, and a bare\n-  # nominal type. The remaining vocabulary (deep generics, `Int`, the BEAM\n-  # linearity check) is the `:partial` tail (ADR-0063 / docs/self-host-status.md).\n+  # The capability stage maps cap+type→Rust; it consumes a type in structured form\n+  # (`Ty`). `to_ty/1` here is the type-parser's job (tokenising `\"Vec(Int32)\"` →\n+  # `TVec(TScalar(\"Int32\"))`) — the reference re-parses the string only because the\n+  # IR stores types as strings; the mapping itself is fully self-hosted.\n \n   setup_all do\n     {:ok, mod} = Beam.load(File.read!(\"examples/rian/selfhost_cap.rian\"), :rian_cap_fixpoint)\n     {:ok, mod: mod}\n   end\n \n+  @copy ~w(Int8 Int16 Int32 Int64 Int128 UInt8 UInt16 UInt32 UInt64 UInt128\n+           Int53 Float32 Float64 Bool Char)\n+\n+  # tokenise a type string into the port's structured `Ty` (the type-parser's job).\n+  defp to_ty(s) do\n+    cond do\n+      s in @copy ->\n+        {:t_scalar, s}\n+\n+      s == \"String\" ->\n+        :t_string\n+\n+      m = Regex.run(~r/^([A-Za-z_]\\w*)\\((.*)\\)$/, s) ->\n+        [_, name, inner] = m\n+\n+        if name == \"Vec\",\n+          do: {:t_vec, to_ty(inner)},\n+          else: {:t_gen, name, Enum.map(TypeStr.split_top_commas(inner), &to_ty/1), s}\n+\n+      true ->\n+        {:t_nom, s}\n+    end\n+  end\n+\n   @caps [:iso, :val, :ref, :tag]\n \n-  # {reference source-type string, the port's `Ty` injection}\n-  @types [\n-    {\"Int53\", :t_int53},\n-    {\"Int32\", :t_int32},\n-    {\"Bool\", :t_bool},\n-    {\"Float64\", :t_float64},\n-    {\"String\", :t_str},\n-    {\"Vec(Int53)\", :t_vec_int53},\n-    {\"Vec(String)\", :t_vec_str},\n-    {\"Foo\", {:t_nom, \"Foo\"}}\n-  ]\n-\n-  describe \"self-hosting capability fixpoint — Rian lowering vs Rian.Capability\" do\n-    test \"rust_param agrees with Rian.Capability.rust_param over the whole matrix\", %{mod: mod} do\n-      for cap <- @caps, {tstr, tport} <- @types do\n-        assert mod.rust_param(cap, tport) == Capability.rust_param(cap, tstr),\n-               \"diverged on #{cap} #{tstr}\"\n+  # the full type vocabulary: every Copy width, String, nominal, nested Vec, and\n+  # parametric generics (incl. nesting and a generic that borrows unlowered).\n+  @types @copy ++\n+           [\n+             \"String\",\n+             \"Foo\",\n+             \"Vec(Int32)\",\n+             \"Vec(String)\",\n+             \"Vec(Foo)\",\n+             \"Vec(Vec(Int8))\",\n+             \"Option(Int64)\",\n+             \"Pair(Int32, String)\",\n+             \"Vec(Option(Int64))\",\n+             \"Map(String, Vec(Int64))\"\n+           ]\n+\n+  describe \"self-hosting capability fixpoint — Rian lowering vs Rian.Capability (full)\" do\n+    test \"rust_param agrees with Rian.Capability.rust_param over the WHOLE matrix\", %{mod: mod} do\n+      for cap <- @caps, t <- @types do\n+        assert mod.rust_param(cap, to_ty(t)) == Capability.rust_param(cap, t),\n+               \"diverged on #{cap} #{t}\"\n       end\n     end\n \n@@ -59,27 +87,31 @@ defmodule Rian.CapFixpointTest do\n     end\n   end\n \n-  describe \"teeth — the lowering is capability- and type-sensitive (not a lookup constant)\" do\n-    test \"val borrows a non-Copy type but passes a Copy scalar by value\", %{mod: mod} do\n-      assert mod.rust_param(:val, :t_str) == \"&str\"\n-      assert mod.rust_param(:val, :t_int53) == \"i64\"\n-      # iso owns the same String that val borrows — capability changes the answer.\n-      refute mod.rust_param(:iso, :t_str) == mod.rust_param(:val, :t_str)\n+  describe \"teeth — the full mapping is capability- and structure-sensitive\" do\n+    test \"val borrows non-Copy but passes Copy by value; every width maps\", %{mod: mod} do\n+      assert mod.rust_param(:val, to_ty(\"String\")) == \"&str\"\n+      assert mod.rust_param(:val, to_ty(\"Int53\")) == \"i64\"\n+      assert mod.rust_param(:val, to_ty(\"UInt128\")) == \"u128\"\n+      refute mod.rust_param(:iso, to_ty(\"String\")) == mod.rust_param(:val, to_ty(\"String\"))\n     end\n \n-    test \"ref emits the BEAM-illegal &mut form\", %{mod: mod} do\n-      assert mod.rust_param(:ref, :t_int53) == \"&mut i64\"\n-      refute mod.rust_param(:ref, :t_int53) == mod.rust_param(:tag, :t_int53)\n+    test \"nested generics lower recursively under iso/owned\", %{mod: mod} do\n+      assert mod.rust_param(:iso, to_ty(\"Vec(Vec(Int8))\")) == \"Vec<Vec<i8>>\"\n+      assert mod.rust_param(:iso, to_ty(\"Option(Int64)\")) == \"Option<i64>\"\n+      assert mod.rust_param(:iso, to_ty(\"Map(String, Vec(Int64))\")) == \"Map<String, Vec<i64>>\"\n     end\n \n-    test \"only ref is BEAM-illegal — the portable core is val/iso/tag (P5)\", %{mod: mod} do\n-      refute mod.beam_legal(:ref)\n-      assert mod.beam_legal(:iso) and mod.beam_legal(:val) and mod.beam_legal(:tag)\n+    test \"the reference quirk: val of a generic borrows the UNLOWERED spelling\", %{mod: mod} do\n+      assert mod.rust_param(:val, to_ty(\"Option(Int64)\")) == \"&Option(Int64)\"\n+      # ...whereas iso lowers it — the two must differ.\n+      refute mod.rust_param(:val, to_ty(\"Option(Int64)\")) ==\n+               mod.rust_param(:iso, to_ty(\"Option(Int64)\"))\n     end\n \n-    test \"the type changes the spelling (not constant per capability)\", %{mod: mod} do\n-      refute mod.rust_param(:iso, :t_str) == mod.rust_param(:iso, :t_vec_int53)\n-      refute mod.rust_param(:tag, :t_int53) == mod.rust_param(:tag, {:t_nom, \"Foo\"})\n+    test \"ref emits &mut and only ref is BEAM-illegal (P5)\", %{mod: mod} do\n+      assert mod.rust_param(:ref, to_ty(\"Int53\")) == \"&mut i64\"\n+      refute mod.beam_legal(:ref)\n+      assert mod.beam_legal(:iso) and mod.beam_legal(:val) and mod.beam_legal(:tag)\n     end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/test%2Frian%2Fcap_fixpoint_test.exs","sha":"85e432d3f2377c459a7fcd77c18140ce31776354","status":"modified"},{"additions":91,"blob_url":"https://github.com/wstein/rian-lab/blob/cdd16e2523a198c1c6985544f697be5beebe1108/test%2Frian%2Fexhaust_fixpoint_test.exs","changes":154,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fexhaust_fixpoint_test.exs?ref=cdd16e2523a198c1c6985544f697be5beebe1108","deletions":63,"filename":"test/rian/exhaust_fixpoint_test.exs","patch":"@@ -4,18 +4,17 @@ defmodule Rian.ExhaustFixpointTest do\n \n   alias Rian.{Beam, Exhaustiveness}\n \n-  # Self-hosting fixpoint (ADR-0063) for the **exhaustiveness gate**: a\n-  # Rian-written exhaustiveness verdict (examples/rian/selfhost_exhaust.rian),\n-  # compiled to real `.beam`, diffed against the reference `Rian.Exhaustiveness`.\n+  # Self-hosting fixpoint (ADR-0063) for the **exhaustiveness gate** (full): a\n+  # Rian-written Maranget usefulness check (examples/rian/selfhost_exhaust.rian),\n+  # compiled to real `.beam`, diffed against the reference\n+  # `Rian.Exhaustiveness.useful?/3` over the whole algorithm — single- and\n+  # multi-column matrices, constructors with arguments (arity-specialised),\n+  # wildcards, and signature completeness — so the port reproduces `useful?`,\n+  # not just a verdict.\n   #\n-  # The diff is against the real Maranget usefulness check: a single-column match\n-  # over signature `sig` is exhaustive iff a wildcard query is NOT useful w.r.t.\n-  # the pattern matrix — `not Exhaustiveness.useful?(rows, [:wild], env)`. The\n-  # Rian port must agree with that on every corpus case.\n-  #\n-  # Slice: single-column matches over a finite sum of nullary constructors. The\n-  # remaining vocabulary (ctors with args, multi-column, list/literal/range\n-  # patterns, redundancy) is the `:partial` tail (ADR-0063).\n+  # The same `useful?` formulation gives exhaustiveness: a `case` is exhaustive\n+  # iff a wildcard query is NOT useful w.r.t. the matrix. The witness/counterexample\n+  # pass (algorithm I) is the `:partial` tail (ADR-0063).\n \n   setup_all do\n     {:ok, mod} =\n@@ -24,78 +23,107 @@ defmodule Rian.ExhaustFixpointTest do\n     {:ok, mod: mod}\n   end\n \n-  # ctor name string -> the reference's ctor id (an atom).\n-  defp cid(name), do: name |> String.downcase() |> String.to_atom()\n+  # type definitions shared by both representations: type -> [{ctor, arity}].\n+  @tdefs [\n+    {\"rgb\", [{\"red\", 0}, {\"green\", 0}, {\"blue\", 0}]},\n+    {\"shape\", [{\"circle\", 1}, {\"square\", 1}, {\"rect\", 2}]},\n+    {\"list\", [{\"nil\", 0}, {\"cons\", 2}]},\n+    {\"box\", [{\"box\", 1}]}\n+  ]\n+\n+  # ── the port's structured env (struct maps) ──\n+  defp port_env do\n+    cdefs =\n+      for {ty, vs} <- @tdefs, {c, a} <- vs, do: %{__struct__: :c_def, name: c, ar: a, ty: ty}\n \n-  # the reference verdict via the real Maranget useful?/3.\n-  defp ref_exhaustive(sig, items) do\n-    env = Exhaustiveness.add_type(Exhaustiveness.base_env(), \"T\", Enum.map(sig, &{cid(&1), 0}))\n+    tdefs =\n+      for {ty, vs} <- @tdefs, do: %{__struct__: :t_def, ty: ty, ctors: Enum.map(vs, &elem(&1, 0))}\n \n-    rows =\n-      Enum.map(items, fn\n-        :wild -> [:wild]\n-        {:ctor, name} -> [{:ctor, cid(name), []}]\n-      end)\n+    %{__struct__: :env, cdefs: cdefs, tdefs: tdefs}\n+  end\n \n-    not Exhaustiveness.useful?(rows, [:wild], env)\n+  # ── the reference env (base_env + add_type) ──\n+  defp ref_env do\n+    Enum.reduce(@tdefs, Exhaustiveness.base_env(), fn {ty, vs}, e ->\n+      Exhaustiveness.add_type(e, ty, Enum.map(vs, fn {c, a} -> {String.to_atom(c), a} end))\n+    end)\n   end\n \n-  # the port verdict: inject items into the Pat sum (PWild / PCtor(name)).\n-  defp port_exhaustive(mod, sig, items) do\n-    pats =\n-      Enum.map(items, fn\n-        :wild -> :p_wild\n-        {:ctor, name} -> {:p_ctor, name}\n-      end)\n+  # a tiny neutral pattern DSL: `:w` | {ctor_name, [sub]}.  Translate to each side.\n+  defp to_port(:w), do: :p_wild\n+  defp to_port({c, args}), do: {:p_ctor, c, Enum.map(args, &to_port/1)}\n \n-    mod.exhaustive(pats, sig)\n+  defp to_ref(:w), do: :wild\n+  defp to_ref({c, args}), do: {:ctor, String.to_atom(c), Enum.map(args, &to_ref/1)}\n+\n+  defp port_useful(mod, rows, q) do\n+    pr = Enum.map(rows, fn row -> Enum.map(row, &to_port/1) end)\n+    mod.useful(port_env(), pr, Enum.map(q, &to_port/1))\n   end\n \n-  @sig [\"Red\", \"Green\", \"Blue\"]\n+  defp ref_useful(rows, q) do\n+    rr = Enum.map(rows, fn row -> Enum.map(row, &to_ref/1) end)\n+    Exhaustiveness.useful?(rr, Enum.map(q, &to_ref/1), ref_env())\n+  end\n \n-  # {signature, column items}\n+  # {matrix, query} cases exercising the whole algorithm.\n   @corpus [\n-    {@sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}, {:ctor, \"Blue\"}]},\n-    {@sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}]},\n-    {@sig, [{:ctor, \"Red\"}]},\n-    {@sig, []},\n-    {@sig, [:wild]},\n-    {@sig, [{:ctor, \"Red\"}, :wild]},\n-    {@sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}, {:ctor, \"Blue\"}, {:ctor, \"Red\"}]},\n-    {[\"On\", \"Off\"], [{:ctor, \"On\"}, {:ctor, \"Off\"}]},\n-    {[\"On\", \"Off\"], [{:ctor, \"On\"}]},\n-    {[\"Only\"], [{:ctor, \"Only\"}]},\n-    {[\"Only\"], []}\n+    # nullary single-column: incomplete / complete / wildcard\n+    {[[{\"red\", []}], [{\"green\", []}]], [:w]},\n+    {[[{\"red\", []}], [{\"green\", []}], [{\"blue\", []}]], [:w]},\n+    {[[{\"red\", []}], [:w]], [:w]},\n+    {[], [:w]},\n+    {[[:w]], []},\n+    # constructors with arguments — specialised by arity\n+    {[[{\"circle\", [:w]}], [{\"square\", [:w]}]], [:w]},\n+    {[[{\"circle\", [:w]}], [{\"square\", [:w]}], [{\"rect\", [:w, :w]}]], [:w]},\n+    {[[{\"rect\", [{\"red\", []}, :w]}]], [:w]},\n+    # lists as nil/cons constructors\n+    {[[{\"nil\", []}], [{\"cons\", [:w, :w]}]], [:w]},\n+    {[[{\"cons\", [:w, :w]}]], [:w]},\n+    {[[{\"cons\", [:w, {\"cons\", [:w, {\"nil\", []}]}]}]], [:w]},\n+    # single-ctor type (box) — one ctor IS the whole signature\n+    {[[{\"box\", [{\"red\", []}]}]], [:w]},\n+    {[[{\"box\", [:w]}]], [:w]},\n+    # multi-column matrices\n+    {[[{\"red\", []}, {\"circle\", [:w]}]], [:w, :w]},\n+    {[[{\"red\", []}, {\"circle\", [:w]}], [{\"green\", []}, {\"square\", [:w]}]], [:w, :w]},\n+    {[[:w, {\"nil\", []}], [:w, {\"cons\", [:w, :w]}]], [:w, :w]}\n   ]\n \n-  describe \"self-hosting exhaustiveness fixpoint — Rian verdict vs Maranget useful?/3\" do\n-    test \"the Rian verdict agrees with Rian.Exhaustiveness on every case\", %{mod: mod} do\n-      for {sig, items} <- @corpus do\n-        assert port_exhaustive(mod, sig, items) == ref_exhaustive(sig, items),\n-               \"diverged on sig=#{inspect(sig)} items=#{inspect(items)}\"\n+  describe \"self-hosting exhaustiveness fixpoint — Rian useful vs Maranget useful?/3\" do\n+    test \"the Rian usefulness check reproduces useful?/3 on every case\", %{mod: mod} do\n+      for {rows, q} <- @corpus do\n+        assert port_useful(mod, rows, q) == ref_useful(rows, q),\n+               \"useful diverged on matrix=#{inspect(rows)} q=#{inspect(q)}\"\n       end\n     end\n   end\n \n-  describe \"teeth — the verdict genuinely detects gaps (not vacuously true)\" do\n-    test \"a missing constructor is reported non-exhaustive\", %{mod: mod} do\n-      refute port_exhaustive(mod, @sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}])\n-      # ...and the reference agrees this is the real gap.\n-      refute ref_exhaustive(@sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}])\n-    end\n-\n-    test \"a wildcard makes any column exhaustive\", %{mod: mod} do\n-      assert port_exhaustive(mod, @sig, [:wild])\n-      assert port_exhaustive(mod, @sig, [{:ctor, \"Red\"}, :wild])\n+  describe \"teeth — the full algorithm (arity, multi-column, signatures) is real\" do\n+    test \"specialisation by arity: a missing multi-arg ctor leaves a gap\", %{mod: mod} do\n+      # circle/square present, rect (arity 2) missing -> wildcard IS useful (gap).\n+      incomplete = [[{\"circle\", [:w]}], [{\"square\", [:w]}]]\n+      assert port_useful(mod, incomplete, [:w])\n+      # add rect -> the whole `shape` signature is covered -> NOT useful (exhaustive).\n+      complete = incomplete ++ [[{\"rect\", [:w, :w]}]]\n+      refute port_useful(mod, complete, [:w])\n     end\n \n-    test \"covering all constructors is exhaustive without a wildcard\", %{mod: mod} do\n-      assert port_exhaustive(mod, @sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}, {:ctor, \"Blue\"}])\n+    test \"exhaustiveness = `not useful` agrees with the reference for lists\", %{mod: mod} do\n+      cons_only = [[{\"cons\", [:w, :w]}]]\n+      full = [[{\"nil\", []}], [{\"cons\", [:w, :w]}]]\n+      # cons-only is non-exhaustive (nil missing); nil+cons is exhaustive.\n+      assert port_useful(mod, cons_only, [:w]) == ref_useful(cons_only, [:w])\n+      assert port_useful(mod, full, [:w]) == ref_useful(full, [:w])\n+      refute port_useful(mod, full, [:w])\n     end\n \n-    test \"the verdict discriminates complete from incomplete columns\", %{mod: mod} do\n-      refute port_exhaustive(mod, @sig, [{:ctor, \"Red\"}]) ==\n-               port_exhaustive(mod, @sig, [{:ctor, \"Red\"}, {:ctor, \"Green\"}, {:ctor, \"Blue\"}])\n+    test \"multi-column: an uncovered column combination is useful\", %{mod: mod} do\n+      # one row red+circle leaves the rest of the rgb × shape space uncovered.\n+      rows = [[{\"red\", []}, {\"circle\", [:w]}]]\n+      assert port_useful(mod, rows, [:w, :w])\n+      assert port_useful(mod, rows, [:w, :w]) == ref_useful(rows, [:w, :w])\n     end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/cdd16e2523a198c1c6985544f697be5beebe1108/test%2Frian%2Fexhaust_fixpoint_test.exs","sha":"fd5cb971bd95c767672bf110b521c991402f022b","status":"modified"}]}