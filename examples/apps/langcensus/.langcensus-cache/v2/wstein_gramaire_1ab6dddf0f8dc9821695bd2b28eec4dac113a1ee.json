{"files":[{"additions":240,"blob_url":"https://github.com/wstein/gramaire/blob/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/docs%2Fplayground-spec.md","changes":308,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fplayground-spec.md?ref=1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee","deletions":68,"filename":"docs/playground-spec.md","patch":"@@ -1,98 +1,270 @@\n-# Product Specification: Grammark Lab (Playground)\n+# Product Specification — Grammark Lab\n \n-## 1. Overview & Vision\n+> A browser-native grammar laboratory. Edit a `.gram.md` grammar, watch it\n+> build, parse input live, and see _why_ — conflicts, parse trees, ambiguity,\n+> generated parsers — with no server and sub-frame feedback.\n \n-Grammark Lab is a browser-native, live grammar laboratory designed to make grammar development as fast, visual, and precise as software development. Traditional parser generators force developers into slow edit-compile-test loops, leaving them blind to why a stream failed or why an ambiguity occurred. Grammark Lab eliminates this black box, providing a real-time, interactive environment.\n+Status: **draft / north-star**. Tier 0 ships the live edit→evaluate loop that\n+exists today; Tiers 1–3 are the roadmap. Every feature here is grounded in a\n+capability the Grammark Core already exposes, so the Lab is a thin, honest skin\n+over real machinery — never a mock.\n \n-By building on top of Grammark's unique Markdown-first Core, Grammark Lab isn't just an evaluator—it is a live documentation tool and diagnostic oracle. It runs 100% client-side, compiling the parser engine and evaluating test inputs with sub-millisecond feedback.\n+---\n+\n+## 1. Vision\n+\n+Parser generators still run an edit → compile → stare-at-a-stack-trace loop.\n+When a grammar is ambiguous you get `conflict in state 47`; when input is\n+rejected you get `unexpected token`. The _grammar author's_ questions — \"which\n+two of **my** rules collide?\", \"is this a real ambiguity or just an LALR\n+artifact?\", \"what tree did this input actually produce?\" — go unanswered.\n+\n+Grammark Lab answers them, in the browser, as you type. It is best-in-class on\n+one axis none of the incumbents own: **the grammar is Markdown**, so the Lab is\n+simultaneously a live editor, a rendering documentation preview, and a\n+diagnostic oracle. The same PureScript Core that powers the CLI is compiled to\n+JavaScript and runs client-side, so there is no round-trip, no upload, and\n+nothing to install.\n+\n+**Design tenets.**\n+\n+1. _No black box._ Every error is phrased in the author's own rules, never in\n+   LR-item jargon (the Core's `Grammark.Diagnostics` already does this).\n+2. _No server._ 100% client-side; a grammar never leaves the tab.\n+3. _One source of truth._ The Lab uses the real Core, not a re-implementation —\n+   what the Lab accepts, `grammark` accepts.\n+4. _Documentation is the artifact._ The thing you share renders as docs on\n+   GitHub and is the exact compiler input.\n \n ---\n \n-## 2. Competitive Analysis and Feature Gap\n+## 2. Competitive analysis\n+\n+Seven established grammar workbenches, scored on the dimensions that matter for\n+fast, visual grammar development. `●` full, `◐` partial, `○` absent.\n \n-To build the best-in-class playground, we analyzed seven existing grammar workbenches. The table below scores their capabilities and reveals critical feature gaps:\n+| Dimension                           | ANTLR Lab | Chevrotain | Nearley | LALRPOP (IDE) | RR/UI (bottlecaps) | Peggy | Flatbars Lab | **Grammark Lab** |\n+| ----------------------------------- | :-------: | :--------: | :-----: | :-----------: | :----------------: | :---: | :----------: | :--------------: |\n+| Runs in-browser (no server)         |     ○     |     ●      |    ●    |       ○       |         ●          |   ●   |      ●       |        ●         |\n+| Declarative grammar (not host code) |     ●     |     ○      |    ◐    |       ●       |         ●          |   ◐   |      ◐       |        ●         |\n+| Live evaluate input → result        |     ●     |     ●      |    ●    |       ○       |         ○          |   ●   |      ●       |        ●         |\n+| Interactive parse tree / CST        |     ●     |     ●      |    ◐    |       ○       |         ○          |   ◐   |      ○       |        ●         |\n+| Token stream linked to source       |     ◐     |     ●      |    ○    |       ○       |         ○          |   ○   |      ◐       |        ●         |\n+| Railroad diagrams                   |     ◐     |     ●      |    ○    |       ○       |         ●          |   ○   |      ○       |        ●         |\n+| FIRST/FOLLOW + state introspection  |     ○     |     ○      |    ○    |       ○       |         ○          |   ○   |      ○       |        ●         |\n+| Grammar-relative conflict messages  |     ○     |     ○      |    ○    |       ●       |         ○          |   ○   |      ◐       |        ●         |\n+| Ambiguity / all-parses view         |     ○     |     ○      |    ●    |       ○       |         ○          |   ○   |      ○       |        ●         |\n+| Error-recovery preview              |     ◐     |     ○      |    ○    |       ○       |         ○          |   ◐   |      ○       |        ◐         |\n+| Multi-target codegen export         |     ●     |     ○      |    ○    |       ●       |         ○          |   ○   |      ○       |        ●         |\n+| Doc-as-grammar (renders as docs)    |     ○     |     ○      |    ○    |       ○       |         ○          |   ○   |      ○       |        ●         |\n+| Dense share link / permalink        |     ○     |     ○      |    ○    |       ○       |         ◐          |   ◐   |      ●       |        ●         |\n \n-| Feature Dimension | ANTLR Lab | Chevrotain | Nearley | LALRPOP | Railroad UI | Peggy JS | Flatbars Lab | **Grammark Lab** |\n-| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |\n-| **Purity of Target** | Server JVM | JS Only | JS Only | Rust IDE | EBNF Only | JS Only | Template VM | **Language Neutral** |\n-| **Compilation Latency** | High (Secs) | None (JS) | Low (MS) | N/A | None | None | None | **None (In-Browser)** |\n-| **Rich Parse-Tree / CST** | yes | Yes | yes | no | no | no | no | **Yes (Interactive)** |\n-| **Visual Token Maps** | yes | yes | no | no | no | no | yes | **Yes (Hover-Linked)** |\n-| **Grammar-Relative Diagnostics** | no | no | no | yes | no | no | yes | **Yes (Production-Linked)** |\n-| **Interactive State Walk** | no | no | no | no | no | no | no | **Yes (LALR State Trace)** |\n-| **Two-way Railroad Sync** | no | yes | no | no | yes | no | no | **Yes (Embedded/Sidecar)** |\n-| **Robust Share / Gist** | no | no | no | no | no | no | yes | **Yes (LZMA / Host)** |\n+### Per-tool teardown\n \n-### Key Competitor Weaknesses\n+- **ANTLR Lab** (`lab.antlr.org`). The most complete _viewer_: live parse-tree\n+  and token views, profiler. But it compiles **out-of-process on a JVM server**,\n+  so there is latency, an upload, and a privacy cost; visualizations are static\n+  images, and conflict reporting is ANTLR's adaptive-LL machinery, not\n+  grammar-author-relative.\n+- **Chevrotain Playground** (`chevrotain.io/playground`). Genuinely in-browser\n+  and ships syntax diagrams + a CST view. The catch: a grammar **is JavaScript**\n+  — you write parser class methods, not declarative rules — so it reads as\n+  boilerplate and is unusable as portable documentation.\n+- **Nearley Playground** (`omrelli.ug/nearley-playground`). Earley means it\n+  handles ambiguity and shows multiple parses (rare and valuable). Diagnostics\n+  are thin: a rejected input yields a raw dump, not \"expected one of …\".\n+- **LALRPOP plugin** (JetBrains). Excellent Rust-grade _diagnostics_, but locked\n+  in an IDE with **zero live evaluation** — you need a full `cargo` build to try\n+  an input.\n+- **Railroad Diagram Generator / UI** (`bottlecaps.de/rr/ui`). Best-in-class\n+  diagrams from W3C EBNF, downloadable. Pure renderer: it **cannot execute** a\n+  grammar against input.\n+- **Peggy** (`peggyjs.org/online`). Clean and instant, with location-tagged PEG\n+  errors. But PEG hides ambiguity by fiat (ordered choice) and offers **no**\n+  state-machine, FIRST/FOLLOW, or conflict insight.\n+- **Flatbars Lab** (`wstein.github.io/flatbars`). The UX bar: Monaco split\n+  editor, schema diagnostics, dense share links. It is a _template_ engine,\n+  though, not a context-free parser workbench — the closest reference for look\n+  and feel, not for capability.\n \n-*   **ANTLR Lab (http://lab.antlr.org/):** Heavy JVM-based backend. Compiles out-of-process on a unix box, inducing latency and privacy concerns. Visualizations are static, and error locations don't map naturally to live-cursor positions.\n-*   **Chevrotain Playground (https://chevrotain.io/playground/):** Bound exclusively to JavaScript. Writing grammars looks like writing boilerplate JS class methods rather than clear EBNF-like grammar rules.\n-*   **Nearley Playground (https://omrelli.ug/nearley-playground/):** Parses arbitrary Earley streams but diagnostics are sparse. When parsing fails, users get dumped with raw stack traces that do not explain the expected token or matching state.\n-*   **LALRPOP IntelliJ (https://plugins.jetbrains.com/plugin/15229-lalrpop):** Excellent Rust diagnostics but locked completely inside an IDE. Zero live evaluation capabilities for test payloads without full cargo compilation.\n-*   **Railroad UI (https://www.bottlecaps.de/rr/ui):** Elegant formatting and parsing of W3C EBNF, but only generates views—it cannot execute or test a grammar against string payloads.\n-*   **Peggy JS (https://peggyjs.org/online.html):** Extremely simple, but offers zero visualization of State machines or internal FIRST/FOLLOW tables.\n-*   **Flatbars Lab (https://wstein.github.io/flatbars/):** Extremely clean Monaco-based split editor with share links and schema diagnostics, but is for template rendering (JSONATA/YAML/HBS), not general context-free parsing.\n+### The gap\n+\n+No incumbent combines (a) in-browser execution, (b) a declarative,\n+portable-as-documentation grammar, and (c) deep, author-relative introspection —\n+conflicts named in your rules, the LALR-artifact-vs-genuine verdict, all parses\n+of an ambiguous grammar, and one-click codegen. That intersection is Grammark\n+Lab's lane, and every piece of it already exists in the Core.\n \n ---\n \n-## 3. Core Architectural Strategy\n+## 3. What only Grammark Lab can do\n+\n+These map one-to-one onto Core modules already in the repository, which is what\n+keeps the Lab honest:\n+\n+- **Conflicts in your rules.** `Grammark.Diagnostics.renderConflict` turns\n+  `shift/reduce in state 7` into \"shift `+` vs reduce `Expr -> Expr + Expr`\",\n+  ready to underline the competing productions in the `lr` block.\n+- **\"Artifact or genuine?\"** `Grammark.Glr.explain` builds the grammar under all\n+  three methods and reports whether a conflict is an **LALR artifact** (canonical\n+  / IELR resolve it — \"switch to IELR\") or **genuine** (the grammar is not LR(1))\n+  — a verdict no other playground gives.\n+- **All the parses.** `Grammark.Glr.forest` returns _every_ derivation of an\n+  ambiguous grammar, so the Lab can show the two trees of `1+1+1` side by side.\n+- **Source ⇄ tree ⇄ input hover-linking.** The spanned tokenizer\n+  (`Grammark.Lexer.tokenizeSpanned`) and the generic CST\n+  (`spec/cst-schema.json`) carry exact source ranges, so hovering a CST node can\n+  highlight both the production and the matched input slice.\n+- **Live, real artifacts.** The same backends the CLI ships —\n+  `ir`, `ebnf`, `dot`, and a runnable **TypeScript** parser + typed Visitor —\n+  emit straight from the IR, so \"Download parser\" is a real generated file, not a\n+  toy.\n+- **Three methods, compared.** Canonical LR(1), LALR(1), and IELR(1) build from\n+  the same automaton; the Lab can show table sizes and conflict counts per\n+  method on one screen.\n+\n+---\n+\n+## 4. Feature roadmap (prioritized)\n+\n+Tiers are shipping order. Each feature names the Core capability it rides so it\n+can be built without new engine work unless noted.\n \n-We reject out-of-process compilation—it is slow, unsecure, and costly. Grammark Lab runs entirely in the browser using the compiled Core.\n+### Tier 0 — the loop (MVP; largely shipping today)\n+\n+- **T0.1 Dual editor.** Left: the `.gram.md` grammar. Right: a raw input\n+  payload. (Today: textareas; Tier 1 upgrades to Monaco.)\n+- **T0.2 Evaluate.** Run the input against the grammar via `Grammark.Lr.parse` +\n+  the table-driven parser; surface **accept / reject** and the reason.\n+- **T0.3 Inline build diagnostics.** Parse / structure / desugar errors shown\n+  with their message (already wired through the real Core path).\n+- **T0.4 Permalink.** Compress `{grammar, input, layout}` into the URL so a state\n+  is shareable (Flatbars-grade; LZMA + URL-safe base64). Open-from-URL on load.\n+\n+### Tier 1 — the workbench\n+\n+- **T1.1 Monaco dual-pane** with `.gram.md` highlighting (Markdown + an `lr`\n+  fenced-block grammar mode), a diagnostics gutter in both panes, and debounced\n+  re-evaluation on every keystroke (target < 16 ms for small grammars).\n+- **T1.2 Interactive CST explorer.** Render the `grammark-cst` tree\n+  (collapsible). Branch nodes show their rule; token leaves show terminal + text.\n+- **T1.3 Hover-linking (the signature feature).** Hover a CST branch → underline\n+  its production in the grammar pane; hover a token leaf → highlight its source\n+  span in the input pane. Powered by spanned tokens + CST `rule` ids.\n+- **T1.4 \"Why did it fail?\"** On reject, read the LR state at the failure head\n+  and list the **expected terminals**; offer one-click insertion of a valid next\n+  token into the input.\n+- **T1.5 Conflict underlining.** On a non-LR(1) build, underline the competing\n+  productions in the `lr` block (from `renderConflict`) instead of printing a\n+  state number.\n+\n+### Tier 2 — the oracle\n+\n+- **T2.1 Method switch + comparison.** Toggle Canonical / LALR / IELR; show\n+  per-method state count and conflict count; flag **LALR artifacts** with a\n+  \"build with IELR\" affordance (`Grammark.Glr.explain`).\n+- **T2.2 Ambiguity view.** When a grammar is ambiguous, render the parse\n+  **forest** — each distinct CST of the current input — from\n+  `Grammark.Glr.forest`.\n+- **T2.3 Live railroad + FIRST/FOLLOW drawer.** Re-render the railroad SVG under\n+  each rule as you type, and show the generated FIRST/FOLLOW table (the same\n+  artifact `grammark fmt` writes).\n+- **T2.4 Desugar lens.** Toggle \"show lowered Core\" to see how `X+ / X* / X? /\n+  Comma<X> / #[inline]` expand to epsilon-free productions (`Grammark.Desugar`) —\n+  a teaching tool and a debugging aid.\n+- **T2.5 Codegen export.** Download buttons for `ir.json`, `.ebnf`, `.dot`, and a\n+  runnable `parser.ts` + `parser.d.ts` (the real `ts` backend), plus \"copy as\".\n+\n+### Tier 3 — the studio\n+\n+- **T3.1 Conformance panel.** A table of accept/reject vectors run as a\n+  differential oracle across all three methods (the `Grammark.Conformance`\n+  harness), green/red per vector — TDD for grammars.\n+- **T3.2 LR state walk.** Step the automaton token-by-token over the input: show\n+  the stack, the current item set, and the action taken — an interactive\n+  teaching view of the parse.\n+- **T3.3 Recovery preview.** With panic-mode recovery, show how an erroneous\n+  input resynchronizes (depends on `tables.recovery`; partial in the Core today).\n+- **T3.4 Gallery + examples.** One-click load of `calc`, `json`, and the\n+  self-describing `lr` grammar; a \"fork this\" flow.\n+- **T3.5 Embeddable lab.** An `<iframe>` / web-component build so a grammar can be\n+  embedded, live, in any docs page (including this site's tutorials).\n+\n+---\n+\n+## 5. Architecture\n+\n+100% client-side. The Core is compiled to ES modules and run in a **Web Worker**\n+so table construction never blocks the UI.\n \n ```mermaid\n-flowchart TD\n-    subgraph UI Thread (Main)\n-        Editor[Monaco MD Editor] <--> VFS[Memory VFS]\n-        InputEditor[Monaco Test Input] <--> RunCtrl[Controller]\n-        RunCtrl <--> ASTView[CST Tree Explorer & Diagnostics]\n-        RunCtrl <--> DiagramView[Interactive Railroad SVG]\n-    end\n-    subgraph Web Worker Thread\n-        WorkerListener[Message Listener] <--> Kernel[PureScript Core Parser]\n-        Kernel --> LexER[Lexer / Spanned Tokenizer]\n-        Kernel --> Generator[LR Table Builder]\n-        Kernel --> Interpreter[Table Parser Engine]\n-    end\n-    VFS <-->|PostMessage| WorkerListener\n-    RunCtrl <-->|PostMessage| WorkerListener\n+flowchart LR\n+  subgraph Main thread\n+    GE[\"Grammar editor (Monaco, .gram.md)\"]\n+    IE[\"Input editor (Monaco)\"]\n+    CST[\"CST explorer + hover-link\"]\n+    DIAG[\"Diagnostics gutter\"]\n+    RR[\"Railroad / FIRST-FOLLOW drawer\"]\n+    URL[\"Permalink (LZMA)\"]\n+  end\n+  subgraph Worker thread\n+    P[\"Grammark.Lr.parse -> Desugar -> Table\"]\n+    G[\"GLR explain / forest\"]\n+    B[\"Backends: ir, ebnf, dot, ts\"]\n+  end\n+  GE -- COMPILE --> P\n+  IE -- EVALUATE --> P\n+  P -- \"CST, diagnostics, FIRST/FOLLOW\" --> CST\n+  P --> DIAG\n+  P --> RR\n+  G --> DIAG\n+  B -- artifacts --> URL\n ```\n \n-### In-Browser Compilation & Virtual File System (VFS)\n-The playground maps all virtual workspace files inside a virtual memory filesystem (`Record String String`) inside the PureScript Core. 100% of the PureScript compiler is compiled to ES6 JavaScript and runs in a separate **Web Worker** thread to prevent locking the UI during parser building.\n+- **Worker protocol.** `{type:\"COMPILE\", source}` and `{type:\"EVALUATE\", input}`\n+  in; `{cst, diagnostics, firstFollow, method, conflicts}` out. Debounced;\n+  latest-wins (drop stale responses).\n+- **Spans everywhere.** The worker returns CST nodes carrying source ranges so\n+  the main thread can hover-link without re-lexing.\n+- **No filesystem dependency.** The Core's FS-freedom guard means the parse path\n+  imports no `node:fs`; the same code runs unchanged in the worker.\n \n ---\n \n-## 4. Feature Specification\n+## 6. UX & layout\n+\n+Three zones, Flatbars-clean, one emerald accent (per `docs/BRANDING.md` — emerald\n+always reads \"valid / green\"):\n \n-### 4.1. Dual Monaco Editor Workspace\n-*   **Left Pane (The Source):** Real-time editor with syntax highlighting for Markdown containing fenced `lr` grammar blocks. Implements GFM inline checks.\n-*   **Right Pane (The Input):** Editor for raw input payloads that the user wants to parsing against the custom grammar.\n-*   **Diagnostic Gutter:** Highlights both grammar errors (using Grammark's conflict diagnostics) and parser input errors (such as unexpected tokens at line/col) in the matching editors.\n+- **Top bar.** Grammar name, method switch (Tier 2), build-status pill (emerald\n+  when green), Share, Download ▾.\n+- **Split body.** Grammar (left, ~60%) · Input (right, ~40%), resizable.\n+- **Bottom drawer (tabbed).** _Result_ (accept/reject + CST) · _Diagnostics_ ·\n+  _Railroad_ · _FIRST/FOLLOW_ · _Lowered Core_ · _Conformance_.\n \n-### 4.2. Visual Token-to-Production Mapping & Hover Inspect\n-*   In the CST Tree view, hovering over any internal rule node highlights the corresponding production in the source grammar editor.\n-*   Hovering over any terminal leaf in the AST view highlights the character range or substring in the input payload editor. This is enabled by the spanned tokenizer introduced in [src/Grammark/Lexer.purs](src/Grammark/Lexer.purs).\n-*   The initial public lab implementation now supports a live editor-and-evaluator loop with inline diagnostics and an accept/reject result surface for quick iteration.\n+Accessibility: full keyboard nav, ARIA on the tree, prefers-reduced-motion\n+respected, monospace for all grammar / CLI text (branding).\n \n-### 4.3. Interactive State Walk and \"Why Did This Fail?\" Diagnostic Gutter\n-*   Upon compilation failure (e.g. Shift/Reduce conflict), the playground does not print \"State 45 has conflict\". Instead, it walks the core's conflict pipeline ([src/Grammark/Diagnostics.purs](src/Grammark/Diagnostics.purs)) and highlights the exact competing tokens directly inside the Markdown `lr` block.\n-*   Upon parsing failure, the engine displays a list of \"Expected Terminals\" based on the LR automaton's state at the failure head, allowing one-click insertion of valid next tokens into the input stream.\n+---\n \n-### 4.4. Live-Rendering Railroad Diagrams & FIRST/FOLLOW Tab\n-*   As the user types, the UI parses alternatives and renders live SVGs below each H2 block.\n-*   An operations drawer displays the auto-generated **FIRST/FOLLOW Table** in real-time, matching GFM format.\n+## 7. Non-goals\n \n-### 4.5. Universal Export & Sharing\n-*   **EBNF / IR Download:** Direct export of the compiled [spec/ir-schema.json](spec/ir-schema.json) structure, or W3C-style clean EBNF through the modular [src/Grammark/Backend/Ebnf.purs](src/Grammark/Backend/Ebnf.purs) backend.\n-*   **Url Serialization:** Compresses the full workspace (grammar, input string, and current layout settings) into a URL query parameter using **LZMA compression** to preserve ultra-dense sharing links under the 2KB browser URL limits.\n+- **No server compilation, ever** (the anti-ANTLR-Lab stance).\n+- **No host-code grammars** (the anti-Chevrotain stance): grammars stay\n+  declarative `.gram.md`.\n+- **No account / cloud storage.** Sharing is by URL; persistence is the user's\n+  repo.\n+- **Not a general IDE.** LSP / incremental editing is the IDE-extension track\n+  (`spec/incremental-spec.md`), not the Lab.\n \n ---\n \n-## 5. Technical Implementation & Live Compilation Flow\n+## 8. Success metrics\n \n-1.  **Change Detection:** The main thread detects user changes in the Markdown editor.\n-2.  **Web Worker Message:** Main thread posts a `{ type: \"COMPILE\", source: markdownContent }` message to the Web Worker.\n-3.  **Core Parsing:** Inside the worker, `Grammark.Lr.parse` reads the grammar, lowers it through `Grammark.Desugar` to compile surface sugar (`+`, `*`, `?`, parameters) to the core epsilon-free notation.\n-4.  **Table Generation:** The worker builds LR tables via canonical LR(1) or IELR(1) split calculations.\n-5.  **Output Serializer:** If successful, the worker runs the tokenizer on the user's Input. If parsing succeeds, it converts the CST to a JSON payload conforming to the CST schema.\n-6.  **UI Update:** The worker posts back the calculated CST, diagnostics, and EBNF representation. The main thread renders the interactive CST tree and draws railroad SVG blocks.\n+- **Time-to-first-parse** under 2 s from cold load on a mid laptop.\n+- **Keystroke-to-feedback** under one frame (16 ms) for grammars ≤ 50 rules.\n+- **Parity:** every Lab verdict matches the `grammark` CLI on the same input\n+  (enforced with a shared conformance fixture).\n+- **Shareability:** a non-trivial grammar + input fits in a URL under 8 KB.\n+- **\"Aha\" rate:** a first-time user can see _why_ an ambiguous grammar is\n+  ambiguous (two trees) without reading the docs.","raw_url":"https://github.com/wstein/gramaire/raw/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/docs%2Fplayground-spec.md","sha":"b30f94781966c0928893f6f836d81b831af57e00","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/gramaire/blob/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fastro.config.mjs","changes":3,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fastro.config.mjs?ref=1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee","deletions":0,"filename":"site/astro.config.mjs","patch":"@@ -8,6 +8,9 @@ export default defineConfig({\n     starlight({\n       title: \"Grammark\",\n       description: \"Grammars that render themselves.\",\n+      // `lr` (Grammark's production blocks) has no Shiki grammar; render it as\n+      // plain monospace text — matching how GitHub shows the same fences.\n+      expressiveCode: { shiki: { langAlias: { lr: \"text\" } } },\n       sidebar: [\n         { label: \"Home\", link: \"/\" },\n         {","raw_url":"https://github.com/wstein/gramaire/raw/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fastro.config.mjs","sha":"14011102cb74f4f0d804b47af44b0670c6f81c1e","status":"modified"},{"additions":6,"blob_url":"https://github.com/wstein/gramaire/blob/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent.config.ts","changes":15,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent.config.ts?ref=1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee","deletions":9,"filename":"site/src/content.config.ts","patch":"@@ -1,10 +1,7 @@\n-import { defineCollection, z } from \"astro:content\";\n+import { defineCollection } from \"astro:content\";\n+import { docsLoader } from \"@astrojs/starlight/loaders\";\n+import { docsSchema } from \"@astrojs/starlight/schema\";\n \n-const docs = defineCollection({\n-  schema: z.object({\n-    title: z.string(),\n-    description: z.string().optional(),\n-  }),\n-});\n-\n-export const collections = { docs };\n+export const collections = {\n+  docs: defineCollection({ loader: docsLoader(), schema: docsSchema() }),\n+};","raw_url":"https://github.com/wstein/gramaire/raw/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent.config.ts","sha":"7fbcf2c33262d746e2fdfb8970347b6a8226a7ed","status":"modified"},{"additions":199,"blob_url":"https://github.com/wstein/gramaire/blob/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":207,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee","deletions":8,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -1,13 +1,204 @@\n ---\n-title: Grammar Format\n-description: Core grammar file format reference for Grammark.\n+title: Language Specification\n+description: The formal Grammark grammar format — document envelope, lexical rules, newline normalization, desugaring, and the IR/CST contracts.\n ---\n \n-The Grammark grammar format is defined in the [fmt output contract](../../../../docs/fmt-output-contract.md).\n+import { Aside } from \"@astrojs/starlight/components\";\n \n-The canonical document structure is:\n+This is the formal definition of the Grammark grammar language (v0.1): the\n+document envelope, the `lr` lexical grammar, the newline rule, the desugaring\n+that compiles surface sugar to the epsilon-free core, and the contracts the\n+compiler emits. Keywords MUST / SHOULD / MAY per RFC 2119.\n \n-1. one H1 naming the grammar\n-2. one H2 per nonterminal\n-3. optional precedence and errors sections\n-4. generated tables and diagnostics\n+<Aside>\n+  The canonical on-disk layout that `grammark fmt` produces is specified\n+  separately in the [fmt output\n+  contract](../../../../../docs/fmt-output-contract.md). This page specifies the\n+  _language_; that one specifies its _formatting_.\n+</Aside>\n+\n+## 1. Document envelope\n+\n+A Grammark grammar is hosted in a GitHub-Flavored Markdown document. The compiler\n+ignores prose, list markers, and images; it extracts the grammar by scanning ATX\n+headings and fenced code blocks whose info string begins with `lr`.\n+\n+- **Grammar name.** The single `# H1`. There MUST be exactly one.\n+- **Nonterminal rules.** Every non-reserved `## H2` names a nonterminal; the\n+  `lr` block beneath it holds that rule's productions.\n+- **Reserved `## H2` sections.** `Precedence` (an `lr precedence` block),\n+  `Error messages` (an `lr errors` block), and `Generated tables` (the emitted\n+  FIRST/FOLLOW table). They appear in that order when present.\n+- **Presentational headings.** `###` and deeper headings carry no structural\n+  meaning; they are free grouping the analyzer skips. Only the H1 and H2 layers\n+  are structural.\n+- **One grammar per file.** A file defines exactly one start symbol and one\n+  nonterminal namespace. Reuse across files is a future cross-file `import`,\n+  never in-file sub-grammars or multiple H1s.\n+\n+## 2. Lexical grammar of an `lr` block\n+\n+Inside an `lr` productions block the lexer emits these token classes:\n+\n+| Token                     | Form                     | Meaning                                              |\n+| ------------------------- | ------------------------ | ---------------------------------------------------- |\n+| `IDENT`                   | `[A-Za-z_][A-Za-z0-9_]*` | a nonterminal (mixed-case) or token class (ALL-CAPS) |\n+| `TERM_LIT`                | `` `…` ``                | a backtick-delimited literal terminal                |\n+| `:` / `\\|`                | `:` / `\\|`               | rule-head colon / alternative separator              |\n+| `ACTION`                  | `{% … %}`                | a semantic action; body kept verbatim                |\n+| `LABEL`                   | `# Name`                 | an alternative label (visitor / accessor name)       |\n+| `ATTR`                    | `#[name]`                | a rule attribute (e.g. `#[inline]`)                  |\n+| `PLUS` `STAR` `QUESTION`  | `+` `*` `?`              | repetition / optional postfixes                      |\n+| `LANGLE` `RANGLE` `COMMA` | `<` `>` `,`              | macro-call punctuation                               |\n+| `NL`                      | one or more line breaks  | structural newline (see §3)                          |\n+\n+A name is a **nonterminal** exactly when it appears as some rule's left-hand\n+side; any other mixed-case name referenced but never defined is a build error\n+(not a silent terminal). An ALL-CAPS name is always a token class. Input\n+tokenization itself is per-language: the `lr` lexer reads the grammar; a separate\n+lexer turns _input_ strings into tokens whose `terminal` names match the\n+grammar's terminals.\n+\n+## 3. Newline normalization (Option A)\n+\n+A line break **inside an alternative is insignificant** — `|` is the only\n+alternative separator, so a long alternative may wrap across physical lines. Only\n+two newlines are structural, and a normalization pass keeps exactly those before\n+the parser runs:\n+\n+- the **head** `NL` inside `IDENT NL :` (or `ATTR IDENT NL :`), which is what\n+  distinguishes a rule head from a same-line `name:Sym` field (`IDENT : Sym`);\n+  and\n+- the **boundary** `NL` immediately before the next head.\n+\n+Every other newline — between two symbols, before a `|`, before a label or\n+action, at a block edge — is dropped. The lookahead that classifies a newline\n+happens once, in this pass, so the grammar stays LR(1) on one token of lookahead\n+with no `;` terminators.\n+\n+<Aside type=\"caution\">\n+  A consequence: a `name:Sym` field MUST stay on one line. Splitting it across a\n+  break reads the name as a rule head.\n+</Aside>\n+\n+## 4. Desugaring to the epsilon-free core\n+\n+Before building tables, the surface grammar is lowered to a pure, epsilon-free\n+context-free grammar. The core automaton never sees a nullable rule, which keeps\n+table construction free of nullable bookkeeping.\n+\n+### 4.1 Repetition — `X+`, `X*`\n+\n+`X+` (one-or-more) lowers to a fresh left-recursive list nonterminal whose action\n+accumulates an `Array`:\n+\n+```text\n+X_plus\n+  : X\n+  | X_plus X\n+```\n+\n+`X*` (zero-or-more) reuses that list but enumerates **present / absent** at the\n+use site, so the empty case is a real production rather than an epsilon — the\n+core stays epsilon-free.\n+\n+### 4.2 Optional — `X?`\n+\n+`X?` desugars by **use-site enumeration with generated wrapper actions**. A rule\n+`A : pre X? post {% f %}` becomes two epsilon-free productions:\n+\n+```text\n+A : pre X post   -- wrapper applies f with (Just x)\n+A : pre post     -- wrapper applies f with Nothing\n+```\n+\n+The wrapper splices the original action verbatim, so it still receives one\n+`Maybe` and arity is preserved. With _k_ optionals an alternative enumerates to\n+2^k productions; an all-optional alternative (which would enumerate to an empty\n+production) is a build error, never a silent epsilon.\n+\n+### 4.3 Macros — `Comma<X>`, `Sep<X, S>`\n+\n+`Comma<X>` and `Sep<X, S>` lower to a fresh one-or-more separated-list rule\n+(separated by `,` or by the given symbol `S`). An unknown macro or wrong arity is\n+a build error.\n+\n+### 4.4 Named fields — `name:X`\n+\n+A `name:X` prefix names that right-hand position. The name is carried onto the\n+IR's `rhs[].field` and feeds CST accessors and generated visitors; it does not\n+change the recognized language. A bare-body action then binds the field names: a\n+production `left:NUM `+` right:NUM {% Add left right %}` rewrites to\n+`\\left _ right -> Add left right`.\n+\n+### 4.5 Inline folding — `#[inline]`\n+\n+`#[inline]` marks a single-production rule to be folded into every use site and\n+then dropped — both a DRY tool and a way to hand-fit a grammar into LR(1). When\n+the using alternative has an action, the inline rule's own action is threaded\n+through a wrapper so values still flow.\n+\n+### 4.6 Labels — `# Name`\n+\n+A `# Name` label names an alternative. It surfaces in the IR as an optional\n+`rules[].label` and drives per-alternative visitor method names and CST accessors.\n+\n+## 5. The IR contract — `grammark-ir`\n+\n+The compiler emits the analyzed grammar and its parse tables as a versioned JSON\n+document, the [IR schema](../ir-schema/). Every backend reads only this artifact —\n+isolating target generators from Markdown processing and layout entirely.\n+\n+```json\n+{\n+  \"irVersion\": 0,\n+  \"grammar\": {\n+    \"name\": \"Calc\",\n+    \"start\": \"Expr\",\n+    \"rules\": [\n+      {\n+        \"id\": 0,\n+        \"lhs\": 0,\n+        \"rhs\": [\n+          { \"id\": 0, \"kind\": \"nonterminal\", \"field\": \"left\" },\n+          { \"id\": 2, \"kind\": \"terminal\" },\n+          { \"id\": 1, \"kind\": \"nonterminal\", \"field\": \"right\" }\n+        ],\n+        \"label\": \"Add\",\n+        \"actions\": [[\"purescript\", \"\\\\l _ r -> Add l r\"]]\n+      }\n+    ]\n+  },\n+  \"tables\": {\n+    \"algorithm\": \"canonical\",\n+    \"stateCount\": 42,\n+    \"action\": [],\n+    \"goto\": []\n+  },\n+  \"conflicts\": []\n+}\n+```\n+\n+## 6. The CST contract — `grammark-cst`\n+\n+An action-free grammar still fully defines a **concrete syntax tree**: a `branch`\n+per reduced production (tagged with its `rules[]` index) and a `token` leaf per\n+consumed terminal, children in source order. This generic tree is the portable\n+artifact any backend can emit and any host can walk; its shape is fixed by the\n+[CST schema](../cst-schema/). The spanned tokenizer carries exact source ranges,\n+so a tool can link a CST node back to both its production and its input slice —\n+the substrate the [Lab](/grammark/lab) uses for hover-linking.\n+\n+## 7. Table construction and conflicts\n+\n+Tables build by canonical **LR(1)**, **LALR(1)** (canonical states merged by\n+LR(0) core), or **IELR(1)** (LALR with the conflicting merges split apart).\n+Canonical is the most powerful and serves as the differential oracle for the\n+other two.\n+\n+A conflict is reported in grammar terms — naming the competing productions, not a\n+state number — and classified by comparing the three methods: a conflict LALR(1)\n+reports but canonical / IELR resolve is an **LALR artifact** (build with IELR);\n+one that survives canonical LR(1) is **genuine** (the grammar is ambiguous or not\n+LR(1)). For genuinely ambiguous grammars the GLR engine returns the full parse\n+forest — every derivation of the input.","raw_url":"https://github.com/wstein/gramaire/raw/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"dfa9a30851aac11cfa402465b363a5ff32968028","status":"modified"},{"additions":242,"blob_url":"https://github.com/wstein/gramaire/blob/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","changes":250,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx?ref=1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee","deletions":8,"filename":"site/src/content/docs/tutorials/intro.mdx","patch":"@@ -1,18 +1,252 @@\n ---\n title: Getting Started\n-description: Beginner tutorial for Grammark grammar authoring.\n+description: Write your first self-rendering grammar — from Markdown structure to a live parse — in about ten minutes.\n ---\n \n-This tutorial walks through the basic grammar authoring flow in Grammark, from Markdown structure to generated diagrams and tables. The live site now uses the repository's real PureScript parser for the examples and the lab, so the browser experience reflects the same parser path as the CLI.\n+import {\n+  Aside,\n+  Steps,\n+  Card,\n+  CardGrid,\n+  LinkCard,\n+} from \"@astrojs/starlight/components\";\n \n-## Create a grammar file\n+Grammark's whole idea is in its name — _gram + mark_, grammar plus Markdown. A\n+`.gram.md` file is a normal Markdown document that renders on GitHub — prose,\n+railroad diagrams, FIRST/FOLLOW tables — and is **at the same time** the exact\n+input the parser generator reads. Your grammar _is_ your documentation.\n \n-Use a Markdown file with a single H1 and one H2 per nonterminal. A good starting point is the calculator example in [examples/calc.gram.md](../../../examples/calc.gram.md).\n+This tutorial builds a small calculator grammar from scratch, then parses input\n+against it. Nothing here is a mock: the [Lab](/grammark/lab) runs the same\n+PureScript core as the `grammark` CLI.\n \n-## Add an lr block\n+<Aside type=\"tip\" title=\"Follow along live\">\n+  Open the [**Lab**](/grammark/lab) in a second tab and paste each snippet as\n+  you go — you'll see the parse, the diagnostics, and the result update as you\n+  type.\n+</Aside>\n \n-Place productions inside a fenced block beginning with `lr`. The parser understands the full Grammark envelope, including precedence blocks, error-message blocks, and action annotations.\n+## 1. The shape of a `.gram.md` file\n \n-## Verify and format\n+A grammar file is a canonical Markdown document with a fixed structure:\n \n-Run the available Grammark tools to validate the file and emit the generated artifacts. In the browser, the lab sends the current grammar to the real parser endpoint and reports the parser output and any diagnostics.\n+<Steps>\n+\n+1. **One `# H1`** — the grammar's name.\n+\n+2. **One `## H2` per nonterminal** — the heading names the rule; an `lr` fenced\n+   block under it holds its productions.\n+\n+3. **Reserved `## H2` sections** — `Precedence`, `Error messages`, and\n+   `Generated tables`, in that order, when present.\n+\n+4. **Everything else is prose** — paragraphs, images, and `###`+ headings are\n+   free presentational grouping the compiler ignores.\n+\n+</Steps>\n+\n+One file is exactly one grammar: one start symbol, one namespace. Productions\n+live in fenced blocks whose info string is `lr`; everything outside them is\n+documentation that travels with the grammar.\n+\n+## 2. Your first rule\n+\n+Create `calc.gram.md` and give it a title and a first nonterminal:\n+\n+````md\n+# Calc\n+\n+A small arithmetic grammar.\n+\n+## Expr\n+\n+An expression is a sum or difference of terms.\n+\n+```lr\n+Expr\n+  : Expr `+` Term\n+  | Expr `-` Term\n+  | Term\n+```\n+````\n+\n+Reading the `lr` block:\n+\n+- `Expr` on its own line names the nonterminal; the `:` on the next line begins\n+  its alternatives.\n+- `|` separates alternatives — and it is the **only** separator. A line break\n+  inside an alternative is insignificant, so a long alternative may wrap freely.\n+- Backtick spellings like `` `+` `` are **literal terminals**.\n+- A **mixed-case** name (`Expr`, `Term`) is a nonterminal; an **ALL-CAPS** name\n+  (`NUMBER`) is a lexer token class. A mixed-case name you never define is a\n+  missing rule — Grammark rejects it by name rather than treating it as a phantom\n+  terminal.\n+\n+<Aside type=\"caution\" title=\"Heads vs. fields\">\n+  A rule head is `Name` on its own line, then `:` on the next — the line break\n+  is load-bearing. It is the one thing that distinguishes a head from a\n+  `name:Sym` **field** (written `IDENT : Sym` with no break, see §4). So never\n+  write a head inline as `Name : …`.\n+</Aside>\n+\n+Add the remaining rules — `Term`, then `Factor` for numbers and parentheses:\n+\n+```lr\n+Term\n+  : Term `*` Factor\n+  | Term `/` Factor\n+  | Factor\n+\n+Factor\n+  : `(` Expr `)`\n+  | NUMBER\n+```\n+\n+## 3. Surface sugar\n+\n+Writing pure, left-recursive, epsilon-free productions by hand is wordy.\n+Grammark gives you sugar that compiles down to that clean core automatically\n+(`Grammark.Desugar`), so the table generator never sees a nullable rule.\n+\n+<CardGrid>\n+  <Card title=\"Repetition\">\n+    `X+` one-or-more, `X*` zero-or-more, `X?` optional. `X+` becomes a fresh\n+    left-recursive list; `X*` and `X?` enumerate present/absent at the use site,\n+    so the core stays epsilon-free.\n+  </Card>\n+  <Card title=\"Separated lists\">\n+    `Comma<X>` and `Sep<X, S>` expand to one-or-more lists separated by `,` or by\n+    any symbol `S` — e.g. `` `{` Comma<Member> `}` ``.\n+  </Card>\n+  <Card title=\"Named fields\">\n+    `left:Expr` names a right-hand position. The name rides onto the IR\n+    (`rhs[].field`) and drives generated CST accessors and visitors — without\n+    changing the recognized language.\n+  </Card>\n+  <Card title=\"Inline folding\">\n+    `#[inline]` on a rule folds its single production into every use site, then\n+    drops it — a DRY tool, and a way to hand-fit a grammar into LR(1).\n+  </Card>\n+</CardGrid>\n+\n+For example, a statement block is just:\n+\n+```lr\n+Block\n+  : `{` Sep<Stmt, `;`> `}`\n+```\n+\n+## 4. Attaching actions\n+\n+A recognizer only says yes or no. To build a value or an AST, put a host-language\n+action between `{%` and `%}`. The default action language is **PureScript** (the\n+core's own language):\n+\n+```lr\n+Expr\n+  : Expr `+` Term   {% \\l _ r -> Add l r %}\n+  | Expr `-` Term   {% \\l _ r -> Sub l r %}\n+  | Term            {% \\t -> t %}\n+```\n+\n+Actions are **positional**: one parameter per right-hand symbol, left to right.\n+In `Expr `+` Term` the three parameters are the left value, the `+` token (which\n+we discard with `_`), and the right value.\n+\n+If you name your fields, you can skip the lambda entirely — a **bare body** binds\n+the field names for you:\n+\n+```lr\n+Expr\n+  : left:Expr `+` right:Term   {% Add left right %}\n+```\n+\n+Grammark rewrites that to `\\left _ right -> Add left right`, so the action reads\n+in terms of your grammar, not positions.\n+\n+<Aside title=\"Actions are data, backends are consumers\">\n+  Actions are carried verbatim into the [IR](../../specs/ir-schema/); each\n+  backend reads only the IR. The TypeScript backend, for instance, emits a\n+  runnable parser and a typed Visitor over the CST without ever seeing your\n+  Markdown.\n+</Aside>\n+\n+## 5. Precedence and conflicts\n+\n+Recursive operators make a grammar ambiguous, which surfaces as shift/reduce\n+conflicts. Declare operator precedence in a reserved section — earlier\n+declarations bind more loosely:\n+\n+````md\n+## Precedence\n+\n+```lr precedence\n+%left `+` `-`\n+%left `*` `/`\n+```\n+````\n+\n+When a build _does_ conflict, Grammark never tells you \"conflict in state 7.\" It\n+names the competing rules — \"shift `+` vs reduce `Expr -> Expr + Expr`\" — and it\n+can tell you _what kind_ of conflict it is:\n+\n+- an **LALR artifact** — canonical LR(1) or IELR(1) resolves it, so just build\n+  with a stronger method; or\n+- a **genuine** conflict — the grammar really is ambiguous or not LR(1), and\n+  needs a refactor, a precedence rule, or the GLR engine.\n+\n+```sh\n+grammark explain-conflict calc.gram.md\n+```\n+\n+## 6. Run it\n+\n+Two feedback loops, same core:\n+\n+<Steps>\n+\n+1. **In the browser.** Paste the grammar into the [Lab](/grammark/lab), type\n+   `1 + 2 * 3` as input, and Evaluate — you get accept/reject and the parse,\n+   live.\n+\n+2. **At the CLI.** Check structure, formatting, and conflicts; then emit:\n+\n+   ```sh\n+   # verify the file (structure, formatting, no conflicts)\n+   grammark fmt calc.gram.md\n+\n+   # emit artifacts from the same grammar\n+   grammark emit --backend ebnf calc.gram.md   # W3C-style EBNF\n+   grammark emit --backend ts   calc.gram.md   # a runnable TS parser + Visitor\n+   grammark emit --backend ir   calc.gram.md   # the grammark-ir JSON\n+   ```\n+\n+</Steps>\n+\n+`grammark fmt` also writes the railroad diagrams and the FIRST/FOLLOW table back\n+into the document, so the file you commit renders as complete documentation.\n+\n+## Where next\n+\n+<CardGrid>\n+  <LinkCard\n+    title=\"Language specification\"\n+    href=\"../../specs/grammar-format/\"\n+    description=\"The formal envelope, lexical rules, desugaring, and the IR contract.\"\n+  />\n+  <LinkCard\n+    title=\"The IR schema\"\n+    href=\"../../specs/ir-schema/\"\n+    description=\"grammark-ir — the one artifact every backend reads.\"\n+  />\n+  <LinkCard\n+    title=\"Open the Lab\"\n+    href=\"/grammark/lab\"\n+    description=\"Edit, parse, and inspect grammars live in the browser.\"\n+  />\n+  <LinkCard\n+    title=\"Schema overview\"\n+    href=\"../../specs/schema-overview/\"\n+    description=\"The published schema and contract artifacts.\"\n+  />\n+</CardGrid>","raw_url":"https://github.com/wstein/gramaire/raw/1ab6dddf0f8dc9821695bd2b28eec4dac113a1ee/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","sha":"c306e62991cd4369fd70b9894c846ac63abc61e5","status":"modified"}]}