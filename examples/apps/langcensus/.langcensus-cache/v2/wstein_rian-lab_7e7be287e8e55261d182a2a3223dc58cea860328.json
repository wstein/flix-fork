{"files":[{"additions":39,"blob_url":"https://github.com/wstein/rian-lab/blob/7e7be287e8e55261d182a2a3223dc58cea860328/examples%2Frian%2Fselfhost_checker.rian","changes":62,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_checker.rian?ref=7e7be287e8e55261d182a2a3223dc58cea860328","deletions":23,"filename":"examples/rian/selfhost_checker.rian","patch":"@@ -17,11 +17,12 @@\n # or a general (non-prim) call is `unknown` — the checker is deliberately\n # conservative and does NOT guess.\n #\n-# Covered Core nodes (7): CNum/CStr/CChar/CId/CUnary/CBin/CCall (closed expressions,\n-# no typing environment). The `:partial` tail — a typing env (bound variables,\n-# user-function/ctor return types), operand-directed arithmetic widths, lambdas,\n-# `if`/`case` LUB-join, lists, and the abstract operators — see ADR-0063 /\n-# docs/self-host-status.md.\n+# A typing ENVIRONMENT (name -> type) is threaded through inference: a bound\n+# variable resolves to its declared type (`infer_env/2`; `infer/1` is the empty-env\n+# convenience). Covered Core nodes (7): CNum/CStr/CChar/CId/CUnary/CBin/CCall. The\n+# `:partial` tail — user-function/ctor return types in the env, operand-directed\n+# arithmetic widths (Rian.Check's `arith_type`: `x + 1.0`, char ordinals), lambdas,\n+# `if`/`case` LUB-join, and lists — see ADR-0063 / docs/self-host-status.md.\n # ---------------------------------------------------------------------------\n \n mod SelfhostChecker do\n@@ -75,24 +76,39 @@ mod SelfhostChecker do\n     end\n   end\n \n-  # Infer the type of a Core node, returning the type name (or \"unknown\").\n+  # Infer the type of a Core node in the empty env (closed expression).\n   pub def infer(Core) String\n-  pub def infer(CNum(t)) := num_ty(t)\n-  pub def infer(CStr(_)) := \"String\"\n-  pub def infer(CChar(_)) := \"Char\"\n-  pub def infer(CId(\"true\")) := \"Bool\"\n-  pub def infer(CId(\"false\")) := \"Bool\"\n-  pub def infer(CId(_)) := \"unknown\"\n-  pub def infer(CUnary(\"not\", _)) := \"Bool\"\n-  pub def infer(CUnary(\"-\", x)) := infer(x)\n-  pub def infer(CBin(op, _, _)) := bin_ty(op)\n+  pub def infer(c) := infer_env(c, Prim.map_new())\n+\n+  # Infer under a typing env (name -> type), threading it through sub-expressions —\n+  # a bound identifier resolves to its env type (ADR-0042; mirrors Rian.Check's\n+  # `Map.get(env, x)`), else `true`/`false` are `Bool`, else `unknown`.\n+  pub def infer_env(Core, Dict(String, String)) String\n+  pub def infer_env(CNum(t), _) := num_ty(t)\n+  pub def infer_env(CStr(_), _) := \"String\"\n+  pub def infer_env(CChar(_), _) := \"Char\"\n+  pub def infer_env(CId(\"true\"), _) := \"Bool\"\n+  pub def infer_env(CId(\"false\"), _) := \"Bool\"\n+  pub def infer_env(CId(x), env) := lookup(env, x)\n+  pub def infer_env(CUnary(\"not\", _), _) := \"Bool\"\n+  pub def infer_env(CUnary(\"-\", x), env) := infer_env(x, env)\n+  pub def infer_env(CBin(op, _, _), _) := bin_ty(op)\n   # primitive intrinsics with a determinate result type (ADR-0036/0047/0069 — mirrors\n-  # Rian.Check's `__prim_*` clauses). A general call has no determinate type without a\n-  # typing env (function/ctor return types), so it is conservatively `unknown`.\n-  pub def infer(CCall(\"__prim_char_code\", [_])) := \"Int53\"\n-  pub def infer(CCall(\"__prim_int_to_float\", [_])) := \"Float64\"\n-  pub def infer(CCall(\"__prim_str_to_atom\", [_])) := \"Symbol\"\n-  pub def infer(CCall(\"__prim_char_to_string\", [_])) := \"String\"\n-  pub def infer(CCall(\"__prim_str_concat_all\", _)) := \"String\"\n-  pub def infer(CCall(_, _)) := \"unknown\"\n+  # Rian.Check's `__prim_*` clauses). A general call has no determinate type without\n+  # function/ctor return types in the env, so it is conservatively `unknown`.\n+  pub def infer_env(CCall(\"__prim_char_code\", [_]), _) := \"Int53\"\n+  pub def infer_env(CCall(\"__prim_int_to_float\", [_]), _) := \"Float64\"\n+  pub def infer_env(CCall(\"__prim_str_to_atom\", [_]), _) := \"Symbol\"\n+  pub def infer_env(CCall(\"__prim_char_to_string\", [_]), _) := \"String\"\n+  pub def infer_env(CCall(\"__prim_str_concat_all\", _), _) := \"String\"\n+  pub def infer_env(CCall(_, _), _) := \"unknown\"\n+\n+  # a bound name's type from the env, else `unknown` (the checker does not guess).\n+  def lookup(env Dict(String, String), x String) String\n+    if Prim.map_has(env, x) do\n+      Prim.map_get(env, x)\n+    else\n+      \"unknown\"\n+    end\n+  end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/7e7be287e8e55261d182a2a3223dc58cea860328/examples%2Frian%2Fselfhost_checker.rian","sha":"43ff7af165450391f893da57339d638f6186d95c","status":"modified"},{"additions":49,"blob_url":"https://github.com/wstein/rian-lab/blob/7e7be287e8e55261d182a2a3223dc58cea860328/test%2Frian%2Fchecker_infer_fixpoint_test.exs","changes":49,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fchecker_infer_fixpoint_test.exs?ref=7e7be287e8e55261d182a2a3223dc58cea860328","deletions":0,"filename":"test/rian/checker_infer_fixpoint_test.exs","patch":"@@ -41,6 +41,15 @@ defmodule Rian.CheckerInferFixpointTest do\n     end\n   end\n \n+  defp ported_env(mod, src, env), do: mod.infer_env(inj(Core.from_expr(Pratt.parse(src))), env)\n+\n+  defp reference_env(src, env) do\n+    case Check.infer(Pratt.parse(src), env) do\n+      :unknown -> \"unknown\"\n+      ty -> ty\n+    end\n+  end\n+\n   @corpus [\n     \"1\",\n     \"3.14\",\n@@ -84,6 +93,46 @@ defmodule Rian.CheckerInferFixpointTest do\n     end\n   end\n \n+  # a typing env binding variables to types — the headline gap the port now closes.\n+  # (operand-directed arithmetic widths are a later rung, so bound vars appear in\n+  # env-lookup / comparison / unary / same-type-Int arithmetic positions.)\n+  @env %{\"x\" => \"Int53\", \"y\" => \"Int53\", \"s\" => \"String\", \"b\" => \"Bool\", \"f\" => \"Float64\"}\n+  @env_corpus [\n+    \"x\",\n+    \"y\",\n+    \"s\",\n+    \"b\",\n+    \"f\",\n+    \"z\",\n+    \"-x\",\n+    \"-f\",\n+    \"not b\",\n+    \"x < y\",\n+    \"x == y\",\n+    \"s <> s\",\n+    \"x + y\",\n+    \"x - y * x\",\n+    \"x + 1\"\n+  ]\n+\n+  describe \"self-hosting checker fixpoint — inference under a typing env\" do\n+    test \"the Rian inference agrees with Rian.Check.infer over bound variables\", %{mod: mod} do\n+      for src <- @env_corpus do\n+        assert ported_env(mod, src, @env) == reference_env(src, @env),\n+               \"env inference diverged on #{inspect(src)}\"\n+      end\n+    end\n+\n+    test \"a bound variable resolves to its env type; an unbound one stays unknown\",\n+         %{mod: mod} do\n+      assert ported_env(mod, \"x\", @env) == \"Int53\"\n+      assert ported_env(mod, \"s\", @env) == \"String\"\n+      assert ported_env(mod, \"z\", @env) == \"unknown\"\n+      assert reference_env(\"x\", @env) == \"Int53\"\n+      assert reference_env(\"z\", @env) == \"unknown\"\n+    end\n+  end\n+\n   describe \"teeth — the inference is real and conservative\" do\n     test \"an unbound identifier is `unknown` — the checker does not guess\", %{mod: mod} do\n       assert ported(mod, \"x\") == \"unknown\"","raw_url":"https://github.com/wstein/rian-lab/raw/7e7be287e8e55261d182a2a3223dc58cea860328/test%2Frian%2Fchecker_infer_fixpoint_test.exs","sha":"1e06715bbb7ef3dec7b22dc17c69c5a32ff47ef5","status":"modified"}]}