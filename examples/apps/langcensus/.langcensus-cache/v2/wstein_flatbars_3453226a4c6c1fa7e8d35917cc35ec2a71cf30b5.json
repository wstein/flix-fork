{"files":[{"additions":48,"blob_url":"https://github.com/wstein/flatbars/blob/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Findex.html","changes":87,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5","deletions":39,"filename":"lab/index.html","patch":"@@ -364,6 +364,10 @@\n       .tabs-wrap > .focus-toggle { flex: 0 0 auto; align-self: center; margin-right: 6px; }\n       .tabs-wrap > #out-copy { margin-left: auto; margin-right: 0; }\n       #out-copy.copied { color: #10b981; }\n+      .tabs-wrap > .mj-truthy { flex: 0 0 auto; align-self: center; display: inline-flex; align-items: center;\n+        gap: 5px; margin-right: 8px; font: 11px var(--font-mono); color: var(--fg-secondary); cursor: pointer;\n+        user-select: none; white-space: nowrap; }\n+      .tabs-wrap > .mj-truthy input { margin: 0; cursor: pointer; }\n \n       /* ── Tabs ────────────────────────────────────────────────────── */\n       .tabs-wrap {\n@@ -1596,8 +1600,14 @@\n                 <path d=\"M2 6V2h4M14 6V2h-4M2 10v4h4M14 10v4h-4\"/>\n               </svg>\n             </button>\n-            <!-- MinBars truthiness is set in the config.yaml tab now\n-                 (minbars.truthiness; default mustacheJs), not a UI toggle (ADR-029). -->\n+            <!-- MinBars truthiness toggle (ADR-022 S2): flip the language-agnostic\n+                 spec rule (0/\"\" truthy) to mustache.js' (0/\"\" falsy). Shown only\n+                 under the MinBars engine; unhidden at boot. -->\n+            <label id=\"mj-truthy\" class=\"mj-truthy\" hidden\n+                   title=\"MinBars truthiness — default (spec/Ruby): 0 and empty string are truthy; mustache.js: 0 and empty string are falsy\">\n+              <input type=\"checkbox\" id=\"mj-truthy-cb\" />\n+              <span>mustache.js truthiness</span>\n+            </label>\n           </div>\n \n           <!-- Contextual controls for the active view; toggled by setOutputView. -->\n@@ -1774,7 +1784,6 @@\n       // js-yaml + jsonata stay vendored: the Node validator (validate.mjs) imports\n       // them and runs offline with no npm. Browser-only libs load from the CDN.\n       import { load as loadYaml, dump as dumpYaml } from \"./vendor/js-yaml.mjs\";\n-      import { parseConfig, minbarsCompatFromConfig } from \"./config.mjs\";\n       import jsonata from \"./vendor/jsonata.mjs\";\n       // Preact + htm (from the CDN, no build step) for the incremental UI\n       // migration — panels move from manual DOM to declarative `html` components\n@@ -1856,8 +1865,23 @@\n         : \"Stem\";\n       byId(\"brand-ver\").textContent = ENGINE_LABEL + \" v\" + renderer.version;\n \n-      // MinBars truthiness is read from the config.yaml tab (minbars.truthiness),\n-      // default mustacheJs/true (ADR-029) — no UI toggle to wire or sync.\n+      // MinBars truthiness toggle (ADR-022 S2): reveal it under MinBars and wire it\n+      // to re-render under the chosen rule, persisting the choice in the share link.\n+      if (ENGINE === \"minbars\") {\n+        byId(\"mj-truthy\").hidden = false;\n+        byId(\"mj-truthy-cb\").addEventListener(\"change\", (e) => {\n+          minbarsCompat = e.target.checked;\n+          markCustom();\n+          run();\n+          saveHash();\n+        });\n+      }\n+      // Reflect `minbarsCompat` on the toggle checkbox (restore / example reset).\n+      // The input is always in the DOM (hidden off MinBars), so this is safe there.\n+      function syncMinbarsToggle() {\n+        const cb = byId(\"mj-truthy-cb\");\n+        if (cb) cb.checked = minbarsCompat;\n+      }\n \n       // ── Docs backlink + colour-legend popover ──────────────────────────────\n       // The \"Docs\" button points at the reference page for the current engine\n@@ -1981,11 +2005,10 @@\n       let outputView   = \"source\";  // \"source\" (Plain Text) is the default landing view\n       let allowScripts = true;       // HTML/Markdown preview always runs <script> in an isolated (null-origin) sandbox\n       let escapeMode   = \"\";\n-      // MinBars truthiness (ADR-029): read from the config.yaml tab's\n-      // `minbars.truthiness` (default mustacheJs ⇒ true = 0/\"\" falsy; \"spec\" ⇒\n-      // false = 0/\"\" truthy). No UI toggle, no persisted `mj` — config.yaml is the\n-      // source and round-trips as a tab. Threaded into render/compileToJs as `compat`.\n-      function minbarsCompat() { return minbarsCompatFromConfig(parseConfig(configSrc(), loadYaml).config); }\n+      // MinBars truthiness toggle (ADR-022 S2): false = the language-agnostic spec\n+      // rule (0/\"\" truthy), true = the mustache.js rule (0/\"\" falsy). MinBars only;\n+      // threaded into render/compileToJs as `compat` and persisted as `mj`.\n+      let minbarsCompat = false;\n       // Compiler & runtime options (ADR-0016).\n       // `standalone`    — strip standalone-tag lines at compile time (ADR-0016, default on).\n       let standalone      = true;\n@@ -2014,17 +2037,11 @@\n       // entry kicks off the async render (which re-runs once it lands).\n       let helpersSrc = \"\";\n       let helpersDirty = false;\n-      const helperSandbox = createHelperSandbox(\"./helpers-worker.mjs?v=3\");\n-      // Reserved tab names (ADR-029), neither a partial: catalog.yaml is the i18n\n-      // message DATA (built into the t/number/… bag, not on MinBars); config.yaml\n-      // is the per-surface SETTINGS (i18n.locale, minbars.truthiness). partialsMap()\n-      // excludes both; catalogSrc()/configSrc() read them.\n-      const CATALOG_TAB = \"catalog.yaml\";\n-      const CONFIG_TAB = \"config.yaml\";\n+      const helperSandbox = createHelperSandbox(\"./helpers-worker.mjs?v=4\");\n       let helperCache = { key: null, output: \"\", error: \"\" };\n       let helperInFlight = null;\n-      function helperKey(tmpl, partials, data, src, catalog, config, dialect) {\n-        try { return JSON.stringify([tmpl, partials, data, src, catalog, config, dialect]); } catch { return null; }\n+      function helperKey(tmpl, partials, data, src) {\n+        try { return JSON.stringify([tmpl, partials, data, src]); } catch { return null; }\n       }\n       async function requestHelperRender(key, req) {\n         if (helperInFlight === key) return; // this exact input is already rendering\n@@ -2066,18 +2083,9 @@\n \n       function partialsMap() {\n         const map = {};\n-        for (let i = 1; i < tabs.length; i++) if (tabs[i].name !== CATALOG_TAB && tabs[i].name !== CONFIG_TAB) map[tabs[i].name] = tabs[i].source;\n+        for (let i = 1; i < tabs.length; i++) map[tabs[i].name] = tabs[i].source;\n         return map;\n       }\n-      // A reserved tab's source by name (ADR-029), or \"\" when there is no such tab.\n-      function reservedTabSrc(name) {\n-        for (let i = 1; i < tabs.length; i++) if (tabs[i].name === name) return tabs[i].source || \"\";\n-        return \"\";\n-      }\n-      // The i18n catalog (message data); empty on MinBars (no t there).\n-      function catalogSrc() { return ENGINE === \"minbars\" ? \"\" : reservedTabSrc(CATALOG_TAB); }\n-      // The per-surface settings tab (locale, MinBars truthiness).\n-      function configSrc() { return reservedTabSrc(CONFIG_TAB); }\n \n       // ── CodeMirror ────────────────────────────────────────────────────\n       // Template highlighting derives from the engine lexer (ADR-014): one\n@@ -3429,7 +3437,7 @@\n           lastSegViews = [];\n           let text = \"// this engine has no compiler\";\n           if (typeof renderer.compileToJs === \"function\") {\n-            const r = renderer.compileToJs(templateSourceFor(\"main\"), undefined, { compat: minbarsCompat() });\n+            const r = renderer.compileToJs(templateSourceFor(\"main\"), undefined, { compat: minbarsCompat });\n             text = r.ok ? r.value : \"// compile error: \" + r.error;\n           }\n           setOutputDoc(text, Decoration.none);\n@@ -3822,17 +3830,12 @@\n           // matches the current inputs; otherwise show \"rendering…\" and let the\n           // async render re-run. Provenance segments aren't produced for helper\n           // renders (as the in-process renderWith path also returns none).\n-          // helpers.js is FullBars-only; the catalog (t/number/…) works on the\n-          // t-surfaces (RawBars/FullBars/MaxBars — catalogSrc() is \"\" on MinBars).\n-          // Both render in the Worker via the surface's operations-aware entry.\n           const helperSrc = (ENGINE === \"fullbars\") ? (helpersSrc || \"\").trim() : \"\";\n-          const catSrc = catalogSrc().trim();\n-          const cfgSrc = configSrc().trim();\n           let output, segments;\n-          if (helperSrc || catSrc) {\n-            const key = helperKey(mainSource, partialsMap(), data, helperSrc, catSrc, cfgSrc, ENGINE);\n+          if (helperSrc) {\n+            const key = helperKey(mainSource, partialsMap(), data, helperSrc);\n             if (helperCache.key !== key) {\n-              requestHelperRender(key, { dialect: ENGINE, template: mainSource, data, partials: partialsMap(), helperSrc, catalogSrc: catSrc, configSrc: cfgSrc });\n+              requestHelperRender(key, { template: mainSource, data, partials: partialsMap(), helperSrc });\n               setOutput(\"rendering…\"); setError(\"\"); renderDock(); saveHash(); lastTimings = null;\n               return;\n             }\n@@ -3842,7 +3845,7 @@\n             }\n             output = helperCache.output; segments = [];\n           } else {\n-            ({ output, segments } = renderer.render(program, data, { map: true, compat: minbarsCompat() }));\n+            ({ output, segments } = renderer.render(program, data, { map: true, compat: minbarsCompat }));\n           }\n           setOutput(output, segments);\n           setError(\"\");\n@@ -4518,6 +4521,7 @@\n         byId(\"escape-select\").value = \"\";\n         standalone = true;\n         st4Mode = false;\n+        minbarsCompat = false; syncMinbarsToggle();\n         allowList = null;\n         syncStandaloneUI();\n         updateRenderStatus();\n@@ -4583,6 +4587,7 @@\n         byId(\"escape-select\").value = \"\";\n         standalone = true;\n         st4Mode = false;\n+        minbarsCompat = false; syncMinbarsToggle();\n         allowList = null;\n         syncStandaloneUI();\n         updateRenderStatus();\n@@ -4637,6 +4642,8 @@\n           ...(st4Mode ? { s4: 1 } : {}),            // ST4 truthiness gate\n           ...(allowList !== null ? { al: allowList } : {}), // ADR-0013 transformer allow-list\n           av: activeView, ai: activeTabIdx,\n+          // MinBars truthiness toggle (ADR-022 S2); omitted when off so links stay compact.\n+          ...(minbarsCompat ? { mj: 1 } : {}),\n           // Active template-pane mode (1 = transform tab, 0/omitted = tmpl).\n           // Persisted so a shared \"I'm editing the transform\" link round-trips.\n           ...(templateMode === \"transform\" ? { tm: 1 } : {}),\n@@ -4701,6 +4708,8 @@\n           byId(\"escape-select\").value = escapeMode;\n           standalone   = s.sa !== 0;  // default true; explicit 0 opts out\n           st4Mode = s.s4 === 1;\n+          minbarsCompat = s.mj === 1; // MinBars truthiness toggle (no-op off MinBars)\n+          syncMinbarsToggle();\n           allowList = Array.isArray(s.al) ? s.al.map(String) : null;\n           syncStandaloneUI();\n           updateRenderStatus();","raw_url":"https://github.com/wstein/flatbars/raw/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Findex.html","sha":"b18496506eea4b903b0451f6377c414bb29340af","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Fopen-in-lab.mjs","changes":9,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fopen-in-lab.mjs?ref=3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5","deletions":8,"filename":"lab/open-in-lab.mjs","patch":"@@ -34,17 +34,10 @@ export function dataText(data) {\n // (restoreHash restores it verbatim rather than reloading a catalog example);\n // tab 0 is the main template, the rest are named partials. Mirrors the field\n // shape `restoreHash` reads (`tabs:[{n,s}]`, `d`, `av`, `ai`).\n-export function workspaceState({ template = \"\", data, partials, helpers, transform, catalog, config, view, dock } = {}) {\n+export function workspaceState({ template = \"\", data, partials, helpers, transform, view, dock } = {}) {\n   const tabs = [{ s: template }];\n   const p = partials || {};\n   for (const name of Object.keys(p)) tabs.push({ n: name, s: p[name] });\n-  // The i18n catalog (message DATA) and the per-surface settings travel as the\n-  // reserved tabs `catalog.yaml` and `config.yaml` (ADR-029). The Lab excludes\n-  // both from the partial set, feeding catalog.yaml to the i18n helper bag and\n-  // config.yaml to the locale / MinBars-truthiness — so a \"Localize\" example\n-  // lands with its strings and settings already loaded.\n-  if (catalog && catalog.trim()) tabs.push({ n: \"catalog.yaml\", s: catalog });\n-  if (config && config.trim()) tabs.push({ n: \"config.yaml\", s: config });\n   const st = { x: -1, tabs, d: dataText(data), av: \"tmpl\", ai: 0 };\n   // Custom-helper JS (ADR-018) — a separate field `h` from the data transform\n   // (`t`). The Lab loads it into its FullBars-only helpers.js editor and runs it","raw_url":"https://github.com/wstein/flatbars/raw/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Fopen-in-lab.mjs","sha":"de810f35a450989d14ccb46dd33b0cc6e8bdf70c","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/flatbars/blob/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Fopen-in-lab.test.mjs","changes":12,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fopen-in-lab.test.mjs?ref=3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5","deletions":12,"filename":"lab/open-in-lab.test.mjs","patch":"@@ -72,18 +72,6 @@ test(\"workspaceState carries custom-helper source in its own field `h` (ADR-018)\n   assert.equal(workspaceState({ template: \"x\", helpers: \"  \\n\" }).h, undefined);\n });\n \n-test(\"workspaceState carries catalog.yaml (data) + config.yaml (settings) as reserved tabs (ADR-029)\", () => {\n-  const catalog = \"pl:\\n  greeting: \\\"Cześć, {name}!\\\"\";\n-  const config = \"i18n:\\n  locale: pl\";\n-  const s = workspaceState({ template: '{{t \"greeting\" name=name}}', catalog, config });\n-  const cat = s.tabs.find((t) => t.n === \"catalog.yaml\");\n-  const cfg = s.tabs.find((t) => t.n === \"config.yaml\");\n-  assert.ok(cat && cfg, \"catalog.yaml and config.yaml tabs are present\");\n-  assert.equal(cat.s, catalog);\n-  assert.equal(cfg.s, config);\n-  // whitespace-only values are treated as none\n-  assert.equal(workspaceState({ template: \"x\", catalog: \"  \\n\", config: \" \" }).tabs.length, 1);\n-});\n \n test(\"workspaceState carries an output view (`v`) and dock panel (`dk`)\", async () => {\n   // A \"Migrate\" example lands in the Migrated MaxBars output view; a \"Lint\"","raw_url":"https://github.com/wstein/flatbars/raw/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/lab%2Fopen-in-lab.test.mjs","sha":"9a83c7c685636d0424aff0bf9f9f53ec7b1995e9","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/flatbars/blob/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx","changes":8,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx?ref=3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5","deletions":3,"filename":"tutorials/src/components/OpenInLab.jsx","patch":"@@ -157,14 +157,16 @@ export default function OpenInLab({ engine, template, data = {}, partials = {},\n     setJs(r && r.ok ? { ok: true, text: r.value } : { ok: false, text: (r && r.error) || \"compile failed\" });\n   }, [canCompile, renderer, tpl]);\n \n-  // Rebuild the Open-in-Lab deep link from the (possibly edited) workspace.\n+  // Rebuild the Open-in-Lab deep link from the (possibly edited) workspace. The\n+  // i18n catalog/config aren't carried yet — the Lab's catalog.yaml/config.yaml\n+  // sidebar sections are still to be built; the live preview above uses them.\n   useEffect(() => {\n     let live = true;\n-    labHref(engine, { template: tpl, data: dataStr, partials: parts, helpers: helpersStr, transform: transformStr, catalog, config }, { labUrl })\n+    labHref(engine, { template: tpl, data: dataStr, partials: parts, helpers: helpersStr, transform: transformStr }, { labUrl })\n       .then((h) => { if (live) setHref(h); })\n       .catch(() => {});\n     return () => { live = false; };\n-  }, [tpl, dataStr, transformStr, parts, helpersStr, catalog, config]);\n+  }, [tpl, dataStr, transformStr, parts, helpersStr]);\n \n   // A full-width template row only pays off when the template is actually wide\n   // (multi-line or long); a short one-liner like `{{> card}}` would just leave a","raw_url":"https://github.com/wstein/flatbars/raw/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx","sha":"06247619fe014724aa54ac097030401487eef369","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/flatbars/blob/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/tutorials%2Fsrc%2Fpages%2Flocalize.astro","changes":9,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fpages%2Flocalize.astro?ref=3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5","deletions":5,"filename":"tutorials/src/pages/localize.astro","patch":"@@ -140,11 +140,10 @@ const intro = sections[0].cells[0];\n     </p>\n     <TryI18n client:visible template={flagship.template} data={flagship.data} locale={flagship.locale} />\n     <p>\n-      <b>Open in Lab</b> carries the whole setup across — the template, the data, the\n-      strings as a dedicated <code>catalog.yaml</code> tab (data, not code), and the\n-      locale in <code>config.yaml</code>. The Lab reserves both tab names, parses\n-      them, and builds the <code>t</code>/<code>number</code>/… bag, rendering the\n-      same output.\n+      The preview above renders through the real engine with the catalogue + locale\n+      applied. (Carrying the strings into the Lab as dedicated <code>catalog.yaml</code>\n+      / <code>config.yaml</code> sidebar sections is in progress; <b>Open in Lab</b>\n+      currently takes the template and data.)\n     </p>\n     <OpenInLab\n       client:visible","raw_url":"https://github.com/wstein/flatbars/raw/3453226a4c6c1fa7e8d35917cc35ec2a71cf30b5/tutorials%2Fsrc%2Fpages%2Flocalize.astro","sha":"53d8e8e4b09add3f8b4b91c52fb686445324ff95","status":"modified"}]}