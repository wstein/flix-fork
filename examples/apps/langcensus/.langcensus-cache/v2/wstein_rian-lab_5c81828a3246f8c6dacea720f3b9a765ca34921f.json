{"files":[{"additions":13,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/SELFHOST.md","changes":23,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":10,"filename":"SELFHOST.md","patch":"@@ -336,16 +336,19 @@ over time. The proof is a four-stage ladder:\n   `sq`/`add`/`main` parsed in Rian runs `main(4) = add(sq(4),4) = 20`\n   ([`decl_fixpoint_test.exs`](test/rian/decl_fixpoint_test.exs)). That is \"Rian\n   front-end produces IR → existing backend compiles+runs it,\" with no Elixir parse in\n-  the loop. It now also handles **multi-clause `def` with clause patterns** —\n-  ctor/nested-literal/var/wildcard, e.g. `def fold(Add(Num(0), b)) := b` — and\n-  **capabilities** (`val`/`iso`/`ref`/`tag`), the dominant real-Rian form: a\n-  multi-clause `simp` over a sum type parses to IR equal to `Rian.Decl.parse` and\n-  **runs** (`simp(Add(Num(0), x)) = x`). The pattern AST lowers straight to Rian.Decl's\n-  pattern tuples (`Wild`→`:wild`, `Ctor(c,ps)`→`{:ctor,c,ps}`), so patterns need no\n-  projection either. **Remaining (the long tail of `Rian.Decl`):** `when` guards,\n-  parametric param types (`Vec(T)`), cons/list/string/char patterns,\n-  `mod`/`struct`/`alias`/`protocol`/generics/doc-comments, and the portable\n-  `Enum`/`Map`/`String` stdlib breadth (ADR-0047).\n+  the loop. The front-end now covers essentially the **function-and-type language**:\n+  multi-clause `def` with **clause patterns** (var/lit/ctor/wildcard and **cons/list**\n+  `[]`/`[h | t]`, nested — `def fold(Add(Num(0), b)) := b`), **capabilities**\n+  (`val`/`iso`/`ref`/`tag`), **parametric types** (`Vec(Int64)`), **list construction**\n+  (`[x | acc]`), and **`when` guards**. The dominant idioms run end to end: a\n+  cons-recursive `rev` parses to IR equal to `Rian.Decl.parse` and runs\n+  (`reverse([1,2,3]) = [3,2,1]`), as does a guarded `clamp`. Patterns lower straight to\n+  Rian.Decl's tuples; list nil/cons chains are flattened to `{:list,…}`/`{:list_lit,…}`\n+  in the projection (the shared `{:tail,…}` tag would otherwise collide), and AST clause\n+  guards reach the backend via a `Pratt.parse/1` passthrough (symmetric with the macro\n+  `parse_body`). **Remaining (the long tail of `Rian.Decl`):** `mod`/`struct`/`alias`/\n+  `protocol`/generics/doc-comments, and the portable `Enum`/`Map`/`String` stdlib\n+  breadth (ADR-0047).\n - **Stage 3 — bootstrap fixed point** (future; **determinism prerequisite verified**):\n   the whole compiler in Rian; compile its source with the Elixir host → v1, compile\n   with v1 → v2, assert **v1 == v2** (bit-identical `.beam`). The canonical terminus.","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/SELFHOST.md","sha":"033077f8065c818d7931aeaff0fdc57c0aa6ba81","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":1,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -42,7 +42,7 @@ gating library work and blocks Stage 2 below.\n |---|---|---|---|\n | **0 — Equivalence** | each ported stage matches the reference on a corpus | fixpoint diff (lexer vs `tokenize/1`; parser vs `Pratt.parse`) | **done** (lexer slices 1-5; parser slice) |\n | **1 — Self-application** | a ported stage processes **real toolchain source** (incl. the self-hosting sources themselves) and agrees with the reference | run the Rian lexer over `selfhost_parse.rian` / preludes, diff vs `Rian.Lexer.tokenize/1` | **done for the lexer** (`test/rian/selfhost_fixedpoint_test.exs`) |\n-| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **core + multi-clause done** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums and `def` functions — **including multi-clause `def` with clause patterns** (ctor/nested-literal/var/wildcard) and **capabilities** (`val`/`iso`/`ref`/`tag`) — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (a multi-clause `simp` over a sum runs; `decl_fixpoint_test.exs`). **Remaining:** `when` guards, parametric param types (`Vec(T)`), cons/list/string/char patterns, `mod`/`struct`/`alias`/`protocol`/generics/doc-comments, and the portable stdlib breadth |\n+| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **function language done** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums and `def` functions — multi-clause with **clause patterns** (var/lit/ctor/wildcard + **cons/list** `[]`/`[h\\|t]`), **capabilities** (`val`/`iso`/`ref`/`tag`), **parametric types** (`Vec(T)`), **list construction**, and **`when` guards** — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (cons-recursive `rev([1,2,3]) -> [3,2,1]`, guarded `clamp`; `decl_fixpoint_test.exs`). **Remaining:** `mod`/`struct`/`alias`/`protocol`/generics/doc-comments and the portable stdlib breadth |\n | **3 — Bootstrap fixed point** | the **whole compiler**, written in Rian, compiles its own source; doing so twice is stable | compile the Rian compiler source with the Elixir-hosted compiler → v1; compile the *same source* with v1 → v2; assert **v1 == v2** (bit-identical `.beam`) | **future** (the canonical terminus); its **determinism prerequisite is verified** — `Rian.Beam` emits byte-identical bytecode on recompilation (`selfhost_fixedpoint_test.exs`) |\n \n **\"Real self-hosting\" = Stage 2** (the front-end genuinely self-hosts, reusing a trusted backend);","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"9b19d7eb29517d69247f100581acfbfbe0cfa881","status":"modified"},{"additions":108,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/examples%2Frian%2Fselfhost_decl.rian","changes":125,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_decl.rian?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":17,"filename":"examples/rian/selfhost_decl.rian","patch":"@@ -23,24 +23,32 @@\n mod SelfhostDecl do\n   type Tok :=\n       TNum(String) | TId(String) | TKw(String) | TOp(String)\n-    | TLP | TRP | TComma | TNL\n+    | TLP | TRP | TLBracket | TRBracket | TComma | TNL\n \n-  # expression AST — constructor names lower to Rian.Pratt's surface tuples.\n+  # expression AST — Num/Id/Bin/Unary/Call lower to Rian.Pratt's surface tuples;\n+  # list construction is nil/cons (NilE / ConsE), flattened to `{:list_lit, …}` by\n+  # the projection (the cons `{:tail, …}` tag would otherwise collide with patterns).\n   type Expr :=\n       Num(String) | Id(String) | Bin(String, Expr, Expr)\n-    | Unary(String, Expr) | Call(Expr, Vec(Expr)) | EErr\n+    | Unary(String, Expr) | Call(Expr, Vec(Expr))\n+    | NilE | ConsE(Expr, Expr) | EErr\n \n-  # clause PATTERNS — names chosen to lower to Rian.Decl's pattern tuples:\n-  #   Wild -> :wild   Var(n) -> {:var,n}   Lit(k) -> {:lit,k}   Ctor(c,ps) -> {:ctor,c,ps}\n-  type Pat := Wild | Var(String) | Lit(Int64) | Ctor(String, Vec(Pat))\n+  # clause PATTERNS — Wild/Var/Lit/Ctor lower straight to Rian.Decl's tuples\n+  # (:wild / {:var,n} / {:lit,k} / {:ctor,c,ps}); list patterns are nil/cons\n+  # (NilP / ConsP), flattened to `{:list, elems, :close | {:tail,p}}` by the test.\n+  type Pat :=\n+      Wild | Var(String) | Lit(Int64) | Ctor(String, Vec(Pat))\n+    | NilP | ConsP(Pat, Pat)\n \n   type Variant := V(String, Vec(String))\n   type Param := Par(String, String, String)\n+  type PType := PT(String, Vec(Tok))\n   type Decl :=\n       DType(String, Vec(Variant))\n     | DFunc(String, Vec(Param), String, Expr)\n     | DSig(String, Vec(Param), String)\n     | DClause(String, Vec(Pat), Expr)\n+    | DClauseG(String, Vec(Pat), Expr, Expr)\n     | DErr\n \n   # result pairs (a value + the unconsumed tokens)\n@@ -127,20 +135,32 @@ mod SelfhostDecl do\n   def take_parens([t | rest], depth, acc) := take_parens(rest, depth, [t | acc])\n   def take_parens([], depth, acc) := Paren(rev_tok(acc, []), [])\n \n-  # `(pats) := body` -> a pattern clause; `(params) Ret := body` -> a typed single\n-  # clause; `(params) Ret` -> a bodiless signature.\n+  # `(pats) when g := body` / `(pats) := body` -> a pattern clause; `(params) Ret\n+  # := body` -> a typed single clause; `(params) Ret` -> a bodiless signature.\n   def classify_def(name String, inside Vec(Tok), after Vec(Tok)) DeclRes\n+  def classify_def(name, inside, [TKw(\"when\") | rest])\n+    case parse_bp(0, rest) do\n+      P(g, [TOp(\":=\") | body]) ->\n+        case parse_bp(0, body) do\n+          P(b, rest2) -> DR(DClauseG(name, parse_pats(inside), g, b), rest2)\n+        end\n+      P(_, other) -> DR(DErr, other)\n+    end\n+  end\n   def classify_def(name, inside, [TOp(\":=\") | body])\n     case parse_bp(0, body) do\n       P(b, rest) -> DR(DClause(name, parse_pats(inside), b), rest)\n     end\n   end\n-  def classify_def(name, inside, [TId(ret), TOp(\":=\") | body])\n-    case parse_bp(0, body) do\n-      P(b, rest) -> DR(DFunc(name, parse_params(inside), ret, b), rest)\n+  def classify_def(name, inside, [TId(rb) | rb_rest])\n+    case parse_ptype([TId(rb) | rb_rest]) do\n+      PT(ret, [TOp(\":=\") | body]) ->\n+        case parse_bp(0, body) do\n+          P(b, rest) -> DR(DFunc(name, parse_params(inside), ret, b), rest)\n+        end\n+      PT(ret, rest) -> DR(DSig(name, parse_params(inside), ret), rest)\n     end\n   end\n-  def classify_def(name, inside, [TId(ret) | rest]) := DR(DSig(name, parse_params(inside), ret), rest)\n   def classify_def(name, inside, after) := DR(DErr, after)\n \n   # ── parameters: `name [cap] Type` (cap in val/iso/ref/tag, default val) ────\n@@ -153,14 +173,43 @@ mod SelfhostDecl do\n     end\n   end\n \n+  # `name [cap] Type` where cap in val/iso/ref/tag (default val) and Type may be a\n+  # simple id or a single-arg parametric type `Base(Arg)` (e.g. `Vec(Int64)`).\n   def parse_param(ts Vec(Tok)) ParamRes\n-  def parse_param([TId(n), TId(\"val\"), TId(t) | rest]) := PR(Par(n, \"val\", t), rest)\n-  def parse_param([TId(n), TId(\"iso\"), TId(t) | rest]) := PR(Par(n, \"iso\", t), rest)\n-  def parse_param([TId(n), TId(\"ref\"), TId(t) | rest]) := PR(Par(n, \"ref\", t), rest)\n-  def parse_param([TId(n), TId(\"tag\"), TId(t) | rest]) := PR(Par(n, \"tag\", t), rest)\n-  def parse_param([TId(n), TId(t) | rest]) := PR(Par(n, \"val\", t), rest)\n+  def parse_param([TId(n), TId(\"val\") | rest])\n+    case parse_ptype(rest) do\n+      PT(t, r) -> PR(Par(n, \"val\", t), r)\n+    end\n+  end\n+  def parse_param([TId(n), TId(\"iso\") | rest])\n+    case parse_ptype(rest) do\n+      PT(t, r) -> PR(Par(n, \"iso\", t), r)\n+    end\n+  end\n+  def parse_param([TId(n), TId(\"ref\") | rest])\n+    case parse_ptype(rest) do\n+      PT(t, r) -> PR(Par(n, \"ref\", t), r)\n+    end\n+  end\n+  def parse_param([TId(n), TId(\"tag\") | rest])\n+    case parse_ptype(rest) do\n+      PT(t, r) -> PR(Par(n, \"tag\", t), r)\n+    end\n+  end\n+  def parse_param([TId(n) | rest])\n+    case parse_ptype(rest) do\n+      PT(t, r) -> PR(Par(n, \"val\", t), r)\n+    end\n+  end\n   def parse_param(ts) := PR(Par(\"\", \"val\", \"\"), ts)\n \n+  # a type: simple `Id`, or single-arg parametric `Base(Arg)` rebuilt as the string\n+  # `\"Base(Arg)\"` (matches Rian.Decl's collapsed spacing).\n+  def parse_ptype(ts Vec(Tok)) PType\n+  def parse_ptype([TId(base), TLP, TId(arg), TRP | rest]) := PT(Prim.str_concat(base, Prim.str_concat(\"(\", Prim.str_concat(arg, \")\"))), rest)\n+  def parse_ptype([TId(t) | rest]) := PT(t, rest)\n+  def parse_ptype(ts) := PT(\"\", ts)\n+\n   # ── clause patterns ───────────────────────────────────────────────────────\n   def parse_pats(ts Vec(Tok)) Vec(Pat)\n   def parse_pats([]) := []\n@@ -174,6 +223,7 @@ mod SelfhostDecl do\n   def parse_pat(ts Vec(Tok)) PatRes\n   def parse_pat([TId(\"_\") | rest]) := PP(Wild, rest)\n   def parse_pat([TNum(s) | rest]) := PP(Lit(str_to_int(s)), rest)\n+  def parse_pat([TLBracket | rest]) := parse_list_pat(rest)\n   def parse_pat([TId(ctor), TLP | rest])\n     case parse_patlist(rest) do\n       PL(args, rest2) -> PP(Ctor(ctor, args), rest2)\n@@ -188,6 +238,26 @@ mod SelfhostDecl do\n   end\n   def parse_pat(ts) := PP(Wild, ts)\n \n+  # list pattern as a nil/cons chain: `[]` -> NilP, `[a, b]` -> ConsP(a, ConsP(b,\n+  # NilP)), `[h | t]` -> ConsP(h, t).\n+  def parse_list_pat(ts Vec(Tok)) PatRes\n+  def parse_list_pat([TRBracket | rest]) := PP(NilP, rest)\n+  def parse_list_pat(ts)\n+    case parse_pat(ts) do\n+      PP(h, [TComma | rest]) ->\n+        case parse_list_pat(rest) do\n+          PP(t, r) -> PP(ConsP(h, t), r)\n+        end\n+      PP(h, [TOp(\"|\") | rest]) ->\n+        case parse_pat(rest) do\n+          PP(t, [TRBracket | r]) -> PP(ConsP(h, t), r)\n+          PP(t, other) -> PP(ConsP(h, t), other)\n+        end\n+      PP(h, [TRBracket | rest]) -> PP(ConsP(h, NilP), rest)\n+      PP(h, rest) -> PP(ConsP(h, NilP), rest)\n+    end\n+  end\n+\n   def parse_patlist(ts Vec(Tok)) PatList\n   def parse_patlist([TRP | rest]) := PL([], rest)\n   def parse_patlist(ts)\n@@ -237,6 +307,7 @@ mod SelfhostDecl do\n   def parse_primary(ts Vec(Tok)) Parse\n   def parse_primary([TNum(n) | rest]) := parse_postfix(Num(n), rest)\n   def parse_primary([TId(x) | rest]) := parse_postfix(Id(x), rest)\n+  def parse_primary([TLBracket | rest]) := parse_list_expr(rest)\n   def parse_primary([TLP | rest])\n     case parse_bp(0, rest) do\n       P(e, [TRP | rest2]) -> parse_postfix(e, rest2)\n@@ -245,6 +316,26 @@ mod SelfhostDecl do\n   end\n   def parse_primary(ts) := P(EErr, ts)\n \n+  # list construction as a nil/cons chain (mirrors parse_list_pat): `[x | acc]` ->\n+  # ConsE(x, acc); elements are full expressions.\n+  def parse_list_expr(ts Vec(Tok)) Parse\n+  def parse_list_expr([TRBracket | rest]) := P(NilE, rest)\n+  def parse_list_expr(ts)\n+    case parse_bp(0, ts) do\n+      P(h, [TComma | rest]) ->\n+        case parse_list_expr(rest) do\n+          P(t, r) -> P(ConsE(h, t), r)\n+        end\n+      P(h, [TOp(\"|\") | rest]) ->\n+        case parse_bp(0, rest) do\n+          P(t, [TRBracket | r]) -> P(ConsE(h, t), r)\n+          P(t, other) -> P(ConsE(h, t), other)\n+        end\n+      P(h, [TRBracket | rest]) -> P(ConsE(h, NilE), rest)\n+      P(h, rest) -> P(ConsE(h, NilE), rest)\n+    end\n+  end\n+\n   def parse_postfix(node Expr, ts Vec(Tok)) Parse\n   def parse_postfix(node, [TLP | rest])\n     case parse_args(rest) do","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/examples%2Frian%2Fselfhost_decl.rian","sha":"a90518c3d118367762b6171f659a5ff12e37af62","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Fbeam.ex","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fbeam.ex?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":2,"filename":"lib/rian/beam.ex","patch":"@@ -368,9 +368,10 @@ defmodule Rian.Beam do\n      body_forms(body, scope, rtable)}\n   end\n \n-  # a clause guard is a source string (from `Rian.Decl`); normalize to core (or nil)\n+  # a clause guard is a source string (from `Rian.Decl`) or an already-parsed AST\n+  # (from a Rian-written front-end, ADR-0063); `Pratt.parse/1` accepts either.\n   defp guard_core(nil), do: nil\n-  defp guard_core(g) when is_binary(g), do: Core.from_expr(Pratt.parse(g))\n+  defp guard_core(g), do: Core.from_expr(Pratt.parse(g))\n \n   defp guard_form(nil, _scope), do: []\n   defp guard_form(core, scope), do: [[expr_form(core, scope)]]","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Fbeam.ex","sha":"7424f408c265a248958e9866cc9b622a576cb884","status":"modified"},{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Flower.ex","changes":10,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Flower.ex?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":4,"filename":"lib/rian/lower.ex","patch":"@@ -428,14 +428,16 @@ defmodule Rian.Lower do\n   # `when …` on Elixir and `if …` on Rust (clauses-guards §5).\n   defp guard_str(c, target, deref \\\\ []) do\n     case Map.get(c, :guard) do\n-      g when is_binary(g) ->\n+      nil ->\n+        \"\"\n+\n+      g ->\n         # On Rust, a binder bound inside a slice/list element is a `&T` borrow\n         # (match ergonomics); a guard comparing it (`*c == 32`) must deref it.\n+        # `g` is a source string (Rian.Decl) or an already-parsed AST (Stage-2\n+        # front-end, ADR-0063) — `Pratt.parse/1` accepts either.\n         ast = Pratt.parse(g) |> deref_ids(deref)\n         guard_kw(target) <> (emit(Core.from_expr(ast), target) |> elem(0))\n-\n-      _ ->\n-        \"\"\n     end\n   end\n ","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Flower.ex","sha":"f31fa9273646bb61b2b6e486a6a45dafe153eff4","status":"modified"},{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Fpratt.ex","changes":7,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fpratt.ex?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":1,"filename":"lib/rian/pratt.ex","patch":"@@ -17,7 +17,12 @@ defmodule Rian.Pratt do\n   # it is the failable-bind arrow, valid only in `with`/`for` clause headers.\n   @infix ~w(+ - * / rem div <> in |> < <= > >= == != and or <~)\n \n-  def parse(str) do\n+  # already-parsed passthrough (symmetric with `parse_body/1`): a Rian-written\n+  # front-end (ADR-0063 Stage 2) may hand the checker/emitters an AST directly —\n+  # e.g. a clause guard parsed in Rian — so they can re-`parse` it idempotently.\n+  def parse(ast) when is_tuple(ast), do: ast\n+\n+  def parse(str) when is_binary(str) do\n     {ast, rest} = parse_expr(Rian.Lexer.expr_tokens(str), 0)\n     if rest != [], do: raise(ArgumentError, \"trailing tokens: #{inspect(rest)}\")\n     Rian.Prim.normalize(ast)","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/lib%2Frian%2Fpratt.ex","sha":"575052595f163977e519371212f66b01c28aec04","status":"modified"},{"additions":123,"blob_url":"https://github.com/wstein/rian-lab/blob/5c81828a3246f8c6dacea720f3b9a765ca34921f/test%2Frian%2Fdecl_fixpoint_test.exs","changes":135,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_fixpoint_test.exs?ref=5c81828a3246f8c6dacea720f3b9a765ca34921f","deletions":12,"filename":"test/rian/decl_fixpoint_test.exs","patch":"@@ -13,9 +13,11 @@ defmodule Rian.DeclFixpointTest do\n   # Stage-2 thesis: a Rian front-end producing IR the existing backend consumes,\n   # with no Elixir parse in the loop.\n   #\n-  # Core forms only (type sums + single-clause `def` over simple types); the long\n-  # tail of `Rian.Decl` (multi-clause, capabilities, parametric types, mod/struct/\n-  # protocol/generics) remains — see ADR-0063.\n+  # Covers: `type` sums; `def` functions (single + multi-clause); clause patterns\n+  # (var/lit/ctor/wildcard/cons-list); capabilities (val/iso/ref/tag); parametric\n+  # param/return types (`Vec(T)`); list construction; and `when` guards. Remaining\n+  # long tail of `Rian.Decl`: `mod`/`struct`/`alias`/`protocol`/generics/doc-comments\n+  # and the portable stdlib breadth — see ADR-0063.\n \n   setup_all do\n     {:ok, fe} = Beam.load(File.read!(\"examples/rian/selfhost_decl.rian\"), :rian_decl_frontend)\n@@ -31,6 +33,8 @@ defmodule Rian.DeclFixpointTest do\n   defp inject({:rparen}), do: :trp\n   defp inject({:comma}), do: :t_comma\n   defp inject({:nl}), do: :tnl\n+  defp inject({:lbracket}), do: :tl_bracket\n+  defp inject({:rbracket}), do: :tr_bracket\n \n   defp front_decls(fe, src), do: fe.parse_program(src |> Lexer.tokenize() |> Enum.map(&inject/1))\n \n@@ -48,7 +52,55 @@ defmodule Rian.DeclFixpointTest do\n   end\n \n   defp param({:par, n, cap, t}), do: %Param{name: n, type: t, cap: String.to_atom(cap)}\n-  defp clause(pats, body), do: %Clause{pats: pats, body: {:block, [{:expr, body}]}, guard: nil}\n+\n+  defp clause(pats, body),\n+    do: %Clause{pats: Enum.map(pats, &cp/1), body: {:block, [{:expr, ce(body)}]}, guard: nil}\n+\n+  # convert the front-end's nil/cons list PATTERNS to Rian.Decl's `{:list, …}` shape\n+  # (var/lit/ctor/wild lower directly); flatten the cons chain.\n+  defp cp({:ctor, c, args}), do: {:ctor, c, Enum.map(args, &cp/1)}\n+  defp cp(:nil_p), do: {:list, [], :close}\n+\n+  defp cp({:cons_p, _, _} = c) do\n+    {elems, tail} = flat_p(c)\n+    {:list, Enum.map(elems, &cp/1), tail}\n+  end\n+\n+  defp cp(leaf), do: leaf\n+\n+  defp flat_p(:nil_p), do: {[], :close}\n+  defp flat_p({:cons_p, h, :nil_p}), do: {[h], :close}\n+\n+  defp flat_p({:cons_p, h, {:cons_p, _, _} = t}) do\n+    {es, tl} = flat_p(t)\n+    {[h | es], tl}\n+  end\n+\n+  defp flat_p({:cons_p, h, t}), do: {[h], {:tail, cp(t)}}\n+\n+  # convert the front-end's nil/cons list EXPRESSIONS to `{:list_lit, …}` (recurse\n+  # into bin/unary/call); num/id lower directly.\n+  defp ce(:nil_e), do: {:list_lit, [], nil}\n+\n+  defp ce({:cons_e, _, _} = c) do\n+    {elems, tail} = flat_e(c)\n+    {:list_lit, Enum.map(elems, &ce/1), tail}\n+  end\n+\n+  defp ce({:bin, op, l, r}), do: {:bin, op, ce(l), ce(r)}\n+  defp ce({:unary, op, x}), do: {:unary, op, ce(x)}\n+  defp ce({:call, f, args}), do: {:call, ce(f), Enum.map(args, &ce/1)}\n+  defp ce(leaf), do: leaf\n+\n+  defp flat_e(:nil_e), do: {[], nil}\n+  defp flat_e({:cons_e, h, :nil_e}), do: {[h], nil}\n+\n+  defp flat_e({:cons_e, h, {:cons_e, _, _} = t}) do\n+    {es, tl} = flat_e(t)\n+    {[h | es], tl}\n+  end\n+\n+  defp flat_e({:cons_e, h, t}), do: {[h], {:tail, ce(t)}}\n \n   defp func(name, params, ret, clauses) do\n     %Func{\n@@ -77,11 +129,23 @@ defmodule Rian.DeclFixpointTest do\n   end\n \n   defp group([{:d_sig, name, params, ret} | rest]) do\n-    {cls, rest2} = Enum.split_while(rest, &match?({:d_clause, ^name, _, _}, &1))\n-    clauses = Enum.map(cls, fn {:d_clause, _, pats, body} -> clause(pats, body) end)\n-    [func(name, params, ret, clauses) | group(rest2)]\n+    {cls, rest2} =\n+      Enum.split_while(rest, fn d ->\n+        match?({:d_clause, ^name, _, _}, d) or match?({:d_clause_g, ^name, _, _, _}, d)\n+      end)\n+\n+    [func(name, params, ret, Enum.map(cls, &to_clause/1)) | group(rest2)]\n   end\n \n+  defp to_clause({:d_clause, _, pats, body}), do: clause(pats, body)\n+\n+  defp to_clause({:d_clause_g, _, pats, guard, body}),\n+    do: %Clause{\n+      pats: Enum.map(pats, &cp/1),\n+      body: {:block, [{:expr, ce(body)}]},\n+      guard: ce(guard)\n+    }\n+\n   defp to_prog(decls) do\n     ir = group(decls)\n \n@@ -94,10 +158,18 @@ defmodule Rian.DeclFixpointTest do\n     }\n   end\n \n-  # clause bodies differ in form (Decl.parse keeps a source string; the front-end a\n-  # parsed AST) — normalize both through `Pratt.parse_body/1` to compare.\n-  defp norm_func(f),\n-    do: %{f | clauses: Enum.map(f.clauses, &%{&1 | body: Pratt.parse_body(&1.body)})}\n+  # clause bodies/guards differ in form (Decl.parse keeps source strings; the\n+  # front-end parsed ASTs) — normalize both through `Pratt.parse_body`/`parse` to\n+  # compare (`Pratt.parse/1` is idempotent on an AST).\n+  defp norm_func(f) do\n+    %{\n+      f\n+      | clauses:\n+          Enum.map(f.clauses, fn c ->\n+            %{c | body: Pratt.parse_body(c.body), guard: c.guard && Pratt.parse(c.guard)}\n+          end)\n+    }\n+  end\n \n   @corpus [\n     \"type Color := Red | RGB(Int64, Int64)\",\n@@ -118,7 +190,18 @@ defmodule Rian.DeclFixpointTest do\n     # multi-clause over integers (literal + var clauses)\n     \"def classify(n Int64) Int64\\n\" <>\n       \"def classify(0) := 100\\n\" <>\n-      \"def classify(n) := n * 2\"\n+      \"def classify(n) := n * 2\",\n+    # parametric types + cons/list patterns + list construction (cons recursion)\n+    \"def rev(xs val Vec(Int64), acc val Vec(Int64)) Vec(Int64)\\n\" <>\n+      \"def rev([], acc) := acc\\n\" <>\n+      \"def rev([x | xs], acc) := rev(xs, [x | acc])\",\n+    \"def len(xs val Vec(Int64)) Int64\\n\" <>\n+      \"def len([]) := 0\\n\" <>\n+      \"def len([_ | t]) := 1 + len(t)\",\n+    # `when` guards\n+    \"def clamp(n Int64) Int64\\n\" <>\n+      \"def clamp(n) when n < 0 := 0\\n\" <>\n+      \"def clamp(n) := n\"\n   ]\n \n   describe \"Stage 2 declarations — the Rian front-end builds the same IR as Rian.Decl.parse\" do\n@@ -196,5 +279,33 @@ defmodule Rian.DeclFixpointTest do\n       assert mod.classify(0) == 100\n       assert mod.classify(7) == 14\n     end\n+\n+    test \"a CONS-RECURSIVE function (list patterns + construction) runs\", %{frontend: fe} do\n+      # the lexer/parser idiom: `[]` / `[h | t]` patterns and `[x | acc]` construction\n+      src = \"\"\"\n+      def rev(xs val Vec(Int64), acc val Vec(Int64)) Vec(Int64)\n+      def rev([], acc) := acc\n+      def rev([x | xs], acc) := rev(xs, [x | acc])\n+      def reverse(xs val Vec(Int64)) Vec(Int64) := rev(xs, [])\n+      \"\"\"\n+\n+      prog = front_decls(fe, src) |> to_prog()\n+      {:ok, mod} = Beam.load_ir(prog, :RianStage2Rev)\n+      assert mod.reverse([1, 2, 3]) == [3, 2, 1]\n+      assert mod.rev([1, 2], [9]) == [2, 1, 9]\n+    end\n+\n+    test \"a GUARDED multi-clause function runs\", %{frontend: fe} do\n+      src = \"\"\"\n+      def clamp(n Int64) Int64\n+      def clamp(n) when n < 0 := 0\n+      def clamp(n) := n\n+      \"\"\"\n+\n+      prog = front_decls(fe, src) |> to_prog()\n+      {:ok, mod} = Beam.load_ir(prog, :RianStage2Clamp)\n+      assert mod.clamp(-5) == 0\n+      assert mod.clamp(7) == 7\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/5c81828a3246f8c6dacea720f3b9a765ca34921f/test%2Frian%2Fdecl_fixpoint_test.exs","sha":"8474094ffe4197e077ba48ca2599fc35290c402b","status":"modified"}]}