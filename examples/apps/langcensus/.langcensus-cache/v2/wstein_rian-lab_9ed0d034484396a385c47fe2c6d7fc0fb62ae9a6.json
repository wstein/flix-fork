{"files":[{"additions":34,"blob_url":"https://github.com/wstein/rian-lab/blob/9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6/lib%2Frian%2Flexer.ex","changes":34,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Flexer.ex?ref=9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6","deletions":0,"filename":"lib/rian/lexer.ex","patch":"@@ -41,6 +41,12 @@ defmodule Rian.Lexer do\n   # checked first) still wins when `@` is immediately followed by an identifier.\n   @single [\"+\", \"-\", \"*\", \"/\", \"<\", \">\", \".\", \"|\", \":\", \"&\", \"@\", \"^\"]\n \n+  # Operator-name atoms (`:==`, `:/=`, `:=<`, `:and`): a `:` directly followed by an\n+  # operator run made of these chars, or by an operator word, is the atom NAMING that\n+  # operator (the Erlang/Elixir-AST operator tables the transpiler emits). The bare\n+  # bind `:=` (run is exactly `=`) and the bitstring `::` are NOT atoms.\n+  @atom_op_chars ~w(+ - * / < > = !)\n+\n   @num_re ~r/^\\d[\\d_]*(?:\\.\\d[\\d_]*)?(?:[eE][+-]?\\d+)?/\n   # Identifiers may carry a single trailing `?` or `!` — the Elixir/Ruby/Crystal\n   # predicate/bang convention (ADR-0033): `empty?`, `gate!`. A `!` is part of the\n@@ -419,6 +425,12 @@ defmodule Rian.Lexer do\n         {cp, rest} = lex_char(advance(str, 1))\n         lex(rest, [{:char, cp} | acc])\n \n+      # operator-name atom (`:==`/`:/=`/`:=<`/`:and`) — must precede `@multi`, else\n+      # `:=<` would be grabbed as the bind `:=`. Emitted as `:` + an `{:id, name}` so\n+      # the existing `:`+id atom rules handle it in every position.\n+      name = op_atom_name(str) ->\n+        lex(advance(str, 1 + String.length(name)), [{:id, name}, {:op, \":\"} | acc])\n+\n       op = Enum.find(@multi, &String.starts_with?(str, &1)) ->\n         lex(advance(str, String.length(op)), [{:op, op} | acc])\n \n@@ -438,6 +450,28 @@ defmodule Rian.Lexer do\n     end\n   end\n \n+  # The atom name when `str` begins with an operator-name atom, else nil. A symbolic\n+  # run (`==`, `/=`, `=<`) wins unless it is exactly the bind `=`; otherwise an\n+  # operator word (`and`/`or`/…) directly after the `:`. `::` declines (next char `:`\n+  # is not an atom-op char and `:` is no op-word), so the bitstring spec is unaffected.\n+  defp op_atom_name(\":\" <> rest) do\n+    run = rest |> String.graphemes() |> Enum.take_while(&(&1 in @atom_op_chars)) |> Enum.join()\n+\n+    cond do\n+      run != \"\" and run != \"=\" -> run\n+      true -> op_word_name(rest)\n+    end\n+  end\n+\n+  defp op_atom_name(_), do: nil\n+\n+  defp op_word_name(rest) do\n+    case Regex.run(~r/^(and|or|not|in|rem|div)\\b/, rest) do\n+      [_, w] -> w\n+      _ -> nil\n+    end\n+  end\n+\n   defp punct(str) do\n     case str do\n       \"(\" <> _ -> {:lparen}","raw_url":"https://github.com/wstein/rian-lab/raw/9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6/lib%2Frian%2Flexer.ex","sha":"c81758654571908ddd54ce1ba59a18fea980170d","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6/test%2Frian%2Fpratt_test.exs","changes":18,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fpratt_test.exs?ref=9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6","deletions":2,"filename":"test/rian/pratt_test.exs","patch":"@@ -378,8 +378,22 @@ defmodule Rian.PrattTest do\n       assert Pratt.parse_pats(~s|:\"+\"|) == [{:atom, \"+\"}]\n     end\n \n-    test \"a bare operator atom `:+` is still a parse error (use the quoted form)\" do\n-      assert_raise ArgumentError, fn -> Pratt.parse(\":+\") end\n+    test \"a bare operator-name atom parses (`:+`, `:==`, `:/=`, `:=<`, `:and`)\" do\n+      # `:` + an operator run (or operator word) names that operator as an atom — the\n+      # Erlang/Elixir-AST operator tables the transpiler emits. The quoted form still\n+      # works for names outside the operator-char set.\n+      assert Pratt.parse(\":+\") == {:atom, \"+\"}\n+      assert Pratt.parse(\":==\") == {:atom, \"==\"}\n+      assert Pratt.parse(\":/=\") == {:atom, \"/=\"}\n+      assert Pratt.parse(\":=<\") == {:atom, \"=<\"}\n+      assert Pratt.parse(\":and\") == {:atom, \"and\"}\n+    end\n+\n+    test \"the bind `:=` and bitstring `::` are NOT operator atoms\" do\n+      assert Pratt.parse_body(\"x := 1; x\") ==\n+               {:block, [{:bind, \"x\", {:num, \"1\"}}, {:expr, {:id, \"x\"}}]}\n+\n+      assert Pratt.parse(\"<<c::utf8>>\") == {:bitstr, [{:bitseg, {:id, \"c\"}, [type: \"utf8\"]}]}\n     end\n   end\n ","raw_url":"https://github.com/wstein/rian-lab/raw/9ed0d034484396a385c47fe2c6d7fc0fb62ae9a6/test%2Frian%2Fpratt_test.exs","sha":"8726d570cd28507cefe5b41fd528a28978626f30","status":"modified"}]}