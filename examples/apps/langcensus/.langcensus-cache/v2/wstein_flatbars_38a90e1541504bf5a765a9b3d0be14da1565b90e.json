{"files":[{"additions":8,"blob_url":"https://github.com/wstein/flatbars/blob/38a90e1541504bf5a765a9b3d0be14da1565b90e/lab%2Fapp%2FCTX-DESIGN.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fapp%2FCTX-DESIGN.md?ref=38a90e1541504bf5a765a9b3d0be14da1565b90e","deletions":0,"filename":"lab/app/CTX-DESIGN.md","patch":"@@ -42,6 +42,14 @@ const ctx = {\n `ctx` mutates in place exactly where the `let`s were reassigned — `lastOutput = x`\n becomes `ctx.caches.lastOutput = x`. No semantic change.\n \n+## Progress\n+\n+- ✅ All 11 dock/inspector panels extracted to `app/dock/*.mjs` (leaf consumers).\n+- ✅ `ctx` object introduced; the output caches (lastOutput/lastSegments/lastData/\n+  lastProgram) migrated into `ctx.caches.*` via a word-boundary sweep (43 sites),\n+  rename-only — verified by the smoke (a missed site throws a loud ReferenceError).\n+  Other last* caches migrate into ctx.caches as their owning subsystem moves.\n+\n ## Move order (leaf-most consumers first, run() last)\n \n Each function moves to a module as `export function f(ctx, …args)`; index.html","raw_url":"https://github.com/wstein/flatbars/raw/38a90e1541504bf5a765a9b3d0be14da1565b90e/lab%2Fapp%2FCTX-DESIGN.md","sha":"daaedff66a9324ee9ec5bd9bc2c9490302dcf74a","status":"modified"},{"additions":43,"blob_url":"https://github.com/wstein/flatbars/blob/38a90e1541504bf5a765a9b3d0be14da1565b90e/lab%2Findex.html","changes":79,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=38a90e1541504bf5a765a9b3d0be14da1565b90e","deletions":36,"filename":"lab/index.html","patch":"@@ -3003,11 +3003,18 @@\n       // ./app/render-helpers.mjs. applyEscape takes the `escape-modes` capability\n       // as an argument (it was the only engine coupling).\n \n-      let lastOutput = \"\";\n-      let lastSegments = [];\n-      let lastData = null; // the post-transform data the template rendered (Render Data view)\n+      // The shared render-context object (CTX-DESIGN.md). Phase 0 introduces it for\n+      // the output caches first — the cluster's most widely-read group. Other last*\n+      // caches migrate into ctx.caches as their owning subsystem moves out.\n+      const ctx = {\n+        caches: {\n+          lastOutput: \"\",\n+          lastSegments: [],\n+          lastData: null,    // the post-transform data the template rendered (Render Data view)\n+          lastProgram: null, // the compiled wire program, for the Bytecode view\n+        },\n+      };\n       let lastSegViews = []; // [{from,to,file,kind,start,end}] — segment char ranges in the output editor\n-      let lastProgram = null; // the compiled wire program, for the Bytecode view\n       let lastUsedTransformers = []; // ADR-0013 report: transformer names called by the compiled program\n       let dockProblems = []; // diagnostics for the Problems tab, refreshed each run()\n       let hadProblems = false; // tracks the 0->>0 transition so the dock auto-opens once\n@@ -3036,16 +3043,16 @@\n       let lastRequiredAssigns = []; // engine reflection: assign names the program reads\n       let lastMissingAssigns = [];  // intersection: requiredAssigns NOT in current data\n \n-      // outputHasContent now lives in ./app/render-helpers.mjs — it takes a snapshot\n-      // of { lastData, lastProgram, lastOutput } (the ctx-restructure gateway form).\n-      const outputCaches = () => ({ lastData, lastProgram, lastOutput });\n+      // outputHasContent (./app/render-helpers.mjs) destructures { lastData,\n+      // lastProgram, lastOutput } from a cache snapshot — ctx.caches has exactly\n+      // those keys (plus lastSegments, which it ignores), so pass it directly.\n \n       function setOutput(str, segments = []) {\n-        lastOutput = str;\n-        lastSegments = segments;\n+        ctx.caches.lastOutput = str;\n+        ctx.caches.lastSegments = segments;\n         paintTextView();\n         renderPreview();\n-        qs(\".output-body\").dataset.empty = outputHasContent(outputView, outputCaches()) ? \"false\" : \"true\";\n+        qs(\".output-body\").dataset.empty = outputHasContent(outputView, ctx.caches) ? \"false\" : \"true\";\n         renderDock();\n         renderDataInspect();\n         refreshSearchPanel();\n@@ -3055,7 +3062,7 @@\n       // Render Data / Bytecode) that is exactly what the read-only editor shows;\n       // for the rendered/markdown iframe views it is the raw rendered string.\n       function copyOutput() {\n-        const text = byId(\"output-text\").hidden ? lastOutput : outputEditor.state.doc.toString();\n+        const text = byId(\"output-text\").hidden ? ctx.caches.lastOutput : outputEditor.state.doc.toString();\n         const flash = () => {\n           const btn = byId(\"out-copy\");\n           btn.querySelector(\".ico-copy\").hidden = true;\n@@ -3101,7 +3108,7 @@\n         if (transformSrc.trim()) out.push(\n           \"## Transform (JSONata)\", \"\",\n           mdFence(transformSrc, \"jsonata\"), \"\");\n-        out.push(\"## Output\", \"\", mdFence(lastOutput, \"\"), \"\");\n+        out.push(\"## Output\", \"\", mdFence(ctx.caches.lastOutput, \"\"), \"\");\n         return out.join(\"\\n\");\n       }\n \n@@ -3118,7 +3125,7 @@\n           content: transformSrc,\n         });\n         if (helpersSrc.trim()) files.push({ name: \"helpers.js\", content: helpersSrc });\n-        files.push({ name: \"output.txt\", content: lastOutput });\n+        files.push({ name: \"output.txt\", content: ctx.caches.lastOutput });\n         files.push({ name: \"README.md\", content: await buildMarkdownDoc() });\n         return files;\n       }\n@@ -3199,12 +3206,12 @@\n       function paintTextView() {\n         if (outputView === \"bytecode\") {\n           lastSegViews = [];\n-          setOutputDoc(lastProgram ? disassemble(lastProgram) : \"\", Decoration.none);\n+          setOutputDoc(ctx.caches.lastProgram ? disassemble(ctx.caches.lastProgram) : \"\", Decoration.none);\n         } else if (outputView === \"data\") {\n           let text = \"\";\n-          if (lastData != null) {\n-            try { text = dumpYaml(lastData, { indent: 2, lineWidth: -1, noRefs: true }); }\n-            catch { text = String(lastData); }\n+          if (ctx.caches.lastData != null) {\n+            try { text = dumpYaml(ctx.caches.lastData, { indent: 2, lineWidth: -1, noRefs: true }); }\n+            catch { text = String(ctx.caches.lastData); }\n           }\n           lastSegViews = [];\n           setOutputDoc(text, Decoration.none);\n@@ -3239,9 +3246,9 @@\n           }\n           setOutputDoc(text, Decoration.none);\n         } else {\n-          const { segViews, deco } = buildProvenance(lastOutput, lastSegments);\n+          const { segViews, deco } = buildProvenance(ctx.caches.lastOutput, ctx.caches.lastSegments);\n           lastSegViews = segViews;\n-          setOutputDoc(lastOutput, deco);\n+          setOutputDoc(ctx.caches.lastOutput, deco);\n         }\n         updateOutLang();\n       }\n@@ -3362,7 +3369,7 @@\n         // `sandbox`) recovers it. Guarding the attribute makes the first paint stick.\n         const wantSandbox = scripts ? \"allow-scripts\" : \"allow-same-origin\";\n         if (frame.getAttribute(\"sandbox\") !== wantSandbox) frame.setAttribute(\"sandbox\", wantSandbox);\n-        const body = outputView === \"markdown\" ? marked.parse(lastOutput) : lastOutput;\n+        const body = outputView === \"markdown\" ? marked.parse(ctx.caches.lastOutput) : ctx.caches.lastOutput;\n         frame.srcdoc = previewDoc(body, scripts);\n       }\n \n@@ -3380,7 +3387,7 @@\n         // match the current sources again — clear the stale-offset guard.\n         renderPending = false;\n         setRecomputing(false); // I-1 · debounce window ended; clear before sync work\n-        lastData = null; // reset; reassigned once data is parsed + transformed below\n+        ctx.caches.lastData = null; // reset; reassigned once data is parsed + transformed below\n         dockProblems = []; // success leaves this empty; error paths populate it below\n         // Reset per-run analyses so a stale value never leaks into the dock.\n         lastTimings = null; lastAst = null; lastDataAccess = []; lastWhitespace = [];\n@@ -3397,7 +3404,7 @@\n         const tCompileEnd = performance.now();\n \n         if (compiled.errors) {\n-          lastProgram = null;\n+          ctx.caches.lastProgram = null;\n           lastUsedTransformers = [];\n           // The compiler reports every recoverable parse error in one pass, in\n           // source order. Each names the file (\"main\" or a partial) it occurred\n@@ -3428,7 +3435,7 @@\n           setOutput(\"\"); saveHash(); return;\n         }\n \n-        lastProgram = compiled.program;\n+        ctx.caches.lastProgram = compiled.program;\n         lastUsedTransformers = renderer.usedTransformers(compiled.program);\n         lastRequiredAssigns = renderer.requiredAssigns(compiled.program);\n         clearDiagnostics();\n@@ -3501,22 +3508,22 @@\n           try {\n             data = jsonata(transformSrc).evaluate(data);\n           } catch (err) {\n-            lastData = null;\n+            ctx.caches.lastData = null;\n             const message = \"transform · \" + (err.message || err.code || \"invalid JSONata\");\n             setError(\"transform error: \" + (err.message || err.code || \"invalid JSONata\"));\n             dockProblems = [{ severity: \"error\", file: \"transform\", line: 1, col: 1, message }];\n             setOutput(\"\"); saveHash(); return;\n           }\n         }\n-        lastData = data; // expose the view-model to the \"Render Data\" output view\n+        ctx.caches.lastData = data; // expose the view-model to the \"Render Data\" output view\n \n         // Inject catalog-driven transformer lists for the cheat-sheet example so\n         // the template never hard-codes transformer metadata. The original data.yaml\n         // carries `categories` / `overrides` / `entries` on each group; this step\n         // populates `transformers` from the live engine catalog.\n         if (loadedExampleIdx >= 0 && examples[loadedExampleIdx]?.id === \"cheat-sheet\") {\n           data = buildCheatSheetData(data, renderer.catalog());\n-          lastData = data;\n+          ctx.caches.lastData = data;\n         }\n \n         // Data-aware analyses, now that the view-model is finalized. Coverage\n@@ -3644,7 +3651,7 @@\n           compileMs: tCompileEnd - tCompileStart,\n           renderMs: tRenderEnd - tRenderStart,\n           totalMs: (tCompileEnd - tCompileStart) + (tParseEnd - tParseStart) + (tRenderEnd - tRenderStart),\n-          outBytes: (lastOutput || \"\").length,\n+          outBytes: (ctx.caches.lastOutput || \"\").length,\n         };\n         renderDock();\n         saveHash();\n@@ -3767,15 +3774,15 @@\n         byId(\"view-controls\").hidden = !showLang;\n         // Text views paint synchronously. The preview iframe is deferred to the\n         // next frame: switching *to* the preview (on example load / a shared\n-        // \"rendered\" deep-link) runs before run() has recomputed `lastOutput`, so a\n+        // \"rendered\" deep-link) runs before run() has recomputed `ctx.caches.lastOutput`, so a\n         // synchronous paint would show the PREVIOUS example's output (\"renders to\n         // old content\"), and the visibility change races the srcdoc set. By the\n-        // time the frame fires, run() has updated `lastOutput` and layout settled —\n+        // time the frame fires, run() has updated `ctx.caches.lastOutput` and layout settled —\n         // one clean paint of the current render. (Edits paint via setOutput, which\n         // calls renderPreview directly, so they stay instant.)\n         if (isText) { paintTextView(); outputEditor.requestMeasure(); }\n         else requestAnimationFrame(renderPreview);\n-        qs(\".output-body\").dataset.empty = outputHasContent(view, outputCaches()) ? \"false\" : \"true\";\n+        qs(\".output-body\").dataset.empty = outputHasContent(view, ctx.caches) ? \"false\" : \"true\";\n         caretLinkKey = null; syncOutputToCaret();\n         // Re-render the dock (Coverage panel) and the inspector pane (Context\n         // Inspector) so their render-only placeholders show when switching\n@@ -4313,8 +4320,8 @@\n         updateLangPlaceholder();\n         setOutputView(validView(\"rendered\"));\n         showActiveTab();\n-        run(); // synchronous render — populates lastOutput\n-        const verdict = vendoredVerdict(fx, lastOutput);\n+        run(); // synchronous render — populates ctx.caches.lastOutput\n+        const verdict = vendoredVerdict(fx, ctx.caches.lastOutput);\n         setExampleLabel(`vendored ${fx.id} — ` + (verdict.match ? \"✓ conforms\" : \"✗ \" + verdict.severity));\n         // Show the link back to the upstream source the fixture was copied from.\n         const srcEl = byId(\"vendored-source\");\n@@ -5164,7 +5171,7 @@\n         // the active file / last render (re-rendered by renderDock after each run).\n         { id: \"context\", label: \"Context Inspector\", noBadge: true, requires: [\"context-inspect\"],\n           count: () => 0, badge: () => \"\", render: (body) => renderContext(body,\n-            { ctxTarget, program: lastProgram, data: lastData, inspectAt: renderer.inspectAt, minbarsCompat }) },\n+            { ctxTarget, program: ctx.caches.lastProgram, data: ctx.caches.lastData, inspectAt: renderer.inspectAt, minbarsCompat }) },\n         { id: \"ast\", label: \"Syntax Tree\", noBadge: true,\n           count: () => 0, badge: () => \"\", render: renderAst },\n       ];\n@@ -5294,9 +5301,9 @@\n           name,\n           template: tmpl,\n           partials,\n-          data: lastData == null ? {} : lastData,\n+          data: ctx.caches.lastData == null ? {} : ctx.caches.lastData,\n           escape: escapeMode,\n-          expected: lastOutput,\n+          expected: ctx.caches.lastOutput,\n           transformers: lastUsedTransformers,\n         };\n         const json = JSON.stringify(vector);\n@@ -5369,7 +5376,7 @@\n       // (loop bodies yield one per iteration).\n       // Context Inspector (renderContext + the snapTitle/renderSnap/ctxSnippet/\n       // ctxType/oneLineLabel helpers) now lives in ./app/dock/context.mjs — it\n-      // takes ctxTarget, the lastProgram/lastData caches, renderer.inspectAt, and\n+      // takes ctxTarget, the ctx.caches.lastProgram/ctx.caches.lastData caches, renderer.inspectAt, and\n       // the MinBars compat flag (the descriptor above passes them).\n \n ","raw_url":"https://github.com/wstein/flatbars/raw/38a90e1541504bf5a765a9b3d0be14da1565b90e/lab%2Findex.html","sha":"c1658c4fa61d8e0856b299efd7cda5289410a588","status":"modified"}]}