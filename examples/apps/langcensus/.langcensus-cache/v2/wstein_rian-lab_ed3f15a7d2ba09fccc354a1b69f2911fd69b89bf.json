{"files":[{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/SELFHOST.md","changes":10,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf","deletions":3,"filename":"SELFHOST.md","patch":"@@ -346,9 +346,13 @@ over time. The proof is a four-stage ladder:\n   Rian.Decl's tuples; list nil/cons chains are flattened to `{:list,…}`/`{:list_lit,…}`\n   in the projection (the shared `{:tail,…}` tag would otherwise collide), and AST clause\n   guards reach the backend via a `Pratt.parse/1` passthrough (symmetric with the macro\n-  `parse_body`). **Remaining (the long tail of `Rian.Decl`):** `mod`/`struct`/`alias`/\n-  `protocol`/generics/doc-comments, and the portable `Enum`/`Map`/`String` stdlib\n-  breadth (ADR-0047).\n+  `parse_body`). It now also parses **`struct` records** (field access `p.x` +\n+  labeled construction `Point(x: 0, y: 0)`) and **`mod` nesting** (incl. `pub def`):\n+  a struct program and a `mod` compiled to its **own BEAM module** (`Calc.double`/\n+  `Calc.quad`, local cross-call) both run via `Rian.Beam.load_ir`/`load_program_ir`.\n+  **Remaining (the last of the `Rian.Decl` tail):** string/char clause patterns,\n+  `alias`/`const`/`protocol`/`impl`/generics/doc-comments, block (`do … end`) bodies,\n+  and the portable `Enum`/`Map`/`String` stdlib breadth (ADR-0047).\n - **Stage 3 — bootstrap fixed point** (future; **determinism prerequisite verified**):\n   the whole compiler in Rian; compile its source with the Elixir host → v1, compile\n   with v1 → v2, assert **v1 == v2** (bit-identical `.beam`). The canonical terminus.","raw_url":"https://github.com/wstein/rian-lab/raw/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/SELFHOST.md","sha":"56a0e5dffa5229f6e25b74a09ccaa9b253d29686","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf","deletions":1,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -42,7 +42,7 @@ gating library work and blocks Stage 2 below.\n |---|---|---|---|\n | **0 — Equivalence** | each ported stage matches the reference on a corpus | fixpoint diff (lexer vs `tokenize/1`; parser vs `Pratt.parse`) | **done** (lexer slices 1-5; parser slice) |\n | **1 — Self-application** | a ported stage processes **real toolchain source** (incl. the self-hosting sources themselves) and agrees with the reference | run the Rian lexer over `selfhost_parse.rian` / preludes, diff vs `Rian.Lexer.tokenize/1` | **done for the lexer** (`test/rian/selfhost_fixedpoint_test.exs`) |\n-| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **function language done** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums and `def` functions — multi-clause with **clause patterns** (var/lit/ctor/wildcard + **cons/list** `[]`/`[h\\|t]`), **capabilities** (`val`/`iso`/`ref`/`tag`), **parametric types** (`Vec(T)`), **list construction**, and **`when` guards** — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (cons-recursive `rev([1,2,3]) -> [3,2,1]`, guarded `clamp`; `decl_fixpoint_test.exs`). **Remaining:** `mod`/`struct`/`alias`/`protocol`/generics/doc-comments and the portable stdlib breadth |\n+| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **near-complete** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums, **`struct` records**, **`mod` nesting** (incl. `pub def`), and `def` functions — multi-clause with **clause patterns** (var/lit/ctor/wildcard + **cons/list**), **capabilities**, **parametric types** (`Vec(T)`), **list construction**, **`.field`/labeled construction**, and **`when` guards** — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (cons-recursive `rev`, a `struct` program, a `mod` compiled to its own BEAM module; `decl_fixpoint_test.exs`). **Remaining:** string/char clause patterns, `alias`/`protocol`/generics/doc-comments, and the portable stdlib breadth |\n | **3 — Bootstrap fixed point** | the **whole compiler**, written in Rian, compiles its own source; doing so twice is stable | compile the Rian compiler source with the Elixir-hosted compiler → v1; compile the *same source* with v1 → v2; assert **v1 == v2** (bit-identical `.beam`) | **future** (the canonical terminus); its **determinism prerequisite is verified** — `Rian.Beam` emits byte-identical bytecode on recompilation (`selfhost_fixedpoint_test.exs`) |\n \n **\"Real self-hosting\" = Stage 2** (the front-end genuinely self-hosts, reusing a trusted backend);","raw_url":"https://github.com/wstein/rian-lab/raw/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"517dc058b1cc53ec9d2321b2e58c52ada7cc1168","status":"modified"},{"additions":78,"blob_url":"https://github.com/wstein/rian-lab/blob/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/examples%2Frian%2Fselfhost_decl.rian","changes":99,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_decl.rian?ref=ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf","deletions":21,"filename":"examples/rian/selfhost_decl.rian","patch":"@@ -9,15 +9,18 @@\n #\n # Forms covered:\n #   type Name := V1 | V2(T, …)                         sum declarations\n+#   struct Name(field Type, …)                         product (record) types\n+#   mod Name do … end                                  module nesting (incl. `pub def`)\n #   def name(p [cap] Type, …) Ret := expr              single typed clause\n-#   def name(p [cap] Type, …) Ret                      bodiless signature\n-#   def name(pat, …) := expr                           pattern clause (multi-clause)\n-#   patterns: _  ·  var  ·  Int literal  ·  Ctor(pat, …)  ·  nullary Ctor (nested)\n-#   capabilities: val / iso / ref / tag (default val)\n+#   def name(pat, …) [when g] := expr                  pattern clause (multi-clause)\n+#   patterns: _ · var · Int literal · Ctor(pat, …) · cons/list [] / [h | t] (nested)\n+#   capabilities: val / iso / ref / tag (default val); parametric types `Vec(T)`\n+#   exprs: binops (precedence climbing) · prefix - / not · calls · `.field` access\n+#          · labeled construction `Name(f: v)` · list construction `[x | acc]`\n #\n-# Out of slice (the long tail of Rian.Decl): `when` guards, parametric param types\n-# (`Vec(T)`), cons/list/string/char patterns, negative-literal patterns, block\n-# bodies, and `mod`/`struct`/`alias`/`const`/`protocol`/`impl`/`forall`/doc-comments.\n+# Out of slice (the remaining long tail of Rian.Decl): string/char clause patterns,\n+# negative-literal patterns, `alias`/`const`/`protocol`/`impl`/`forall` generics,\n+# doc-comments, and block (`do … end`) function bodies.\n # ---------------------------------------------------------------------------\n \n mod SelfhostDecl do\n@@ -31,6 +34,7 @@ mod SelfhostDecl do\n   type Expr :=\n       Num(String) | Id(String) | Bin(String, Expr, Expr)\n     | Unary(String, Expr) | Call(Expr, Vec(Expr))\n+    | Dot(Expr, String) | Label(String, Expr)\n     | NilE | ConsE(Expr, Expr) | EErr\n \n   # clause PATTERNS — Wild/Var/Lit/Ctor lower straight to Rian.Decl's tuples\n@@ -43,12 +47,16 @@ mod SelfhostDecl do\n   type Variant := V(String, Vec(String))\n   type Param := Par(String, String, String)\n   type PType := PT(String, Vec(Tok))\n+  # `pub?` (a Bool) is carried on the function head (DFunc/DSig) so `pub def`\n+  # inside a `mod` projects to `Func.pub? = true` (exported on the BEAM).\n   type Decl :=\n       DType(String, Vec(Variant))\n-    | DFunc(String, Vec(Param), String, Expr)\n-    | DSig(String, Vec(Param), String)\n+    | DStruct(String, Vec(Param))\n+    | DFunc(Bool, String, Vec(Param), String, Expr)\n+    | DSig(Bool, String, Vec(Param), String)\n     | DClause(String, Vec(Pat), Expr)\n     | DClauseG(String, Vec(Pat), Expr, Expr)\n+    | DMod(String, Vec(Decl))\n     | DErr\n \n   # result pairs (a value + the unconsumed tokens)\n@@ -62,6 +70,7 @@ mod SelfhostDecl do\n   type Paren := Paren(Vec(Tok), Vec(Tok))\n   type PatRes := PP(Pat, Vec(Tok))\n   type PatList := PL(Vec(Pat), Vec(Tok))\n+  type ModBody := MB(Vec(Tok), Vec(Tok))\n \n   # ── program: declarations separated by significant newlines ──────────────\n   pub def parse_program(ts Vec(Tok)) Vec(Decl)\n@@ -73,12 +82,52 @@ mod SelfhostDecl do\n     end\n   end\n   pub def parse_program([TKw(\"def\") | rest])\n-    case parse_def(rest) do\n+    case parse_def(false, rest) do\n       DR(d, rest2) -> [d | parse_program(rest2)]\n     end\n   end\n+  pub def parse_program([TKw(\"pub\"), TKw(\"def\") | rest])\n+    case parse_def(true, rest) do\n+      DR(d, rest2) -> [d | parse_program(rest2)]\n+    end\n+  end\n+  pub def parse_program([TKw(\"struct\") | rest])\n+    case parse_struct(rest) do\n+      DR(d, rest2) -> [d | parse_program(rest2)]\n+    end\n+  end\n+  pub def parse_program([TKw(\"mod\"), TId(name), TKw(\"do\") | rest])\n+    case take_mod_body(rest, []) do\n+      MB(inner, rest2) -> [DMod(name, parse_program(inner)) | parse_program(rest2)]\n+    end\n+  end\n   pub def parse_program([_ | rest]) := parse_program(rest)\n \n+  # struct Name(field Type, …) — a product type; fields reuse the param grammar.\n+  def parse_struct(ts Vec(Tok)) DeclRes\n+  def parse_struct([TId(name), TLP | rest])\n+    case take_parens(rest, 0, []) do\n+      Paren(inside, after) -> DR(DStruct(name, parse_params(inside)), after)\n+    end\n+  end\n+  def parse_struct(ts) := DR(DErr, ts)\n+\n+  # collect a `mod` body up to its matching `end` (a block `end` from a nested\n+  # `if`/`case` is balanced by its `do`); returns (inner-tokens, rest).\n+  def take_mod_body(ts Vec(Tok), acc Vec(Tok)) ModBody\n+  def take_mod_body([TKw(\"do\") | rest], acc) := take_mod_body_do(rest, [TKw(\"do\") | acc], 1)\n+  def take_mod_body([TKw(\"end\") | rest], acc) := MB(rev_tok(acc, []), rest)\n+  def take_mod_body([t | rest], acc) := take_mod_body(rest, [t | acc])\n+  def take_mod_body([], acc) := MB(rev_tok(acc, []), [])\n+\n+  # inside a nested do-block: track depth so the block's `end` doesn't close the mod.\n+  def take_mod_body_do(ts Vec(Tok), acc Vec(Tok), depth Int64) ModBody\n+  def take_mod_body_do([TKw(\"do\") | rest], acc, depth) := take_mod_body_do(rest, [TKw(\"do\") | acc], depth + 1)\n+  def take_mod_body_do([TKw(\"end\") | rest], acc, 1) := take_mod_body(rest, [TKw(\"end\") | acc])\n+  def take_mod_body_do([TKw(\"end\") | rest], acc, depth) := take_mod_body_do(rest, [TKw(\"end\") | acc], depth - 1)\n+  def take_mod_body_do([t | rest], acc, depth) := take_mod_body_do(rest, [t | acc], depth)\n+  def take_mod_body_do([], acc, depth) := MB(rev_tok(acc, []), [])\n+\n   # ── type Name := variant ('|' variant)* ──────────────────────────────────\n   def parse_type(ts Vec(Tok)) DeclRes\n   def parse_type([TId(name), TOp(\":=\") | rest])\n@@ -119,13 +168,13 @@ mod SelfhostDecl do\n   def parse_typelist(ts) := TL([], ts)\n \n   # ── def: split the parenthesized head, then classify by what follows ──────\n-  def parse_def(ts Vec(Tok)) DeclRes\n-  def parse_def([TId(name), TLP | rest])\n+  def parse_def(pb Bool, ts Vec(Tok)) DeclRes\n+  def parse_def(pb, [TId(name), TLP | rest])\n     case take_parens(rest, 0, []) do\n-      Paren(inside, after) -> classify_def(name, inside, after)\n+      Paren(inside, after) -> classify_def(pb, name, inside, after)\n     end\n   end\n-  def parse_def(ts) := DR(DErr, ts)\n+  def parse_def(pb, ts) := DR(DErr, ts)\n \n   # collect tokens up to the matching `)` (depth 0), returning (inside, rest).\n   def take_parens(ts Vec(Tok), depth Int64, acc Vec(Tok)) Paren\n@@ -137,8 +186,8 @@ mod SelfhostDecl do\n \n   # `(pats) when g := body` / `(pats) := body` -> a pattern clause; `(params) Ret\n   # := body` -> a typed single clause; `(params) Ret` -> a bodiless signature.\n-  def classify_def(name String, inside Vec(Tok), after Vec(Tok)) DeclRes\n-  def classify_def(name, inside, [TKw(\"when\") | rest])\n+  def classify_def(pb Bool, name String, inside Vec(Tok), after Vec(Tok)) DeclRes\n+  def classify_def(pb, name, inside, [TKw(\"when\") | rest])\n     case parse_bp(0, rest) do\n       P(g, [TOp(\":=\") | body]) ->\n         case parse_bp(0, body) do\n@@ -147,21 +196,21 @@ mod SelfhostDecl do\n       P(_, other) -> DR(DErr, other)\n     end\n   end\n-  def classify_def(name, inside, [TOp(\":=\") | body])\n+  def classify_def(pb, name, inside, [TOp(\":=\") | body])\n     case parse_bp(0, body) do\n       P(b, rest) -> DR(DClause(name, parse_pats(inside), b), rest)\n     end\n   end\n-  def classify_def(name, inside, [TId(rb) | rb_rest])\n+  def classify_def(pb, name, inside, [TId(rb) | rb_rest])\n     case parse_ptype([TId(rb) | rb_rest]) do\n       PT(ret, [TOp(\":=\") | body]) ->\n         case parse_bp(0, body) do\n-          P(b, rest) -> DR(DFunc(name, parse_params(inside), ret, b), rest)\n+          P(b, rest) -> DR(DFunc(pb, name, parse_params(inside), ret, b), rest)\n         end\n-      PT(ret, rest) -> DR(DSig(name, parse_params(inside), ret), rest)\n+      PT(ret, rest) -> DR(DSig(pb, name, parse_params(inside), ret), rest)\n     end\n   end\n-  def classify_def(name, inside, after) := DR(DErr, after)\n+  def classify_def(pb, name, inside, after) := DR(DErr, after)\n \n   # ── parameters: `name [cap] Type` (cap in val/iso/ref/tag, default val) ────\n   def parse_params(ts Vec(Tok)) Vec(Param)\n@@ -336,16 +385,24 @@ mod SelfhostDecl do\n     end\n   end\n \n+  # postfix := '.' field (field access) | '(' args ')' (call), repeated\n   def parse_postfix(node Expr, ts Vec(Tok)) Parse\n+  def parse_postfix(node, [TOp(\".\"), TId(field) | rest]) := parse_postfix(Dot(node, field), rest)\n   def parse_postfix(node, [TLP | rest])\n     case parse_args(rest) do\n       AP(args, rest2) -> parse_postfix(Call(node, args), rest2)\n     end\n   end\n   def parse_postfix(node, ts) := P(node, ts)\n \n+  # args := ε | arg (',' arg)*  where arg := Id ':' expr (labeled) | expr\n   def parse_args(ts Vec(Tok)) Args\n   def parse_args([TRP | rest]) := AP([], rest)\n+  def parse_args([TId(name), TOp(\":\") | rest])\n+    case parse_bp(0, rest) do\n+      P(v, after) -> finish_arg(Label(name, v), after)\n+    end\n+  end\n   def parse_args(ts)\n     case parse_bp(0, ts) do\n       P(a, rest) -> finish_arg(a, rest)","raw_url":"https://github.com/wstein/rian-lab/raw/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/examples%2Frian%2Fselfhost_decl.rian","sha":"2278e90f2cacbf82f9843a085318df5504c06383","status":"modified"},{"additions":34,"blob_url":"https://github.com/wstein/rian-lab/blob/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/lib%2Frian%2Fbeam.ex","changes":34,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fbeam.ex?ref=ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf","deletions":0,"filename":"lib/rian/beam.ex","patch":"@@ -182,6 +182,40 @@ defmodule Rian.Beam do\n     {:ok, module}\n   end\n \n+  @doc \"\"\"\n+  Compile a pre-built **multi-module** program IR (each `%IR.Mod{}` -> its own\n+  `Elixir.<Mod>` BEAM module) — the Stage-2 seam for a Rian front-end that parses\n+  `mod` declarations (see `compile_program/1`, IR form).\n+  \"\"\"\n+  def compile_program_ir(prog) do\n+    top = Map.get(prog, :ranges, [])\n+\n+    prog\n+    |> Map.get(:mods, [])\n+    |> Enum.map(fn m ->\n+      {:ok, atom, bin} =\n+        beam_for(\n+          :\"Elixir.#{m.name}\",\n+          m.funcs,\n+          top ++ Map.get(m, :ranges, []),\n+          Map.get(m, :types, []),\n+          Map.get(m, :structs, [])\n+        )\n+\n+      {atom, bin}\n+    end)\n+  end\n+\n+  @doc \"Compile and load a multi-module program IR (see `compile_program_ir/1`).\"\n+  def load_program_ir(prog) do\n+    prog\n+    |> compile_program_ir()\n+    |> Enum.map(fn {atom, bin} ->\n+      {:module, ^atom} = :code.load_binary(atom, ~c\"#{atom}.beam\", bin)\n+      atom\n+    end)\n+  end\n+\n   # build one module's `.beam` from its function list. `ranges` (a list of\n   # `%IR.Range{}`) lets `Name.of(n)` construction desugar (ADR-0036). `types` and\n   # `structs` let `-spec` attributes expand sum/struct types (Stage 0.5).","raw_url":"https://github.com/wstein/rian-lab/raw/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/lib%2Frian%2Fbeam.ex","sha":"14a8299d1dcaf2824853987637304d160fb928a9","status":"modified"},{"additions":90,"blob_url":"https://github.com/wstein/rian-lab/blob/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/test%2Frian%2Fdecl_fixpoint_test.exs","changes":106,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_fixpoint_test.exs?ref=ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf","deletions":16,"filename":"test/rian/decl_fixpoint_test.exs","patch":"@@ -3,7 +3,7 @@ defmodule Rian.DeclFixpointTest do\n   use ExUnit.Case, async: false\n \n   alias Rian.{Beam, Decl, Lexer, Pratt}\n-  alias Rian.IR.{Clause, Field, Func, Param, Type, Variant}\n+  alias Rian.IR.{Clause, Field, Func, Mod, Param, Struct, Type, Variant}\n \n   # Stage 2 of the bootstrap ladder (ADR-0063) for **declarations**: the\n   # Rian-written front-end (examples/rian/selfhost_decl.rian) parses `type` sums\n@@ -13,11 +13,12 @@ defmodule Rian.DeclFixpointTest do\n   # Stage-2 thesis: a Rian front-end producing IR the existing backend consumes,\n   # with no Elixir parse in the loop.\n   #\n-  # Covers: `type` sums; `def` functions (single + multi-clause); clause patterns\n-  # (var/lit/ctor/wildcard/cons-list); capabilities (val/iso/ref/tag); parametric\n-  # param/return types (`Vec(T)`); list construction; and `when` guards. Remaining\n-  # long tail of `Rian.Decl`: `mod`/`struct`/`alias`/`protocol`/generics/doc-comments\n-  # and the portable stdlib breadth — see ADR-0063.\n+  # Covers: `type` sums; `struct` records; `mod` nesting (incl. `pub def`); `def`\n+  # functions (single + multi-clause); clause patterns (var/lit/ctor/wildcard/\n+  # cons-list); capabilities (val/iso/ref/tag); parametric types (`Vec(T)`); list\n+  # construction; `.field` access + labeled construction; and `when` guards.\n+  # Remaining long tail of `Rian.Decl`: string/char clause patterns, `alias`/\n+  # `protocol`/generics/doc-comments, and the portable stdlib breadth — see ADR-0063.\n \n   setup_all do\n     {:ok, fe} = Beam.load(File.read!(\"examples/rian/selfhost_decl.rian\"), :rian_decl_frontend)\n@@ -90,6 +91,8 @@ defmodule Rian.DeclFixpointTest do\n   defp ce({:bin, op, l, r}), do: {:bin, op, ce(l), ce(r)}\n   defp ce({:unary, op, x}), do: {:unary, op, ce(x)}\n   defp ce({:call, f, args}), do: {:call, ce(f), Enum.map(args, &ce/1)}\n+  defp ce({:dot, e, field}), do: {:dot, ce(e), field}\n+  defp ce({:label, name, v}), do: {:label, name, ce(v)}\n   defp ce(leaf), do: leaf\n \n   defp flat_e(:nil_e), do: {[], nil}\n@@ -102,13 +105,13 @@ defmodule Rian.DeclFixpointTest do\n \n   defp flat_e({:cons_e, h, t}), do: {[h], {:tail, ce(t)}}\n \n-  defp func(name, params, ret, clauses) do\n+  defp func(name, params, ret, clauses, pub) do\n     %Func{\n       name: name,\n       params: Enum.map(params, &param/1),\n       ret: ret,\n       clauses: clauses,\n-      pub?: false,\n+      pub?: pub,\n       tvars: [],\n       bounds: %{},\n       doc: nil,\n@@ -118,23 +121,47 @@ defmodule Rian.DeclFixpointTest do\n     }\n   end\n \n+  defp to_struct({:d_struct, name, params}),\n+    do: %Struct{\n+      name: name,\n+      fields: Enum.map(params, fn {:par, n, _, t} -> %Field{label: n, type: t} end)\n+    }\n+\n+  defp to_mod({:d_mod, name, decls}) do\n+    ir = group(decls)\n+\n+    %Mod{\n+      name: name,\n+      types: Enum.filter(ir, &match?(%Type{}, &1)),\n+      structs: Enum.filter(ir, &match?(%Struct{}, &1)),\n+      funcs: Enum.filter(ir, &match?(%Func{}, &1)),\n+      uses: [],\n+      ranges: [],\n+      consts: [],\n+      doc: nil,\n+      targets: nil\n+    }\n+  end\n+\n   # group a flat decl list into IR: a `d_sig` absorbs the following `d_clause`s of\n   # the same name (multi-clause); a `d_func` is a single typed clause.\n   defp group([]), do: []\n   defp group([{:d_type, _, _} = t | rest]), do: [to_type(t) | group(rest)]\n+  defp group([{:d_struct, _, _} = s | rest]), do: [to_struct(s) | group(rest)]\n+  defp group([{:d_mod, _, _} = m | rest]), do: [to_mod(m) | group(rest)]\n \n-  defp group([{:d_func, name, params, ret, body} | rest]) do\n+  defp group([{:d_func, pub, name, params, ret, body} | rest]) do\n     pats = Enum.map(params, fn {:par, n, _, _} -> {:var, n} end)\n-    [func(name, params, ret, [clause(pats, body)]) | group(rest)]\n+    [func(name, params, ret, [clause(pats, body)], pub) | group(rest)]\n   end\n \n-  defp group([{:d_sig, name, params, ret} | rest]) do\n+  defp group([{:d_sig, pub, name, params, ret} | rest]) do\n     {cls, rest2} =\n       Enum.split_while(rest, fn d ->\n         match?({:d_clause, ^name, _, _}, d) or match?({:d_clause_g, ^name, _, _, _}, d)\n       end)\n \n-    [func(name, params, ret, Enum.map(cls, &to_clause/1)) | group(rest2)]\n+    [func(name, params, ret, Enum.map(cls, &to_clause/1), pub) | group(rest2)]\n   end\n \n   defp to_clause({:d_clause, _, pats, body}), do: clause(pats, body)\n@@ -152,9 +179,9 @@ defmodule Rian.DeclFixpointTest do\n     %{\n       types: Enum.filter(ir, &match?(%Type{}, &1)),\n       funcs: Enum.filter(ir, &match?(%Func{}, &1)),\n-      structs: [],\n-      ranges: [],\n-      mods: []\n+      structs: Enum.filter(ir, &match?(%Struct{}, &1)),\n+      mods: Enum.filter(ir, &match?(%Mod{}, &1)),\n+      ranges: []\n     }\n   end\n \n@@ -201,19 +228,33 @@ defmodule Rian.DeclFixpointTest do\n     # `when` guards\n     \"def clamp(n Int64) Int64\\n\" <>\n       \"def clamp(n) when n < 0 := 0\\n\" <>\n-      \"def clamp(n) := n\"\n+      \"def clamp(n) := n\",\n+    # struct declarations + field access + labeled construction\n+    \"struct Point(x Int64, y Int64)\",\n+    \"struct Point(x Int64, y Int64)\\n\" <>\n+      \"def mag(p Point) Int64 := p.x * p.x + p.y * p.y\\n\" <>\n+      \"def origin() Point := Point(x: 0, y: 0)\",\n+    # mod with a pub def\n+    \"mod Calc do\\n  pub def double(n Int64) Int64 := n * 2\\nend\",\n+    \"mod M do\\n  type T := A | B\\n  pub def f(n Int64) Int64 := n + 1\\nend\"\n   ]\n \n+  defp norm_mod(m), do: %{m | funcs: Enum.map(m.funcs, &norm_func/1)}\n+\n   describe \"Stage 2 declarations — the Rian front-end builds the same IR as Rian.Decl.parse\" do\n     test \"type sums and def functions project to IR equal to Rian.Decl.parse\", %{frontend: fe} do\n       for src <- @corpus do\n         prog = front_decls(fe, src) |> to_prog()\n         ref = Decl.parse(src)\n \n         assert prog.types == ref.types, \"types diverged from Rian.Decl.parse on #{inspect(src)}\"\n+        assert prog.structs == ref.structs, \"structs diverged on #{inspect(src)}\"\n \n         assert Enum.map(prog.funcs, &norm_func/1) == Enum.map(ref.funcs, &norm_func/1),\n                \"funcs diverged from Rian.Decl.parse on #{inspect(src)}\"\n+\n+        assert Enum.map(prog.mods, &norm_mod/1) == Enum.map(ref.mods, &norm_mod/1),\n+               \"mods diverged from Rian.Decl.parse on #{inspect(src)}\"\n       end\n     end\n   end\n@@ -307,5 +348,38 @@ defmodule Rian.DeclFixpointTest do\n       assert mod.clamp(-5) == 0\n       assert mod.clamp(7) == 7\n     end\n+\n+    test \"a STRUCT program (construction + field access) runs\", %{frontend: fe} do\n+      src = \"\"\"\n+      struct Point(x Int64, y Int64)\n+      def mag(p Point) Int64 := p.x * p.x + p.y * p.y\n+      def origin() Point := Point(x: 0, y: 0)\n+      def shift(p Point) Int64 := mag(p) + p.x\n+      \"\"\"\n+\n+      prog = front_decls(fe, src) |> to_prog()\n+      {:ok, mod} = Beam.load_ir(prog, :RianStage2Struct)\n+      # a struct value is a tagged map; construct one and read it back\n+      pt = mod.origin()\n+      assert mod.mag(%{__struct__: :point, x: 3, y: 4}) == 25\n+      assert mod.shift(%{__struct__: :point, x: 3, y: 4}) == 28\n+      assert pt.x == 0 and pt.y == 0\n+    end\n+\n+    test \"a MOD declaration compiles to its own BEAM module and runs\", %{frontend: fe} do\n+      src = \"\"\"\n+      mod Calc do\n+        pub def double(n Int64) Int64 := n * 2\n+        pub def quad(n Int64) Int64 := double(double(n))\n+      end\n+      \"\"\"\n+\n+      prog = front_decls(fe, src) |> to_prog()\n+      [mod] = Beam.load_program_ir(prog)\n+      assert mod == Calc\n+      assert Calc.double(21) == 42\n+      # quad calls double — a local call within the compiled mod\n+      assert Calc.quad(5) == 20\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/ed3f15a7d2ba09fccc354a1b69f2911fd69b89bf/test%2Frian%2Fdecl_fixpoint_test.exs","sha":"633b2eb128ad23fab4b775c1804c976551a485a6","status":"modified"}]}