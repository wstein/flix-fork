{"files":[{"additions":70,"blob_url":"https://github.com/wstein/cx-cli/blob/130800c4124d2daf42998d843a098f31bede50f4/src%2Fcli%2Fcommands%2Fconfig.ts","changes":76,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/src%2Fcli%2Fcommands%2Fconfig.ts?ref=130800c4124d2daf42998d843a098f31bede50f4","deletions":6,"filename":"src/cli/commands/config.ts","patch":"@@ -1,3 +1,5 @@\n+import fs from \"node:fs/promises\";\n+import { parse as parseToml } from \"smol-toml\";\n import {\n   DEFAULT_BEHAVIOR_VALUES,\n   DEFAULT_CONFIG_VALUES,\n@@ -7,7 +9,6 @@ import {\n   getCLIOverrides,\n   readEnvOverrides,\n } from \"../../config/env.js\";\n-import { loadCxConfig } from \"../../config/load.js\";\n import type {\n   CxConfigDuplicateEntryMode,\n   CxDedupMode,\n@@ -40,6 +41,70 @@ interface EffectiveSettings {\n   };\n }\n \n+const VALID_DEDUP_MODES = new Set<CxDedupMode>([\"fail\", \"warn\", \"first-wins\"]);\n+const VALID_REPOMIX_MISSING = new Set<CxRepomixMissingExtensionMode>([\n+  \"fail\",\n+  \"warn\",\n+]);\n+const VALID_CONFIG_DUPLICATE = new Set<CxConfigDuplicateEntryMode>([\n+  \"fail\",\n+  \"warn\",\n+  \"first-wins\",\n+]);\n+\n+function expectEnum<T extends string>(\n+  value: unknown,\n+  label: string,\n+  validValues: Set<T>,\n+): T {\n+  if (typeof value !== \"string\" || !validValues.has(value as T)) {\n+    throw new CxError(\n+      `${label} must be one of: ${[...validValues].join(\", \")}.`,\n+    );\n+  }\n+\n+  return value as T;\n+}\n+\n+async function getConfigFileBehaviorValues(configPath: string): Promise<{\n+  dedupMode?: CxDedupMode;\n+  repomixMissingExtension?: CxRepomixMissingExtensionMode;\n+  configDuplicateEntry?: CxConfigDuplicateEntryMode;\n+}> {\n+  const raw = await fs.readFile(configPath, \"utf8\");\n+  const parsed = parseToml(raw) as Record<string, unknown>;\n+  const result: {\n+    dedupMode?: CxDedupMode;\n+    repomixMissingExtension?: CxRepomixMissingExtensionMode;\n+    configDuplicateEntry?: CxConfigDuplicateEntryMode;\n+  } = {};\n+\n+  const dedup = parsed?.dedup as Record<string, unknown> | undefined;\n+  if (dedup?.mode !== undefined) {\n+    result.dedupMode = expectEnum(dedup.mode, \"dedup.mode\", VALID_DEDUP_MODES);\n+  }\n+\n+  const repomix = parsed?.repomix as Record<string, unknown> | undefined;\n+  if (repomix?.missing_extension !== undefined) {\n+    result.repomixMissingExtension = expectEnum(\n+      repomix.missing_extension,\n+      \"repomix.missing_extension\",\n+      VALID_REPOMIX_MISSING,\n+    );\n+  }\n+\n+  const configSection = parsed?.config as Record<string, unknown> | undefined;\n+  if (configSection?.duplicate_entry !== undefined) {\n+    result.configDuplicateEntry = expectEnum(\n+      configSection.duplicate_entry,\n+      \"config.duplicate_entry\",\n+      VALID_CONFIG_DUPLICATE,\n+    );\n+  }\n+\n+  return result;\n+}\n+\n /**\n  * Determine whether the env overrides came from CX_STRICT (convenience\n  * shorthand) rather than individual CX_* vars.\n@@ -121,11 +186,10 @@ export async function runConfigCommand(options: {\n \n   if (configExists) {\n     try {\n-      // Load the config with no overrides at all to isolate the raw file values.\n-      const loaded = await loadCxConfig(options.config, {}, {});\n-      dedupModeFromFile = loaded.dedup.mode;\n-      repomixMissingExtensionFromFile = loaded.behavior.repomixMissingExtension;\n-      configDuplicateEntryFromFile = loaded.behavior.configDuplicateEntry;\n+      const values = await getConfigFileBehaviorValues(options.config);\n+      dedupModeFromFile = values.dedupMode;\n+      repomixMissingExtensionFromFile = values.repomixMissingExtension;\n+      configDuplicateEntryFromFile = values.configDuplicateEntry;\n     } catch (error: unknown) {\n       if (options.json) {\n         process.stdout.write(","raw_url":"https://github.com/wstein/cx-cli/raw/130800c4124d2daf42998d843a098f31bede50f4/src%2Fcli%2Fcommands%2Fconfig.ts","sha":"f88771f4b0d721621463acb16d55215a31975a2f","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/cx-cli/blob/130800c4124d2daf42998d843a098f31bede50f4/src%2Fcli%2Fcompletion.ts","changes":6,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/src%2Fcli%2Fcompletion.ts?ref=130800c4124d2daf42998d843a098f31bede50f4","deletions":2,"filename":"src/cli/completion.ts","patch":"@@ -414,10 +414,12 @@ function generateFishCompletion(): string {\n \n   let globalOptions = \"\";\n   for (const opt of GLOBAL_OPTIONS) {\n-    const desc = opt.description.replace(/\"/g, '\\\\\"');\n+    const rawDesc = opt.description.replace(/\"/g, '\\\\\"');\n     const name = opt.name;\n     const short = opt.short ? ` -s ${opt.short}` : \"\";\n-    globalOptions += `complete -c cx -n \"not __fish_seen_subcommand_from ${commandList}\"${short} -l ${name} -d \"${desc}\"\\n`;\n+    const longForm = `--${name}`;\n+    const longDesc = `${longForm}${opt.short ? ` (-${opt.short})` : \"\"} ${rawDesc}`;\n+    globalOptions += `complete -c cx -n \"not __fish_seen_subcommand_from ${commandList}\"${short} -l ${name} -d \"${longDesc}\"\\n`;\n   }\n \n   let commandOptions = \"\";","raw_url":"https://github.com/wstein/cx-cli/raw/130800c4124d2daf42998d843a098f31bede50f4/src%2Fcli%2Fcompletion.ts","sha":"ff54d632e29794cc4e7e27afb4b5a5eec852a760","status":"modified"}]}