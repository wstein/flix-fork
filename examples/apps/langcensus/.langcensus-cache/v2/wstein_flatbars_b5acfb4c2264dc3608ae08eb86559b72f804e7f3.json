{"files":[{"additions":37,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Fcases.mjs","changes":39,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fconformance%2Fcases.mjs?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":2,"filename":"trussbars/conformance/cases.mjs","patch":"@@ -369,9 +369,12 @@ export const cases = [\n   },\n   {\n     // ADR-042 §8 follow-up: a hash-argument include WITHOUT a signature — the body's\n-    // `{{who}}` is a `lookup this \"who\"`, shadowed by the hash key (so AOT-typed from\n-    // the call value `name`, no phantom `who` context field).\n+    // `{{who}}` is a `lookup this \"who\"`, bound by the hash key `who=name` at the call site.\n+    // inference gap (docs/03 §Not-yet): the unsignatured hash binding is mis-read as a phantom\n+    // `who` context field, so use the data shape (which omits it) — same class as the capture\n+    // bindings below. (The committed report.json had not caught this — it was stale.)\n     id: \"include-hash-no-sig\",\n+    ctxFromData: true,\n     template: '{% inline \"g\" %}hi {{who}}!{% endinline %}{% include \"g\" who=name %}',\n     data: { name: \"Ada\" },\n   },\n@@ -567,4 +570,36 @@ export const cases = [\n     template: \"{% scope {a: {b: 1}} %}{{a.b}}{% endscope %}\",\n     data: {},\n   },\n+  // {% capture %} (ADR-25 / docs/18): render the body once into a forward-bound Safe value.\n+  // inference gap (docs/03 §Not-yet): the captured *binding* (`by`/`b`) is read as `{{ … }}`\n+  // output, so schema inference mis-classifies it as a context field instead of a `{% local %}`\n+  // shadow — use the data shape, which omits it (the binding is generated, not a ctx field).\n+  {\n+    id: \"capture-reuse\",\n+    ctxFromData: true,\n+    template:\n+      \"{% capture by %}{{author}}{% if vip %} ★{% endif %}{% endcapture %}<h>{{by}}</h><f>{{by}}</f>\",\n+    data: { author: \"Ann\", vip: true },\n+  },\n+  {\n+    id: \"capture-no-double-escape\",\n+    ctxFromData: true,\n+    template: \"{% capture b %}{{html}}{% endcapture %}{{b}}\",\n+    data: { html: \"<i>x</i>\" },\n+  },\n+  {\n+    id: \"capture-pipeable\",\n+    ctxFromData: true,\n+    template: \"{% capture b %}  hi  {% endcapture %}[{{b | trim}}]\",\n+    data: {},\n+  },\n+  {\n+    id: \"capture-in-loop\",\n+    // inference gap (docs/03 §Not-yet): the loop element type is unpinned when `{{this}}`\n+    // is used only inside the capture's hoisted `@cap` inline partial — use the data shape.\n+    ctxFromData: true,\n+    template:\n+      \"{% for xs %}{% capture c %}<{{this}}>{% endcapture %}{{c}}{{c}}{% endfor %}\",\n+    data: { xs: [\"a\", \"b\"] },\n+  },\n ];","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Fcases.mjs","sha":"fb8afa635ffcf87aa9421a10d086105673212b19","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Freport.json","changes":15,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fconformance%2Freport.json?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":10,"filename":"trussbars/conformance/report.json","patch":"@@ -1,19 +1,14 @@\n {\n   \"profile\": \"perf (ecma-float, default)\",\n-  \"total\": 87,\n+  \"total\": 91,\n   \"positive\": {\n-    \"count\": 75,\n-    \"byte_matched\": 75,\n+    \"count\": 80,\n+    \"byte_matched\": 80,\n     \"mismatched\": 0\n   },\n   \"excluded\": {\n-    \"count\": 1,\n-    \"cases\": [\n-      {\n-        \"id\": \"include-hash-no-sig\",\n-        \"reason\": \"unsupported: partial with a hash or a dynamic name\"\n-      }\n-    ]\n+    \"count\": 0,\n+    \"cases\": []\n   },\n   \"oracle_drift\": 0\n }","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Freport.json","sha":"c7a58b1b39d2e308c2915eb9e6beffc068e0c922","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Fsnapshots.json","changes":17,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fconformance%2Fsnapshots.json?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":12,"filename":"trussbars/conformance/snapshots.json","patch":"@@ -6,14 +6,6 @@\n   \"comparison\": \"many (5)\",\n   \"logic\": \"both\",\n   \"float\": \"ratio = 0.125\",\n-  \"even-guard\": \"even\",\n-  \"odd-guard\": \"odd\",\n-  \"even-call\": \"true/false\",\n-  \"divisible-by\": \"fizzbuzz\",\n-  \"concat-tilde\": \"Hi Ada\",\n-  \"concat-chain\": \"xyz\",\n-  \"concat-literal\": \"Ada!\",\n-  \"concat-pipe\": \"Bob?\",\n   \"uppercase\": \"ANN\",\n   \"pipe-chain\": \"Bob!\",\n   \"if-else\": \"off\",\n@@ -60,9 +52,6 @@\n   \"partial-in-each\": \"<li>a</li><li>b</li>\",\n   \"partial-context\": \"[Intro]\",\n   \"include-hash-no-sig\": \"hi Ada!\",\n-  \"inline-sig-default-omitted\": \"<h3>Hi</h3>\",\n-  \"inline-sig-default-provided\": \"<h3>Hi</h3><b>new</b>\",\n-  \"inline-sig-reused\": \"[a/-][x/y]\",\n   \"inherit-override-default\": \"<h>Site</h><b>Hi Ada</b>\",\n   \"inherit-super\": \"[Base]\",\n   \"inline-comment\": \"abc\",\n@@ -85,5 +74,9 @@\n   \"dict-call-form\": \"1\",\n   \"dict-local-literal\": \"dark/12\",\n   \"dict-path-value\": \"hi Zed\",\n-  \"dict-nested\": \"1\"\n+  \"dict-nested\": \"1\",\n+  \"capture-reuse\": \"<h>Ann ★</h><f>Ann ★</f>\",\n+  \"capture-no-double-escape\": \"&lt;i&gt;x&lt;/i&gt;\",\n+  \"capture-pipeable\": \"[hi]\",\n+  \"capture-in-loop\": \"<a><a><b><b>\"\n }","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fconformance%2Fsnapshots.json","sha":"42419fac52330d2f03bea4481a71a538a05ea4b3","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-interp%2Fsrc%2Flib.rs","changes":37,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-interp%2Fsrc%2Flib.rs?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":0,"filename":"trussbars/crates/trussbars-interp/src/lib.rs","patch":"@@ -1447,6 +1447,43 @@ mod tests {\n         assert!(render(r#\"{{ render \"nav\" }}\"#, Value::Null).is_err());\n     }\n \n+    /// `{% capture %}` (ADR-25 stage 3) — renders its body once into a `Safe` value bound\n+    /// forward, reused without re-rendering and emitted verbatim (no double-escape). Desugars\n+    /// to an `@cap$` inline + a `{% local %}` over the sibling tail.\n+    #[test]\n+    fn capture_block() {\n+        // build-once-use-many; the body escapes its interpolation, the Safe reuse does not.\n+        let d = obj(&[(\"author\", s(\"<b>A</b>\")), (\"vip\", Value::Bool(true))]);\n+        assert_eq!(\n+            render(\n+                r#\"{% capture by %}{{author}}{% if vip %} ★{% endif %}{% endcapture %}<h>{{by}}</h><f>{{by}}</f>\"#,\n+                d\n+            )\n+            .unwrap(),\n+            \"<h>&lt;b&gt;A&lt;/b&gt; ★</h><f>&lt;b&gt;A&lt;/b&gt; ★</f>\"\n+        );\n+        // forward scope: the binding is live in the sibling tail but dies at the block close.\n+        let d2 = obj(&[(\"x\", s(\"hi\"))]);\n+        assert_eq!(\n+            render(\n+                r#\"{% if x %}{% capture c %}[{{x}}]{% endcapture %}{{c}}{% endif %}{{c}}\"#,\n+                d2\n+            )\n+            .unwrap(),\n+            \"[hi]\"\n+        );\n+        // capture inside a loop sees the current element (the body renders in-scope).\n+        let d3 = obj(&[(\"xs\", arr(&[s(\"a\"), s(\"b\")]))]);\n+        assert_eq!(\n+            render(\n+                r#\"{% for xs %}{% capture c %}<{{this}}>{% endcapture %}{{c}}{{c}}{% endfor %}\"#,\n+                d3\n+            )\n+            .unwrap(),\n+            \"<a><a><b><b>\"\n+        );\n+    }\n+\n     /// `Value::Safe` (docs/25) — a pre-escaped value: `{{ x }}` emits it verbatim (no\n     /// double-escape, unlike `Str`), it's truthy iff non-empty, and string ops treat it\n     /// like `Str` (coerced via `raw_text`, or the `Safe`-aware direct-match arms).","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-interp%2Fsrc%2Flib.rs","sha":"8d159f962623a98ba8f1b9bf495d301fd181d38f","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","changes":16,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":0,"filename":"trussbars/crates/trussbars-template/src/emit.rs","patch":"@@ -990,6 +990,22 @@ fn emit_app(env: &Env, name: &str, args: &[Expr]) -> Result<String, String> {\n         (\"some\", _) => coll_filter(env, \"any\", false, args),\n         (\"every\", _) => coll_filter(env, \"all\", false, args),\n         (\"find\", _) => coll_find(env, args),\n+        // ADR-25: render a hoisted `{% capture %}`/`{% apply %}` body (an `@…` inline partial)\n+        // to a `Safe` value in the CURRENT scope — loop vars / locals preserved. Emits a block\n+        // that shadows the output buffer (`out`) with a fresh `String`, renders the body into\n+        // it, and wraps it `Safe`. Literal `@`-name only (internal; the names-static boundary).\n+        (\"render\", [Expr::Lit(Value::Str(name))]) if name.starts_with('@') => {\n+            let Some(def) = env.partials.get(name) else {\n+                return Err(format!(\"internal: unknown captured fragment '{name}'\"));\n+            };\n+            let def_src = Rc::clone(&def.src);\n+            let def_body = def.body.clone();\n+            let mut body_code = String::new();\n+            emit_nodes(env, &def_src, &def_body, &mut body_code)?;\n+            Ok(format!(\n+                \"trussbars_std::safe(&{{ let mut out = String::new();\\n{body_code}out }})\"\n+            ))\n+        }\n         _ => {\n             if let Some(call) = emit_helper(env, name, args) {\n                 call","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","sha":"cbdd029ecb71e6ec2290747d2aec4033c6c719b0","status":"modified"},{"additions":49,"blob_url":"https://github.com/wstein/flatbars/blob/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fparse.rs","changes":50,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fparse.rs?ref=b5acfb4c2264dc3608ae08eb86559b72f804e7f3","deletions":1,"filename":"trussbars/crates/trussbars-template/src/parse.rs","patch":"@@ -187,7 +187,16 @@ impl Blocks<'_> {\n                     interior,\n                     span,\n                 } => {\n-                    let node = self.open_block(*span, interior.of(self.src), scope)?;\n+                    let interior_str = interior.of(self.src);\n+                    // `{% capture NAME %}` (ADR-25 / docs/18) is a *tail-wrapping* desugar: the\n+                    // rest of this sibling list becomes the body of a forward `{% local %}`, so\n+                    // NAME binds forward to the enclosing block's close. Handled here, not in\n+                    // `open_block`, because it emits two nodes and consumes the sibling tail.\n+                    if head_word(interior_str) == \"capture\" {\n+                        let stop = self.capture_desugar(*span, interior_str, scope, &mut nodes)?;\n+                        return Ok((nodes, stop));\n+                    }\n+                    let node = self.open_block(*span, interior_str, scope)?;\n                     nodes.push(node);\n                 }\n                 Lexeme::RawBlock { body, span, .. } => {\n@@ -494,6 +503,45 @@ impl Blocks<'_> {\n         })\n     }\n \n+    /// `{% capture NAME %}BODY{% endcapture %}TAIL` → an inline def holding BODY + a forward\n+    /// `{% local NAME = (render \"@cap$k\") %}TAIL{% endlocal %}` (ADR-25 / docs/18, mirroring the\n+    /// oracle's `Kernel.SetSugar.liftSet`). The sibling tail becomes the local's body (forward\n+    /// scope); `(render \"@cap$k\")` renders BODY to a `Safe` value in the surrounding context.\n+    /// Pushes the two nodes onto `nodes` and returns the tail's `Stop` (the enclosing close).\n+    fn capture_desugar(\n+        &mut self,\n+        span: crate::span::Span,\n+        interior: &str,\n+        scope: &Scope,\n+        nodes: &mut Vec<Node>,\n+    ) -> Result<Stop, ParseError> {\n+        let rest = interior[head_word(interior).len()..].trim();\n+        let (name, _) = read_ident(rest).ok_or_else(|| ParseError {\n+            message: \"{% capture %} needs a binding name\".into(),\n+            at: span.start,\n+        })?;\n+        let name = name.to_string();\n+        self.pos += 1; // consume the `{% capture %}` open tag\n+        let (body, stop) = self.parse_until(scope)?;\n+        expect_close(&stop, \"capture\", span.start)?;\n+        // A hygienic, collision-free synthetic partial name (the source offset) — `@`-prefixed\n+        // so it is unreachable from author syntax and `render` stays internal (ADR-25 §4).\n+        let cap = format!(\"@cap${}\", span.start);\n+        nodes.push(Node::Inline {\n+            span,\n+            name: cap.clone(),\n+            params: Vec::new(),\n+            body,\n+        });\n+        let (tail, tail_stop) = self.parse_until(&scope.with(name.as_str()))?;\n+        nodes.push(Node::Let {\n+            span,\n+            bindings: vec![(name, Expr::App(\"render\".to_string(), vec![Expr::str(&cap)]))],\n+            body: tail,\n+        });\n+        Ok(tail_stop)\n+    }\n+\n     fn inline_block(\n         &mut self,\n         span: crate::span::Span,","raw_url":"https://github.com/wstein/flatbars/raw/b5acfb4c2264dc3608ae08eb86559b72f804e7f3/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Fparse.rs","sha":"a30acf4a7b62c69a161a9986b858ca6542c0744e","status":"modified"}]}