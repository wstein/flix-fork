{"files":[{"additions":63,"blob_url":"https://github.com/wstein/bin/blob/b6ed351c596fd5fb561d1c0c614f11ae72c588f4/list-vscode-extensions","changes":96,"contents_url":"https://api.github.com/repos/wstein/bin/contents/list-vscode-extensions?ref=b6ed351c596fd5fb561d1c0c614f11ae72c588f4","deletions":33,"filename":"list-vscode-extensions","patch":"@@ -21,16 +21,43 @@ OptionParser.new do |opts|\n   opts.on('-m', '--markdown', 'Output in Markdown format') { options[:format] = 'markdown' }\n   opts.on('-y', '--yaml', 'Output in YAML format') { options[:format] = 'yaml' }\n   opts.on('--workers=N', Integer, 'Number of parallel workers (default: 10)') { |n| options[:workers] = n }\n+  opts.on('-o', '--output=FILE', 'Write output to FILE instead of stdout') { |file| options[:output] = file }\n+  opts.on('-i', '--input=FILE', 'Read extension list from FILE') { |file| options[:input] = file }\n end.parse!\n \n+# Helper to read extension list from stdin, file, or VS Code CLI\n+def read_extensions(options)\n+  input_lines = []\n+  if !STDIN.tty? && !STDIN.closed? && !STDIN.eof?\n+    input_lines = STDIN.read.split(\"\\n\")\n+  elsif options[:input]\n+    input_lines = File.read(options[:input]).split(\"\\n\")\n+  end\n+  input_lines = input_lines.map(&:strip).reject(&:empty?)\n+  if input_lines.empty?\n+    # fallback to installed extensions\n+    input_lines = `code --list-extensions --show-versions`.split(\"\\n\").map(&:strip).reject(&:empty?)\n+  end\n+  # Remove duplicates\n+  input_lines.uniq\n+end\n \n-# Get list of installed extensions with versions\n-extensions_with_versions = `code --list-extensions --show-versions`.split(\"\\n\").map(&:strip).reject(&:empty?)\n-extensions = extensions_with_versions.map { |ext_with_version| ext_with_version.split('@').first }\n-extension_versions = Hash[extensions_with_versions.map { |ext_with_version|\n-  parts = ext_with_version.split('@')\n-  [parts.first, parts.last] if parts.size > 1\n-}.compact]\n+# Parse extension list into [id, version] pairs\n+def parse_extensions(ext_lines)\n+  ext_lines.map do |line|\n+    if line.include?('@')\n+      id, version = line.split('@', 2)\n+      [id, version]\n+    else\n+      [line, nil]\n+    end\n+  end\n+end\n+\n+ext_lines = read_extensions(options)\n+ext_pairs = parse_extensions(ext_lines)\n+ext_ids = ext_pairs.map(&:first)\n+ext_versions = Hash[ext_pairs.select { |id, v| v }.map { |id, v| [id, v] }]\n \n # Query VS Marketplace for extension details\n def fetch_extension_details(extension_id)\n@@ -111,8 +138,8 @@ end\n \n # Parallel fetching with specified number of workers\n queue = Queue.new\n-extensions.each_with_index { |ext, idx| queue << [idx, ext] }\n-results = Array.new(extensions.size)\n+ext_ids.each_with_index { |ext, idx| queue << [idx, ext] }\n+results = Array.new(ext_ids.size)\n mutex = Mutex.new\n completed = 0\n failed = 0\n@@ -144,62 +171,65 @@ workers = options[:workers].times.map do\n           failed += 1\n         end\n       ensure\n-        print_progress(completed, extensions.size)\n+        print_progress(completed, ext_ids.size)\n       end\n     end\n   end\n end\n \n workers.each(&:join)\n-STDERR.puts \"\\nCompleted #{completed}/#{extensions.size} extensions (#{failed} with errors)\"\n+STDERR.puts \"\\nCompleted #{completed}/#{ext_ids.size} extensions (#{failed} with errors)\"\n \n # Process only valid results, sort by id\n valid_results = results.compact.sort_by { |d| d['id'] }\n STDERR.puts \"Retrieved #{valid_results.size} extension details\"\n \n+# Output to file or stdout\n+output_io = options[:output] ? File.open(options[:output], 'w') : $stdout\n+\n+def output_puts(io, str)\n+  io.puts str\n+end\n+\n+def output_close(io, options)\n+  io.close if options[:output]\n+end\n+\n if options[:format] == 'recommend'\n-  puts '{'\n-  puts '  \"recommendations\": ['\n+  output_puts(output_io, '{')\n+  output_puts(output_io, '  \"recommendations\": [')\n   valid_results.each_with_index do |details, idx|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    puts\n-    puts \"    \\\"#{details['id']}\\\", // #{details['versions']&.first&.dig('version') || 'unknown'} [#{details['flags']}]\"\n-    puts \"    // #{details['shortDescription']}\"\n-    puts \"    // Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings) on #{Time.now.strftime(\"%Y-%m-%d\")}\"\n-    puts \"    // #{details['marketplace']}\"\n+    output_puts(output_io, \"\\n    \\\"#{details['id']}\\\", // #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\")\n+    output_puts(output_io, \"    // #{details['shortDescription']}\")\n+    output_puts(output_io, \"    // Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings) on #{Time.now.strftime(\"%Y-%m-%d\")}\")\n+    output_puts(output_io, \"    // #{details['marketplace']}\")\n   end\n-  puts '  ]'\n-  puts '}'\n+  output_puts(output_io, '  ]')\n+  output_puts(output_io, '}')\n elsif options[:format] == 'markdown'\n-  puts \"# VS Code Extensions\"\n-  puts\n+  output_puts(output_io, \"# VS Code Extensions\\n\")\n   valid_results.each do |details|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    puts \"- [#{details['id']}](#{details['marketplace']}) #{details['versions']&.first&.dig('version') || 'unknown'} [#{details['flags']}]\"\n-    puts \"  > #{details['shortDescription']}\"\n-    puts \"  > Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\"\n-    puts\n+    output_puts(output_io, \"- [#{details['id']}](#{details['marketplace']}) #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\\n  > #{details['shortDescription']}\\n  > Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n\")\n   end\n elsif options[:format] == 'json'\n-  puts JSON.pretty_generate(valid_results)\n+  output_puts(output_io, JSON.pretty_generate(valid_results))\n elsif options[:format] == 'yaml'\n-  puts valid_results.to_yaml\n+  output_puts(output_io, valid_results.to_yaml)\n else\n   valid_results.each do |details|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    puts \"#{details['id']} #{details['versions']&.first&.dig('version') || 'unknown'} [#{details['flags']}]\"\n-    puts \"  Description: #{details['shortDescription']}\"\n-    puts \"  Last Updated: #{last_updated_date} Installs: #{installs},  Rating: #{rating} (#{rating_count} ratings)\"\n-    puts \"  Marketplace: #{details['marketplace']}\"\n-    puts\n+    output_puts(output_io, \"#{details['id']} #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\\n  Description: #{details['shortDescription']}\\n  Last Updated: #{last_updated_date} Installs: #{installs},  Rating: #{rating} (#{rating_count} ratings)\\n  Marketplace: #{details['marketplace']}\\n\")\n   end\n end\n+output_close(output_io, options)","raw_url":"https://github.com/wstein/bin/raw/b6ed351c596fd5fb561d1c0c614f11ae72c588f4/list-vscode-extensions","sha":"b0a33d375eb94e33fc3c6815defa236c4b9a37ae","status":"modified"}]}