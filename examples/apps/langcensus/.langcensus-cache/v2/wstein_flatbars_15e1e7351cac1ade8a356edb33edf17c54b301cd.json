{"files":[{"additions":13,"blob_url":"https://github.com/wstein/flatbars/blob/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Fflatbars.mjs","changes":15,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fflatbars.mjs?ref=15e1e7351cac1ade8a356edb33edf17c54b301cd","deletions":2,"filename":"lab/flatbars.mjs","patch":"@@ -33,6 +33,8 @@ import {\n   compileSurface as bbCompileSurface,\n   compileMaxbars as bbCompileMaxbars,\n   renderWith as bbRenderWith,\n+  renderRawWith as bbRenderRawWith,\n+  renderMaxWith as bbRenderMaxWith,\n   renderSurfaceI18n as bbRenderSurfaceI18n,\n   analyze as bbAnalyze,\n   analyzeWith as bbAnalyzeWith,\n@@ -142,11 +144,20 @@ export async function createFlatBarsRenderer(dialectArg) {\n       // the host translator. (The Lab's i18n examples are FullBars/surface.)\n       res = bbRenderSurfaceI18n(program.translator, program.source, d);\n     } else if (program.dialect === \"core\") {\n-      res = bbRender(program.source, d);\n+      // Custom operations (ADR-019 addendum) register through RawBars' strict\n+      // registrar; otherwise the plain core render. Needed e.g. for a raw block\n+      // whose head must resolve to a defined operation.\n+      res = hasHelpers\n+        ? bbRenderRawWith(program.helpers, program.partials || {}, program.source, d)\n+        : bbRender(program.source, d);\n     } else if (program.dialect === \"maxbars\") {\n       // MaxBars reuses the FullBars surface pipeline; named external partials are\n       // not threaded through its entrypoint, so inline `{{#inline}}` only here.\n-      res = bbRenderMaxbars(program.source, d);\n+      // With custom operations, route through MaxBars' operation registrar so a\n+      // registered head (e.g. a raw block's `{{{{#op}}}}`) actually resolves.\n+      res = hasHelpers\n+        ? bbRenderMaxWith(program.helpers, program.partials || {}, program.source, d)\n+        : bbRenderMaxbars(program.source, d);\n     } else if (hasHelpers) {\n       // Custom helpers (ADR-018) render through the facade's renderWith, which\n       // also threads partials — the surface/FullBars path.","raw_url":"https://github.com/wstein/flatbars/raw/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Fflatbars.mjs","sha":"58e04c7d03368edab390e422c959883d84fdad90","status":"modified"},{"additions":13,"blob_url":"https://github.com/wstein/flatbars/blob/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Fflatbars.test.mjs","changes":13,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fflatbars.test.mjs?ref=15e1e7351cac1ade8a356edb33edf17c54b301cd","deletions":0,"filename":"lab/flatbars.test.mjs","patch":"@@ -37,6 +37,19 @@ test(\"renders user-defined helpers via opts.helpers (ADR-018)\", async () => {\n   assert.equal(run(r, \"{{loud x}}\", { x: \"<b>ada\" }, { helpers }), \"&lt;B&gt;ADA\");\n });\n \n+test(\"opts.helpers register for the maxbars and core dialects too (operation registrars)\", async () => {\n+  // A raw block's head must resolve to a defined operation (the strict-raw-block\n+  // rule) — so a maxbars/rawbars example with custom ops must route through the\n+  // per-dialect registrar, not the helper-less render. Regression: the Lab once\n+  // rendered maxbars before checking opts.helpers, so {{{{#rawloud}}}} threw\n+  // UnknownHelper in the playground despite the example registering it.\n+  const r = await createFlatBarsRenderer();\n+  const helpers = { rawloud: (options) => options.fn().toUpperCase() };\n+  const tpl = \"{{{{#rawloud}}}}\\n  {{bar}}\\n{{{{/rawloud}}}}\";\n+  assert.equal(run(r, tpl, null, { dialect: \"maxbars\", helpers }), \"\\n  {{BAR}}\\n\");\n+  assert.equal(run(r, tpl, null, { dialect: \"core\", helpers }), \"\\n  {{BAR}}\\n\");\n+});\n+\n test(\"renders the core dialect when selected\", async () => {\n   const r = await createFlatBarsRenderer();\n   assert.equal(","raw_url":"https://github.com/wstein/flatbars/raw/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Fflatbars.test.mjs","sha":"e4962030b5bf72a6b1dedc43bdd01e116f4502b8","status":"modified"},{"additions":17,"blob_url":"https://github.com/wstein/flatbars/blob/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Findex.html","changes":30,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=15e1e7351cac1ade8a356edb33edf17c54b301cd","deletions":13,"filename":"lab/index.html","patch":"@@ -1629,7 +1629,7 @@\n       // engine adapters so a reload never pairs a fresh index.html / example\n       // file with a stale cached adapter module.\n       import { createRenderer } from \"./stem.mjs?v=1\";\n-      import { createFlatBarsRenderer } from \"./flatbars.mjs?v=4\";\n+      import { createFlatBarsRenderer } from \"./flatbars.mjs?v=5\";\n       import { createMinBarsRenderer } from \"./minbars.mjs?v=2\";\n       // Engine-derived INNER-TOKEN highlighting (ADR-014 amendment): the shared\n       // painter + the lexer's own tokenizer, in place of a hand-written tag regex.\n@@ -1884,8 +1884,9 @@\n       // parsed data before rendering. Engine-agnostic; empty = pass data through.\n       let transformSrc = \"\";\n       // Custom helpers (ADR-018) — a fundamentally different thing from the\n-      // transform: registerHelper(...) JS that extends the FullBars engine. A\n-      // SEPARATE editor (helpers.js), FullBars only. The source is evaluated and\n+      // transform: registerHelper(...) JS that extends the engine. A SEPARATE\n+      // editor (helpers.js), for every FlatBars dialect (RawBars/FullBars/MaxBars;\n+      // not MinBars). The source is evaluated and\n       // run in a Worker (no DOM / no page access), never on the main thread, so a\n       // shared workspace cannot deface or read the page (no consent prompt\n       // needed). `run()` is synchronous, so the Worker renders OUT OF BAND: its\n@@ -1932,7 +1933,7 @@\n       // files the user has opened (from the explorer or by navigating), not\n       // every partial. Keys: \"main\" (entry, pinned) · partial name · \"transform\".\n       // Closing a tab removes it here but never deletes the underlying file.\n-      let openEditors = [\"main\", \"data\", \"transform\", ...(ENGINE === \"fullbars\" ? [\"helpers\"] : [])];\n+      let openEditors = [\"main\", \"data\", \"transform\", ...(ENGINE !== \"minbars\" ? [\"helpers\"] : [])];\n       let yamlDirty      = false;\n       let transformDirty = false;\n \n@@ -2467,9 +2468,11 @@\n               title: \"data → view-model transform (JSONata)\",\n               on: () => selectTransformTab() },\n           ]},\n-          // Custom helpers (ADR-018) are a separate, FullBars-only editor — they\n-          // extend the engine, not the data. (RawBars/MaxBars terminology: ADR-019.)\n-          ...(ENGINE === \"fullbars\" ? [{ key: \"helpers\", label: \"HELPERS\", files: [\n+          // Custom helpers/operations (ADR-018; ADR-019 for the native dialects)\n+          // are a separate editor that extends the engine, not the data — available\n+          // for every FlatBars dialect (RawBars/FullBars/MaxBars), MinBars excluded\n+          // (Mustache is logic-less). The Worker routes to the dialect's registrar.\n+          ...(ENGINE !== \"minbars\" ? [{ key: \"helpers\", label: \"HELPERS\", files: [\n             { icon: \"fn\",\n               name: \"helpers.js\",\n               active: hlpActive, dirty: helpersDirty,\n@@ -3735,16 +3738,17 @@\n         }\n         try {\n           const program = applyEscape(compiled.program, escapeMode);\n-          // Custom-helper render (ADR-018): FullBars + Controller source runs in\n+          // Custom-helper render (ADR-018/019): the registerHelper source runs in\n           // the Worker sandbox, out of band. Use the cached result when it\n           // matches the current inputs; otherwise show \"rendering…\" and let the\n           // async render re-run. Provenance segments aren't produced for helper\n           // renders (as the in-process renderWith path also returns none).\n-          // helpers.js is FullBars-only; the catalog (t/number/…) works on the\n-          // t-surfaces (RawBars/FullBars/MaxBars) — ADR-029, MinBars excluded. Both\n-          // render in the Worker via the surface's operations-aware entry, with the\n-          // locale from config.yaml.\n-          const helperSrc = (ENGINE === \"fullbars\") ? (helpersSrc || \"\").trim() : \"\";\n+          // Both helpers.js and the catalog (t/number/…) work on every FlatBars\n+          // dialect — the t-surfaces RawBars/FullBars/MaxBars (ADR-029) — MinBars\n+          // excluded (logic-less). The Worker routes each to the dialect's\n+          // operations-aware entry (renderRawWith/renderWith/renderMaxWith), with\n+          // the locale from config.yaml.\n+          const helperSrc = (ENGINE !== \"minbars\") ? (helpersSrc || \"\").trim() : \"\";\n           const catSrc = (ENGINE !== \"minbars\") ? (catalogSource || \"\").trim() : \"\";\n           let output, segments;\n           if (helperSrc || catSrc) {","raw_url":"https://github.com/wstein/flatbars/raw/15e1e7351cac1ade8a356edb33edf17c54b301cd/lab%2Findex.html","sha":"e80e35791f0495c3c990e667430587224e32a7f0","status":"modified"}]}