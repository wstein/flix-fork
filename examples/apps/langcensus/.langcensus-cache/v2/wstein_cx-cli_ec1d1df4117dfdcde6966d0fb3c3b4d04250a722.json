{"files":[{"additions":79,"blob_url":"https://github.com/wstein/cx-cli/blob/ec1d1df4117dfdcde6966d0fb3c3b4d04250a722/.github%2Fworkflows%2Fpublish.yml","changes":112,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/.github%2Fworkflows%2Fpublish.yml?ref=ec1d1df4117dfdcde6966d0fb3c3b4d04250a722","deletions":33,"filename":".github/workflows/publish.yml","patch":"@@ -20,12 +20,11 @@ permissions:\n   contents: read\n \n jobs:\n-  release:\n-    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')\n+  prepare-release:\n     runs-on: ubuntu-latest\n-    environment: homebrew\n-    permissions:\n-      contents: write\n+    outputs:\n+      release_version: ${{ steps.release-version.outputs.version }}\n+      tarball_name: ${{ steps.pack.outputs.tarball_name }}\n     steps:\n       - name: Checkout current repo\n         uses: actions/checkout@v4\n@@ -38,25 +37,6 @@ jobs:\n       - name: Install dependencies\n         run: bun install --frozen-lockfile\n \n-      - name: Preflight release secrets\n-        env:\n-          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}\n-          HOMEBREW_TAP_PUSH_TOKEN: ${{ secrets.HOMEBREW_TAP_PUSH_TOKEN }}\n-        run: |\n-          set -euo pipefail\n-          missing=0\n-          if [ -z \"${NPM_TOKEN:-}\" ]; then\n-            echo \"NPM_TOKEN secret is required for npm publish.\"\n-            missing=1\n-          fi\n-          if [ -z \"${HOMEBREW_TAP_PUSH_TOKEN:-}\" ]; then\n-            echo \"HOMEBREW_TAP_PUSH_TOKEN secret is required to authenticate the push to wstein/homebrew-tap.\"\n-            missing=1\n-          fi\n-          if [ \"$missing\" -ne 0 ]; then\n-            exit 1\n-          fi\n-\n       - name: Determine release version\n         id: release-version\n         run: |\n@@ -84,6 +64,53 @@ jobs:\n       - name: Build package\n         run: bun run build\n \n+      - name: Pack release tarball\n+        id: pack\n+        run: |\n+          set -euo pipefail\n+          mkdir -p release-artifacts\n+          npm pack --json --pack-destination release-artifacts > release-artifacts/pack.json\n+          tarball_name=$(node -p \"JSON.parse(require('fs').readFileSync('release-artifacts/pack.json', 'utf8'))[0].filename\")\n+          rm release-artifacts/pack.json\n+          echo \"tarball_name=$tarball_name\" >> $GITHUB_OUTPUT\n+\n+      - name: Generate Homebrew formula\n+        run: |\n+          bun run scripts/generate-homebrew-formula.ts \"${{ steps.release-version.outputs.version }}\" --tarball \"release-artifacts/${{ steps.pack.outputs.tarball_name }}\" --output release-artifacts/cx-cli.rb\n+\n+      - name: Validate formula syntax\n+        run: ruby -c release-artifacts/cx-cli.rb\n+\n+      - name: Upload release artifacts\n+        uses: actions/upload-artifact@v4\n+        with:\n+          name: release-artifacts\n+          path: release-artifacts\n+          if-no-files-found: error\n+          retention-days: 1\n+\n+  publish-npm:\n+    needs: prepare-release\n+    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')\n+    runs-on: ubuntu-latest\n+    environment: node\n+    steps:\n+      - name: Download release artifacts\n+        uses: actions/download-artifact@v4\n+        with:\n+          name: release-artifacts\n+          path: release-artifacts\n+\n+      - name: Preflight npm secret\n+        env:\n+          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}\n+        run: |\n+          set -euo pipefail\n+          if [ -z \"${NPM_TOKEN:-}\" ]; then\n+            echo \"NPM_TOKEN secret is required for npm publish.\"\n+            exit 1\n+          fi\n+\n       - name: Setup npm auth\n         uses: actions/setup-node@v4\n         with:\n@@ -92,17 +119,33 @@ jobs:\n           always-auth: true\n \n       - name: Publish to npm\n-        run: npm publish --access public\n+        run: npm publish --access public \"release-artifacts/${{ needs.prepare-release.outputs.tarball_name }}\"\n         env:\n           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}\n \n-      - name: Generate Homebrew formula\n-        run: |\n-          mkdir -p homebrew-formula\n-          bun run scripts/generate-homebrew-formula.ts \"${{ steps.release-version.outputs.version }}\" --output homebrew-formula/cx-cli.rb\n+  publish-homebrew:\n+    needs: prepare-release\n+    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')\n+    runs-on: ubuntu-latest\n+    environment: homebrew\n+    permissions:\n+      contents: write\n+    steps:\n+      - name: Download release artifacts\n+        uses: actions/download-artifact@v4\n+        with:\n+          name: release-artifacts\n+          path: release-artifacts\n \n-      - name: Validate formula syntax\n-        run: ruby -c homebrew-formula/cx-cli.rb\n+      - name: Preflight tap secret\n+        env:\n+          HOMEBREW_TAP_PUSH_TOKEN: ${{ secrets.HOMEBREW_TAP_PUSH_TOKEN }}\n+        run: |\n+          set -euo pipefail\n+          if [ -z \"${HOMEBREW_TAP_PUSH_TOKEN:-}\" ]; then\n+            echo \"HOMEBREW_TAP_PUSH_TOKEN secret is required to authenticate the push to wstein/homebrew-tap.\"\n+            exit 1\n+          fi\n \n       - name: Checkout Homebrew tap\n         uses: actions/checkout@v4\n@@ -111,6 +154,9 @@ jobs:\n           token: ${{ secrets.HOMEBREW_TAP_PUSH_TOKEN }}\n           path: homebrew-tap\n \n+      - name: Validate formula syntax\n+        run: ruby -c release-artifacts/cx-cli.rb\n+\n       - name: Update tap formula\n         run: |\n           set -euo pipefail\n@@ -119,11 +165,11 @@ jobs:\n           git config user.email \"github-actions[bot]@users.noreply.github.com\"\n           git checkout main || git checkout -b main\n           mkdir -p Formula\n-          cp ../homebrew-formula/cx-cli.rb Formula/cx-cli.rb\n+          cp ../release-artifacts/cx-cli.rb Formula/cx-cli.rb\n           if git diff --quiet -- Formula/cx-cli.rb; then\n             echo \"No formula updates required.\"\n             exit 0\n           fi\n           git add Formula/cx-cli.rb\n-          git commit -m \"chore(homebrew): update cx-cli formula for v${{ steps.release-version.outputs.version }}\"\n+          git commit -m \"chore(homebrew): update cx-cli formula for v${{ needs.prepare-release.outputs.release_version }}\"\n           git push origin main","raw_url":"https://github.com/wstein/cx-cli/raw/ec1d1df4117dfdcde6966d0fb3c3b4d04250a722/.github%2Fworkflows%2Fpublish.yml","sha":"c43ef670a715ba61e5e506583b726023d9f08086","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/cx-cli/blob/ec1d1df4117dfdcde6966d0fb3c3b4d04250a722/docs%2FRELEASE_CHECKLIST.md","changes":4,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/docs%2FRELEASE_CHECKLIST.md?ref=ec1d1df4117dfdcde6966d0fb3c3b4d04250a722","deletions":2,"filename":"docs/RELEASE_CHECKLIST.md","patch":"@@ -10,6 +10,6 @@ Use this short checklist when cutting a release.\n - Mirror the same schema files into the GitHub Release assets for the tagged release.\n - Use `make release VERSION=x.y.z` to hand off the tagged release to the release script.\n - Ensure `package.json` version matches the git tag before publishing release artifacts.\n-- The release workflow requires `NPM_TOKEN` so `npm publish` can authenticate to the npm registry, and `HOMEBREW_TAP_PUSH_TOKEN` so it can authenticate the cross-repo push to `wstein/homebrew-tap` from the `homebrew` environment.\n-- Confirm both secrets are set before the release workflow starts; the workflow now fails fast if either one is missing.\n+- The release workflow requires `NPM_TOKEN` in the `node` environment so `npm publish` can authenticate to the npm registry, and `HOMEBREW_TAP_PUSH_TOKEN` in the `homebrew` environment so it can authenticate the cross-repo push to `wstein/homebrew-tap`.\n+- Confirm both environment secrets are set before the release workflow starts; the workflow now fails fast if either one is missing.\n - Verify the external tap repo `wstein/homebrew-tap` accepts direct formula updates from the single release workflow.","raw_url":"https://github.com/wstein/cx-cli/raw/ec1d1df4117dfdcde6966d0fb3c3b4d04250a722/docs%2FRELEASE_CHECKLIST.md","sha":"07589113177b9e752b71bcaac6bd84d3c5d63355","status":"modified"}]}