{"files":[{"additions":103,"blob_url":"https://github.com/wstein/gramaire/blob/97ed61b560e07d56ab68f8ab3ad48becac305a10/site%2Fsrc%2Flab%2FLabIsland.tsx","changes":104,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flab%2FLabIsland.tsx?ref=97ed61b560e07d56ab68f8ab3ad48becac305a10","deletions":1,"filename":"site/src/lab/LabIsland.tsx","patch":"@@ -9,6 +9,7 @@ import type {\n   LrStepInfo,\n   Method,\n   ProductionInfo,\n+  Strategy,\n } from \"./protocol\";\n import type {\n   AnnotatedNode,\n@@ -34,13 +35,17 @@ type Tab =\n   | \"walk\"\n   | \"forest\"\n   | \"lowered\"\n-  | \"analysis\";\n+  | \"analysis\"\n+  | \"atn\";\n \n const METHODS = [\"Canonical\", \"LALR\", \"IELR\"] as const;\n \n const grammarSource = signal(DEFAULT_SOURCE);\n const targetInput = signal(DEFAULT_INPUT);\n const method = signal<Method>(\"Canonical\");\n+// \"lr\" (the default) leaves LabResponse.atn null — additive only, never changes buildOk/parse/\n+// forest/analysis/evaluatorJs (see LabRequest.strategy's own doc comment in protocol.ts).\n+const strategy = signal<Strategy>(\"lr\");\n // null means \"no override\" — the request omits startRule, so the engine uses the grammar's own\n // natural declaration order (its first rule). Set only by the start-rule picker.\n const startRule = signal<string | null>(null);\n@@ -227,6 +232,7 @@ function scheduleEvaluate() {\n       input: targetInput.value.length > 0 ? targetInput.value : null,\n       method: method.value,\n       startRule: startRule.value,\n+      strategy: strategy.value,\n     };\n     const message: WorkerRequestMessage = { id, request };\n     ensureWorker().postMessage(message);\n@@ -524,6 +530,20 @@ export default function LabIsland() {\n             <option value=\"IELR\">IELR(1)</option>\n           </select>\n         </label>\n+        <label class=\"lab__method\">\n+          Strategy\n+          <select\n+            value={strategy.value}\n+            onChange={(e) => {\n+              strategy.value = (e.target as HTMLSelectElement)\n+                .value as Strategy;\n+              scheduleEvaluate();\n+            }}\n+          >\n+            <option value=\"lr\">LR / GLR</option>\n+            <option value=\"ll-star\">ALL(*) (ll-star)</option>\n+          </select>\n+        </label>\n         {ruleNames.value.length > 0 && (\n           <label class=\"lab__method\">\n             Start rule\n@@ -691,6 +711,7 @@ export default function LabIsland() {\n               \"forest\",\n               \"lowered\",\n               \"analysis\",\n+              \"atn\",\n             ] as const\n           ).map((tab) => (\n             <button\n@@ -721,6 +742,7 @@ export default function LabIsland() {\n           {activeTab.value === \"forest\" && <AllParsesPanel />}\n           {activeTab.value === \"lowered\" && <LoweredCorePanel />}\n           {activeTab.value === \"analysis\" && <GrammarAnalysisPanel />}\n+          {activeTab.value === \"atn\" && <AtnDiagnosticsPanel />}\n         </div>\n       </div>\n       <StatusBar />\n@@ -808,6 +830,11 @@ function tabLabel(tab: Tab): string {\n       return \"Lowered Core\";\n     case \"analysis\":\n       return \"Grammar analysis\";\n+    case \"atn\":\n+      // Just \"ATN\", not \"ATN diagnostics\": Playwright's has-text matching is case-insensitive, and\n+      // a Diagnostics substring here would collide with lab.spec.ts's \"no separate Diagnostics tab\"\n+      // assertion (Diagnostics was folded into Output — see this file's own Tab doc comment).\n+      return \"ATN\";\n   }\n }\n \n@@ -1260,6 +1287,81 @@ function GrammarAnalysisPanel() {\n   );\n }\n \n+// The ll-star-only diagnostics tab: whether Ll.recognize accepts the target input, the ATN\n+// prediction DFA cache's hit rate, and every declaration-order-resolved ambiguity hit along the\n+// way — LabResponse.atn, populated only under Strategy \"ALL(*) (ll-star)\" with input given, and\n+// independent of buildOk (a grammar the LR table build rejects can still be worth seeing through\n+// ALL(*)'s own lens, same as Forest).\n+function AtnDiagnosticsPanel() {\n+  if (strategy.value !== \"ll-star\")\n+    return (\n+      <p class=\"lab__empty\">\n+        Switch Strategy to \"ALL(*) (ll-star)\" above to see ATN diagnostics.\n+      </p>\n+    );\n+\n+  const d = response.value?.atn;\n+  if (!d)\n+    return (\n+      <p class=\"lab__empty\">\n+        No ATN diagnostics — enter target input to run Ll.recognize.\n+      </p>\n+    );\n+\n+  const total = d.hits + d.misses;\n+  const hitPct = total > 0 ? Math.round((d.hits / total) * 100) : 0;\n+\n+  return (\n+    <div>\n+      <div class=\"lab__analysis-section\">\n+        <div class=\"lab__analysis-heading\">Ll.recognize</div>\n+        <p>\n+          <span\n+            class={`lab__parsestatus lab__parsestatus--${d.accepted ? \"accepted\" : \"rejected\"}`}\n+          >\n+            {d.accepted ? \"accepted\" : \"rejected\"}\n+          </span>{\" \"}\n+          — {d.hits}/{total} DFA cache hits ({hitPct}%)\n+        </p>\n+      </div>\n+\n+      <div class=\"lab__analysis-section\">\n+        <div class=\"lab__analysis-heading\">ambiguities</div>\n+        {d.ambiguities.length === 0 ? (\n+          <p class=\"lab__empty\">\n+            No ambiguities — every decision resolved uniquely.\n+          </p>\n+        ) : (\n+          <table class=\"lab__table\">\n+            <thead>\n+              <tr>\n+                <th>rule</th>\n+                <th>decision</th>\n+                <th>pos</th>\n+                <th>tied alts</th>\n+              </tr>\n+            </thead>\n+            <tbody>\n+              {d.ambiguities.map((a, i) => (\n+                <tr\n+                  key={i}\n+                  onMouseEnter={() => (hoverRule.value = a.rule)}\n+                  onMouseLeave={() => (hoverRule.value = null)}\n+                >\n+                  <td class=\"lab__mono\">{a.rule}</td>\n+                  <td class=\"lab__mono\">{a.decision}</td>\n+                  <td class=\"lab__mono\">{a.pos}</td>\n+                  <td class=\"lab__mono\">{a.alts.join(\", \")}</td>\n+                </tr>\n+              ))}\n+            </tbody>\n+          </table>\n+        )}\n+      </div>\n+    </div>\n+  );\n+}\n+\n // The SVG is server-rendered by Railroad.renderSvg from the grammar the user is already editing in\n // this same tab — the same trust boundary as the grammar source itself, not third-party or\n // cross-origin content. dangerouslySetInnerHTML content isn't part of Preact's vdom, so it starts","raw_url":"https://github.com/wstein/gramaire/raw/97ed61b560e07d56ab68f8ab3ad48becac305a10/site%2Fsrc%2Flab%2FLabIsland.tsx","sha":"8c978a1839a6454761a821a8ceb26e0122f164af","status":"modified"},{"additions":19,"blob_url":"https://github.com/wstein/gramaire/blob/97ed61b560e07d56ab68f8ab3ad48becac305a10/site%2Ftests%2Fvisual%2Flab.spec.ts","changes":19,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Ftests%2Fvisual%2Flab.spec.ts?ref=97ed61b560e07d56ab68f8ab3ad48becac305a10","deletions":0,"filename":"site/tests/visual/lab.spec.ts","patch":"@@ -165,6 +165,25 @@ test(\"the Lab tabs show real, engine-computed data\", async ({ page }) => {\n   await page.click('button[aria-label=\"last step\"]');\n   await expect(page.locator(\".lab__walk-counter\")).toHaveText(\"step 14 / 14\");\n   await expect(page.locator(\".lab__walk-action\")).toContainText(\"accept\");\n+\n+  // ATN diagnostics is additive: Strategy defaults to LR, so the tab starts as an empty state\n+  // prompting the user to switch, with no atn data requested at all.\n+  await page.click('button[role=\"tab\"]:has-text(\"ATN\")');\n+  await expect(page.locator(\".lab__empty\")).toContainText(\"Switch Strategy\");\n+\n+  await page.getByLabel(\"Strategy\").selectOption(\"ll-star\");\n+  await expect(page.locator(\".lab__panel\")).toContainText(\"Ll.recognize\", {\n+    timeout: 5000,\n+  });\n+  // Scoped to .lab__panel: StatusBar's own always-visible badge reuses the same\n+  // .lab__parsestatus class, and both are in the DOM at once once this tab is active.\n+  await expect(page.locator(\".lab__panel .lab__parsestatus\")).toHaveText(\n+    \"accepted\",\n+  );\n+  await expect(page.locator(\".lab__panel\")).toContainText(\"DFA cache hits\");\n+  await expect(page.locator(\".lab__panel\")).toContainText(\n+    \"No ambiguities — every decision resolved uniquely.\",\n+  );\n });\n \n test(\"the Lab's All-parses tab shows every derivation of an ambiguous grammar\", async ({","raw_url":"https://github.com/wstein/gramaire/raw/97ed61b560e07d56ab68f8ab3ad48becac305a10/site%2Ftests%2Fvisual%2Flab.spec.ts","sha":"e25a4b5fddaa7bd2d1251c7a271c740d6828ce5e","status":"modified"}]}