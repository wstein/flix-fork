{"files":[{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/6c164d849cb0453ff196662256fcb0b53d4c5010/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=6c164d849cb0453ff196662256fcb0b53d4c5010","deletions":1,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -44,7 +44,7 @@ gating library work and blocks Stage 2 below.\n |---|---|---|---|\n | **0 — Equivalence** | each ported stage matches the reference on a corpus | fixpoint diff (lexer vs `tokenize/1`; parser vs `Pratt.parse`) | **done** (lexer slices 1-5; parser slice) |\n | **1 — Self-application** | a ported stage processes **real toolchain source** (incl. the self-hosting sources themselves) and agrees with the reference | run the Rian lexer over `selfhost_parse.rian` / preludes, diff vs `Rian.Lexer.tokenize/1` | **done for the lexer** (`test/rian/selfhost_fixedpoint_test.exs`) |\n-| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **near-complete** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums, **`struct` records**, **`mod` nesting** (incl. `pub def`), and `def` functions — multi-clause with **clause patterns** (var/lit/ctor/wildcard + **cons/list**), **capabilities**, **parametric types** (`Vec(T)`), **list construction**, **`.field`/labeled construction**, and **`when` guards** — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (cons-recursive `rev`, a `struct` program, a `mod` compiled to its own BEAM module; `decl_fixpoint_test.exs`). **Remaining:** string/char clause patterns, `alias`/`protocol`/generics/doc-comments, and the portable stdlib breadth |\n+| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; the **declaration** front-end's IR equals `Rian.Decl.parse` and runs via `Rian.Beam.compile_ir/2` | **near-complete** — a Rian **declaration** front-end (`selfhost_decl.rian`) parses `type` sums, **`struct` records**, **`mod` nesting** (incl. `pub def`), and `def` functions — multi-clause with **clause patterns** (var/lit/ctor/wildcard + **cons/list**), **capabilities**, **parametric types** (`Vec(T)`), **list construction**, **`.field`/labeled construction**, **`when` guards**, and **`forall` generics** (`Ret forall T, U: Eq + Ord` → `Func.tvars`/`Func.bounds`, ADR-0042) — into IR that **equals `Rian.Decl.parse`** and **compiles+runs** on the real backend (cons-recursive `rev`, a `struct` program, a `mod` compiled to its own BEAM module, a generic `id`/`head` with tvars erased; `decl_fixpoint_test.exs`). **Remaining:** string/char clause patterns, `alias`/`protocol`/doc-comments, and the portable stdlib breadth |\n | **3 — Bootstrap fixed point** | the **whole compiler**, written in Rian, compiles its own source; doing so twice is stable | compile the Rian compiler source with the Elixir-hosted compiler → v1; compile the *same source* with v1 → v2; assert **v1 == v2** (bit-identical `.beam`) | **future** (the canonical terminus); its **determinism prerequisite is verified** — `Rian.Beam` emits byte-identical bytecode on recompilation (`selfhost_fixedpoint_test.exs`) |\n \n **\"Real self-hosting\" = Stage 2** (the front-end genuinely self-hosts, reusing a trusted backend);","raw_url":"https://github.com/wstein/rian-lab/raw/6c164d849cb0453ff196662256fcb0b53d4c5010/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"f8d49c0d781e016fe5b12baf8683b56c153301ca","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/6c164d849cb0453ff196662256fcb0b53d4c5010/docs%2Fself-host-status.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fself-host-status.md?ref=6c164d849cb0453ff196662256fcb0b53d4c5010","deletions":1,"filename":"docs/self-host-status.md","patch":"@@ -12,7 +12,7 @@ terminus and is *not* measured here.\n | Stage | Role | Self-hosted | Evidence | Notes |\n | --- | --- | --- | --- | --- |\n | Lexer | frontend | ✅ yes | `examples/rian/selfhost_lexer_v2.rian` · `test/rian/fixpoint_test.exs` | token stream equals Rian.Lexer over slices 1-5 (incl. tokenize/1, {:nl}) |\n-| Declaration parser | frontend | 🟡 partial | `examples/rian/selfhost_decl.rian` · `test/rian/decl_fixpoint_test.exs` | IR equals Rian.Decl over type/struct/mod/def slice; alias/protocol/generics remain |\n+| Declaration parser | frontend | 🟡 partial | `examples/rian/selfhost_decl.rian` · `test/rian/decl_fixpoint_test.exs` | IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042); alias/protocol/doc-comments remain |\n | Expression/pattern parser | frontend | ✅ yes | `examples/rian/selfhost_parse.rian` · `test/rian/parse_fixpoint_test.exs` | the full Rian.Pratt grammar — prefix/primary (if/case/with/list/map/tuple/lambda/capture/atom/str/char/num/id), postfix dot/call, labelled args, precedence climbing, AND the full pattern grammar + blocks — equals Rian.Pratt.parse with no projection; string-interpolation/`<-`-propagation sugar is out of scope; `:if`/`:case`/`:with`/`:struct` keyword-atom tags need a counted String.to_atom crutch |\n | Typed Core IR (from_expr/from_pat) | frontend | ✅ yes | `examples/rian/selfhost_core.rian` · `test/rian/core_fixpoint_test.exs` | the full surface→Core lowering — every Pratt-produced expression (literals/unary/binary/call/label/dot/if/tuple/list/map/block/case/lambda/capture/with) and pattern (wild/var/lit/char/atom/tuple/ctor/list/struct/map) — equals Rian.Core.from_expr/from_pat; the Lower-internal resolved nodes + never-parsed as/pin patterns are not surface-reachable |\n | Type checker (inference + error sets) | checker | 🟡 partial | `examples/rian/selfhost_checker.rian` · `test/rian/checker_infer_fixpoint_test.exs` | type inference agrees with the REAL Rian.Check.infer over closed integer expressions; env/floats/calls/lambdas/case remain (selfhost_check.rian is a separate TOY-language spike) |","raw_url":"https://github.com/wstein/rian-lab/raw/6c164d849cb0453ff196662256fcb0b53d4c5010/docs%2Fself-host-status.md","sha":"e3780e05af71be7609e35e9729fec508eab8c57e","status":"modified"},{"additions":64,"blob_url":"https://github.com/wstein/rian-lab/blob/6c164d849cb0453ff196662256fcb0b53d4c5010/examples%2Frian%2Fselfhost_decl.rian","changes":73,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_decl.rian?ref=6c164d849cb0453ff196662256fcb0b53d4c5010","deletions":9,"filename":"examples/rian/selfhost_decl.rian","patch":"@@ -11,15 +11,17 @@\n #   type Name := V1 | V2(T, …)                         sum declarations\n #   struct Name(field Type, …)                         product (record) types\n #   mod Name do … end                                  module nesting (incl. `pub def`)\n-#   def name(p [cap] Type, …) Ret := expr              single typed clause\n+#   def name(p [cap] Type, …) Ret [forall T, U: Bound] := expr   single typed clause\n #   def name(pat, …) [when g] := expr                  pattern clause (multi-clause)\n+#   generics: `Ret forall T` / `Ret forall T, U: Eq + Ord` binders (ADR-0042),\n+#             carried as `tvars`/`bounds` on the function head\n #   patterns: _ · var · Int literal · Ctor(pat, …) · cons/list [] / [h | t] (nested)\n #   capabilities: val / iso / ref / tag (default val); parametric types `Vec(T)`\n #   exprs: binops (precedence climbing) · prefix - / not · calls · `.field` access\n #          · labeled construction `Name(f: v)` · list construction `[x | acc]`\n #\n # Out of slice (the remaining long tail of Rian.Decl): string/char clause patterns,\n-# negative-literal patterns, `alias`/`const`/`protocol`/`impl`/`forall` generics,\n+# negative-literal patterns, `alias`/`const`/`protocol`/`impl`,\n # doc-comments, and block (`do … end`) function bodies.\n # ---------------------------------------------------------------------------\n \n@@ -47,13 +49,17 @@ mod SelfhostDecl do\n   type Variant := V(String, Vec(String))\n   type Param := Par(String, String, String)\n   type PType := PT(String, Vec(Tok))\n+  # a `forall` binder (ADR-0042): the tvar name + its protocol bounds (empty list\n+  # when unconstrained). `forall T: Eq + Ord` -> TV(\"T\", [\"Eq\", \"Ord\"]).\n+  type TVar := TV(String, Vec(String))\n   # `pub?` (a Bool) is carried on the function head (DFunc/DSig) so `pub def`\n-  # inside a `mod` projects to `Func.pub? = true` (exported on the BEAM).\n+  # inside a `mod` projects to `Func.pub? = true` (exported on the BEAM); the\n+  # `Vec(TVar)` carries the `forall` generics (empty when none).\n   type Decl :=\n       DType(String, Vec(Variant))\n     | DStruct(String, Vec(Param))\n-    | DFunc(Bool, String, Vec(Param), String, Expr)\n-    | DSig(Bool, String, Vec(Param), String)\n+    | DFunc(Bool, String, Vec(Param), String, Vec(TVar), Expr)\n+    | DSig(Bool, String, Vec(Param), String, Vec(TVar))\n     | DClause(String, Vec(Pat), Expr)\n     | DClauseG(String, Vec(Pat), Expr, Expr)\n     | DMod(String, Vec(Decl))\n@@ -71,6 +77,9 @@ mod SelfhostDecl do\n   type PatRes := PP(Pat, Vec(Tok))\n   type PatList := PL(Vec(Pat), Vec(Tok))\n   type ModBody := MB(Vec(Tok), Vec(Tok))\n+  type Gen := GR(Vec(TVar), Vec(Tok))\n+  type TVarRes := TVR(TVar, Vec(Tok))\n+  type BoundRes := BR(Vec(String), Vec(Tok))\n \n   # ── program: declarations separated by significant newlines ──────────────\n   pub def parse_program(ts Vec(Tok)) Vec(Decl)\n@@ -203,14 +212,60 @@ mod SelfhostDecl do\n   end\n   def classify_def(pb, name, inside, [TId(rb) | rb_rest])\n     case parse_ptype([TId(rb) | rb_rest]) do\n-      PT(ret, [TOp(\":=\") | body]) ->\n+      PT(ret, after) -> finish_sig(pb, name, inside, ret, after)\n+    end\n+  end\n+  def classify_def(pb, name, inside, after) := DR(DErr, after)\n+\n+  # after the return type comes an optional `forall T, U: Bound` binder list, then\n+  # either `:= body` (a typed single clause) or nothing (a bodiless signature).\n+  def finish_sig(pb Bool, name String, inside Vec(Tok), ret String, after Vec(Tok)) DeclRes\n+  def finish_sig(pb, name, inside, ret, after)\n+    case parse_forall(after) do\n+      GR(tvs, [TOp(\":=\") | body]) ->\n         case parse_bp(0, body) do\n-          P(b, rest) -> DR(DFunc(pb, name, parse_params(inside), ret, b), rest)\n+          P(b, rest) -> DR(DFunc(pb, name, parse_params(inside), ret, tvs, b), rest)\n         end\n-      PT(ret, rest) -> DR(DSig(pb, name, parse_params(inside), ret), rest)\n+      GR(tvs, rest) -> DR(DSig(pb, name, parse_params(inside), ret, tvs), rest)\n     end\n   end\n-  def classify_def(pb, name, inside, after) := DR(DErr, after)\n+\n+  # `forall T` / `forall T, U: Eq + Ord` -> the binder list (each tvar with its\n+  # bounds); absent -> no binders. Mirrors Rian.Decl.split_forall (ADR-0042).\n+  def parse_forall(ts Vec(Tok)) Gen\n+  def parse_forall([TId(\"forall\") | rest]) := parse_tvars(rest)\n+  def parse_forall(ts) := GR([], ts)\n+\n+  def parse_tvars(ts Vec(Tok)) Gen\n+  def parse_tvars(ts)\n+    case parse_tvar(ts) do\n+      TVR(tv, [TComma | rest]) ->\n+        case parse_tvars(rest) do\n+          GR(more, r) -> GR([tv | more], r)\n+        end\n+      TVR(tv, rest) -> GR([tv], rest)\n+    end\n+  end\n+\n+  # one binder: `T` (unconstrained) or `T: Proto (+ Proto)*`.\n+  def parse_tvar(ts Vec(Tok)) TVarRes\n+  def parse_tvar([TId(name), TOp(\":\") | rest])\n+    case parse_bounds(rest) do\n+      BR(bs, r) -> TVR(TV(name, bs), r)\n+    end\n+  end\n+  def parse_tvar([TId(name) | rest]) := TVR(TV(name, []), rest)\n+  def parse_tvar(ts) := TVR(TV(\"\", []), ts)\n+\n+  # bounds after a `:` — `Proto` chained with `+`, stopping at `,`/`:=`/end.\n+  def parse_bounds(ts Vec(Tok)) BoundRes\n+  def parse_bounds([TId(p), TOp(\"+\") | rest])\n+    case parse_bounds(rest) do\n+      BR(more, r) -> BR([p | more], r)\n+    end\n+  end\n+  def parse_bounds([TId(p) | rest]) := BR([p], rest)\n+  def parse_bounds(ts) := BR([], ts)\n \n   # ── parameters: `name [cap] Type` (cap in val/iso/ref/tag, default val) ────\n   def parse_params(ts Vec(Tok)) Vec(Param)","raw_url":"https://github.com/wstein/rian-lab/raw/6c164d849cb0453ff196662256fcb0b53d4c5010/examples%2Frian%2Fselfhost_decl.rian","sha":"f39970863097b55d7a8526e5b028cefb053c57f9","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/6c164d849cb0453ff196662256fcb0b53d4c5010/lib%2Frian%2Fself_host.ex","changes":3,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fself_host.ex?ref=6c164d849cb0453ff196662256fcb0b53d4c5010","deletions":1,"filename":"lib/rian/self_host.ex","patch":"@@ -52,7 +52,8 @@ defmodule Rian.SelfHost do\n       status: :partial,\n       source: \"selfhost_decl.rian\",\n       test: \"test/rian/decl_fixpoint_test.exs\",\n-      note: \"IR equals Rian.Decl over type/struct/mod/def slice; alias/protocol/generics remain\"\n+      note:\n+        \"IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042); alias/protocol/doc-comments remain\"\n     },\n     %{\n       id: :expr_parser,","raw_url":"https://github.com/wstein/rian-lab/raw/6c164d849cb0453ff196662256fcb0b53d4c5010/lib%2Frian%2Fself_host.ex","sha":"457fd17559c1ad220d14aab17a29d2278f1c1d6a","status":"modified"},{"additions":40,"blob_url":"https://github.com/wstein/rian-lab/blob/6c164d849cb0453ff196662256fcb0b53d4c5010/test%2Frian%2Fdecl_fixpoint_test.exs","changes":50,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_fixpoint_test.exs?ref=6c164d849cb0453ff196662256fcb0b53d4c5010","deletions":10,"filename":"test/rian/decl_fixpoint_test.exs","patch":"@@ -16,9 +16,10 @@ defmodule Rian.DeclFixpointTest do\n   # Covers: `type` sums; `struct` records; `mod` nesting (incl. `pub def`); `def`\n   # functions (single + multi-clause); clause patterns (var/lit/ctor/wildcard/\n   # cons-list); capabilities (val/iso/ref/tag); parametric types (`Vec(T)`); list\n-  # construction; `.field` access + labeled construction; and `when` guards.\n+  # construction; `.field` access + labeled construction; `when` guards; and\n+  # `forall` generics (`Ret forall T, U: Eq + Ord` -> Func.tvars/Func.bounds).\n   # Remaining long tail of `Rian.Decl`: string/char clause patterns, `alias`/\n-  # `protocol`/generics/doc-comments, and the portable stdlib breadth — see ADR-0063.\n+  # `protocol`/doc-comments, and the portable stdlib breadth — see ADR-0063.\n \n   setup_all do\n     {:ok, fe} = Beam.load(File.read!(\"examples/rian/selfhost_decl.rian\"), :rian_decl_frontend)\n@@ -105,15 +106,18 @@ defmodule Rian.DeclFixpointTest do\n \n   defp flat_e({:cons_e, h, t}), do: {[h], {:tail, ce(t)}}\n \n-  defp func(name, params, ret, clauses, pub) do\n+  # the front-end carries `forall` binders as `{:tv, name, bounds}` (ADR-0042);\n+  # project them onto Func.tvars (order-preserved) + Func.bounds (constrained only),\n+  # mirroring Rian.Decl.split_forall.\n+  defp func(name, params, ret, clauses, pub, tvs) do\n     %Func{\n       name: name,\n       params: Enum.map(params, &param/1),\n       ret: ret,\n       clauses: clauses,\n       pub?: pub,\n-      tvars: [],\n-      bounds: %{},\n+      tvars: Enum.map(tvs, fn {:tv, n, _} -> n end),\n+      bounds: for({:tv, n, bs} <- tvs, bs != [], into: %{}, do: {n, bs}),\n       doc: nil,\n       synthetic: false,\n       test?: false,\n@@ -150,18 +154,18 @@ defmodule Rian.DeclFixpointTest do\n   defp group([{:d_struct, _, _} = s | rest]), do: [to_struct(s) | group(rest)]\n   defp group([{:d_mod, _, _} = m | rest]), do: [to_mod(m) | group(rest)]\n \n-  defp group([{:d_func, pub, name, params, ret, body} | rest]) do\n+  defp group([{:d_func, pub, name, params, ret, tvs, body} | rest]) do\n     pats = Enum.map(params, fn {:par, n, _, _} -> {:var, n} end)\n-    [func(name, params, ret, [clause(pats, body)], pub) | group(rest)]\n+    [func(name, params, ret, [clause(pats, body)], pub, tvs) | group(rest)]\n   end\n \n-  defp group([{:d_sig, pub, name, params, ret} | rest]) do\n+  defp group([{:d_sig, pub, name, params, ret, tvs} | rest]) do\n     {cls, rest2} =\n       Enum.split_while(rest, fn d ->\n         match?({:d_clause, ^name, _, _}, d) or match?({:d_clause_g, ^name, _, _, _}, d)\n       end)\n \n-    [func(name, params, ret, Enum.map(cls, &to_clause/1), pub) | group(rest2)]\n+    [func(name, params, ret, Enum.map(cls, &to_clause/1), pub, tvs) | group(rest2)]\n   end\n \n   defp to_clause({:d_clause, _, pats, body}), do: clause(pats, body)\n@@ -236,7 +240,13 @@ defmodule Rian.DeclFixpointTest do\n       \"def origin() Point := Point(x: 0, y: 0)\",\n     # mod with a pub def\n     \"mod Calc do\\n  pub def double(n Int64) Int64 := n * 2\\nend\",\n-    \"mod M do\\n  type T := A | B\\n  pub def f(n Int64) Int64 := n + 1\\nend\"\n+    \"mod M do\\n  type T := A | B\\n  pub def f(n Int64) Int64 := n + 1\\nend\",\n+    # generics — `forall` binders (ADR-0042): unconstrained, bounded, multi-tvar\n+    \"def id(x val T) T forall T := x\",\n+    \"def cmp(a val T, b val T) Bool forall T: Eq := a == b\",\n+    \"def srt(xs val Vec(T)) Vec(T) forall T: Ord + Eq\\ndef srt(xs) := xs\",\n+    \"def pair(a val A, b val B) A forall A, B := a\",\n+    \"def head(xs val Vec(T)) T forall T\\ndef head([h | _]) := h\"\n   ]\n \n   defp norm_mod(m), do: %{m | funcs: Enum.map(m.funcs, &norm_func/1)}\n@@ -381,5 +391,25 @@ defmodule Rian.DeclFixpointTest do\n       # quad calls double — a local call within the compiled mod\n       assert Calc.quad(5) == 20\n     end\n+\n+    test \"a GENERIC function (forall binders, tvars erased on the BEAM) runs\", %{frontend: fe} do\n+      # the front-end parses `forall T` / `forall T: Eq`; on the BEAM generics are\n+      # erased, so a polymorphic identity + list-head compile and run unchanged.\n+      src = \"\"\"\n+      def id(x val T) T forall T := x\n+      def head(xs val Vec(T)) T forall T\n+      def head([h | _]) := h\n+      \"\"\"\n+\n+      prog = front_decls(fe, src) |> to_prog()\n+      # the binders survived into the IR (not silently dropped)\n+      assert Enum.find(prog.funcs, &(&1.name == \"id\")).tvars == [\"T\"]\n+      assert Enum.find(prog.funcs, &(&1.name == \"head\")).tvars == [\"T\"]\n+\n+      {:ok, mod} = Beam.load_ir(prog, :RianStage2Generic)\n+      assert mod.id(42) == 42\n+      assert mod.id({:a, :b}) == {:a, :b}\n+      assert mod.head([7, 8, 9]) == 7\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/6c164d849cb0453ff196662256fcb0b53d4c5010/test%2Frian%2Fdecl_fixpoint_test.exs","sha":"c53ac41b7277cf846d6bbc8df1e705d545f4770e","status":"modified"}]}