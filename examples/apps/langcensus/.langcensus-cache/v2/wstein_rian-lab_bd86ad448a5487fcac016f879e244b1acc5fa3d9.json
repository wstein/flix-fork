{"files":[{"additions":48,"blob_url":"https://github.com/wstein/rian-lab/blob/bd86ad448a5487fcac016f879e244b1acc5fa3d9/lib%2Fmix%2Ftasks%2Frian.tour.ex","changes":48,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Fmix%2Ftasks%2Frian.tour.ex?ref=bd86ad448a5487fcac016f879e244b1acc5fa3d9","deletions":0,"filename":"lib/mix/tasks/rian.tour.ex","patch":"@@ -0,0 +1,48 @@\n+defmodule Mix.Tasks.Rian.Tour do\n+  @shortdoc \"Regenerate site/src/data/tour.json from the real emitters\"\n+\n+  @moduledoc \"\"\"\n+  Regenerate the **\"Rian by Example\"** dataset the site consumes.\n+\n+      mix rian.tour [--check]\n+\n+  `Rian.Tour.generate/0` runs the real Rust/JS/Kotlin emitters and the\n+  reachability analysis over the curated tour programs and writes the result to\n+  `site/src/data/tour.json`. The site pages (by-example, homepage, playground,\n+  docs reader) import that file, so the per-target code they show is exactly what\n+  the compiler produces — it cannot drift from the language.\n+\n+    * (no flags)  write `site/src/data/tour.json`\n+    * `--check`   exit non-zero if the committed file is stale (CI / `Rian.TourTest`\n+                  runs the same comparison)\n+  \"\"\"\n+  use Mix.Task\n+\n+  @path \"site/src/data/tour.json\"\n+\n+  @impl Mix.Task\n+  def run(args) do\n+    {opts, _argv, _} = OptionParser.parse(args, strict: [check: :boolean])\n+    Mix.Task.run(\"compile\")\n+\n+    json = Rian.Tour.to_json() <> \"\\n\"\n+\n+    if opts[:check] do\n+      check(json)\n+    else\n+      File.mkdir_p!(Path.dirname(@path))\n+      File.write!(@path, json)\n+      Mix.shell().info(\"wrote #{@path} (#{byte_size(json)} bytes)\")\n+    end\n+  end\n+\n+  defp check(json) do\n+    case File.read(@path) do\n+      {:ok, ^json} ->\n+        Mix.shell().info(\"#{@path} is up to date ✓\")\n+\n+      _ ->\n+        Mix.raise(\"#{@path} is stale — run `mix rian.tour` and commit the result\")\n+    end\n+  end\n+end","raw_url":"https://github.com/wstein/rian-lab/raw/bd86ad448a5487fcac016f879e244b1acc5fa3d9/lib%2Fmix%2Ftasks%2Frian.tour.ex","sha":"fc88556ea21f4c7c0c9519ad092bdd3108e0962b","status":"added"},{"additions":237,"blob_url":"https://github.com/wstein/rian-lab/blob/bd86ad448a5487fcac016f879e244b1acc5fa3d9/lib%2Frian%2Ftour.ex","changes":237,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Ftour.ex?ref=bd86ad448a5487fcac016f879e244b1acc5fa3d9","deletions":0,"filename":"lib/rian/tour.ex","patch":"@@ -0,0 +1,237 @@\n+defmodule Rian.Tour do\n+  @moduledoc \"\"\"\n+  The compiler-derived data behind the **\"Rian by Example\"** tour page\n+  (`site/src/pages/by-example.astro`) and the homepage/playground that reuse it.\n+\n+  Each cell is a small, self-contained Rian program. `generate/0` runs the *real*\n+  emitters — `Rian.Lower.rust_program/1` (Rust), `Rian.JS.compile/1` (ECMAScript),\n+  `Rian.JVM.compile/1` (Kotlin), and the Elixir-text debug view via\n+  `Rian.Decl.compile/1` — together with the reachability analysis\n+  (`Rian.Reach.analyze/1`). So the per-target code the site shows is exactly what\n+  this compiler produces, never a hand-transcribed approximation: the page cannot\n+  drift from the language.\n+\n+  The committed `site/src/data/tour.json` is the JSON of `generate/0`; the\n+  `mix rian.tour` task regenerates it and `Rian.TourTest` fails the build if the\n+  committed file and `generate/0` ever disagree.\n+\n+  The data is JSON-shaped (string keys, lists where order matters), so a decoded\n+  `tour.json` compares equal to `generate/0` directly.\n+  \"\"\"\n+\n+  alias Rian.{Decl, JS, JVM, Lower, Reach}\n+\n+  @targets ~w(ex rs js jvm)\n+\n+  # Ordered tour cells. Each is a minimal program that teaches one idea and\n+  # reaches all four targets, so every pane is real emitted code.\n+  @cells [\n+    %{\n+      id: \"basics\",\n+      title: \"The basics\",\n+      file: \"twice.rian\",\n+      blurb:\n+        \"Rian is expression-oriented and `:=` is single-assignment. A typed \" <>\n+          \"head is the type boundary; the body is the value.\",\n+      covers: [\"def\", \":=\", \"type ascription\"],\n+      source: \"\"\"\n+      # expression-oriented · := single assignment\n+      def twice(n Int53) Int53 := n * 2\n+      \"\"\"\n+    },\n+    %{\n+      id: \"types\",\n+      title: \"Types & match\",\n+      file: \"area.rian\",\n+      blurb:\n+        \"A sealed sum and clause-per-constructor dispatch. The same definition \" <>\n+          \"becomes a tagged match on the BEAM, an `enum` + `match` on Rust, a tag \" <>\n+          \"switch on JS, and a sealed `when` on Kotlin.\",\n+      covers: [\"type\", \"constructors\", \"pattern clauses\"],\n+      source: \"\"\"\n+      type Shape := Circle(radius Float64) | Square(side Float64)\n+\n+      def area(s val Shape) Float64\n+      def area(Circle(r)) := 3.14159 * r * r\n+      def area(Square(s)) := s * s\n+      \"\"\"\n+    },\n+    %{\n+      id: \"clauses\",\n+      title: \"Clauses & guards\",\n+      file: \"classify.rian\",\n+      blurb:\n+        \"Ordered clauses with a restricted guard sublanguage. The Maranget \" <>\n+          \"exhaustiveness gate proves the set total before any target is emitted — \" <>\n+          \"a missing case is a compile error, not a runtime surprise.\",\n+      covers: [\"when\", \"literal patterns\", \"exhaustiveness\"],\n+      source: \"\"\"\n+      def classify(n Int53) String\n+      def classify(n) when n < 0 := \"negative\"\n+      def classify(0) := \"zero\"\n+      def classify(n) := \"positive\"\n+      \"\"\"\n+    },\n+    %{\n+      id: \"capabilities\",\n+      title: \"Capabilities\",\n+      file: \"capability.rian\",\n+      blurb:\n+        \"A parameter's reference capability drives its Rust signature and its \" <>\n+          \"BEAM-side linearity — you never write a lifetime. `val` borrows \" <>\n+          \"(`&Shape`); `iso` owns and is consumed once (an owned `Shape`).\",\n+      covers: [\"val\", \"iso\", \"ownership\"],\n+      source: \"\"\"\n+      type Shape := Circle(radius Float64) | Square(side Float64)\n+\n+      def area(s val Shape) Float64\n+      def area(Circle(r)) := 3.14159 * r * r\n+      def area(Square(s)) := s * s\n+\n+      # `iso`: owned, consumed exactly once\n+      def consume(s iso Shape) Float64 := area(s)\n+      \"\"\"\n+    }\n+  ]\n+\n+  # Functions whose inferred reachability is NOT the full set — the honest matrix\n+  # (ADR-0057): portability is inferred, and some capabilities/FFI pin a function\n+  # to a subset. Rendered as the page's reachability strip and the homepage matrix.\n+  @reach_examples [\n+    %{\n+      name: \"area\",\n+      sig: \"area(s val Shape) Float64\",\n+      blurb: \"Pure, portable — reaches every target.\"\n+    },\n+    %{\n+      name: \"scale\",\n+      sig: \"scale(s ref Vec(Float64), k Float64) Vec(Float64)\",\n+      blurb: \"`ref` (&mut) is BEAM-rejected (no process-local proof in the PoC).\",\n+      defs: \"\"\"\n+      def mul_each(xs val Vec(Float64), k Float64) Vec(Float64) := xs\n+      def scale(s ref Vec(Float64), k Float64) Vec(Float64) := mul_each(s, k)\n+      \"\"\"\n+    },\n+    %{\n+      name: \"shout\",\n+      sig: \"shout(s String) String := String.upcase(s)\",\n+      blurb: \"Host FFI is native-per-target, so it pins to the BEAM.\",\n+      defs: \"def shout(s String) String := String.upcase(s)\\n\"\n+    }\n+  ]\n+\n+  @doc \"\"\"\n+  Build the full tour dataset as JSON-shaped Elixir terms (string keys).\n+  \"\"\"\n+  @spec generate() :: map()\n+  def generate do\n+    %{\n+      \"targets\" => @targets,\n+      \"cells\" => Enum.map(@cells, &build_cell/1),\n+      \"reachExamples\" => Enum.map(@reach_examples, &build_reach_example/1)\n+    }\n+  end\n+\n+  @doc \"Render `generate/0` as deterministic, pretty-printed JSON (sorted keys).\"\n+  @spec to_json() :: String.t()\n+  def to_json, do: generate() |> encode(0) |> IO.iodata_to_binary()\n+\n+  defp build_cell(%{id: id, source: source} = cell) do\n+    src = String.trim_trailing(source) <> \"\\n\"\n+    prog = Decl.parse(src)\n+    reach = reach_map(prog)\n+\n+    %{\n+      \"id\" => id,\n+      \"title\" => cell.title,\n+      \"file\" => cell.file,\n+      \"blurb\" => cell.blurb,\n+      \"covers\" => cell.covers,\n+      \"source\" => String.trim_trailing(src),\n+      \"panes\" => %{\n+        \"ex\" => elixir_module(src),\n+        \"rs\" => Lower.rust_program(prog),\n+        \"js\" => String.trim_trailing(JS.compile(src)),\n+        \"jvm\" => String.trim_trailing(JVM.compile(src))\n+      },\n+      \"reach\" => reach\n+    }\n+  end\n+\n+  defp build_reach_example(%{name: name, sig: sig, blurb: blurb} = ex) do\n+    src = Map.get(ex, :defs, \"def #{name}() Int53 := 0\\n\")\n+    reach = src |> Decl.parse() |> reach_map() |> Map.fetch!(name)\n+    %{\"name\" => name, \"sig\" => sig, \"blurb\" => blurb, \"reach\" => reach}\n+  end\n+\n+  defp reach_map(prog) do\n+    prog\n+    |> Reach.analyze()\n+    |> Map.new(fn {name, %{reach: reach}} ->\n+      {name, reach |> MapSet.to_list() |> Enum.map(&to_string/1) |> Enum.sort()}\n+    end)\n+  end\n+\n+  # Assemble the per-function Elixir-text debug view into one module, emitting the\n+  # shared `@type` declarations once.\n+  defp elixir_module(src) do\n+    units =\n+      for {_name, out} <- Decl.compile(src), out[:elixir], do: out[:elixir]\n+\n+    {type_lines, body_blocks} =\n+      Enum.reduce(units, {[], []}, fn text, {types, blocks} ->\n+        {ts, rest} = text |> String.split(\"\\n\") |> Enum.split_with(&String.starts_with?(&1, \"@\"))\n+        {types ++ ts, blocks ++ [Enum.join(rest, \"\\n\") |> String.trim()]}\n+      end)\n+\n+    [Enum.uniq(type_lines), Enum.reject(body_blocks, &(&1 == \"\"))]\n+    |> List.flatten()\n+    |> Enum.join(\"\\n\")\n+    |> String.trim()\n+  end\n+\n+  # ── minimal deterministic JSON encoder (sorted object keys, 2-space indent) ──\n+\n+  defp encode(map, indent) when is_map(map) do\n+    pad = String.duplicate(\"  \", indent + 1)\n+    close = String.duplicate(\"  \", indent)\n+\n+    case map |> Map.keys() |> Enum.sort() do\n+      [] ->\n+        \"{}\"\n+\n+      keys ->\n+        body =\n+          Enum.map_join(keys, \",\\n\", fn k ->\n+            [pad, encode_string(to_string(k)), \": \", encode(Map.fetch!(map, k), indent + 1)]\n+          end)\n+\n+        [\"{\\n\", body, \"\\n\", close, \"}\"]\n+    end\n+  end\n+\n+  defp encode([], _indent), do: \"[]\"\n+\n+  defp encode(list, indent) when is_list(list) do\n+    pad = String.duplicate(\"  \", indent + 1)\n+    close = String.duplicate(\"  \", indent)\n+    body = Enum.map_join(list, \",\\n\", fn v -> [pad, encode(v, indent + 1)] end)\n+    [\"[\\n\", body, \"\\n\", close, \"]\"]\n+  end\n+\n+  defp encode(s, _indent) when is_binary(s), do: encode_string(s)\n+  defp encode(b, _indent) when is_boolean(b), do: to_string(b)\n+  defp encode(n, _indent) when is_integer(n), do: Integer.to_string(n)\n+\n+  defp encode_string(s) do\n+    escaped =\n+      s\n+      |> String.replace(\"\\\\\", \"\\\\\\\\\")\n+      |> String.replace(\"\\\"\", \"\\\\\\\"\")\n+      |> String.replace(\"\\n\", \"\\\\n\")\n+      |> String.replace(\"\\t\", \"\\\\t\")\n+      |> String.replace(\"\\r\", \"\\\\r\")\n+\n+    [?\", escaped, ?\"]\n+  end\n+end","raw_url":"https://github.com/wstein/rian-lab/raw/bd86ad448a5487fcac016f879e244b1acc5fa3d9/lib%2Frian%2Ftour.ex","sha":"ce1de2f7cb7d474d898a0de90c01446cba32ae97","status":"added"},{"additions":151,"blob_url":"https://github.com/wstein/rian-lab/blob/bd86ad448a5487fcac016f879e244b1acc5fa3d9/site%2Fsrc%2Fdata%2Ftour.json","changes":151,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fdata%2Ftour.json?ref=bd86ad448a5487fcac016f879e244b1acc5fa3d9","deletions":0,"filename":"site/src/data/tour.json","patch":"@@ -0,0 +1,151 @@\n+{\n+  \"cells\": [\n+    {\n+      \"blurb\": \"Rian is expression-oriented and `:=` is single-assignment. A typed head is the type boundary; the body is the value.\",\n+      \"covers\": [\n+        \"def\",\n+        \":=\",\n+        \"type ascription\"\n+      ],\n+      \"file\": \"twice.rian\",\n+      \"id\": \"basics\",\n+      \"panes\": {\n+        \"ex\": \"def twice(n) do n * 2 end\",\n+        \"js\": \"function twice(a0) {\\n  { const n = a0; return (n * 2); }\\n  throw new Error(\\\"twice: no clause matched\\\");\\n}\",\n+        \"jvm\": \"private fun twice(a0: Long): Long {\\n  val n = a0; return (n * 2L)\\n}\",\n+        \"rs\": \"fn twice(n: i64) -> i64 {\\n    match n {\\n        n => n * 2,\\n    }\\n}\"\n+      },\n+      \"reach\": {\n+        \"twice\": [\n+          \"ex\",\n+          \"js\",\n+          \"jvm\",\n+          \"rs\"\n+        ]\n+      },\n+      \"source\": \"# expression-oriented · := single assignment\\ndef twice(n Int53) Int53 := n * 2\",\n+      \"title\": \"The basics\"\n+    },\n+    {\n+      \"blurb\": \"A sealed sum and clause-per-constructor dispatch. The same definition becomes a tagged match on the BEAM, an `enum` + `match` on Rust, a tag switch on JS, and a sealed `when` on Kotlin.\",\n+      \"covers\": [\n+        \"type\",\n+        \"constructors\",\n+        \"pattern clauses\"\n+      ],\n+      \"file\": \"area.rian\",\n+      \"id\": \"types\",\n+      \"panes\": {\n+        \"ex\": \"@type shape :: {:circle, float()} | {:square, float()}\\ndef area({:circle, r}) do 3.14159 * r * r end\\ndef area({:square, s}) do s * s end\",\n+        \"js\": \"function area(a0) {\\n  { if (a0[0] === \\\"Circle\\\") { const r = a0[1]; return ((3.14159 * r) * r); } }\\n  { if (a0[0] === \\\"Square\\\") { const s = a0[1]; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\",\n+        \"jvm\": \"sealed interface Shape\\ndata class Circle(val f0: Double) : Shape\\ndata class Square(val f0: Double) : Shape\\n\\nprivate fun area(a0: Shape): Double {\\n  if (a0 is Circle) { val r = a0.f0; return ((3.14159 * r) * r) }\\n  if (a0 is Square) { val s = a0.f0; return (s * s) }\\n  throw RuntimeException(\\\"area: no clause matched\\\")\\n}\",\n+        \"rs\": \"#[derive(Clone, Debug, PartialEq)]\\nenum Shape {\\n    Circle { radius: f64 },\\n    Square { side: f64 },\\n}\\n\\nfn area(s: &Shape) -> f64 {\\n    match s {\\n        Shape::Circle { radius: r } => 3.14159 * r * r,\\n        Shape::Square { side: s } => s * s,\\n    }\\n}\"\n+      },\n+      \"reach\": {\n+        \"area\": [\n+          \"ex\",\n+          \"js\",\n+          \"jvm\",\n+          \"rs\"\n+        ]\n+      },\n+      \"source\": \"type Shape := Circle(radius Float64) | Square(side Float64)\\n\\ndef area(s val Shape) Float64\\ndef area(Circle(r)) := 3.14159 * r * r\\ndef area(Square(s)) := s * s\",\n+      \"title\": \"Types & match\"\n+    },\n+    {\n+      \"blurb\": \"Ordered clauses with a restricted guard sublanguage. The Maranget exhaustiveness gate proves the set total before any target is emitted — a missing case is a compile error, not a runtime surprise.\",\n+      \"covers\": [\n+        \"when\",\n+        \"literal patterns\",\n+        \"exhaustiveness\"\n+      ],\n+      \"file\": \"classify.rian\",\n+      \"id\": \"clauses\",\n+      \"panes\": {\n+        \"ex\": \"def classify(n) when n < 0 do \\\"negative\\\" end\\ndef classify(0) do \\\"zero\\\" end\\ndef classify(n) do \\\"positive\\\" end\",\n+        \"js\": \"function classify(a0) {\\n  { const n = a0; if ((n < 0)) { return \\\"negative\\\"; } }\\n  { if (a0 === 0) { return \\\"zero\\\"; } }\\n  { const n = a0; return \\\"positive\\\"; }\\n  throw new Error(\\\"classify: no clause matched\\\");\\n}\",\n+        \"jvm\": \"private fun classify(a0: Long): String {\\n  run { val n = a0; if ((n < 0L)) { return \\\"negative\\\" } }\\n  if (a0 == 0L) { return \\\"zero\\\" }\\n  val n = a0; return \\\"positive\\\"\\n}\",\n+        \"rs\": \"fn classify(n: i64) -> String {\\n    match n {\\n        n if n < 0 => (\\\"negative\\\").to_string(),\\n        0 => (\\\"zero\\\").to_string(),\\n        n => (\\\"positive\\\").to_string(),\\n    }\\n}\"\n+      },\n+      \"reach\": {\n+        \"classify\": [\n+          \"ex\",\n+          \"js\",\n+          \"jvm\",\n+          \"rs\"\n+        ]\n+      },\n+      \"source\": \"def classify(n Int53) String\\ndef classify(n) when n < 0 := \\\"negative\\\"\\ndef classify(0) := \\\"zero\\\"\\ndef classify(n) := \\\"positive\\\"\",\n+      \"title\": \"Clauses & guards\"\n+    },\n+    {\n+      \"blurb\": \"A parameter's reference capability drives its Rust signature and its BEAM-side linearity — you never write a lifetime. `val` borrows (`&Shape`); `iso` owns and is consumed once (an owned `Shape`).\",\n+      \"covers\": [\n+        \"val\",\n+        \"iso\",\n+        \"ownership\"\n+      ],\n+      \"file\": \"capability.rian\",\n+      \"id\": \"capabilities\",\n+      \"panes\": {\n+        \"ex\": \"@type shape :: {:circle, float()} | {:square, float()}\\ndef area({:circle, r}) do 3.14159 * r * r end\\ndef area({:square, s}) do s * s end\\ndef consume(s) do area(s) end\",\n+        \"js\": \"function area(a0) {\\n  { if (a0[0] === \\\"Circle\\\") { const r = a0[1]; return ((3.14159 * r) * r); } }\\n  { if (a0[0] === \\\"Square\\\") { const s = a0[1]; return (s * s); } }\\n  throw new Error(\\\"area: no clause matched\\\");\\n}\\n\\nfunction consume(a0) {\\n  { const s = a0; return area(s); }\\n  throw new Error(\\\"consume: no clause matched\\\");\\n}\",\n+        \"jvm\": \"sealed interface Shape\\ndata class Circle(val f0: Double) : Shape\\ndata class Square(val f0: Double) : Shape\\n\\nprivate fun area(a0: Shape): Double {\\n  if (a0 is Circle) { val r = a0.f0; return ((3.14159 * r) * r) }\\n  if (a0 is Square) { val s = a0.f0; return (s * s) }\\n  throw RuntimeException(\\\"area: no clause matched\\\")\\n}\\n\\nprivate fun consume(a0: Shape): Double {\\n  val s = a0; return area(s)\\n}\",\n+        \"rs\": \"#[derive(Clone, Debug, PartialEq)]\\nenum Shape {\\n    Circle { radius: f64 },\\n    Square { side: f64 },\\n}\\n\\nfn area(s: &Shape) -> f64 {\\n    match s {\\n        Shape::Circle { radius: r } => 3.14159 * r * r,\\n        Shape::Square { side: s } => s * s,\\n    }\\n}\\n\\nfn consume(s: Shape) -> f64 {\\n    match s {\\n        s => area(s),\\n    }\\n}\"\n+      },\n+      \"reach\": {\n+        \"area\": [\n+          \"ex\",\n+          \"js\",\n+          \"jvm\",\n+          \"rs\"\n+        ],\n+        \"consume\": [\n+          \"ex\",\n+          \"js\",\n+          \"jvm\",\n+          \"rs\"\n+        ]\n+      },\n+      \"source\": \"type Shape := Circle(radius Float64) | Square(side Float64)\\n\\ndef area(s val Shape) Float64\\ndef area(Circle(r)) := 3.14159 * r * r\\ndef area(Square(s)) := s * s\\n\\n# `iso`: owned, consumed exactly once\\ndef consume(s iso Shape) Float64 := area(s)\",\n+      \"title\": \"Capabilities\"\n+    }\n+  ],\n+  \"reachExamples\": [\n+    {\n+      \"blurb\": \"Pure, portable — reaches every target.\",\n+      \"name\": \"area\",\n+      \"reach\": [\n+        \"ex\",\n+        \"js\",\n+        \"jvm\",\n+        \"rs\"\n+      ],\n+      \"sig\": \"area(s val Shape) Float64\"\n+    },\n+    {\n+      \"blurb\": \"`ref` (&mut) is BEAM-rejected (no process-local proof in the PoC).\",\n+      \"name\": \"scale\",\n+      \"reach\": [\n+        \"js\",\n+        \"jvm\",\n+        \"rs\"\n+      ],\n+      \"sig\": \"scale(s ref Vec(Float64), k Float64) Vec(Float64)\"\n+    },\n+    {\n+      \"blurb\": \"Host FFI is native-per-target, so it pins to the BEAM.\",\n+      \"name\": \"shout\",\n+      \"reach\": [\n+        \"ex\"\n+      ],\n+      \"sig\": \"shout(s String) String := String.upcase(s)\"\n+    }\n+  ],\n+  \"targets\": [\n+    \"ex\",\n+    \"rs\",\n+    \"js\",\n+    \"jvm\"\n+  ]\n+}","raw_url":"https://github.com/wstein/rian-lab/raw/bd86ad448a5487fcac016f879e244b1acc5fa3d9/site%2Fsrc%2Fdata%2Ftour.json","sha":"abb545331b262c2524376fc612ebec39a309bf58","status":"added"},{"additions":62,"blob_url":"https://github.com/wstein/rian-lab/blob/bd86ad448a5487fcac016f879e244b1acc5fa3d9/test%2Frian%2Ftour_test.exs","changes":62,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ftour_test.exs?ref=bd86ad448a5487fcac016f879e244b1acc5fa3d9","deletions":0,"filename":"test/rian/tour_test.exs","patch":"@@ -0,0 +1,62 @@\n+defmodule Rian.TourTest do\n+  use ExUnit.Case, async: true\n+\n+  alias Rian.Tour\n+\n+  @json_path Path.expand(\"../../site/src/data/tour.json\", __DIR__)\n+\n+  describe \"generate/0 derives every pane from the real emitters\" do\n+    setup do\n+      %{data: Tour.generate()}\n+    end\n+\n+    test \"every cell reaches all four targets and emits non-empty code per pane\", %{data: data} do\n+      assert data[\"targets\"] == ~w(ex rs js jvm)\n+      assert length(data[\"cells\"]) == 4\n+\n+      for cell <- data[\"cells\"] do\n+        for t <- ~w(ex rs js jvm) do\n+          assert is_binary(cell[\"panes\"][t]) and cell[\"panes\"][t] != \"\",\n+                 \"#{cell[\"id\"]}/#{t} pane is empty\"\n+        end\n+\n+        for {fn_name, reach} <- cell[\"reach\"] do\n+          assert Enum.sort(reach) == ~w(ex js jvm rs),\n+                 \"#{cell[\"id\"]}: #{fn_name} should reach all four targets, got #{inspect(reach)}\"\n+        end\n+      end\n+    end\n+\n+    test \"the capabilities cell shows the val-borrow vs iso-owned Rust distinction\", %{data: data} do\n+      rust = cell(data, \"capabilities\")[\"panes\"][\"rs\"]\n+      assert rust =~ \"fn area(s: &Shape)\"\n+      assert rust =~ \"fn consume(s: Shape)\"\n+    end\n+\n+    test \"the clauses cell carries the JVM guard fix (no invalid empty `if ()`)\", %{data: data} do\n+      jvm = cell(data, \"clauses\")[\"panes\"][\"jvm\"]\n+      refute jvm =~ \"if ()\"\n+      assert jvm =~ \"run { val n = a0; if ((n < 0L)) { return \\\"negative\\\" } }\"\n+    end\n+\n+    test \"the reachability strip is the honest, inferred matrix (ADR-0057)\", %{data: data} do\n+      by_name = Map.new(data[\"reachExamples\"], &{&1[\"name\"], &1[\"reach\"]})\n+\n+      assert Enum.sort(by_name[\"area\"]) == ~w(ex js jvm rs)\n+      # `ref` is BEAM-rejected — off :ex, but still reaches the others\n+      refute \"ex\" in by_name[\"scale\"]\n+      assert Enum.sort(by_name[\"scale\"]) == ~w(js jvm rs)\n+      # host FFI is native-per-target — pinned to the BEAM\n+      assert by_name[\"shout\"] == [\"ex\"]\n+    end\n+  end\n+\n+  test \"the committed tour.json is up to date (run `mix rian.tour` if this fails)\" do\n+    committed = @json_path |> File.read!() |> :json.decode()\n+\n+    assert committed == Tour.generate(),\n+           \"site/src/data/tour.json is stale — regenerate it with `mix rian.tour`\"\n+  end\n+\n+  defp cell(data, id), do: Enum.find(data[\"cells\"], &(&1[\"id\"] == id))\n+end","raw_url":"https://github.com/wstein/rian-lab/raw/bd86ad448a5487fcac016f879e244b1acc5fa3d9/test%2Frian%2Ftour_test.exs","sha":"b81e8dcc3c889dee190ee784d8958f3e94c3f125","status":"added"}]}