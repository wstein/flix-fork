{"files":[{"additions":90,"blob_url":"https://github.com/wstein/rian-lab/blob/88124de5d1596284ac7013e933e9777b1c60e775/docs%2Fadr%2F0073-foldable-protocol.md","changes":90,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0073-foldable-protocol.md?ref=88124de5d1596284ac7013e933e9777b1c60e775","deletions":0,"filename":"docs/adr/0073-foldable-protocol.md","patch":"@@ -0,0 +1,90 @@\n+# ADR-0073 — `Foldable`: eager polymorphic reduction over a protocol\n+\n+**Status:** Accepted (direction) · the **concrete-element** form is implemented and verified on the BEAM; the **element-generic** form is deferred behind an associated-type extension. Debated 2026-06-15.\n+**Implemented:** partial — `Foldable` + `fsum`/`fany_pos`/`fall_pos`/`fcount` over two container types (`examples/rian/foldable.rian`, `test/rian/prim_test.exs`), running on the BEAM and reach-gated honestly. Element-generic `Foldable(T)` and the lazy-iterator surface are **not** implemented (by design / pending an extension).\n+**Refines:** ADR-0047 (portable prelude — Tier 2 over the Tier-1 `List` reducers), ADR-0042 (protocols/impls/bounded generics — the mechanism this rides), ADR-0061 (multi-target protocol lowering — the reach reality below).\n+**Refs:** ADR-0057 (concurrency/laziness native-per-target — the *principle* for rejecting lazy iterators), ADR-0034 (no `nil`; totality), ADR-0064 (portable `Int53`), ADR-0035 (no hidden control flow), ADR-0000 (honesty: the matrix matches the emitters).\n+**Owners:** Arthur Pendelton (bounds / dispatch) · Maya Lin (emitters) · Elena Rostova (Rust traits / coherence) · Kira Neri (Reach honesty) · Mira (totality) · Rachel Okafor (PM)\n+\n+## Context\n+\n+`prelude_list.rian` (ADR-0047 Tier 1) provides eager reducers — `sum`/`product`/`any`/\n+`all`/`length` — over a concrete `Vec`. They reach all four targets because a list is\n+pure cons. The open question (the \"iterable-accepting tools\" ask): should those reducers\n+work over *any* container — a user `Bag`, a `Span`, `Dict` values — the way Python's\n+`sum`/`any`/`all` accept any iterable?\n+\n+Python answers with a **lazy** duck-typed iterator protocol (`__iter__`). Rian cannot\n+copy that: laziness has no portable lowering across BEAM `Stream` / Rust `Iterator` / JS\n+iterators / JVM `Sequence` (ADR-0057 already made laziness's sibling, concurrency,\n+native-per-target rather than a portable surface). What Rian *can* offer is the **typed,\n+eager** analogue: a protocol that bridges any container to a concrete list, with the\n+reducers written once over the protocol.\n+\n+## Decision\n+\n+**1. `Foldable` is a one-method protocol that materialises a container's elements.**\n+\n+```\n+protocol Foldable do\n+  def to_list(self Self) Vec(Int53)\n+end\n+```\n+\n+Reducers are written **once** against the bound and dispatch by first-arg type\n+(ADR-0042): `def fsum(x T) Int53 forall T: Foldable := sum_l(to_list(x))`. One `fsum`\n+serves `Bag` and `Span`; a non-`Foldable` argument is a compile error (`requires\n+T: Foldable`).\n+\n+**2. Eager only — no lazy `Iterator`.** `to_list` materialises. A lazy generator protocol\n+is **rejected** for the same reason ADR-0057 rejected portable concurrency: laziness is a\n+per-target *evaluation* concern, not portable sequential *logic*. A portable lazy model\n+either collapses to eager (pointless) or drags a Stream runtime into each backend. Write\n+lazy pipelines in the native, target-pinned region instead.\n+\n+**3. The reducers live in the prelude over `Foldable`, not duplicated per container.**\n+Tier 1's `List.*` are the concrete folds `to_list` bottoms out in; `Foldable` is the thin\n+polymorphic layer above them.\n+\n+## Two honest constraints (and what they cost)\n+\n+**A. Concrete element today — no associated type.** ADR-0042/0061 protocols are\n+single-`Self` with **no associated/element type**: `Self` is one concrete type, so\n+`to_list` must name a concrete element (`Vec(Int53)` here). A fully element-generic\n+`Foldable(T)` — `to_list(self Self) Vec(Elem)` with `Elem` an associated type — is\n+**deferred**; it needs a protocol-mechanism extension (associated types or higher-kinded\n+`Self`). The concrete-element form is the implementable subset and is what ships.\n+\n+**B. Sum-dispatch reaches `[:ex, :js]`, not Rust/JVM — and that is consistent, not new.**\n+Dispatching a protocol over **sum types** encodes the constructor tag as a bare atom in\n+the BEAM/JS dispatcher, which `Rian.Reach` pins off `:rs`/`:jvm` (the `bare atom literal`\n+blocker, ADR-0061). Measured:\n+\n+| dispatch over | example | reach |\n+|---|---|---|\n+| primitives (`Int53`/`Bool`/`String`) | `Eq`/`Ord`, `contains` | `:ex :rs :js :jvm` |\n+| **sum types** | `Show`-over-`Expr`, **`Foldable`-over-`Bag`** | `:ex :js` |\n+\n+So `Foldable` over user sums has **exactly** the reach of the already-shipped\n+`Show`-over-`Expr` — no better, no worse. The underlying concrete fold (`sum_l`) is\n+all-target; only the dispatcher gates it. (Closing this — letting the Rust trait path's\n+sum dispatch count in Reach — is ADR-0061's dispatcher-reach item, out of scope here.)\n+\n+## Alternatives considered\n+\n+- **A `fold(self, acc B, combine Fn(B,A)→B)` method** (the Haskell `Foldable`): more\n+  general, but puts a **higher-order function in the protocol method**, which complicates\n+  the Rust lowering (closures-as-values) and adds reach risk for little gain over the\n+  eager `to_list` bridge. Rejected for v1; revisit with the associated-type extension.\n+- **A lazy `Iterator` protocol** (Python/Rust): rejected — see Decision §2.\n+- **Duplicate `sum`/`any`/`all` per container type**: rejected — that is the boilerplate\n+  `Foldable` exists to remove.\n+\n+## Consequences\n+\n+- One reducer set, many containers — the eager, typed \"iterable-accepting\" ergonomic,\n+  total (each reducer has an empty identity; no `Option`, no crash), bound-enforced.\n+- Honest portability: all-target when impl'd for primitives; `[:ex, :js]` when impl'd for\n+  sums (matching `Show`). Documented, reach-gated, not silently degraded.\n+- A clear, named extension path: associated types unlock element-generic `Foldable(T)`\n+  and fold over `Dict` values / arbitrary `T` containers.","raw_url":"https://github.com/wstein/rian-lab/raw/88124de5d1596284ac7013e933e9777b1c60e775/docs%2Fadr%2F0073-foldable-protocol.md","sha":"734c119c79a5c747f9d34ed0feeff7716b4c33da","status":"added"},{"additions":8,"blob_url":"https://github.com/wstein/rian-lab/blob/88124de5d1596284ac7013e933e9777b1c60e775/examples%2Frian%2FREADME.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2FREADME.md?ref=88124de5d1596284ac7013e933e9777b1c60e775","deletions":0,"filename":"examples/rian/README.md","patch":"@@ -390,6 +390,14 @@ in the real file, so it can't drift into a toy). **Honest scope:** the loop is\n   `mix rian.targets`). A *lazy* generator/`Iterator` protocol is deliberately NOT\n   provided — laziness is a per-target evaluation concern, native like concurrency\n   (ADR-0057), not portable sequential logic.\n+- [foldable.rian](foldable.rian) — **Tier 2: `Foldable`, eager polymorphic reduction\n+  over a protocol (ADR-0073).** A one-method protocol (`to_list(self) Vec(Int53)`)\n+  bridges any container to a list, so `fsum`/`fany_pos`/`fall_pos`/`fcount` are written\n+  ONCE and dispatch over `Bag` AND `Span` by runtime type (ADR-0042). Eager — no lazy\n+  `Iterator` (rejected, ADR-0057). Honest limits: the protocol is single-`Self` (no\n+  associated type), so the element is concrete `Int53` today (element-generic deferred);\n+  and a sum-dispatch consumer reaches `[:ex, :js]` (the constructor-tag atom pins off\n+  `:rs`/`:jvm`) — exactly the reach of the shipped `Show`-over-`Expr`. Verified on BEAM.\n \n See [SELFHOST.md](../../SELFHOST.md) for the blocker ledger they produced.\n ","raw_url":"https://github.com/wstein/rian-lab/raw/88124de5d1596284ac7013e933e9777b1c60e775/examples%2Frian%2FREADME.md","sha":"da055d93d4c647c111f9f10521567dd2465101de","status":"modified"},{"additions":73,"blob_url":"https://github.com/wstein/rian-lab/blob/88124de5d1596284ac7013e933e9777b1c60e775/examples%2Frian%2Ffoldable.rian","changes":73,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Ffoldable.rian?ref=88124de5d1596284ac7013e933e9777b1c60e775","deletions":0,"filename":"examples/rian/foldable.rian","patch":"@@ -0,0 +1,73 @@\n+# ===========================================================================\n+# `Foldable` — EAGER polymorphic reduction over a protocol (ADR-0073 / ADR-0042)\n+# ===========================================================================\n+# Tier 1 (`prelude_list.rian`) gave `sum`/`any`/`all`/`length` over a concrete\n+# `Vec`. Tier 2 lifts them to ANY container that can produce its elements, via a\n+# one-method protocol — the eager, typed analogue of Python's \"any iterable\":\n+#\n+#   protocol Foldable { to_list(self) -> Vec(Int53) }\n+#\n+# A reducer is then written ONCE against the protocol (`fsum`/`fany`/`fall`) and\n+# dispatches to each impl by the receiver's runtime type (ADR-0042 first-arg\n+# dispatch). `fsum(Bag([1,2,3]))` and `fsum(Sp(4,5))` call the SAME `fsum`.\n+#\n+# DELIBERATELY EAGER. `to_list` materialises — there is no lazy `Iterator`, which\n+# would need a portable lazy-sequence abstraction Rian does not provide (laziness\n+# is native-per-target, like concurrency in ADR-0057).\n+#\n+# TWO honest limits of today's protocol mechanism (ADR-0042/0061), both in the ADR:\n+#   * No associated/element type — `Self` is one concrete type — so `to_list`\n+#     fixes a CONCRETE element (`Int53`) here. An element-generic `Foldable(T)`\n+#     needs associated types (deferred).\n+#   * Dispatching over SUM types uses a constructor-tag atom in the BEAM/JS\n+#     desugaring, so `Rian.Reach` pins these dispatchers to `[:ex, :js]` (exactly\n+#     as it does the shipped `Show`-over-`Expr`); primitive-dispatch protocols\n+#     (`Eq`/`Ord`) reach all four. Verified on the BEAM.\n+# ---------------------------------------------------------------------------\n+\n+# the bridge: a container yields its elements as a concrete list.\n+protocol Foldable do\n+  def to_list(self Self) Vec(Int53)\n+end\n+\n+type Bag := Bag(items Vec(Int53))\n+type Span := Sp(lo Int53, hi Int53)\n+\n+# impl methods take a plain receiver and destructure with `case` (ADR-0042 — the\n+# dispatcher binds the receiver; the head is not a constructor pattern).\n+impl Foldable for Bag do\n+  def to_list(b) := case b do Bag(xs) -> xs end\n+end\n+\n+impl Foldable for Span do\n+  def to_list(s) := case s do Sp(a, b) -> [a, b] end\n+end\n+\n+# ── reducers written ONCE over the protocol; `forall T: Foldable` enforces the\n+#    bound at every call site (a non-Foldable type is a compile error) ─────────\n+def fsum(x T) Int53 forall T: Foldable := sum_l(to_list(x))\n+def fany_pos(x T) Bool forall T: Foldable := anypos_l(to_list(x))\n+def fall_pos(x T) Bool forall T: Foldable := allpos_l(to_list(x))\n+def fcount(x T) Int53 forall T: Foldable := len_l(to_list(x))\n+\n+# the concrete `Vec(Int53)` folds these bottom out in (cf. `List.*` in\n+# prelude_list.rian — inlined here so the example is one self-contained module).\n+def sum_l(Vec(Int53)) Int53\n+def sum_l([]) := 0\n+def sum_l([h | t]) := h + sum_l(t)\n+\n+def anypos_l(Vec(Int53)) Bool\n+def anypos_l([]) := false\n+def anypos_l([h | t]) := if h > 0 do true else anypos_l(t) end\n+\n+def allpos_l(Vec(Int53)) Bool\n+def allpos_l([]) := true\n+def allpos_l([h | t]) := if h > 0 do allpos_l(t) else false end\n+\n+def len_l(Vec(Int53)) Int53\n+def len_l([]) := 0\n+def len_l([_ | t]) := 1 + len_l(t)\n+\n+# fsum(Bag([1,2,3])) = 6 · fsum(Sp(4,5)) = 9 · fall_pos(Sp(-1,2)) = false ·\n+# fany_pos(Bag([0,0,3])) = true · fcount(Bag([1,2,3])) = 3 — one reducer set,\n+# two container types, selected by runtime shape.","raw_url":"https://github.com/wstein/rian-lab/raw/88124de5d1596284ac7013e933e9777b1c60e775/examples%2Frian%2Ffoldable.rian","sha":"2dcaac576c65184ac43bc46c020dfedb7a2531cb","status":"added"},{"additions":20,"blob_url":"https://github.com/wstein/rian-lab/blob/88124de5d1596284ac7013e933e9777b1c60e775/test%2Frian%2Fprim_test.exs","changes":20,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fprim_test.exs?ref=88124de5d1596284ac7013e933e9777b1c60e775","deletions":0,"filename":"test/rian/prim_test.exs","patch":"@@ -113,6 +113,26 @@ defmodule Rian.PrimTest do\n       end\n     end\n \n+    test \"`Foldable` protocol — one reducer set dispatches over two container types (ADR-0073)\" do\n+      src = File.read!(\"examples/rian/foldable.rian\")\n+      {:ok, m} = Beam.load(src, :rian_prim_foldable)\n+\n+      # the SAME `fsum`/`fall_pos`/`fcount` run over Bag AND Span (first-arg dispatch)\n+      assert m.fsum({:bag, [1, 2, 3]}) == 6\n+      assert m.fsum({:sp, 4, 5}) == 9\n+      assert m.fall_pos({:sp, -1, 2}) == false\n+      assert m.fany_pos({:bag, [0, 0, 3]}) == true\n+      assert m.fcount({:bag, [1, 2, 3]}) == 3\n+\n+      # reach is honest: a SUM-dispatching protocol consumer is `[:ex, :js]` (the\n+      # constructor-tag atom in the dispatcher pins off `:rs`/`:jvm`) — exactly the\n+      # reach of the shipped `Show`-over-`Expr` dispatcher, no better, no worse.\n+      rep = Rian.Reach.analyze(Rian.Decl.parse(src))\n+      assert Enum.sort(MapSet.to_list(rep[\"fsum\"].reach)) == [:ex, :js]\n+      # the underlying concrete fold IS all-target — only the dispatcher gates it.\n+      assert Enum.sort(MapSet.to_list(rep[\"sum_l\"].reach)) == [:ex, :js, :jvm, :rs]\n+    end\n+\n     test \"selfhost lexer using `Prim.char_code`/`Prim.str_chars` round-trips\" do\n       {:ok, m} =\n         Beam.load(File.read!(\"examples/rian/selfhost_lexer.rian\"), :rian_prim_selfhost_lex)","raw_url":"https://github.com/wstein/rian-lab/raw/88124de5d1596284ac7013e933e9777b1c60e775/test%2Frian%2Fprim_test.exs","sha":"adcba3b3af41c9811ec07d7b9c5c55efb7b3fa40","status":"modified"}]}