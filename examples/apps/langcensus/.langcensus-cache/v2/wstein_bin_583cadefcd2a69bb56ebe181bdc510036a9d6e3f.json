{"files":[{"additions":26,"blob_url":"https://github.com/wstein/bin/blob/583cadefcd2a69bb56ebe181bdc510036a9d6e3f/list-vscode-extensions","changes":64,"contents_url":"https://api.github.com/repos/wstein/bin/contents/list-vscode-extensions?ref=583cadefcd2a69bb56ebe181bdc510036a9d6e3f","deletions":38,"filename":"list-vscode-extensions","patch":"@@ -26,6 +26,12 @@ extensions = `code --list-extensions`.split(\"\\n\").map(&:strip).reject(&:empty?)\n \n # Query VS Marketplace for extension details\n def fetch_extension_details(extension_id)\n+\n+  ext = {\n+\t\t'id' => extension_id,\n+\t\t'marketplace' => \"https://marketplace.visualstudio.com/items?itemName=#{extension_id}\"\n+\t  }\n+\n   uri = URI('https://marketplace.visualstudio.com/_apis/public/gallery/extensionquery')\n   req = Net::HTTP::Post.new(uri)\n   req['Content-Type'] = 'application/json'\n@@ -44,31 +50,13 @@ def fetch_extension_details(extension_id)\n \n   res = http.request(req)\n \n+\n   if res.code.to_i == 200\n     json = JSON.parse(res.body)\n-    ext = json.dig('results', 0, 'extensions', 0)\n-    return nil unless ext\n-\n-    description = ext['shortDescription'] || ext['displayName'] || ''\n-    marketplace = \"https://marketplace.visualstudio.com/items?itemName=#{extension_id}\"\n-\n-    # Extract statistics\n-    stats = {}\n-    if ext['statistics']\n-      ext['statistics'].each do |stat|\n-        stats[stat['statisticName']] = stat['value']\n-      end\n-    end\n+    data = json.dig('results', 0, 'extensions', 0)\n \n-    # Add stats to the returned details\n-    {\n-      id: extension_id,\n-      description: description,\n-      marketplace: marketplace,\n-      installs: stats['install'],\n-      rating_count: stats['ratingcount'],\n-      weighted_rating: stats['weightedRating'],\n-    }\n+    data['shortDescription'] ||= 'No description available...'\n+    ext.merge(data) if data\n   else\n     if res.code.to_i == 429\n       reset_time = res['X-RateLimit-Reset'] || 'unknown'\n@@ -79,11 +67,11 @@ def fetch_extension_details(extension_id)\n     else\n       STDERR.puts \"\\nAPI error for #{extension_id}: #{res.code} - #{res.message}\"\n     end\n-    { id: extension_id, description: '', marketplace: \"https://marketplace.visualstudio.com/items?itemName=#{extension_id}\" }\n+    ext.merge({ 'shortDescription' => 'Error fetching details...' })\n   end\n rescue => e\n   STDERR.puts \"\\nError fetching #{extension_id}: #{e.message}\"\n-  { id: extension_id, description: '', marketplace: \"https://marketplace.visualstudio.com/items?itemName=#{extension_id}\" }\n+  ext.merge({ 'shortDescription' => 'Error fetching details...' })\n end\n \n completed = 0\n@@ -110,7 +98,7 @@ workers = 10.times.map do\n   Thread.new do\n     while true\n       begin\n-        item = queue.pop(true)  # Will raise error when queue is empty\n+        item = mutex.synchronize { queue.pop(true) } # Will raise error when queue is empty\n       rescue ThreadError\n         break  # Queue is empty, exit the thread\n       end\n@@ -121,14 +109,14 @@ workers = 10.times.map do\n         mutex.synchronize do\n           results[idx] = details\n           completed += 1\n-          if details[:description].empty? && details[:id]\n+          if details['shortDescription'].empty? && details['id']\n             failed += 1\n           end\n         end\n       rescue => e\n         mutex.synchronize do\n           STDERR.puts \"\\nUnhandled error processing #{ext}: #{e.message}\"\n-          results[idx] = { id: ext, description: \"Error: #{e.message}\", marketplace: \"https://marketplace.visualstudio.com/items?itemName=#{ext}\" }\n+          results[idx] = { 'id' => ext, 'shortDescription' => \"Error: #{e.message}\", 'marketplace' => \"https://marketplace.visualstudio.com/items?itemName=#{ext}\" }\n           completed += 1\n           failed += 1\n         end\n@@ -151,30 +139,30 @@ if options[:format] == 'recommend'\n   puts '  \"recommendations\": ['\n   valid_results.each_with_index do |details, idx|\n     puts\n-    puts \"    \\\"#{details[:id]}\\\",\"\n-    puts \"    // #{details[:description]}\"\n-    puts \"    // Installs: #{details[:installs].to_i}, Rating: #{details[:weighted_rating]&.round(1)} (#{details[:rating_count]&.to_i} ratings) on #{Time.now.strftime(\"%Y-%m-%d\")}\"\n-    puts \"    // #{details[:marketplace]}\"\n+    puts \"    \\\"#{details['id']}\\\",\"\n+    puts \"    // #{details['shortDescription']}\"\n+    puts \"    // Installs: #{details['statistics']&.find { |stat| stat['statisticName'] == 'install' }&.dig('value').to_i}, Rating: #{details['statistics']&.find { |stat| stat['statisticName'] == 'weightedRating' }&.dig('value')&.round(1)} (#{details['statistics']&.find { |stat| stat['statisticName'] == 'ratingcount' }&.dig('value').to_i} ratings) on #{Time.now.strftime(\"%Y-%m-%d\")}\"\n+    puts \"    // #{details['marketplace']}\"\n   end\n   puts '  ]'\n   puts '}'\n elsif options[:format] == 'markdown'\n   puts \"# VS Code Extensions\\n\"\n \n   valid_results.each do |details|\n-    puts \"- [#{details[:id]}](#{details[:marketplace]})\"\n-    puts \"  > #{details[:description]}\"\n-    puts \"  > Installs: #{details[:installs].to_i}, Rating: #{details[:weighted_rating]&.round(1)} (#{details[:rating_count]&.to_i} ratings)\"\n+    puts \"- [#{details['id']}](#{details['marketplace']})\"\n+    puts \"  > #{details['shortDescription']}\"\n+    puts \"  > Installs: #{details['statistics']&.find { |stat| stat['statisticName'] == 'install' }&.dig('value').to_i}, Rating: #{details['statistics']&.find { |stat| stat['statisticName'] == 'weightedRating' }&.dig('value')&.round(1)} (#{details['statistics']&.find { |stat| stat['statisticName'] == 'ratingcount' }&.dig('value').to_i} ratings)\"\n     puts\n   end\n elsif options[:format] == 'json'\n   puts JSON.pretty_generate(valid_results)\n else\n   valid_results.each do |details|\n-    puts \"#{details[:id]}\"\n-    puts \"  Description: #{details[:description]}\"\n-    puts \"  Installs: #{details[:installs].to_i},  Rating: #{details[:weighted_rating]&.round(1)} (#{details[:rating_count]&.to_i} ratings)\"\n-    puts \"  Marketplace: #{details[:marketplace]}\"\n+    puts \"#{details['id']}\"\n+    puts \"  Description: #{details['shortDescription']}\"\n+    puts \"  Installs: #{details['statistics']&.find { |stat| stat['statisticName'] == 'install' }&.dig('value').to_i},  Rating: #{details['statistics']&.find { |stat| stat['statisticName'] == 'weightedRating' }&.dig('value')&.round(1)} (#{details['statistics']&.find { |stat| stat['statisticName'] == 'ratingcount' }&.dig('value').to_i} ratings)\"\n+    puts \"  Marketplace: #{details['marketplace']}\"\n     puts\n   end\n end","raw_url":"https://github.com/wstein/bin/raw/583cadefcd2a69bb56ebe181bdc510036a9d6e3f/list-vscode-extensions","sha":"c7dba3ac27711ba4eb9fa5fa8c4c2e1a647460a4","status":"modified"}]}