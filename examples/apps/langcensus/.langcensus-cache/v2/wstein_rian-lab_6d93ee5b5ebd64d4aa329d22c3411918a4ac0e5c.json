{"files":[{"additions":20,"blob_url":"https://github.com/wstein/rian-lab/blob/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro","changes":20,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro?ref=6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c","deletions":0,"filename":"site/src/components/site/LoweredCell.astro","patch":"@@ -20,6 +20,12 @@ interface Props {\n \n const { cell, heading = true } = Astro.props as Props;\n const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n+\n+// \"Open in Lab\" deep-link — the cell source, base64url'd into the playground's `#code=`\n+// permalink. Built here at build time with Node's `Buffer` so it needs no client JS; the\n+// browser side (`rian-live.ts` decodeSource) reads the identical encoding (ADR-0090).\n+const base = import.meta.env.BASE_URL.replace(/\\/$/, \"\");\n+const labHref = `${base}/playground#code=${Buffer.from(cell.source, \"utf8\").toString(\"base64url\")}`;\n ---\n \n <section id={cell.id} class=\"rl-cell\" data-cell-id={cell.id}>\n@@ -44,6 +50,9 @@ const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n         <span class=\"rl-mono rl-edithint\" title=\"Edit the source — it compiles and runs live in your browser\">\n           ✎ EDIT · LIVE\n         </span>\n+        <a class=\"rl-mono rl-lablink\" href={labHref} title=\"Open this source in the Playground (the Lab)\">\n+          Open in Lab ↗\n+        </a>\n       </div>\n       <div class=\"rl-codearea\">\n         <pre class=\"rl-code rl-hl\" data-cell-hl set:html={highlight(cell.source)}></pre>\n@@ -141,6 +150,17 @@ const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n     letter-spacing: 0.06em;\n     color: var(--accent);\n   }\n+  .rl-lablink {\n+    margin-left: 12px;\n+    font-size: 10.5px;\n+    letter-spacing: 0.04em;\n+    color: var(--accent);\n+    text-decoration: none;\n+    white-space: nowrap;\n+  }\n+  .rl-lablink:hover {\n+    text-decoration: underline;\n+  }\n   .rl-codearea {\n     position: relative;\n     min-width: 0;","raw_url":"https://github.com/wstein/rian-lab/raw/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro","sha":"33b4903cfec20e30d4afa361648b528f2282eabe","status":"modified"},{"additions":33,"blob_url":"https://github.com/wstein/rian-lab/blob/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Flib%2Frian-live.ts","changes":33,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Flib%2Frian-live.ts?ref=6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c","deletions":0,"filename":"site/src/lib/rian-live.ts","patch":"@@ -102,3 +102,36 @@ export function runInSandbox(jsModule: string, timeoutMs = 4000): Promise<RunRes\n // strip the leading `Kind: ` an exception message often carries, for terser output.\n export const cleanError = (e: unknown): string =>\n   String((e && (e as any).message) || e).replace(/^\\w+: /, \"\");\n+\n+// ── shareable source state (the `#code=` permalink, ADR-0090) ────────────────\n+// A lab buffer round-trips through the URL *hash* (never the query — the source\n+// stays client-only, off the server/Pages logs). Encoding is synchronous\n+// base64url(utf8): no async on the load path, no dependency, and the tour cells\n+// are well under any URL limit. The build-time half (the by-example \"Open in\n+// Lab\" hrefs) computes the same string in Astro via `Buffer.toString(\"base64url\")`,\n+// so the two sides agree byte-for-byte. (Deflate would shorten big pastes but is\n+// async + Safari-gated — deferred until sharing large buffers is a real need.)\n+export function encodeSource(src: string): string {\n+  const bytes = new TextEncoder().encode(src);\n+  let bin = \"\";\n+  for (const b of bytes) bin += String.fromCharCode(b);\n+  return btoa(bin).replace(/\\+/g, \"-\").replace(/\\//g, \"_\").replace(/=+$/, \"\");\n+}\n+\n+export function decodeSource(frag: string): string | null {\n+  try {\n+    const b64 = frag.replace(/-/g, \"+\").replace(/_/g, \"/\");\n+    const padded = b64 + \"===\".slice((b64.length + 3) % 4);\n+    const bin = atob(padded);\n+    const bytes = Uint8Array.from(bin, (c) => c.charCodeAt(0));\n+    return new TextDecoder().decode(bytes);\n+  } catch {\n+    return null;\n+  }\n+}\n+\n+// Read a shared buffer out of `location.hash` (`#code=<base64url>`), or null.\n+export function sharedFromHash(hash: string): string | null {\n+  const m = /^#code=(.+)$/.exec(hash);\n+  return m ? decodeSource(m[1]) : null;\n+}","raw_url":"https://github.com/wstein/rian-lab/raw/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Flib%2Frian-live.ts","sha":"496d62414f53441871a6ec3fa465216a2c114325","status":"modified"},{"additions":97,"blob_url":"https://github.com/wstein/rian-lab/blob/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Fpages%2Fplayground.astro","changes":107,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fplayground.astro?ref=6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c","deletions":10,"filename":"site/src/pages/playground.astro","patch":"@@ -47,8 +47,10 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n             <div class=\"pg-console-bar\">\n               <span class=\"rl-mono pg-console-title\">OUTPUT</span>\n               <span class=\"pg-edited\" id=\"pg-edited\" hidden>● edited · live</span>\n+              <span class=\"pg-shared\" id=\"pg-shared\" hidden title=\"This buffer came from a shared link; it compiles + runs in a sandbox.\">↗ shared · sandboxed</span>\n               <div class=\"pg-console-btns\">\n                 <button class=\"pg-btn pg-run\" id=\"pg-run\">▷ Run</button>\n+                <button class=\"pg-btn\" id=\"pg-link\" title=\"Copy a permalink to this buffer\">⎘ Copy link</button>\n                 <button class=\"pg-btn\" id=\"pg-reset\">↺ Reset</button>\n               </div>\n             </div>\n@@ -100,7 +102,15 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n \n <script>\n   import { highlight } from \"../lib/highlight\";\n-  import { loadCompiler, compilerLoaded, looksIncomplete, runInSandbox, cleanError } from \"../lib/rian-live\";\n+  import {\n+    loadCompiler,\n+    compilerLoaded,\n+    looksIncomplete,\n+    runInSandbox,\n+    cleanError,\n+    encodeSource,\n+    sharedFromHash,\n+  } from \"../lib/rian-live\";\n   import tour from \"../data/tour.json\";\n \n   type Cell = (typeof tour.cells)[number];\n@@ -114,10 +124,15 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   const gutter = $(\"#pg-gutter\");\n   const out = $(\"#pg-out\");\n   const edited = $(\"#pg-edited\");\n+  const sharedBadge = $(\"#pg-shared\");\n   const statusReach = $(\"#pg-status-reach\");\n   const statusLn = $(\"#pg-status-ln\");\n \n-  let current: Cell = cells[0];\n+  // a synthetic \"file\" for a buffer that arrived via a `#code=` share link — it has\n+  // no pre-generated reference panes, so the non-JS targets show an honest placeholder.\n+  type AnyCell = Cell | { id: \"shared\"; file: string; source: string; panes: {}; reach: {} };\n+  let current: AnyCell = cells[0];\n+  let isShared = false;\n   // the live JS/TS the in-browser compiler emits for the current buffer (null = reference / awaiting Run)\n   let liveJs: string | null = null;\n   let liveTs: string | null = null;\n@@ -133,20 +148,40 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     edited.hidden = code === current.source;\n   }\n \n+  const PANE_LABEL: Record<string, string> = { ex: \"BEAM · Elixir\", rs: \"Rust\", jvm: \"JVM · Kotlin\" };\n+\n   function renderPanes() {\n+    const panes = (current as any).panes as Record<string, string>;\n+    // Only JS/TS lower in-browser; BEAM/Rust/JVM ship as pre-generated reference for the\n+    // *original* cell source. So once the buffer diverges from that source (an edit) or is a\n+    // shared buffer with no reference at all, those panes would be SHOWING A STALE LIE — the\n+    // original cell's output mislabeled as this buffer's. Mark them explicitly instead.\n+    const stale = isShared || src.value !== current.source;\n     for (const t of [\"ex\", \"rs\", \"js\", \"ts\", \"jvm\"]) {\n       const el = document.querySelector(`.pg-pane-code[data-pane=\"${t}\"]`);\n       if (!el) continue;\n-      // the JS + TS panes are LIVE — the in-browser compiler's real `.mjs`/`.ts` emission for the\n-      // current buffer once Run has compiled it; the other targets show the pre-generated reference\n-      // (their emitters aren't in-browser). The `.ts` pane has no reference, so it waits for Run.\n       let code: string;\n-      if (t === \"js\") code = liveJs != null ? liveJs : (current.panes as Record<string, string>).js;\n-      else if (t === \"ts\") code = liveTs != null ? liveTs : \"// Run to compile this buffer to native TypeScript.\";\n-      else code = (current.panes as Record<string, string>)[t];\n+      let isStale = false;\n+      if (t === \"js\") {\n+        code = liveJs != null ? liveJs : (panes.js ?? \"// editing — compiles live as you type.\");\n+      } else if (t === \"ts\") {\n+        code = liveTs != null ? liveTs : \"// Run to compile this buffer to native TypeScript.\";\n+      } else if (stale) {\n+        isStale = true;\n+        code = isShared\n+          ? `// ⓘ ${PANE_LABEL[t]} is not lowered in-browser.\\n// A shared/edited buffer compiles live to JS/TS only; BEAM/Rust/JVM\\n// need the toolchain — they ship here as pre-generated reference (ADR-0090).`\n+          : `// ⓘ ${PANE_LABEL[t]}: reference for the ORIGINAL ${current.file}.\\n// In-browser lowering covers JS/TS only — your edits update those panes;\\n// the BEAM/Rust/JVM emitters aren't in the browser yet (ADR-0090).`;\n+      } else {\n+        code = panes[t];\n+      }\n       el.innerHTML = highlight(code);\n+      (el as HTMLElement).classList.toggle(\"pg-pane-stale\", isStale);\n     }\n-    const reach = Object.values(current.reach)[0] as string[];\n+    if (isShared) {\n+      statusReach.innerHTML = `<span style=\"opacity:1\">:js</span> · <span style=\"opacity:1\">:ts</span> live · <span style=\"opacity:0.45\">other targets need the toolchain</span>`;\n+      return;\n+    }\n+    const reach = (Object.values((current as any).reach)[0] as string[]) ?? [];\n     const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n     statusReach.innerHTML =\n       \"reaches \" +\n@@ -160,6 +195,10 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n \n   function loadFile(id: string) {\n     current = byId.get(id)!;\n+    isShared = false;\n+    sharedBadge.hidden = true;\n+    // a tab is an explicit choice — drop any `#code=` so a reload doesn't snap back to the share.\n+    if (location.hash.startsWith(\"#code=\")) history.replaceState(null, \"\", location.pathname + location.search);\n     src.value = current.source;\n     liveJs = null;\n     liveTs = null;\n@@ -170,6 +209,22 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     out.textContent = \"Start typing — it compiles + runs live in your browser. Or press ▷ Run.\";\n   }\n \n+  // a buffer that arrived via a `#code=` permalink (or \"Open in Lab\"): no tab, no\n+  // pre-generated reference panes, a \"shared · sandboxed\" badge, and immediate eval.\n+  function loadShared(source: string) {\n+    current = { id: \"shared\", file: \"shared.rian\", source, panes: {}, reach: {} };\n+    isShared = true;\n+    src.value = source;\n+    liveJs = null;\n+    liveTs = null;\n+    document.querySelectorAll(\".pg-tab\").forEach((b) => b.classList.remove(\"on\"));\n+    sharedBadge.hidden = false;\n+    renderEditor();\n+    renderPanes();\n+    out.dataset.kind = \"hint\";\n+    out.textContent = \"Loaded a shared buffer — compiling + running in a sandbox…\";\n+  }\n+\n   // ── live, forgiving evaluation ──────────────────────────────────────────────\n   // Compile (and run) the buffer as the user types, debounced. The Rian parser is\n   // not error-recovering, so a half-typed line throws — so we (1) stay quiet for\n@@ -237,6 +292,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   );\n   src.addEventListener(\"input\", () => {\n     renderEditor();\n+    renderPanes(); // flip BEAM/Rust/JVM to the honest placeholder the moment the buffer diverges\n     clearTimeout(liveTimer);\n     liveTimer = window.setTimeout(() => liveEval(false), 280);\n   });\n@@ -250,11 +306,32 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   $(\"#pg-reset\").addEventListener(\"click\", () => {\n     src.value = current.source;\n     renderEditor();\n+    renderPanes();\n     if (compilerLoaded()) liveEval(false);\n     else setOut(\"hint\", \"Reset to \" + current.file + \".\");\n   });\n+  // Copy link — encode the buffer into the URL hash (client-only, never the query) and\n+  // put the permalink on the clipboard. Anyone who opens it lands on this exact buffer.\n+  $(\"#pg-link\").addEventListener(\"click\", async () => {\n+    const enc = encodeSource(src.value);\n+    history.replaceState(null, \"\", \"#code=\" + enc);\n+    const url = location.href;\n+    try {\n+      await navigator.clipboard.writeText(url);\n+      setOut(\"ok\", \"⎘ link copied — it carries this exact buffer (which runs in a sandbox on open).\");\n+    } catch {\n+      setOut(\"hint\", \"The permalink is now in your address bar — copy it from there.\");\n+    }\n+  });\n \n-  loadFile(cells[0].id);\n+  // Boot: a `#code=` permalink wins over the default first cell.\n+  const shared = sharedFromHash(location.hash);\n+  if (shared != null && shared.trim() !== \"\") {\n+    loadShared(shared);\n+    liveEval(true); // compile + run the shared buffer right away, and focus the JS pane\n+  } else {\n+    loadFile(cells[0].id);\n+  }\n </script>\n \n <style>\n@@ -414,6 +491,16 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     font-size: 11px;\n     color: #f59e0b;\n   }\n+  .pg-shared {\n+    font-family: \"JetBrains Mono\", monospace;\n+    font-size: 11px;\n+    color: #7dd3fc;\n+  }\n+  /* a non-JS pane showing the honest \"reference / not this buffer\" placeholder */\n+  .pg-pane-stale {\n+    opacity: 0.66;\n+    font-style: italic;\n+  }\n   .pg-console-btns {\n     margin-left: auto;\n     display: flex;","raw_url":"https://github.com/wstein/rian-lab/raw/6d93ee5b5ebd64d4aa329d22c3411918a4ac0e5c/site%2Fsrc%2Fpages%2Fplayground.astro","sha":"f1a6becc62d243b44ee7c1adc27c6a7e4a107565","status":"modified"}]}