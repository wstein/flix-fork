{"files":[{"additions":3,"blob_url":"https://github.com/wstein/flatbars/blob/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0007-minbars.adoc","changes":5,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0007-minbars.adoc?ref=b54cd24a399194bf788e59b1bbfd7f18ac42d618","deletions":2,"filename":"docs/modules/ROOT/pages/adr-0007-minbars.adoc","patch":"@@ -36,8 +36,9 @@ unchanged\" claims to be false. They are recorded here because they reshape the p\n   dotted names, implicit iterator, standalone whitespace — *plus* inheritance\n   (`pass:[{{<p}}]`/`pass:[{{$b}}]`/`pass:[{{<*}}]`/`pass:[{{/*}}]`) and dynamic-name partials\n   (`pass:[{{>* }}]`). Lambdas are dropped (`Value` has no function arm). Set delimiters\n-  (`pass:[{{=<% %>=}}]`) were initially deferred but are now *implemented* (ADR-015; MinBars passes\n-  the full `delimiters` module, 14/14).\n+  (`pass:[{{=<% %>=}}]`) were initially deferred but are now *implemented* MinBars-exclusively\n+  (xref:adr-0015-set-delimiters.adoc[ADR-015] + 2026-06-04 scope narrowing; RawBars / MaxBars /\n+  FullBars reject the inline form). MinBars passes the full `delimiters` module, 14/14.\n \n . *Truthiness `mustache` default = a shared parameter.* Add `mustache = false null []` to the alias\n   table *and* thread a per-engine default `FalsySet` through `resolveTruthiness` + `runResolved`","raw_url":"https://github.com/wstein/flatbars/raw/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0007-minbars.adoc","sha":"e9d1ffa82a0a45544aed2d4b7cec2aa7806e348a","status":"modified"},{"additions":150,"blob_url":"https://github.com/wstein/flatbars/blob/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0015-set-delimiters.adoc","changes":263,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0015-set-delimiters.adoc?ref=b54cd24a399194bf788e59b1bbfd7f18ac42d618","deletions":113,"filename":"docs/modules/ROOT/pages/adr-0015-set-delimiters.adoc","patch":"@@ -1,136 +1,173 @@\n = ADR-015: Mustache set delimiters via a gated lexer mode\n :navtitle: ADR-015 Set delimiters\n \n-*Status:* Accepted (implemented across all four phases; MinBars 14/14 on the mustache/spec `delimiters`\n-module, RawBars/MaxBars opt-in shipped). *Date:* 2026-06-01. Lifts the set-delimiters deferral recorded in\n-xref:adr-0007-minbars.adoc[ADR-007]; constrained by xref:adr-0001-structural-parser-and-walker.adoc[ADR-001]\n-(one structural lexer) and xref:adr-0014-highlighting-derives-from-lexer.adoc[ADR-014] (no forked\n-grammar). Implementation plan: `set-delimiters-spec.md` (to be written before coding).\n+*Status:* Accepted. *Date:* 2026-06-01; narrowed to MinBars-exclusive 2026-06-04\n+(see \"Scope narrowing\" below). MinBars passes 14/14 on the mustache/spec\n+`delimiters` module. Lifts the set-delimiters deferral recorded in\n+xref:adr-0007-minbars.adoc[ADR-007]; constrained by\n+xref:adr-0001-structural-parser-and-walker.adoc[ADR-001] (one structural lexer)\n+and xref:adr-0014-highlighting-derives-from-lexer.adoc[ADR-014] (no forked\n+grammar).\n \n == Context\n \n Mustache lets a template change its own tag delimiters mid-stream:\n-`pass:[{{=<% %>=}}]` switches `pass:[{{ }}]` to `pass:[<% %>]` until a later `pass:[<%={{ }}=%>]`\n-switches back. ADR-007 deferred it (\"no customer corpus to audit\"); the tutorial shows it as a static\n-`spec-only` block. It is the last unimplemented *core* `mustache/spec` module (delimiters is core, not\n-a `~optional` module like inheritance / dynamic-names), so MinBars's \"Mustache-conformant\" claim is\n-currently \"conformant except the one module we skip.\"\n-\n-The decisive constraint, found in the code: **MinBars lexes through the *shared* core lexer** —\n-`FlatBars.Lexer.tokenizeTemplate`, the same lexer the RawBars ⊂ FullBars ⊂ MaxBars ladder uses, whose\n-`openerLiteralAt` hardcodes `pass:[{{]` / `pass:[{{{]` / `pass:[{{!--]` … via `matchAt cs i \"{{\"`. Set\n-delimiters makes the open/close pair **mutable mid-parse**. So this is a shared-lexer question, not a\n-MinBars-local one, and Handlebars (hence FullBars/MaxBars) has no `pass:[{{=...=}}]` — the feature is\n-Mustache-only.\n+`pass:[{{=<% %>=}}]` switches `pass:[{{ }}]` to `pass:[<% %>]` until a later\n+`pass:[<%={{ }}=%>]` switches back. ADR-007 deferred it (\"no customer corpus to\n+audit\"); the tutorial showed it as a static `spec-only` block. It is the last\n+unimplemented *core* `mustache/spec` module (delimiters is core, not a\n+`~optional` module like inheritance / dynamic-names), so MinBars's\n+\"Mustache-conformant\" claim was previously \"conformant except the one module\n+we skip.\"\n+\n+The decisive constraint, found in the code: **MinBars lexes through the\n+*shared* core lexer** — `FlatBars.Lexer.tokenizeTemplate`, the same lexer the\n+RawBars ⊂ FullBars ⊂ MaxBars ladder uses, whose `openerLiteralAt` hardcodes\n+`pass:[{{]` / `pass:[{{{]` / `pass:[{{!--]` … via `matchAt cs i \"{{\"`. Set\n+delimiters makes the open/close pair **mutable mid-parse**. So this is a\n+shared-lexer question, not a MinBars-local one, and Handlebars (hence\n+FullBars / MaxBars) has no `pass:[{{=...=}}]` — the feature is Mustache-only.\n \n == Decision\n \n-Implement set delimiters as a **flag-gated mode of the one core lexer**, following the\n-`LexOptions { infixArith }` precedent (the established dialect-scoping seam):\n-\n-. Add `mustacheDelims :: Boolean` to `LexOptions`. When **false** (the default for the whole ladder),\n-  the lexer behaves byte-identically to today — the meaning-free core and the Handlebars-family\n-  dialects are untouched, and the index-slicing hot path is unaffected.\n-. When **true**, the lexer carries a *current* `{ open, close }` pair, initialised to `pass:[{{]` /\n-  `pass:[}}]`. On encountering a `pass:[<open>=A B=<close>]` tag it swaps the pair and emits a new\n-  `RSetDelim` `RawTok`. Single pass, O(n); spans stay correct because the lexer already threads a `base`\n-  offset. No re-lex, no source rewrite, no second lexer. Per the Mustache manual, the new open/close are\n-  whitespace-separated and **may not contain whitespace or `=`** — which also makes a space-separated\n-  directive/CLI value unambiguous to parse.\n-\n-*Who sets the flag — by mimicking Mustache and Handlebars.* Mustache **has** set delimiters; Handlebars\n-**does not**. So:\n-\n-* *MinBars* — `mustacheDelims = true` by **default** (Mustache conformance), inline `pass:[{{=...=}}]`\n-  changes supported mid-stream.\n-* *RawBars and MaxBars* — **opt-in**, off by default, via any of four mechanisms: project **config**\n-  (`flatbars.json` `\"delimiters\": [\"<%\",\"%>\"]`), **runtime** (`ParseOptions { delimiters }`), **CLI**\n-  (`--delimiters '<% %>'`), and a per-file **directive** `pass:[{{! @delimiters: <% %> }}]` (rides the\n-  comment lexeme like `@truthiness` / `@trim`; sets the file's *initial* pair). When enabled, inline\n-  `pass:[{{=...=}}]` changes work too.\n-* *FullBars* — **excluded**. FullBars is the Handlebars-faithful dialect, and Handlebars has no set\n-  delimiters, so it gains nothing here.\n-\n-The off-by-default invariant is the contract: *with the flag off, every ladder dialect lexes exactly as\n-before* (byte-identical). This keeps **one grammar** (ADR-001 / ADR-014) and confines the stateful\n-behaviour behind a single flag whose default preserves today's behaviour everywhere.\n+Implement set delimiters as a **flag-gated mode of the one core lexer**,\n+following the `LexOptions { infixArith }` precedent (the established\n+dialect-scoping seam):\n+\n+. Add `mustacheDelims :: Boolean` to `LexConfig`. When **false** (the default\n+  for every dialect except MinBars), the lexer behaves byte-identically to\n+  today — the meaning-free core and the Handlebars-family dialects are\n+  untouched, and the index-slicing hot path is unaffected.\n+. When **true**, the lexer carries a *current* `{ open, close }` pair,\n+  initialised to `pass:[{{]` / `pass:[}}]`. On encountering a\n+  `pass:[<open>=A B=<close>]` tag it swaps the pair and emits a new\n+  `RSetDelim` `RawTok`. Single pass, O(n); spans stay correct because the\n+  lexer already threads a `base` offset. No re-lex, no source rewrite, no\n+  second lexer. Per the Mustache manual, the new open/close are\n+  whitespace-separated and **may not contain whitespace or `=`** — which also\n+  makes a space-separated directive value unambiguous to parse.\n+\n+*Per-dialect flag values:* set-delimiter is a *Mustache* feature, not a\n+\"non-Handlebars\" feature. Only MinBars enables it; the rest of the ladder\n+keeps the flag off so the dialect ladder has a single consistent answer to\n+\"does delimiter switching work here?\" — yes only on the Mustache surface.\n+\n+* *MinBars* — `mustacheDelims = true`. Inline `pass:[{{=...=}}]` switches the\n+  active pair mid-stream; the `pass:[{{! @delimiters: <% %> }}]` long-comment\n+  directive seeds the file's initial pair.\n+* *RawBars*, *MaxBars*, *FullBars* — `mustacheDelims = false`. The inline form\n+  is rejected at the parser (a `LexError: unexpected character` from the\n+  recovering parser, refined into the actionable LSP diagnostic below). The\n+  `{{! @delimiters: …}}` long-comment form parses as a normal comment, and the\n+  directive inside is silently ignored — the `@`-prefixed directive convention\n+  has no meaning outside the Mustache surface.\n+\n+The off-by-default invariant is the contract: *with the flag off, every\n+non-MinBars dialect lexes exactly as before* (byte-identical). This keeps **one\n+grammar** (ADR-001 / ADR-014) and confines the stateful behaviour behind a\n+single flag whose default preserves today's behaviour everywhere outside\n+MinBars.\n \n === Normative rulings (the spec-suite traps)\n \n-* *Rebasing scope.* Under custom delimiters, only the **2-char pair + the Mustache sigils**\n-  (`# / ^ & > ! =`) rebase. The FlatBars/Handlebars extensions `pass:[{{{]` (triple), `pass:[{{!--]`\n-  (long comment), `pass:[{{{{]` (raw block), and `pass:[{{<]` / `pass:[{{$]` (inheritance) do **not**\n-  rebase — their behaviour under custom delimiters is undefined by the spec. (Unescaped output under\n-  custom delimiters is the ampersand form `pass:[<%& name %>]`.)\n-* *Standalone whitespace.* A set-delimiter tag alone on a line is **standalone**: the line is stripped.\n-  `RSetDelim` must be standalone-eligible in `MinBars.Standalone`.\n-* *Partial-boundary reset.* Delimiter changes are **template-scoped** — a partial begins with the\n-  default `pass:[{{ }}]` regardless of the caller's current delimiters.\n-* *Highlighting.* `RSetDelim` carries a token kind so `pass:[<%=...=%>]` and custom-delimited tags\n-  colour in both front-ends — folds into the ADR-014 Tier-1 `tokenizeTemplate` span work, so the\n-  highlighter never disagrees with the engine.\n+* *Rebasing scope.* Under custom delimiters, only the **2-char pair + the\n+  Mustache sigils** (`# / ^ & > ! =`) rebase. The FlatBars / Handlebars\n+  extensions `pass:[{{{]` (triple), `pass:[{{!--]` (long comment),\n+  `pass:[{{{{]` (raw block), and `pass:[{{<]` / `pass:[{{$]` (inheritance) do\n+  **not** rebase — their behaviour under custom delimiters is undefined by the\n+  spec. (Unescaped output under custom delimiters is the ampersand form\n+  `pass:[<%& name %>]`.)\n+* *Standalone whitespace.* A set-delimiter tag alone on a line is\n+  **standalone**: the line is stripped. `RSetDelim` is standalone-eligible in\n+  `MinBars.Standalone`.\n+* *Partial-boundary reset.* Delimiter changes are **template-scoped** — a\n+  partial begins with the default `pass:[{{ }}]` regardless of the caller's\n+  current delimiters.\n+* *Highlighting.* `RSetDelim` carries a token kind so `pass:[<%=...=%>]` and\n+  custom-delimited tags colour in both front-ends — folds into the ADR-014\n+  Tier-1 `tokenizeTemplate` span work, so the highlighter never disagrees with\n+  the engine.\n+\n+=== Editor diagnostic on non-MinBars use\n+\n+The LSP (`editors/lsp/src/tokens.mjs` `dialectDiagnostics`) surfaces a clear,\n+actionable message in every non-MinBars dialect, at the `{{=` offset:\n+\n+____\n+Set-delimiter directives `pass:[{{=A B=}}]` are not valid in *<Dialect>* —\n+they're a Mustache feature reserved for MinBars. Switch the file to MinBars to\n+use them (Cmd-Shift-P → Change Language Mode).\n+____\n+\n+This replaces the generic `LexError: unexpected character` the recovering\n+parser otherwise emits — users get a remediation instead of a cryptic lexer\n+error.\n \n === Measured, not asserted\n \n-Corpus-first, per the project's gate culture (`examples:verify`, `test:compile`): vendor `mustache/spec`'s\n-`delimiters.json`, baseline it honestly (≈0/N today), implement, **then** flip it into the strict\n-`examples:verify` / `test:minbars-spec` gates and regenerate the conformance table. No conformance claim\n+Corpus-first, per the project's gate culture (`examples:verify`,\n+`test:compile`): the `mustache/spec` `delimiters.json` corpus is vendored,\n+gated, and currently 14/14 (`test:minbars-spec`). No conformance claim\n precedes a passing fixture.\n \n == Alternatives considered\n \n-* *(A) Mutable delimiters in the core lexer for every dialect* — puts Mustache-only stateful behaviour\n-  into the meaning-free core and onto the ladder, which has no such feature. Rejected.\n-* *(C) MinBars source-rewrite pre-pass* (normalise `pass:[<% %>]` → `pass:[{{ }}]` before lexing) — the\n-  rewriter must tokenize with dynamic delimiters anyway (same hard problem), and rewriting **destroys\n-  source offsets**, breaking error spans and ADR-014 highlighting. Rejected.\n-* *(D) MinBars forks its own Mustache lexer* — a second lexer (ADR-014 anti-pattern); duplicates\n-  standalone/whitespace logic and will drift. Rejected.\n-* *(E) Stay deferred* (status quo) — honest, zero-risk, but leaves MinBars one core module short of the\n-  conformance it advertises. Superseded by this ADR once the corpus confirms there is something to pass.\n+* *(A) Mutable delimiters in the core lexer for every dialect* — puts\n+  Mustache-only stateful behaviour into the meaning-free core and onto the\n+  ladder, which has no such feature. Rejected.\n+* *(B) RawBars + MaxBars opt-in via config / runtime / CLI / directive* —\n+  shipped in the original 2026-06-01 decision, withdrawn in the 2026-06-04\n+  scope narrowing (see \"Scope narrowing\"). Producing three yeses and one no\n+  for the same feature added complexity without a use case; the editor-side\n+  diagnostic now explains the MinBars-exclusive policy.\n+* *(C) MinBars source-rewrite pre-pass* (normalise `pass:[<% %>]` →\n+  `pass:[{{ }}]` before lexing) — the rewriter must tokenize with dynamic\n+  delimiters anyway (same hard problem), and rewriting **destroys source\n+  offsets**, breaking error spans and ADR-014 highlighting. Rejected.\n+* *(D) MinBars forks its own Mustache lexer* — a second lexer (ADR-014\n+  anti-pattern); duplicates standalone / whitespace logic and will drift.\n+  Rejected.\n+* *(E) Stay deferred* (status quo) — honest, zero-risk, but leaves MinBars one\n+  core module short of the conformance it advertises. Superseded by this ADR\n+  once the corpus confirmed there is something to pass.\n \n == Consequences\n \n-* `flatbars-js` / the core lexer gain a small, principled config knob and one new `RawTok` (`RSetDelim`);\n-  the ladder is provably unaffected (flag off ⇒ identical lexing).\n-* The conformance table can finally claim delimiters — *after* the gate proves it.\n-* ADR-007's deferral is lifted; the tutorial's static set-delimiters block becomes a live runnable card.\n-* RawBars and MaxBars gain set delimiters as an **opt-in** (off by default) via four mechanisms —\n-  config / runtime / CLI / the `pass:[{{! @delimiters: <% %> }}]` directive — all seeding the same lexer\n-  pair-config MinBars uses. FullBars stays Handlebars-faithful (no set delimiters). The off-by-default\n-  invariant keeps existing ladder templates byte-identical.\n-\n-\n-== Amendment (2026-06-04): set delimiters are MinBars-exclusive\n-\n-The original decision enabled `mustacheDelims` in RawBars and MaxBars\n-alongside MinBars, on the rationale that they're \"non-Handlebars dialects\".\n-That rationale was wrong: set-delimiter is a *Mustache* feature, not a\n-\"non-Handlebars\" feature. RawBars (the meaning-free core) and MaxBars\n-(the operators + pipes flagship) have no semantic reason to ship a\n-Mustache lexer mode; carrying it just produced three yeses and one no\n-where the dialect ladder benefits from one consistent answer.\n-\n-Effective immediately:\n-\n-* MinBars accepts `{{=A B=}}` and the `{{! @delimiters: <% %> }}`\n-  long-comment directive (unchanged).\n-* RawBars, MaxBars, and FullBars all reject the inline form at the\n-  parser (`LexError: unexpected character`).\n-* The `{{! @delimiters: …}}` long-comment form parses as a normal\n-  comment in RawBars / MaxBars / FullBars and the directive inside is\n-  silently ignored — Mustache's @-prefixed directive convention has no\n-  meaning in the Handlebars surface.\n-* The LSP surfaces a clear, actionable diagnostic in all three\n-  non-MinBars dialects: \"Set-delimiter directives `{{=A B=}}` are not\n-  valid in *<Dialect>* — they're a Mustache feature reserved for\n-  MinBars. Switch the file to MinBars to use them.\"\n-\n-Implementation: `RawBars.coreOptions.lexConfig` and\n-`MaxBars.maxOptions.lexConfig` revert to `defaultLexConfig` (no\n-`mustacheDelims`); `editors/js/src/FullBars/JS.purs` `highlightConfig`\n-matches at the highlight layer; the rawbars and maxbars test suites\n-flip from \"asserts switching works\" to \"asserts the inline form is\n-rejected\". `dialectDiagnostics` in `editors/lsp/src/tokens.mjs` emits\n-the actionable message for FullBars / RawBars / MaxBars; MinBars\n-silent.\n+* `flatbars-js` / the core lexer gain a small, principled config knob and one\n+  new `RawTok` (`RSetDelim`); the ladder is provably unaffected (flag off ⇒\n+  identical lexing).\n+* The conformance table claims delimiters — `test:minbars-spec` confirms\n+  14/14.\n+* ADR-007's deferral is lifted; the tutorial's static set-delimiters block is\n+  a live runnable card.\n+* FullBars stays Handlebars-faithful (no set delimiters). The off-by-default\n+  invariant keeps existing RawBars / MaxBars / FullBars templates\n+  byte-identical.\n+\n+== Scope narrowing (2026-06-04)\n+\n+The 2026-06-01 decision enabled `mustacheDelims` on RawBars and MaxBars (via\n+the four opt-in mechanisms in alternative (B)) on the rationale that they're\n+\"non-Handlebars dialects\". That rationale was wrong: set-delimiter is a\n+*Mustache* feature, not a \"non-Handlebars\" feature. RawBars (the meaning-free\n+core) and MaxBars (the operators + pipes flagship) have no semantic reason to\n+ship a Mustache lexer mode; carrying it produced three yeses and one no where\n+the dialect ladder benefits from one consistent answer.\n+\n+The Decision section above is the binding policy as of 2026-06-04. The\n+implementation collapsed accordingly:\n+\n+* `RawBars.coreOptions.lexConfig` and `MaxBars.maxOptions.lexConfig` revert to\n+  `defaultLexConfig` (no `mustacheDelims`).\n+* `packages/js/src/FullBars/JS.purs` `highlightConfig` matches at the\n+  highlight layer for `rawbars`, so highlighting agrees with parsing.\n+* The rawbars and maxbars test suites flip from \"asserts switching works\" to\n+  \"asserts the inline form is rejected and the long-comment form is ignored\".\n+* `dialectDiagnostics` (`editors/lsp/src/tokens.mjs`) emits the actionable\n+  message described in \"Editor diagnostic\" above for FullBars / RawBars /\n+  MaxBars; MinBars silent.\n+\n+The four opt-in mechanisms (config / runtime / CLI / directive) listed in\n+alternative (B) above are withdrawn — they're no longer reachable from any\n+shipped dialect. If a future use case demands cross-dialect support, this\n+section becomes the next ADR's \"Context\".","raw_url":"https://github.com/wstein/flatbars/raw/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0015-set-delimiters.adoc","sha":"8a2cd501b59a9fbbbbfbcb296b9542727c5fe1d0","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/flatbars/blob/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-final-review.adoc","changes":4,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-final-review.adoc?ref=b54cd24a399194bf788e59b1bbfd7f18ac42d618","deletions":1,"filename":"docs/modules/ROOT/pages/adr-final-review.adoc","patch":"@@ -160,7 +160,9 @@ and the RawBars docstring. *Rating 4.*\n   *implementation*, and it touches MinBars, labelled loops, and self-including partials.\n * *xref:adr-0013-labelled-loops.adoc[ADR-013] (labelled loops)* — now drafted (Proposed); lifts the\n   ADR-006 §5 v1 outer-loop limit once a naming/scoping design is settled.\n-* *Set-delimiters* (deferred in ADR-007) will reopen the shape-gate axis if a corpus ever justifies it.\n+* *Set-delimiters* shipped MinBars-only (xref:adr-0015-set-delimiters.adoc[ADR-015] +\n+  2026-06-04 scope narrowing); the dialect ladder deliberately excludes them. A future\n+  cross-dialect demand reopens that ADR rather than the shape-gate axis.\n * *A third grammar seam*, if one is ever proposed, triggers a review of axis A rather than a quiet\n   addition (ADR-004 Watch).\n ","raw_url":"https://github.com/wstein/flatbars/raw/b54cd24a399194bf788e59b1bbfd7f18ac42d618/docs%2Fmodules%2FROOT%2Fpages%2Fadr-final-review.adoc","sha":"f3c39b4921c11534e5a9c03904928e461b4cd786","status":"modified"}]}