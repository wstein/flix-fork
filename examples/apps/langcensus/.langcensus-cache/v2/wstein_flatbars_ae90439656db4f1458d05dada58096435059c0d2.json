{"files":[{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/ae90439656db4f1458d05dada58096435059c0d2/docs%2Fmodules%2FROOT%2Fnav.adoc","changes":1,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fnav.adoc?ref=ae90439656db4f1458d05dada58096435059c0d2","deletions":0,"filename":"docs/modules/ROOT/nav.adoc","patch":"@@ -31,6 +31,7 @@\n * xref:adr-0026-editor-capability-matrix.adoc[ADR-026: editor capability matrix]\n * xref:adr-0027-cross-template-scope-graph.adoc[ADR-027: cross-template scope graph (proposed)]\n * xref:adr-0028-hybrid-template-lsp.adoc[ADR-028: hybrid host+FlatBars LSP (proposed)]\n+* xref:adr-0029-i18n-helper.adoc[ADR-029: i18n helper (proposed)]\n * xref:adr-final-review.adoc[Final ADR review (2026-06-01)]\n \n .Specification","raw_url":"https://github.com/wstein/flatbars/raw/ae90439656db4f1458d05dada58096435059c0d2/docs%2Fmodules%2FROOT%2Fnav.adoc","sha":"8469a4baa380544b6c9c07dceb9557768d261c12","status":"modified"},{"additions":164,"blob_url":"https://github.com/wstein/flatbars/blob/ae90439656db4f1458d05dada58096435059c0d2/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc","changes":164,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc?ref=ae90439656db4f1458d05dada58096435059c0d2","deletions":0,"filename":"docs/modules/ROOT/pages/adr-0029-i18n-helper.adoc","patch":"@@ -0,0 +1,164 @@\n+= ADR-0029: i18n via a host-plugged `translate` callback (the `t` helper), not bundled CLDR\n+:navtitle: ADR-029 i18n helper (proposed)\n+:hbs: Handlebars\n+\n+*Status:* Proposed. *Date:* 2026-06-05. Builds on\n+xref:adr-0001-structural-parser-and-walker.adoc[ADR-001] (helpers are the meaning plug-in; the core stays\n+meaning-free), reuses the IoC callback shape of\n+xref:adr-0022-analyse-mode.adoc[ADR-022] (truthiness is a per-engine lambda the host plugs in, seeded into\n+both interpreter and compiled runtime, *no per-file config, no baked data*), extends the user-extension\n+path of xref:adr-0018-user-defined-helpers.adoc[ADR-018] (`registerHelper`), and is gated by the\n+compile ≡ interpret invariant of xref:adr-0011-compile-to-js.adoc[ADR-011]. It respects the dialect-purity\n+boundaries of xref:adr-0007-minbars.adoc[ADR-007] (MinBars is *Mustache*) and\n+xref:adr-0009-fullbars.adoc[ADR-009] (FullBars is *Handlebars-faithful*), using the engine-level opt-in\n+precedent of xref:adr-0015-set-delimiters.adoc[ADR-015] (set-delimiters is a MinBars-only knob, not a\n+global). Escaping follows xref:adr-0002-escaping-and-value-policy.adoc[ADR-002].\n+\n+== Context\n+\n+The https://en.wikipedia.org/wiki/Comparison_of_web_template_engines[Wikipedia comparison of web template\n+engines] scores FlatBars *i18n: No*. The evidence is direct: the 90-operation prelude\n+(`FullBars.preludeSchema`) ships no `t` / `translate` / `gettext`. Two facts frame the decision:\n+\n+* *This is the company we keep.* {hbs} and Mustache *also* have no built-in i18n — their ecosystems do it\n+  with userland helpers/lambdas (i18next, `handlebars-i18n`, Mustache lambdas). So \"absent from the core\"\n+  is not anomalous; the gap is a *workflow/DX* gap, not a correctness gap.\n+* *i18n is meaning, and meaning is plugged in, not built in (ADR-001).* Real localization is not string\n+  substitution: it needs *pluralization* (CLDR gives Polish/Arabic/Russian 3–6 plural categories),\n+  *gender/select*, and locale-aware *number/date* formatting (ICU MessageFormat is the de-facto standard).\n+  A naive key-lookup `t` produces grammatically wrong output for most languages — *worse* than nothing,\n+  because it looks finished.\n+\n+Three hard constraints fell out of team review (debate recorded against this ADR):\n+\n+. *No CLDR/ICU data in our core or runtime.* The compiled runtime (`flatbars-runtime.mjs`) and the\n+  committed Lab bundle are size-gated; CLDR plural data is tens of KB and would tax *every* compiled\n+  template, including the majority that never localize.\n+. *The prelude is one shared schema across all four dialects.* Adding `t` to the base prelude adds it to\n+  *MinBars* (breaking the \"pure Mustache\" claim, ADR-007) and *FullBars* (Handlebars ships no `t`,\n+  ADR-009) — a faithfulness regression even though byte-identical conformance would stay green (the spec\n+  corpora never call `t`).\n+. *Whatever ships must satisfy compile ≡ interpret (ADR-011)* or `test:compile` goes red.\n+\n+== Decision\n+\n+Adopt the truthiness pattern (ADR-022) for localization: *FlatBars supplies the call site; the host\n+supplies the brain; we never bundle CLDR.*\n+\n+. *`t` is a blessed operation in an opt-in i18n prelude module*, not the base prelude. It is enabled for\n+  the *core-surface family* — *RawBars, FullBars, and MaxBars* — and *off only for MinBars* (the separate\n+  Mustache peer engine, which stays Mustache-pure, ADR-007). *RawBars is included precisely because it is\n+  the desugared core surface* (not a meaning-free substrate): FullBars's `pass:[{{t …}}]` desugars to a\n+  core `t` operation, so the operation *must* exist at the RawBars level for the desugar target to be\n+  complete, and RawBars runs the same prelude/engine — there is no substrate reason to exclude it. A host\n+  may still enable or disable the module explicitly on any dialect.\n+\n+. *Localization is a host-plugged callback*, the IoC shape ADR-022 already uses:\n++\n+[source]\n+----\n+translate :: { locale :: String, key :: String, args :: Value } -> Maybe String\n+----\n++\n+The host owns the message catalog, locale resolution, and all CLDR/ICU behaviour (plurals, select, gender,\n+number/date) — typically by delegating to its existing i18n library. *FlatBars ships no plural rules and no\n+CLDR data.* The callback receives *named arguments* as a `Value` dict, so the host's ICU implementation has\n+everything it needs (`count`, `gender`, arbitrary interpolation values).\n+\n+. *Surface.* The head is `t`, the first argument is the message key, the rest are named arguments:\n++\n+[source,handlebars]\n+----\n+{{t \"cart.summary\" count=cart.size name=user.name}}\n+----\n++\n+In MaxBars this also composes with pipes (`cart.size | t \"cart.summary\"` is *not* the shape; the key is\n+the head argument — pipe sugar applies to value-producing operations, see xref:maxbars.adoc[]).\n++\n+*Name: lowercase `t`, not `T`.* An uppercase `T` was considered for ergonomics — it reads more like a\n+sigil and is harder to overlook in a dense template — and rejected. `t` is the universal i18n function\n+name (i18next, gettext, Rails, Vue, react-i18next), so it is recognized on sight and lets ported templates\n+work unchanged; the prelude is case-sensitive, so `T` would silently break `pass:[{{t …}}]`. It would also\n+be the *only* uppercase operation among the 90 lowercase ones. Most importantly, `t` is an *operation\n+name*, not a sigil (FlatBars has no keywords — \"the head of an application is always a name\", ADR-001);\n+making it look like grammar would miscategorize it. The \"don't overlook it\" need is met in the *editor*\n+layer instead — bold tag emphasis (ADR-026) and operation-name painting both already make `pass:[{{t …}}]`\n+stand out — which is the correct place for visual salience. No `translate` alias ships (canonical-only;\n+legacy spellings are a lint concern, ADR-019).\n+\n+. *Escaping is unchanged (ADR-002).* The callback returns a `String`; it lands *escaped* in\n+  `pass:[{{t …}}]` and *raw* in `pass:[{{{t …}}}]`, exactly like every other helper, with `safe` as the\n+  SafeString escape hatch. Messages that intentionally carry markup use the triple-stache. *No new\n+  escaping axis is introduced.*\n+\n+. *Missing-key policy: fallback-and-flag, never throw.* `translate` returning `Nothing` renders a visible\n+  fallback (the key, or a host-configured marker), and *analyse mode* (ADR-022 Part B) / the linter flags\n+  it. A strict mode that errors on a missing key is opt-in (for CI, not render). Production renders never\n+  hard-fail on one missing translation.\n+\n+. *compile ≡ interpret.* The `translate` callback is seeded into *both* the interpreter env and the\n+  compiled runtime through the same seam truthiness uses (ADR-011/ADR-022) — not a second mechanism.\n+  `test:compile` gates byte-identical output.\n+\n+== Consequences\n+\n+* The Wikipedia *i18n: No* becomes *Yes (host-plugged)* for RawBars/FullBars/MaxBars, with the honest\n+  footnote that the plural/format brain is the host's — which is exactly how i18next-backed {hbs} works.\n+* The catalog gains a `t` entry *only when the i18n module is enabled*; `npm run gen:catalog` /\n+  `check:catalog` and `gen:operations` / `check:operations` must learn the module is conditional so the\n+  docs catalog and the LSP `operations.json` reflect the enabled set (new gate work).\n+* *Only MinBars is provably unaffected* — its schema does not gain `t`, so ADR-007 purity and the Mustache\n+  conformance claim are untouched. RawBars *does* gain `t` (ADR-008's `nonEmpty` truthiness is orthogonal\n+  and unchanged); this is required, not incidental — it is the desugar target FullBars/MaxBars lower to.\n+* Zero bundle-size impact on templates that don't localize: no CLDR ships.\n+\n+=== What we explicitly do NOT do\n+\n+* *No ICU MessageFormat engine in-core, no CLDR plural rules in our runtime* (rejected option D in the\n+  debate): heavy data, violates meaning-free + the bundle gates. Plurals are the host callback's job.\n+* *No per-file `@locale` directive.* Locale is the host's, passed via the callback — mirroring ADR-022's\n+  rejection of per-file `@truthiness`.\n+* *No `t` in the base shared prelude* (rejected option B): that would force `t` onto MinBars too, breaking\n+  the Mustache-purity claim. The opt-in module keeps it on the core-surface family (RawBars/FullBars/MaxBars)\n+  while leaving the MinBars peer schema clean.\n+\n+== Alternatives considered (debate ratings)\n+\n+[cols=\"1,4,1\"]\n+|===\n+| Option | Summary | Verdict\n+\n+| A — recipe only\n+| Document `registerHelper('t', …)` + i18next; ship no engine surface. The honest \"you can already do\n+  this today\" answer, and how {hbs}/Mustache users localize.\n+| *Adopt as the interim*, but it documents a workaround rather than closing the gap.\n+\n+| B — `t` in the base prelude\n+| Simplest, but adds `t` to MinBars + FullBars schemas.\n+| Rejected — pollutes Mustache/Handlebars faithfulness.\n+\n+| C — opt-in module + host `translate` callback\n+| This ADR. Truthiness pattern applied to localization; no CLDR shipped; dialect-gated.\n+| *Accepted (proposed).*\n+\n+| D — full ICU/CLDR in-core\n+| Correct plurals out of the box, but tens of KB of data in core/runtime.\n+| Rejected — violates meaning-free + bundle gates.\n+\n+| E — `number` / `date` / `selectPlural` helpers\n+| A locale-aware formatting helper set, same host-callback discipline.\n+| Deferred — a follow-up *on top of* C, not v1.\n+|===\n+\n+== Follow-ups\n+\n+. *Interim, ships independently of this ADR:* a documented `registerHelper('t', …)` + i18next recipe\n+  (tutorials + `editors`/host docs), so the gap has an answer today.\n+. *This ADR (C):* the `t` operation, the i18n prelude module + engine opt-in wiring, the seeded\n+  `translate` callback in interpreter and compiled runtime, conditional catalog/operations generation, and\n+  `test:compile` + analyse-mode coverage.\n+. *Deferred (E):* locale-aware `number` / `date` / `selectPlural` helpers under the same host-callback\n+  discipline.\n+. *Conformance note:* add an `i18n` row to xref:fullbars-compat.adoc[] documenting that the `t` operation\n+  (shared by RawBars/FullBars/MaxBars) is an *extension* beyond Handlebars (faithful to the workflow, not a\n+  Handlebars built-in), the same framing ADR-018 used for `registerHelper`.","raw_url":"https://github.com/wstein/flatbars/raw/ae90439656db4f1458d05dada58096435059c0d2/docs%2Fmodules%2FROOT%2Fpages%2Fadr-0029-i18n-helper.adoc","sha":"4e8de2431309a70a3bf5b6fb76908c1278c494ff","status":"added"}]}