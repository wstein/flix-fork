{"files":[{"additions":68,"blob_url":"https://github.com/wstein/bin/blob/84d7e69edae36465b1def33c4d0d250647447909/list-vscode-extensions","changes":131,"contents_url":"https://api.github.com/repos/wstein/bin/contents/list-vscode-extensions?ref=84d7e69edae36465b1def33c4d0d250647447909","deletions":63,"filename":"list-vscode-extensions","patch":"@@ -10,6 +10,7 @@ require 'optparse'\n require 'io/console'\n require 'yaml'\n require 'time'\n+require 'erb'\n \n options = { workers: 10 }\n OptionParser.new do |opts|\n@@ -146,6 +147,17 @@ def id_with_version(details, ext_versions, options)\n   end\n end\n \n+# Add helper to format flags\n+def format_flags(details)\n+  details['flags'] ? details['flags'].split(',').map(&:strip).map { |f| \"*#{f}*\" }.join(' ') : ''\n+end\n+\n+# Add helper to format flags in brackets for default output\n+def bracket_flags(details)\n+  flags = details['flags'] ? details['flags'].split(',').map(&:strip).join(', ') : ''\n+  flags.empty? ? '' : \"[#{flags}]\"\n+end\n+\n # Parallel fetching with specified number of workers\n queue = Queue.new\n ext_ids.each_with_index { |ext, idx| queue << [idx, ext] }\n@@ -205,69 +217,62 @@ def output_close(io, options)\n   io.close if options[:output]\n end\n \n-if options[:format] == 'recommend'\n-  valid_results.each do |details|\n-    last_updated_date = format_last_updated(details)\n-    installs = extract_stat(details, :install).to_i\n-    rating = extract_stat(details, :rating)&.round(1)\n-    rating_count = extract_stat(details, :rating_count).to_i\n-    input_version = ext_versions[details['id']]\n-    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n-    id_with_ver = if options[:id_with_version]\n-      ver = input_version || fetched_version\n-      \"#{details['id']}@#{ver}\"\n-    else\n-      details['id']\n-    end\n-    flags = details['flags'] ? details['flags'].split(',').map(&:strip).map { |f| \"*#{f}*\" }.join(' ') : ''\n-    output_puts(output_io, \"    \\\"#{id_with_ver}\\\",  // #{flags}\\n    // #{details['shortDescription']}\\n    // Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings) on #{Time.now.strftime('%Y-%m-%d')}\\n    // #{details['marketplace']}\\n\")\n-  end\n-elsif options[:format] == 'markdown'\n-  output_puts(output_io, \"# VS Code Extensions\\n\")\n-  valid_results.each do |details|\n-    last_updated_date = format_last_updated(details)\n-    installs = extract_stat(details, :install).to_i\n-    rating = extract_stat(details, :rating)&.round(1)\n-    rating_count = extract_stat(details, :rating_count).to_i\n-    input_version = ext_versions[details['id']]\n-    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n-    id_with_ver = if options[:id_with_version]\n-      ver = input_version || fetched_version\n-      \"#{details['id']}@#{ver}\"\n-    else\n-      details['id']\n-    end\n-    flags = details['flags'] ? \" *#{details['flags'].gsub(/,\\s*/, '* *')}*\" : ''\n-    output_puts(output_io, \"- [#{id_with_ver}](#{details['marketplace']})#{flags}\\n  > #{details['shortDescription']}\\n  > Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n\")\n-  end\n-elsif options[:format] == 'json'\n-  output_puts(output_io, JSON.pretty_generate(valid_results.map { |details|\n-    d = details.dup\n-    d['id'] = id_with_version(details, ext_versions, options)\n-    d\n-  }))\n-elsif options[:format] == 'yaml'\n-  output_puts(output_io, valid_results.map { |details|\n-    d = details.dup\n-    d['id'] = id_with_version(details, ext_versions, options)\n-    d\n-  }.to_yaml)\n+def render_erb_template(template_str, locals)\n+  valid_results = locals[:valid_results]\n+  ext_versions  = locals[:ext_versions]\n+  options       = locals[:options]\n+  ERB.new(template_str, trim_mode: '-').result(binding)\n+end\n+\n+recommend_template = %q{\n+<% valid_results.each do |details| %>\n+    \"<%= id_with_version(details, ext_versions, options) %>\",  // <%= format_flags(details) %>\n+    // <%= details['shortDescription'] %>\n+    // Latest <%= details['versions']&.first&.dig('version') || 'unknown' %> - <%= format_last_updated(details) %>, Installs: <%= extract_stat(details, :install).to_i %>, Rating: <%= extract_stat(details, :rating)&.round(1) %> (<%= extract_stat(details, :rating_count).to_i %> ratings) on <%= Time.now.strftime('%Y-%m-%d') %>\n+    // <%= details['marketplace'] %>\n+<% end %>\n+}\n+\n+markdown_template = %q{\n+# VS Code Extensions\n+<% valid_results.each do |details| %>\n+- [<%= id_with_version(details, ext_versions, options) %>](<%= details['marketplace'] %>)<%= format_flags(details).empty? ? '' : \" #{format_flags(details)}\" %>\n+  > <%= details['shortDescription'] %>\n+  > Latest <%= details['versions']&.first&.dig('version') || 'unknown' %> - <%= format_last_updated(details) %>, Installs: <%= extract_stat(details, :install).to_i %>, Rating: <%= extract_stat(details, :rating)&.round(1) %> (<%= extract_stat(details, :rating_count).to_i %> ratings)\n+<% end %>\n+}\n+\n+json_template = %q{\n+<%= JSON.pretty_generate(valid_results.map { |details| d = details.dup; d['id'] = id_with_version(details, ext_versions, options); d }) %>\n+}\n+\n+yaml_template = %q{\n+<%= valid_results.map { |details| d = details.dup; d['id'] = id_with_version(details, ext_versions, options); d }.to_yaml %>\n+}\n+\n+default_template = %q{\n+<% valid_results.each do |details| %>\n+<%= details['id'] %><%= options[:id_with_version] ? \"@#{ext_versions[details['id']] || '0.1.0'}\" : \"\" %><%= format_flags(details).empty? ? '' : \" #{format_flags(details)}\" %>\n+  Description: <%= details['shortDescription'] %>\n+  Latest <%= details['versions']&.first&.dig('version') || 'unknown' %> - <%= format_last_updated(details) %> Installs: <%= extract_stat(details, :install).to_i %>,  Rating: <%= extract_stat(details, :rating)&.round(1) %> (<%= extract_stat(details, :rating_count).to_i %> ratings)\n+  Marketplace: <%= details['marketplace'] %>\n+<% end %>\n+}\n+case options[:format]\n+when 'recommend'\n+  output_puts(output_io, render_erb_template(recommend_template,\n+    {valid_results: valid_results, ext_versions: ext_versions, options: options}))\n+when 'markdown'\n+  output_puts(output_io, render_erb_template(markdown_template,\n+    {valid_results: valid_results, ext_versions: ext_versions, options: options}))\n+when 'json'\n+  output_puts(output_io, render_erb_template(json_template,\n+    {valid_results: valid_results, ext_versions: ext_versions, options: options}))\n+when 'yaml'\n+  output_puts(output_io, render_erb_template(yaml_template,\n+    {valid_results: valid_results, ext_versions: ext_versions, options: options}))\n else\n-  valid_results.each do |details|\n-    last_updated_date = format_last_updated(details)\n-    installs = extract_stat(details, :install).to_i\n-    rating = extract_stat(details, :rating)&.round(1)\n-    rating_count = extract_stat(details, :rating_count).to_i\n-    input_version = ext_versions[details['id']]\n-    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n-    id_with_ver = if options[:id_with_version]\n-      ver = input_version || fetched_version\n-      \"#{details['id']}@#{ver}\"\n-    else\n-      details['id']\n-    end\n-    flags = details['flags'] ? \" *#{details['flags'].gsub(/,\\s*/, '* *')}*\" : ''\n-    output_puts(output_io, \"- #{id_with_ver}#{flags}\\n  > #{details['shortDescription']}\\n  > Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n  Marketplace: #{details['marketplace']}\\n\")\n-  end\n+  output_puts(output_io, render_erb_template(default_template,\n+    {valid_results: valid_results, ext_versions: ext_versions, options: options}))\n end\n output_close(output_io, options)","raw_url":"https://github.com/wstein/bin/raw/84d7e69edae36465b1def33c4d0d250647447909/list-vscode-extensions","sha":"53a7b06648aa032e17d554ef8baa834f1dc9949d","status":"modified"}]}