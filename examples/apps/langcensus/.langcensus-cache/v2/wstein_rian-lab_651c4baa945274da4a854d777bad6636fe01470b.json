{"files":[{"additions":8,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/docs%2Fadr%2F0036-range-subrange-types.md","changes":14,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0036-range-subrange-types.md?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":6,"filename":"docs/adr/0036-range-subrange-types.md","patch":"@@ -149,12 +149,14 @@ on the `@type` spec and first-match clauses; no shim needed.\n   the BEAM (charlists are integer lists) and a **BigInt** codepoint in JS, but a native **`char`** on\n   Rust (`__prim_str_chars : Vec(Char)` → `Vec<char>`; `Rian.Capability` lowers `Char` → `char`, a\n   `Copy` scalar). Ordinal comparison (`==`, `<`, `>=`) works directly on every target (Rust `char`\n-  is `Ord`+`Eq`). **Arithmetic does not widen implicitly** (ADR-0035 — no hidden coercion): use\n-  `__prim_char_code(c) : Int64` for the explicit `Char` → codepoint conversion (identity on BEAM/JS,\n-  `char as i64` on Rust). The self-hosting lexer now reads `lex(cs Vec(Char))` with `when c == '+'`\n-  / `['(' | rest]` and `acc * 10 + __prim_char_code(c) - __prim_char_code('0')`, lowering and running\n-  on all three targets. **Still future:** `range` *construction* over `Char` (`'A'..'Z'`) — the\n-  ordinal-base machinery (ADR-0036 above) builds on this `Char` type.\n+  is `Ord`+`Eq`). **Ordinal arithmetic widens to the `Int64` base** — `'9' - '0' : Int64` (not\n+  `Char`; `9 ∉ Char`), matching the `range` arithmetic rule above. On the BEAM/JS a `Char` is already\n+  an integer, so this is free; on Rust a `Char` operand of `+ - * rem div` is wrapped in\n+  `__prim_char_code/1` (→ `char as i64`) by a lowering pre-pass. The explicit `__prim_char_code(c) :\n+  Int64` conversion remains available. The self-hosting lexer reads `lex(cs Vec(Char))` with\n+  `when c == '+'` / `['(' | rest]`, lowering and running on all three targets. **Still future:**\n+  `range` *construction* over `Char` (`'A'..'Z'`) — the ordinal-base machinery (ADR-0036 above)\n+  builds on this `Char` type.\n - ~~**Range arithmetic & coercion.**~~ **Resolved 2026-06-12: arithmetic widens to base.**\n   `Digit + Digit : Int64` — *not* `Digit`, because `9 + 9 = 18 ∉ 0..9`; any in-bounds wrap or hidden\n   `RangeError` on `+` would be hidden control flow (ADR-0035). Re-narrow explicitly with `Digit.of(18)`","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/docs%2Fadr%2F0036-range-subrange-types.md","sha":"983f1a85ea3c76e4798f39d758108c313fd2fd06","status":"modified"},{"additions":25,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/lib%2Frian%2Fcheck.ex","changes":33,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcheck.ex?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":8,"filename":"lib/rian/check.ex","patch":"@@ -141,8 +141,8 @@ defmodule Rian.Check do\n     do: if(String.contains?(n, \".\") or String.match?(n, ~r/[eE]/), do: \"Float64\", else: \"Int64\")\n \n   def infer(%EStr{}, _env, _ic), do: \"String\"\n-  # a `Char` literal is the `Char` primitive (ADR-0036); to use its codepoint in\n-  # arithmetic, convert explicitly with `__prim_char_code(c) : Int64`\n+  # a `Char` literal is the `Char` primitive (ADR-0036); ordinal arithmetic on it\n+  # widens to the `Int64` base (see `ordinal_base/1`)\n   def infer(%EChar{}, _env, _ic), do: \"Char\"\n   def infer(%EId{name: b}, _env, _ic) when b in ~w(true false), do: \"Bool\"\n   # a name resolves to a bound var, else a nullary variant constructor, else unknown\n@@ -152,12 +152,23 @@ defmodule Rian.Check do\n \n   def infer(%EBin{op: op, left: l, right: r}, env, ic) do\n     cond do\n-      op in @bool_ops -> \"Bool\"\n-      op == \"<>\" -> \"String\"\n-      op == \"/\" -> \"Float64\"\n-      op in @int_ops -> \"Int64\"\n-      op in @arith -> conservative(unify(infer(l, env, ic), infer(r, env, ic)))\n-      true -> :unknown\n+      op in @bool_ops ->\n+        \"Bool\"\n+\n+      op == \"<>\" ->\n+        \"String\"\n+\n+      op == \"/\" ->\n+        \"Float64\"\n+\n+      op in @int_ops ->\n+        \"Int64\"\n+\n+      op in @arith ->\n+        conservative(unify(ordinal_base(infer(l, env, ic)), ordinal_base(infer(r, env, ic))))\n+\n+      true ->\n+        :unknown\n     end\n   end\n \n@@ -476,6 +487,12 @@ defmodule Rian.Check do\n   defp conservative(:mismatch), do: :unknown\n   defp conservative(t), do: t\n \n+  # ordinal arithmetic widens to the base (ADR-0036): `Char ± _` is `Int64`, not\n+  # `Char` (`'9' - '0' = 9 ∉ Char`). A `Char` operand contributes its codepoint\n+  # base; every other type passes through unchanged.\n+  defp ordinal_base(\"Char\"), do: \"Int64\"\n+  defp ordinal_base(t), do: t\n+\n   # ── function checking ──────────────────────────────────────────────────\n   @doc \"\"\"\n   Check one function. `ic` is the inference context (`:tdefs`/`:funs`/`:ctors`);","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/lib%2Frian%2Fcheck.ex","sha":"e0917334b01c45c046dcdca1731edd9361346ad3","status":"modified"},{"additions":47,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/lib%2Frian%2Flower.ex","changes":47,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Flower.ex?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":0,"filename":"lib/rian/lower.ex","patch":"@@ -634,6 +634,7 @@ defmodule Rian.Lower do\n         ast =\n           c.body\n           |> body_ast(ctx)\n+          |> widen_char_arith(char_vars(func.params, c.pats))\n           |> resolve_rust_pats(ctx.meta)\n           |> insert_borrows(Map.get(ctx, :funs, %{}))\n           |> Core.from_expr()\n@@ -685,6 +686,52 @@ defmodule Rian.Lower do\n   defp catchall_pat?(%PVar{}), do: true\n   defp catchall_pat?(_), do: false\n \n+  # ── Char-arithmetic widening (ADR-0036, Rust only) ─────────────────────\n+  # On Rust a `Char` is a native `char`, and ordinal arithmetic widens to the\n+  # base `Int64` — so a `Char` operand of `+ - * rem div` is wrapped in\n+  # `__prim_char_code/1` (lowered to `char as i64`). BEAM/JS need no pass: there a\n+  # `Char` is already a codepoint integer, so the arithmetic runs as-is. `cvars`\n+  # is the set of `Char`-typed names in scope for the clause.\n+  @char_arith_ops ~w(+ - * rem div)\n+\n+  defp widen_char_arith({:bin, op, l, r}, cvars) when op in @char_arith_ops do\n+    {:bin, op, wrap_char(widen_char_arith(l, cvars), cvars),\n+     wrap_char(widen_char_arith(r, cvars), cvars)}\n+  end\n+\n+  defp widen_char_arith(node, cvars),\n+    do: Rian.Macro.map_node(node, &widen_char_arith(&1, cvars))\n+\n+  defp wrap_char({:char, _} = c, _cvars), do: {:call, {:id, \"__prim_char_code\"}, [c]}\n+\n+  defp wrap_char({:id, n} = v, cvars),\n+    do: if(MapSet.member?(cvars, n), do: {:call, {:id, \"__prim_char_code\"}, [v]}, else: v)\n+\n+  defp wrap_char(other, _cvars), do: other\n+\n+  # names bound at `Char` type in a clause: a `Char` param matched by a variable,\n+  # plus the element binders of a `Vec(Char)` param matched by a list/cons pattern\n+  # (the head `c` in `[c | rest]` is a `Char`; the tail `rest` is a `Vec(Char)`).\n+  defp char_vars(params, pats) do\n+    params\n+    |> Enum.zip(pats)\n+    |> Enum.reduce(MapSet.new(), fn {p, pat}, acc ->\n+      case p.type do\n+        \"Char\" -> add_var(acc, pat)\n+        \"Vec(Char)\" -> add_list_elem_vars(acc, pat)\n+        _ -> acc\n+      end\n+    end)\n+  end\n+\n+  defp add_var(acc, {:var, n}), do: MapSet.put(acc, n)\n+  defp add_var(acc, _), do: acc\n+\n+  defp add_list_elem_vars(acc, {:list, elems, _tail}),\n+    do: Enum.reduce(elems, acc, fn e, a -> add_var(a, e) end)\n+\n+  defp add_list_elem_vars(acc, _), do: acc\n+\n   # param positions that are an owned `iso Vec` destructured by a list/cons\n   # pattern — those match `param.as_slice()` and get owned rebinds in each arm\n   defp iso_cons_positions(func) do","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/lib%2Frian%2Flower.ex","sha":"680d463e53688e96a6046b30d0bc48ea7389b720","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Fbeam_test.exs","changes":10,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fbeam_test.exs?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":0,"filename":"test/rian/beam_test.exs","patch":"@@ -43,6 +43,16 @@ defmodule Rian.BeamTest do\n       assert mod.to_digit(?9) == 9\n     end\n \n+    test \"implicit Char ordinal arithmetic widens to Int64 and runs (ADR-0036)\" do\n+      # `c - '0'` needs no explicit conversion — a `Char` is a codepoint integer\n+      # on the BEAM; the checker types the result `Int64`\n+      {:ok, mod} =\n+        Beam.load(\"def dval(c Char) Int64 := c - '0'\", :rian_beam_char_arith)\n+\n+      assert mod.dval(?7) == 7\n+      assert mod.dval(?0) == 0\n+    end\n+\n     test \"multi-clause with a `when` guard\" do\n       {:ok, mod} =\n         Beam.load(","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Fbeam_test.exs","sha":"f367e7186d45b13932d033adacd289783e2bb6eb","status":"modified"},{"additions":8,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Fcheck_test.exs","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcheck_test.exs?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":0,"filename":"test/rian/check_test.exs","patch":"@@ -157,6 +157,14 @@ defmodule Rian.CheckTest do\n       assert Check.infer(Pratt.parse_body(\"__prim_char_code('0')\")) == \"Int64\"\n     end\n \n+    test \"ordinal arithmetic widens Char to its Int64 base (ADR-0036)\" do\n+      # `'9' - '0' = 9` is an Int64, not a Char\n+      assert Check.infer(Pratt.parse_body(\"'9' - '0'\")) == \"Int64\"\n+      assert Check.infer(Pratt.parse_body(\"'a' + 1\")) == \"Int64\"\n+      # a Char-typed param flows through: `c - '0' : Int64`\n+      assert Check.check(\"def dval(c Char) Int64 := c - '0'\") == :ok\n+    end\n+\n     test \"a typed binding may declare `Char`\" do\n       assert Check.check(\"def f(n Int64) Int64 := c Char := 'A' ; n\") == :ok\n       assert Check.infer(Pratt.parse_body(\"c Char := 'A' ; c\")) == \"Char\"","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Fcheck_test.exs","sha":"fe2289cc8b2a13bc37db985001bba438f75b9027","status":"modified"},{"additions":20,"blob_url":"https://github.com/wstein/rian-lab/blob/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Frange_test.exs","changes":20,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Frange_test.exs?ref=651c4baa945274da4a854d777bad6636fe01470b","deletions":0,"filename":"test/rian/range_test.exs","patch":"@@ -88,5 +88,25 @@ defmodule Rian.RangeTest do\n           assert {\"1 0\\n\", 0} = System.cmd(bin, [])\n       end\n     end\n+\n+    @tag :rust\n+    test \"implicit Char arithmetic lowers to `char as i64` casts and runs under rustc\" do\n+      case System.find_executable(\"rustc\") do\n+        nil ->\n+          :ok\n+\n+        rustc ->\n+          [{_, %{rust: rust}}] = Decl.compile(\"def dval(c Char) Int64 := c - '0'\")\n+          # the native `char` operands are widened to their codepoint base\n+          assert rust =~ \"(c as i64) - ('0' as i64)\"\n+\n+          dir = System.tmp_dir!()\n+          src = Path.join(dir, \"rian_chararith_#{System.unique_integer([:positive])}.rs\")\n+          bin = String.trim_trailing(src, \".rs\")\n+          File.write!(src, \"#{rust}\\nfn main() { println!(\\\"{}\\\", dval('7')); }\")\n+          {_, 0} = System.cmd(rustc, [\"--edition\", \"2021\", src, \"-o\", bin])\n+          assert {\"7\\n\", 0} = System.cmd(bin, [])\n+      end\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/651c4baa945274da4a854d777bad6636fe01470b/test%2Frian%2Frange_test.exs","sha":"d6286af88d39d872c76363d1a73f6911feffd4c7","status":"modified"}]}