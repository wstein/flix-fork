{"files":[{"additions":60,"blob_url":"https://github.com/wstein/rian-lab/blob/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro","changes":64,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro?ref=db2723bdfff4d971a6ac552f590f20a20d111c98","deletions":4,"filename":"site/src/components/site/LoweredCell.astro","patch":"@@ -22,7 +22,7 @@ const { cell, heading = true } = Astro.props as Props;\n const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n ---\n \n-<section id={cell.id} class=\"rl-cell\">\n+<section id={cell.id} class=\"rl-cell\" data-cell-id={cell.id}>\n   {\n     heading && (\n       <header class=\"rl-cell-head\">\n@@ -41,9 +41,15 @@ const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n       <div class=\"rl-pane-head\">\n         <span class=\"rl-rtile\">R</span>\n         <span class=\"rl-pane-name\">{cell.file}</span>\n-        <span class=\"rl-mono\" style=\"margin-left:auto; font-size:10.5px; color:#64748b; letter-spacing:0.06em;\">SOURCE</span>\n+        <span class=\"rl-mono rl-edithint\" title=\"Edit the source — it compiles and runs live in your browser\">\n+          ✎ EDIT · LIVE\n+        </span>\n       </div>\n-      <pre class=\"rl-code\" set:html={highlight(cell.source)}></pre>\n+      <div class=\"rl-codearea\">\n+        <pre class=\"rl-code rl-hl\" data-cell-hl set:html={highlight(cell.source)}></pre>\n+        <textarea class=\"rl-code rl-src\" data-cell-src spellcheck=\"false\" autocomplete=\"off\" autocapitalize=\"off\">{cell.source}</textarea>\n+      </div>\n+      <pre class=\"rl-cell-out rl-mono\" data-cell-out data-kind=\"idle\">▷ edit the source to compile + run it live</pre>\n     </div>\n     <div class=\"rl-col\">\n       {\n@@ -55,7 +61,7 @@ const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n                 {TARGET_META[t].label}\n               </span>\n             </div>\n-            <pre class=\"rl-code\" set:html={highlight(cell.panes[t])} />\n+            <pre class=\"rl-code\" data-cell-pane={t} set:html={highlight(cell.panes[t])} />\n           </div>\n         ))\n       }\n@@ -127,4 +133,54 @@ const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n       border-bottom: 1px solid var(--border);\n     }\n   }\n+\n+  /* ── live editable source (overlay editor, same metrics as .rl-code) ──────── */\n+  .rl-edithint {\n+    margin-left: auto;\n+    font-size: 10.5px;\n+    letter-spacing: 0.06em;\n+    color: var(--accent);\n+  }\n+  .rl-codearea {\n+    position: relative;\n+    min-width: 0;\n+  }\n+  .rl-codearea .rl-hl {\n+    pointer-events: none;\n+  }\n+  .rl-codearea .rl-src {\n+    position: absolute;\n+    inset: 0;\n+    width: 100%;\n+    height: 100%;\n+    resize: none;\n+    white-space: pre;\n+    background: transparent;\n+    color: transparent;\n+    caret-color: var(--accent);\n+    border: 0;\n+    outline: none;\n+  }\n+  /* live execution line under the source */\n+  .rl-cell-out {\n+    margin: 0;\n+    padding: 9px 18px;\n+    font-size: 11.5px;\n+    line-height: 1.5;\n+    white-space: pre-wrap;\n+    border-top: 1px solid var(--border);\n+    background: var(--code-chrome, var(--code-bg));\n+    color: #94a3b8;\n+    transition: color 0.12s ease;\n+  }\n+  .rl-cell-out[data-kind=\"hint\"],\n+  .rl-cell-out[data-kind=\"idle\"] {\n+    color: #64748b;\n+  }\n+  .rl-cell-out[data-kind=\"soft\"] {\n+    color: #d9a441;\n+  }\n+  .rl-cell-out[data-kind=\"ok\"] {\n+    color: #34d399;\n+  }\n </style>","raw_url":"https://github.com/wstein/rian-lab/raw/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fcomponents%2Fsite%2FLoweredCell.astro","sha":"588efc2d8a370612f38edef1a9442d17d42b50c8","status":"modified"},{"additions":101,"blob_url":"https://github.com/wstein/rian-lab/blob/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Flib%2Frian-live.ts","changes":101,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Flib%2Frian-live.ts?ref=db2723bdfff4d971a6ac552f590f20a20d111c98","deletions":0,"filename":"site/src/lib/rian-live.ts","patch":"@@ -0,0 +1,101 @@\n+// Shared live-evaluation helpers for the in-browser Rian compiler (ADR-0090),\n+// used by both the playground (`pages/playground.astro`) and the by-example\n+// tutorial (`pages/by-example.astro`). The compiler is the real Rian → JS\n+// compiler, compiled to JS by stock purs and served from `public/`.\n+\n+export type Compiler = { compile: (src: string) => string };\n+\n+// The deploy base, slash-safe: `/rian-lab` on GitHub Pages (configure-pages emits\n+// no trailing slash), `/` locally. `BASE_URL` is build-time replaced.\n+const base = import.meta.env.BASE_URL.replace(/\\/$/, \"\");\n+const compilerUrl = base + \"/rian-compiler.mjs\";\n+\n+// lazy, shared singleton — the ~300KB module loads once across every editor on a page.\n+let compilerP: Promise<Compiler> | null = null;\n+export const loadCompiler = (): Promise<Compiler> =>\n+  (compilerP ??= import(/* @vite-ignore */ compilerUrl));\n+export const compilerLoaded = (): boolean => compilerP != null;\n+\n+// A cheap \"is the buffer obviously mid-edit?\" check — NOT a parser, just enough to\n+// keep live eval quiet while a line is being written (the Rian parser is not yet\n+// error-recovering, so a half-typed line throws). Unbalanced delimiters/strings, an\n+// unclosed `do`, a dangling operator/keyword, or empty all read as incomplete.\n+export function looksIncomplete(srcText: string): boolean {\n+  const code = srcText.replace(/#[^\\n]*/g, \" \"); // drop line comments\n+  if (code.trim() === \"\") return true;\n+  // single walk: balance delimiters AND collapse each string literal to a `0`\n+  // placeholder so the `do`/`end` and trailing-operator checks see only real code.\n+  let inStr = false,\n+    q = \"\",\n+    par = 0,\n+    brk = 0,\n+    brc = 0,\n+    bare = \"\";\n+  for (let i = 0; i < code.length; i++) {\n+    const c = code[i];\n+    if (inStr) {\n+      if (c === q && code[i - 1] !== \"\\\\\") inStr = false;\n+      continue;\n+    }\n+    if (c === '\"' || c === \"'\") (inStr = true), (q = c), (bare += \"0\");\n+    else {\n+      if (c === \"(\") par++;\n+      else if (c === \")\") par--;\n+      else if (c === \"[\") brk++;\n+      else if (c === \"]\") brk--;\n+      else if (c === \"{\") brc++;\n+      else if (c === \"}\") brc--;\n+      bare += c;\n+    }\n+  }\n+  if (inStr || par > 0 || brk > 0 || brc > 0) return true;\n+  const doN = (bare.match(/\\bdo\\b/g) || []).length;\n+  const endN = (bare.match(/\\bend\\b/g) || []).length;\n+  if (doN > endN) return true;\n+  return /(:=|<~|<-|->|\\|>|<>|[-+*\\/<>=.,]|\\b(?:and|or|not|when|do|else|div|rem|in)\\b)\\s*$/.test(bare.trim());\n+}\n+\n+export type RunResult = { ok: boolean; text: string };\n+\n+// Execute an emitted ECMAScript module in a sandboxed iframe (`allow-scripts`,\n+// opaque origin — no page access). Imports it from a blob URL; calls `main()` if\n+// exported, else reports the module loaded and lists its exports.\n+export function runInSandbox(jsModule: string, timeoutMs = 4000): Promise<RunResult> {\n+  return new Promise((resolve) => {\n+    const frame = document.createElement(\"iframe\");\n+    frame.sandbox.add(\"allow-scripts\");\n+    frame.style.display = \"none\";\n+    let done = false;\n+    const finish = (r: RunResult) => {\n+      if (done) return;\n+      done = true;\n+      window.removeEventListener(\"message\", onMsg);\n+      frame.remove();\n+      resolve(r);\n+    };\n+    const onMsg = (ev: MessageEvent) => {\n+      if (ev.source === frame.contentWindow && ev.data && ev.data.__rian) finish(ev.data.r);\n+    };\n+    window.addEventListener(\"message\", onMsg);\n+    const runner =\n+      '<!doctype html><script type=\"module\">' +\n+      \"const code = \" +\n+      JSON.stringify(jsModule) +\n+      \";\" +\n+      \"(async () => { let r; try {\" +\n+      \"  const url = URL.createObjectURL(new Blob([code], { type: 'text/javascript' }));\" +\n+      \"  const mod = await import(url);\" +\n+      \"  if (typeof mod.main === 'function') r = { ok: true, text: 'main() = ' + JSON.stringify(mod.main()) };\" +\n+      \"  else r = { ok: true, text: 'module loaded · exports: ' + Object.keys(mod).join(', ') + ' (define `main()` to see a value)' };\" +\n+      \"} catch (e) { r = { ok: false, text: String((e && e.message) || e) }; }\" +\n+      \"  parent.postMessage({ __rian: true, r }, '*'); })();\" +\n+      \"<\\/script>\";\n+    frame.srcdoc = runner;\n+    document.body.appendChild(frame);\n+    setTimeout(() => finish({ ok: false, text: \"timed out (\" + timeoutMs + \"ms) — a runaway loop?\" }), timeoutMs);\n+  });\n+}\n+\n+// strip the leading `Kind: ` an exception message often carries, for terser output.\n+export const cleanError = (e: unknown): string =>\n+  String((e && (e as any).message) || e).replace(/^\\w+: /, \"\");","raw_url":"https://github.com/wstein/rian-lab/raw/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Flib%2Frian-live.ts","sha":"9c120d328d2011dff03221ae1bc85c87d462af6e","status":"added"},{"additions":62,"blob_url":"https://github.com/wstein/rian-lab/blob/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fpages%2Fby-example.astro","changes":62,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fby-example.astro?ref=db2723bdfff4d971a6ac552f590f20a20d111c98","deletions":0,"filename":"site/src/pages/by-example.astro","patch":"@@ -158,6 +158,68 @@ const onThis = [\n   </div>\n </SiteShell>\n \n+<script>\n+  import { highlight } from \"../lib/highlight\";\n+  import { loadCompiler, looksIncomplete, runInSandbox, cleanError } from \"../lib/rian-live\";\n+\n+  // Make every by-example cell a live editor: edit the source and it re-compiles +\n+  // runs in your browser (the same in-browser Rian → JS compiler the playground uses),\n+  // forgiving of half-typed input (`looksIncomplete`) and keeping the last good JS\n+  // pane on a transient error. The other target panes stay the verified reference.\n+  document.querySelectorAll<HTMLElement>(\"[data-cell-id]\").forEach((cell) => {\n+    const ta = cell.querySelector<HTMLTextAreaElement>(\"[data-cell-src]\");\n+    const hlEl = cell.querySelector<HTMLElement>(\"[data-cell-hl]\");\n+    const outEl = cell.querySelector<HTMLElement>(\"[data-cell-out]\");\n+    const jsPane = cell.querySelector<HTMLElement>('[data-cell-pane=\"js\"]');\n+    if (!ta || !hlEl || !outEl || !jsPane) return;\n+    const original = ta.value;\n+    const refJs = jsPane.innerHTML; // the verified reference emission, restored when unedited\n+\n+    let timer = 0;\n+    let gen = 0;\n+    const setOut = (kind: string, text: string) => ((outEl.dataset.kind = kind), (outEl.textContent = text));\n+\n+    async function liveEval() {\n+      const g = ++gen;\n+      const source = ta!.value;\n+      if (source === original) {\n+        jsPane!.innerHTML = refJs;\n+        setOut(\"idle\", \"▷ edit the source to compile + run it live\");\n+        return;\n+      }\n+      if (looksIncomplete(source)) {\n+        setOut(\"hint\", \"▍ keep typing…\");\n+        return;\n+      }\n+      let js: string;\n+      try {\n+        const { compile } = await loadCompiler();\n+        if (g !== gen) return; // a newer keystroke superseded this run\n+        js = compile(source);\n+      } catch (e) {\n+        if (g !== gen) return;\n+        setOut(\"soft\", \"… \" + cleanError(e)); // keep the last good JS pane\n+        return;\n+      }\n+      jsPane!.innerHTML = highlight(js);\n+      setOut(\"ok\", \"✓ compiled to JS — running…\");\n+      const res = await runInSandbox(js);\n+      if (g !== gen) return;\n+      setOut(res.ok ? \"ok\" : \"soft\", res.ok ? \"▷ \" + res.text : \"✗ runtime: \" + res.text);\n+    }\n+\n+    ta.addEventListener(\"input\", () => {\n+      hlEl.innerHTML = highlight(ta.value) + \"\\n\";\n+      clearTimeout(timer);\n+      timer = window.setTimeout(liveEval, 320);\n+    });\n+    ta.addEventListener(\"scroll\", () => {\n+      hlEl.scrollTop = ta.scrollTop;\n+      hlEl.scrollLeft = ta.scrollLeft;\n+    });\n+  });\n+</script>\n+\n <style>\n   .be-wrap {\n     max-width: 1240px;","raw_url":"https://github.com/wstein/rian-lab/raw/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fpages%2Fby-example.astro","sha":"ab9675ed202660836296b9ae6ca9bacc87fc5358","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/rian-lab/blob/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fpages%2Fplayground.astro","changes":103,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fplayground.astro?ref=db2723bdfff4d971a6ac552f590f20a20d111c98","deletions":93,"filename":"site/src/pages/playground.astro","patch":"@@ -100,6 +100,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n \n <script>\n   import { highlight } from \"../lib/highlight\";\n+  import { loadCompiler, compilerLoaded, looksIncomplete, runInSandbox, cleanError } from \"../lib/rian-live\";\n   import tour from \"../data/tour.json\";\n \n   type Cell = (typeof tour.cells)[number];\n@@ -120,16 +121,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   // the live JS the in-browser compiler emits for the current buffer (null = show the reference pane)\n   let liveJs: string | null = null;\n \n-  // the real Rian compiler, compiled to JS by stock purs (ADR-0090) — lazy-loaded once on first Run.\n-  // Served from `public/` beside the site; the URL must carry the deploy base (`/rian-lab` on GitHub\n-  // Pages, `/` locally) or it resolves against the domain root and 404s. `BASE_URL` is build-time\n-  // replaced but its trailing slash is NOT guaranteed (`configure-pages` emits `/rian-lab` with none),\n-  // so strip-and-re-add exactly one — the pattern the rest of the site uses. The variable +\n-  // `@vite-ignore` keep it a runtime import (a static literal would make Rollup resolve it at build).\n-  let compilerP: Promise<{ compile: (src: string) => string }> | null = null;\n-  const base = import.meta.env.BASE_URL.replace(/\\/$/, \"\");\n-  const compilerUrl = base + \"/rian-compiler.mjs\";\n-  const getCompiler = () => (compilerP ??= import(/* @vite-ignore */ compilerUrl));\n+  // the real Rian compiler + live-eval helpers are shared with the tutorial (`lib/rian-live`).\n \n   function renderEditor() {\n     const code = src.value;\n@@ -172,91 +164,16 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     out.textContent = \"Start typing — it compiles + runs live in your browser. Or press ▷ Run.\";\n   }\n \n-  // Execute an emitted ECMAScript module in a sandboxed iframe (`allow-scripts`, opaque origin — no\n-  // page access). Imports it from a blob URL; calls `main()` if exported, else reports it loaded.\n-  function runInSandbox(jsModule: string, timeoutMs = 4000): Promise<{ ok: boolean; text: string }> {\n-    return new Promise((resolve) => {\n-      const frame = document.createElement(\"iframe\");\n-      frame.sandbox.add(\"allow-scripts\");\n-      frame.style.display = \"none\";\n-      let done = false;\n-      const finish = (r: { ok: boolean; text: string }) => {\n-        if (done) return;\n-        done = true;\n-        window.removeEventListener(\"message\", onMsg);\n-        frame.remove();\n-        resolve(r);\n-      };\n-      const onMsg = (ev: MessageEvent) => {\n-        if (ev.source === frame.contentWindow && ev.data && ev.data.__rian) finish(ev.data.r);\n-      };\n-      window.addEventListener(\"message\", onMsg);\n-      const runner =\n-        '<!doctype html><script type=\"module\">' +\n-        \"const code = \" + JSON.stringify(jsModule) + \";\" +\n-        \"(async () => { let r; try {\" +\n-        \"  const url = URL.createObjectURL(new Blob([code], { type: 'text/javascript' }));\" +\n-        \"  const mod = await import(url);\" +\n-        \"  if (typeof mod.main === 'function') r = { ok: true, text: 'main() = ' + JSON.stringify(mod.main()) };\" +\n-        \"  else r = { ok: true, text: 'module loaded · exports: ' + Object.keys(mod).join(', ') + ' (define `main()` to see a value)' };\" +\n-        \"} catch (e) { r = { ok: false, text: String((e && e.message) || e) }; }\" +\n-        \"  parent.postMessage({ __rian: true, r }, '*'); })();\" +\n-        \"<\\/script>\";\n-      frame.srcdoc = runner;\n-      document.body.appendChild(frame);\n-      setTimeout(() => finish({ ok: false, text: \"timed out (\" + timeoutMs + \"ms) — a runaway loop?\" }), timeoutMs);\n-    });\n-  }\n-\n   // ── live, forgiving evaluation ──────────────────────────────────────────────\n   // Compile (and run) the buffer as the user types, debounced. The Rian parser is\n   // not error-recovering, so a half-typed line throws — so we (1) stay quiet for\n-  // input that is *obviously* mid-edit (unbalanced delimiters/strings, an unclosed\n-  // `do`, a dangling operator), and (2) on any compile error keep the LAST GOOD\n-  // panes and report it gently instead of flashing red. A generation token drops\n-  // stale results when a newer keystroke supersedes an in-flight compile/run.\n+  // input that is *obviously* mid-edit (`looksIncomplete`, shared), and (2) on any\n+  // compile error keep the LAST GOOD panes and report it gently instead of flashing\n+  // red. A generation token drops stale results when a newer keystroke supersedes\n+  // an in-flight compile/run.\n   let liveTimer = 0;\n   let evalGen = 0;\n \n-  // A cheap \"is the buffer obviously incomplete?\" check — NOT a parser, just enough\n-  // to avoid yelling at code the user is still in the middle of writing.\n-  function looksIncomplete(srcText: string): boolean {\n-    const code = srcText.replace(/#[^\\n]*/g, \" \"); // drop line comments\n-    if (code.trim() === \"\") return true;\n-    // single walk: balance delimiters AND collapse each string literal to a `0`\n-    // placeholder, so the `do`/`end` and trailing-operator checks below see only\n-    // real code (a string like \"do\" or a trailing \"text\" must not mislead them).\n-    let inStr = false,\n-      q = \"\",\n-      par = 0,\n-      brk = 0,\n-      brc = 0,\n-      bare = \"\";\n-    for (let i = 0; i < code.length; i++) {\n-      const c = code[i];\n-      if (inStr) {\n-        if (c === q && code[i - 1] !== \"\\\\\") inStr = false;\n-        continue; // string contents contribute nothing to `bare`\n-      }\n-      if (c === '\"' || c === \"'\") (inStr = true), (q = c), (bare += \"0\");\n-      else {\n-        if (c === \"(\") par++;\n-        else if (c === \")\") par--;\n-        else if (c === \"[\") brk++;\n-        else if (c === \"]\") brk--;\n-        else if (c === \"{\") brc++;\n-        else if (c === \"}\") brc--;\n-        bare += c;\n-      }\n-    }\n-    if (inStr || par > 0 || brk > 0 || brc > 0) return true; // unterminated string / open delimiter\n-    const doN = (bare.match(/\\bdo\\b/g) || []).length;\n-    const endN = (bare.match(/\\bend\\b/g) || []).length;\n-    if (doN > endN) return true; // an unclosed `do` block\n-    // a buffer ending on an operator/keyword is mid-expression\n-    return /(:=|<~|<-|->|\\|>|<>|[-+*\\/<>=.,]|\\b(?:and|or|not|when|do|else|div|rem|in)\\b)\\s*$/.test(bare.trim());\n-  }\n-\n   function setOut(kind: string, text: string) {\n     out.dataset.kind = kind;\n     out.textContent = text;\n@@ -271,13 +188,13 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     }\n     let js: string;\n     try {\n-      const { compile } = await getCompiler();\n+      const { compile } = await loadCompiler();\n       if (gen !== evalGen) return; // a newer keystroke superseded this run\n       js = compile(source);\n     } catch (e: any) {\n       if (gen !== evalGen) return;\n       // forgiving: a gate/parse error keeps the last good panes; report it softly.\n-      setOut(\"soft\", \"… \" + String((e && e.message) || e).replace(/^\\w+: /, \"\"));\n+      setOut(\"soft\", \"… \" + cleanError(e));\n       return;\n     }\n     liveJs = js;\n@@ -299,7 +216,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n       loadFile((b as HTMLElement).dataset.file!);\n       // once the compiler is loaded (the reader has engaged), switching cells\n       // evaluates the new one immediately — no need to re-press Run.\n-      if (compilerP) liveEval(false);\n+      if (compilerLoaded()) liveEval(false);\n     }),\n   );\n   document.querySelectorAll(\".pg-otab\").forEach((b) =>\n@@ -326,7 +243,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   $(\"#pg-reset\").addEventListener(\"click\", () => {\n     src.value = current.source;\n     renderEditor();\n-    if (compilerP) liveEval(false);\n+    if (compilerLoaded()) liveEval(false);\n     else setOut(\"hint\", \"Reset to \" + current.file + \".\");\n   });\n ","raw_url":"https://github.com/wstein/rian-lab/raw/db2723bdfff4d971a6ac552f590f20a20d111c98/site%2Fsrc%2Fpages%2Fplayground.astro","sha":"205d0c616e526a6dfbf30f9833b3905520fba5df","status":"modified"}]}