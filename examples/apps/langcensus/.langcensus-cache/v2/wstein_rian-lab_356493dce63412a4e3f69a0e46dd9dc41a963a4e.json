{"files":[{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/356493dce63412a4e3f69a0e46dd9dc41a963a4e/lib%2Frian%2Ftranspile.ex","changes":12,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Ftranspile.ex?ref=356493dce63412a4e3f69a0e46dd9dc41a963a4e","deletions":5,"filename":"lib/rian/transpile.ex","patch":"@@ -1744,11 +1744,13 @@ defmodule Rian.Transpile do\n        when is_map_key(@binops, op),\n        do: expr({op, [], [l, r]})\n \n-  # A match `=` reached in expression position (a `with`/`case` arm, a nested\n-  # statement) is Rian's bind `:=`, same as the block-statement path (`stmt/1`).\n-  # Without this it falls through to the generic local-call clause and mis-renders\n-  # as the prefix `=(l, r)`.\n-  defp expr({:=, _, [l, r]}), do: \"#{pat(l)} := #{expr(r)}\"\n+  # A match `=` reached in a true EXPRESSION position — an operator operand (`(j = e) != x`),\n+  # a call argument, a list element, or a `with` filter clause (Rian's `with` wants `<-`, not a\n+  # bind) — has no Rian image: a `:=` bind is a statement, never a sub-expression, so it must be\n+  # hoisted by hand. Emit an honest marker. (Statement-position binds render via `stmt/1`, which\n+  # does not route through here, so the genuinely-valid binds are untouched.)\n+  defp expr({:=, _, _} = n),\n+    do: ~s|TODO_PORT(\"assign in expression position — hoist the bind: #{escape(snippet(n))}\")|\n \n   defp expr({:-, _, [x]}), do: \"-#{paren_unary(x)}\"\n   defp expr({:not, _, [x]}), do: \"not #{paren_unary(x)}\"","raw_url":"https://github.com/wstein/rian-lab/raw/356493dce63412a4e3f69a0e46dd9dc41a963a4e/lib%2Frian%2Ftranspile.ex","sha":"71aee0656635e6810c584a1d47a1d7b1f2f94c7c","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/rian-lab/blob/356493dce63412a4e3f69a0e46dd9dc41a963a4e/test%2Frian%2Ftranspile_test.exs","changes":14,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ftranspile_test.exs?ref=356493dce63412a4e3f69a0e46dd9dc41a963a4e","deletions":2,"filename":"test/rian/transpile_test.exs","patch":"@@ -768,8 +768,18 @@ end|) =~ ~S|\"v=${x}!\"|\n       refute out =~ \"%{:lo\"\n     end\n \n-    test \"a match `=` in expression position renders as `:=`, not the prefix `=(l, r)`\" do\n-      out = rian(\"defmodule M do\\n  def g(x), do: with(y = f(x), do: y)\\nend\")\n+    test \"a match `=` in a true expression position renders an honest marker, not invalid Rian\" do\n+      # a `:=` bind is a statement, never a sub-expression — an assign as an operator operand\n+      # (or a `with` filter / call arg) has no Rian image, so it must be hoisted by hand. The\n+      # marker is honest (and parses); the old `=(l, r)` prefix and a mid-expression `:=` do not.\n+      out = rian(\"defmodule M do\\n  def g(x), do: (y = f(x)) != nil\\nend\")\n+      assert out =~ \"assign in expression position\"\n+      refute out =~ \"=(y\"\n+      refute out =~ \"(y := f(x))\"\n+    end\n+\n+    test \"a match `=` as a block statement renders as the bind `:=` (a valid position)\" do\n+      out = rian(\"defmodule M do\\n  def g(x) do\\n    y = f(x)\\n    h(y)\\n  end\\nend\")\n       assert out =~ \"y := f(x)\"\n       refute out =~ \"=(y\"\n     end","raw_url":"https://github.com/wstein/rian-lab/raw/356493dce63412a4e3f69a0e46dd9dc41a963a4e/test%2Frian%2Ftranspile_test.exs","sha":"5f3e4493fd495c9c2e136987e8bcd295d1683b91","status":"modified"}]}