{"files":[{"additions":20,"blob_url":"https://github.com/wstein/rian-lab/blob/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fcore.ex","changes":20,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcore.ex?ref=c8e0f9b22ae89c7377b17eb7012506e6b750d438","deletions":0,"filename":"lib/rian/core.ex","patch":"@@ -375,4 +375,24 @@ defmodule Rian.Core do\n     do: t |> Tuple.to_list() |> Enum.find_value(&first_unsupported(&1, unsup))\n \n   def first_unsupported(_node, _unsup), do: nil\n+\n+  @doc \"\"\"\n+  Reject any function whose body uses a construct a partial emitter cannot lower.\n+  For each clause, parse the body, find the first node whose struct is in `unsup`\n+  (`first_unsupported/2`), and `raise exception` naming the function, the\n+  construct, and `target`. Shared by `Rian.JS` and `Rian.JVM`; each passes its own\n+  struct→label map, target atom, and module-local `Unsupported` exception.\n+  \"\"\"\n+  def reject_unsupported!(funcs, unsup, target, exception) do\n+    Enum.each(funcs, fn f ->\n+      Enum.each(f.clauses, fn c ->\n+        body = c.body |> Rian.Pratt.parse_body() |> from_expr()\n+\n+        case first_unsupported(body, unsup) do\n+          nil -> :ok\n+          label -> raise exception, \"`#{f.name}`: #{label} is not yet supported on :#{target}\"\n+        end\n+      end)\n+    end)\n+  end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fcore.ex","sha":"bb8bfd184c12a4340ca3ac05659063303249456d","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fjs.ex","changes":43,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fjs.ex?ref=c8e0f9b22ae89c7377b17eb7012506e6b750d438","deletions":27,"filename":"lib/rian/js.ex","patch":"@@ -100,6 +100,21 @@ defmodule Rian.JS do\n     defexception [:message]\n   end\n \n+  # Fast-fail diagnostic: before emitting, scan each function body for a construct\n+  # the JS emitter does not yet implement and raise ONE clear error naming the\n+  # function and construct — instead of a deep `inspect`-dump surfacing mid-emission.\n+  # Reach stays *architectural* (atoms/Result are portable per ADR-0041); this is an\n+  # *implementation-status* check, so it covers only constructs the emitter handles\n+  # in NO context (a bare atom is left to the emitter's own `Unsupported`, since an\n+  # atom inside an FFI call like `:lists.reverse` IS lowered). `Core.reject_unsupported!`\n+  # runs the shared walk; this map is the JS-specific construct→label set.\n+  @js_unsupported %{\n+    Core.EWith => \"a `with` expression\",\n+    Core.ELambda => \"a lambda\",\n+    Core.ECapture => \"a function capture (`&(…)`)\",\n+    Core.ECaptureNamed => \"a function capture (`&name/arity`)\"\n+  }\n+\n   @doc \"Compile `src`'s functions to a single ECMAScript module (a string).\"\n   def compile(src) do\n     prog = Decl.parse(src)\n@@ -126,39 +141,13 @@ defmodule Rian.JS do\n     # JS keeps the `:impl` methods (they lower as plain functions) and regenerates\n     # the dispatcher with JS-native guards (ADR-0061 §3).\n     funcs = prog |> all_funcs() |> Enum.reject(&(Map.get(&1, :dispatch) == :dispatcher))\n-    reject_unsupported!(funcs)\n+    Core.reject_unsupported!(funcs, @js_unsupported, :js, Unsupported)\n     fn_js = Enum.map_join(funcs, \"\\n\\n\", &function_js(&1, i53))\n     disp_js = protocol_dispatchers_js(prog, i53)\n \n     [fn_js, disp_js] |> Enum.reject(&(&1 == \"\")) |> Enum.join(\"\\n\\n\")\n   end\n \n-  # Fast-fail diagnostic: before emitting, scan each function body for a construct\n-  # the JS emitter does not yet implement and raise ONE clear error naming the\n-  # function and construct — instead of a deep `inspect`-dump surfacing mid-emission.\n-  # Reach stays *architectural* (atoms/Result are portable per ADR-0041); this is an\n-  # *implementation-status* check, so it covers only constructs the emitter handles\n-  # in NO context (a bare atom is left to the emitter's own `Unsupported`, since an\n-  # atom inside an FFI call like `:lists.reverse` IS lowered).\n-  @js_unsupported %{\n-    Core.EWith => \"a `with` expression\",\n-    Core.ELambda => \"a lambda\",\n-    Core.ECapture => \"a function capture (`&(…)`)\",\n-    Core.ECaptureNamed => \"a function capture (`&name/arity`)\"\n-  }\n-  defp reject_unsupported!(funcs) do\n-    Enum.each(funcs, fn f ->\n-      Enum.each(f.clauses, fn c ->\n-        body = c.body |> Pratt.parse_body() |> Core.from_expr()\n-\n-        case Core.first_unsupported(body, @js_unsupported) do\n-          nil -> :ok\n-          label -> raise Unsupported, \"`#{f.name}`: #{label} is not yet supported on :js\"\n-        end\n-      end)\n-    end)\n-  end\n-\n   # every function the JS file emits: the top-level ones plus every `mod`'s,\n   # flattened into one namespace — JS erases module boundaries, so a cross-module\n   # call `Mod.fun(…)` lowers to a bare `fun(…)` (see the `EDot`-call clause). This","raw_url":"https://github.com/wstein/rian-lab/raw/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fjs.ex","sha":"1623829db657fe5ab4ec1420ec64709373376f41","status":"modified"},{"additions":18,"blob_url":"https://github.com/wstein/rian-lab/blob/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fjvm.ex","changes":47,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fjvm.ex?ref=c8e0f9b22ae89c7377b17eb7012506e6b750d438","deletions":29,"filename":"lib/rian/jvm.ex","patch":"@@ -82,6 +82,23 @@ defmodule Rian.JVM do\n     defexception [:message]\n   end\n \n+  # Fast-fail diagnostic (parity with `Rian.JS`): before emitting, scan each\n+  # function body for a construct the Tier-2 JVM emitter does not yet implement and\n+  # raise ONE clear error naming the function and construct, rather than a deep\n+  # `inspect`-dump mid-emission. Reach stays *architectural* (ADR-0041); this is an\n+  # *implementation-status* check over the constructs the emitter handles in NO\n+  # context. (Type-level gaps — `Vec`/`Map` params, etc. — are left to `kt_type`.)\n+  # `Core.reject_unsupported!` runs the shared walk; this map is the JVM-specific set.\n+  @jvm_unsupported %{\n+    Core.EWith => \"a `with` expression\",\n+    Core.ELambda => \"a lambda\",\n+    Core.ECapture => \"a function capture (`&(…)`)\",\n+    Core.ECaptureNamed => \"a function capture (`&name/arity`)\",\n+    Core.ETuple => \"a tuple\",\n+    Core.EMap => \"a map\",\n+    Core.EStruct => \"a struct construction\"\n+  }\n+\n   @doc \"Compile `src`'s types + functions to a single Kotlin source module (a string).\"\n   def compile(src) do\n     prog = Decl.parse(src)\n@@ -94,7 +111,7 @@ defmodule Rian.JVM do\n     # the BEAM `:dispatcher` is a guarded runtime type-test, not the Kotlin shape;\n     # protocol lowering for the JVM is a later increment.\n     funcs = prog |> all_funcs() |> Enum.reject(&(Map.get(&1, :dispatch) == :dispatcher))\n-    reject_unsupported!(funcs)\n+    Core.reject_unsupported!(funcs, @jvm_unsupported, :jvm, Unsupported)\n     type_decls = Enum.map_join(all_types(prog), \"\\n\\n\", &sum_decl/1)\n     fn_decls = Enum.map_join(funcs, \"\\n\\n\", &function_kt/1)\n     # inject the float-repr helper only when the program lowers `__prim_float_repr`.\n@@ -127,34 +144,6 @@ defmodule Rian.JVM do\n     \"\"\"\n   end\n \n-  # Fast-fail diagnostic (parity with `Rian.JS`): before emitting, scan each\n-  # function body for a construct the Tier-2 JVM emitter does not yet implement and\n-  # raise ONE clear error naming the function and construct, rather than a deep\n-  # `inspect`-dump mid-emission. Reach stays *architectural* (ADR-0041); this is an\n-  # *implementation-status* check over the constructs the emitter handles in NO\n-  # context. (Type-level gaps — `Vec`/`Map` params, etc. — are left to `kt_type`.)\n-  @jvm_unsupported %{\n-    Core.EWith => \"a `with` expression\",\n-    Core.ELambda => \"a lambda\",\n-    Core.ECapture => \"a function capture (`&(…)`)\",\n-    Core.ECaptureNamed => \"a function capture (`&name/arity`)\",\n-    Core.ETuple => \"a tuple\",\n-    Core.EMap => \"a map\",\n-    Core.EStruct => \"a struct construction\"\n-  }\n-  defp reject_unsupported!(funcs) do\n-    Enum.each(funcs, fn f ->\n-      Enum.each(f.clauses, fn c ->\n-        body = c.body |> Pratt.parse_body() |> Core.from_expr()\n-\n-        case Core.first_unsupported(body, @jvm_unsupported) do\n-          nil -> :ok\n-          label -> raise Unsupported, \"`#{f.name}`: #{label} is not yet supported on :jvm\"\n-        end\n-      end)\n-    end)\n-  end\n-\n   @doc \"\"\"\n   Assemble `src` into a JVM `.jar` at `jar_path` by emitting Kotlin (`compile/1`)\n   and invoking `kotlinc -include-runtime`. This is the **rung-B** path (ADR-0062):","raw_url":"https://github.com/wstein/rian-lab/raw/c8e0f9b22ae89c7377b17eb7012506e6b750d438/lib%2Frian%2Fjvm.ex","sha":"858e1d24e8dacc0860d6ce871d60bcaf318921ac","status":"modified"}]}