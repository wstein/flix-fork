{"files":[{"additions":20,"blob_url":"https://github.com/wstein/flatbars/blob/0f6cd5daadbb89628f8215c4c411465dfb755d12/docs%2Fplayground-smoke.md","changes":20,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/docs%2Fplayground-smoke.md?ref=0f6cd5daadbb89628f8215c4c411465dfb755d12","deletions":0,"filename":"docs/playground-smoke.md","patch":"@@ -37,6 +37,26 @@ preview views, confirm the iframe renders.\n - [ ] **HTML Preview** / **Markdown Preview** — iframe renders (text pane hidden).\n - [ ] **Render Data** — the view-model as YAML.\n - [ ] **Compiled JS** — the emitted module (FlatBars dialect), JS-highlighted.\n+- [ ] **Migrated MaxBars** — the template rewritten from Handlebars to MaxBars\n+      (template-grammar highlighted); residuals appear as leading `{{! … }}`\n+      comments. The tab is **absent** under a non-FlatBars engine (gates on the\n+      `migrate` capability). Opening `/lint`'s migrate examples via *Open in Lab*\n+      lands here.\n+\n+## Checklist — Lint diagnostics panel (ADR-019)\n+\n+The canonicalization lint is a **dock panel** (next to Truthiness). With a\n+template using a deprecated alias or legacy scoped variable\n+(e.g. `{{ plus a b }}`, or `{{#each xs}}{{ index }}{{/each}}` in RawBars/MaxBars):\n+\n+- [ ] the **Lint** tab appears in the diagnostics dock, with a finding count and an\n+      amber badge (green when there are none).\n+- [ ] each finding row shows the offending name and the message naming the\n+      canonical spelling.\n+- [ ] the tab is **absent** under a non-FlatBars engine (gates on the `lint`\n+      capability), and the scoped-variable lint does **not** fire in FullBars (where\n+      `{{@index}}` is canonical).\n+- [ ] opening `/lint`'s lint examples via *Open in Lab* lands with this panel open.\n \n ## Checklist — Truthiness diagnostics panel (ADR-022)\n ","raw_url":"https://github.com/wstein/flatbars/raw/0f6cd5daadbb89628f8215c4c411465dfb755d12/docs%2Fplayground-smoke.md","sha":"c1a29937057e7087cbb2fc3d7fef4697090ad525","status":"modified"},{"additions":88,"blob_url":"https://github.com/wstein/flatbars/blob/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Findex.html","changes":90,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=0f6cd5daadbb89628f8215c4c411465dfb755d12","deletions":2,"filename":"lab/index.html","patch":"@@ -1695,7 +1695,7 @@\n       // engine adapters so a reload never pairs a fresh index.html / example\n       // file with a stale cached adapter module.\n       import { createRenderer } from \"./stem.mjs?v=1\";\n-      import { createFlatBarsRenderer } from \"./flatbars.mjs?v=3\";\n+      import { createFlatBarsRenderer } from \"./flatbars.mjs?v=4\";\n       import { createMinBarsRenderer } from \"./minbars.mjs?v=1\";\n       // Engine-derived template highlighting (ADR-014): the shared presenter +\n       // the lexer's own tokenizer, in place of a hand-written tag regex.\n@@ -1741,7 +1741,7 @@\n         validView as ovValidView,\n         viewKind,\n         applyView,\n-      } from \"./output-view.mjs?v=2\";\n+      } from \"./output-view.mjs?v=3\";\n       import { EditorState, Annotation, StateField, StateEffect, Compartment } from \"https://esm.sh/@codemirror/state@6.5.2/es2022/state.mjs\";\n       import {\n         Decoration,\n@@ -3203,6 +3203,7 @@\n       let lastEscapeRuns = null;    // #5: { html, plain, raw, total }\n       let lastCoverage = [];        // #7: [{ kind, file, start, end, line, col, executed, count }]\n       let lastAnalyse = { ok: true, findings: [] }; // ADR-022: truthiness portability (Truthiness panel)\n+      let lastLint = { ok: true, findings: [] }; // ADR-019: canonicalization lint findings (Lint panel)\n       let lastRequiredAssigns = []; // engine reflection: assign names the program reads\n       let lastMissingAssigns = [];  // intersection: requiredAssigns NOT in current data\n \n@@ -3213,6 +3214,7 @@\n         if (view === \"data\") return lastData != null;\n         if (view === \"bytecode\") return lastProgram != null;\n         if (view === \"compiled\") return true; // depends only on the template source\n+        if (view === \"migrated\") return true; // depends only on the template source\n         return !!lastOutput;\n       }\n \n@@ -3405,6 +3407,25 @@\n             text = r.ok ? r.value : \"// compile error: \" + r.error;\n           }\n           setOutputDoc(text, Decoration.none);\n+        } else if (outputView === \"migrated\") {\n+          // The template rewritten from Handlebars to MaxBars source (the\n+          // `migrate` engine extension). Residuals the migrator could not safely\n+          // rewrite are surfaced as leading `{{! … }}` MaxBars comments, so the\n+          // pane stays valid MaxBars and a clean migration shows pure source.\n+          lastSegViews = [];\n+          let text = \"{{! this engine has no migrator }}\";\n+          if (typeof renderer.migrate === \"function\") {\n+            const r = renderer.migrate(templateSourceFor(\"main\"));\n+            if (!r.ok) {\n+              text = \"{{! migrate error: \" + r.error + \" }}\";\n+            } else {\n+              const head = (r.residuals || []).map((x) =>\n+                \"{{! residual — \" + x.kind + \": \" + x.message +\n+                (x.suggestion ? \" — \" + x.suggestion : \"\") + \" }}\").join(\"\\n\");\n+              text = head ? head + \"\\n\" + r.source : r.source;\n+            }\n+          }\n+          setOutputDoc(text, Decoration.none);\n         } else {\n           const { segViews, deco } = buildProvenance(lastOutput, lastSegments);\n           lastSegViews = segViews;\n@@ -3475,6 +3496,12 @@\n           outputEditor.dispatch({ effects: outLangComp.reconfigure([jsLang]) });\n           return;\n         }\n+        if (outputView === \"migrated\") {\n+          // Migrated output is MaxBars template source — highlight it with the\n+          // engine's own template grammar, like the source editor.\n+          outputEditor.dispatch({ effects: outLangComp.reconfigure([htmlLang(), flatbarsHighlighting]) });\n+          return;\n+        }\n         if (outputView !== \"source\") return;\n         if (outLangChoice === \"Auto\") {\n           const kind = loadedExampleIdx !== -1 ? examples[loadedExampleIdx].view : lastExView;\n@@ -3569,6 +3596,7 @@\n         lastTimings = null; lastAst = null; lastDataAccess = []; lastWhitespace = [];\n         lastDeps = null; lastCalls = []; lastEscapeRuns = null; lastCoverage = [];\n         lastAnalyse = { ok: true, findings: [] };\n+        lastLint = { ok: true, findings: [] };\n         lastRequiredAssigns = []; lastMissingAssigns = [];\n         const dataSource = yamlSrc; // the YAML tab's source (editor may show the transform)\n         const mainSource = tabs[0].source;\n@@ -3710,6 +3738,11 @@\n         lastAnalyse = typeof renderer.analyze === \"function\"\n           ? renderer.analyze({ source: mainSource }, data)\n           : { ok: true, findings: [] };\n+        // ADR-019: deprecated-alias + non-canonical scoped-variable findings for\n+        // the active dialect (the Lint dock panel; FlatBars engines only).\n+        lastLint = typeof renderer.lint === \"function\"\n+          ? renderer.lint(mainSource)\n+          : { ok: true, findings: [] };\n         refreshCoverageGutter();\n         // Engine-reported assigns that aren't present at the data root — a\n         // tight, false-positive-free \"missing assigns\" signal (Data Access).\n@@ -4643,6 +4676,12 @@\n           loadedExampleIdx = -1;\n           setExampleLabel(\"(custom)\");\n           setOutputView(validView(s.v));\n+          // A shared \"Lint\" link opens the requested dock panel (feature-gated\n+          // like any tab, so it no-ops under an engine that lacks the feature).\n+          if (typeof s.dk === \"string\") {\n+            const dt = dockTabById(s.dk);\n+            if (dt && tabVisible(dt)) { dockActiveTab = s.dk; setDockVisible(true); }\n+          }\n           showActiveTab(); run();\n           return true;\n         } catch { return false; }\n@@ -5370,6 +5409,15 @@\n             return lastAnalyse.findings.length ? \"warn\" : \"success\";\n           },\n           render: renderTruthiness },\n+        // ADR-019: deprecated aliases + non-canonical scoped variables, each with\n+        // the canonical spelling the editor quick-fix / `flatbars lint` steers to.\n+        { id: \"lint\", label: \"Lint\", requires: [\"lint\"],\n+          count: () => lastLint.findings.length,\n+          badge: () => {\n+            if (!lastLint.ok) return \"\";\n+            return lastLint.findings.length ? \"warn\" : \"success\";\n+          },\n+          render: renderLint },\n         // The read-only inspectors (moved here from the Sources pane). They are\n         // not analyses with a count — `noBadge` suppresses the badge — and follow\n         // the active file / last render (re-rendered by renderDock after each run).\n@@ -5887,6 +5935,44 @@\n         body.append(section);\n       }\n \n+      // Lint panel (ADR-019): the deprecated-alias + non-canonical scoped-variable\n+      // findings for the active dialect — the same canonicalization the editor\n+      // quick-fix and `flatbars lint` surface. Each row is severity + the message\n+      // that names the canonical spelling; a clean template is positive evidence.\n+      function renderLint(body) {\n+        const a = lastLint;\n+        const section = makeEl(\"div\", { class: \"cv-section\" });\n+        const plain = (s) => String(s).replace(/`/g, \"\");\n+        if (!a.ok) {\n+          section.append(makeEl(\"div\", { class: \"cv-empty\" },\n+            \"Lint unavailable: \" + (a.error || \"the template did not parse.\")));\n+          body.append(section);\n+          return;\n+        }\n+        const rows = a.findings;\n+        section.append(makeEl(\"div\", { class: \"cv-head\" }, [\n+          makeEl(\"strong\", {}, \"Lint\"),\n+          \" — \", String(rows.length), \" canonicalization finding\", rows.length === 1 ? \"\" : \"s\",\n+        ]));\n+        if (rows.length === 0) {\n+          section.append(makeEl(\"div\", { class: \"cv-empty\" },\n+            \"Every name is canonical — no deprecated aliases or legacy scoped variables.\"));\n+          body.append(section);\n+          return;\n+        }\n+        section.append(makeEl(\"div\", { class: \"cv-hint\" },\n+          \"Each finding renders identically today, but a canonical spelling keeps the template portable. The editor quick-fix and flatbars lint steer to it.\"));\n+        const list = makeEl(\"div\", { class: \"cv-rows\" });\n+        for (const f of rows) {\n+          list.append(makeEl(\"div\", { class: \"cv-row\" }, [\n+            makeEl(\"span\", { class: \"cv-kind\" }, f.name || f.severity),\n+            makeEl(\"span\", { class: \"cv-label\" }, plain(f.message)),\n+          ]));\n+        }\n+        section.append(list);\n+        body.append(section);\n+      }\n+\n       // Context Inspector: snapshots of the render context at the last-clicked\n       // output span, from the engine's `inspect_at` — one card per execution\n       // (loop bodies yield one per iteration).","raw_url":"https://github.com/wstein/flatbars/raw/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Findex.html","sha":"a3922c8f80a8fd69fcb9197f37fe6b529dfa0e68","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/flatbars/blob/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Foutput-view.mjs","changes":5,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Foutput-view.mjs?ref=0f6cd5daadbb89628f8215c4c411465dfb755d12","deletions":2,"filename":"lab/output-view.mjs","patch":"@@ -25,6 +25,7 @@ export const VIEW_TABS = [\n   { view: \"data\", label: \"Render Data\" },\n   { view: \"bytecode\", label: \"Bytecode\", requires: [\"bytecode-wire\"] },\n   { view: \"compiled\", label: \"Compiled JS\", requires: [\"compile-js\"] },\n+  { view: \"migrated\", label: \"Migrated MaxBars\", requires: [\"migrate\"] },\n ];\n \n // The views the loaded engine actually exposes (ADR-0020 gate applied).\n@@ -39,11 +40,11 @@ export function validView(view, features) {\n }\n \n // Whether a view paints into the read-only text editor (vs the preview iframe).\n-// The text views: Plain Text, Render Data, Bytecode, ST4, Compiled JS.\n+// The text views: Plain Text, Render Data, Bytecode, ST4, Compiled JS, Migrated.\n export function viewKind(view) {\n   const text =\n     view === \"source\" || view === \"data\" || view === \"bytecode\" || view === \"st4\" ||\n-    view === \"compiled\";\n+    view === \"compiled\" || view === \"migrated\";\n   return text ? \"text\" : \"preview\";\n }\n ","raw_url":"https://github.com/wstein/flatbars/raw/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Foutput-view.mjs","sha":"ae9a97cecac1763f67ac18e8191a4063d7093a7c","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/flatbars/blob/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Foutput-view.test.mjs","changes":13,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Foutput-view.test.mjs?ref=0f6cd5daadbb89628f8215c4c411465dfb755d12","deletions":3,"filename":"lab/output-view.test.mjs","patch":"@@ -21,14 +21,14 @@ import {\n } from \"./output-view.mjs\";\n \n // A FlatBars-shaped feature vector (mirrors flatbars.mjs BB_FEATURES, trimmed).\n-const FB = [\"surface-dialect\", \"core-dialect\", \"compile-js\"];\n-// An engine without the compile-js capability (e.g. a minimal/other engine).\n+const FB = [\"surface-dialect\", \"core-dialect\", \"compile-js\", \"migrate\"];\n+// An engine without the compile-js / migrate capabilities (e.g. MinBars).\n const NO_COMPILE = [\"surface-dialect\", \"core-dialect\"];\n \n // ── Layer 1: decision ───────────────────────────────────────────────────────\n \n test(\"viewKind: text views paint the editor, previews use the iframe\", () => {\n-  for (const v of [\"source\", \"data\", \"bytecode\", \"st4\", \"compiled\"]) {\n+  for (const v of [\"source\", \"data\", \"bytecode\", \"st4\", \"compiled\", \"migrated\"]) {\n     assert.equal(viewKind(v), \"text\", `${v} should be a text view`);\n   }\n   for (const v of [\"rendered\", \"markdown\"]) {\n@@ -55,6 +55,13 @@ test(\"validView keeps an exposed view and falls back to rendered otherwise\", ()\n   assert.equal(validView(\"bogus\", FB), \"rendered\");\n });\n \n+test(\"the Migrated MaxBars view is gated on the `migrate` capability\", () => {\n+  assert.ok(visibleViews(FB).some((t) => t.view === \"migrated\"), \"shown when migrate advertised\");\n+  assert.ok(!visibleViews(NO_COMPILE).some((t) => t.view === \"migrated\"), \"hidden otherwise\");\n+  assert.equal(validView(\"migrated\", FB), \"migrated\");\n+  assert.equal(validView(\"migrated\", NO_COMPILE), \"rendered\"); // gated off ⇒ fallback\n+});\n+\n // ── Layer 2: wiring (hand-rolled fake DOM — interface only, no layout) ────────\n \n // The two output containers as fakes; `applyView` only ever sets `.hidden`.","raw_url":"https://github.com/wstein/flatbars/raw/0f6cd5daadbb89628f8215c4c411465dfb755d12/lab%2Foutput-view.test.mjs","sha":"e361329cb87bc4c4bdb732183fd41103c95f91ee","status":"modified"}]}