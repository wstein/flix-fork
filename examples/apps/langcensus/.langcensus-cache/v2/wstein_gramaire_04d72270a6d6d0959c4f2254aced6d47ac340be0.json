{"files":[{"additions":44,"blob_url":"https://github.com/wstein/gramaire/blob/04d72270a6d6d0959c4f2254aced6d47ac340be0/site%2Fsrc%2Fpages%2Flab.astro","changes":48,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fpages%2Flab.astro?ref=04d72270a6d6d0959c4f2254aced6d47ac340be0","deletions":4,"filename":"site/src/pages/lab.astro","patch":"@@ -114,6 +114,15 @@ const TABS = [\n         </section>\n       </div>\n \n+      <!-- horizontal separator: editor split ↕ analysis drawer -->\n+      <div\n+        class=\"vgrip\"\n+        id=\"vgrip\"\n+        role=\"separator\"\n+        aria-label=\"Resize editor and drawer\"\n+      >\n+      </div>\n+\n       <!-- b) Analysis drawer -->\n       <div class=\"drawer\">\n         <div class=\"tabbar\" id=\"tabbar\">\n@@ -155,10 +164,20 @@ const TABS = [\n       /* a) split */\n       .split {\n         display: flex;\n-        min-height: 200px;\n-        flex: 1 1 auto;\n+        min-height: 60px;\n+        flex: 1 1 50%;\n         overflow: hidden;\n       }\n+      /* Horizontal separator between the editor split and the analysis drawer,\n+         so their vertical ratio is draggable (row-resize). */\n+      .vgrip {\n+        flex: 0 0 7px;\n+        cursor: row-resize;\n+        background: var(--border);\n+      }\n+      .vgrip:hover {\n+        background: var(--accent);\n+      }\n       .pane {\n         display: flex;\n         flex-direction: column;\n@@ -254,11 +273,11 @@ const TABS = [\n       }\n       /* b) drawer */\n       .drawer {\n-        flex: 1 1 45%;\n+        flex: 1 1 50%;\n         display: flex;\n         flex-direction: column;\n         border-top: 1px solid var(--border);\n-        min-height: 180px;\n+        min-height: 60px;\n         overflow: hidden;\n       }\n       .tabbar {\n@@ -994,6 +1013,27 @@ const TABS = [\n       });\n       window.addEventListener(\"mouseup\", () => (dragging = false));\n \n+      // Horizontal separator: drag the editor/drawer vertical ratio (clamp\n+      // 15–85% of their combined height).\n+      const drawer = document.querySelector<HTMLElement>(\".drawer\")!;\n+      const vgrip = $(\"vgrip\");\n+      let vdragging = false;\n+      vgrip.addEventListener(\"mousedown\", (e) => {\n+        vdragging = true;\n+        e.preventDefault();\n+      });\n+      window.addEventListener(\"mousemove\", (e) => {\n+        if (!vdragging) return;\n+        // Pixel model: fix the editor's height, let the drawer (flex-grow)\n+        // take the rest. Deterministic — no basis/grow tug-of-war. Clamp so\n+        // each keeps a usable minimum.\n+        const top = split.getBoundingClientRect().top;\n+        const total = drawer.getBoundingClientRect().bottom - top;\n+        const px = Math.max(60, Math.min(total - 60, e.clientY - top));\n+        split.style.flex = \"0 0 \" + px + \"px\";\n+      });\n+      window.addEventListener(\"mouseup\", () => (vdragging = false));\n+\n       safeReparse();\n     </script>\n ","raw_url":"https://github.com/wstein/gramaire/raw/04d72270a6d6d0959c4f2254aced6d47ac340be0/site%2Fsrc%2Fpages%2Flab.astro","sha":"c83fa601586070db130a710c052f94f9da631b2c","status":"modified"}]}