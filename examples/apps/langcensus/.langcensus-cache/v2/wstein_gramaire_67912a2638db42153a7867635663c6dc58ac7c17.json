{"files":[{"additions":11,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConformance.scala","changes":12,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConformance.scala?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":1,"filename":"core/src/main/scala/gramaire/Conformance.scala","patch":"@@ -106,8 +106,18 @@ object Conformance:\n     TestVector(\"rule with a semantic action\", \"Foo\\n: 'x' {% \\\\a -> a %} ;\", Outcome.Accept),\n     TestVector(\"two alternatives\", \"Foo\\n: 'x'\\n| 'y' ;\", Outcome.Accept),\n     TestVector(\"two rules\", \"A\\n: 'x' ;\\n\\nB\\n: 'y' ;\", Outcome.Accept),\n+    TestVector(\n+      \"single-line rule head, no newline before ':' (ANTLR/Bison-style)\",\n+      \"Foo : 'x' ;\",\n+      Outcome.Accept\n+    ),\n+    TestVector(\n+      \"two single-line rules back to back, no whitespace at all\",\n+      \"A:'x';B:'y';\",\n+      Outcome.Accept\n+    ),\n     TestVector(\"leading terminal, no lhs\", \"'x'\", Outcome.Reject),\n-    TestVector(\"missing newline after lhs\", \"Foo Bar\", Outcome.Reject),\n+    TestVector(\"two bare identifiers, no ':' at all\", \"Foo Bar\", Outcome.Reject),\n     TestVector(\"colon but empty body\", \"Foo\\n:\", Outcome.Reject),\n     TestVector(\"body starts with a bar\", \"Foo\\n| 'x'\", Outcome.Reject),\n     TestVector(\"missing terminating semicolon\", \"Foo\\n: 'x'\", Outcome.Reject),","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConformance.scala","sha":"1ff5c3367a17fd8e3cc8904828a3175d51f73b59","status":"modified"},{"additions":55,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala","changes":74,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":19,"filename":"core/src/main/scala/gramaire/Lexer.scala","patch":"@@ -116,40 +116,76 @@ object Lexer:\n \n   /** Reclassify newlines for the parser (line-continuation spec, §3). Only ONE `NL` shape survives:\n     * the head separator inside `IDENT NL :` (or `ATTR IDENT NL :`). Every other `NL` is dropped, so\n-    * a line break inside an alternative is insignificant — including, now, the newline between two\n+    * a line break inside an alternative is insignificant — including the newline between two\n     * consecutive rules, since a mandatory trailing `;` (Bootstrap.scala's `Rule`) unambiguously\n     * ends each rule without needing a preserved boundary newline to tell them apart. A prior\n     * revision of this function also kept that boundary `NL` (the one immediately before the next\n     * rule's own head) for exactly that disambiguation job; `;` replaced it, not layered alongside\n     * it.\n+    *\n+    * A rule head may also be written with NO newline at all before its `:` (`Foo : 'x' ;`,\n+    * ANTLR/Bison-style) — `;` already makes \"the token right after a `;`, or the very first token\n+    * in the document\" an unambiguous rule-start position, the same way it disambiguates consecutive\n+    * rules above, so a missing head `NL` there is synthesized (zero-width, ADR D59)\n+    * rather than left for the grammar to reject. This keeps `Rule`'s own two productions — and\n+    * every production id after them, in `CodegenScala.lrActionsScala` and `Lr.reduce` alike —\n+    * untouched: a rule head always presents an `IDENT NL ':'` shape to the parser, real or\n+    * synthesized. A `name:Sym` field (`IDENT ':' Sym`, always mid-`Body`, never right after a\n+    * `;`/at the start) is never mistaken for a head, because synthesis only ever fires at a\n+    * position a field can't occupy.\n     */\n   def normalizeNewlines(toks: Vector[Token]): Vector[Token] =\n-    def term(j: Int): Option[String] =\n-      if j >= 0 && j < toks.length then Some(toks(j).terminal) else None\n-\n-    def decide(i: Int, t: Token): Option[Token] =\n-      if t.terminal != \"NL\" then Some(t)\n-      else if term(i - 1) == Some(\"IDENT\") && term(i + 1) == Some(\":\") then Some(t) // head `NL`\n-      else None // every other NL, dropped\n-\n-    toks.zipWithIndex.flatMap { case (t, i) => decide(i, t) }\n+    normalizeNewlinesGeneric[Token](toks, _.terminal, (prev, term, txt) => Token(term, txt))\n \n   /** `normalizeNewlines`, but over a span-carrying token stream — the form `Lr.parseWith` needs to\n     * keep a failing token's source span reachable after normalization drops the insignificant\n     * `NL`s. Mirrors `normalizeNewlines`'s logic exactly (same predicate, `Spanned.terminal` in\n     * place of `Token.terminal`), duplicated rather than shared for the same reason `Scanner.scan`/\n-    * `scanSpanned` are two functions instead of one generic over token shape.\n+    * `scanSpanned` are two functions instead of one generic over token shape. A synthesized head\n+    * `NL` gets a zero-width span at the boundary right after the head `IDENT` (or `ATTR`-prefixed\n+    * `IDENT`), before the `:` — a sensible location if a future diagnostic ever needs to point at\n+    * it.\n     */\n   def normalizeNewlinesSpanned(toks: Vector[Spanned]): Vector[Spanned] =\n-    def term(j: Int): Option[String] =\n-      if j >= 0 && j < toks.length then Some(toks(j).terminal) else None\n-\n-    def decide(i: Int, t: Spanned): Option[Spanned] =\n-      if t.terminal != \"NL\" then Some(t)\n-      else if term(i - 1) == Some(\"IDENT\") && term(i + 1) == Some(\":\") then Some(t)\n-      else None\n+    normalizeNewlinesGeneric[Spanned](\n+      toks,\n+      _.terminal,\n+      (prev, term, txt) => Spanned(term, txt, prev.end, prev.end)\n+    )\n+\n+  // Shared implementation for `normalizeNewlines`/`normalizeNewlinesSpanned`: `mk(prev, terminal,\n+  // text)` builds a synthesized token of the caller's shape, given the token it is inserted right\n+  // after (for `Spanned`'s zero-width position; ignored by the plain `Token` form).\n+  private def normalizeNewlinesGeneric[T](\n+      toks: Vector[T],\n+      terminal: T => String,\n+      mk: (T, String, String) => T\n+  ): Vector[T] =\n+    def termOf(v: Vector[T], j: Int): Option[String] =\n+      if j >= 0 && j < v.length then Some(terminal(v(j))) else None\n+\n+    // A rule can only start right after a `;` (the previous rule's own terminator) or at the very\n+    // start of the document — never mid-`Body`, the only other place an `IDENT` can be followed by\n+    // `:`. So an `IDENT` (optionally `ATTR`-prefixed) at one of those two positions is always a rule\n+    // head, never a `name:Sym` field.\n+    def isBoundary(j: Int): Boolean = j < 0 || termOf(toks, j) == Some(\";\")\n+    def isHeadIdent(i: Int): Boolean =\n+      termOf(toks, i) == Some(\"IDENT\") &&\n+        (isBoundary(i - 1) || (termOf(toks, i - 1) == Some(\"ATTR\") && isBoundary(i - 2)))\n+\n+    val withSynthesizedHeads: Vector[T] = toks.zipWithIndex.flatMap { case (t, i) =>\n+      if isHeadIdent(i) && termOf(toks, i + 1) != Some(\"NL\") then Vector(t, mk(t, \"NL\", \"\"))\n+      else Vector(t)\n+    }\n+\n+    def decide(i: Int, t: T): Option[T] =\n+      if terminal(t) != \"NL\" then Some(t)\n+      else if termOf(withSynthesizedHeads, i - 1) == Some(\"IDENT\") &&\n+        termOf(withSynthesizedHeads, i + 1) == Some(\":\")\n+      then Some(t) // head `NL`, real or synthesized\n+      else None // every other NL, dropped\n \n-    toks.zipWithIndex.flatMap { case (t, i) => decide(i, t) }\n+    withSynthesizedHeads.zipWithIndex.flatMap { case (t, i) => decide(i, t) }\n \n   private def isLayout(c: Char): Boolean = c == ' ' || c == '\\t' || c == '\\r' || c == '\\n'\n   private def isDigit(c: Char): Boolean = c >= '0' && c <= '9'","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLexer.scala","sha":"2f83179d37bb2a002fc5989ecb1f2f4527b6b7c1","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","changes":7,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":2,"filename":"core/src/main/scala/gramaire/Lr.scala","patch":"@@ -556,8 +556,11 @@ object Lr:\n     parseWith(method, md).map(g => withExternals(withDocComments(g, md), md))\n \n   // A token-class definition line: an ALL-CAPS name then `:` on one\n-  // unindented line. A production head is a Mixed-case name on its OWN\n-  // line with the `:` on the next, so it never matches.\n+  // unindented line. A production head's name is always mixed-case\n+  // (`isUpperName` requires an all-caps, digit/underscore-only spelling), so\n+  // it never matches here regardless of whether the head's own `:` is on\n+  // this same line or the next (D59 — a rule head no longer requires a\n+  // newline before its `:`).\n   private def isTokenDef(l: String): Boolean =\n     val c0 = l.headOption\n     c0 != Some(' ') && c0 != Some('\\t') && {","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","sha":"8d1c266f2a34ec77c1de2be73e4e718ed4ad6651","status":"modified"},{"additions":63,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FLexerSuite.scala","changes":63,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FLexerSuite.scala?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":0,"filename":"core/src/test/scala/gramaire/LexerSuite.scala","patch":"@@ -141,6 +141,69 @@ class LexerSuite extends munit.FunSuite:\n     )\n   }\n \n+  test(\"normalizeNewlines synthesizes a missing head NL at the very start of the stream\") {\n+    assertEquals(\n+      Lexer\n+        .normalizeNewlines(Vector(tk(\"IDENT\", \"A\"), tk(\":\", \":\"), tk(\"TERM_LIT\", \"'x'\")))\n+        .map(_.terminal),\n+      Vector(\"IDENT\", \"NL\", \":\", \"TERM_LIT\")\n+    )\n+  }\n+\n+  test(\"normalizeNewlines synthesizes a missing head NL right after a `;` boundary\") {\n+    assertEquals(\n+      Lexer\n+        .normalizeNewlines(\n+          Vector(tk(\";\", \";\"), tk(\"IDENT\", \"B\"), tk(\":\", \":\"), tk(\"TERM_LIT\", \"'y'\"))\n+        )\n+        .map(_.terminal),\n+      Vector(\";\", \"IDENT\", \"NL\", \":\", \"TERM_LIT\")\n+    )\n+  }\n+\n+  test(\"normalizeNewlines synthesizes a missing head NL for an ATTR-prefixed head too\") {\n+    assertEquals(\n+      Lexer\n+        .normalizeNewlines(\n+          Vector(tk(\";\", \";\"), tk(\"ATTR\", \"inline\"), tk(\"IDENT\", \"B\"), tk(\":\", \":\"))\n+        )\n+        .map(_.terminal),\n+      Vector(\";\", \"ATTR\", \"IDENT\", \"NL\", \":\")\n+    )\n+  }\n+\n+  test(\"normalizeNewlines never synthesizes a head NL for a mid-body `name:Sym` field\") {\n+    // \"ex\" sits right after \":\", never after `;`/stream-start, so it can only be a field.\n+    assertEquals(\n+      Lexer\n+        .normalizeNewlines(\n+          Vector(\n+            tk(\"IDENT\", \"Factor\"),\n+            tk(\"NL\", \"\\n\"),\n+            tk(\":\", \":\"),\n+            tk(\"IDENT\", \"ex\"),\n+            tk(\":\", \":\"),\n+            tk(\"IDENT\", \"Expr\")\n+          )\n+        )\n+        .map(_.terminal),\n+      Vector(\"IDENT\", \"NL\", \":\", \"IDENT\", \":\", \"IDENT\")\n+    )\n+  }\n+\n+  test(\"normalizeNewlinesSpanned synthesizes a zero-width head NL right after the head IDENT\") {\n+    val toks = Vector(\n+      Spanned(\"IDENT\", \"A\", 0, 1),\n+      Spanned(\":\", \":\", 2, 3),\n+      Spanned(\"TERM_LIT\", \"'x'\", 4, 7)\n+    )\n+    val normalized = Lexer.normalizeNewlinesSpanned(toks)\n+    assertEquals(normalized.map(_.terminal), Vector(\"IDENT\", \"NL\", \":\", \"TERM_LIT\"))\n+    val nl = normalized(1)\n+    assertEquals(nl.start, 1)\n+    assertEquals(nl.end, 1)\n+  }\n+\n   test(\"an unterminated action is a LexError\") {\n     assert(Lexer.tokenize(\"X {% oops\").isLeft)\n   }","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FLexerSuite.scala","sha":"197b9a492e393b0bdd4382ffea078754bb4456f6","status":"modified"},{"additions":13,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/docs%2Ffmt-output-contract.md","changes":16,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Ffmt-output-contract.md?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":3,"filename":"docs/fmt-output-contract.md","patch":"@@ -141,12 +141,22 @@ Expr                          Expr\n Only one newline is structural: the one inside a **rule head** `IDENT NL :`\n (which is exactly what distinguishes a head from a same-line `name:Sym` field,\n `IDENT : Sym`). The lexer's `normalizeNewlines` pass keeps that one and drops\n-every other newline before the LR parser sees the stream — including, now, the\n+every other newline before the LR parser sees the stream — including the\n boundary newline that used to separate consecutive rules before `;` existed;\n the mandatory terminator replaced that job outright, not alongside it. A\n consequence: a `name:Sym` field must stay on one line — splitting it reads the\n-name as a head. `fmt` keeps an alternative on one line when it fits and may\n-wrap longer ones; wrapping is parse-invariant by construction.\n+name as a head.\n+\n+That head `NL` no longer has to be present in the source at all (D59): a rule\n+can only start right after a `;` or at the very start of the document, so\n+`normalizeNewlines` synthesizes a zero-width one there when it's missing —\n+`Foo : 'x' ;` parses identically to `Foo\\n  : 'x' ;`. `fmt`'s own canonical\n+output still always emits the name on its own line (this contract's layout\n+choice, not a language requirement); the single-line form is for grammars\n+authored or converted from a notation, like ANTLR's or Bison's, that never\n+needed the newline convention to begin with. `fmt` keeps an alternative on one\n+line when it fits and may wrap longer ones; wrapping is parse-invariant by\n+construction.\n \n ### Railroad diagrams\n ","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/docs%2Ffmt-output-contract.md","sha":"5b388bd56811f5bac2a1348cd596d23afbcc3780","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/docs%2Fmulti-backend-implementation-plan.md","changes":1,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":0,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -758,6 +758,7 @@ everything target-specific is independent.\n | D56 | `.gram.lock` records which `--diagram-view` produced committed artifacts, for provenance only | **A `diagramView` field on the sidecar `Lock`, following the exact optional/omit-when-default convention `sourceLayout` already established (D-numbered implicitly by that field's own prior addition): absent — and never written when `Source` — so every lock predating this field, and every lock `fmt` writes under the still-default `Source` view, keeps round-tripping byte-for-byte; present as `\"diagramView\": \"simplified\"` only when `--diagram-view=simplified` produced the committed sidecar SVGs. Like `sourceLayout`, `checkStructure`/`checkDrift` don't key on it at all — both classify a document from its fence content and headings alone, never from which view rendered a committed diagram, so this is maintainer-facing provenance, not a new gate.** | Closes a real, previously-silent gap: nothing on disk recorded which view produced a grammar's committed diagrams, so a maintainer (or a future audit) had no way to tell \"simplified\" from \"source\" short of re-running `fmt` and diffing. Deliberately not wired into `checkDrift` — the lock already has one established convention for a provenance-only field (`sourceLayout`), and this reuses it rather than inventing a second one. |\n | D57 | Railroad node navigation is shared code (`site/src/lab/railroadNav.ts`), bound to the renderer's own `data-rr-*` grouping | **Both the Lab and the Notebook island now bind nonterminal-node hover/click/keyboard navigation through one shared `bindRailroadNodeNav` helper, querying the `<g class=\"rr-node rr-node-nonterm\" data-rr-kind=\"...\" data-rr-label=\"...\">` grouping `Railroad.scala`'s `renderSvg` already emits (railroad-renderer-roadmap's phase 3, \"semantic affordances\" — this is the \"built from that structure\" half that hadn't landed yet). The Lab's own hover/click cross-highlight (pre-existing) was migrated off querying `rect.rr-nonterm` plus its text sibling separately (two listener targets per node, because the text painted on top of the rect) onto one listener on the `<g>` itself. The Notebook, which had no diagram interactivity at all, gets the same click/keyboard \"jump to that rule's cell\" affordance the outline sidebar already had (`jumpToRule`, resolving a label to a block index the same way `jumpToOutlineEntry` does). Every click/keydown handler calls `stopPropagation()`, since both islands nest the diagram inside a larger click/keydown-handling container (a cell's own \"click to edit\" surface, in the Notebook's case) — activating a nonterminal reference must navigate, never also trigger the ancestor's own activation.** | The metadata already existed (per-node `<title>`/`data-rr-*`, shipped earlier) but nothing consumed it — the Lab's interactivity predated that metadata and was never migrated, and the Notebook never got equivalent interactivity built at all. One shared module rather than two islands growing their own subtly-diverging DOM-query logic; verified end-to-end against the real Scala.js engine (not mocked), including that navigating never also opens the source cell it navigated from. |\n | D58 | Real `---` YAML frontmatter for `.gram.md`, dual-read migration, threaded through lock + site nav | **A leading, `---`-delimited frontmatter block (`name:`/`lang:`, room for future keys) replaces the embedded fenced `` ```gramaire `` \"General settings\" block as the recommended way to declare a grammar's own metadata — chosen over extending the existing fence (the lower-migration-cost default a design debate recommended first) because the user explicitly wanted genuine frontmatter: free extensibility (lists/maps/quoting), standard tooling recognition (every markdown-aware editor/linter/static-site generator already special-cases a leading `---` block), and discoverability (metadata at byte offset 0, not buried in the Nth fence). Parsed by a new, deliberately minimal hand-rolled subset (`core/src/main/scala/gramaire/Frontmatter.scala`: flow scalars, quoted strings with backslash escapes, flow lists — no anchors, tags, multi-document, or block scalars) rather than a general YAML library, since `core` cross-builds to Scala.js (a JVM-only library like SnakeYAML can't) and the schema this project actually needs never exceeds this subset. `Frontmatter.strip` blanks the stripped lines to SAME-LENGTH space-padded text (not merely the same line count), so every downstream `SrcSpan`/diagnostic offset computed against the remaining document stays byte-for-byte correct with no caller-side adjustment. `Lr.scala` dual-reads for one release: a leading frontmatter block wins when present; otherwise the old fence still works, with a deprecation warning pointing at `gramaire fmt`. `gramaire fmt` gained a migration codemod (`GramaireCheck.migrateToFrontmatter`) that lifts `name:`/`lang:` out of the old fence into a new leading block and removes the fence (run once across every shipped example/grammar file in this same change). `Lock` gained an optional `name` field (populated from `Lr.nameOf`), following the exact omit-when-default provenance convention D56 established for `diagramView` — neither `checkStructure` nor `checkDrift` key on it. Sitewide, `parseMarkdownLite` (the one parser shared by the Notebook prose cell, Paper view, and PDF export) gained a frontmatter-skip step alongside its existing `<details>`/HTML-comment housekeeping skips, so the leading block never renders as a literal \"--- name: X lang: Y ---\" paragraph — this was the original, concretely-reported bug that motivated the whole change. `LabResponse` gained a `name` field (`Lr.nameOf`, read independent of whether the grammar notation parses) so the Notebook's download-source/download-PDF filename comes from the real frontmatter-aware name instead of a stale client-side `%name` regex that matched neither the old fence nor the new frontmatter format and always fell back to `\"gramaire-notebook\"`.** | Closes a concretely reported bug (frontmatter rendering as literal prose in Paper/PDF) and a design question (\"how do I add real frontmatter, and how do the values reach the rest of the toolchain\") in one pass, since fixing the render bug properly meant giving frontmatter a real parser and a real place in the pipeline rather than a narrow one-off text-skip. The dual-read window (one release, per this document's own migration-safety convention) means no `.gram.md` in the wild breaks the moment this ships; removing the old-fence fallback and its `knownSettingDirectives` machinery entirely is an explicit follow-up, not bundled here. Full YAML support was explicitly rejected as scope creep — the fixed, small schema Gramaire actually needs (`name`, `lang`, room to grow) never justifies anchors/tags/multi-document/block-scalar support, and a general library would break the Scala.js cross-build this project already depends on. |\n+| D59 | A rule head's `NL` before `:` is optional — single-line, ANTLR/Bison-style rules now parse | **`Lexer.normalizeNewlines`/`normalizeNewlinesSpanned` now synthesize a zero-width head `NL` whenever a rule can legitimately start — right after a `;` (the previous rule's own terminator) or at the very start of the document — and the real one is missing, so `Foo : 'x' ;` parses identically to the traditional `Foo\\n  : 'x' ;`. `Rule`'s own two productions in `bootstrapGrammar`/`grammar/Productions.gram.md` are untouched: the parser still only ever sees an `IDENT NL ':'` shape, real or synthesized, so every downstream production id in `CodegenScala.lrActionsScala` and `Lr.reduce`, every golden IR/DOT/`.g4` fixture, and `Productions.gram.md`'s own railroad diagrams and `.gram.lock` stay byte-for-byte unchanged. A `name:Sym` field (`IDENT ':' Sym`) is never mistaken for a head, because synthesis only fires at a position a field can never occupy — mid-`Body`, a field's `:` is never immediately preceded by a `;` or the document start.** | ANTLR handles single-line rules because it never reuses `:` for two jobs (`=`/`+=` bind element labels, `:` only ever separates a rule head); Gramaire reuses `:` for both a rule's own head and a `name:Sym` field, which is why the newline convention existed in the first place (fmt-output-contract.md's \"Option A\"). The mandatory trailing `;` already made \"the token right after a `;`, or the document's first token\" an unambiguous rule-start position (that's what let a prior revision drop the *boundary* NL between consecutive rules, D-numbered implicitly by that revision); this reuses the exact same boundary to also drop the *requirement* that a head's own `NL` be present in the source, without touching the grammar itself. Synthesizing in the lexer rather than adding new `Rule` alternatives was a deliberate choice: `Rule` is nonterminal #3 of the self-hosting bootstrap grammar, so any new alternative shifts every later production's global id, cascading through three independently hand-synchronized tables (`CodegenScala.lrActionsScala`, `Lr.reduce`, and every golden fixture keyed to the `lr` corpus) for no behavioral gain a lexer-level fix doesn't already deliver. |\n \n ## Platform & tooling roadmap (D43)\n ","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/docs%2Fmulti-backend-implementation-plan.md","sha":"0e2e32751c5cefa8d3b8898438a561cbc4d3ed14","status":"modified"},{"additions":18,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/grammar%2FProductions.gram.md","changes":27,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/grammar%2FProductions.gram.md?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":9,"filename":"grammar/Productions.gram.md","patch":"@@ -28,7 +28,13 @@ unambiguous with a single token of lookahead:\n   newline separating consecutive rules before `;` existed; the mandatory\n   terminator replaced that job outright, not alongside it. (This resolves\n   the newline-significance question as \"Option A\"; see\n-  `docs/fmt-output-contract.md`.)\n+  `docs/fmt-output-contract.md`.) That head newline no longer has to be\n+  *present in the source* either (D59): `;` already makes \"right after a\n+  `;`, or the very start of the document\" an unambiguous rule-start\n+  position, so the lexer synthesizes one there when it's missing — `Foo :\n+  'x' ;` parses identically to `Foo\\n  : 'x' ;`. A `name:Sym` field is never\n+  mistaken for a head, because that synthesis only ever fires at a position\n+  a field can't occupy.\n - **Terminals** are written either as quoted literals — `'x'` or `\"x\"` (such\n   as `':'` and `'|'`) — or as ALL-CAPS lexer token classes (`IDENT`,\n   `TERM_LIT`, `ACTION`, `NL`).\n@@ -132,11 +138,14 @@ RuleList\n \n ## Rule\n \n-A rule is its name on its own line, then `:` and its `|`-separated alternatives,\n-ending with a mandatory `;` (mirroring Bison/YACC/ANTLR4's own convention). The\n-`NL` between the name and its `:` is load-bearing: it is the only thing that\n-tells a rule head (`IDENT NL :`) from a `name:Sym` field (`IDENT : Sym`), so the\n-head form is fixed and never written inline.\n+A rule is its name, then `:` and its `|`-separated alternatives, ending with a\n+mandatory `;` (mirroring Bison/YACC/ANTLR4's own convention) — conventionally\n+with the name on its own line, but a single-line ANTLR/Bison-style head\n+(`Foo : 'x' ;`) parses identically. The `NL` between the name and its `:` is\n+what tells a rule head (`IDENT NL :`) from a `name:Sym` field (`IDENT : Sym`);\n+it doesn't have to be written, though, since the lexer synthesizes it whenever\n+one is missing at a position only a rule head can occupy — right after the\n+previous rule's `;`, or at the very start of the document (D59).\n \n A rule may carry `#[attr]` attributes (e.g. `#[inline]`, which `Gramaire.Desugar`\n folds into use sites) before its name.\n@@ -477,10 +486,10 @@ SetItem\n Curated messages keyed by the parser state they are reported from.\n \n ```text\n-after IDENT NL:\n+after IDENT (NL synthesized or real) at a rule-start position:\n   Expected `:` to begin this rule's alternatives.\n-  A rule is its name on one line, then `:` and the first alternative\n-  on the next.\n+  A rule is its name, then `:` and its first alternative — on the\n+  same line or the next.\n \n after Body, lookahead is ATTR or IDENT (the shape of a new rule's own head):\n   Expected `;` to end the previous rule. A rule (like a token definition)","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/grammar%2FProductions.gram.md","sha":"a4f5e18c805a50e25410ad3f315363fa4f3b790e","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":18,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":4,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -31,10 +31,11 @@ from the concatenated content of every fenced code block whose info string is\n   line a `name:`/`lang:` directive → **Settings**; every line a\n   `%left`/`%right`/`%nonassoc` declaration → **Precedence**; every line an\n   unindented `ALLCAPS : …` token definition → **Tokens**; anything else → a\n-  nonterminal's **productions**. This is unambiguous by construction: no valid\n-  production can share a token definition's one-line shape, since a rule's `:`\n-  MUST follow a newline (`IDENT NL :`, §2) while a token definition's `:` is on\n-  the same line as its name. A suffixed opener (` ```gramaire tokens `,\n+  nonterminal's **productions**. This is unambiguous by construction: a token\n+  definition's name MUST be ALL-CAPS (§2), while a production's own name is\n+  always mixed-case, so the two shapes never overlap — regardless of whether a\n+  rule head's `:` sits on the same line as its name or the next one (§3). A\n+  suffixed opener (` ```gramaire tokens `,\n   ` ```gramaire settings `, ` ```gramaire precedence `, ` ```gramaire errors `) is\n   a removed notation and MUST be rejected with a located, actionable error\n   (`gramaire fmt --migrate` merges it into a bare fence).\n@@ -110,6 +111,15 @@ a newline happens once, in this pass.\n   break reads the name as a rule head.\n </Aside>\n \n+The head `NL` MAY also be absent from the source entirely: a rule can only\n+start right after a `;` (the previous rule's own terminator) or at the very\n+start of the document — never mid-`Body`, the one other place an `IDENT` can\n+be followed by `:`. So the same pass synthesizes a zero-width head `NL` at\n+either position when a real one isn't there, and `Foo : 'x' ;` (ANTLR/Bison-\n+style, no line break at all) parses identically to the traditional `Foo\\n  :\n+'x' ;`. A `name:Sym` field is never mistaken for a head this way, since\n+synthesis only ever fires at a position a field can't occupy.\n+\n ## 4. Desugaring to the epsilon-free core\n \n Before building tables, the surface grammar is lowered to a pure, epsilon-free","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"3de4a211598d8e921019b7e34ba7ad0310ea55db","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","changes":11,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":4,"filename":"site/src/content/docs/tutorials/intro.mdx","patch":"@@ -110,10 +110,13 @@ Reading the `gramaire` block:\n   terminal.\n \n <Aside type=\"caution\" title=\"Heads vs. fields\">\n-  A rule head is `Name` on its own line, then `:` on the next — the line break\n-  is load-bearing. It is the one thing that distinguishes a head from a\n-  `name:Sym` **field** (written `IDENT : Sym` with no break, see §4). So never\n-  write a head inline as `Name : …`.\n+  A rule head is conventionally `Name` on its own line, then `:` on the next —\n+  that line break is what distinguishes a head from a `name:Sym` **field**\n+  (written `IDENT : Sym` with no break, see §4). You can also write a head\n+  inline, `Name : …` (ANTLR/Bison-style) — Gramaire recognizes it as a head\n+  either way, since a field can only ever appear *inside* a rule's own body,\n+  never at a position where a new rule could start. `fmt`'s canonical layout\n+  always puts the name on its own line regardless of which style you write.\n </Aside>\n \n Add the remaining rules — `Term`, then `Factor` for numbers and parentheses:","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","sha":"631e53a89d84ec2a35e354ee5407e615a65a950e","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/gramaire/blob/67912a2638db42153a7867635663c6dc58ac7c17/spec%2Flexer-spec.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/spec%2Flexer-spec.md?ref=67912a2638db42153a7867635663c6dc58ac7c17","deletions":4,"filename":"spec/lexer-spec.md","patch":"@@ -47,10 +47,10 @@ used but never defined\") unless the block is in external mode (§6).\n Like the Settings- and Precedence-role fences, a Tokens-role fence is a\n hand-parsed sidecar notation, not itself a `gramaire` grammar — but unlike\n those two, it is never ambiguous with production content: a token\n-definition's ALL-CAPS-name-before-`:` shape can never overlap a valid\n-production, since the `lr` notation requires a newline between a rule's name\n-and its `:` (`IDENT NL :`) while a token definition's `:` is on the same line\n-as its name.\n+definition's name-before-`:` MUST be ALL-CAPS, while a production's own name\n+is always mixed-case (a nonterminal), so the two shapes never overlap\n+regardless of whether a rule head's `:` sits on the same line as its name or\n+the next one.\n \n ## 3. The regular sublanguage\n ","raw_url":"https://github.com/wstein/gramaire/raw/67912a2638db42153a7867635663c6dc58ac7c17/spec%2Flexer-spec.md","sha":"b4541c692d6b70758e1a0d683582c6595a3b9fdb","status":"modified"}]}