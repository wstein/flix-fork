{"files":[{"additions":7,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/.github%2Fworkflows%2Fci.yml","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.github%2Fworkflows%2Fci.yml?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":6,"filename":".github/workflows/ci.yml","patch":"@@ -209,13 +209,14 @@ jobs:\n         # Ll.recognize on every vector.\n         run: npm run check:atn-ts-parity\n \n-      - name: scala-peg / scala-peg-fastparse backend parity gate (PEG-engine plan Phases 3-4)\n+      - name: scala-peg backends parity gate (PEG-engine plan Phases 3-4)\n         # Runs the conformance corpus through `gramaire.cli.ScalaPegParityMain` (sbt runMain) for\n-        # BOTH IR.rewritten-reading backends — the dependency-free `scala-peg` and the opt-in\n-        # `scala-peg-fastparse` — which writes each grammar's generated output into the throwaway\n-        # `codegen-scratch` sbt subproject and prints Ll.parse's own accept/reject + rendered Cst;\n-        # a second sbt command in the same invocation compiles (against the real `fastparse`\n-        # library for that backend) and runs the generated Scala for real, proving each emitted\n+        # ALL THREE IR.rewritten-reading backends — the dependency-free `scala-peg` and the two\n+        # opt-in production-grade alternatives, `scala-peg-fastparse` and `scala-peg-combinators`\n+        # — which writes each grammar's generated output into the throwaway `codegen-scratch` sbt\n+        # subproject and prints Ll.parse's own accept/reject + rendered Cst; a second sbt command\n+        # in the same invocation compiles (against the real `fastparse`/`scala-parser-combinators`\n+        # library for those backends) and runs the generated Scala for real, proving each emitted\n         # PEG parser — not just its golden text — actually reproduces Ll.parse's exact Cst, the\n         # same behavioral proof the atn-ts gate above gives its backend.\n         run: npm run check:scala-peg-parity","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/.github%2Fworkflows%2Fci.yml","sha":"bceeaf673823c7799cb1494fbd96eabffb9130ab","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/build.sbt","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/build.sbt?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":3,"filename":"build.sbt","patch":"@@ -92,10 +92,12 @@ lazy val codegenScratch = project\n   .settings(\n     name := \"gramaire-codegen-scratch\",\n     publish / skip := true,\n-    // Only the opt-in `scala-peg-fastparse` backend's generated code uses this — the\n-    // dependency-free `scala-peg` backend's own generated code doesn't reference it at all, so\n-    // it's simply unused (harmlessly) whenever `codegen-scratch` compiles that backend's output.\n+    // Only the opt-in `scala-peg-fastparse`/`scala-peg-combinators` backends' generated code uses\n+    // these — the dependency-free `scala-peg` backend's own generated code references neither, so\n+    // each is simply unused (harmlessly) whenever `codegen-scratch` compiles a different backend's\n+    // output.\n     libraryDependencies += \"com.lihaoyi\" %% \"fastparse\" % \"3.1.1\",\n+    libraryDependencies += \"org.scala-lang.modules\" %% \"scala-parser-combinators\" % \"2.4.0\",\n   )\n \n lazy val root = project","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/build.sbt","sha":"0538779ae60b15f5a61aed7db9df48b814ee5bf5","status":"modified"},{"additions":10,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala","changes":15,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":5,"filename":"cli/jvm/src/main/scala/gramaire/cli/ScalaPegParityMain.scala","patch":"@@ -44,9 +44,10 @@ object ScalaPegParityMain:\n     case _           => None\n \n   private def emitFor(backendKey: String): Option[IR => String] = backendKey match\n-    case \"scala-peg\"           => Some(BackendScalaPeg.emit)\n-    case \"scala-peg-fastparse\" => Some(BackendScalaPegFastparse.emit)\n-    case _                     => None\n+    case \"scala-peg\"             => Some(BackendScalaPeg.emit)\n+    case \"scala-peg-fastparse\"   => Some(BackendScalaPegFastparse.emit)\n+    case \"scala-peg-combinators\" => Some(BackendScalaPegCombinators.emit)\n+    case _                       => None\n \n   // Writes `Generated.scala` under `scratchSrcDir/gramaire/scratch/` — always this one fixed path,\n   // named \"Generated\" regardless of the grammar's own name (see the module header) — via\n@@ -62,7 +63,11 @@ object ScalaPegParityMain:\n       backendKey: String\n   ): Either[String, Unit] =\n     emitFor(backendKey) match\n-      case None => Left(s\"unknown backend '$backendKey' (expected scala-peg | scala-peg-fastparse)\")\n+      case None =>\n+        Left(\n+          s\"unknown backend '$backendKey' \" +\n+            \"(expected scala-peg | scala-peg-fastparse | scala-peg-combinators)\"\n+        )\n       case Some(emit) =>\n         IR.buildIRP(prec, Method.Canonical, \"Generated\", g) match\n           case Left(conflicts) => Left(s\"LR(1) conflicts building the table: $conflicts\")\n@@ -133,6 +138,6 @@ object ScalaPegParityMain:\n       case _ =>\n         System.err.println(\n           \"usage: ScalaPegParityMain <scratchSrcDir> <vectorsPath> \" +\n-            \"<calc|json|ecma404|calc-prec> <scala-peg|scala-peg-fastparse>\"\n+            \"<calc|json|ecma404|calc-prec> <scala-peg|scala-peg-fastparse|scala-peg-combinators>\"\n         )\n         sys.exit(1)","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala","sha":"ccdcfe9c3ec10e6a8f4b1176f3d75039087a8987","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":1,"filename":"cli/jvm/src/test/scala/gramaire/cli/MainSuite.scala","patch":"@@ -7,6 +7,7 @@ import gramaire.{\n   BackendJs,\n   BackendRegistry,\n   BackendScalaPeg,\n+  BackendScalaPegCombinators,\n   BackendScalaPegFastparse,\n   BackendTs,\n   Grammar,\n@@ -117,9 +118,12 @@ class MainSuite extends munit.FunSuite:\n     // scala-peg reads only IR.rewritten (the ll-star-only CST-fold-back section).\n     assert(!Main.backendSupportsStrategy(BackendScalaPeg.backend, \"lr\"))\n     assert(Main.backendSupportsStrategy(BackendScalaPeg.backend, \"ll-star\"))\n-    // scala-peg-fastparse reads the same IR.rewritten section, so the same gate applies.\n+    // scala-peg-fastparse/scala-peg-combinators read the same IR.rewritten section, so the same\n+    // gate applies to both.\n     assert(!Main.backendSupportsStrategy(BackendScalaPegFastparse.backend, \"lr\"))\n     assert(Main.backendSupportsStrategy(BackendScalaPegFastparse.backend, \"ll-star\"))\n+    assert(!Main.backendSupportsStrategy(BackendScalaPegCombinators.backend, \"lr\"))\n+    assert(Main.backendSupportsStrategy(BackendScalaPegCombinators.backend, \"ll-star\"))\n   }\n \n   test(\"BackendRegistry: atn-ts is registered and reachable by name\") {\n@@ -137,6 +141,13 @@ class MainSuite extends munit.FunSuite:\n     )\n   }\n \n+  test(\"BackendRegistry: scala-peg-combinators is registered and reachable by name\") {\n+    assertEquals(\n+      BackendRegistry.findBackend(\"scala-peg-combinators\"),\n+      Some(BackendScalaPegCombinators.backend)\n+    )\n+  }\n+\n   test(\n     \"strategyIgnoresPredicates: an lr build of a `{%? %}` grammar is flagged, ll-star is not\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","sha":"15f1ea1ea0a9e3005d4da64131697cdb5042d9cf","status":"modified"},{"additions":22,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","changes":22,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":0,"filename":"core/.jvm/src/test/scala/gramaire/BackendGoldenSuite.scala","patch":"@@ -133,6 +133,28 @@ class BackendGoldenSuite extends munit.FunSuite:\n             )\n   }\n \n+  // A golden-text pin, same convention as every other backend above — but see\n+  // site/scripts/check-scala-peg-parity.mjs for the test that actually matters for this backend:\n+  // a golden diff alone can't tell a correctly-translated IRProv/IRRuleBody from one that merely\n+  // still emits stable-looking text, only compiling (against the real\n+  // `scala-parser-combinators` library, via `codegen-scratch`) and running the emitted Scala can.\n+  test(\n+    \"scala-peg-combinators: examples/calc.gram.md under --strategy ll-star matches the committed golden\"\n+  ) {\n+    val md = readFile(\"examples/calc.gram.md\")\n+    Lr.parse(md) match\n+      case Left(e) => fail(s\"could not parse examples/calc.gram.md: $e\")\n+      case Right(g) =>\n+        IR.buildIR(Method.Canonical, \"Calc\", g) match\n+          case Left(_) => fail(\"could not build IR for calc\")\n+          case Right(ir) =>\n+            val irLl = IR.withStrategy(\"ll-star\", g, ir, Lr.precedenceOf(md))\n+            assertEquals(\n+              BackendScalaPegCombinators.emit(irLl),\n+              readFile(\"test/golden/CalcCombinators.scala\")\n+            )\n+  }\n+\n   test(\n     \"js: calc-js bakes its inline actions into one evaluate(cst), matching the committed golden\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","sha":"60e8eaaa4dc0ac858f17bd3c985a67c05a4a6d37","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","changes":3,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":1,"filename":"core/src/main/scala/gramaire/BackendRegistry.scala","patch":"@@ -14,7 +14,8 @@ object BackendRegistry:\n       BackendJs.backend,\n       BackendAtnTs.backend,\n       BackendScalaPeg.backend,\n-      BackendScalaPegFastparse.backend\n+      BackendScalaPegFastparse.backend,\n+      BackendScalaPegCombinators.backend\n     )\n \n   /** Find a backend by its `--backend` name. */","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","sha":"01120475cff898ef627ec58d58282eccbcb3b02b","status":"modified"},{"additions":17,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala","changes":23,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":6,"filename":"core/src/main/scala/gramaire/BackendScalaPeg.scala","patch":"@@ -17,6 +17,12 @@ package gramaire\n //     `IRRewrittenSuite`'s own reference-walker `loop` function, which is this backend's design\n //     proof; the parity gate (`ScalaPegParityMain`/`check-scala-peg-parity.mjs`) proves the\n //     translation is faithful by actually compiling and running the emitted code.\n+//\n+// `ident`/`strLit`/`originExpr`/`tagExpr`/`indented` are `private[gramaire]` (not `private`) so\n+// `BackendScalaPegCombinators` (Phase 4's opt-in, `scala-parser-combinators`-based backend, which\n+// reads the same `IRRewritten*` shapes and needs the identical `IRProv`-to-Cst-tagging-expression\n+// translation, just wired into a different sequencing library) can reuse them verbatim rather\n+// than duplicating a second, driftable copy.\n object BackendScalaPeg:\n   val backend: Backend = Backend(\n     name = \"scala-peg\",\n@@ -29,7 +35,7 @@ object BackendScalaPeg:\n     s\"// GENERATED by Gramaire from the `${ir.grammar.name}` grammar — $what.\\n\" +\n       \"// Do not edit; regenerate with `gramaire emit --backend scala-peg --strategy ll-star`.\"\n \n-  private def strLit(s: String): String =\n+  private[gramaire] def strLit(s: String): String =\n     \"\\\"\" + s.flatMap {\n       case '\"'  => \"\\\\\\\"\"\n       case '\\\\' => \"\\\\\\\\\"\n@@ -41,21 +47,26 @@ object BackendScalaPeg:\n   // A grammar rule name is a `.gram.md` identifier already (letters/digits/underscore) — this is\n   // a defensive normalization, not something the real corpus ever exercises, so an arbitrary\n   // (deterministic) substitution for anything else is fine.\n-  private def ident(name: String): String =\n+  private[gramaire] def ident(name: String): String =\n     val safe = name.map(c => if c.isLetterOrDigit || c == '_' then c else '_')\n     if safe.headOption.exists(_.isDigit) then s\"_$safe\" else safe\n \n-  private def parseFnName(rule: String): String = s\"parse_${ident(rule)}\"\n+  private[gramaire] def parseFnName(rule: String): String = s\"parse_${ident(rule)}\"\n \n-  private def originExpr(origin: IRAltOrigin, kidsExpr: String): String = origin match\n+  private[gramaire] def originExpr(origin: IRAltOrigin, kidsExpr: String): String = origin match\n     case IRAltOrigin.Unwrap           => s\"$kidsExpr.head\"\n     case IRAltOrigin.Original(prodId) => s\"Cst.Branch($prodId, $kidsExpr)\"\n \n   // Translates one alt's `IRProv` to a Scala expression computing its tagged `Cst`, given\n   // `kidsVar` (a `Vector[Cst]` already bound in scope) and — only meaningful inside a `Folded`\n   // rule's own operator alts — `accVar` (the fold's running accumulator). Mirrors\n   // `IRRewrittenSuite.resolveProv` exactly, just emitting Scala source instead of interpreting.\n-  private def tagExpr(prov: IRProv, kidsVar: String, accVar: Option[String], depth: Int): String =\n+  private[gramaire] def tagExpr(\n+      prov: IRProv,\n+      kidsVar: String,\n+      accVar: Option[String],\n+      depth: Int\n+  ): String =\n     prov match\n       case IRProv.Leaf(origin) => originExpr(origin, kidsVar)\n       case IRProv.OpLeaf(origin) =>\n@@ -161,7 +172,7 @@ ${opDefs.mkString(\"\\n\")}\n   // defined by one grammar's generated file can never collide with another's on the same\n   // classpath (`codegen-scratch`'s parity driver may compile more than one grammar's output at\n   // once — see Phase 3 of the PEG-engine plan).\n-  private def indented(body: String): String =\n+  private[gramaire] def indented(body: String): String =\n     body.linesIterator.map(l => if l.isEmpty then l else s\"  $l\").mkString(\"\\n\")\n \n   /** Emit the `<Name>.scala` module (a single `object <Name>:` — see `indented`'s doc for why), or","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala","sha":"0baccb4424b09d93d7665efd8ddd25c754cff883","status":"modified"},{"additions":142,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegCombinators.scala","changes":142,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegCombinators.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":0,"filename":"core/src/main/scala/gramaire/BackendScalaPegCombinators.scala","patch":"@@ -0,0 +1,142 @@\n+package gramaire\n+\n+// A `Cst`-capable, `scala-parser-combinators`-based codegen backend — Phase 4 of the PEG-engine\n+// plan (docs/all-star-port-plan.md), reading the same `IR.rewritten` section `BackendScalaPeg`\n+// (the dependency-free backend, Phase 2) does.\n+//\n+// Originally planned around `fastparse` for \"production-grade performance\", but fastparse's\n+// `ParserInput` is fundamentally `Char`/`Byte`-oriented (confirmed: its only supported inputs are\n+// `String`, `Array[Byte]`, `java.io.InputStream`, or another `geny.Readable` — no generic\n+// token-stream support), while this port's whole pipeline works over a pre-lexed `Vector[Token]`.\n+// `scala.util.parsing.combinator.Parsers` is generic over `type Elem`, so it parses a token\n+// stream directly via a custom `Reader[Token]` — no character-encoding workaround needed — at the\n+// cost of being a separately-versioned, less actively maintained module than fastparse (a real\n+// tradeoff, not a free substitution; recorded in the PEG-engine plan's own ADR).\n+//\n+// The two correctness properties `BackendScalaPeg` established are properties of ANY ordered-\n+// choice combinator library, not something specific to hand-rolled backtracking, so both carry\n+// over unchanged: `scala-parser-combinators`' own `|` commits to the first alternative that\n+// locally succeeds — it does NOT retry a different alternative merely because something AFTER the\n+// `|` later fails (the same PEG \"hiding problem\" `BackendScalaPeg`'s own doc explains) — so\n+// `IRRuleBody.Plain` still needs its `Unwrap`-last ordering, and `IRRuleBody.Folded` still needs\n+// its explicit repetition (`rep`) shape rather than raw ordered \"no-tail | with-tail\" alts.\n+//\n+// Reuses `BackendScalaPeg`'s `ident`/`strLit`/`originExpr`/`tagExpr`/`indented` (widened to\n+// `private[gramaire]` there) for the identical `IRProv`-to-Cst-tagging-expression translation —\n+// only the sequencing/matching syntax differs between the two backends.\n+object BackendScalaPegCombinators:\n+  val backend: Backend = Backend(\n+    name = \"scala-peg-combinators\",\n+    capabilities = Vector(Capability.Cst),\n+    strategies = Vector(\"ll-star\"),\n+    emit = ir => Vector(Output(s\"${ir.grammar.name}.scala\", emit(ir)))\n+  )\n+\n+  private def banner(ir: IR, what: String): String =\n+    s\"// GENERATED by Gramaire from the `${ir.grammar.name}` grammar — $what.\\n\" +\n+      \"// Do not edit; regenerate with `gramaire emit --backend scala-peg-combinators --strategy ll-star`.\"\n+\n+  // One alt's parser expression: sequences its own symbols via `~`, then `^^` to build+tag the\n+  // resulting `Cst`. `forOp = false` (an ordinary `Plain`/`Folded.bases` alt) produces a\n+  // `Parser[Cst]`; `forOp = true` (a `Folded.operators` alt) produces a `Parser[Cst => Cst]` — a\n+  // function from the fold's running accumulator to the next one, since `rep`'s own repetitions\n+  // have no way to thread a changing accumulator INTO each step directly (unlike `BackendScalaPeg`'s\n+  // hand-rolled tail-recursive loop, which passes `acc` as a real parameter) — the fold happens\n+  // afterward, at the `rep` call site in `ruleFn`, by applying each step function in turn.\n+  private def altParser(alt: IRRewrittenAlt, forOp: Boolean): String =\n+    val n = alt.syms.length\n+    val binders = (0 until n).map(i => s\"k$i\")\n+    val kidsVec = s\"Vector(${binders.mkString(\", \")})\"\n+    val accVar = if forOp then Some(\"__acc\") else None\n+    val tagged = BackendScalaPeg.tagExpr(alt.prov, kidsVec, accVar, depth = 0)\n+    val result = if forOp then s\"(__acc: Cst) => $tagged\" else tagged\n+    val terms = alt.syms.map {\n+      case IRRewrittenSym.Terminal(label)   => s\"term(${BackendScalaPeg.strLit(label)})\"\n+      case IRRewrittenSym.NonTerminal(rule) => BackendScalaPeg.parseFnName(rule)\n+    }\n+    n match\n+      case 0 => s\"success($result)\"\n+      case 1 => s\"(${terms.head} ^^ { ${binders.head} => $result })\"\n+      case _ =>\n+        val seq = terms.mkString(\" ~ \")\n+        val pattern = binders.mkString(\" ~ \")\n+        s\"((${seq}) ^^ { case $pattern => $result })\"\n+\n+  private def orChain(exprs: Vector[String]): String = exprs.mkString(\" | \")\n+\n+  private def ruleFn(r: IRRewrittenRule): String =\n+    val fnName = BackendScalaPeg.parseFnName(r.name)\n+    r.body match\n+      case IRRuleBody.Plain(alts) =>\n+        val altExprs = alts.map(a => altParser(a, forOp = false))\n+        s\"def $fnName: Parser[Cst] = ${orChain(altExprs)}\"\n+      case IRRuleBody.Folded(bases, operators) =>\n+        val baseExprs = bases.map(a => altParser(a, forOp = false))\n+        val opExprs = operators.map(a => altParser(a, forOp = true))\n+        s\"\"\"def $fnName: Parser[Cst] = {\n+  val __base: Parser[Cst] = ${orChain(baseExprs)}\n+  val __step: Parser[Cst => Cst] = ${orChain(opExprs)}\n+  (__base ~ rep(__step)) ^^ { case base ~ steps => steps.foldLeft(base) { (acc, step) => step(acc) } }\n+}\"\"\"\n+\n+  private val preamble: String =\n+    Vector(\n+      \"final case class Token(terminal: String, text: String)\",\n+      \"\",\n+      \"enum Cst derives CanEqual:\",\n+      \"  case Branch(rule: Int, kids: Vector[Cst])\",\n+      \"  case Token(terminal: String, text: String)\",\n+      \"\",\n+      // An index into the SAME underlying `Vector`, not a `.tail`-sliced copy per step — `Vector`\n+      // indexing is O(1), so this avoids any repeated-slicing cost across a long token stream.\n+      \"final case class TokenReader(tokens: Vector[Token], index: Int = 0) extends Reader[Token]:\",\n+      \"  def first: Token = tokens(index)\",\n+      \"  def atEnd: Boolean = index >= tokens.length\",\n+      \"  def pos: Position = NoPosition\",\n+      \"  def rest: Reader[Token] = TokenReader(tokens, index + 1)\",\n+      \"\",\n+      \"def term(label: String): Parser[Cst] =\",\n+      \"  accept(label, { case t if t.terminal == label => Cst.Token(t.terminal, t.text) })\"\n+    ).mkString(\"\\n\")\n+\n+  /** Emit the `<Name>.scala` module (a single `object <Name> extends Parsers:`, `type Elem = Token`\n+    * — see `BackendScalaPeg.indented`'s doc for why the whole module is one object), or an\n+    * error-commented stub if `ir.rewritten` is absent.\n+    */\n+  def emit(ir: IR): String =\n+    val objName = BackendScalaPeg.ident(ir.grammar.name)\n+    ir.rewritten match\n+      case None =>\n+        banner(ir, \"PEG parser (scala-parser-combinators)\") +\n+          s\"\\nobject $objName:\\n\" +\n+          \"  // ERROR: no rewritten grammar on this IR — build with --strategy ll-star,\\n\" +\n+          \"  // or this grammar's own shape isn't yet supported (see IR.rewrittenGrammarOf's doc).\\n\" +\n+          \"  def recognize(toks: Vector[Nothing]): Boolean = throw new RuntimeException(\\\"unavailable\\\")\\n\"\n+      case Some(rg) =>\n+        val fns = rg.rules.map(ruleFn)\n+        val startFn = BackendScalaPeg.parseFnName(rg.start)\n+        val body = Vector(\n+          preamble,\n+          \"\",\n+          fns.mkString(\"\\n\\n\"),\n+          s\"\"\"def parse(toks: Vector[Token]): Option[Cst] =\n+  $startFn(TokenReader(toks)) match {\n+    case Success(cst, rest) if rest.atEnd => Some(cst)\n+    case _                                => None\n+  }\"\"\",\n+          \"\"\"def recognize(toks: Vector[Token]): Boolean = parse(toks).isDefined\"\"\"\n+        ).mkString(\"\\n\")\n+        Vector(\n+          banner(ir, \"PEG parser (scala-parser-combinators, strategy ll-star)\"),\n+          \"\",\n+          // Imports must precede the `object ... extends Parsers:` line, not live inside its own\n+          // body — the `extends` clause resolves `Parsers` before any import nested in the body\n+          // would take effect (verified by actually compiling this: nesting the imports inside\n+          // produced a \"Not found: Parsers\" error on the object header itself).\n+          \"import scala.util.parsing.combinator.Parsers\",\n+          \"import scala.util.parsing.input.{NoPosition, Position, Reader}\",\n+          \"\",\n+          s\"object $objName extends Parsers:\",\n+          \"  type Elem = Token\",\n+          BackendScalaPeg.indented(body)\n+        ).mkString(\"\\n\")","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegCombinators.scala","sha":"0372ba9818b1230a69b13da909e7d37f6c53b6a9","status":"added"},{"additions":9,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/site%2Fscripts%2Fcheck-scala-peg-parity.mjs","changes":17,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fscripts%2Fcheck-scala-peg-parity.mjs?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":8,"filename":"site/scripts/check-scala-peg-parity.mjs","patch":"@@ -1,9 +1,9 @@\n #!/usr/bin/env node\n-// The `scala-peg`/`scala-peg-fastparse` backends' parity gate: proves each emitted Scala PEG\n-// parser, once actually COMPILED AND RUN, accepts/rejects the same conformance-corpus vectors as\n-// `Ll.parse` does AND builds the exact same Cst — the behavioral proof a golden-text diff alone\n-// can't give (a bug in the emitted alt/fold translation could still emit text that happens to\n-// golden-diff clean).\n+// The `scala-peg`/`scala-peg-fastparse`/`scala-peg-combinators` backends' parity gate: proves\n+// each emitted Scala PEG parser, once actually COMPILED AND RUN, accepts/rejects the same\n+// conformance-corpus vectors as `Ll.parse` does AND builds the exact same Cst — the behavioral\n+// proof a golden-text diff alone can't give (a bug in the emitted alt/fold translation could\n+// still emit text that happens to golden-diff clean).\n //\n // Architecture mirrors check-atn-ts-parity.mjs, with one structural difference: no standalone\n // Scala tooling exists in this environment (no `scala-cli`/`scala`/`scalac`/`coursier`), so the\n@@ -17,8 +17,9 @@\n //      EXPECTED answers (Ll.parse's own accept/reject + rendered Cst) to stdout.\n //   2. `codegenScratch/runMain gramaire.scratch.Main <vectorsPath>` — compiles the just-written\n //      `Generated.scala` (a real sbt compile of real, independently-executed code — for\n-//      `scala-peg-fastparse` this exercises the real `fastparse` library too, via\n-//      `codegen-scratch`'s own dependency) and prints the ACTUAL answers to stdout.\n+//      `scala-peg-fastparse`/`scala-peg-combinators` this exercises the real `fastparse`/\n+//      `scala-parser-combinators` library too, via `codegen-scratch`'s own dependencies) and\n+//      prints the ACTUAL answers to stdout.\n // Both marker-delimited blocks land in the same combined stdout stream; this script pulls them\n // out and diffs by vector name.\n //\n@@ -32,7 +33,7 @@ const siteDir = path.dirname(path.dirname(fileURLToPath(import.meta.url)));\n const repoRoot = path.join(siteDir, \"..\");\n \n const GRAMMARS = [\"calc\", \"json\", \"ecma404\", \"calc-prec\"];\n-const BACKENDS = [\"scala-peg\", \"scala-peg-fastparse\"];\n+const BACKENDS = [\"scala-peg\", \"scala-peg-fastparse\", \"scala-peg-combinators\"];\n \n const EXPECTED_START = \"===SCALA-PEG-EXPECTED-START===\";\n const EXPECTED_END = \"===SCALA-PEG-EXPECTED-END===\";","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/site%2Fscripts%2Fcheck-scala-peg-parity.mjs","sha":"861c9fc53a89eca88f3798c27fec59b04cd61d1e","status":"modified"},{"additions":42,"blob_url":"https://github.com/wstein/gramaire/blob/8d8df87fddd46a172f597431797f8a88e8c7f903/test%2Fgolden%2FCalcCombinators.scala","changes":42,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/test%2Fgolden%2FCalcCombinators.scala?ref=8d8df87fddd46a172f597431797f8a88e8c7f903","deletions":0,"filename":"test/golden/CalcCombinators.scala","patch":"@@ -0,0 +1,42 @@\n+// GENERATED by Gramaire from the `Calc` grammar — PEG parser (scala-parser-combinators, strategy ll-star).\n+// Do not edit; regenerate with `gramaire emit --backend scala-peg-combinators --strategy ll-star`.\n+\n+import scala.util.parsing.combinator.Parsers\n+import scala.util.parsing.input.{NoPosition, Position, Reader}\n+\n+object Calc extends Parsers:\n+  type Elem = Token\n+  final case class Token(terminal: String, text: String)\n+\n+  enum Cst derives CanEqual:\n+    case Branch(rule: Int, kids: Vector[Cst])\n+    case Token(terminal: String, text: String)\n+\n+  final case class TokenReader(tokens: Vector[Token], index: Int = 0) extends Reader[Token]:\n+    def first: Token = tokens(index)\n+    def atEnd: Boolean = index >= tokens.length\n+    def pos: Position = NoPosition\n+    def rest: Reader[Token] = TokenReader(tokens, index + 1)\n+\n+  def term(label: String): Parser[Cst] =\n+    accept(label, { case t if t.terminal == label => Cst.Token(t.terminal, t.text) })\n+\n+  def parse_Expr: Parser[Cst] = {\n+    val __base: Parser[Cst] = (parse_Term ^^ { k0 => Cst.Branch(2, Vector(k0)) })\n+    val __step: Parser[Cst => Cst] = ((term(\"+\") ~ parse_Term) ^^ { case k0 ~ k1 => (__acc: Cst) => Cst.Branch(0, (__acc +: Vector(k0, k1))) }) | ((term(\"-\") ~ parse_Term) ^^ { case k0 ~ k1 => (__acc: Cst) => Cst.Branch(1, (__acc +: Vector(k0, k1))) })\n+    (__base ~ rep(__step)) ^^ { case base ~ steps => steps.foldLeft(base) { (acc, step) => step(acc) } }\n+  }\n+\n+  def parse_Term: Parser[Cst] = {\n+    val __base: Parser[Cst] = (parse_Factor ^^ { k0 => Cst.Branch(5, Vector(k0)) })\n+    val __step: Parser[Cst => Cst] = ((term(\"*\") ~ parse_Factor) ^^ { case k0 ~ k1 => (__acc: Cst) => Cst.Branch(3, (__acc +: Vector(k0, k1))) }) | ((term(\"/\") ~ parse_Factor) ^^ { case k0 ~ k1 => (__acc: Cst) => Cst.Branch(4, (__acc +: Vector(k0, k1))) })\n+    (__base ~ rep(__step)) ^^ { case base ~ steps => steps.foldLeft(base) { (acc, step) => step(acc) } }\n+  }\n+\n+  def parse_Factor: Parser[Cst] = ((term(\"(\") ~ parse_Expr ~ term(\")\")) ^^ { case k0 ~ k1 ~ k2 => Cst.Branch(6, Vector(k0, k1, k2)) }) | (term(\"NUMBER\") ^^ { k0 => Cst.Branch(7, Vector(k0)) })\n+  def parse(toks: Vector[Token]): Option[Cst] =\n+    parse_Expr(TokenReader(toks)) match {\n+      case Success(cst, rest) if rest.atEnd => Some(cst)\n+      case _                                => None\n+    }\n+  def recognize(toks: Vector[Token]): Boolean = parse(toks).isDefined\n\\ No newline at end of file","raw_url":"https://github.com/wstein/gramaire/raw/8d8df87fddd46a172f597431797f8a88e8c7f903/test%2Fgolden%2FCalcCombinators.scala","sha":"5408c739f5178409d313b2edad0766f0f6b1276f","status":"added"}]}