{"files":[{"additions":9,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/.github%2Fworkflows%2Fci.yml","changes":16,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.github%2Fworkflows%2Fci.yml?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":7,"filename":".github/workflows/ci.yml","patch":"@@ -209,13 +209,15 @@ jobs:\n         # Ll.recognize on every vector.\n         run: npm run check:atn-ts-parity\n \n-      - name: scala-peg backend parity gate (PEG-engine plan Phase 3)\n-        # Runs the conformance corpus through `gramaire.cli.ScalaPegParityMain` (sbt runMain),\n-        # which writes each grammar's `scala-peg` output into the throwaway `codegen-scratch`\n-        # sbt subproject and prints Ll.parse's own accept/reject + rendered Cst; a second sbt\n-        # command in the same invocation compiles and runs that generated Scala for real,\n-        # proving the emitted PEG parser — not just its golden text — actually reproduces\n-        # Ll.parse's exact Cst, the same behavioral proof the atn-ts gate above gives its backend.\n+      - name: scala-peg / scala-peg-fastparse backend parity gate (PEG-engine plan Phases 3-4)\n+        # Runs the conformance corpus through `gramaire.cli.ScalaPegParityMain` (sbt runMain) for\n+        # BOTH IR.rewritten-reading backends — the dependency-free `scala-peg` and the opt-in\n+        # `scala-peg-fastparse` — which writes each grammar's generated output into the throwaway\n+        # `codegen-scratch` sbt subproject and prints Ll.parse's own accept/reject + rendered Cst;\n+        # a second sbt command in the same invocation compiles (against the real `fastparse`\n+        # library for that backend) and runs the generated Scala for real, proving each emitted\n+        # PEG parser — not just its golden text — actually reproduces Ll.parse's exact Cst, the\n+        # same behavioral proof the atn-ts gate above gives its backend.\n         run: npm run check:scala-peg-parity\n \n       - name: Build","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/.github%2Fworkflows%2Fci.yml","sha":"155cafd609ed91de5efd536947de7ee1e68190d8","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/build.sbt","changes":4,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/build.sbt?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":0,"filename":"build.sbt","patch":"@@ -92,6 +92,10 @@ lazy val codegenScratch = project\n   .settings(\n     name := \"gramaire-codegen-scratch\",\n     publish / skip := true,\n+    // Only the opt-in `scala-peg-fastparse` backend's generated code uses this — the\n+    // dependency-free `scala-peg` backend's own generated code doesn't reference it at all, so\n+    // it's simply unused (harmlessly) whenever `codegen-scratch` compiles that backend's output.\n+    libraryDependencies += \"com.lihaoyi\" %% \"fastparse\" % \"3.1.1\",\n   )\n \n lazy val root = project","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/build.sbt","sha":"733995743ffda40472497667f2d2ed2ec28f57e5","status":"modified"},{"additions":33,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala","changes":55,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":22,"filename":"cli/jvm/src/main/scala/gramaire/cli/ScalaPegParityMain.scala","patch":"@@ -43,32 +43,42 @@ object ScalaPegParityMain:\n     case \"calc-prec\" => loadEntry(\"examples/calc-prec.gram.md\", Conformance.calcPrecDescriptor)\n     case _           => None\n \n+  private def emitFor(backendKey: String): Option[IR => String] = backendKey match\n+    case \"scala-peg\"           => Some(BackendScalaPeg.emit)\n+    case \"scala-peg-fastparse\" => Some(BackendScalaPegFastparse.emit)\n+    case _                     => None\n+\n   // Writes `Generated.scala` under `scratchSrcDir/gramaire/scratch/` — always this one fixed path,\n   // named \"Generated\" regardless of the grammar's own name (see the module header) — via\n-  // `IR.buildIRP(prec, ..., \"Generated\", g)`, the same real `BackendScalaPeg.emit` a `gramaire\n-  // emit --backend scala-peg` invocation would use, just naming the IR differently for this\n+  // `IR.buildIRP(prec, ..., \"Generated\", g)`, the same real `<backend>.emit` a `gramaire emit\n+  // --backend <backendKey>` invocation would use, just naming the IR differently for this\n   // harness's own fixed-name convention. Returns `Left` if the grammar's own LR(1) table build\n-  // fails (never expected for the corpus) or its `rewritten` section isn't available.\n+  // fails (never expected for the corpus), its `rewritten` section isn't available, or\n+  // `backendKey` names neither of the two `IR.rewritten`-reading backends.\n   private def emitGenerated(\n       g: Grammar,\n       prec: Precedence,\n-      scratchSrcDir: String\n+      scratchSrcDir: String,\n+      backendKey: String\n   ): Either[String, Unit] =\n-    IR.buildIRP(prec, Method.Canonical, \"Generated\", g) match\n-      case Left(conflicts) => Left(s\"LR(1) conflicts building the table: $conflicts\")\n-      case Right(ir0) =>\n-        val ir = IR.withStrategy(\"ll-star\", g, ir0, prec)\n-        if ir.rewritten.isEmpty then Left(\"IR.rewrittenGrammarOf declined this grammar's shape\")\n-        else\n-          val dir = Path.of(s\"$scratchSrcDir/gramaire/scratch\")\n-          Files.createDirectories(dir)\n-          // `BackendScalaPeg.emit` itself never writes a `package` declaration (a real CLI user\n-          // drops the file wherever they like and packages it themselves) — prepended here only\n-          // because `codegen-scratch/Main.scala` (the fixed driver) expects `Generated` in its\n-          // own `gramaire.scratch` package, matching the directory this writes into.\n-          val text = s\"package gramaire.scratch\\n\\n${BackendScalaPeg.emit(ir)}\"\n-          Files.writeString(dir.resolve(\"Generated.scala\"), text)\n-          Right(())\n+    emitFor(backendKey) match\n+      case None => Left(s\"unknown backend '$backendKey' (expected scala-peg | scala-peg-fastparse)\")\n+      case Some(emit) =>\n+        IR.buildIRP(prec, Method.Canonical, \"Generated\", g) match\n+          case Left(conflicts) => Left(s\"LR(1) conflicts building the table: $conflicts\")\n+          case Right(ir0) =>\n+            val ir = IR.withStrategy(\"ll-star\", g, ir0, prec)\n+            if ir.rewritten.isEmpty then Left(\"IR.rewrittenGrammarOf declined this grammar's shape\")\n+            else\n+              val dir = Path.of(s\"$scratchSrcDir/gramaire/scratch\")\n+              Files.createDirectories(dir)\n+              // Neither backend's own `emit` writes a `package` declaration (a real CLI user\n+              // drops the file wherever they like and packages it themselves) — prepended here\n+              // only because `codegen-scratch/Main.scala` (the fixed driver) expects `Generated`\n+              // in its own `gramaire.scratch` package, matching the directory this writes into.\n+              val text = s\"package gramaire.scratch\\n\\n${emit(ir)}\"\n+              Files.writeString(dir.resolve(\"Generated.scala\"), text)\n+              Right(())\n \n   private def b64(s: String): String = Base64.getEncoder.encodeToString(s.getBytes(\"UTF-8\"))\n \n@@ -103,13 +113,13 @@ object ScalaPegParityMain:\n \n   def main(args: Array[String]): Unit =\n     args match\n-      case Array(scratchSrcDir, vectorsPath, grammarKey) =>\n+      case Array(scratchSrcDir, vectorsPath, grammarKey, backendKey) =>\n         entryFor(grammarKey) match\n           case None =>\n             System.err.println(s\"$grammarKey: skipped — grammar file missing or unparseable\")\n             sys.exit(1)\n           case Some(Entry(d, prec)) =>\n-            emitGenerated(d.grammar, prec, scratchSrcDir) match\n+            emitGenerated(d.grammar, prec, scratchSrcDir, backendKey) match\n               case Left(reason) =>\n                 System.err.println(s\"$grammarKey: skipped — $reason\")\n                 sys.exit(1)\n@@ -122,6 +132,7 @@ object ScalaPegParityMain:\n                 println(endMarker)\n       case _ =>\n         System.err.println(\n-          \"usage: ScalaPegParityMain <scratchSrcDir> <vectorsPath> <calc|json|ecma404|calc-prec>\"\n+          \"usage: ScalaPegParityMain <scratchSrcDir> <vectorsPath> \" +\n+            \"<calc|json|ecma404|calc-prec> <scala-peg|scala-peg-fastparse>\"\n         )\n         sys.exit(1)","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FScalaPegParityMain.scala","sha":"00527092be1e52b2957b8817c0084aefe02aceda","status":"modified"},{"additions":11,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","changes":11,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":0,"filename":"cli/jvm/src/test/scala/gramaire/cli/MainSuite.scala","patch":"@@ -7,6 +7,7 @@ import gramaire.{\n   BackendJs,\n   BackendRegistry,\n   BackendScalaPeg,\n+  BackendScalaPegFastparse,\n   BackendTs,\n   Grammar,\n   IR,\n@@ -116,6 +117,9 @@ class MainSuite extends munit.FunSuite:\n     // scala-peg reads only IR.rewritten (the ll-star-only CST-fold-back section).\n     assert(!Main.backendSupportsStrategy(BackendScalaPeg.backend, \"lr\"))\n     assert(Main.backendSupportsStrategy(BackendScalaPeg.backend, \"ll-star\"))\n+    // scala-peg-fastparse reads the same IR.rewritten section, so the same gate applies.\n+    assert(!Main.backendSupportsStrategy(BackendScalaPegFastparse.backend, \"lr\"))\n+    assert(Main.backendSupportsStrategy(BackendScalaPegFastparse.backend, \"ll-star\"))\n   }\n \n   test(\"BackendRegistry: atn-ts is registered and reachable by name\") {\n@@ -126,6 +130,13 @@ class MainSuite extends munit.FunSuite:\n     assertEquals(BackendRegistry.findBackend(\"scala-peg\"), Some(BackendScalaPeg.backend))\n   }\n \n+  test(\"BackendRegistry: scala-peg-fastparse is registered and reachable by name\") {\n+    assertEquals(\n+      BackendRegistry.findBackend(\"scala-peg-fastparse\"),\n+      Some(BackendScalaPegFastparse.backend)\n+    )\n+  }\n+\n   test(\n     \"strategyIgnoresPredicates: an lr build of a `{%? %}` grammar is flagged, ll-star is not\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","sha":"ff1da987e8fd2230f40a25d91d35b7fa50b0fef0","status":"modified"},{"additions":22,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","changes":22,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":0,"filename":"core/.jvm/src/test/scala/gramaire/BackendGoldenSuite.scala","patch":"@@ -111,6 +111,28 @@ class BackendGoldenSuite extends munit.FunSuite:\n             assertEquals(BackendScalaPeg.emit(irLl), readFile(\"test/golden/Calc.scala\"))\n   }\n \n+  // A golden-text pin, same convention as every other backend above — but see\n+  // site/scripts/check-scala-peg-parity.mjs for the test that actually matters for this backend:\n+  // a golden diff alone can't tell a correctly-translated IRProv/IRRuleBody from one that merely\n+  // still emits stable-looking text, only compiling (against the real `fastparse` library, via\n+  // `codegen-scratch`) and running the emitted Scala can.\n+  test(\n+    \"scala-peg-fastparse: examples/calc.gram.md under --strategy ll-star matches the committed golden\"\n+  ) {\n+    val md = readFile(\"examples/calc.gram.md\")\n+    Lr.parse(md) match\n+      case Left(e) => fail(s\"could not parse examples/calc.gram.md: $e\")\n+      case Right(g) =>\n+        IR.buildIR(Method.Canonical, \"Calc\", g) match\n+          case Left(_) => fail(\"could not build IR for calc\")\n+          case Right(ir) =>\n+            val irLl = IR.withStrategy(\"ll-star\", g, ir, Lr.precedenceOf(md))\n+            assertEquals(\n+              BackendScalaPegFastparse.emit(irLl),\n+              readFile(\"test/golden/CalcFastparse.scala\")\n+            )\n+  }\n+\n   test(\n     \"js: calc-js bakes its inline actions into one evaluate(cst), matching the committed golden\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","sha":"069f1b3155cca33792bd54aec90ad89516357e76","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","changes":3,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":1,"filename":"core/src/main/scala/gramaire/BackendRegistry.scala","patch":"@@ -13,7 +13,8 @@ object BackendRegistry:\n       BackendAntlr.backend,\n       BackendJs.backend,\n       BackendAtnTs.backend,\n-      BackendScalaPeg.backend\n+      BackendScalaPeg.backend,\n+      BackendScalaPegFastparse.backend\n     )\n \n   /** Find a backend by its `--backend` name. */","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","sha":"258eb277073cf5d694ab52db49f21b295fefe9a7","status":"modified"},{"additions":217,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegFastparse.scala","changes":217,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegFastparse.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":0,"filename":"core/src/main/scala/gramaire/BackendScalaPegFastparse.scala","patch":"@@ -0,0 +1,217 @@\n+package gramaire\n+\n+// A `Cst`-capable, `fastparse`-targeting codegen backend — the opt-in, production-grade-\n+// performance counterpart to the dependency-free `scala-peg` backend (`BackendScalaPeg`),\n+// reading the SAME `IR.rewritten` section (see `IR.rewrittenGrammarOf`'s own doc).\n+//\n+// **Why this isn't just `BackendScalaPeg` with `fastparse` combinators swapped in for the\n+// hand-rolled `Option`-based backtracking**: `fastparse`'s `ParserInput` is hardcoded to\n+// `Char`/`String` — there is no generic or token-stream `ParserInput` (verified against\n+// `fastparse`'s own source: `IndexedParserInput`/`IteratorParserInput` both fix the element type\n+// to `Char`, and there is no documented extension point for a custom element type). It therefore\n+// cannot consume the same already-lexed `Vector[Token]` `Ll.parse`/`BackendScalaPeg`'s generated\n+// code reads directly.\n+//\n+// The alternative — emitting a SECOND, `fastparse`-native lexer (translating `IR.lexer`'s token\n+// classes/regexes into `fastparse` char-combinators) so this backend could parse raw source text\n+// in one pass — was deliberately rejected: it would need its own conformance proof that the\n+// emitted lexer reproduces Gramaire's real `Scanner` token stream byte-for-byte, the same bar\n+// `docs/multi-backend-implementation-plan.md`'s D31 already holds any alternate lexer to, and\n+// would make this backend a fundamentally different (and much larger) undertaking than \"reuse\n+// `IR.rewritten`, target a different combinator style\" — the scope the PEG-engine plan commits to.\n+//\n+// Instead, this backend keeps the SAME \"already-lexed tokens in\" contract as `BackendScalaPeg`:\n+// each already-lexed `Token`'s TERMINAL is mapped to one synthetic `Char` (Unicode Private Use\n+// Area, `` + a per-distinct-terminal index, built fresh per `parse` call from the actual\n+// tokens), producing a synthetic `String` of the same length as the token vector for `fastparse`'s\n+// ordinary `CharPred` combinators to match against, one terminal-check at a time; `fastparse`'s own\n+// `Index` primitive recovers which real `Token` a matched synthetic char corresponds to, so a\n+// built `Cst.Token` always carries the REAL terminal/text, never the synthetic stand-in. This is a\n+// real, idiomatic `fastparse` parser — genuinely compiled and run by the parity gate — just driven\n+// by a synthetic input `fastparse` can actually consume instead of the token vector directly.\n+object BackendScalaPegFastparse:\n+  val backend: Backend = Backend(\n+    name = \"scala-peg-fastparse\",\n+    capabilities = Vector(Capability.Cst),\n+    strategies = Vector(\"ll-star\"),\n+    emit = ir => Vector(Output(s\"${ir.grammar.name}Fastparse.scala\", emit(ir)))\n+  )\n+\n+  private def banner(ir: IR, what: String): String =\n+    s\"// GENERATED by Gramaire from the `${ir.grammar.name}` grammar — $what.\\n\" +\n+      \"// Do not edit; regenerate with `gramaire emit --backend scala-peg-fastparse --strategy ll-star`.\"\n+\n+  private def strLit(s: String): String =\n+    \"\\\"\" + s.flatMap {\n+      case '\"'  => \"\\\\\\\"\"\n+      case '\\\\' => \"\\\\\\\\\"\n+      case '\\n' => \"\\\\n\"\n+      case '\\t' => \"\\\\t\"\n+      case c    => c.toString\n+    } + \"\\\"\"\n+\n+  private def ident(name: String): String =\n+    val safe = name.map(c => if c.isLetterOrDigit || c == '_' then c else '_')\n+    if safe.headOption.exists(_.isDigit) then s\"_$safe\" else safe\n+\n+  private def parseFnName(rule: String): String = s\"parse_${ident(rule)}\"\n+\n+  private def originExpr(origin: IRAltOrigin, kidsExpr: String): String = origin match\n+    case IRAltOrigin.Unwrap           => s\"$kidsExpr.head\"\n+    case IRAltOrigin.Original(prodId) => s\"Cst.Branch($prodId, $kidsExpr)\"\n+\n+  // Identical to `BackendScalaPeg`'s own `tagExpr` — `IRProv` resolution doesn't depend on which\n+  // combinator library drives the match, only on the (already fully-parsed) `kids`/`acc` values.\n+  private def tagExpr(prov: IRProv, kidsVar: String, accVar: Option[String], depth: Int): String =\n+    prov match\n+      case IRProv.Leaf(origin) => originExpr(origin, kidsVar)\n+      case IRProv.OpLeaf(origin) =>\n+        val acc = accVar.getOrElse(\n+          throw new IllegalStateException(\n+            \"BackendScalaPegFastparse: OpLeaf outside an operator context\"\n+          )\n+        )\n+        originExpr(origin, s\"($acc +: $kidsVar)\")\n+      case IRProv.Wrap(origin, innerSpan, inner) =>\n+        val innerKids = s\"innerKids$depth\"\n+        val ownKids = s\"ownKids$depth\"\n+        val innerExpr = tagExpr(inner, innerKids, accVar, depth + 1)\n+        s\"{ val ($innerKids, $ownKids) = $kidsVar.splitAt($innerSpan); \" +\n+          s\"${originExpr(origin, s\"($innerExpr +: $ownKids)\")} }\"\n+\n+  // One symbol's own fastparse combinator (a synthetic-char terminal match or a recursive rule\n+  // call) — every symbol parser has type `P[Cst]`, so a sequence's own combined type is a flat\n+  // tuple of exactly that many `Cst`s (fastparse auto-flattens `~`-chained tuples).\n+  private def symParser(sym: IRRewrittenSym): String = sym match\n+    case IRRewrittenSym.Terminal(label)   => s\"term(${strLit(label)})\"\n+    case IRRewrittenSym.NonTerminal(rule) => parseFnName(rule)\n+\n+  // A sequence of `n` symbols chained with `~`, its captured kids pattern-matched into `k0..k(n-1)`\n+  // — fastparse gives a bare value (not a 1-tuple) for a single combinator and no tuple at all for\n+  // zero, so those arities are special-cased; `n >= 2` is an ordinary flat tuple.\n+  private def seqParser(syms: Vector[IRRewrittenSym]): String =\n+    syms.map(symParser).mkString(\" ~ \")\n+\n+  private def kidsPattern(n: Int): String = n match\n+    case 0 => \"_\"\n+    case 1 => \"k0\"\n+    case _ => s\"(${(0 until n).map(i => s\"k$i\").mkString(\", \")})\"\n+\n+  private def kidsVec(n: Int): String = s\"Vector(${(0 until n).map(i => s\"k$i\").mkString(\", \")})\"\n+\n+  // One `Plain`/base alt's own parser: match its sequence, tag the result — a self-contained\n+  // `P[Cst]` needing no accumulator.\n+  private def altParser(alt: IRRewrittenAlt): String =\n+    val n = alt.syms.length\n+    val tagged = tagExpr(alt.prov, kidsVec(n), accVar = None, depth = 0)\n+    if n == 0 then s\"Pass(($tagged))\"\n+    else s\"(${seqParser(alt.syms)}).map { case ${kidsPattern(n)} => $tagged }\"\n+\n+  // One `Folded` operator alt's own RAW parser: match its sequence, but yield only its own kids\n+  // (as a `Vector[Cst]`) — tagging is deferred to `ruleFn`'s post-`.rep` fold, since `IRProv`\n+  // resolution for an operator needs the fold's running accumulator, not known until fold time.\n+  private def opRawParser(alt: IRRewrittenAlt): String =\n+    val n = alt.syms.length\n+    if n == 0 then \"Pass(Vector.empty[Cst])\"\n+    else s\"(${seqParser(alt.syms)}).map { case ${kidsPattern(n)} => ${kidsVec(n)} }\"\n+\n+  private def orChain(exprs: Vector[String]): String =\n+    exprs.reduceLeft((acc, e) => s\"($acc | $e)\")\n+\n+  private def ruleFn(r: IRRewrittenRule): String =\n+    val fnName = parseFnName(r.name)\n+    r.body match\n+      case IRRuleBody.Plain(alts) =>\n+        s\"def $fnName[$$: P]: P[Cst] = P(${orChain(alts.map(altParser))})\"\n+      case IRRuleBody.Folded(bases, operators) =>\n+        val baseExpr = orChain(bases.map(altParser))\n+        val opTags = operators.zipWithIndex.map { case (op, i) =>\n+          s\"          case $i => ${tagExpr(op.prov, \"kids\", Some(\"acc\"), depth = 0)}\"\n+        }\n+        s\"\"\"def $fnName[$$: P]: P[Cst] = P(\n+  ($baseExpr) ~ (${orChain(operators.zipWithIndex.map { case (op, i) =>\n+            s\"(${opRawParser(op)}).map(kids => ($i, kids))\"\n+          })}).rep\n+).map { case (base, ops) =>\n+  def tagOp(opIdx: Int, acc: Cst, kids: Vector[Cst]): Cst = opIdx match {\n+${opTags.mkString(\"\\n\")}\n+    case other => throw new IllegalStateException(s\"unknown operator index $$other\")\n+  }\n+  ops.foldLeft(base) { case (acc, (opIdx, kids)) => tagOp(opIdx, acc, kids) }\n+}\"\"\"\n+\n+  private def preamble: String =\n+    Vector(\n+      \"import fastparse._, NoWhitespace._\",\n+      \"\",\n+      \"final case class Token(terminal: String, text: String)\",\n+      \"\",\n+      \"enum Cst derives CanEqual:\",\n+      \"  case Branch(rule: Int, kids: Vector[Cst])\",\n+      \"  case Token(terminal: String, text: String)\"\n+    ).mkString(\"\\n\")\n+\n+  // Every generated function starts at column 0; `indented` (shared shift-by-one-level logic,\n+  // mirroring `BackendScalaPeg`'s own) nests the whole module inside `object <Name>:`.\n+  private def indented(body: String): String =\n+    body.linesIterator.map(l => if l.isEmpty then l else s\"  $l\").mkString(\"\\n\")\n+\n+  /** Emit the `<Name>Fastparse.scala` module, or an error-commented stub if `ir.rewritten` is\n+    * absent — same conditions/rationale as `BackendScalaPeg.emit`'s own stub.\n+    */\n+  def emit(ir: IR): String =\n+    val objName = ident(ir.grammar.name)\n+    ir.rewritten match\n+      case None =>\n+        banner(ir, \"fastparse PEG parser\") +\n+          s\"\\nobject $objName:\\n\" +\n+          \"  // ERROR: no rewritten grammar on this IR — build with --strategy ll-star,\\n\" +\n+          \"  // or this grammar's own shape isn't yet supported (see IR.rewrittenGrammarOf's doc).\\n\" +\n+          \"  def recognize(toks: Vector[Nothing]): Boolean = throw new RuntimeException(\\\"unavailable\\\")\\n\"\n+      case Some(rg) =>\n+        val fns = rg.rules.map(ruleFn)\n+        val startFn = parseFnName(rg.start)\n+        // A synthetic per-terminal Char alphabet (Private Use Area, ``+, escaped rather\n+        // than embedded raw in the generated SOURCE TEXT) built fresh from `tokens` —\n+        // `fastparse`'s own `CharPred`/`Index` combinators then drive matching over `input` (a\n+        // same-length synthetic String), never over `tokens` directly. See the module header for\n+        // why: `fastparse.ParserInput` cannot consume a token stream itself. `￿` (a reserved\n+        // noncharacter, never assigned by `termChar`) is the \"never matches\" sentinel for a\n+        // terminal absent from this call's own token vector.\n+        val driverMembers = Vector(\n+          \"\"\"private val termChar: Map[String, Char] =\n+  tokens.map(_.terminal).distinct.zipWithIndex.map { case (t, i) => t -> (0xE000 + i).toChar }.toMap\n+private val input: String = tokens.map(t => termChar.getOrElse(t.terminal, '\\uFFFF')).mkString\n+\n+def term[$: P](label: String): P[Cst] =\n+  val ch = termChar.getOrElse(label, '\\uFFFF')\n+  // fastparse elides a Unit-typed result from a `~` chain, so `Index ~ CharPred(...)` is\n+  // `P[Int]`, not `P[(Int, Unit)]` — verified by actually compiling this (a tuple pattern here\n+  // fails with a confusing \"Found: Any, Required: Int\" instead of a clear arity mismatch).\n+  P(Index ~ CharPred(_ == ch)).map { idx =>\n+    Cst.Token(tokens(idx).terminal, tokens(idx).text)\n+  }\"\"\",\n+          fns.mkString(\"\\n\\n\"),\n+          s\"\"\"def parseAll: Option[Cst] =\n+  fastparse.parse(input, $startFn(using _)) match {\n+    case Parsed.Success(cst, idx) if idx == input.length => Some(cst)\n+    case _ => None\n+  }\n+def recognizeAll: Boolean = parseAll.isDefined\"\"\"\n+        ).mkString(\"\\n\\n\")\n+        val driverClass =\n+          s\"private class Driver(tokens: Vector[Token]):\\n${indented(driverMembers)}\"\n+        val body = Vector(\n+          preamble,\n+          \"\",\n+          driverClass,\n+          \"\",\n+          \"def parse(tokens: Vector[Token]): Option[Cst] = Driver(tokens).parseAll\",\n+          \"def recognize(tokens: Vector[Token]): Boolean = Driver(tokens).recognizeAll\"\n+        ).mkString(\"\\n\")\n+        Vector(\n+          banner(ir, \"fastparse PEG parser (strategy ll-star)\"),\n+          \"\",\n+          s\"object $objName:\",\n+          indented(body)\n+        ).mkString(\"\\n\")","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPegFastparse.scala","sha":"90e5b640410bffe70f9ddc78e8d81037257cab0f","status":"added"},{"additions":44,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/docs%2Fall-star-port-plan.md","changes":46,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fall-star-port-plan.md?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":2,"filename":"docs/all-star-port-plan.md","patch":"@@ -587,8 +587,8 @@ surface is reached by **conversion**, not syntax expansion (§4).\n   every accept vector across all four real corpora\n   (`calc`/`json`/`ECMA-404`/`calc-prec`) — all pass. This is Phase 1 of a\n   follow-on plan to add a dependency-free Scala PEG/combinator backend and an\n-  opt-in `fastparse` one from the same `IR.rewritten` input (Phase 2 below is\n-  done; Phases 3-4 are not yet built).\n+  opt-in `fastparse` one from the same `IR.rewritten` input (Phases 2-4 below\n+  are all done).\n - ✅ **Phase 2 of that follow-on plan: `scala-peg`, a dependency-free codegen\n   backend reading `IR.rewritten`.** `BackendScalaPeg.scala` emits one\n   self-contained `object <Name>:` per grammar — a `Cst`/`Token` type plus one\n@@ -630,6 +630,48 @@ surface is reached by **conversion**, not syntax expansion (§4).\n   (Paull-substitution) path — the emitted Scala, actually compiled and run,\n   agrees with `Ll.parse` on every one. Wired into CI right after the `atn-ts`\n   gate (`npm run check:scala-peg-parity`).\n+- ✅ **Phase 4: `scala-peg-fastparse`, an opt-in backend targeting the\n+  production-grade `fastparse` combinator library, reading the SAME\n+  `IR.rewritten` section.** A genuine design fork surfaced here, not just a\n+  mechanical codegen swap: `fastparse`'s `ParserInput` is hardcoded to\n+  `Char`/`String` (confirmed against its own source —\n+  `IndexedParserInput`/`IteratorParserInput` both fix the element type to\n+  `Char`, no generic or token-stream `ParserInput` exists), so it cannot\n+  consume the same already-lexed `Vector[Token]` `Ll.parse`/`scala-peg`'s\n+  generated code reads directly. Emitting a SECOND, `fastparse`-native lexer\n+  (translating `IR.lexer`'s token classes/regexes into `fastparse`\n+  char-combinators) was deliberately rejected — it would need its own\n+  conformance proof that the emitted lexer reproduces the real `Scanner`'s\n+  token stream byte-for-byte, the same bar\n+  `docs/multi-backend-implementation-plan.md`'s D31 already holds any\n+  alternate lexer to, making this a fundamentally different (and much larger)\n+  undertaking than \"reuse `IR.rewritten`, target a different combinator\n+  style.\" Instead, `BackendScalaPegFastparse.scala` keeps the same\n+  already-lexed-tokens contract: each token's terminal maps to one synthetic\n+  `Char` (Unicode Private Use Area, built fresh per `parse` call from the\n+  actual tokens), producing a same-length synthetic `String` `fastparse`'s\n+  ordinary `CharPred`/`Index` combinators match against one terminal-check at\n+  a time; a matched position is looked up back against the real token vector\n+  so a built `Cst.Token` always carries the real terminal/text, never the\n+  synthetic stand-in. A `Folded` rule's operators are matched raw (as\n+  `(opIndex, kids)` pairs) via `fastparse`'s own `.rep`, then folded into\n+  tagged `Cst`s afterward — `IRProv` resolution needs the fold's running\n+  accumulator, not known until fold time. One real `fastparse`-specific\n+  compile bug caught by actually compiling the generated code: `fastparse`\n+  elides a `Unit`-typed result from a `~` chain, so `Index ~ CharPred(...)` is\n+  `P[Int]`, not `P[(Int, Unit)]` — a tuple-destructuring pattern there fails\n+  with a confusing \"Found: Any, Required: Int\" rather than a clear arity\n+  mismatch. Registered as `\"scala-peg-fastparse\"` (`Capability.Cst`,\n+  `ll-star`-only); reuses `codegen-scratch` (now depending on the real\n+  `fastparse` library, `\"com.lihaoyi\" %% \"fastparse\" % \"3.1.1\"`) and the same\n+  `ScalaPegParityMain`/`check-scala-peg-parity.mjs` harness, parameterized by\n+  backend name. **All 61 vectors across all four real corpora pass for this\n+  backend too** (the same set Phase 3 already proved for `scala-peg`),\n+  including the `json`/`ECMA-404` `IRProv.Wrap` path and `calc-prec`'s\n+  `LeftRec`+`PrecClimb` combination — genuinely compiled against `fastparse`\n+  and run, not golden-diffed. `MainSuite`/`BackendRegistrySuite`/\n+  `BackendGoldenSuite` gained the usual strategy-gate, registry-lookup, and\n+  golden-text tests (`test/golden/CalcFastparse.scala`).\n \n ### Phase 6 — Diagnostics, profiling, conformance ✅ done (backend + Lab surface)\n ","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/docs%2Fall-star-port-plan.md","sha":"500ff2960a965e62e7cb23bf619b7f2d13e7b6f8","status":"modified"},{"additions":52,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/site%2Fscripts%2Fcheck-scala-peg-parity.mjs","changes":99,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fscripts%2Fcheck-scala-peg-parity.mjs?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":47,"filename":"site/scripts/check-scala-peg-parity.mjs","patch":"@@ -1,22 +1,24 @@\n #!/usr/bin/env node\n-// The `scala-peg` backend's parity gate: proves the emitted Scala PEG parser, once actually\n-// COMPILED AND RUN, accepts/rejects the same conformance-corpus vectors as `Ll.parse` does AND\n-// builds the exact same Cst — the behavioral proof a golden-text diff alone can't give (a bug in\n-// the emitted alt/fold translation could still emit text that happens to golden-diff clean).\n+// The `scala-peg`/`scala-peg-fastparse` backends' parity gate: proves each emitted Scala PEG\n+// parser, once actually COMPILED AND RUN, accepts/rejects the same conformance-corpus vectors as\n+// `Ll.parse` does AND builds the exact same Cst — the behavioral proof a golden-text diff alone\n+// can't give (a bug in the emitted alt/fold translation could still emit text that happens to\n+// golden-diff clean).\n //\n // Architecture mirrors check-atn-ts-parity.mjs, with one structural difference: no standalone\n // Scala tooling exists in this environment (no `scala-cli`/`scala`/`scalac`/`coursier`), so the\n // emitted Scala can't be compiled and run as an external process the way `tsc`+`node` runs the\n-// emitted TypeScript. Instead, for each corpus grammar, this shells ONE `sbt` invocation running\n-// TWO commands in sequence within the same JVM/sbt session:\n-//   1. `cli/runMain gramaire.cli.ScalaPegParityMain <scratchSrcDir> <vectorsPath> <grammarKey>` —\n-//      writes that grammar's generated `codegen-scratch/src/main/scala/gramaire/scratch/\n-//      Generated.scala` (see ScalaPegParityMain's own header for why it's always named\n+// emitted TypeScript. Instead, for each (grammar, backend) pair, this shells ONE `sbt` invocation\n+// running TWO commands in sequence within the same JVM/sbt session:\n+//   1. `cli/runMain gramaire.cli.ScalaPegParityMain <scratchSrcDir> <vectorsPath> <grammarKey>\n+//      <backendKey>` — writes that grammar's generated `codegen-scratch/src/main/scala/gramaire/\n+//      scratch/Generated.scala` (see ScalaPegParityMain's own header for why it's always named\n //      \"Generated\", not the grammar's own name) and the vectors it needs to run, then prints the\n //      EXPECTED answers (Ll.parse's own accept/reject + rendered Cst) to stdout.\n //   2. `codegenScratch/runMain gramaire.scratch.Main <vectorsPath>` — compiles the just-written\n-//      `Generated.scala` (a real sbt compile of real, independently-executed code) and prints the\n-//      ACTUAL answers to stdout.\n+//      `Generated.scala` (a real sbt compile of real, independently-executed code — for\n+//      `scala-peg-fastparse` this exercises the real `fastparse` library too, via\n+//      `codegen-scratch`'s own dependency) and prints the ACTUAL answers to stdout.\n // Both marker-delimited blocks land in the same combined stdout stream; this script pulls them\n // out and diffs by vector name.\n //\n@@ -30,6 +32,7 @@ const siteDir = path.dirname(path.dirname(fileURLToPath(import.meta.url)));\n const repoRoot = path.join(siteDir, \"..\");\n \n const GRAMMARS = [\"calc\", \"json\", \"ecma404\", \"calc-prec\"];\n+const BACKENDS = [\"scala-peg\", \"scala-peg-fastparse\"];\n \n const EXPECTED_START = \"===SCALA-PEG-EXPECTED-START===\";\n const EXPECTED_END = \"===SCALA-PEG-EXPECTED-END===\";\n@@ -67,12 +70,12 @@ function parseResults(text) {\n   return results;\n }\n \n-function runGrammar(grammarKey, scratchSrcDir, vectorsPath) {\n+function runGrammar(grammarKey, backendKey, scratchSrcDir, vectorsPath) {\n   const out = execFileSync(\n     \"sbt\",\n     [\n       \"-batch\",\n-      `cli/runMain gramaire.cli.ScalaPegParityMain ${scratchSrcDir} ${vectorsPath} ${grammarKey}`,\n+      `cli/runMain gramaire.cli.ScalaPegParityMain ${scratchSrcDir} ${vectorsPath} ${grammarKey} ${backendKey}`,\n       `codegenScratch/runMain gramaire.scratch.Main ${vectorsPath}`,\n     ],\n     { cwd: repoRoot, encoding: \"utf8\" },\n@@ -81,7 +84,7 @@ function runGrammar(grammarKey, scratchSrcDir, vectorsPath) {\n   const actual = block(out, ACTUAL_START, ACTUAL_END);\n   if (expected === null || actual === null) {\n     throw new Error(\n-      `${grammarKey}: missing expected/actual block in sbt output:\\n${out}`,\n+      `${backendKey} / ${grammarKey}: missing expected/actual block in sbt output:\\n${out}`,\n     );\n   }\n   return { expected: parseResults(expected), actual: parseResults(actual) };\n@@ -97,43 +100,45 @@ const scratchSrcDir = path.join(\n const vectorsPath = path.join(repoRoot, \"codegen-scratch\", \"vectors.txt\");\n \n let failed = false;\n-for (const grammarKey of GRAMMARS) {\n-  console.log(\n-    `running ${grammarKey} through ScalaPegParityMain + codegen-scratch ...`,\n-  );\n-  const { expected, actual } = runGrammar(\n-    grammarKey,\n-    scratchSrcDir,\n-    vectorsPath,\n-  );\n-\n-  if (expected.size === 0) {\n-    failed = true;\n-    console.error(`  FAIL  ${grammarKey}: no expected vectors reported at all`);\n-    continue;\n-  }\n+for (const backendKey of BACKENDS) {\n+  for (const grammarKey of GRAMMARS) {\n+    console.log(\n+      `running ${backendKey} / ${grammarKey} through ScalaPegParityMain + codegen-scratch ...`,\n+    );\n+    const { expected, actual } = runGrammar(\n+      grammarKey,\n+      backendKey,\n+      scratchSrcDir,\n+      vectorsPath,\n+    );\n \n-  for (const [name, exp] of expected) {\n-    const act = actual.get(name);\n-    if (act === undefined) {\n+    if (expected.size === 0) {\n       failed = true;\n       console.error(\n-        `  FAIL  ${grammarKey} / ${name}: no actual result reported`,\n+        `  FAIL  ${backendKey} / ${grammarKey}: no expected vectors reported at all`,\n       );\n-    } else if (exp.accepted !== act.accepted) {\n-      failed = true;\n-      console.error(\n-        `  FAIL  ${grammarKey} / ${name}: accepted=${act.accepted}, expected accepted=${exp.accepted}`,\n-      );\n-    } else if (exp.accepted && exp.cst !== act.cst) {\n-      failed = true;\n-      console.error(\n-        `  FAIL  ${grammarKey} / ${name}: Cst diverged from Ll.parse`,\n-      );\n-      console.error(`    expected:\\n${exp.cst}`);\n-      console.error(`    actual:\\n${act.cst}`);\n-    } else {\n-      console.log(`  ok    ${grammarKey} / ${name}: accepted=${act.accepted}`);\n+      continue;\n+    }\n+\n+    for (const [name, exp] of expected) {\n+      const act = actual.get(name);\n+      const label = `${backendKey} / ${grammarKey} / ${name}`;\n+      if (act === undefined) {\n+        failed = true;\n+        console.error(`  FAIL  ${label}: no actual result reported`);\n+      } else if (exp.accepted !== act.accepted) {\n+        failed = true;\n+        console.error(\n+          `  FAIL  ${label}: accepted=${act.accepted}, expected accepted=${exp.accepted}`,\n+        );\n+      } else if (exp.accepted && exp.cst !== act.cst) {\n+        failed = true;\n+        console.error(`  FAIL  ${label}: Cst diverged from Ll.parse`);\n+        console.error(`    expected:\\n${exp.cst}`);\n+        console.error(`    actual:\\n${act.cst}`);\n+      } else {\n+        console.log(`  ok    ${label}: accepted=${act.accepted}`);\n+      }\n     }\n   }\n }","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/site%2Fscripts%2Fcheck-scala-peg-parity.mjs","sha":"f252606b26c2dba8bce9cf348fdb1bfdcc5a1fb7","status":"modified"},{"additions":59,"blob_url":"https://github.com/wstein/gramaire/blob/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/test%2Fgolden%2FCalcFastparse.scala","changes":59,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/test%2Fgolden%2FCalcFastparse.scala?ref=3329a5b5a67142c9c92df5d8e0dd779942c8f1d1","deletions":0,"filename":"test/golden/CalcFastparse.scala","patch":"@@ -0,0 +1,59 @@\n+// GENERATED by Gramaire from the `Calc` grammar — fastparse PEG parser (strategy ll-star).\n+// Do not edit; regenerate with `gramaire emit --backend scala-peg-fastparse --strategy ll-star`.\n+\n+object Calc:\n+  import fastparse._, NoWhitespace._\n+\n+  final case class Token(terminal: String, text: String)\n+\n+  enum Cst derives CanEqual:\n+    case Branch(rule: Int, kids: Vector[Cst])\n+    case Token(terminal: String, text: String)\n+\n+  private class Driver(tokens: Vector[Token]):\n+    private val termChar: Map[String, Char] =\n+      tokens.map(_.terminal).distinct.zipWithIndex.map { case (t, i) => t -> (0xE000 + i).toChar }.toMap\n+    private val input: String = tokens.map(t => termChar.getOrElse(t.terminal, '\\uFFFF')).mkString\n+\n+    def term[$: P](label: String): P[Cst] =\n+      val ch = termChar.getOrElse(label, '\\uFFFF')\n+      // fastparse elides a Unit-typed result from a `~` chain, so `Index ~ CharPred(...)` is\n+      // `P[Int]`, not `P[(Int, Unit)]` — verified by actually compiling this (a tuple pattern here\n+      // fails with a confusing \"Found: Any, Required: Int\" instead of a clear arity mismatch).\n+      P(Index ~ CharPred(_ == ch)).map { idx =>\n+        Cst.Token(tokens(idx).terminal, tokens(idx).text)\n+      }\n+\n+    def parse_Expr[$: P]: P[Cst] = P(\n+      ((parse_Term).map { case k0 => Cst.Branch(2, Vector(k0)) }) ~ ((((term(\"+\") ~ parse_Term).map { case (k0, k1) => Vector(k0, k1) }).map(kids => (0, kids)) | ((term(\"-\") ~ parse_Term).map { case (k0, k1) => Vector(k0, k1) }).map(kids => (1, kids)))).rep\n+    ).map { case (base, ops) =>\n+      def tagOp(opIdx: Int, acc: Cst, kids: Vector[Cst]): Cst = opIdx match {\n+              case 0 => Cst.Branch(0, (acc +: kids))\n+              case 1 => Cst.Branch(1, (acc +: kids))\n+        case other => throw new IllegalStateException(s\"unknown operator index $other\")\n+      }\n+      ops.foldLeft(base) { case (acc, (opIdx, kids)) => tagOp(opIdx, acc, kids) }\n+    }\n+\n+    def parse_Term[$: P]: P[Cst] = P(\n+      ((parse_Factor).map { case k0 => Cst.Branch(5, Vector(k0)) }) ~ ((((term(\"*\") ~ parse_Factor).map { case (k0, k1) => Vector(k0, k1) }).map(kids => (0, kids)) | ((term(\"/\") ~ parse_Factor).map { case (k0, k1) => Vector(k0, k1) }).map(kids => (1, kids)))).rep\n+    ).map { case (base, ops) =>\n+      def tagOp(opIdx: Int, acc: Cst, kids: Vector[Cst]): Cst = opIdx match {\n+              case 0 => Cst.Branch(3, (acc +: kids))\n+              case 1 => Cst.Branch(4, (acc +: kids))\n+        case other => throw new IllegalStateException(s\"unknown operator index $other\")\n+      }\n+      ops.foldLeft(base) { case (acc, (opIdx, kids)) => tagOp(opIdx, acc, kids) }\n+    }\n+\n+    def parse_Factor[$: P]: P[Cst] = P(((term(\"(\") ~ parse_Expr ~ term(\")\")).map { case (k0, k1, k2) => Cst.Branch(6, Vector(k0, k1, k2)) } | (term(\"NUMBER\")).map { case k0 => Cst.Branch(7, Vector(k0)) }))\n+\n+    def parseAll: Option[Cst] =\n+      fastparse.parse(input, parse_Expr(using _)) match {\n+        case Parsed.Success(cst, idx) if idx == input.length => Some(cst)\n+        case _ => None\n+      }\n+    def recognizeAll: Boolean = parseAll.isDefined\n+\n+  def parse(tokens: Vector[Token]): Option[Cst] = Driver(tokens).parseAll\n+  def recognize(tokens: Vector[Token]): Boolean = Driver(tokens).recognizeAll\n\\ No newline at end of file","raw_url":"https://github.com/wstein/gramaire/raw/3329a5b5a67142c9c92df5d8e0dd779942c8f1d1/test%2Fgolden%2FCalcFastparse.scala","sha":"06bce65fcc9ccbfe810515785b94e4fbb314b947","status":"added"}]}