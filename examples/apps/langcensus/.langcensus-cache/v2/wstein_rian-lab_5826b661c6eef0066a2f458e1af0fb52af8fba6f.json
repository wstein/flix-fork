{"files":[{"additions":53,"blob_url":"https://github.com/wstein/rian-lab/blob/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Fdecl.ex","changes":62,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fdecl.ex?ref=5826b661c6eef0066a2f458e1af0fb52af8fba6f","deletions":9,"filename":"lib/rian/decl.ex","patch":"@@ -29,16 +29,19 @@ defmodule Rian.Decl do\n     * `struct Name(field Type, …)` — product types; lower to `defstruct` (BEAM) /\n       `struct {…}` (Rust) and are built with positional `Name(v1, v2)` or named\n       `Name(field: v, …)` constructor calls.\n-    * `mod Name do … end` — modules grouping types/structs/defs; lower to a\n-      `defmodule` (BEAM) / `mod` (Rust). `pub` exports a `def`/`type`/`struct`\n-      (`def`/`pub fn`); unmarked items are private (`defp`/`fn`).\n+    * `mod Name do … end` — modules grouping types/structs/consts/defs; lower to\n+      a `defmodule` (BEAM) / `mod` (Rust). `pub` exports a `def`/`type`/`struct`/\n+      `const` (`def`/`pub fn`/`pub const`); unmarked items are private.\n+    * `const NAME Type := value` — module-scoped constants; lower to a 0-arity\n+      accessor (BEAM) / a `const` (Rust); references resolve per target.\n \n   ## Not yet supported\n \n-  Inside a `mod`, `const` and `use` (imports) raise `Rian.Decl.Error`.\n+  `use` (imports) raises `Rian.Decl.Error`; a top-level `const` (outside any\n+  `mod`) is rejected.\n   \"\"\"\n   alias Rian.{Check, Lexer, Lower}\n-  alias Rian.IR.{Clause, Field, Func, Mod, Param, Struct, Type, Variant}\n+  alias Rian.IR.{Clause, Const, Field, Func, Mod, Param, Struct, Type, Variant}\n \n   defmodule Error do\n     defexception [:message]\n@@ -53,15 +56,26 @@ defmodule Rian.Decl do\n     aliases = collect_aliases(decls)\n     prog = assemble(decls, aliases)\n \n+    # A top-level constant has no enclosing module to hold its accessor — `const`\n+    # is module-scoped (ADR-0033 / modules: items live in a `mod`).\n+    if prog.consts != [], do: raise(Error, \"`const` must appear inside a `mod`\")\n+\n     mods =\n       for {:mod, name, inner} <- decls do\n         # top-level aliases are visible inside a module; module-local aliases add to them\n         scoped = Map.merge(aliases, collect_aliases(inner))\n         p = assemble(inner, scoped)\n-        %Mod{name: name, types: p.types, structs: p.structs, funcs: p.funcs}\n+\n+        %Mod{\n+          name: name,\n+          types: p.types,\n+          structs: p.structs,\n+          consts: p.consts,\n+          funcs: p.funcs\n+        }\n       end\n \n-    Map.put(prog, :mods, mods)\n+    prog |> Map.delete(:consts) |> Map.put(:mods, mods)\n   end\n \n   # `alias Name := Type` is a transparent synonym: collect the name->type map so\n@@ -89,7 +103,11 @@ defmodule Rian.Decl do\n       for({:struct, s, pub?} <- decls, do: parse_struct(s, pub?))\n       |> Enum.map(&subst_struct(&1, aliases))\n \n-    %{types: types, structs: structs, funcs: funcs}\n+    consts =\n+      for({:const, c, pub?} <- decls, do: parse_const(c, pub?))\n+      |> Enum.map(&subst_const(&1, aliases))\n+\n+    %{types: types, structs: structs, consts: consts, funcs: funcs}\n   end\n \n   defp parse_alias(text) do\n@@ -184,6 +202,11 @@ defmodule Rian.Decl do\n     {{:alias, Lexer.detokenize(toks)}, rest}\n   end\n \n+  defp take_decl([{:kw, \"const\"} | rest]) do\n+    {toks, rest} = take_type(rest, [])\n+    {{:const, Lexer.detokenize(toks), false}, rest}\n+  end\n+\n   defp take_decl([{:kw, \"def\"} | rest]) do\n     {raw, rest} = take_def(rest)\n     {{:def, raw}, rest}\n@@ -220,8 +243,11 @@ defmodule Rian.Decl do\n \n   defp mark_pub({:type, s, _}), do: {:type, s, true}\n   defp mark_pub({:struct, s, _}), do: {:struct, s, true}\n+  defp mark_pub({:const, s, _}), do: {:const, s, true}\n   defp mark_pub({:def, raw}), do: {:def, Map.put(raw, :pub, true)}\n-  defp mark_pub(_other), do: raise(Error, \"`pub` may only precede `def` / `type` / `struct`\")\n+\n+  defp mark_pub(_other),\n+    do: raise(Error, \"`pub` may only precede `def` / `type` / `struct` / `const`\")\n \n   # A `type` runs to the newline that begins the next declaration (variant lines\n   # beginning with `|` are continuations) or the enclosing module's `end`.\n@@ -347,6 +373,24 @@ defmodule Rian.Decl do\n     end\n   end\n \n+  # ── `const` declarations ───────────────────────────────────────────────\n+  # `const NAME Type := value` — a named compile-time constant (juxtaposed type,\n+  # like a parameter without a capability).\n+  defp parse_const(text, pub?) do\n+    case split_once(text, \":=\") do\n+      {decl, value} ->\n+        case decl |> collapse_parens() |> String.split(~r/\\s+/, trim: true) do\n+          [name, type] -> %Const{name: name, type: type, value: value, pub?: pub?}\n+          _ -> raise Error, \"const needs `NAME Type := value`: #{text}\"\n+        end\n+\n+      :none ->\n+        raise Error, \"const needs `:=`: #{text}\"\n+    end\n+  end\n+\n+  defp subst_const(%Const{type: t} = c, aliases), do: %Const{c | type: subst_type_str(t, aliases)}\n+\n   # Detokenized type strings space their parens/commas (`Vec ( Int64 )`); collapse\n   # them back so a parenthesized type is one whitespace-split token (`Vec(Int64)`).\n   defp collapse_parens(s), do: Regex.replace(~r/\\s*([(),])\\s*/, s, \"\\\\1\")","raw_url":"https://github.com/wstein/rian-lab/raw/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Fdecl.ex","sha":"4f045cd22450cf0de7f720832974aa027b09977b","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/rian-lab/blob/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Fir.ex","changes":18,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fir.ex?ref=5826b661c6eef0066a2f458e1af0fb52af8fba6f","deletions":4,"filename":"lib/rian/ir.ex","patch":"@@ -47,14 +47,24 @@ defmodule Rian.IR do\n     defstruct name: nil, fields: [], pub?: false\n   end\n \n+  defmodule Const do\n+    @moduledoc \"\"\"\n+    A named compile-time constant (`const NAME Type := value`). `value` is the\n+    body source (parsed on lowering, like a function body). Lowers to a 0-arity\n+    accessor on the BEAM (`def`/`defp`) and a `const` on Rust; `pub?` exports it.\n+    \"\"\"\n+    @enforce_keys [:name, :type, :value]\n+    defstruct name: nil, type: nil, value: nil, pub?: false\n+  end\n+\n   defmodule Mod do\n     @moduledoc \"\"\"\n-    A module (`mod Name do … end`) — a namespace grouping `types`, `structs`, and\n-    `funcs`. Lowers to a `defmodule` on the BEAM and a `mod` on Rust; `pub?` items\n-    are exported (`def`/`pub fn`), the rest are private (`defp`/`fn`).\n+    A module (`mod Name do … end`) — a namespace grouping `types`, `structs`,\n+    `consts`, and `funcs`. Lowers to a `defmodule` on the BEAM and a `mod` on\n+    Rust; `pub?` items are exported (`def`/`pub fn`), the rest private (`defp`/`fn`).\n     \"\"\"\n     @enforce_keys [:name]\n-    defstruct name: nil, types: [], structs: [], funcs: []\n+    defstruct name: nil, types: [], structs: [], consts: [], funcs: []\n   end\n \n   defmodule Param do","raw_url":"https://github.com/wstein/rian-lab/raw/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Fir.ex","sha":"abab1b6bcade421657af7766e061b91c3dd045f0","status":"modified"},{"additions":62,"blob_url":"https://github.com/wstein/rian-lab/blob/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Flower.ex","changes":82,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Flower.ex?ref=5826b661c6eef0066a2f458e1af0fb52af8fba6f","deletions":20,"filename":"lib/rian/lower.ex","patch":"@@ -53,19 +53,22 @@ defmodule Rian.Lower do\n   @doc \"Compile a module to the BEAM target only.\"\n   def compile_module_beam(%Rian.IR.Mod{} = m), do: %{elixir: module_elixir(m)}\n \n-  defp module_elixir(%{name: name, types: types, structs: structs, funcs: funcs}) do\n+  defp module_elixir(%{name: name, types: types, structs: structs, funcs: funcs} = m) do\n     env = build_env(types, structs)\n     Enum.each(funcs, &(:ok = check!(&1, env)))\n     smeta = build_struct_meta(structs)\n+    consts = Map.get(m, :consts, [])\n+    cset = const_set(consts)\n \n     body =\n       [\n         Enum.map_join(structs, \"\\n\", &ex_struct/1),\n         Enum.map_join(types, \"\\n\", &ex_typespec/1),\n+        Enum.map_join(consts, \"\\n\", &ex_const(&1, smeta, cset)),\n         Enum.map_join(\n           funcs,\n           \"\\n\",\n-          &elixir_clauses(&1, smeta, if(&1.pub?, do: \"def\", else: \"defp\"))\n+          &elixir_clauses(&1, smeta, cset, if(&1.pub?, do: \"def\", else: \"defp\"))\n         )\n       ]\n       |> Enum.reject(&(&1 == \"\"))\n@@ -74,24 +77,47 @@ defmodule Rian.Lower do\n     \"defmodule #{name} do\\n#{body}\\nend\"\n   end\n \n-  defp module_rust(%{name: name, types: types, structs: structs, funcs: funcs}) do\n+  defp module_rust(%{name: name, types: types, structs: structs, funcs: funcs} = m) do\n     env = build_env(types, structs)\n     Enum.each(funcs, &(:ok = check!(&1, env)))\n     meta = build_meta(types)\n     smeta = build_struct_meta(structs)\n+    consts = Map.get(m, :consts, [])\n+    cset = const_set(consts)\n \n     body =\n       [\n         Enum.map_join(structs, \"\\n\\n\", &rust_struct/1),\n         Enum.map_join(types, \"\\n\\n\", &rust_enum/1),\n-        Enum.map_join(funcs, \"\\n\\n\", &rust_fn(&1, meta, smeta, if(&1.pub?, do: \"pub \", else: \"\")))\n+        Enum.map_join(consts, \"\\n\", &rust_const(&1, meta, smeta, cset)),\n+        Enum.map_join(\n+          funcs,\n+          \"\\n\\n\",\n+          &rust_fn(&1, meta, smeta, cset, if(&1.pub?, do: \"pub \", else: \"\"))\n+        )\n       ]\n       |> Enum.reject(&(&1 == \"\"))\n       |> Enum.join(\"\\n\\n\")\n \n     \"mod #{PL.to_snake(name)} {\\n#{body}\\n}\"\n   end\n \n+  defp const_set(consts), do: MapSet.new(consts, & &1.name)\n+\n+  # `const NAME Type := value` -> a 0-arity accessor on the BEAM (`def`/`defp`).\n+  defp ex_const(c, smeta, cset) do\n+    def_kw = if c.pub?, do: \"def\", else: \"defp\"\n+    val = c.value |> body_ast(smeta, cset) |> emit(:elixir) |> elem(0)\n+    \"#{def_kw} #{PL.to_snake(c.name)}() do #{val} end\"\n+  end\n+\n+  # `const NAME Type := value` -> a Rust `const` (`pub const` when exported).\n+  defp rust_const(c, meta, smeta, cset) do\n+    vis = if c.pub?, do: \"pub \", else: \"\"\n+    val = c.value |> body_ast(smeta, cset) |> resolve_rust_pats(meta) |> emit(:rust) |> elem(0)\n+    \"#{vis}const #{c.name}: #{prim_rust(c.type)} = #{val};\"\n+  end\n+\n   defp build_env(types, structs) do\n     env =\n       Enum.reduce(types, E.base_env(), fn t, env ->\n@@ -149,25 +175,29 @@ defmodule Rian.Lower do\n     typespecs = Enum.map_join(types, \"\\n\", &ex_typespec/1)\n     struct_defs = Enum.map_join(structs, \"\\n\", &ex_struct/1)\n \n-    [struct_defs, typespecs, elixir_clauses(func, smeta, \"def\")]\n+    [struct_defs, typespecs, elixir_clauses(func, smeta, MapSet.new(), \"def\")]\n     |> Enum.reject(&(&1 == \"\"))\n     |> Enum.join(\"\\n\")\n   end\n \n   # The `def`/`defp` clauses of one function (no type/struct preamble). `def_kw`\n   # selects the visibility keyword (top-level is always `def`; inside a `mod` a\n-  # private function is `defp`).\n-  defp elixir_clauses(func, smeta, def_kw) do\n+  # private function is `defp`). `cset` is the set of in-scope constant names.\n+  defp elixir_clauses(func, smeta, cset, def_kw) do\n     Enum.each(func.params, &Rian.Capability.beam_legal!(&1.cap))\n \n     Enum.map_join(func.clauses, \"\\n\", fn c ->\n       head = \"#{def_kw} #{func.name}(#{Enum.map_join(c.pats, \", \", &pat_ex/1)})\"\n-      body_ast = c.body |> Pratt.parse_body() |> resolve_structs(smeta)\n-      body = emit(body_ast, :elixir) |> elem(0)\n+      body = c.body |> body_ast(smeta, cset) |> emit(:elixir) |> elem(0)\n       \"#{head}#{guard_str(c, :elixir)} do #{body} end\"\n     end)\n   end\n \n+  # Parse a body source and run the target-neutral resolution passes (struct\n+  # construction + constant references) the emitter relies on.\n+  defp body_ast(src, smeta, cset),\n+    do: src |> Pratt.parse_body() |> resolve_structs(smeta) |> resolve_consts(cset)\n+\n   # `struct Point(x Float64, y Float64)` -> a nested module carrying `defstruct`,\n   # so `%Point{…}` construction and `p.x` access resolve on the BEAM.\n   defp ex_struct(s) do\n@@ -190,6 +220,15 @@ defmodule Rian.Lower do\n \n   defp resolve_structs(node, smeta), do: Rian.Macro.map_node(node, &resolve_structs(&1, smeta))\n \n+  # Rewrite a reference to an in-scope constant (`{:id, NAME}`) into a\n+  # `{:const_ref, NAME}` node, so the emitter spells it per target (a BEAM\n+  # accessor call vs. a Rust `const` name) without needing ambient context.\n+  defp resolve_consts({:id, name} = node, cset) do\n+    if MapSet.member?(cset, name), do: {:const_ref, name}, else: node\n+  end\n+\n+  defp resolve_consts(node, cset), do: Rian.Macro.map_node(node, &resolve_consts(&1, cset))\n+\n   # Build the `{label, value}` pairs of a struct literal. All-positional args zip\n   # onto the declared field order; all-named args (`x: …`) are placed by name (so\n   # source order is free); a mix is rejected.\n@@ -305,13 +344,14 @@ defmodule Rian.Lower do\n     enums = Enum.map_join(types, \"\\n\\n\", &rust_enum/1)\n     struct_defs = Enum.map_join(structs, \"\\n\\n\", &rust_struct/1)\n \n-    [struct_defs, enums, rust_fn(func, meta, smeta, \"\")]\n+    [struct_defs, enums, rust_fn(func, meta, smeta, MapSet.new(), \"\")]\n     |> Enum.reject(&(&1 == \"\"))\n     |> Enum.join(\"\\n\\n\")\n   end\n \n-  # One Rust `fn` (no type/struct preamble). `vis` is `\"\"` or `\"pub \"`.\n-  defp rust_fn(func, meta, smeta, vis) do\n+  # One Rust `fn` (no type/struct preamble). `vis` is `\"\"` or `\"pub \"`; `cset` is\n+  # the set of in-scope constant names.\n+  defp rust_fn(func, meta, smeta, cset, vis) do\n     param_decls =\n       Enum.map_join(func.params, \", \", fn p ->\n         \"#{p.name}: #{Rian.Capability.rust_param(p.cap, p.type)}\"\n@@ -324,14 +364,12 @@ defmodule Rian.Lower do\n     arms =\n       Enum.map_join(func.clauses, \"\\n\", fn c ->\n         pat = tuple_or_one(c.pats, &pat_rs(&1, meta))\n-        # Resolve struct construction and `case` constructor patterns into the\n-        # body IR here, where the type/struct meta is available — so the recursive\n-        # emitter needs no ambient context (the IR carries the resolution).\n-        body_ast =\n-          c.body |> Pratt.parse_body() |> resolve_structs(smeta) |> resolve_rust_pats(meta)\n-\n-        body = emit(body_ast, :rust) |> elem(0)\n-        \"        #{pat}#{guard_str(c, :rust)} => #{rust_arm_body(body_ast, body)},\"\n+        # Resolve struct construction, constant references, and `case` constructor\n+        # patterns into the body IR here, where the meta is available — so the\n+        # recursive emitter needs no ambient context (the IR carries it).\n+        ast = c.body |> body_ast(smeta, cset) |> resolve_rust_pats(meta)\n+        body = emit(ast, :rust) |> elem(0)\n+        \"        #{pat}#{guard_str(c, :rust)} => #{rust_arm_body(ast, body)},\"\n       end)\n \n     \"#{vis}fn #{func.name}(#{param_decls}) -> #{prim_rust(func.ret)} {\\n\" <>\n@@ -521,6 +559,10 @@ defmodule Rian.Lower do\n \n   defp emit({:map_lit, _}, :rust), do: raise(\"map literals are BEAM-only in PoC\")\n \n+  # constant reference — a 0-arity accessor call on the BEAM, the `const` name on Rust\n+  defp emit({:const_ref, name}, :elixir), do: {\"#{PL.to_snake(name)}()\", 12}\n+  defp emit({:const_ref, name}, :rust), do: {name, 12}\n+\n   # struct literal — `%Name{x: …}` on the BEAM, `Name { x: … }` on Rust\n   defp emit({:struct_lit, name, pairs}, :elixir),\n     do:","raw_url":"https://github.com/wstein/rian-lab/raw/5826b661c6eef0066a2f458e1af0fb52af8fba6f/lib%2Frian%2Flower.ex","sha":"af5d0d086ff8df200f233cb693b4c5f57ed1e9be","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/rian-lab/blob/5826b661c6eef0066a2f458e1af0fb52af8fba6f/test%2Frian%2Fdecl_test.exs","changes":39,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_test.exs?ref=5826b661c6eef0066a2f458e1af0fb52af8fba6f","deletions":2,"filename":"test/rian/decl_test.exs","patch":"@@ -399,10 +399,45 @@ defmodule Rian.DeclTest do\n     end\n   end\n \n+  describe \"const declarations (module constants)\" do\n+    @consts \"\"\"\n+    mod Scaling do\n+      pub const TAU Float64 := 6.28318530\n+      const SCALE Float64 := 2.0\n+\n+      pub def scaled(r Float64) Float64 := TAU * r * SCALE\n+    end\n+    \"\"\"\n+\n+    test \"consts lower to accessors / Rust const, and references resolve per target\" do\n+      [{\"Scaling\", out}] = Decl.compile(@consts)\n+\n+      assert out.elixir =~ \"def tau() do 6.28318530 end\"\n+      assert out.elixir =~ \"defp scale() do 2.0 end\"\n+      assert out.elixir =~ \"def scaled(r) do tau() * r * scale() end\"\n+\n+      assert out.rust =~ \"pub const TAU: f64 = 6.28318530;\"\n+      assert out.rust =~ \"const SCALE: f64 = 2.0;\"\n+      assert out.rust =~ \"TAU * r * SCALE\"\n+    end\n+\n+    test \"a module with constants runs on the BEAM\" do\n+      [{\"Scaling\", out}] = Decl.compile(@consts)\n+      Code.eval_string(out.elixir)\n+      assert_in_delta Scaling.scaled(3.0), 6.28318530 * 3.0 * 2.0, 1.0e-9\n+    end\n+\n+    test \"a top-level const (no enclosing module) is rejected\" do\n+      assert_raise Decl.Error, ~r/`const` must appear inside a `mod`/, fn ->\n+        Decl.parse(\"const TAU Float64 := 6.28\")\n+      end\n+    end\n+  end\n+\n   describe \"honest limits raise Rian.Decl.Error\" do\n     test \"unsupported declaration keywords are rejected\" do\n-      assert_raise Decl.Error, ~r/unsupported declaration `const`/, fn ->\n-        Decl.parse(\"const TAU Float64 := 6.28\")\n+      assert_raise Decl.Error, ~r/unsupported declaration `use`/, fn ->\n+        Decl.parse(\"use Math\")\n       end\n     end\n   end","raw_url":"https://github.com/wstein/rian-lab/raw/5826b661c6eef0066a2f458e1af0fb52af8fba6f/test%2Frian%2Fdecl_test.exs","sha":"8715ad02de92861d7326e66b87808d762d318e48","status":"modified"}]}