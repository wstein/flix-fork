{"files":[{"additions":31,"blob_url":"https://github.com/wstein/bin/blob/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/README.adoc","changes":31,"contents_url":"https://api.github.com/repos/wstein/bin/contents/README.adoc?ref=bc71b2a0a11760ea9605c0d49b6cbc9d38bba165","deletions":0,"filename":"README.adoc","patch":"@@ -1,3 +1,34 @@\n = My shell scripts\n \n Should be compatible with POSIX shell.\n+\n+== j2ad\n+\n+`j2ad` converts JSON to AION delimited syntax. It reads JSON from a file or\n+standard input and writes compact output by default:\n+\n+----\n+cat input.json | j2ad\n+j2ad --pretty --width 80 input.json output.aion\n+----\n+\n+Supported CLI options:\n+\n+* `--pretty` / `--no-pretty` toggles expanded output.\n+* `--width N` or `--width=N` sets the target pretty-print width. The minimum is\n+  `40`.\n+* `--object-threshold N` or `--object-threshold=N` expands objects with at\n+  least `N` fields even when the compact form fits. Set `0` to disable that\n+  heuristic.\n+* `--expand-lists` / `--no-expand-lists` toggles expansion of lists containing\n+  non-scalar values.\n+\n+Configuration is read from `~/.j2adrc` and then `.j2adrc`; CLI options override\n+both files. Config files accept whole-line and inline `#` comments:\n+\n+----\n+pretty true\n+width 80 # keep output readable in terminals\n+object_threshold 3\n+expand_lists true\n+----","raw_url":"https://github.com/wstein/bin/raw/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/README.adoc","sha":"43291d6f12f8efa8e75e7bc3f62bc1530c2977d8","status":"modified"},{"additions":77,"blob_url":"https://github.com/wstein/bin/blob/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/j2ad","changes":137,"contents_url":"https://api.github.com/repos/wstein/bin/contents/j2ad?ref=bc71b2a0a11760ea9605c0d49b6cbc9d38bba165","deletions":60,"filename":"j2ad","patch":"@@ -13,8 +13,8 @@ defmodule J2AD do\n   Options:\n     --pretty                  Pretty (expanded) output\n     --no-pretty               Compact output (overrides config file)\n-    --width=N                 Target line width (default: 100, min: 40)\n-    --object-threshold=N      Expand objects with >= N fields even when compact\n+    --width N                 Target line width (default: 100, min: 40)\n+    --object-threshold N      Expand objects with >= N fields even when compact\n                               form fits; 0 disables this heuristic (default: 3)\n     --expand-lists            Expand lists that contain non-scalar elements\n     --no-expand-lists         Disable list expansion heuristic\n@@ -25,6 +25,9 @@ defmodule J2AD do\n     object_threshold  N\n     expand_lists      true|false\n \n+  Options may also use --width=N and --object-threshold=N forms. Config files\n+  support whole-line and inline # comments.\n+\n   CLI flags override .j2adrc which overrides ~/.j2adrc which overrides defaults.\n \n   Default compact output:\n@@ -116,87 +119,93 @@ defmodule J2AD do\n   end\n \n   defp parse_args(argv) do\n-    {flags, args} = Enum.split_with(argv, &String.starts_with?(&1, \"--\"))\n+    {cli_opts, args, invalid} =\n+      OptionParser.parse(argv,\n+        strict: [\n+          pretty: :boolean,\n+          width: :integer,\n+          object_threshold: :integer,\n+          expand_lists: :boolean,\n+          help: :boolean\n+        ],\n+        aliases: [h: :help]\n+      )\n+\n+    if invalid != [] do\n+      invalid_option_error(invalid)\n+    end\n \n     opts =\n       default_opts()\n       |> merge_config(load_config(Path.expand(\"~/.j2adrc\")))\n       |> merge_config(load_config(\".j2adrc\"))\n-      |> merge_cli_flags(flags)\n+      |> merge_cli_flags(cli_opts)\n \n-    {opts, args}\n+    if Keyword.get(cli_opts, :help, false) do\n+      {opts, [\"--help\" | args]}\n+    else\n+      {opts, args}\n+    end\n   end\n \n   defp merge_config(opts, extra), do: Map.merge(opts, extra)\n \n-  defp merge_cli_flags(opts, flags) do\n-    bool_flags = [\"--pretty\", \"--no-pretty\", \"--expand-lists\", \"--no-expand-lists\"]\n-\n-    {value_flags, plain_flags} = Enum.split_with(flags, &String.contains?(&1, \"=\"))\n-\n-    {width_flags, threshold_flags, unknown_value} =\n-      Enum.reduce(value_flags, {[], [], []}, fn flag, {w, t, u} ->\n-        cond do\n-          String.starts_with?(flag, \"--width=\") -> {[flag | w], t, u}\n-          String.starts_with?(flag, \"--object-threshold=\") -> {w, [flag | t], u}\n-          true -> {w, t, [flag | u]}\n-        end\n-      end)\n+  defp invalid_option_error(invalid) do\n+    cond do\n+      Enum.any?(invalid, &(elem(&1, 0) == \"--width\")) ->\n+        flag_error(\"--width must be an integer >= 40\")\n \n-    unknown = (plain_flags -- bool_flags) ++ unknown_value\n+      Enum.any?(invalid, &(elem(&1, 0) == \"--object-threshold\")) ->\n+        flag_error(\"--object-threshold must be an integer >= 0\")\n \n-    if unknown != [] do\n-      IO.puts(:stderr, \"unknown option(s): #{Enum.join(unknown, \", \")}\")\n-      IO.puts(:stderr, @usage)\n-      System.halt(64)\n+      true ->\n+        invalid\n+        |> Enum.map(&elem(&1, 0))\n+        |> Enum.join(\", \")\n+        |> then(&flag_error(\"unknown option(s): #{&1}\"))\n     end\n+  end\n \n-    width =\n-      case width_flags do\n-        [] -> opts.width\n-        [flag] -> parse_flag_int!(flag, \"--width\", min: 40)\n-        _ -> flag_error(\"--width may be provided only once\")\n-      end\n-\n-    object_threshold =\n-      case threshold_flags do\n-        [] -> opts.object_threshold\n-        [flag] -> parse_flag_int!(flag, \"--object-threshold\", min: 0)\n-        _ -> flag_error(\"--object-threshold may be provided only once\")\n-      end\n-\n-    pretty? =\n-      cond do\n-        \"--no-pretty\" in plain_flags -> false\n-        \"--pretty\" in plain_flags -> true\n-        true -> opts.pretty?\n-      end\n-\n-    expand_lists =\n-      cond do\n-        \"--no-expand-lists\" in plain_flags -> false\n-        \"--expand-lists\" in plain_flags -> true\n-        true -> opts.expand_lists\n-      end\n-\n+  defp merge_cli_flags(opts, cli_opts) do\n     %{\n       opts\n-      | pretty?: pretty?,\n-        width: width,\n-        object_threshold: object_threshold,\n-        expand_lists: expand_lists\n+      | pretty?: cli_bool(cli_opts, :pretty, opts.pretty?),\n+        width: cli_int(cli_opts, :width, \"--width\", opts.width, min: 40),\n+        object_threshold:\n+          cli_int(\n+            cli_opts,\n+            :object_threshold,\n+            \"--object-threshold\",\n+            opts.object_threshold,\n+            min: 0\n+          ),\n+        expand_lists: cli_bool(cli_opts, :expand_lists, opts.expand_lists)\n     }\n   end\n \n-  defp parse_flag_int!(flag, name, min: min) do\n-    raw = flag |> String.split(\"=\", parts: 2) |> List.last()\n+  defp cli_bool(cli_opts, key, default) do\n+    values = Keyword.get_values(cli_opts, key)\n+\n+    cond do\n+      false in values -> false\n+      true in values -> true\n+      true -> default\n+    end\n+  end\n+\n+  defp cli_int(cli_opts, key, name, default, min: min) do\n+    case Keyword.get_values(cli_opts, key) do\n+      [] ->\n+        default\n \n-    case Integer.parse(raw) do\n-      {value, \"\"} when value >= min ->\n+      [value] when is_integer(value) and value >= min ->\n         value\n \n-      _ ->\n+      [_value] ->\n         flag_error(\"#{name} must be an integer >= #{min}\")\n+\n+      _ ->\n+        flag_error(\"#{name} may be provided only once\")\n     end\n   end\n \n@@ -227,6 +236,7 @@ defmodule J2AD do\n     content\n     |> String.split(\"\\n\")\n     |> Enum.with_index(1)\n+    |> Enum.map(fn {line, lineno} -> {strip_inline_comment(line), lineno} end)\n     |> Enum.reject(fn {line, _} ->\n       t = String.trim(line)\n       t == \"\" or String.starts_with?(t, \"#\")\n@@ -243,6 +253,13 @@ defmodule J2AD do\n     end)\n   end\n \n+  defp strip_inline_comment(line) do\n+    line\n+    |> String.split(\"#\", parts: 2)\n+    |> hd()\n+    |> String.trim()\n+  end\n+\n   defp parse_config_entry(acc, key, value, path, lineno) do\n     warn = fn msg -> IO.puts(:stderr, \"warning: #{path}:#{lineno}: #{msg}, ignoring\") end\n ","raw_url":"https://github.com/wstein/bin/raw/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/j2ad","sha":"194c259186267f60c140cf082bea004795f3ab32","status":"modified"},{"additions":53,"blob_url":"https://github.com/wstein/bin/blob/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/j2ad_test.exs","changes":53,"contents_url":"https://api.github.com/repos/wstein/bin/contents/j2ad_test.exs?ref=bc71b2a0a11760ea9605c0d49b6cbc9d38bba165","deletions":0,"filename":"j2ad_test.exs","patch":"@@ -26,6 +26,19 @@ defmodule J2ADTest do\n   defp pretty(json, overrides \\\\ %{}),\n     do: J2AD.convert!(json, opts(Map.merge(%{pretty?: true}, overrides)))\n \n+  defp run_cli(args, input) do\n+    path = Path.join(System.tmp_dir!(), \"j2ad-test-#{System.unique_integer([:positive])}.json\")\n+    File.write!(path, input)\n+\n+    try do\n+      System.cmd(\"env\", [\"-u\", \"J2AD_TESTING\", \"elixir\", \"j2ad\" | args ++ [path]],\n+        stderr_to_stdout: true\n+      )\n+    after\n+      File.rm(path)\n+    end\n+  end\n+\n   # ---------------------------------------------------------------------------\n   # Compact: objects\n   # ---------------------------------------------------------------------------\n@@ -326,6 +339,20 @@ defmodule J2ADTest do\n       assert J2AD.parse_config_text(\"# comment\\nwidth 80\\n# another\") == %{width: 80}\n     end\n \n+    test \"inline # comments are ignored after values\" do\n+      config = \"\"\"\n+      width 80 # keep compact lines readable\n+      object_threshold 0 # disable expansion heuristic\n+      pretty true # expand root objects\n+      \"\"\"\n+\n+      assert J2AD.parse_config_text(config) == %{\n+               width: 80,\n+               object_threshold: 0,\n+               pretty?: true\n+             }\n+    end\n+\n     test \"blank lines are ignored\" do\n       assert J2AD.parse_config_text(\"\\n\\nwidth 80\\n\\n\") == %{width: 80}\n     end\n@@ -406,6 +433,32 @@ defmodule J2ADTest do\n     end\n   end\n \n+  # ---------------------------------------------------------------------------\n+  # CLI options\n+  # ---------------------------------------------------------------------------\n+\n+  describe \"CLI options\" do\n+    test \"accepts --width N with OptionParser argument style\" do\n+      {output, 0} =\n+        run_cli(\n+          [\"--pretty\", \"--width\", \"40\", \"--object-threshold\", \"0\"],\n+          ~s|{\"outer\":{\"longkey1\":\"longvalue1\",\"longkey2\":\"longvalue2\"}}|\n+        )\n+\n+      assert output == \"{\\n  outer {\\n    longkey1 longvalue1\\n    longkey2 longvalue2\\n  }\\n}\\n\"\n+    end\n+\n+    test \"still accepts --width=N and --object-threshold=N\" do\n+      {output, 0} =\n+        run_cli(\n+          [\"--pretty\", \"--width=1000\", \"--object-threshold=0\"],\n+          ~s|{\"outer\":{\"x\":1,\"y\":2,\"z\":3}}|\n+        )\n+\n+      assert output == \"{\\n  outer {x 1 y 2 z 3}\\n}\\n\"\n+    end\n+  end\n+\n   # ---------------------------------------------------------------------------\n   # Atom quoting\n   # ---------------------------------------------------------------------------","raw_url":"https://github.com/wstein/bin/raw/bc71b2a0a11760ea9605c0d49b6cbc9d38bba165/j2ad_test.exs","sha":"896ba78e54914262936a05f84634aadb31733dd0","status":"modified"}]}