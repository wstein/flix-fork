{"files":[{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/7dda5197ee417dcb969f0dd3f581f5e2430cdbad/site%2Fsrc%2Fpages%2Fplayground.astro","changes":10,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fplayground.astro?ref=7dda5197ee417dcb969f0dd3f581f5e2430cdbad","deletions":4,"filename":"site/src/pages/playground.astro","patch":"@@ -121,12 +121,14 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   let liveJs: string | null = null;\n \n   // the real Rian compiler, compiled to JS by stock purs (ADR-0090) — lazy-loaded once on first Run.\n-  // Served from `public/` beside the site; the URL must carry the deploy base (`/rian-lab/` on GitHub\n+  // Served from `public/` beside the site; the URL must carry the deploy base (`/rian-lab` on GitHub\n   // Pages, `/` locally) or it resolves against the domain root and 404s. `BASE_URL` is build-time\n-  // replaced and always ends in `/`; the variable + `@vite-ignore` keep it a runtime import (a static\n-  // literal would make Rollup try to resolve the public asset at build).\n+  // replaced but its trailing slash is NOT guaranteed (`configure-pages` emits `/rian-lab` with none),\n+  // so strip-and-re-add exactly one — the pattern the rest of the site uses. The variable +\n+  // `@vite-ignore` keep it a runtime import (a static literal would make Rollup resolve it at build).\n   let compilerP: Promise<{ compile: (src: string) => string }> | null = null;\n-  const compilerUrl = import.meta.env.BASE_URL + \"rian-compiler.mjs\";\n+  const base = import.meta.env.BASE_URL.replace(/\\/$/, \"\");\n+  const compilerUrl = base + \"/rian-compiler.mjs\";\n   const getCompiler = () => (compilerP ??= import(/* @vite-ignore */ compilerUrl));\n \n   function renderEditor() {","raw_url":"https://github.com/wstein/rian-lab/raw/7dda5197ee417dcb969f0dd3f581f5e2430cdbad/site%2Fsrc%2Fpages%2Fplayground.astro","sha":"a39d45b421709067be595d31d304f6d71edcf27d","status":"modified"}]}