{"files":[{"additions":57,"blob_url":"https://github.com/wstein/bin/blob/5a3bb737274777831c765dca597ee0060ae0422d/histlog","changes":88,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=5a3bb737274777831c765dca597ee0060ae0422d","deletions":31,"filename":"histlog","patch":"@@ -1614,7 +1614,12 @@ def output_json_results(results)\n       result\n     end\n \n-    puts JSON.pretty_generate(json_results)\n+    begin\n+      puts JSON.pretty_generate(json_results)\n+    rescue Errno::EPIPE\n+      # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n+      exit 0\n+    end\n   ensure\n     db.close if db\n   end\n@@ -1671,22 +1676,32 @@ def output_yaml_results(results)\n       result\n     end\n \n-    puts yaml_results.to_yaml\n+    begin\n+      puts yaml_results.to_yaml\n+    rescue Errno::EPIPE\n+      # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n+      exit 0\n+    end\n   ensure\n     db.close if db\n   end\n end\n \n # Output results in plain format with timestamps\n def output_plain_results(results, options)\n-  results.each do |row|\n-    command = row['cmd_text']\n-    if row['start_time']\n-      timestamp = Time.at(row['start_time'].to_f).strftime('%Y-%m-%d %H:%M:%S')\n-      puts \"[#{timestamp}] #{command}\"\n-    else\n-      puts command\n+  begin\n+    results.each do |row|\n+      command = row['cmd_text']\n+      if row['start_time']\n+        timestamp = Time.at(row['start_time'].to_f).strftime('%Y-%m-%d %H:%M:%S')\n+        puts \"[#{timestamp}] #{command}\"\n+      else\n+        puts command\n+      end\n     end\n+  rescue Errno::EPIPE\n+    # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n+    exit 0\n   end\n end\n \n@@ -1714,7 +1729,12 @@ def output_shell_history_results(results, format, options)\n   }\n \n   formatted_commands = format_commands_for_shell(formatted_results, format, format_options)\n-  formatted_commands.each { |line| puts line }\n+  begin\n+    formatted_commands.each { |line| puts line }\n+  rescue Errno::EPIPE\n+    # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n+    exit 0\n+  end\n end\n \n # Output results for display\n@@ -1725,30 +1745,36 @@ def output_interactive_results(results, options)\n   end\n \n   # Simple output: just print commands (optionally prefixed by ISO8601 start_time)\n-  results.each do |row|\n-  command = row['cmd_text'] || row['command']\n-    lines = command.split(\"\\n\")\n-    time_prefix = \"\"\n-    if options && options[:show_time] && row['start_time']\n-      begin\n-        t = Time.at(row['start_time'].to_f)\n-        # Simplified format: YYYY-MM-DD HH:MM\n-        time_prefix = t.strftime(\"%Y-%m-%d %H:%M:%S\") + \" \"\n-      rescue\n-        time_prefix = \"\"\n+  begin\n+    results.each do |row|\n+    command = row['cmd_text'] || row['command']\n+      lines = command.split(\"\\n\")\n+      time_prefix = \"\"\n+      if options && options[:show_time] && row['start_time']\n+        begin\n+          t = Time.at(row['start_time'].to_f)\n+          # Simplified format: YYYY-MM-DD HH:MM\n+          time_prefix = t.strftime(\"%Y-%m-%d %H:%M:%S\") + \" \"\n+        rescue\n+          time_prefix = \"\"\n+        end\n       end\n-    end\n \n-    if results.length == 1\n-      # Single result: show full command, even if multiline\n-      puts time_prefix + command\n-    elsif lines.length > 1\n-      # Multiple results with multiline command: show first line + \"...\"\n-      puts time_prefix + \"#{lines.first}...\"\n-    else\n-      # Multiple results with single line command: show as-is\n-      puts time_prefix + command\n+      if results.length == 1\n+        # Single result: show full command, even if multiline\n+        puts time_prefix + command\n+      elsif lines.length > 1\n+        # Multiple results with multiline command: show first line + \"...\"\n+        puts time_prefix + \"#{lines.first}...\"\n+      else\n+        # Multiple results with single line command: show as-is\n+        puts time_prefix + command\n+      end\n     end\n+  rescue Errno::EPIPE\n+    # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n+    # This is normal, just exit quietly\n+    exit 0\n   end\n end\n ","raw_url":"https://github.com/wstein/bin/raw/5a3bb737274777831c765dca597ee0060ae0422d/histlog","sha":"75f4915cb5c80beae7d26f2622e5b223c6f9db61","status":"modified"}]}