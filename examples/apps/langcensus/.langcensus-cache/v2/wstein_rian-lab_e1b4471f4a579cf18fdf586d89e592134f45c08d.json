{"files":[{"additions":31,"blob_url":"https://github.com/wstein/rian-lab/blob/e1b4471f4a579cf18fdf586d89e592134f45c08d/examples%2Frian%2F10_function_forms.rian","changes":31,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2F10_function_forms.rian?ref=e1b4471f4a579cf18fdf586d89e592134f45c08d","deletions":0,"filename":"examples/rian/10_function_forms.rian","patch":"@@ -72,3 +72,34 @@ def show2(Add(a, b))                  # block body: name the parts, then join\n   rs := show2(b)\n   \"(\" <> ls <> \" + \" <> rs <> \")\"\n end\n+\n+# --- (4) functions as VALUES: the `Fn(…)` type (ADR-0042) -----------------\n+# A function value has a type, written `Fn(Arg1, …, ArgN, Ret)` — the argument\n+# types followed by the return type. THE LAST ELEMENT IS ALWAYS THE RETURN.\n+#\n+#   Fn(Int64, Bool)            a one-arg fn: takes Int64, returns Bool\n+#   Fn(Int64, Int64, Int64)    a two-arg fn: (Int64, Int64) -> Int64\n+#   Fn(Bool)                   a NULLARY fn: () -> Bool  (just the return)\n+#\n+# This is the type you give a parameter that receives a lambda `(x) -> e` or a\n+# capture `&name/arity`. Applying such a parameter (`f(x)`) yields its return\n+# type, so the checker types higher-order code instead of giving up.\n+\n+# A higher-order function: `f` is itself a function. Applying it twice is typed\n+# end to end — `f(x)` is Int64, so `f(f(x))` is Int64, matching the return.\n+def apply_twice(f Fn(Int64, Int64), x Int64) Int64 := f(f(x))\n+\n+# The canonical generic `map`: `f` maps each `T` to a `U`, `xs` is a `Vec(T)`,\n+# the result is a `Vec(U)`. `forall T, U` binds the type variables (ADR-0042).\n+def map(f Fn(T, U), xs Vec(T)) Vec(U) forall T, U\n+def map(_, [])      := []\n+def map(f, [h | t]) := [f(h) | map(f, t)]\n+\n+# A function may RETURN a function (a closure over its parameters). The declared\n+# return type is an `Fn(…)`; the lambda body is checked against it.\n+def adder(n Int64) Fn(Int64, Int64) := (x) -> x + n\n+\n+# Both a lambda literal and a named-function capture are values of an `Fn(…)`\n+# type, so either can be passed where an `Fn(…)` parameter is expected.\n+def doubled(xs val Vec(Int64)) Vec(Int64) := map((x) -> x * 2, xs)\n+def doubled_cap(xs val Vec(Int64)) Vec(Int64) := map(&double/1, xs)","raw_url":"https://github.com/wstein/rian-lab/raw/e1b4471f4a579cf18fdf586d89e592134f45c08d/examples%2Frian%2F10_function_forms.rian","sha":"4a7003fb670ea8c7d80a929036ac8040aaed025b","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/e1b4471f4a579cf18fdf586d89e592134f45c08d/lib%2Frian%2Fcheck.ex","changes":17,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcheck.ex?ref=e1b4471f4a579cf18fdf586d89e592134f45c08d","deletions":1,"filename":"lib/rian/check.ex","patch":"@@ -164,7 +164,7 @@ defmodule Rian.Check do\n   def infer(%ECall{fun: %EId{name: f}}, env, ic) do\n     cond do\n       fn_type?(ft = Map.get(env, f)) -> fn_ret(ft)\n-      true -> ctor_type(ic, f) || Map.get(Map.get(ic, :funs, %{}), f) || :unknown\n+      true -> ctor_type(ic, f) || called_ret(ic, f)\n     end\n   end\n \n@@ -284,6 +284,21 @@ defmodule Rian.Check do\n \n   defp ctor_type(ic, name), do: Map.get(Map.get(ic, :ctors, %{}), name)\n \n+  # a named function's declared return type — but a *generic* return (one that\n+  # mentions an un-instantiated type variable, e.g. `Vec(U)` from a `forall`\n+  # function) infers `:unknown`: we don't instantiate generics, so pinning it to\n+  # the literal `Vec(U)` would wrongly contradict a concrete caller (conservative)\n+  defp called_ret(ic, f) do\n+    case Map.get(Map.get(ic, :funs, %{}), f) do\n+      nil -> :unknown\n+      ret -> if has_tvar?(ret), do: :unknown, else: ret\n+    end\n+  end\n+\n+  # does a type string mention a standalone type variable (a single capital,\n+  # optionally one digit) — `U`, `Vec(U)`, `Fn(T, U)` yes; `Int64`, `Vec(Int64)` no\n+  defp has_tvar?(s), do: Regex.match?(~r/\\b[A-Z][0-9]?\\b/, s)\n+\n   defp infer_tail(:close, _env, _ic), do: :unknown\n   defp infer_tail(tail, env, ic), do: infer(tail, env, ic)\n ","raw_url":"https://github.com/wstein/rian-lab/raw/e1b4471f4a579cf18fdf586d89e592134f45c08d/lib%2Frian%2Fcheck.ex","sha":"d9f7476c3eac5bb53aa76828a7dac8a2c19b2fc9","status":"modified"},{"additions":18,"blob_url":"https://github.com/wstein/rian-lab/blob/e1b4471f4a579cf18fdf586d89e592134f45c08d/test%2Frian%2Fcheck_test.exs","changes":18,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcheck_test.exs?ref=e1b4471f4a579cf18fdf586d89e592134f45c08d","deletions":0,"filename":"test/rian/check_test.exs","patch":"@@ -297,6 +297,24 @@ defmodule Rian.CheckTest do\n       %{funcs: [f]} = Rian.Decl.parse(src)\n       assert [%{name: \"f\", type: \"Fn(T,U)\"}, %{name: \"xs\", type: \"Vec(T)\"}] = f.params\n     end\n+\n+    test \"calling a generic function yields `:unknown`, not its literal `Vec(U)`\" do\n+      # `map` returns `Vec(U)`; we don't instantiate generics, so a concrete\n+      # caller must NOT be contradicted by the uninstantiated return.\n+      assert Check.check(\"\"\"\n+             def map(f Fn(T, U), xs Vec(T)) Vec(U) forall T, U := []\n+             def double(n Int64) Int64 := n * 2\n+             def doubled(xs val Vec(Int64)) Vec(Int64) := map(&double/1, xs)\n+             \"\"\") == :ok\n+    end\n+\n+    test \"a full higher-order recursive `map` over a Vec checks end to end\" do\n+      assert Check.check(\"\"\"\n+             def map(f Fn(T, U), xs Vec(T)) Vec(U) forall T, U\n+             def map(_, []) := []\n+             def map(f, [h | t]) := [f(h) | map(f, t)]\n+             \"\"\") == :ok\n+    end\n   end\n \n   describe \"annotate/3 — types on nodes (ADR-0050 §3)\" do","raw_url":"https://github.com/wstein/rian-lab/raw/e1b4471f4a579cf18fdf586d89e592134f45c08d/test%2Frian%2Fcheck_test.exs","sha":"a81cbb71ddad9eeb9b0afbc58c9716208f167820","status":"modified"}]}