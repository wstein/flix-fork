{"files":[{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/docs%2Fself-host-status.md","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fself-host-status.md?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":2,"filename":"docs/self-host-status.md","patch":"@@ -14,7 +14,7 @@ stages verified against the reference *in isolation* (each fixpoint uses Elixir\n projection glue); it can reach 100% without the pipeline ever closing a loop. The\n composition rung measures the orthogonal question — stages handing their Rian output\n to the next Rian stage with **no glue**. Current rung: **SelfhostLexerV2.tokenize → SelfhostDecl.parse_program → lower → SelfhostBeam.compile_forms → load**\n-(4 stages), over the a whole multi-function module whose ENTIRE front-end is verified ports — tokenized by selfhost_lexer_v2, parsed by selfhost_decl (def heads, multi-clause patterns incl. cons-LISTS, SUM-TYPE declarations + constructor dispatch, arithmetic + calls; `if`/strings not yet) — and compiled by the selfhost_beam backend; all THREE ports called CROSS-MODULE, with only a surface→Core lowering + Form inflater as driver glue. Rungs 1-6 built a driver owning the source→loaded-module loop over a TOY pipeline. Rung 7 made the BACKEND verified (`selfhost_beam`); rung 8 made body-parsing verified (`selfhost_parse`); rung 9 made declaration-parsing verified (`selfhost_decl`); rung 10 makes LEXING verified too (`selfhost_lexer_v2`), so the driver owns NO lexing or parsing — the whole front-end AND the back-end are equivalence-locked ports, composed cross-module. The lexer's token tags are a superset of the parser's (same tags), so `SelfhostLexerV2.tokenize` feeds `SelfhostDecl.parse_program` with NO projection. The only driver-local code left reimplements no stage: the surface→Core lowering (selfhost_decl's Decl/Expr/Pat IR → selfhost_beam Core/Pat, incl. cons-list patterns → PList) and the Form inflater. Rung 11 widens the SURFACE to sum types + constructor dispatch using selfhost_decl's already-locked type/ctor capability (no port change — only the driver glue grows: `type` decls are erased, and the Form inflater learns FCtorN/FCtor via a to_snake matching Rian.PatternLower). Surface is now selfhost_decl's slice (arithmetic + calls + multi-clause + cons-lists + sum-type declarations/dispatch; list recursion like sum/len AND ctor dispatch like Color/Shape work). All ports load under :\"Elixir.Selfhost*\" atoms (Pascal calls, ADR-0041); sibling-port calls are composition, not host crutches (excluded from the FFI ledger). Only host FFI: :compile.forms/:code.load_binary. The fixpoint calls only build/2 and runs the result identically to the full Elixir toolchain. The path to v1==v2: widen selfhost_decl off :partial (it lacks `if`/`case`/block bodies/strings/sum-types) and the surface→Core lowering to match, until build compiles a real selfhost_*.rian slice.\n+(4 stages), over the a whole multi-function module whose ENTIRE front-end is verified ports — tokenized by selfhost_lexer_v2, parsed by selfhost_decl (def heads, multi-clause patterns incl. cons-LISTS, SUM-TYPE declarations + constructor dispatch, `if`/comparison/boolean, arithmetic + calls; strings/`case` not yet) — and compiled by the selfhost_beam backend; all THREE ports called CROSS-MODULE, with only a surface→Core lowering + Form inflater as driver glue. Rungs 1-6 built a driver owning the source→loaded-module loop over a TOY pipeline. Rung 7 made the BACKEND verified (`selfhost_beam`); rung 8 made body-parsing verified (`selfhost_parse`); rung 9 made declaration-parsing verified (`selfhost_decl`); rung 10 makes LEXING verified too (`selfhost_lexer_v2`), so the driver owns NO lexing or parsing — the whole front-end AND the back-end are equivalence-locked ports, composed cross-module. The lexer's token tags are a superset of the parser's (same tags), so `SelfhostLexerV2.tokenize` feeds `SelfhostDecl.parse_program` with NO projection. The only driver-local code left reimplements no stage: the surface→Core lowering (selfhost_decl's Decl/Expr/Pat IR → selfhost_beam Core/Pat, incl. cons-list patterns → PList) and the Form inflater. Rung 11 widens the SURFACE to sum types + constructor dispatch using selfhost_decl's already-locked type/ctor capability (no port change — only the driver glue grows: `type` decls are erased, and the Form inflater learns FCtorN/FCtor via a to_snake matching Rian.PatternLower). Surface is now selfhost_decl's slice (arithmetic + calls + multi-clause + cons-lists + sum-type declarations/dispatch; list recursion like sum/len AND ctor dispatch like Color/Shape work). All ports load under :\"Elixir.Selfhost*\" atoms (Pascal calls, ADR-0041); sibling-port calls are composition, not host crutches (excluded from the FFI ledger). Only host FFI: :compile.forms/:code.load_binary. The fixpoint calls only build/2 and runs the result identically to the full Elixir toolchain. Rung 12 widens selfhost_decl ITSELF off its old :partial slice — `if … do … else … end` expressions now parse (equivalence-locked: decl_fixpoint diffs the projected IR against Rian.Decl.parse), and the build's lowering/inflater learn `if`→`case`, so the composed build compiles `if` end to end. The path to v1==v2: keep widening selfhost_decl (it still lacks `case`/strings/block bodies) and the surface→Core lowering to match, until build compiles a whole real selfhost_*.rian file.\n Source: `selfhost_compose_real_sum.rian`, fixpoint: `test/rian/compose_real_sum_fixpoint_test.exs`. The bootstrap\n terminus (Stage 3, v1==v2) is gated on this reaching the whole pipeline — not on the\n per-stage percentage.\n@@ -34,7 +34,7 @@ a whole real `selfhost_*.rian` file remains the work before v1==v2.\n | Stage | Role | Self-hosted | Evidence | Notes |\n | --- | --- | --- | --- | --- |\n | Lexer | frontend | ✅ yes | `examples/rian/selfhost_lexer_v2.rian` · `test/rian/fixpoint_test.exs` | token stream equals Rian.Lexer over slices 1-5 (incl. tokenize/1, {:nl}) |\n-| Declaration parser | frontend | 🟡 partial | `examples/rian/selfhost_decl.rian` · `test/rian/decl_fixpoint_test.exs` | IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042); alias/protocol/doc-comments remain |\n+| Declaration parser | frontend | 🟡 partial | `examples/rian/selfhost_decl.rian` · `test/rian/decl_fixpoint_test.exs` | IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042) and `if … do … else … end` expressions (single-expr branches → `{:if, c, {:block,…}, {:block,…}}`, ADR-0063); `case`/block-bodies/strings, alias/protocol/doc-comments remain |\n | Expression/pattern parser | frontend | ✅ yes | `examples/rian/selfhost_parse.rian` · `test/rian/parse_fixpoint_test.exs` | the full Rian.Pratt grammar — prefix/primary (if/case/with/list/map/tuple/lambda/capture/atom/str/char/num/id), postfix dot/call, labelled args, precedence climbing, AND the full pattern grammar + blocks — equals Rian.Pratt.parse with no projection; string-interpolation/`<-`-propagation sugar is out of scope; FFI-free — the `:if`/`:case`/`:with`/`:struct` keyword-atom tags are quoted-atom literals (`:\"if\"`) |\n | Typed Core IR (from_expr/from_pat) | frontend | ✅ yes | `examples/rian/selfhost_core.rian` · `test/rian/core_fixpoint_test.exs` | the full surface→Core lowering — every Pratt-produced expression (literals/unary/binary/call/label/dot/if/tuple/list/map/block/case/lambda/capture/with) and pattern (wild/var/lit/char/atom/tuple/ctor/list/struct/map) — equals Rian.Core.from_expr/from_pat; the Lower-internal resolved nodes + never-parsed as/pin patterns are not surface-reachable |\n | Type checker (inference + error sets) | checker | 🟡 partial | `examples/rian/selfhost_checker.rian` · `test/rian/checker_infer_fixpoint_test.exs` | type inference agrees with the REAL Rian.Check.infer over closed integer expressions; env/floats/calls/lambdas/case remain (selfhost_check.rian is a separate TOY-language spike) |","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/docs%2Fself-host-status.md","sha":"f520f2779687c0523c6ea3a7c574b3c2f7195cbf","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/examples%2Frian%2Fselfhost_compose_real_sum.rian","changes":12,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_compose_real_sum.rian?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":0,"filename":"examples/rian/selfhost_compose_real_sum.rian","patch":"@@ -150,6 +150,9 @@ mod SelfhostComposeRealSum do\n   def lower_surface({:bin, op, l, r}) := CBin(op, lower_surface(l), lower_surface(r))\n   def lower_surface({:unary, op, x}) := CUnary(op, lower_surface(x))\n   def lower_surface({:call, callee, args}) := CCall(callee_name(callee), lower_all(args))\n+  # selfhost_decl's `If(cond, then, else)` (single-expr branches) -> selfhost_beam's\n+  # CIf with single-statement blocks; the backend lowers it to a `case` on the bool.\n+  def lower_surface({:\"if\", c, t, e}) := CIf(lower_surface(c), Block([SExpr(lower_surface(t))]), Block([SExpr(lower_surface(e))]))\n \n   def callee_name(c) String\n   def callee_name({:id, f}) := f\n@@ -225,6 +228,8 @@ mod SelfhostComposeRealSum do\n   # constructor is a tagged tuple {tag, arg, …} — the BEAM rep of a sum value.\n   def inflate(FCtorN(c)) := {:atom, 0, to_snake(c)}\n   def inflate(FCtor(c, args)) := {:tuple, 0, [{:atom, 0, to_snake(c)} | inflate_all(args)]}\n+  # `if` lowers (via selfhost_beam's FIf) to a `case` on the boolean condition.\n+  def inflate(FIf(c, t, e)) := {:\"case\", 0, inflate(c), [{:clause, 0, [{:atom, 0, :true}], [], inflate_all(t)}, {:clause, 0, [{:atom, 0, :false}], [], inflate_all(e)}]}\n \n   def var_atom(n String) Symbol := Prim.str_to_atom(cap(n))\n \n@@ -274,6 +279,13 @@ mod SelfhostComposeRealSum do\n   def op_atom(\"+\") := :\"+\"\n   def op_atom(\"-\") := :\"-\"\n   def op_atom(\"*\") := :\"*\"\n+  def op_atom(\"==\") := :\"==\"\n+  def op_atom(\"<\") := :\"<\"\n+  def op_atom(\">\") := :\">\"\n+  def op_atom(\">=\") := :\">=\"\n+  def op_atom(\"<=\") := :\"=<\"\n+  def op_atom(\"and\") := :andalso\n+  def op_atom(\"or\") := :orelse\n \n   def cap(s String) String\n     case Prim.str_chars(s) do","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/examples%2Frian%2Fselfhost_compose_real_sum.rian","sha":"2f4ae1fbd76d2e8931deae86215c1f8390c7288d","status":"modified"},{"additions":30,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/examples%2Frian%2Fselfhost_decl.rian","changes":33,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_decl.rian?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":3,"filename":"examples/rian/selfhost_decl.rian","patch":"@@ -19,10 +19,11 @@\n #   capabilities: val / iso / ref / tag (default val); parametric types `Vec(T)`\n #   exprs: binops (precedence climbing) · prefix - / not · calls · `.field` access\n #          · labeled construction `Name(f: v)` · list construction `[x | acc]`\n+#          · `if COND do THEN else ELSE end` (single-expr branches; ADR-0063)\n #\n-# Out of slice (the remaining long tail of Rian.Decl): string/char clause patterns,\n-# negative-literal patterns, `alias`/`const`/`protocol`/`impl`,\n-# doc-comments, and block (`do … end`) function bodies.\n+# Out of slice (the remaining long tail of Rian.Decl): `case`, string/char clause\n+# patterns, negative-literal patterns, `alias`/`const`/`protocol`/`impl`,\n+# doc-comments, and multi-statement block (`do … end`) function bodies.\n # ---------------------------------------------------------------------------\n \n mod SelfhostDecl do\n@@ -37,6 +38,7 @@ mod SelfhostDecl do\n       Num(String) | Id(String) | Bin(String, Expr, Expr)\n     | Unary(String, Expr) | Call(Expr, Vec(Expr))\n     | Dot(Expr, String) | Label(String, Expr)\n+    | If(Expr, Expr, Expr)\n     | NilE | ConsE(Expr, Expr) | EErr\n \n   # clause PATTERNS — Wild/Var/Lit/Ctor lower straight to Rian.Decl's tuples\n@@ -412,6 +414,7 @@ mod SelfhostDecl do\n   def parse_primary([TNum(n) | rest]) := parse_postfix(Num(n), rest)\n   def parse_primary([TId(x) | rest]) := parse_postfix(Id(x), rest)\n   def parse_primary([TLBracket | rest]) := parse_list_expr(rest)\n+  def parse_primary([TKw(\"if\") | rest]) := parse_if(rest)\n   def parse_primary([TLP | rest])\n     case parse_bp(0, rest) do\n       P(e, [TRP | rest2]) -> parse_postfix(e, rest2)\n@@ -420,6 +423,30 @@ mod SelfhostDecl do\n   end\n   def parse_primary(ts) := P(EErr, ts)\n \n+  # `if COND do THEN else ELSE end` — each part is a full sub-expression. The\n+  # projection (decl_fixpoint) wraps THEN/ELSE in `{:block, [{:expr, …}]}` to match\n+  # Rian.Pratt's `{:if, c, {:block, …}, {:block, …}}`. Single-expression branches.\n+  def parse_if(ts Vec(Tok)) Parse\n+  def parse_if(ts)\n+    case parse_bp(0, ts) do\n+      P(cnd, [TKw(\"do\") | r2]) -> parse_if_then(cnd, r2)\n+    end\n+  end\n+\n+  def parse_if_then(cnd Expr, ts Vec(Tok)) Parse\n+  def parse_if_then(cnd, ts)\n+    case parse_bp(0, ts) do\n+      P(thn, [TKw(\"else\") | r3]) -> parse_if_else(cnd, thn, r3)\n+    end\n+  end\n+\n+  def parse_if_else(cnd Expr, thn Expr, ts Vec(Tok)) Parse\n+  def parse_if_else(cnd, thn, ts)\n+    case parse_bp(0, ts) do\n+      P(els, [TKw(\"end\") | r4]) -> parse_postfix(If(cnd, thn, els), r4)\n+    end\n+  end\n+\n   # list construction as a nil/cons chain (mirrors parse_list_pat): `[x | acc]` ->\n   # ConsE(x, acc); elements are full expressions.\n   def parse_list_expr(ts Vec(Tok)) Parse","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/examples%2Frian%2Fselfhost_decl.rian","sha":"1d2a4d48bd36eca33e9a6b4e0c31badc8daabec8","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/lib%2Frian%2Fself_host.ex","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fself_host.ex?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":3,"filename":"lib/rian/self_host.ex","patch":"@@ -53,7 +53,7 @@ defmodule Rian.SelfHost do\n       source: \"selfhost_decl.rian\",\n       test: \"test/rian/decl_fixpoint_test.exs\",\n       note:\n-        \"IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042); alias/protocol/doc-comments remain\"\n+        \"IR equals Rian.Decl over type/struct/mod/def slice incl. `forall` generics (tvars/bounds, ADR-0042) and `if … do … else … end` expressions (single-expr branches → `{:if, c, {:block,…}, {:block,…}}`, ADR-0063); `case`/block-bodies/strings, alias/protocol/doc-comments remain\"\n     },\n     %{\n       id: :expr_parser,\n@@ -161,7 +161,7 @@ defmodule Rian.SelfHost do\n       \"SelfhostLexerV2.tokenize → SelfhostDecl.parse_program → lower → SelfhostBeam.compile_forms → load\",\n     stages: 4,\n     subset:\n-      \"a whole multi-function module whose ENTIRE front-end is verified ports — tokenized by selfhost_lexer_v2, parsed by selfhost_decl (def heads, multi-clause patterns incl. cons-LISTS, SUM-TYPE declarations + constructor dispatch, arithmetic + calls; `if`/strings not yet) — and compiled by the selfhost_beam backend; all THREE ports called CROSS-MODULE, with only a surface→Core lowering + Form inflater as driver glue\",\n+      \"a whole multi-function module whose ENTIRE front-end is verified ports — tokenized by selfhost_lexer_v2, parsed by selfhost_decl (def heads, multi-clause patterns incl. cons-LISTS, SUM-TYPE declarations + constructor dispatch, `if`/comparison/boolean, arithmetic + calls; strings/`case` not yet) — and compiled by the selfhost_beam backend; all THREE ports called CROSS-MODULE, with only a surface→Core lowering + Form inflater as driver glue\",\n     source: \"selfhost_compose_real_sum.rian\",\n     test: \"test/rian/compose_real_sum_fixpoint_test.exs\",\n     # honesty distinction (ADR-0063): the composed loop is self-COMPILING (codegen —\n@@ -177,7 +177,7 @@ defmodule Rian.SelfHost do\n     closed_on_real_source: \"selfhost_cap.rian (Ty + copyt)\",\n     real_source_test: \"test/rian/compose_selfcompile_fixpoint_test.exs\",\n     note:\n-      \"Rungs 1-6 built a driver owning the source→loaded-module loop over a TOY pipeline. Rung 7 made the BACKEND verified (`selfhost_beam`); rung 8 made body-parsing verified (`selfhost_parse`); rung 9 made declaration-parsing verified (`selfhost_decl`); rung 10 makes LEXING verified too (`selfhost_lexer_v2`), so the driver owns NO lexing or parsing — the whole front-end AND the back-end are equivalence-locked ports, composed cross-module. The lexer's token tags are a superset of the parser's (same tags), so `SelfhostLexerV2.tokenize` feeds `SelfhostDecl.parse_program` with NO projection. The only driver-local code left reimplements no stage: the surface→Core lowering (selfhost_decl's Decl/Expr/Pat IR → selfhost_beam Core/Pat, incl. cons-list patterns → PList) and the Form inflater. Rung 11 widens the SURFACE to sum types + constructor dispatch using selfhost_decl's already-locked type/ctor capability (no port change — only the driver glue grows: `type` decls are erased, and the Form inflater learns FCtorN/FCtor via a to_snake matching Rian.PatternLower). Surface is now selfhost_decl's slice (arithmetic + calls + multi-clause + cons-lists + sum-type declarations/dispatch; list recursion like sum/len AND ctor dispatch like Color/Shape work). All ports load under :\\\"Elixir.Selfhost*\\\" atoms (Pascal calls, ADR-0041); sibling-port calls are composition, not host crutches (excluded from the FFI ledger). Only host FFI: :compile.forms/:code.load_binary. The fixpoint calls only build/2 and runs the result identically to the full Elixir toolchain. The path to v1==v2: widen selfhost_decl off :partial (it lacks `if`/`case`/block bodies/strings/sum-types) and the surface→Core lowering to match, until build compiles a real selfhost_*.rian slice\"\n+      \"Rungs 1-6 built a driver owning the source→loaded-module loop over a TOY pipeline. Rung 7 made the BACKEND verified (`selfhost_beam`); rung 8 made body-parsing verified (`selfhost_parse`); rung 9 made declaration-parsing verified (`selfhost_decl`); rung 10 makes LEXING verified too (`selfhost_lexer_v2`), so the driver owns NO lexing or parsing — the whole front-end AND the back-end are equivalence-locked ports, composed cross-module. The lexer's token tags are a superset of the parser's (same tags), so `SelfhostLexerV2.tokenize` feeds `SelfhostDecl.parse_program` with NO projection. The only driver-local code left reimplements no stage: the surface→Core lowering (selfhost_decl's Decl/Expr/Pat IR → selfhost_beam Core/Pat, incl. cons-list patterns → PList) and the Form inflater. Rung 11 widens the SURFACE to sum types + constructor dispatch using selfhost_decl's already-locked type/ctor capability (no port change — only the driver glue grows: `type` decls are erased, and the Form inflater learns FCtorN/FCtor via a to_snake matching Rian.PatternLower). Surface is now selfhost_decl's slice (arithmetic + calls + multi-clause + cons-lists + sum-type declarations/dispatch; list recursion like sum/len AND ctor dispatch like Color/Shape work). All ports load under :\\\"Elixir.Selfhost*\\\" atoms (Pascal calls, ADR-0041); sibling-port calls are composition, not host crutches (excluded from the FFI ledger). Only host FFI: :compile.forms/:code.load_binary. The fixpoint calls only build/2 and runs the result identically to the full Elixir toolchain. Rung 12 widens selfhost_decl ITSELF off its old :partial slice — `if … do … else … end` expressions now parse (equivalence-locked: decl_fixpoint diffs the projected IR against Rian.Decl.parse), and the build's lowering/inflater learn `if`→`case`, so the composed build compiles `if` end to end. The path to v1==v2: keep widening selfhost_decl (it still lacks `case`/strings/block bodies) and the surface→Core lowering to match, until build compiles a whole real selfhost_*.rian file\"\n   }\n \n   @doc \"\"\"","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/lib%2Frian%2Fself_host.ex","sha":"70a243663eb8ec725561c1f13450642317978f42","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/test%2Frian%2Fcompose_real_sum_fixpoint_test.exs","changes":12,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcompose_real_sum_fixpoint_test.exs?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":0,"filename":"test/rian/compose_real_sum_fixpoint_test.exs","patch":"@@ -59,6 +59,18 @@ defmodule Rian.ComposeRealSumFixpointTest do\n       \"type Box := B(Int53)\\ndef wrap(n) := B(n)\\ndef unwrap(B(n)) := n\",\n       \"type Box := B(Int53)\\ndef wrap(n Int53) Box := B(n)\\ndef unwrap(b Box) Int53\\ndef unwrap(B(n)) := n\",\n       [{:wrap, [7]}, {:wrap, [42]}, {:unwrap, [{:b, 9}]}]\n+    },\n+    # `if` — now in selfhost_decl's surface (ADR-0063 widening); compiles end to end\n+    {\n+      \"def max(a, b) := if a > b do a else b end\\ndef min(a, b) := if a < b do a else b end\",\n+      \"def max(a Int53, b Int53) Int53 := if a > b do a else b end\\ndef min(a Int53, b Int53) Int53 := if a < b do a else b end\",\n+      [{:max, [3, 7]}, {:max, [9, 2]}, {:min, [3, 7]}]\n+    },\n+    # `if`-guarded recursion + sum-type constructor comparison in one program\n+    {\n+      \"type Color := Red | Green\\ndef code(c) := if c == Red do 0 else 1 end\",\n+      \"type Color := Red | Green\\ndef code(c Color) Int53 := if c == Red do 0 else 1 end\",\n+      [{:code, [:red]}, {:code, [:green]}]\n     }\n   ]\n ","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/test%2Frian%2Fcompose_real_sum_fixpoint_test.exs","sha":"7f48985b42cab846964f1da4bbfee43e426b8556","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/rian-lab/blob/909dfde1b81d7313bcb09b1499bbdef3b492b160/test%2Frian%2Fdecl_fixpoint_test.exs","changes":14,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_fixpoint_test.exs?ref=909dfde1b81d7313bcb09b1499bbdef3b492b160","deletions":0,"filename":"test/rian/decl_fixpoint_test.exs","patch":"@@ -94,6 +94,11 @@ defmodule Rian.DeclFixpointTest do\n   defp ce({:call, f, args}), do: {:call, ce(f), Enum.map(args, &ce/1)}\n   defp ce({:dot, e, field}), do: {:dot, ce(e), field}\n   defp ce({:label, name, v}), do: {:label, name, ce(v)}\n+  # the front-end's `If(cond, then, else)` (single-expr branches) projects onto\n+  # Rian.Pratt's `{:if, c, {:block, [{:expr, t}]}, {:block, [{:expr, e}]}}`.\n+  defp ce({:if, c, t, e}),\n+    do: {:if, ce(c), {:block, [{:expr, ce(t)}]}, {:block, [{:expr, ce(e)}]}}\n+\n   defp ce(leaf), do: leaf\n \n   defp flat_e(:nil_e), do: {[], nil}\n@@ -229,6 +234,15 @@ defmodule Rian.DeclFixpointTest do\n     \"def len(xs val Vec(Int64)) Int64\\n\" <>\n       \"def len([]) := 0\\n\" <>\n       \"def len([_ | t]) := 1 + len(t)\",\n+    # `if … do … else … end` expressions (single-expr branches) — ADR-0063 widening\n+    \"def max(a Int64, b Int64) Int64 := if a > b do a else b end\",\n+    \"def clampv(x Int64) Int64 := if x > 10 do 10 else x end\",\n+    # `if` in a multi-clause body, with a nested arithmetic condition + recursion\n+    \"def sign(n Int64) Int64\\n\" <>\n+      \"def sign(0) := 0\\n\" <>\n+      \"def sign(n) := if n > 0 do 1 else 0 end\",\n+    # `if` whose branches are themselves compound expressions (calls / arithmetic)\n+    \"def pickf(a Int64, b Int64) Int64 := if a == b do a + b else a - b end\",\n     # `when` guards\n     \"def clamp(n Int64) Int64\\n\" <>\n       \"def clamp(n) when n < 0 := 0\\n\" <>","raw_url":"https://github.com/wstein/rian-lab/raw/909dfde1b81d7313bcb09b1499bbdef3b492b160/test%2Frian%2Fdecl_fixpoint_test.exs","sha":"e299cd1d125813e6b123bf89b85ca342cf0dc314","status":"modified"}]}