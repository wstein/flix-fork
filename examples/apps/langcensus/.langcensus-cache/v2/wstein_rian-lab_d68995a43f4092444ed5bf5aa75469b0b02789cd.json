{"files":[{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/d68995a43f4092444ed5bf5aa75469b0b02789cd/lib%2Frian%2Ftranspile.ex","changes":16,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Ftranspile.ex?ref=d68995a43f4092444ed5bf5aa75469b0b02789cd","deletions":0,"filename":"lib/rian/transpile.ex","patch":"@@ -1253,6 +1253,14 @@ defmodule Rian.Transpile do\n   defp expr({:not, _, [x]}), do: \"not #{paren_unary(x)}\"\n   defp expr({:!, _, [x]}), do: \"not #{paren_unary(x)}\"\n \n+  # Elixir `raise` → Rian `panic` (ADR-0035/0040): the diverging, uncatchable abort.\n+  # `raise Mod, msg` / `raise msg` panic with the message; bare `raise Mod` with the\n+  # error name. This emits valid Rian (vs the invalid bare `raise(…)`); an *expected*\n+  # error (one a caller should recover) is then restructured to a `Result` by hand —\n+  # the same draft-finishing the transpiler always requires.\n+  defp expr({:raise, _, args}), do: \"panic(#{raise_msg(args)})\"\n+  defp expr({:reraise, _, args}), do: \"panic(#{raise_msg(args)})\"\n+\n   defp expr({:if, _, [c, kw]}) do\n     t = render_body(Keyword.get(kw, :do))\n     e = if Keyword.has_key?(kw, :else), do: render_body(Keyword.get(kw, :else)), else: nil\n@@ -1412,6 +1420,14 @@ defmodule Rian.Transpile do\n \n   defp expr(other), do: ~s|TODO_PORT(#{inspect(snippet(other))})|\n \n+  # the panic message for an Elixir `raise`: `raise Mod, msg` / `raise msg` use the\n+  # message expression; a bare `raise Mod` uses the error name; a no-arg raise the\n+  # literal \"panic\".\n+  defp raise_msg([{:__aliases__, _, parts}]), do: expr(parts |> List.last() |> to_string())\n+  defp raise_msg([_mod, msg | _]), do: expr(msg)\n+  defp raise_msg([msg]), do: expr(msg)\n+  defp raise_msg(_), do: expr(\"panic\")\n+\n   # Render an operand of the Rian operator `parent_op`, wrapping it in parens iff\n   # omitting them would change the Rian re-parse. Using levels where *lower binds\n   # tighter*, the operand needs parens when it binds looser than the parent, or binds","raw_url":"https://github.com/wstein/rian-lab/raw/d68995a43f4092444ed5bf5aa75469b0b02789cd/lib%2Frian%2Ftranspile.ex","sha":"bc1615d171fb03a6710e142797cabda734eb5d90","status":"modified"},{"additions":19,"blob_url":"https://github.com/wstein/rian-lab/blob/d68995a43f4092444ed5bf5aa75469b0b02789cd/test%2Frian%2Ftranspile_test.exs","changes":19,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ftranspile_test.exs?ref=d68995a43f4092444ed5bf5aa75469b0b02789cd","deletions":0,"filename":"test/rian/transpile_test.exs","patch":"@@ -738,6 +738,25 @@ end|) =~ ~S|\"v=${x}!\"|\n       refute out =~ \"a ++ b\"\n     end\n \n+    test \"Elixir `raise` → Rian `panic` (the diverging abort, ADR-0035/0040)\" do\n+      out =\n+        rian(\"\"\"\n+        defmodule M do\n+          def a, do: raise(ArgumentError, \"bad input\")\n+          def b, do: raise(\"boom\")\n+          def c, do: raise(SomeError)\n+        end\n+        \"\"\")\n+\n+      assert out =~ ~s|:= panic(\"bad input\")|\n+      assert out =~ ~s|:= panic(\"boom\")|\n+      # a bare `raise Mod` panics with the error name\n+      assert out =~ ~s|:= panic(\"SomeError\")|\n+      # never the invalid bare `raise(...)` (no such Rian builtin)\n+      body = out |> String.split(\"mod M do\") |> List.last()\n+      refute body =~ \"raise(\"\n+    end\n+\n     test \"truthy `&&`/`||` become a TODO_PORT marker (no faithful Rian image)\" do\n       # Rian's `and`/`or` are boolean (they lower to native `&&`/`||`), so they are\n       # NOT a sound port of Elixir's value-returning, nil-coalescing `&&`/`||`; the","raw_url":"https://github.com/wstein/rian-lab/raw/d68995a43f4092444ed5bf5aa75469b0b02789cd/test%2Frian%2Ftranspile_test.exs","sha":"7483037d5232d377ea2923695ea57e8847db09e1","status":"modified"}]}