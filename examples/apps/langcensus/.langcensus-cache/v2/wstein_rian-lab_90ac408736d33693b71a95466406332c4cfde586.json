{"files":[{"additions":111,"blob_url":"https://github.com/wstein/rian-lab/blob/90ac408736d33693b71a95466406332c4cfde586/examples%2Frian%2Fselfhost_checker.rian","changes":184,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_checker.rian?ref=90ac408736d33693b71a95466406332c4cfde586","deletions":73,"filename":"examples/rian/selfhost_checker.rian","patch":"@@ -30,16 +30,17 @@\n # flow-NARROWED by the arm pattern (a variable binds the scrutinee's type). A list\n # literal infers `Vec(T)` when its elements (and any cons tail) LUB-join to a concrete\n # `T`. A lambda infers its arrow type `Fn(arg.., body)` (an unannotated param is `_`),\n-# the body inferred under the param bindings. Covered Core nodes (12, FULL): CNum/CStr/\n-# CChar/CId/CUnary/CBin/CCall/CIf/CBlock/CCase/CList/CLambda. The `:partial` tail is now\n-# a general call resolves a function-typed callee to its return type (higher-order:\n-# `g(5)` with `g : Fn(Int64,Int64)` -> `Int64`). The port now AGREES WITH\n-# `Rian.Check.infer` over the whole fixpoint domain — every expression node + a typing\n-# env, with an empty inference context. The remaining gap needs a NON-empty `ic`\n-# (named-function/constructor return types, constructor-pattern field narrowing); the\n-# fixpoint supplies no `ic`, so exercising it means threading an `ic` through both the\n-# port and the test. The join LUB is complete (numeric widening + the parametric\n-# covariant join). See ADR-0063 / docs/self-host-status.md.\n+# the body inferred under the param bindings. A general call resolves a function-typed\n+# env var (higher-order: `g(5)` with `g : Fn(Int64,Int64)` -> `Int64`), an applied\n+# constructor (`ic.ctors`), or a non-generic named function's return (`ic.funs`); a\n+# bare identifier resolves a nullary constructor's sum type (`ic.ctors`) when not a\n+# bound variable. Covered Core nodes (12, FULL): CNum/CStr/CChar/CId/CUnary/CBin/CCall/\n+# CIf/CBlock/CCase/CList/CLambda. Both a typing env AND an inference context `ic`\n+# (`:ctors`/`:funs`) are threaded through (`infer_ic/3`; `infer_env/2` and `infer/1`\n+# are empty-context conveniences). The join LUB is complete (numeric widening + the\n+# parametric covariant join). The remaining tail: GENERIC function-return\n+# instantiation (`ic.fsigs` with type variables, ADR-0042) and constructor-pattern\n+# field narrowing (`ic.tdefs`). See ADR-0063 / docs/self-host-status.md.\n # ---------------------------------------------------------------------------\n \n mod SelfhostChecker do\n@@ -207,77 +208,114 @@ mod SelfhostChecker do\n \n   def conservative(t String) String := if t == \"mismatch\" do \"unknown\" else t end\n \n-  # Infer the type of a Core node in the empty env (closed expression).\n+  # Infer the type of a Core node in the empty env + empty inference context.\n   pub def infer(Core) String\n-  pub def infer(c) := infer_env(c, Prim.map_new())\n+  pub def infer(c) := infer_ic(c, Prim.map_new(), Prim.map_new())\n \n-  # Infer under a typing env (name -> type), threading it through sub-expressions —\n-  # a bound identifier resolves to its env type (ADR-0042; mirrors Rian.Check's\n-  # `Map.get(env, x)`), else `true`/`false` are `Bool`, else `unknown`.\n+  # Infer under a typing env only (empty inference context).\n   pub def infer_env(Core, Dict(String, String)) String\n-  pub def infer_env(CNum(t), _) := num_ty(t)\n-  pub def infer_env(CStr(_), _) := \"String\"\n-  pub def infer_env(CChar(_), _) := \"Char\"\n-  pub def infer_env(CId(\"true\"), _) := \"Bool\"\n-  pub def infer_env(CId(\"false\"), _) := \"Bool\"\n-  pub def infer_env(CId(x), env) := lookup(env, x)\n-  pub def infer_env(CUnary(\"not\", _), _) := \"Bool\"\n-  pub def infer_env(CUnary(\"-\", x), env) := infer_env(x, env)\n-  # arithmetic (`+ - * div rem`) is operand-directed (arith_type); every other op's\n-  # result is fixed by the op (comparisons/`and`/`or`/`in` → Bool, `<>` → String,\n-  # `/` → Float64) via bin_ty.\n-  pub def infer_env(CBin(op, l, r), env)\n+  pub def infer_env(c, env) := infer_ic(c, env, Prim.map_new())\n+\n+  # Infer under a typing env AND an inference context `ic` (a map with `:ctors`\n+  # name->type and `:funs` name->return-type). A bound id resolves to its env type,\n+  # else a nullary constructor's sum type (`ic.ctors`), else `unknown`. A call\n+  # resolves a function-typed env var (higher-order), else an applied constructor\n+  # (`ic.ctors`), else a non-generic named function's return (`ic.funs`). (Generic-\n+  # return instantiation and constructor-pattern field narrowing — needing `ic.fsigs`\n+  # / `ic.tdefs` — are the remaining tail.)\n+  pub def infer_ic(Core, Dict(String, String), Dict(Symbol, Dict(String, String))) String\n+  pub def infer_ic(CNum(t), _, _) := num_ty(t)\n+  pub def infer_ic(CStr(_), _, _) := \"String\"\n+  pub def infer_ic(CChar(_), _, _) := \"Char\"\n+  pub def infer_ic(CId(\"true\"), _, _) := \"Bool\"\n+  pub def infer_ic(CId(\"false\"), _, _) := \"Bool\"\n+  pub def infer_ic(CId(x), env, ic) := id_ty(x, env, ic)\n+  pub def infer_ic(CUnary(\"not\", _), _, _) := \"Bool\"\n+  pub def infer_ic(CUnary(\"-\", x), env, ic) := infer_ic(x, env, ic)\n+  pub def infer_ic(CBin(op, l, r), env, ic)\n     if arith_op(op) do\n-      arith_type(l, r, infer_env(l, env), infer_env(r, env))\n+      arith_type(l, r, infer_ic(l, env, ic), infer_ic(r, env, ic))\n     else\n       bin_ty(op)\n     end\n   end\n-  # primitive intrinsics with a determinate result type (ADR-0036/0047/0069 — mirrors\n-  # Rian.Check's `__prim_*` clauses). A general call has no determinate type without\n-  # function/ctor return types in the env, so it is conservatively `unknown`.\n-  pub def infer_env(CCall(\"__prim_char_code\", [_]), _) := \"Int53\"\n-  pub def infer_env(CCall(\"__prim_int_to_float\", [_]), _) := \"Float64\"\n-  pub def infer_env(CCall(\"__prim_str_to_atom\", [_]), _) := \"Symbol\"\n-  pub def infer_env(CCall(\"__prim_char_to_string\", [_]), _) := \"String\"\n-  pub def infer_env(CCall(\"__prim_str_concat_all\", _), _) := \"String\"\n-  # a general call: if the callee NAME is bound to a function type in the env (a\n-  # higher-order value), the call's type is that function's return type; else\n-  # `unknown` (a named function / constructor return needs the inference context `ic`,\n-  # which this fixpoint does not supply — Rian.Check with an empty `ic` agrees).\n-  pub def infer_env(CCall(f, _), env) := call_ret(lookup(env, f))\n-  # `if` — the LUB-join of the two branches (each a single-expression block); a\n-  # block's type is its last expression's, binds extending the env (ADR-0059).\n-  pub def infer_env(CIf(_, t, e), env) := branch_join([BP(t, infer_env(t, env)), BP(e, infer_env(e, env))])\n-  pub def infer_env(CBlock(stmts), env) := infer_block(stmts, env)\n-  # `case` — each arm body is inferred under the env NARROWED by the arm pattern\n-  # against the scrutinee's type (a variable pattern binds it); the case's type is the\n-  # LUB-join of the arm bodies (Rian.Check; ADR-0059).\n-  pub def infer_env(CCase(scrut, arms), env) := branch_join(arm_pairs(arms, infer_env(scrut, env), env))\n-  # a list literal infers `Vec(T)` when its elements — and any cons tail — LUB-join to\n-  # a concrete element type `T`; else `unknown` (Rian.Check; ADR-0059).\n-  pub def infer_env(CList(elems, tail), env) := list_ty(join_all(infer_all(elems, env)), list_elem(infer_tail(tail, env)))\n-  # a lambda infers the arrow type `Fn(a_t.., body_t)` — each annotated param\n-  # contributes its type (an unannotated one is `_`), and the body is inferred under\n-  # those bindings (Rian.Check; ADR-0042).\n-  pub def infer_env(CLambda(params, body), env) := build_fn(param_types(params), infer_env(body, extend_lenv(params, env)))\n-\n-  # a bound name's type from the env, else `unknown` (the checker does not guess).\n-  def lookup(env Dict(String, String), x String) String\n+  pub def infer_ic(CCall(\"__prim_char_code\", [_]), _, _) := \"Int53\"\n+  pub def infer_ic(CCall(\"__prim_int_to_float\", [_]), _, _) := \"Float64\"\n+  pub def infer_ic(CCall(\"__prim_str_to_atom\", [_]), _, _) := \"Symbol\"\n+  pub def infer_ic(CCall(\"__prim_char_to_string\", [_]), _, _) := \"String\"\n+  pub def infer_ic(CCall(\"__prim_str_concat_all\", _), _, _) := \"String\"\n+  pub def infer_ic(CCall(f, _), env, ic) := call_ty(f, env, ic)\n+  pub def infer_ic(CIf(_, t, e), env, ic) := branch_join([BP(t, infer_ic(t, env, ic)), BP(e, infer_ic(e, env, ic))])\n+  pub def infer_ic(CBlock(stmts), env, ic) := infer_block(stmts, env, ic)\n+  pub def infer_ic(CCase(scrut, arms), env, ic) := branch_join(arm_pairs(arms, infer_ic(scrut, env, ic), env, ic))\n+  pub def infer_ic(CList(elems, tail), env, ic) := list_ty(join_all(infer_all(elems, env, ic)), list_elem(infer_tail(tail, env, ic)))\n+  pub def infer_ic(CLambda(params, body), env, ic) := build_fn(param_types(params), infer_ic(body, extend_lenv(params, env), ic))\n+\n+  # a bare identifier: its env type if bound, else a nullary constructor's sum type\n+  # (`ic.ctors`), else `unknown` (Rian.Check's `Map.get(env,x) || ctor_type(ic,x)`).\n+  def id_ty(x String, env Dict(String, String), ic Dict(Symbol, Dict(String, String))) String\n+  def id_ty(x, env, ic)\n     if Prim.map_has(env, x) do\n       Prim.map_get(env, x)\n+    else\n+      ctor_type(ic, x)\n+    end\n+  end\n+\n+  # a call's result: a function-typed env var yields its return type; else an applied\n+  # constructor's sum type (`ic.ctors`); else a non-generic named function's return\n+  # (`ic.funs`); else `unknown`.\n+  def call_ty(f String, env Dict(String, String), ic Dict(Symbol, Dict(String, String))) String\n+  def call_ty(f, env, ic)\n+    if Prim.map_has(env, f) do\n+      call_ret(Prim.map_get(env, f))\n+    else\n+      ctor_or_fn(f, ic)\n+    end\n+  end\n+\n+  def ctor_or_fn(f String, ic Dict(Symbol, Dict(String, String))) String\n+  def ctor_or_fn(f, ic)\n+    case ctor_type(ic, f) do\n+      \"unknown\" -> called_ret(ic, f)\n+      ty -> ty\n+    end\n+  end\n+\n+  # `ic.ctors[name]` (a nullary or applied constructor's sum type), else `unknown`.\n+  def ctor_type(ic Dict(Symbol, Dict(String, String)), name String) String := ic_lookup(ic, :ctors, name)\n+\n+  # a non-generic named function's declared return type (`ic.funs[f]`), else\n+  # `unknown`. (A return that is itself a type variable, or a generic `ic.fsigs`\n+  # signature needing instantiation, is the deferred tail.)\n+  def called_ret(ic Dict(Symbol, Dict(String, String)), f String) String := ic_lookup(ic, :funs, f)\n+\n+  # look `name` up in the `key` sub-map of `ic`; `unknown` if either is absent.\n+  def ic_lookup(ic Dict(Symbol, Dict(String, String)), key Symbol, name String) String\n+  def ic_lookup(ic, key, name)\n+    if Prim.map_has(ic, key) do\n+      sub_lookup(Prim.map_get(ic, key), name)\n+    else\n+      \"unknown\"\n+    end\n+  end\n+\n+  def sub_lookup(m Dict(String, String), name String) String\n+  def sub_lookup(m, name)\n+    if Prim.map_has(m, name) do\n+      Prim.map_get(m, name)\n     else\n       \"unknown\"\n     end\n   end\n \n   # a block's type is its LAST expression's; a `name := e` bind extends the env for\n   # the rest (Rian.Check.infer_block). An empty block is `unknown`.\n-  def infer_block(Vec(Stmt), Dict(String, String)) String\n-  def infer_block([], _) := \"unknown\"\n-  def infer_block([SExpr(e)], env) := infer_env(e, env)\n-  def infer_block([SExpr(_) | rest], env) := infer_block(rest, env)\n-  def infer_block([SBind(n, e) | rest], env) := infer_block(rest, Prim.map_put(env, n, infer_env(e, env)))\n+  def infer_block(Vec(Stmt), Dict(String, String), Dict(Symbol, Dict(String, String))) String\n+  def infer_block([], _, _) := \"unknown\"\n+  def infer_block([SExpr(e)], env, ic) := infer_ic(e, env, ic)\n+  def infer_block([SExpr(_) | rest], env, ic) := infer_block(rest, env, ic)\n+  def infer_block([SBind(n, e) | rest], env, ic) := infer_block(rest, Prim.map_put(env, n, infer_ic(e, env, ic)), ic)\n \n   # LUB-join of branch/arm types (ADR-0059). An integer-LITERAL branch is\n   # width-flexible: when at least one branch is a non-literal, the literals adopt the\n@@ -569,9 +607,9 @@ mod SelfhostChecker do\n   def parse_digits([c | rest], acc) := parse_digits(rest, acc * 10 + c - 48)\n \n   # each arm -> a (body, type) pair, the body inferred under the narrowed env.\n-  def arm_pairs(Vec(Arm), String, Dict(String, String)) Vec(BPair)\n-  def arm_pairs([], _, _) := []\n-  def arm_pairs([CArm(pat, body) | rest], st, env) := [BP(body, infer_env(body, narrow(pat, st, env))) | arm_pairs(rest, st, env)]\n+  def arm_pairs(Vec(Arm), String, Dict(String, String), Dict(Symbol, Dict(String, String))) Vec(BPair)\n+  def arm_pairs([], _, _, _) := []\n+  def arm_pairs([CArm(pat, body) | rest], st, env, ic) := [BP(body, infer_ic(body, narrow(pat, st, env), ic)) | arm_pairs(rest, st, env, ic)]\n \n   # flow narrowing (Rian.Check.narrow): a VARIABLE pattern binds the scrutinee's\n   # (concretized) type into the env; every other pattern leaves it unchanged.\n@@ -597,13 +635,13 @@ mod SelfhostChecker do\n   def tvar(_) := false\n \n   # ── list inference helpers (Rian.Check) ──\n-  def infer_all(Vec(Core), Dict(String, String)) Vec(String)\n-  def infer_all([], _) := []\n-  def infer_all([e | rest], env) := [infer_env(e, env) | infer_all(rest, env)]\n+  def infer_all(Vec(Core), Dict(String, String), Dict(Symbol, Dict(String, String))) Vec(String)\n+  def infer_all([], _, _) := []\n+  def infer_all([e | rest], env, ic) := [infer_ic(e, env, ic) | infer_all(rest, env, ic)]\n \n-  def infer_tail(Tail, Dict(String, String)) String\n-  def infer_tail(TClose, _) := \"unknown\"\n-  def infer_tail(TCons(e), env) := infer_env(e, env)\n+  def infer_tail(Tail, Dict(String, String), Dict(Symbol, Dict(String, String))) String\n+  def infer_tail(TClose, _, _) := \"unknown\"\n+  def infer_tail(TCons(e), env, ic) := infer_ic(e, env, ic)\n \n   # the element type the list type carries: `Vec(T)` -> `T`; anything else -> unknown.\n   def list_elem(t String) String := le(Prim.str_chars(t))","raw_url":"https://github.com/wstein/rian-lab/raw/90ac408736d33693b71a95466406332c4cfde586/examples%2Frian%2Fselfhost_checker.rian","sha":"e825d8a024cf467dcaa7ed55235dad75678a2276","status":"modified"},{"additions":53,"blob_url":"https://github.com/wstein/rian-lab/blob/90ac408736d33693b71a95466406332c4cfde586/test%2Frian%2Fchecker_infer_fixpoint_test.exs","changes":53,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fchecker_infer_fixpoint_test.exs?ref=90ac408736d33693b71a95466406332c4cfde586","deletions":0,"filename":"test/rian/checker_infer_fixpoint_test.exs","patch":"@@ -69,6 +69,15 @@ defmodule Rian.CheckerInferFixpointTest do\n     end\n   end\n \n+  defp ported_ic(mod, src, env, ic), do: mod.infer_ic(inj(Core.from_expr(Pratt.parse(src))), env, ic)\n+\n+  defp reference_ic(src, env, ic) do\n+    case Check.infer(Pratt.parse(src), env, ic) do\n+      :unknown -> \"unknown\"\n+      ty -> ty\n+    end\n+  end\n+\n   @corpus [\n     \"1\",\n     \"3.14\",\n@@ -245,6 +254,50 @@ end|,\n     end\n   end\n \n+  # an inference context `ic`: constructor names -> their sum type (`:ctors`), and\n+  # non-generic function names -> their return type (`:funs`). The env still takes\n+  # precedence over a constructor of the same name.\n+  @ic %{\n+    ctors: %{\n+      \"Red\" => \"Color\",\n+      \"Green\" => \"Color\",\n+      \"RGB\" => \"Color\",\n+      \"None\" => \"Opt\",\n+      \"Some\" => \"Opt\"\n+    },\n+    funs: %{\"area\" => \"Int64\", \"name_of\" => \"String\", \"mkvec\" => \"Vec(Int8)\"}\n+  }\n+  @ic_corpus [\n+    # a nullary constructor resolves to its sum type\n+    {\"Red\", \"Color\"},\n+    {\"Green\", \"Color\"},\n+    # an applied constructor too\n+    {\"RGB(1, 2)\", \"Color\"},\n+    {\"Some(1)\", \"Opt\"},\n+    # a non-generic named function resolves to its declared return type\n+    {\"area(5)\", \"Int64\"},\n+    {~S|name_of(1)|, \"String\"},\n+    {\"mkvec()\", \"Vec(Int8)\"},\n+    # an unknown name / call is still unknown\n+    {\"Nope\", \"unknown\"},\n+    {\"nope(1)\", \"unknown\"},\n+    # the env shadows a constructor of the same name\n+    {\"x\", \"Int53\"}\n+  ]\n+\n+  describe \"self-hosting checker fixpoint — inference under an inference context (ic)\" do\n+    test \"constructor + non-generic-function returns agree with Rian.Check.infer/3\",\n+         %{mod: mod} do\n+      for {src, expected} <- @ic_corpus do\n+        assert ported_ic(mod, src, @env, @ic) == reference_ic(src, @env, @ic),\n+               \"ic inference diverged on #{inspect(src)}\"\n+\n+        assert ported_ic(mod, src, @env, @ic) == expected,\n+               \"ic inference wrong on #{inspect(src)} — got #{inspect(ported_ic(mod, src, @env, @ic))}, want #{inspect(expected)}\"\n+      end\n+    end\n+  end\n+\n   describe \"teeth — the inference is real and conservative\" do\n     test \"an unbound identifier is `unknown` — the checker does not guess\", %{mod: mod} do\n       assert ported(mod, \"x\") == \"unknown\"","raw_url":"https://github.com/wstein/rian-lab/raw/90ac408736d33693b71a95466406332c4cfde586/test%2Frian%2Fchecker_infer_fixpoint_test.exs","sha":"fbec778cf2eb82602b8998160d0398c1bcb89a49","status":"modified"}]}