{"files":[{"additions":2,"blob_url":"https://github.com/wstein/wiregram-playground/blob/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fcorpus%2Fcrystal%2F03_macros.cr","changes":4,"contents_url":"https://api.github.com/repos/wstein/wiregram-playground/contents/warp%2Fcorpus%2Fcrystal%2F03_macros.cr?ref=8bb50f30b03f7cef1c947f20f35723d9f63c7d27","deletions":2,"filename":"warp/corpus/crystal/03_macros.cr","patch":"@@ -12,11 +12,11 @@ macro create_methods\n   def foo_uppercase\n     \"foo\".upcase\n   end\n-  \n+\n   def bar_uppercase\n     \"bar\".upcase\n   end\n-  \n+\n   def baz_uppercase\n     \"baz\".upcase\n   end","raw_url":"https://github.com/wstein/wiregram-playground/raw/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fcorpus%2Fcrystal%2F03_macros.cr","sha":"232bf1350d842350b67cf8e50cfe021c264a2624","status":"modified"},{"additions":51,"blob_url":"https://github.com/wstein/wiregram-playground/blob/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fcorpus%2Fcrystal%2F09_crystal_metaprogramming_test.cr","changes":51,"contents_url":"https://api.github.com/repos/wstein/wiregram-playground/contents/warp%2Fcorpus%2Fcrystal%2F09_crystal_metaprogramming_test.cr?ref=8bb50f30b03f7cef1c947f20f35723d9f63c7d27","deletions":0,"filename":"warp/corpus/crystal/09_crystal_metaprogramming_test.cr","patch":"@@ -0,0 +1,51 @@\n+# Source: Metaprogramming Help - Crystal,,,\n+\n+# Test Case 1: Overriding #new via extend and macro hooks\n+module ClassMethods\n+  macro extended\n+    def self.new(number : Int32)\n+      puts \"Calling overridden new added from extend hook, arg is #{number}\"\n+      instance = allocate\n+      instance.initialize(number)\n+      instance\n+    end\n+  end\n+end\n+\n+class Foo\n+  extend ClassMethods\n+  @number : Int32\n+\n+  def initialize(number)\n+    puts \"Foo.initialize called with number #{number}\"\n+    @number = number\n+  end\n+end\n+\n+# Test Case 2: Generating Methods via method_missing Macro\n+class Hashr\n+  getter obj\n+\n+  def initialize(json : Hash(String, JSON::Any) | JSON::Any)\n+    @obj = json\n+  end\n+\n+  macro method_missing(key)\n+    def {{key.id}}\n+      value = obj[{{key.id.stringify}}]\n+      Hashr.new(value)\n+    end\n+  end\n+\n+  def ==(other)\n+    obj == other\n+  end\n+end\n+\n+# Test Case 3: Using previous_def for alias chaining\n+class Klass\n+  def salute\n+    puts \"Calling method...\"\n+    previous_def\n+  end\n+end","raw_url":"https://github.com/wstein/wiregram-playground/raw/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fcorpus%2Fcrystal%2F09_crystal_metaprogramming_test.cr","sha":"2dd51f9616790ef109f44b916cbe72eb0eab8fc5","status":"added"},{"additions":179,"blob_url":"https://github.com/wstein/wiregram-playground/blob/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fsrc%2Fwarp%2Flang%2Fcrystal%2Fsemantic_analyzer.cr","changes":279,"contents_url":"https://api.github.com/repos/wstein/wiregram-playground/contents/warp%2Fsrc%2Fwarp%2Flang%2Fcrystal%2Fsemantic_analyzer.cr?ref=8bb50f30b03f7cef1c947f20f35723d9f63c7d27","deletions":100,"filename":"warp/src/warp/lang/crystal/semantic_analyzer.cr","patch":"@@ -1,139 +1,218 @@\n-# Crystal Semantic Analyzer (Phase 3 core)\n-# Extracts method signatures from Crystal source\n+# Crystal Semantic Analyzer (Phase 3 of transpiler)\n+# Analyzes Crystal CST and extracts semantic information for transpilation\n+# Produces CrystalTranspileContext for use by RubyBuilder\n \n module Warp\n   module Lang\n     module Crystal\n-      class SemanticAnalyzer\n-        def initialize(@bytes : Bytes, @tokens : Array(Token))\n+      struct MethodInfo\n+        getter name : String\n+        getter params : Array(ParamInfo)\n+        getter return_type : String?\n+        getter body_start : Int32\n+        getter body_end : Int32\n+        getter source : String\n+\n+        def initialize(@name, @params, @return_type, @body_start, @body_end, @source)\n         end\n+      end\n \n-        def extract_method_sigs : Array(Warp::Lang::Ruby::Annotations::CrystalMethodSig)\n-          sigs = [] of Warp::Lang::Ruby::Annotations::CrystalMethodSig\n-          idx = 0\n-          source = String.new(@bytes)\n+      struct ParamInfo\n+        getter name : String\n+        getter type : String?\n+        getter default_value : String?\n \n-          while idx < @tokens.size\n-            tok = @tokens[idx]\n-            if tok.kind == TokenKind::Def\n-              def_start = tok.start\n-              line_start = find_line_start(source, def_start)\n-              line_end = find_line_end(source, def_start)\n-              line_text = source.byte_slice(line_start, line_end - line_start)\n-              indent = line_text[/^\\s*/].to_s\n+        def initialize(@name, @type, @default_value = nil)\n+        end\n+      end\n \n-              sig = parse_method_signature(line_text, def_start, indent)\n-              sigs << sig if sig\n-            end\n-            idx += 1\n-          end\n+      struct ClassInfo\n+        getter name : String\n+        getter methods : Array(MethodInfo)\n+        getter body_start : Int32\n+        getter body_end : Int32\n+        getter source : String\n \n-          sigs\n+        def initialize(@name, @methods, @body_start, @body_end, @source)\n         end\n+      end\n \n-        private def parse_method_signature(line_text : String, def_start : Int32, indent : String) : Warp::Lang::Ruby::Annotations::CrystalMethodSig?\n-          return nil unless line_text.includes?(\"def \")\n+      struct CrystalTranspileContext\n+        property source : String\n+        getter methods : Hash(String, MethodInfo)\n+        getter classes : Hash(String, ClassInfo)\n+        getter diagnostics : Array(String)\n+\n+        def initialize\n+          @methods = {} of String => MethodInfo\n+          @classes = {} of String => ClassInfo\n+          @source = \"\"\n+          @diagnostics = [] of String\n+        end\n \n-          # Match def with optional self.\n-          # def foo(x : Int32, y : String) : Bool\n-          if (md = line_text.match(/^\\s*def\\s+((self\\.)?([A-Za-z0-9_!?=]+))\\s*(\\(([^)]*)\\))?\\s*(?::\\s*([^#;]+))?/))\n-            name = md[2]? ? \"self.#{md[3]}\" : md[3]\n-            params_raw = md[5]?\n-            return_type = md[6]?.try(&.strip)\n+        def add_method(method : MethodInfo)\n+          @methods[method.name] = method\n+        end\n \n-            params = params_raw ? parse_param_list(params_raw) : [] of Warp::Lang::Ruby::Annotations::CrystalMethodParam\n+        def add_class(klass : ClassInfo)\n+          @classes[klass.name] = klass\n+        end\n \n-            Warp::Lang::Ruby::Annotations::CrystalMethodSig.new(\n-              name,\n-              params,\n-              return_type.nil? || return_type.empty? ? nil : return_type,\n-              return_type.nil? || return_type.empty?,\n-              def_start,\n-              indent,\n-            )\n+        def add_diagnostic(msg : String)\n+          @diagnostics << msg\n+        end\n+      end\n+\n+      class SemanticAnalyzer\n+        def initialize(@root : GreenNode, @source : String)\n+        end\n+\n+        def analyze : CrystalTranspileContext\n+          context = CrystalTranspileContext.new\n+          context.source = @source\n+          visit(@root, context)\n+          context\n+        end\n+\n+        private def visit(node : GreenNode, context : CrystalTranspileContext)\n+          case node.kind\n+          when NodeKind::Document\n+            visit_children(node, context)\n+          when NodeKind::MethodDef\n+            extract_method(node, context)\n+            visit_children(node, context)\n+          when NodeKind::ClassDef\n+            extract_class(node, context)\n+            visit_children(node, context)\n           else\n-            nil\n+            visit_children(node, context)\n           end\n         end\n \n-        private def parse_param_list(params_raw : String) : Array(Warp::Lang::Ruby::Annotations::CrystalMethodParam)\n-          params = [] of Warp::Lang::Ruby::Annotations::CrystalMethodParam\n-          return params if params_raw.strip.empty?\n-\n-          parts = split_params(params_raw)\n-          parts.each do |raw|\n-            name, type = split_param_and_type(raw)\n-            next if name.empty?\n-            params << Warp::Lang::Ruby::Annotations::CrystalMethodParam.new(name, type)\n+        private def visit_children(node : GreenNode, context : CrystalTranspileContext)\n+          node.children.each do |child|\n+            visit(child, context)\n           end\n+        end\n \n-          params\n+        private def extract_method(node : GreenNode, context : CrystalTranspileContext)\n+          # Extract method name, parameters, and return type from MethodDef node\n+          name = extract_name_from_method(node)\n+          params = extract_params_from_method(node)\n+          return_type = extract_return_type_from_method(node)\n+          body_start = node.start\n+          body_end = node.end\n+          source_text = @source.byte_slice(node.start, node.end - node.start)\n+\n+          method_info = MethodInfo.new(name, params, return_type, body_start, body_end, source_text)\n+          context.add_method(method_info)\n         end\n \n-        private def split_params(params_raw : String) : Array(String)\n-          parts = [] of String\n-          current = \"\"\n-          depth = 0\n-\n-          params_raw.each_char do |ch|\n-            case ch\n-            when '(', '[', '{'\n-              depth += 1\n-              current += ch\n-            when ')', ']', '}'\n-              depth -= 1\n-              current += ch\n-            when ','\n-              if depth == 0\n-                parts << current\n-                current = \"\"\n-              else\n-                current += ch\n-              end\n-            else\n-              current += ch\n-            end\n-          end\n+        private def extract_class(node : GreenNode, context : CrystalTranspileContext)\n+          # Extract class name and methods from ClassDef node\n+          name = extract_name_from_class(node)\n+          methods = extract_methods_from_class(node, context)\n+          body_start = node.start\n+          body_end = node.end\n+          source_text = @source.byte_slice(node.start, node.end - node.start)\n \n-          parts << current unless current.empty?\n-          parts\n+          class_info = ClassInfo.new(name, methods, body_start, body_end, source_text)\n+          context.add_class(class_info)\n         end\n \n-        private def split_param_and_type(raw : String) : {String, String?}\n-          text = raw.strip\n-          return {\"\", nil} if text.empty?\n+        private def extract_name_from_method(node : GreenNode) : String\n+          # Find the method name token/node\n+          node.children.each do |child|\n+            if child.is_a?(GreenNode) && child.kind == NodeKind::Identifier\n+              return @source.byte_slice(child.start, child.end - child.start)\n+            end\n+            if child.is_a?(GreenToken) && child.kind == TokenKind::Identifier\n+              return @source.byte_slice(child.start, child.end - child.start)\n+            end\n+          end\n+          \"unknown\"\n+        end\n \n-          # remove default values for type parsing\n-          before_default = text.split(\"=\", 2)[0].strip\n+        private def extract_name_from_class(node : GreenNode) : String\n+          # Find the class name token/node\n+          node.children.each do |child|\n+            if child.is_a?(GreenNode) && child.kind == NodeKind::Identifier\n+              return @source.byte_slice(child.start, child.end - child.start)\n+            end\n+            if child.is_a?(GreenToken) && child.kind == TokenKind::Constant\n+              return @source.byte_slice(child.start, child.end - child.start)\n+            end\n+          end\n+          \"Unknown\"\n+        end\n \n-          # handle splats and blocks\n-          name_part = before_default\n-          type_part = nil\n+        private def extract_params_from_method(node : GreenNode) : Array(ParamInfo)\n+          params = [] of ParamInfo\n+          # Walk through the MethodDef node to find parameter list\n+          node.children.each do |child|\n+            if child.is_a?(GreenNode) && child.kind == NodeKind::ParameterList\n+              child.children.each do |param_node|\n+                if param_node.is_a?(GreenNode) && param_node.kind == NodeKind::Parameter\n+                  param_info = extract_single_param(param_node)\n+                  params << param_info if param_info\n+                end\n+              end\n+            end\n+          end\n+          params\n+        end\n \n-          if (md = before_default.match(/^(\\*\\*?|&)?([A-Za-z0-9_@]+)\\s*:\\s*(.+)$/))\n-            prefix = md[1]? || \"\"\n-            name = md[2]\n-            type_part = md[3].strip\n-            name_part = \"#{prefix}#{name}\"\n+        private def extract_single_param(node : GreenNode) : ParamInfo?\n+          name = \"\"\n+          type = nil\n+          default_value = nil\n+\n+          node.children.each do |child|\n+            if child.is_a?(GreenToken) && child.kind == TokenKind::Identifier\n+              name = @source.byte_slice(child.start, child.end - child.start)\n+            elsif child.is_a?(GreenNode) && child.kind == NodeKind::TypeAnnotation\n+              type = extract_type_annotation(child)\n+            elsif child.is_a?(GreenNode) && child.kind == NodeKind::Assignment\n+              default_value = @source.byte_slice(child.start, child.end - child.start)\n+            end\n           end\n \n-          {name_part, type_part}\n+          name.empty? ? nil : ParamInfo.new(name, type, default_value)\n         end\n \n-        private def find_line_start(source : String, pos : Int32) : Int32\n-          i = pos - 1\n-          while i >= 0 && source[i] != '\\n'\n-            i -= 1\n+        private def extract_return_type_from_method(node : GreenNode) : String?\n+          # Look for return type annotation in MethodDef\n+          node.children.each do |child|\n+            if child.is_a?(GreenNode) && child.kind == NodeKind::TypeAnnotation\n+              return extract_type_annotation(child)\n+            end\n           end\n-          i + 1\n+          nil\n         end\n \n-        private def find_line_end(source : String, pos : Int32) : Int32\n-          i = pos\n-          while i < source.size && source[i] != '\\n'\n-            i += 1\n+        private def extract_type_annotation(node : GreenNode) : String?\n+          @source.byte_slice(node.start, node.end - node.start)\n+        end\n+\n+        private def extract_methods_from_class(node : GreenNode, context : CrystalTranspileContext) : Array(MethodInfo)\n+          methods = [] of MethodInfo\n+          # Recursively find MethodDef nodes within ClassDef\n+          node.children.each do |child|\n+            if child.is_a?(GreenNode)\n+              case child.kind\n+              when NodeKind::MethodDef\n+                extract_method(child, context)\n+                name = extract_name_from_method(child)\n+                if method_info = context.methods[name]?\n+                  methods << method_info\n+                end\n+              else\n+                nested_methods = extract_methods_from_class(child, context)\n+                methods.concat(nested_methods)\n+              end\n+            end\n           end\n-          i\n+          methods\n         end\n       end\n     end","raw_url":"https://github.com/wstein/wiregram-playground/raw/8bb50f30b03f7cef1c947f20f35723d9f63c7d27/warp%2Fsrc%2Fwarp%2Flang%2Fcrystal%2Fsemantic_analyzer.cr","sha":"695a472473fd8783ca33985014f4c3b665ea0940","status":"modified"}]}