{"files":[{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fcachebust.lock.json","changes":4,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fcachebust.lock.json?ref=6d4077c55d43536fae1093a10c689e94b0ccdbff","deletions":2,"filename":"lab/cachebust.lock.json","patch":"@@ -1,7 +1,7 @@\n {\n   \"lab/playground_utils.mjs\": {\n-    \"v\": 19,\n-    \"sha\": \"b1d67da3187b4824e0b442a5f7033ad9d01dc238ba2bbaa8e01717f3edebd90d\"\n+    \"v\": 20,\n+    \"sha\": \"4a2b2c97f46b3874804124d2bfeec25d413fde5586d5614122fc7b0a2b75a5c8\"\n   },\n   \"lab/vendor/flatbars-engine.mjs\": {\n     \"v\": 105,","raw_url":"https://github.com/wstein/flatbars/raw/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fcachebust.lock.json","sha":"ad583e829c9e9a8d574089535dfaadc46cb744c9","status":"modified"},{"additions":17,"blob_url":"https://github.com/wstein/flatbars/blob/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Findex.html","changes":35,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=6d4077c55d43536fae1093a10c689e94b0ccdbff","deletions":18,"filename":"lab/index.html","patch":"@@ -1677,8 +1677,7 @@\n         astOutline,\n         buildCheatSheetData,\n         buildDependencyGraph,\n-        byteRangeToCharRange,\n-        charToByte,\n+        spanRange,\n         charToLineColumn,\n         debounce,\n         decodeState,\n@@ -1697,7 +1696,7 @@\n       // loaded index.html never pairs with a stale cached module (which silently\n       // ran the old, uncompressed encodeState). Bumped for `segmentRanges` (the\n       // provenance-highlight byte↔UTF-16 fix).\n-      } from \"./playground_utils.mjs?v=19\";\n+      } from \"./playground_utils.mjs?v=20\";\n       // Output-view decisions (extracted for testability; lab/output-view.test.mjs).\n       import {\n         VIEW_TABS,\n@@ -2150,7 +2149,7 @@\n         const total = source.length;\n         for (const r of rowsForFile) {\n           if (r.executed || typeof r.start !== \"number\") continue;\n-          const [from, to] = byteRangeToCharRange(source, r.start, r.end);\n+          const [from, to] = spanRange(source, r.start, r.end);\n           const cls = \"cm-dead-line\";\n           // Walk the line set the span covers — Decoration.line takes a\n           // single offset and decorates the entire line containing it, so we\n@@ -3560,7 +3559,7 @@\n           for (const e of compiled.errors) {\n             const file = e.file || \"main\";\n             const src = fileSource(file);\n-            const [from, to] = byteRangeToCharRange(src, e.start, e.end);\n+            const [from, to] = spanRange(src, e.start, e.end);\n             const pos = charToLineColumn(src, from);\n             // Dedupe identical errors (e.g. a partial expanded at several sites).\n             const key = `${file}:${pos.line}:${pos.column}:${e.message}`;\n@@ -3718,7 +3717,7 @@\n           const file = firstHit?.file || \"main\";\n           const src = fileSource(file);\n           const pos = firstHit && typeof firstHit.start === \"number\"\n-            ? charToLineColumn(src, byteRangeToCharRange(src, firstHit.start, firstHit.end)[0])\n+            ? charToLineColumn(src, spanRange(src, firstHit.start, firstHit.end)[0])\n             : { line: 1, column: 1 };\n           dockProblems.push({\n             severity: \"missing\",\n@@ -3978,7 +3977,7 @@\n         // output, so a snippet would be redundant).\n         if (isExpr && sv.start !== null) {\n           const src = fileSource(file);\n-          const [s, e] = byteRangeToCharRange(src, sv.start, sv.end);\n+          const [s, e] = spanRange(src, sv.start, sv.end);\n           const tag = src.slice(s, e).trim();\n           if (tag) {\n             const code = document.createElement(\"code\");\n@@ -4052,7 +4051,7 @@\n         if (linkedTabEl) { linkedTabEl.classList.remove(\"tab-linked\"); linkedTabEl = null; }\n \n         if (active) {\n-          const [from, to] = byteRangeToCharRange(tabs[idx].source, start, end);\n+          const [from, to] = spanRange(tabs[idx].source, start, end);\n           templateView.dispatch({ effects: setLinkHighlight.of({ from, to }) });\n         } else {\n           templateView.dispatch({ effects: setLinkHighlight.of(null) });\n@@ -4085,20 +4084,20 @@\n         const file = activeTabIdx === 0 ? \"main\" : activeTab().name;\n         const src = templateView.state.doc.toString();\n         const caretChar = templateView.state.selection.main.head;\n-        const byte = charToByte(src, caretChar);\n+        const pos = caretChar; // engine spans are char offsets — compare char↔char\n \n         // A `{{> name}}` tag expands inline and emits no run of its own, so a\n         // caret on it has no covering span; map it to every run that partial\n         // produced instead.\n         const partial = partialNameAt(src, caretChar);\n-        const key = partial ? `>${partial}` : `${file}:${byte}`;\n+        const key = partial ? `>${partial}` : `${file}:${pos}`;\n         if (key === caretLinkKey) return;\n         caretLinkKey = key;\n         const ranges = [];\n         for (const sv of lastSegViews) {\n           const hit = partial\n             ? sv.file === partial\n-            : (sv.file === file && sv.start !== null && sv.start <= byte && byte < sv.end);\n+            : (sv.file === file && sv.start !== null && sv.start <= pos && pos < sv.end);\n           if (hit) ranges.push(outLinkMark.range(sv.from, sv.to));\n         }\n         outputEditor.dispatch({ effects: setOutLink.of(Decoration.set(ranges, true)) });\n@@ -4113,7 +4112,7 @@\n         selectTemplateTab(idx);\n         templateView.focus();\n         if (start !== null) {\n-          const [from, to] = byteRangeToCharRange(tabs[idx].source, start, end);\n+          const [from, to] = spanRange(tabs[idx].source, start, end);\n           templateView.dispatch({ selection: { anchor: from, head: to }, scrollIntoView: true });\n         }\n         hideSegTip();\n@@ -5435,7 +5434,7 @@\n           makeEl,\n           fileSource,\n           charToLineColumn,\n-          byteRangeToCharRange,\n+          spanRange,\n           openProblem,\n           usedTransformersApproximate:\n             featureFidelity(engineFeatures, \"used-transformers\") === \"approximate\",\n@@ -5500,7 +5499,7 @@\n             r.status === \"hit\" ? dxSnippet(r.value) : \"\"));\n           const src = fileSource(r.file);\n           const pos = typeof r.start === \"number\"\n-            ? charToLineColumn(src, byteRangeToCharRange(src, r.start, r.end)[0])\n+            ? charToLineColumn(src, spanRange(src, r.start, r.end)[0])\n             : { line: 1, column: 1 };\n           row.append(makeEl(\"span\", { class: \"dx-where\" }, `${r.file}:${pos.line}:${pos.column}`));\n           // Save-as-vector button on every non-hit row. Hits already render the\n@@ -5802,7 +5801,7 @@\n           const cls = r.executed ? \"ok\" : \"dead\";\n           const src = fileSource(r.file);\n           const pos = typeof r.start === \"number\"\n-            ? charToLineColumn(src, byteRangeToCharRange(src, r.start, r.end)[0])\n+            ? charToLineColumn(src, spanRange(src, r.start, r.end)[0])\n             : { line: 1, column: 1 };\n           const row = makeEl(\"div\", { class: `cv-row ${cls}` }, [\n             makeEl(\"span\", { class: \"cv-kind\" }, r.kind),\n@@ -6079,7 +6078,7 @@\n       function setContextTarget(sv) {\n         if (!sv || sv.start === null) return;\n         const src = fileSource(sv.file);\n-        const [from, to] = byteRangeToCharRange(src, sv.start, sv.end);\n+        const [from, to] = spanRange(src, sv.start, sv.end);\n         const label = src.slice(from, to).trim() || `${sv.file}:${sv.start}`;\n         ctxTarget = { file: sv.file, start: sv.start, end: sv.end, label };\n         selectDataTab(\"context\");\n@@ -6116,12 +6115,12 @@\n           if (typeof r.start === \"number\") {\n             row.classList.add(\"linked\");\n             row.addEventListener(\"mouseenter\", () => {\n-              const [from, to] = byteRangeToCharRange(source, r.start, r.end);\n+              const [from, to] = spanRange(source, r.start, r.end);\n               templateView.dispatch({ effects: setLinkHighlight.of({ from, to }) });\n             });\n             row.addEventListener(\"mouseleave\", clearLink);\n             row.addEventListener(\"click\", () => {\n-              const [from] = byteRangeToCharRange(source, r.start, r.end);\n+              const [from] = spanRange(source, r.start, r.end);\n               templateView.focus();\n               templateView.dispatch({ selection: { anchor: from }, scrollIntoView: true });\n             });","raw_url":"https://github.com/wstein/flatbars/raw/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Findex.html","sha":"fc924094bca1802f53635d5b00447da6a86e7bca","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fpanels%2Ftransformers.mjs","changes":4,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fpanels%2Ftransformers.mjs?ref=6d4077c55d43536fae1093a10c689e94b0ccdbff","deletions":2,"filename":"lab/panels/transformers.mjs","patch":"@@ -61,7 +61,7 @@ export function renderTransformers(body, deps) {\n     makeEl,\n     fileSource,\n     charToLineColumn,\n-    byteRangeToCharRange,\n+    spanRange,\n     openProblem,\n     // ADR-0020 Phase 4: true when the engine derives `used-transformers`\n     // heuristically (a static AST walk) rather than from a compiled wire — the\n@@ -129,7 +129,7 @@ export function renderTransformers(body, deps) {\n       const sitesEl = makeEl(\"div\", { class: \"tx-sites\" });\n       for (const s of sites) {\n         const src = fileSource(s.file);\n-        const pos = (typeof s.start === \"number\") ? charToLineColumn(src, byteRangeToCharRange(src, s.start, s.end)[0]) : { line: 1, column: 1 };\n+        const pos = (typeof s.start === \"number\") ? charToLineColumn(src, spanRange(src, s.start, s.end)[0]) : { line: 1, column: 1 };\n         const link = makeEl(\"button\", { class: \"tx-site\", title: \"jump to source\" },\n           `${s.file}:${pos.line}:${pos.column}`);\n         link.addEventListener(\"click\", () => openProblem({ file: s.file, line: pos.line, col: pos.column }));","raw_url":"https://github.com/wstein/flatbars/raw/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fpanels%2Ftransformers.mjs","sha":"921f230b2f45db9f9d1c2ff8ec6bda7adb7c238f","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/flatbars/blob/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fplayground_utils.mjs","changes":52,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fplayground_utils.mjs?ref=6d4077c55d43536fae1093a10c689e94b0ccdbff","deletions":36,"filename":"lab/playground_utils.mjs","patch":"@@ -1,7 +1,5 @@\n // SPDX-License-Identifier: Apache-2.0\n \n-const encoder = new TextEncoder();\n-\n export function debounce(fn, waitMs = 160) {\n   let handle = null;\n   return (...args) => {\n@@ -50,47 +48,29 @@ export function tabVisibleUnder(features, requires) {\n   return (requires || []).every((feature) => map.has(feature));\n }\n \n-// Rust spans are UTF-8 byte offsets; map them into JS string indices.\n-export function byteToChar(source, byteOffset) {\n-  if (byteOffset <= 0) return 0;\n-\n-  let bytes = 0;\n-  for (let i = 0; i < source.length; ) {\n-    if (bytes >= byteOffset) return i;\n-\n-    const codePoint = source.codePointAt(i);\n-    const char = String.fromCodePoint(codePoint);\n-    const charBytes = encoder.encode(char).length;\n-    if (bytes + charBytes > byteOffset) return i;\n-\n-    bytes += charBytes;\n-    i += codePoint > 0xffff ? 2 : 1;\n-  }\n-\n-  return source.length;\n-}\n-\n-// Inverse of byteToChar: the UTF-8 byte offset of a JS string index, so a\n-// CodeMirror caret position can be compared against Rust byte spans.\n-export function charToByte(source, charIndex) {\n-  const index = Math.max(0, Math.min(charIndex, source.length));\n-  return encoder.encode(source.slice(0, index)).length;\n-}\n-\n-export function byteRangeToCharRange(source, startByte, endByte) {\n-  const from = byteToChar(source, startByte);\n-  const to = byteToChar(source, endByte);\n-  return [from, Math.max(from + 1, to)];\n+// An engine `Span` (`{start, end}`) → a CodeMirror editor range. The PureScript\n+// engine's source spans are UTF-16 code-unit (JS string index) offsets — the same\n+// units CodeMirror positions use (see `FlatBars.Span` / ADR-035) — so the span maps\n+// to a range DIRECTLY, with no byte↔char conversion. Converting it as if it were a\n+// UTF-8 byte range corrupts any span that includes or follows a multibyte char\n+// (`×`/`—`), e.g. a ` ×` literal's `[c,c+2)` collapses to `[c,c+1)` — only the space.\n+// Clamp to the document; keep a minimum width of 1 so a zero-length run is still\n+// selectable.\n+export function spanRange(text, start, end) {\n+  const len = text.length;\n+  const from = Math.min(Math.max(start, 0), len);\n+  const to = Math.min(Math.max(from + 1, end), len);\n+  return [from, to];\n }\n \n // Map provenance segments (ADR-035) to output character ranges + the rows the\n // hover/click recover. `seg.out`/`seg.len` are UTF-16 code-unit offsets into the\n // OUTPUT — JS string indices, exactly what `Kernel.Provenance` emits (NOT UTF-8\n // bytes) — so they are CodeMirror positions directly. Converting them as if they\n // were bytes drifts every mark after a multibyte char (`×`/`—`/…), which is the\n-// provenance-highlight misalignment bug. `start`/`end` are the SOURCE span (UTF-8\n-// bytes into the segment's file), passed through untouched for jump-to-source\n-// (which converts them against the source there). A run at/after EOF is dropped.\n+// provenance-highlight misalignment bug. `start`/`end` are the SOURCE span (also\n+// UTF-16 code units — see `spanRange`), passed through for jump-to-source. A run\n+// at/after EOF is dropped.\n export function segmentRanges(output, segments) {\n   const len = output.length;\n   const views = [];","raw_url":"https://github.com/wstein/flatbars/raw/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fplayground_utils.mjs","sha":"0e1972046282b6895940df9b8144177ad6c26fc2","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/flatbars/blob/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fplayground_utils.test.mjs","changes":48,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Fplayground_utils.test.mjs?ref=6d4077c55d43536fae1093a10c689e94b0ccdbff","deletions":36,"filename":"lab/playground_utils.test.mjs","patch":"@@ -10,9 +10,6 @@ import {\n   analyseWhitespace,\n   buildCheatSheetData,\n   buildDependencyGraph,\n-  byteToChar,\n-  byteRangeToCharRange,\n-  charToByte,\n   charToLineColumn,\n   decodeState,\n   disassemble,\n@@ -23,6 +20,7 @@ import {\n   stemTruthy,\n   partialNameAt,\n   segmentRanges,\n+  spanRange,\n   tabVisibleUnder,\n   validateOverlayName,\n   vendoredWorkspace,\n@@ -96,39 +94,17 @@ test(\"stemTruthy: falsy on null/false/\\\"\\\"/0/[]/{}; truthy on non-empty\", () =>\n   }\n });\n \n-test(\"byteToChar maps UTF-8 byte offsets to JS character indices\", () => {\n-  const source = \"A😀B\";\n-\n-  assert.equal(byteToChar(source, 0), 0);\n-  assert.equal(byteToChar(source, 1), 1);\n-  assert.equal(byteToChar(source, 2), 1);\n-  assert.equal(byteToChar(source, 5), 3);\n-  assert.equal(byteToChar(source, 6), 4);\n-});\n-\n-test(\"charToByte maps JS character indices to UTF-8 byte offsets\", () => {\n-  const source = \"A😀B\";\n-\n-  assert.equal(charToByte(source, 0), 0);\n-  assert.equal(charToByte(source, 1), 1); // after \"A\"\n-  assert.equal(charToByte(source, 3), 5); // after \"A😀\" (😀 is 4 bytes)\n-  assert.equal(charToByte(source, 4), 6); // after \"A😀B\"\n-  // Inverts byteToChar on character boundaries.\n-  for (const byte of [0, 1, 5, 6]) {\n-    assert.equal(charToByte(source, byteToChar(source, byte)), byte);\n-  }\n-});\n-\n-test(\"byteRangeToCharRange returns a safe non-empty range\", () => {\n-  const source = \"line 1\\nline 2\";\n-\n-  const [from, to] = byteRangeToCharRange(source, 0, 0);\n-  assert.equal(from, 0);\n-  assert.equal(to, 1);\n-\n-  const [from2, to2] = byteRangeToCharRange(source, 2, 6);\n-  assert.equal(from2, 2);\n-  assert.equal(to2, 6);\n+test(\"spanRange maps an engine span to a CodeMirror range (UTF-16, not UTF-8 bytes)\", () => {\n+  // RC regression: a ` ×` literal in `{{n}} ×{{q}}` has engine span [5,7] (UTF-16\n+  // code units). Used directly it selects \" ×\"; byte-converting it (the old bug)\n+  // collapsed to [5,6] — only the space — because byte 7 lands mid-× (× = bytes 6,7).\n+  const src = \"{{n}} ×{{q}}\";\n+  assert.deepEqual(spanRange(src, 5, 7), [5, 7]);\n+  assert.equal(src.slice(...spanRange(src, 5, 7)), \" ×\");\n+  // clamps to the document and keeps a minimum width of 1.\n+  assert.deepEqual(spanRange(\"ab\", 1, 1), [1, 2]); // zero-length → width 1\n+  assert.deepEqual(spanRange(\"ab\", 0, 99), [0, 2]); // end past EOF → clamp\n+  assert.deepEqual(spanRange(\"ab\", -3, 1), [0, 1]); // negative start → clamp\n });\n \n test(\"charToLineColumn reports 1-based line and column\", () => {","raw_url":"https://github.com/wstein/flatbars/raw/6d4077c55d43536fae1093a10c689e94b0ccdbff/lab%2Fplayground_utils.test.mjs","sha":"2b7cc467fc7663fde058a3a88a60cdbd7cbddec1","status":"modified"}]}