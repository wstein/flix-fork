{"files":[{"additions":771,"blob_url":"https://github.com/wstein/bin/blob/5c2b19bc9d2e81c27bd0a2eb9aa036dd60bc07fd/histlog","changes":1331,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=5c2b19bc9d2e81c27bd0a2eb9aa036dd60bc07fd","deletions":560,"filename":"histlog","patch":"@@ -1,333 +1,17 @@\n #!/usr/bin/env ruby\n \n+require 'sqlite3'\n+require 'json'\n+require 'fileutils'\n+require 'optparse'\n+require 'ostruct'\n+require 'time'\n+require 'set'\n+\n module Histlog\n-  # Parse --duration flag value into {op: :lt|:gt, value: float, unit: :s|:m|:h|:d|:w}\n   module Utils\n     module_function\n \n-    # Helpers for interactive selection (fzf)\n-    def fzf_available?\n-      begin\n-        which = `which fzf 2>/dev/null`.strip\n-        !which.empty?\n-      rescue\n-        false\n-      end\n-    end\n-\n-    def fzf_select(lines, height = 10)\n-      return nil if lines.nil? || lines.empty?\n-      return nil unless fzf_available?\n-\n-      selection = nil\n-      begin\n-        # Use fzf with a configurable height and layout=reverse so results appear below the prompt\n-        cmd = \"fzf --ansi --no-sort --height=#{height} --layout=reverse\"\n-        IO.popen(cmd, 'r+') do |io|\n-          io.puts(lines.join(\"\\n\"))\n-          io.close_write\n-          selection = io.read\n-        end\n-      rescue Errno::ENOENT\n-        return nil\n-      end\n-      selection && !selection.strip.empty? ? selection.strip : nil\n-    end\n-\n-    # Parse --duration flag value into a hash describing the filter.\n-    # Supports:\n-    #   10s         => ±10% range (9s..11s)\n-    #   <10s, >10s  => less/greater than\n-    #   10s..12s    => explicit range\n-    #   ..10.3s     => <=10.3s\n-    #   2.3d..      => >=2.3d\n-    def parse_duration_flag(str)\n-      s = str.strip\n-      # Helper to parse compound duration (e.g. 1d3m, 12m34s)\n-      parse_compound = ->(s) do\n-        total = 0.0\n-        s.scan(/([\\d.]+)([smhdw])/i) do |num, unit|\n-          mult = case unit.downcase\n-                 when 's' then 1\n-                 when 'm' then 60\n-                 when 'h' then 3600\n-                 when 'd' then 86400\n-                 when 'w' then 604800\n-                 else 1\n-                 end\n-          total += num.to_f * mult\n-        end\n-        total > 0 ? total : nil\n-      end\n-\n-      # Range: a..b (support compound)\n-      if s =~ /^([\\dsmhdw.]+)\\.\\.([\\dsmhdw.]+)$/i\n-        a, b = $1, $2\n-        amin = parse_compound.call(a)\n-        bmax = parse_compound.call(b)\n-        return { type: :range, min: { value: amin, unit: :s }, max: { value: bmax, unit: :s } }\n-      end\n-      # Open-ended: ..b (support compound)\n-      if s =~ /^\\.\\.([\\dsmhdw.]+)$/i\n-        b = $1\n-        bmax = parse_compound.call(b)\n-        return { type: :lte, value: bmax, unit: :s }\n-      end\n-      # Open-ended: a.. (support compound)\n-      if s =~ /^([\\dsmhdw.]+)\\.\\.$/i\n-        a = $1\n-        amin = parse_compound.call(a)\n-        return { type: :gte, value: amin, unit: :s }\n-      end\n-      # Less/greater than: <10s, >10s (support compound)\n-      if s =~ /^([<>])\\s*([\\dsmhdw.]+)$/i\n-        op, val = $1, $2\n-        v = parse_compound.call(val)\n-        return { type: (op == '<' ? :lt : :gt), value: v, unit: :s }\n-      end\n-      # Default: ±10% range (support compound)\n-      if (v = parse_compound.call(s))\n-        return { type: :approx, value: v, unit: :s, tol: 0.1 }\n-      end\n-\n-      raise ArgumentError, \"Invalid duration format: #{str}\"\n-    end\n-\n-    # Convert duration hash to seconds\n-    def duration_to_seconds(dur)\n-      mult = case dur[:unit]\n-             when :s then 1\n-             when :m then 60\n-             when :h then 3600\n-             when :d then 86400\n-             when :w then 604800\n-             else 1\n-             end\n-      dur[:value] * mult\n-    end\n-\n-    # Convert a short pattern into a fuzzy SQL LIKE pattern.\n-    # Example: \"git\" -> \"%g%i%t%\" which emulates simple fzf-like fuzzy matching.\n-    def fuzzy_like_pattern(str)\n-      return \"%\" if str.nil? || str.empty?\n-\n-      \"%\" + str.chars.map { |c| Regexp.escape(c) }.join('%') + \"%\"\n-    end\n-\n-    # Parse --time flag value into [start_unix, end_unix] (nil for open-ended)\n-    # Supports:\n-    #   2022-12-20 08:00..09:10:40 (on a specific date)\n-    #   2022-12-20..2022-12-21\n-    #   ..2022-12-20\n-    #   2022-12-20..\n-    #   3w (last 3 weeks)\n-    #   09:00..10:00 (today)\n-    #   2022-12-20+2w-2w (centered window)\n-    def parse_time_flag(str)\n-      s = str.strip\n-      now = Time.now\n-      # Helper: parse date/time string to Time object\n-      parse_dt = ->(t, ref_date = nil) do\n-        # If only time-of-day, use ref_date or today\n-        if t =~ /^\\d{1,2}:\\d{2}(:\\d{2})?$/\n-          base = ref_date || now\n-          h, m, sec = t.split(':').map(&:to_i)\n-          Time.new(base.year, base.month, base.day, h, m, sec || 0)\n-        elsif t =~ /^\\d{4}-\\d{2}-\\d{2}( \\d{1,2}:\\d{2}(:\\d{2})?)?$/\n-          Time.parse(t)\n-        elsif t =~ /^\\d{4}-\\d{2}-\\d{2}$/\n-          Time.parse(t)\n-        else\n-          # Try relative (e.g., 3w, 2d, 4h)\n-          seconds = Histlog::Utils.parse_time_span_to_seconds(t)\n-          seconds ? now - seconds : nil\n-        end\n-      end\n-\n-      # Range: a..b\n-      if s =~ /^(.+)\\.\\.(.+)$/\n-        a, b = $1.strip, $2.strip\n-        t1 = parse_dt.call(a)\n-        t2 = parse_dt.call(b, t1)\n-        return [t1&.to_f, t2&.to_f]\n-      end\n-      # Open-ended: ..b\n-      if s =~ /^\\.\\.(.+)$/\n-        b = $1.strip\n-        t2 = parse_dt.call(b)\n-        return [nil, t2&.to_f]\n-      end\n-      # Open-ended: a..\n-      if s =~ /^(.+)\\.\\.$/\n-        a = $1.strip\n-        t1 = parse_dt.call(a)\n-        return [t1&.to_f, nil]\n-      end\n-      # Centered window: 2022-12-20+2w-2w\n-      if s =~ /^(.+)\\+([\\ddhmws.]+)-([\\ddhmws.]+)$/\n-        base, plus, minus = $1.strip, $2, $3\n-        t0 = parse_dt.call(base)\n-        psec = Histlog::Utils.parse_time_span_to_seconds(plus)\n-        msec = Histlog::Utils.parse_time_span_to_seconds(minus)\n-        return [t0.to_f - msec, t0.to_f + psec]\n-      end\n-      # Relative: 3w (last 3 weeks)\n-      if s =~ /^([\\ddhmws.]+)$/\n-        sec = Histlog::Utils.parse_time_span_to_seconds(s)\n-        return [now.to_f - sec, now.to_f]\n-      end\n-      # Time-of-day only: 09:00..10:00 (today)\n-      if s =~ /^(\\d{1,2}:\\d{2}(:\\d{2})?)\\.\\.(\\d{1,2}:\\d{2}(:\\d{2})?)$/\n-        t1 = parse_dt.call($1)\n-        t2 = parse_dt.call($3)\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Date only: 2022-12-20\n-      if s =~ /^\\d{4}-\\d{2}-\\d{2}$/\n-        t1 = Time.parse(s)\n-        t2 = t1 + 86400 - 1\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Specific timestamp with minute precision: 2025-09-29 10:40\n-      if s =~ /^\\d{4}-\\d{2}-\\d{2} \\d{1,2}:\\d{2}$/\n-        t1 = Time.parse(s)\n-        t2 = t1 + 60 - 1 # Add 59 seconds to cover the entire minute\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Specific timestamp with second precision: 2025-09-29 10:40:30\n-      if s =~ /^\\d{4}-\\d{2}-\\d{2} \\d{1,2}:\\d{2}:\\d{2}$/\n-        t1 = Time.parse(s)\n-        t2 = t1 + 1 - 0.001 # Add ~1 second to match that exact second\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Time-of-day only with minute precision: 10:40\n-      if s =~ /^\\d{1,2}:\\d{2}$/\n-        t1 = parse_dt.call(s)\n-        t2 = t1 + 60 - 1 # Cover the entire minute\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Time-of-day only with second precision: 10:40:30\n-      if s =~ /^\\d{1,2}:\\d{2}:\\d{2}$/\n-        t1 = parse_dt.call(s)\n-        t2 = t1 + 1 - 0.001 # Cover that exact second\n-        return [t1.to_f, t2.to_f]\n-      end\n-      # Absolute time (fallback for other formats)\n-      t = parse_dt.call(s)\n-      t ? [t.to_f, nil] : [nil, nil]\n-    end\n-\n-    # Helpers for installing init snippets into rc files\n-    def default_rc_for_shell(shell)\n-      home = Dir.home\n-      case shell\n-      when 'fish'\n-        File.join(home, '.config', 'fish', 'config.fish')\n-      when 'zsh'\n-        File.join(home, '.zshrc')\n-      when 'nu', 'nushell'\n-        File.join(home, '.config', 'nushell', 'config.nu')\n-      else\n-        nil\n-      end\n-    end\n-\n-    def install_snippet_to_rc(rc_file, snippet, force = false)\n-      require 'fileutils'\n-\n-      rc_file = File.expand_path(rc_file)\n-      dir = File.dirname(rc_file)\n-      FileUtils.mkdir_p(dir) unless Dir.exist?(dir)\n-\n-      # Ensure snippet ends with a newline\n-      snippet = snippet.end_with?(\"\\n\") ? snippet : snippet + \"\\n\"\n-\n-      start_marker = \"# >>> histlog init start\\n\"\n-      end_marker = \"# <<< histlog init end\\n\"\n-\n-      if File.exist?(rc_file)\n-        content = File.read(rc_file)\n-        # Backup existing file\n-        timestamp = Time.now.strftime('%Y%m%d%H%M%S')\n-        backup = \"#{rc_file}.histlog.bak.#{timestamp}\"\n-        FileUtils.cp(rc_file, backup)\n-\n-        if content.include?(start_marker) && content.include?(end_marker)\n-          if !force\n-            raise \"histlog snippet already present in #{rc_file}; use --force to replace\"\n-          end\n-\n-          # Replace existing snippet block\n-          new_block = start_marker + snippet + end_marker\n-          new_content = content.sub(/# >>> histlog init start.*?# <<< histlog init end/m, new_block)\n-          File.write(rc_file, new_content)\n-        else\n-          # Append snippet at the end\n-          File.open(rc_file, 'a') do |f|\n-            f.write(\"\\n\") unless content.end_with?(\"\\n\")\n-            f.write(start_marker)\n-            f.write(snippet)\n-            f.write(end_marker)\n-          end\n-        end\n-      else\n-        # Create new rc file with the snippet\n-        File.open(rc_file, 'w', 0600) do |f|\n-          f.write(\"# Created by histlog on #{Time.now}\\n\")\n-          f.write(start_marker)\n-          f.write(snippet)\n-          f.write(end_marker)\n-        end\n-      end\n-    end\n-  end\n-\n-  module Config\n-    module_function\n-\n-    # Local helper: format bytes human-readably for DB reports\n-    def format_bytes(bytes)\n-      units = ['B', 'KB', 'MB', 'GB', 'TB']\n-      size = bytes.to_f\n-      unit_index = 0\n-\n-      while size >= 1024 && unit_index < units.length - 1\n-        size /= 1024\n-        unit_index += 1\n-      end\n-\n-      \"#{size.round(2)} #{units[unit_index]}\"\n-    end\n-\n-    def build_cleanup_option_parser(options)\n-      OptionParser.new do |opts|\n-  opts.on(\"--fuzzy\", \"Enable fuzzy (fzf-like) matching for command text\") { options[:fuzzy] = true }\n-  opts.on(\"-i\", \"--interactive\", \"Use fzf for interactive selection\") { options[:interactive] = true }\n-  opts.on(\"--height N\", Integer, \"Set fzf height for interactive selection\") { |n| options[:height] = n }\n-        opts.on(\"--command [PATTERN]\", \"Filter by command text (with optional fuzzy matching)\") do |pattern|\n-          options[:command_pattern] = pattern\n-        end\n-        opts.banner = \"Usage: histlog db cleanup [options]\"\n-        opts.on(\"-v\", \"--verbose\", \"Show detailed cleanup progress\") { options[:verbose] = true }\n-        opts.on(\"-n\", \"--dry-run\", \"Show what would be cleaned without making changes\") { options[:dry_run] = true }\n-        opts.on(\"-h\", \"--help\", \"Show this help\") do\n-          puts opts\n-          exit 0\n-        end\n-      end\n-    end\n-\n-    def build_info_option_parser(options)\n-      OptionParser.new do |opts|\n-        opts.banner = \"Usage: histlog db info [options]\"\n-        opts.on(\"-v\", \"--verbose\", \"Show detailed system information\") { options[:verbose] = true }\n-        opts.on(\"-h\", \"--help\", \"Show this help\") do\n-          puts opts\n-          exit 0\n-        end\n-      end\n-    end\n-\n     DEFAULT_DB_PATH = File.expand_path('~/.local/share/histlog/histlog.db')\n \n     COMMAND_OPTION_DEFAULTS = {\n@@ -342,7 +26,26 @@ module Histlog\n         asc: false,\n         desc: false,\n         sort_by: nil,\n-        format: nil\n+        format: nil,\n+        session: false,\n+        session_id: nil,\n+        dir: false,\n+        dir_path: nil\n+      },\n+      commands: {\n+        searchterm: nil,\n+        regex: false,\n+        fuzzy: false,\n+        interactive: false,\n+        limit: nil,\n+        height: nil,\n+        sort: :count,\n+        asc: false,\n+        desc: false,\n+        session: false,\n+        session_id: nil,\n+        dir: false,\n+        dir_path: nil\n       },\n       cleanup: {\n         verbose: false,\n@@ -354,20 +57,24 @@ module Histlog\n     }.freeze\n \n     COLOR_PALETTES = {\n-      general: {\n-        reset: \"\\e[0m\"\n-      },\n+      general: { reset: \"\\e[0m\" },\n       paths: {\n         exec: \"\\e[38;5;207m\",\n         args: \"\\e[38;5;80m\",\n         dir_path: \"\\e[38;5;12m\"\n       },\n+      commands: {\n+        count: \"\\e[38;5;207m\",\n+        command: \"\\e[38;5;80m\",\n+        flag: \"\\e[38;5;111m\",\n+        s_flag: \"\\e[38;5;82m\",    # green for S\n+        w_flag: \"\\e[38;5;220m\"    # yellow for W\n+      },\n       history: {\n         timestamp: \"\\e[38;5;14m\",\n         duration: \"\\e[33m\",\n         status_success: \"\\e[38;5;84m\",\n         status_fail: \"\\e[38;5;203m\",\n-        # Use terminal color 8 (grey/dark) for null/unknown values\n         status_null: \"\\e[38;5;8m\",\n         status_header: \"\\e[38;5;141m\"\n       }\n@@ -377,175 +84,109 @@ module Histlog\n       COMMAND_OPTION_DEFAULTS.fetch(command, {}).dup\n     end\n \n-    # Return the default history file path for a given shell name (zsh, bash, fish, nu)\n-    def default_history_for_shell(shell_name)\n-      home = Dir.home\n-      case shell_name\n-      when 'fish'\n-        fish_hist = File.join(home, '.local/share/fish/fish_history')\n-        return fish_hist if File.exist?(fish_hist)\n-      when 'zsh'\n-        candidates = [File.join(home, '.zsh_history'), File.join(home, '.zhistory'), ENV['HISTFILE']].compact\n-        candidates.each { |c| return c if File.exist?(c) }\n-      when 'nu', 'nushell'\n-        nu_config_dir = ENV['XDG_CONFIG_HOME'] || File.join(home, '.config')\n-        candidate = File.join(nu_config_dir, 'nushell/history.txt')\n-        return candidate if File.exist?(candidate)\n-\n-        candidate2 = File.join(home, '.config/nushell/history.txt')\n-        return candidate2 if File.exist?(candidate2)\n-      when 'bash'\n-        bash_hist = File.join(home, '.bash_history')\n-        return bash_hist if File.exist?(bash_hist)\n-      end\n-      nil\n-    end\n-\n     def colors(context = nil)\n       palette = COLOR_PALETTES[:general].dup\n       palette.merge!(COLOR_PALETTES[context]) if context && COLOR_PALETTES.key?(context)\n       palette\n     end\n \n-    def db_path\n+    def get_db_path\n       env_path = ENV['HISTLOG_DB']&.strip\n       if env_path && !env_path.empty?\n         File.expand_path(env_path)\n       else\n         DEFAULT_DB_PATH\n       end\n     end\n-  end\n-\n-  # CLI command handling module\n-  module CLI\n-    # Common utilities shared across CLI modules\n-    module Common\n-      module_function\n \n-      # Formatting utilities\n-      def format_bytes(bytes)\n-        units = ['B', 'KB', 'MB', 'GB', 'TB']\n-        size = bytes.to_f\n-        unit_index = 0\n-\n-        while size >= 1024 && unit_index < units.length - 1\n-          size /= 1024\n-          unit_index += 1\n-        end\n+    # Backwards-compatible alias used in other parts of the codebase\n+    def db_path\n+      get_db_path\n+    end\n \n-        \"#{size.round(2)} #{units[unit_index]}\"\n+    # Helpers for interactive selection (fzf)\n+    def fzf_available?\n+      begin\n+        which = `which fzf 2>/dev/null`.strip\n+        !which.empty?\n+      rescue\n+        false\n       end\n+    end\n \n-      def format_time(timestamp)\n-        return 'unknown' unless timestamp\n-\n-        # Convert from Julian Day to Unix timestamp\n-        unix_time = (timestamp - 2440587.5) * 86400\n-        Time.at(unix_time).strftime('%Y-%m-%d %H:%M:%S')\n-      end\n+    def fzf_select(lines, height = 10, preview: true)\n+      return nil if lines.nil? || lines.empty?\n+      return nil unless fzf_available?\n \n-      def format_duration(duration)\n-        return 'N/A' unless duration\n-\n-        if duration < 1\n-          \"#{(duration * 1000).round(1)}ms\"\n-        elsif duration < 60\n-          \"#{duration.round(2)}s\"\n-        elsif duration < 3600\n-          minutes = (duration / 60).to_i\n-          seconds = (duration % 60).round(1)\n-          \"#{minutes}m #{seconds}s\"\n-        else\n-          hours = (duration / 3600).to_i\n-          minutes = ((duration % 3600) / 60).to_i\n-          \"#{hours}h #{minutes}m\"\n+      selection = nil\n+      begin\n+        # Use an ANSI-aware, colorful fzf layout for better discoverability.\n+        # Keep --ansi to allow color escape sequences in the lines.\n+        # The --color argument is intentionally conservative and should work\n+        # across different terminal themes; users can override with their\n+        # own FZF_DEFAULT_OPTS if desired.\n+        # Use quoted color string to avoid shell parsing issues.\n+        # Use an ANSI-aware fzf layout. Keep --ansi to allow color escape\n+        # sequences in the lines. For some callers (paths) we may want to\n+        # disable the preview to keep the UI compact.\n+        cmd = \"fzf --ansi --no-sort --height=#{height} --layout=reverse\"\n+        if preview\n+          cmd += \" --preview 'echo {}' --preview-window=down:3:wrap\"\n         end\n-      end\n+        IO.popen(cmd, 'r+') do |io|\n+          # For easier debugging in user environments, allow dumping the\n+          # first few lines that will be piped to fzf by setting\n+          # HISTLOG_DEBUG_FZF=1 in the environment. This helps determine\n+          # whether columns are missing before fzf renders them.\n+          if ENV['HISTLOG_DEBUG_FZF'] == '1'\n+            warn \"--- histlog: fzf input (first 20 lines, ANSI-stripped) ---\"\n+            lines.first(20).each do |l|\n+              warn l.gsub(/\\e\\[[\\d;]*m/, '')\n+            end\n+            warn \"--- end fzf input ---\"\n+          end\n \n-      # Database utilities\n-      def ensure_database_exists(db_path)\n-        unless File.exist?(db_path)\n-          raise \"Database not found: #{db_path}\"\n+          io.puts(lines.join(\"\\n\"))\n+          io.close_write\n+          selection = io.read\n         end\n-\n-        true\n+      rescue Errno::ENOENT\n+        return nil\n       end\n+      selection && !selection.strip.empty? ? selection.strip : nil\n+    end\n \n-      def format_database_error(error)\n-        \"Database error: #{error.message}\"\n-      end\n+    # Convert a short pattern into a fuzzy SQL LIKE pattern.\n+    def fuzzy_like_pattern(str)\n+      return \"%\" if str.nil? || str.empty?\n \n-      # Option parsing\n-      def build_cleanup_option_parser(options)\n-        OptionParser.new do |opts|\n-          opts.banner = \"Usage: histlog db cleanup [options]\"\n-          opts.on(\"-v\", \"--verbose\", \"Show detailed cleanup progress\") { options[:verbose] = true }\n-          opts.on(\"-n\", \"--dry-run\", \"Show what would be cleaned without making changes\") { options[:dry_run] = true }\n-          opts.on(\"-h\", \"--help\", \"Show this help\") do\n-            puts opts\n-            exit 0\n-          end\n-        end\n-      end\n+      \"%\" + str.chars.map { |c| Regexp.escape(c) }.join('%') + \"%\"\n+    end\n \n-      def build_info_option_parser(options)\n-        OptionParser.new do |opts|\n-          opts.banner = \"Usage: histlog db info [options]\"\n-          opts.on(\"-v\", \"--verbose\", \"Show detailed system information\") { options[:verbose] = true }\n-          opts.on(\"-h\", \"--help\", \"Show this help\") do\n-            puts opts\n-            exit 0\n-          end\n-        end\n-      end\n+    # Format timestamp for display in tables (always full date and time)\n+    def format_timestamp(timestamp)\n+      return \"                   \" if timestamp.nil? || timestamp == 0\n \n-      # Color palette access (delegate to Config)\n-      def colors(context = nil)\n-        Histlog::Config.colors(context)\n-      end\n+      Time.at(timestamp).strftime(\"%Y-%m-%d %H:%M:%S\")\n+    rescue\n+      \"                   \"\n     end\n+  end\n \n+  module CLI\n     module_function\n \n-    # Print top-level help. If all is true, include porcelain/internal commands as well.\n-    def show_help(all = false)\n-      puts \"Usage: histlog <command> [options]\"\n-      puts \"\"\n-\n-      puts \"Available commands:\"\n-      puts \"  query       - Flexible interactive query of your command history\"\n-      puts \"  sessions    - List and inspect recorded shell sessions\"\n-      puts \"  import      - Import shell history from files (json, yaml, fish, zsh, nu, bash)\"\n-      puts \"  paths       - Show tracked file/directory paths and usage counts\"\n-      puts \"  db          - Database operations (cleanup, info, stats, maintain)\"\n-      puts \"  init        - Print shell integration snippets for supported shells\"\n-      puts \"  mcp         - Run the MCP server (developer feature)\"\n-      puts \"  help        - Show this help\"\n-\n-      puts \"\"\n-      puts \"Global flags:\"\n-      puts \"  -h, --help        Show top-level help (this page)\"\n-      puts \"  --help-all        Show full help including porcelain/internal commands\"\n-      puts \"\"\n-      puts \"Environment variables (affect behavior):\"\n-      puts \"  HISTLOG_DB        Path to the histlog SQLite database (overrides default)\"\n-\n-      if all\n-        puts \"\"\n-        puts \"Porcelain / internal commands (normally hidden):\"\n-        puts \"  _on_session - Initialize a new shell session (internal)\"\n-        puts \"  _on_preexec - Record command start (internal)\"\n-        puts \"  _on_postcmd - Record command completion (internal)\"\n-      end\n-\n-      puts \"\"\n-      puts \"For command-specific help run: histlog <command> --help\"\n-    end\n-\n     def build_paths_option_parser(options)\n       OptionParser.new do |opts|\n         opts.banner = \"Usage: histlog paths [--files] [--dirs] [SEARCHTERM] [--regex] [--count] [--asc|--desc] [--sort-by FIELD]\"\n+        opts.on(\"--session [ID]\", \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n+          options[:session] = true\n+          options[:session_id] = id.to_i if id\n+        end\n+        opts.on(\"--dir [PATH]\", \"Show paths from current or specific directory\") do |path|\n+          options[:dir] = true\n+          options[:dir_path] = path\n+        end\n         opts.on(\"--files\", \"Show all unique file paths\") { options[:files] = true }\n         opts.on(\"--dirs\", \"Show all unique directory paths\") { options[:dirs] = true }\n         opts.on(\"-i\", \"--interactive\", \"Use fzf for interactive selection\") { options[:interactive] = true }\n@@ -567,12 +208,50 @@ module Histlog\n       end\n     end\n \n+    def build_commands_option_parser(options)\n+      OptionParser.new do |opts|\n+        opts.banner = \"Usage: histlog commands [options] [SEARCHTERM]\"\n+        opts.on(\"--limit N\", Integer, \"Limit number of commands displayed\") { |n| options[:limit] = n }\n+        opts.on(\"--regex\", \"Interpret searchterm as a Ruby regex\") { options[:regex] = true }\n+        opts.on(\"--fuzzy\", \"Enable fuzzy (fzf-like) matching for the search term\") { options[:fuzzy] = true }\n+        opts.on(\"--session [ID]\", \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n+          options[:session] = true\n+          options[:session_id] = id.to_i if id\n+        end\n+        opts.on(\"--dir [PATH]\", \"Commands executed in current or specific directory\") do |path|\n+          options[:dir] = true\n+          options[:dir_path] = path\n+        end\n+        opts.on(\"-i\", \"--interactive\", \"Select a command interactively using fzf\") { options[:interactive] = true }\n+        opts.on(\"--height N\", Integer, \"Set fzf height for interactive selection\") { |n| options[:height] = n }\n+        opts.on(\"--plain\", \"Output only commands, one per line\") { options[:format] = 'plain' }\n+        opts.on(\"--sort FIELD\", \"Sort by count (default), recent, alpha, or context\") do |field|\n+          sort_key = field.to_s.downcase.to_sym\n+          if %i[count recent alpha context].include?(sort_key)\n+            options[:sort] = sort_key\n+          else\n+            STDERR.puts \"Unknown sort field '#{field}'. Allowed values: count, recent, alpha, context.\"\n+            exit 1\n+          end\n+        end\n+        # Shortcuts: set context sorting\n+\n+        opts.on(\"--context\", \"Shortcut for --sort context (spelled) \") { options[:sort] = :context }\n+        opts.on(\"--asc\", \"Sort ascending\") { options[:asc] = true }\n+        opts.on(\"--desc\", \"Sort descending\") { options[:desc] = true }\n+        opts.on(\"-h\", \"--help\", \"Show this help\") do\n+          puts opts\n+          exit 0\n+        end\n+      end\n+    end\n+\n     def show_paths_command(args)\n-      options = Histlog::Config.command_options(:paths)\n+      options = Histlog::Utils.command_options(:paths)\n \n       build_paths_option_parser(options).parse!(args)\n \n-      # Accept positional searchterm (first non-option remaining)\n+      # Positional search term (first remaining non-option)\n       if args.length > 0 && !args[0].start_with?('-')\n         options[:searchterm] = args.shift\n       end\n@@ -601,6 +280,31 @@ module Histlog\n         type_cond = types.length == 2 ? \"type IN ('f','d')\" : \"type = '#{types[0] == 'file' ? 'f' : 'd'}'\"\n         query = \"SELECT path, type#{select_count} FROM paths WHERE #{type_cond}\"\n         params = []\n+        # If session filtering requested, restrict to paths used in that session\n+        if options[:session]\n+          sess_id = options[:session_id]\n+          unless sess_id\n+            env_sid = ENV['__histlog_session_id']&.to_i\n+            if env_sid && env_sid > 0\n+              sess_id = env_sid\n+            else\n+              STDERR.puts \"Error: --session used without argument and __histlog_session_id environment variable is not set\"\n+              STDERR.puts \"Either provide a session ID: --session 123\"\n+              STDERR.puts \"Or set the environment variable: export __histlog_session_id=123\"\n+              exit 1\n+            end\n+          end\n+          # Restrict paths to those referenced by commands in the session\n+          query << \" AND (id IN (SELECT path_old_id FROM commands WHERE session_id = ?) OR id IN (SELECT path_new_id FROM commands WHERE session_id = ?) OR id IN (SELECT path_id FROM path_args WHERE command_id IN (SELECT id FROM commands WHERE session_id = ?)))\"\n+          params << sess_id << sess_id << sess_id\n+        end\n+        # If dir filtering requested, restrict to exact path match\n+        if options[:dir]\n+          dir_path = options[:dir_path] || Dir.pwd\n+          abs_dir = File.expand_path(dir_path)\n+          query << \" AND path = ?\"\n+          params << abs_dir\n+        end\n         if options[:searchterm]\n           if options[:regex]\n             query << \" AND path REGEXP ?\"\n@@ -673,8 +377,7 @@ module Histlog\n           if path_id\n             args_count = db.get_first_value(\"SELECT COUNT(*) FROM path_args WHERE path_id = ?\", path_id) || 0\n             exec_count = db.get_first_value(\n-              \"SELECT COUNT(*) FROM commands WHERE path_old_id = ? OR path_new_id = ?\",\n-              path_id,\n+              \"SELECT COUNT(*) FROM commands WHERE path_old_id = ?\",\n               path_id\n             ) || 0\n           end\n@@ -699,11 +402,37 @@ module Histlog\n         if options[:interactive]\n           limited_rows = options[:limit] && options[:limit] > 0 ? data_rows.first(options[:limit]) : data_rows\n \n-          plain_lines = limited_rows.map do |r|\n-            if r['type'] == 'd'\n-              r['path'].end_with?('/') ? r['path'] : (r['path'] + '/')\n-            else\n-              r['path']\n+          # Build selection lines according to requested output format so that\n+          # --interactive does not implicitly change the output format.\n+          if options[:format] == 'plain'\n+            plain_lines = limited_rows.map do |r|\n+              if r['type'] == 'd'\n+                r['path'].end_with?('/') ? r['path'] : (r['path'] + '/')\n+              else\n+                r['path']\n+              end\n+            end\n+          else\n+            palette = Histlog::Utils.colors(:paths)\n+            exec_col = palette[:exec] || ''\n+            args_col = palette[:args] || ''\n+            dir_path_col = palette[:dir_path] || ''\n+            reset_col = palette[:reset] || ''\n+\n+            plain_lines = limited_rows.map do |r|\n+              exec_str = r['exec'].to_i.to_s.rjust(6)\n+              args_str = r['args'].to_i.to_s.rjust(6)\n+              display_path = if r['type'] == 'd'\n+                               if r['path'] == '/'\n+                                 \"#{dir_path_col}/#{reset_col}\"\n+                               else\n+                                 base = r['path'].end_with?('/') ? r['path'].chomp('/') : r['path']\n+                                 \"#{dir_path_col}#{base}#{reset_col}/\"\n+                               end\n+                             else\n+                               r['path']\n+                             end\n+              \"#{exec_col}#{exec_str}#{reset_col} #{args_col}#{args_str}#{reset_col} │ #{display_path}\"\n             end\n           end\n \n@@ -712,36 +441,36 @@ module Histlog\n             # fall through to normal output\n           else\n             height = options[:height] || 10\n-            sel = Histlog::Utils.fzf_select(plain_lines, height)\n+            # Disable preview for paths interactive for a simpler UI\n+            sel = Histlog::Utils.fzf_select(plain_lines, height, preview: false)\n             if sel.nil? || sel.empty?\n               # User cancelled or no selection\n               return\n             end\n \n-            puts sel\n+            # If the selection was a table-like line, try to extract the path\n+            if options[:format] == 'plain'\n+              puts sel\n+            else\n+              # For the table-like lines we constructed above the path is after the\n+              # visible separator. Strip ANSI and split on the separator for\n+              # robust extraction.\n+              sel_no_ansi = sel.gsub(/\\e\\[[\\d;]*m/, '')\n+              parts = sel_no_ansi.split(/\\s*│\\s*/)\n+              path_part = parts.last ? parts.last.strip : sel_no_ansi.strip\n+              puts path_part\n+            end\n             return\n           end\n         end\n \n         if options[:limit] && options[:limit] > 0\n-          limited = data_rows.first(options[:limit])\n-          begin\n-            limited.each do |r|\n-              if r['type'] == 'd'\n-                puts r['path'].end_with?('/') ? r['path'] : (r['path'] + '/')\n-              else\n-                puts r['path']\n-              end\n-            end\n-          rescue Errno::EPIPE\n-            return\n-          end\n-          return\n+          data_rows = data_rows.first(options[:limit])\n         end\n \n         # Note: --cd behavior removed. Shell helpers should call histlog and perform cd in the shell.\n \n-        palette = Histlog::Config.colors(:paths)\n+        palette = Histlog::Utils.colors(:paths)\n         exec_col = palette[:exec]\n         args_col = palette[:args]\n         dir_path_col = palette[:dir_path]\n@@ -782,18 +511,387 @@ module Histlog\n       end\n     end\n \n+    def show_commands_command(args)\n+      options = Histlog::Utils.command_options(:commands)\n+\n+      build_commands_option_parser(options).parse!(args)\n+\n+      # Positional search term (first remaining non-option)\n+      if args.length > 0 && !args[0].start_with?('-')\n+        options[:searchterm] = args.shift\n+      end\n+\n+      if options[:regex] && options[:fuzzy]\n+        STDERR.puts \"--regex cannot be combined with --fuzzy\"\n+        return\n+      end\n+\n+      if options[:limit]\n+        options[:limit] = options[:limit].to_i\n+        options[:limit] = nil if options[:limit] <= 0\n+      end\n+\n+      if options[:height]\n+        options[:height] = options[:height].to_i\n+        options[:height] = nil if options[:height] <= 0\n+      end\n+\n+      if options[:regex] && (options[:searchterm].nil? || options[:searchterm].empty?)\n+        STDERR.puts \"--regex requires a search pattern\"\n+        return\n+      end\n+\n+      if options[:fuzzy] && (options[:searchterm].nil? || options[:searchterm].empty?)\n+        STDERR.puts \"--fuzzy requires a search term\"\n+        return\n+      end\n+\n+      regex_object = nil\n+      if options[:regex] && options[:searchterm]\n+        begin\n+          regex_object = Regexp.new(options[:searchterm])\n+        rescue RegexpError => e\n+          STDERR.puts \"Invalid regex: #{e.message}\"\n+          return\n+        end\n+      end\n+\n+      db_path = Histlog::Utils.get_db_path\n+      unless File.exist?(db_path)\n+        puts \"No histlog.db found in current directory\"\n+        return\n+      end\n+\n+      db = nil\n+      begin\n+        db = SQLite3::Database.new(db_path)\n+        db.results_as_hash = true\n+\n+        base_query = <<~SQL\n+          SELECT\n+            cmd.id AS cmd_text_id,\n+            cmd.command AS command_text,\n+            COUNT(*) AS usage_count,\n+            SUM(CASE WHEN c.exit_code = 0 THEN 1 ELSE 0 END) AS success_count,\n+            SUM(CASE WHEN c.exit_code != 0 AND c.exit_code IS NOT NULL THEN 1 ELSE 0 END) AS failure_count,\n+            MAX(c.start_time) AS last_used,\n+            AVG(c.duration) AS avg_duration\n+          FROM commands c\n+          JOIN cmd_texts cmd ON c.cmd_text_id = cmd.id\n+        SQL\n+\n+        where_clauses = [\"cmd.command IS NOT NULL\", \"cmd.command != ''\"]\n+        params = []\n+        regex_clause_index = nil\n+\n+        if options[:searchterm]\n+          if options[:regex]\n+            where_clauses << \"cmd.command REGEXP ?\"\n+            regex_clause_index = params.length\n+            params << options[:searchterm]\n+          else\n+            where_clauses << \"cmd.command LIKE ?\"\n+            params << \"%#{options[:searchterm]}%\"\n+          end\n+        end\n+\n+        query = base_query.dup\n+        query << \"WHERE #{where_clauses.join(' AND ')}\\n\"\n+        query << \"GROUP BY cmd.id, cmd.command\\n\"\n+\n+        sort_key = options[:sort]\n+        sort_key = sort_key.to_sym if sort_key.respond_to?(:to_sym)\n+        sort_key = :count unless %i[count recent alpha].include?(sort_key)\n+\n+        direction = if options[:asc]\n+                      \"ASC\"\n+                    elsif options[:desc]\n+                      \"DESC\"\n+                    else\n+                      sort_key == :alpha ? \"ASC\" : \"DESC\"\n+                    end\n+\n+        # Extend allowed sort keys with :context which groups by session/cwd\n+        sort_key = :count unless %i[count recent alpha context].include?(sort_key)\n+\n+        order_clause = case sort_key\n+                       when :recent\n+                         \"ORDER BY last_used #{direction}\"\n+                       when :alpha\n+                         \"ORDER BY command_text #{direction}\"\n+                       when :context\n+                         # We'll compute context ordering in Ruby since it depends on env/session\n+                         \"ORDER BY last_used #{direction}\"\n+                       else\n+                         \"ORDER BY usage_count #{direction}, command_text ASC\"\n+                       end\n+        query << \"#{order_clause}\\n\"\n+\n+        # Do not apply SQL LIMIT here; fetch candidates and apply limit after dedup in Ruby\n+\n+        rows = []\n+        begin\n+          rows = db.execute(query, params)\n+        rescue SQLite3::SQLException => e\n+          if options[:regex] && e.message =~ /no such function: REGEXP/\n+            fallback_where = where_clauses.reject { |clause| clause.include?('REGEXP') }\n+            fallback_query = base_query.dup\n+            fallback_query << \"WHERE #{fallback_where.join(' AND ')}\\n\"\n+            fallback_query << \"GROUP BY cmd.id, cmd.command\\n\"\n+            fallback_query << \"#{order_clause}\\n\"\n+            fallback_query << \"LIMIT ?\\n\" if options[:limit]\n+            fallback_params = params.dup\n+            fallback_params.delete_at(regex_clause_index) if regex_clause_index\n+            rows = db.execute(fallback_query, fallback_params)\n+            rows.select! { |row| regex_object =~ (row['command_text'] || '') } if regex_object\n+            if options[:limit]\n+              rows = rows.first(options[:limit])\n+            end\n+          else\n+            raise e\n+          end\n+        end\n+\n+        if rows.empty?\n+          puts \"No commands found.\"\n+          return\n+        end\n+\n+        # Deduplicate commands by command_text: show each command only once\n+        unique = {}\n+        rows.each do |r|\n+          key = (r['command_text'] || '').strip\n+          # Choose the row with the most recent last_used for this command\n+          if unique[key].nil? || (r['last_used'] || 0) > (unique[key]['last_used'] || 0)\n+            unique[key] = r\n+          end\n+        end\n+        rows = unique.values\n+\n+        # If plain output requested (one command per line), emit and exit\n+        if options[:format] == 'plain' && !options[:interactive]\n+          begin\n+            to_print = if options[:limit] && options[:limit] > 0\n+                         rows.first(options[:limit])\n+                       else\n+                         rows\n+                       end\n+            to_print.each do |r|\n+              puts (r['command_text'] || '')\n+            end\n+          rescue Errno::EPIPE\n+            return\n+          end\n+          return\n+        end\n+\n+        # Always compute SW flags (same session / same cwd) for table output so the SW column is available.\n+        env_session_id = ENV['__histlog_session_id']&.to_i\n+        cwd = File.expand_path(options[:dir_path] || Dir.pwd)\n+        rows.each do |r|\n+          # Determine if this command_text has any executions in the current session\n+          if env_session_id && env_session_id > 0\n+            begin\n+              chk_s = db.get_first_value(\"SELECT COUNT(*) FROM commands WHERE cmd_text_id = ? AND session_id = ?\", [r['cmd_text_id'], env_session_id])\n+              r['_same_session'] = chk_s.to_i > 0 ? 1 : 0\n+            rescue SQLite3::Exception\n+              r['_same_session'] = 0\n+            end\n+          else\n+            r['_same_session'] = 0\n+          end\n+\n+          # Determine if this command_text was executed in the current working directory\n+          begin\n+            chk_c = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE c.cmd_text_id = ? AND p.path = ?\", [r['cmd_text_id'], cwd])\n+            r['_same_cwd'] = chk_c.to_i > 0 ? 1 : 0\n+          rescue SQLite3::Exception\n+            r['_same_cwd'] = 0\n+          end\n+        end\n+\n+        # If context sort was requested, compute ordering: same session first, then same cwd, then others\n+        if options[:sort].to_sym == :context\n+          # Group 1: Commands from same session (SW and S )\n+          # Group 2: Commands from same directory only ( W)\n+          # Group 3: Other commands (  )\n+          # Within each group, sort by timestamp\n+          if direction == 'DESC'\n+            rows.sort_by! { |r|\n+              # Priority groups: 0=same_session(SW,S ), 1=same_cwd_only( W), 2=others(  )\n+              group = if r['_same_session'] == 1\n+                        0  # Same session (includes both SW and S )\n+                      elsif r['_same_cwd'] == 1\n+                        1  # Same directory only ( W)\n+                      else\n+                        2  # Others (  )\n+                      end\n+              [group, -(r['last_used'] || 0)]\n+            }\n+          else\n+            rows.sort_by! { |r|\n+              # Priority groups: 0=same_session(SW,S ), 1=same_cwd_only( W), 2=others(  )\n+              group = if r['_same_session'] == 1\n+                        0  # Same session (includes both SW and S )\n+                      elsif r['_same_cwd'] == 1\n+                        1  # Same directory only ( W)\n+                      else\n+                        2  # Others (  )\n+                      end\n+              [group, (r['last_used'] || 0)]\n+            }\n+          end\n+        end\n+\n+        # Apply limit after deduplication and sorting\n+        if options[:limit] && options[:limit] > 0\n+          rows = rows.first(options[:limit])\n+        end\n+\n+        if options[:interactive]\n+          if !Histlog::Utils.fzf_available?\n+            STDERR.puts \"fzf not found in PATH; please install fzf to use --interactive\"\n+          else\n+            # Build selection lines according to requested output format so that\n+            # --interactive does not implicitly change the output format.\n+            palette_i = Histlog::Utils.colors(:commands)\n+            count_col_i = palette_i[:count] || ''\n+            s_flag_col_i = palette_i[:s_flag] || ''\n+            w_flag_col_i = palette_i[:w_flag] || ''\n+            timestamp_col_i = Histlog::Utils.colors(:history)[:timestamp] || ''\n+            reset_col_i = palette_i[:reset] || ''\n+\n+            if options[:format] == 'plain'\n+              plain_lines = rows.map { |row| row['command_text'] || '' }\n+            else\n+              # Use colored fields for better visibility in fzf and tabs for reliable extraction.\n+              palette_cols = Histlog::Utils.colors(:commands)\n+              count_col_i = palette_cols[:count] || ''\n+              s_flag_col_i = palette_cols[:s_flag] || ''\n+              w_flag_col_i = palette_cols[:w_flag] || ''\n+              timestamp_col_i = Histlog::Utils.colors(:history)[:timestamp] || ''\n+              reset_col_i = palette_cols[:reset] || ''\n+\n+              plain_lines = rows.map do |row|\n+                count = (row['usage_count'] || 0).to_i.to_s.rjust(6)\n+                last_used = Histlog::Utils.format_timestamp(row['last_used'])\n+                # colored SW flags\n+                flag_colored = if row.key?('_same_session') && row['_same_session'] == 1 && row.key?('_same_cwd') && row['_same_cwd'] == 1\n+                                 \"#{s_flag_col_i}S#{w_flag_col_i}W#{reset_col_i}\"\n+                               elsif row.key?('_same_session') && row['_same_session'] == 1\n+                                 \"#{s_flag_col_i}S #{reset_col_i}\"\n+                               elsif row.key?('_same_cwd') && row['_same_cwd'] == 1\n+                                 \"#{w_flag_col_i} W#{reset_col_i}\"\n+                               else\n+                                 '  '\n+                               end\n+                count_colored = \"#{count_col_i}#{count}#{reset_col_i}\"\n+                last_used_colored = \"#{timestamp_col_i}#{last_used}#{reset_col_i}\"\n+                cmd = row['command_text'] || ''\n+                \"#{count_colored} #{last_used_colored} #{flag_colored} │ #{cmd}\"\n+              end\n+            end\n+\n+            height = options[:height] || 12\n+            sel = Histlog::Utils.fzf_select(plain_lines, height)\n+            return if sel.nil? || sel.strip.empty?\n+\n+            # Strip ANSI sequences and extract the command (last '│' separated field)\n+            sel_no_ansi = sel.gsub(/\\e\\[[\\d;]*m/, '')\n+            parts = sel_no_ansi.split(/\\s*│\\s*/)\n+            selected_command = parts.last ? parts.last.strip : sel_no_ansi.strip\n+            puts selected_command\n+            return\n+          end\n+        end\n+\n+        palette = Histlog::Utils.colors(:commands)\n+        count_col = palette[:count] || ''\n+        command_col = ''\n+        flag_col = palette[:flag] || ''\n+        s_flag_col = palette[:s_flag] || ''\n+        w_flag_col = palette[:w_flag] || ''\n+        timestamp_col = Histlog::Utils.colors(:history)[:timestamp] || ''\n+        reset_col = palette[:reset] || ''\n+\n+        begin\n+          # Header: Count, Last Used, SW flag, Command (user requested column order)\n+          # Use the same SW coloring as the data columns for consistency.\n+          header_flag = s_flag_col + 'S' + w_flag_col + 'W' + reset_col\n+          printf(\"%s%6s%s %s%-19s%s %s%s%s %s\\n\", count_col, \"Count\", reset_col, timestamp_col, \"Last Used\", reset_col, flag_col, header_flag, reset_col, \"Command\")\n+          puts \"--------------------------------------------------------------------------\"\n+        rescue Errno::EPIPE\n+          return\n+        end\n+\n+        rows.each do |row|\n+          usage = row['usage_count'].to_i\n+          count_str = usage.to_s.rjust(6)\n+          command_text = row['command_text'] || ''\n+          timestamp_str = Histlog::Utils.format_timestamp(row['last_used'])\n+\n+          # Compute flag display with color\n+          flag = '  '\n+          flag_str = ''\n+          if row.key?('_same_session') && row['_same_session'] == 1 && row.key?('_same_cwd') && row['_same_cwd'] == 1\n+            # SW: S in green, W in yellow\n+            flag_str = s_flag_col + 'S' + w_flag_col + 'W' + reset_col\n+          elsif row.key?('_same_session') && row['_same_session'] == 1\n+            flag_str = s_flag_col + 'S ' + reset_col\n+          elsif row.key?('_same_cwd') && row['_same_cwd'] == 1\n+            flag_str = w_flag_col + ' W' + reset_col\n+          else\n+            flag_str = '  '\n+          end\n+\n+          begin\n+            # Print in requested order: Count, Last Used, SW, Command\n+            printf(\"%s%s%s %s%-19s%s %s%s %s%s%s\\n\",\n+                   count_col, count_str, reset_col,\n+                   timestamp_col, timestamp_str, reset_col,\n+                   flag_col, flag_str,\n+                   command_col, command_text, reset_col)\n+          rescue Errno::EPIPE\n+            return\n+          end\n+        end\n+      ensure\n+        db&.close\n+      end\n+    end\n+\n     # Database subcommand helpers (group DB-related CLI operations)\n     module DB\n       module_function\n \n-      # OptionParser for `histlog db info` - delegates to Common\n+      # OptionParser for `histlog db info`\n       def build_info_option_parser(options)\n-        Histlog::CLI::Common.build_info_option_parser(options)\n+        OptionParser.new do |opts|\n+          opts.banner = \"Usage: histlog db info [options]\"\n+          opts.on(\"-v\", \"--verbose\", \"Show detailed system information\") { options[:verbose] = true }\n+          opts.on(\"-h\", \"--help\", \"Show this help\") do\n+            puts opts\n+            exit\n+          end\n+        end\n+      end\n+\n+      # OptionParser for `histlog db cleanup`\n+      def build_cleanup_option_parser(options)\n+        OptionParser.new do |opts|\n+          opts.banner = \"Usage: histlog db cleanup [options]\"\n+          opts.on(\"-n\", \"--dry-run\", \"Show what would be cleaned without making changes\") { options[:dry_run] = true }\n+          opts.on(\"-v\", \"--verbose\", \"Show detailed cleanup progress\") { options[:verbose] = true }\n+          opts.on(\"-h\", \"--help\", \"Show this help\") do\n+            puts opts\n+            exit\n+          end\n+        end\n       end\n \n       def db_cleanup(args)\n-        options = Histlog::Config.command_options(:cleanup)\n-        Histlog::CLI::Common.build_cleanup_option_parser(options).parse!(args)\n+        options = Histlog::Utils.command_options(:cleanup)\n+        build_cleanup_option_parser(options).parse!(args)\n \n         db_path = Histlog::Utils.get_db_path\n         unless File.exist?(db_path)\n@@ -877,7 +975,7 @@ module Histlog\n       end\n \n       def db_info(args)\n-        options = Histlog::Config.command_options(:info)\n+        options = Histlog::Utils.command_options(:info)\n         build_info_option_parser(options).parse!(args)\n \n         db_path = Histlog::Utils.get_db_path\n@@ -887,7 +985,7 @@ module Histlog\n \n         if File.exist?(db_path)\n           puts \"Database exists: Yes\"\n-          puts \"Database size: #{Histlog::CLI::Common.format_bytes(File.size(db_path))}\"\n+          puts \"Database size: #{Histlog::Utils.format_bytes(File.size(db_path))}\"\n           puts \"Last modified: #{File.mtime(db_path).strftime('%Y-%m-%d %H:%M:%S')}\"\n \n           # Connect to get database info\n@@ -1252,7 +1350,7 @@ module Histlog\n           integrity = db.get_first_value(\"PRAGMA integrity_check\")\n           puts \"Integrity check:     #{integrity}\"\n           # Database size\n-          puts \"Database size:      #{Histlog::CLI::Common.format_bytes(original_size)}\"\n+          puts \"Database size:      #{Histlog::Utils.format_bytes(original_size)}\"\n           puts \"\\nThis will run: cleanup, optimize, vacuum, integrity check.\"\n           print \"Proceed with all maintenance steps? [y/N]: \"\n           answer = STDIN.gets.chomp.downcase\n@@ -1307,12 +1405,12 @@ module Histlog\n         puts \"\\n=== Maintenance Summary ===\"\n         old_size = defined?(original_size) ? original_size : File.size(db_path)\n         new_size = File.size(db_path)\n-        puts \"Database size before: #{Histlog::CLI::Common.format_bytes(old_size)}\"\n-        puts \"Database size after:  #{Histlog::CLI::Common.format_bytes(new_size)}\"\n+        puts \"Database size before: #{Histlog::Utils.format_bytes(old_size)}\"\n+        puts \"Database size after:  #{Histlog::Utils.format_bytes(new_size)}\"\n         savings = old_size - new_size\n         percentage = savings > 0 ? (savings.to_f / old_size * 100).round(2) : 0\n         if savings > 0\n-          puts \"Space saved:         #{Histlog::CLI::Common.format_bytes(savings)} (#{percentage}%)\"\n+          puts \"Space saved:         #{Histlog::Utils.format_bytes(savings)} (#{percentage}%)\"\n         else\n           puts \"Space saved:         0 (already optimized)\"\n         end\n@@ -1364,6 +1462,7 @@ module Histlog\n         puts \"  integrity         Check database integrity\"\n         puts \"  migrate           Update database schema\"\n         puts \"  optimize          Optimize database performance\"\n+        puts \"  vacuum            Run VACUUM on the database to reclaim space\"\n         puts \"  full-maintenance  Run all maintenance operations (cleanup, optimize, vacuum, integrity check)\"\n         puts \"\"\n         puts \"Run 'histlog db <subcommand> --help' for command-specific help where available.\"\n@@ -1789,9 +1888,7 @@ module Histlog\n \n         # Command text filtering\n         opts.on(\"--fuzzy\", \"Enable fuzzy (fzf-like) matching for command text\") { options[:fuzzy] = true }\n-        opts.on(\"--command [PATTERN]\", \"Filter by command text (with optional fuzzy matching). If given without PATTERN, lists unique commands with counts\") do |pattern|\n-          # If pattern is nil, user asked for the interactive list of unique commands\n-          options[:command_list] = pattern.nil?\n+        opts.on(\"--command [PATTERN]\", \"Filter by command text (with optional fuzzy matching)\") do |pattern|\n           options[:command_pattern] = pattern\n         end\n \n@@ -1958,6 +2055,8 @@ module Histlog\n             cmd_params << \"%#{options[:command_pattern]}%\"\n           end\n \n+          # NOTE: unique-command listing was moved to the new `commands` command.\n+\n           where_clause = cmd_where.any? ? \"WHERE \" + cmd_where.join(\" AND \") : \"\"\n           q = <<-SQL\n             SELECT cmd.command AS command_text, COUNT(*) AS usage_count\n@@ -1969,37 +2068,120 @@ module Histlog\n             LIMIT 1000\n           SQL\n \n-            begin\n-              rows = db.execute(q, cmd_params)\n-              if rows.empty?\n-                puts \"No commands found.\"\n-              else\n-                if options[:interactive]\n-                  # Interactive selection over unique commands\n+          begin\n+            rows = db.execute(q, cmd_params)\n+            if rows.empty?\n+              puts \"No commands found.\"\n+            else\n+              if options[:interactive]\n+                # Interactive selection over unique commands\n+                # Build selection lines according to requested output format\n+                if options[:format] == 'plain'\n+                  # plain: just command text\n                   plain_lines = rows.map { |r| r['command_text'] || '' }\n-                  if !Histlog::Utils.fzf_available?\n-                    STDERR.puts \"fzf not found in PATH; please install fzf to use --interactive\"\n-                  else\n-                    height = options[:height] || 10\n-                    sel = Histlog::Utils.fzf_select(plain_lines, height)\n-                    if sel && !sel.empty?\n+                else\n+                  # Table-like interactive lines should match the displayed table order:\n+                  # Count, Last Used (unknown for unique summary -> spaces), SW flag placeholder, Command\n+                  palette_cols = Histlog::Utils.colors(:commands)\n+                  count_col_i = palette_cols[:count] || ''\n+                  s_flag_col_i = palette_cols[:s_flag] || ''\n+                  w_flag_col_i = palette_cols[:w_flag] || ''\n+                  timestamp_col_i = Histlog::Utils.colors(:history)[:timestamp] || ''\n+                  reset_col_i = palette_cols[:reset] || ''\n+\n+                  plain_lines = rows.map do |r|\n+                    count = (r['usage_count'] || 0).to_i.to_s.rjust(6)\n+                    last_used = \"                   \" # placeholder 19 chars\n+                    # SW placeholder (no context info here)\n+                    flag_colored = '  '\n+                    count_colored = \"#{count_col_i}#{count}#{reset_col_i}\"\n+                    last_used_colored = \"#{timestamp_col_i}#{last_used}#{reset_col_i}\"\n+                    cmd = r['command_text'] || ''\n+                    \"#{count_colored} #{last_used_colored} #{flag_colored} │ #{cmd}\"\n+                  end\n+                end\n+                if !Histlog::Utils.fzf_available?\n+                  STDERR.puts \"fzf not found in PATH; please install fzf to use --interactive\"\n+                else\n+                  height = options[:height] || 10\n+                  sel = Histlog::Utils.fzf_select(plain_lines, height)\n+                  if sel && !sel.empty?\n+                    if options[:format] == 'plain'\n                       puts sel\n+                    else\n+                      sel_no_ansi = sel.gsub(/\\e\\[[\\d;]*m/, '')\n+                      parts = sel_no_ansi.split(/\\s*│\\s*/)\n+                      selected_command = parts.last ? parts.last.strip : sel_no_ansi.strip\n+                      puts selected_command\n                     end\n-                    db.close if db\n-                    return\n                   end\n-                else\n-                  rows.each do |r|\n-                    puts \"%6d  %s\" % [r['usage_count'] || 0, r['command_text'] || '']\n+                  db.close if db\n+                  return\n+                end\n+              else\n+                # Non-interactive table output: always include SW flag column\n+                palette = Histlog::Utils.colors(:commands)\n+                count_col = palette[:count] || ''\n+                flag_col = palette[:flag] || ''\n+                reset_col = palette[:reset] || ''\n+                command_col = ''\n+\n+                begin\n+                  printf(\"%s %s%6s%s %s\\n\", \"SW\", count_col, \"Count\", reset_col, \"Command\")\n+                  puts \"-----------------------------------------\"\n+                rescue Errno::EPIPE\n+                  return\n+                end\n+\n+                env_session_id = ENV['__histlog_session_id']&.to_i\n+                cwd = File.expand_path(Dir.pwd)\n+\n+                rows.each do |r|\n+                  usage = r['usage_count'] || 0\n+                  count_str = usage.to_i.to_s.rjust(6)\n+                  command_text = r['command_text'] || ''\n+\n+                  # Determine flags via small DB checks\n+                  same_session = 0\n+                  same_cwd = 0\n+                  begin\n+                    if env_session_id && env_session_id > 0\n+                      chk = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id WHERE ct.command = ? AND c.session_id = ?\", [command_text, env_session_id])\n+                      same_session = chk.to_i > 0 ? 1 : 0\n+                    end\n+                  rescue SQLite3::Exception\n+                    same_session = 0\n+                  end\n+                  begin\n+                    chk2 = db.get_first_value(\"SELECT COUNT(*) FROM commands c JOIN cmd_texts ct ON c.cmd_text_id = ct.id JOIN paths p ON (c.path_new_id = p.id OR c.path_old_id = p.id) WHERE ct.command = ? AND p.path = ?\", [command_text, cwd])\n+                    same_cwd = chk2.to_i > 0 ? 1 : 0\n+                  rescue SQLite3::Exception\n+                    same_cwd = 0\n+                  end\n+\n+                  flag = '  '\n+                  if same_session == 1 && same_cwd == 1\n+                    flag = 'SW'\n+                  elsif same_session == 1\n+                    flag = 'S '\n+                  elsif same_cwd == 1\n+                    flag = ' W'\n+                  end\n+\n+                  begin\n+                    puts \"#{flag_col}#{flag}#{reset_col} #{count_col}#{count_str}#{reset_col} #{command_col}#{command_text}#{reset_col}\"\n+                  rescue Errno::EPIPE\n+                    return\n                   end\n                 end\n               end\n-            rescue SQLite3::Exception => e\n-              STDERR.puts \"Database error: #{e.message}\"\n-            ensure\n-              db.close if db\n             end\n-            return\n+          rescue SQLite3::Exception => e\n+            STDERR.puts \"Database error: #{e.message}\"\n+          ensure\n+            db.close if db\n+          end\n+          return\n         end\n \n         # Build the base query\n@@ -2329,7 +2511,20 @@ module Histlog\n         # selected command (one line). This makes `histlog query --interactive` usable by\n         # shell helpers like `hlc` which expect a single command on stdout.\n         if options[:interactive]\n-          plain_lines = results.map { |r| r['cmd_text'] || '' }\n+          # Build selection lines according to requested output format so that\n+          # --interactive does not implicitly change the output format.\n+          if options[:format] == 'plain'\n+            plain_lines = results.map { |r| r['cmd_text'] || '' }\n+          else\n+            # Build table-like selection lines: Count, Last Used, SW placeholder, Command\n+            plain_lines = results.map do |r|\n+              count = (r['usage_count'] || 0).to_i.to_s.rjust(6)\n+              last_used = Histlog::Utils.format_timestamp(r['start_time'])\n+              sw = '  '\n+              \"%s %s %s %s\" % [count, last_used, sw, (r['cmd_text'] || '')]\n+            end\n+          end\n+\n           if plain_lines.empty?\n             puts \"No commands found.\"\n             db.close if db\n@@ -2347,7 +2542,13 @@ module Histlog\n               return\n             end\n \n-            puts sel\n+            # Print the command itself. If table-like, strip leading count prefix.\n+            if options[:format] == 'plain'\n+              puts sel\n+            else\n+              puts sel.sub(/^\\s*\\d+\\s+/, '')\n+            end\n+\n             db.close if db\n             return\n           end\n@@ -2460,7 +2661,7 @@ module Histlog\n       if file_path.nil?\n         # If user specified a format (shell) but no file, suggest the default history file for that shell\n         if options[:format] && %w[zsh bash fish nu].include?(options[:format])\n-          candidate = Histlog::Config.default_history_for_shell(options[:format])\n+          candidate = Histlog::Utils.default_history_for_shell(options[:format])\n           if candidate && File.exist?(candidate)\n             # Ask user whether to use the detected default file\n             print \"No file specified. Use default history file for #{options[:format]}: #{candidate}? [Y/n] \"\n@@ -3355,7 +3556,7 @@ module Histlog\n       # Timestamp: #6272a4, Command: default, Result: ✓ #50fa7b, ✗ #ff5555, Duration: #bd93f9\n       begin\n         # Colors\n-        palette = Histlog::Config.colors(:history)\n+        palette = Histlog::Utils.colors(:history)\n         ts_col = palette[:timestamp]\n         dur_col = palette[:duration]\n         success_col = palette[:status_success]\n@@ -3424,7 +3625,7 @@ module Histlog\n \n           # Print each column once: color start, padded text, reset, etc.\n           # Match header layout: session (4 chars), timestamp (19 chars, left), two spaces, duration (8 chars normally, 9 when null), space, status, then 4 spaces before command\n-          printf(\"%s%4s%s %s%-19s%s  %s%s%s %s    %s\\n\",\n+          printf(\"%s%4s%s %s%-19s%s  %s%s%s %s %s\\n\",\n                  session_color, session_str, reset_col,\n                  ts_color, ts_display, reset_col,\n                  dur_color, dur_field, reset_col,\n@@ -3728,6 +3929,26 @@ module Histlog\n       end\n     end\n \n+    # Helper to format bytes\n+    def format_bytes(bytes)\n+      return '0 B' if bytes == 0\n+\n+      units = %w[B KB MB GB TB]\n+      size = bytes.to_f\n+      unit_index = 0\n+\n+      while size >= 1024 && unit_index < units.length - 1\n+        size /= 1024\n+        unit_index += 1\n+      end\n+\n+      if unit_index == 0\n+        \"#{size.to_i} #{units[unit_index]}\"\n+      else\n+        \"#{size.round(2)} #{units[unit_index]}\"\n+      end\n+    end\n+\n     # Helper to truncate command text\n     def truncate_command(command, max_length = 60)\n       return 'N/A' unless command\n@@ -3781,11 +4002,8 @@ module Histlog\n       end\n     end\n \n-    # SQLite database setup\n-    def get_db_path\n-      # Prefer explicit environment variable if provided, else default to configured db path\n-      Histlog::Config.db_path\n-    end\n+    # SQLite database setup - method removed to avoid circular reference\n+    # Use Histlog::Utils.get_db_path directly\n \n     # parse_time_span_to_seconds is implemented as a module-level helper below\n     # to avoid nested definitions. Use that implementation for parsing --since.\n@@ -3799,12 +4017,12 @@ module Histlog\n \n     # Expose path accessor\n     def path\n-      Histlog::Config.db_path\n+      Histlog::Utils.get_db_path\n     end\n \n     # initialize_db implementation\n     def initialize_db\n-      db_path = Histlog::Config.db_path\n+      db_path = Histlog::Utils.get_db_path\n       db_dir = File.dirname(db_path)\n       FileUtils.mkdir_p(db_dir) unless Dir.exist?(db_dir)\n       db = SQLite3::Database.new(db_path)\n@@ -4619,15 +4837,15 @@ def fish_config\n \n     function hlc\n       # Interactive command selection and place into the commandline buffer\n-      set -l cmd (histlog query --interactive $argv | head -n1)\n+      set -l cmd (histlog commands --context --plain --interactive $argv)\n       if test -n \"$cmd\"\n         commandline -r \"$cmd\"\n       end\n     end\n \n     function hlC\n       # Non-interactive: select top command and place into the commandline buffer\n-      set -l cmd (histlog query --limit 1 $argv | head -n1)\n+      set -l cmd (histlog commands --context --plain --limit 1 $argv)\n       if test -n \"$cmd\"\n         commandline -r \"$cmd\"\n       end\n@@ -4683,7 +4901,7 @@ def zsh_config\n \n       hlc() {\n         local cmd\n-        cmd=\"$($__histlog_path query --command --interactive \"$@\" | head -n1)\"\n+        cmd=\"$($__histlog_path commands --context --plain --interactive \"$@\")\"\n         if [ -n \"$cmd\" ]; then\n           # Push cmd into zsh edit buffer\n           print -z -- \"$cmd\"\n@@ -4692,7 +4910,7 @@ def zsh_config\n \n       hlC() {\n         local cmd\n-        cmd=\"$($__histlog_path query --command --limit 1 \"$@\" | head -n1)\"\n+        cmd=\"$($__histlog_path commands --context --plain --limit 1 \"$@\")\"\n         if [ -n \"$cmd\" ]; then\n           print -z -- \"$cmd\"\n         fi\n@@ -4736,12 +4954,12 @@ def nu_config\n       if $target != $null { cd $target }\n     }\n       def hlc [] {\n-        let cmd = ($env.__histlog_path query --command --interactive $env.ARGS | first)\n+        let cmd = ($env.__histlog_path commands --context --plain --interactive $env.ARGS | first)\n         if $cmd != $null { echo $cmd }\n       }\n \n       def hlC [] {\n-        let cmd = ($env.__histlog_path query --command --limit 1 $env.ARGS | first)\n+        let cmd = ($env.__histlog_path commands --context --plain --limit 1 $env.ARGS | first)\n         if $cmd != $null { echo $cmd }\n       }\n   NUSHELL\n@@ -5412,8 +5630,9 @@ class HistlogApp\n     end\n \n     # Define known commands\n-    known_commands = ['query', 'history', 'sessions', 'import', 'help', 'init', 'paths',\n-                      'mcp', 'db', '_on_session', '_on_preexec', '_on_postcmd']\n+    known_commands = ['query', 'sessions', 'import', 'help', 'init', 'paths', 'commands',\n+                      'stats', 'cleanup', 'info', 'mcp', 'maintain', 'db',\n+                      '_on_session', '_on_preexec', '_on_postcmd']\n \n     if !known_commands.include?(@command) && !@command.start_with?('-')\n       STDERR.puts \"Error: Unknown command '#{@command}'\"\n@@ -5441,6 +5660,8 @@ class HistlogApp\n       Histlog::Import.import_shell_history(@argv)\n     when 'paths'\n       Histlog::CLI.show_paths_command(@argv)\n+    when 'commands'\n+      Histlog::CLI.show_commands_command(@argv)\n     when 'cleanup'\n       STDERR.puts \"The 'cleanup' command has moved to 'histlog db cleanup'.\"\n       STDERR.puts \"Run: histlog db cleanup --help\"\n@@ -5552,6 +5773,9 @@ class HistlogApp\n       Histlog::CLI::DB.db_migrate(args)\n     when 'optimize'\n       Histlog::CLI::DB.db_optimize(args)\n+    when 'vacuum'\n+      # Convenience shortcut for running only VACUUM\n+      Histlog::CLI::DB.db_maintain(['--vacuum'])\n     when 'full-maintenance'\n       Histlog::CLI::DB.db_full_maintenance(args)\n     when nil, '--help', '-h', 'help'\n@@ -5636,7 +5860,7 @@ go with full-maintenance delete `db all` command\n ---\n \n \n-update query duration formatting and Exit column width:\n+update query duration formatting and Exit column width to 4 chars for alignment:\n \n ```\n  bsh ❯ histlog query\n@@ -5668,16 +5892,3 @@ keep duration formatting consistent and perfect aligned for humans, keep in mind\n Exit should always be 4 characters wide for alignment.\n \n ---\n-\n-remove `histlog history` command and implementation - use `histlog query` instead\n-\n----\n-\n-Add a --fuzzy option to query that enables fzf-like fuzzy matching for command text.\n-For histlog query --command:\n-It should filter by command text, e.g., --command \"git\" shows all commands containing \"git\".\n-If --command is given without an argument, show a list of unique commands (with usage counts), sorted by frequency, to help users pick or discover commands they’ve run before.\n-If an argument is given, filter as usual (with fuzzy if --fuzzy is also set).\n-please add --command, --fuzzy, to query\n-also add --fuzzy to paths\n----","raw_url":"https://github.com/wstein/bin/raw/5c2b19bc9d2e81c27bd0a2eb9aa036dd60bc07fd/histlog","sha":"5c0714c32d303516c5206b9d90d7f6ce3911ecb4","status":"modified"}]}