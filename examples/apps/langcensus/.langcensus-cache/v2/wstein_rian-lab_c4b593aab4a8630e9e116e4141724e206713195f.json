{"files":[{"additions":18,"blob_url":"https://github.com/wstein/rian-lab/blob/c4b593aab4a8630e9e116e4141724e206713195f/SELFHOST.md","changes":22,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=c4b593aab4a8630e9e116e4141724e206713195f","deletions":4,"filename":"SELFHOST.md","patch":"@@ -271,10 +271,24 @@ operators, and parens (the slice-1 fixpoint test). Direct evidence that the\n   `_` separators, decimals, and signed/unsigned exponents (the slice-4 corpus +\n   a number-lexeme teeth test).\n \n-**Next:** the jump from `expr_tokens/1` to the full **`tokenize/1`** stream that\n-`Rian.Decl` actually consumes — **significant newlines (`{:nl}`), `;`, and the\n-`@annot` lane** — plus escapes inside literals and brackets. That parity is the\n-gateway to porting the *declaration* front-end, not just the expression lexer.\n+- **Slice 5 — `tokenize/1` parity (the declaration stream).** The jump from\n+  `expr_tokens/1` to the full **`tokenize/1`** stream `Rian.Decl` actually\n+  consumes: **significant newlines** (`{:nl}`, runs collapsed via `dedup_nl` +\n+  leading/trailing drop, matching `Rian.Lexer.collapse_nl`), `;` `,`, **`@annot`**,\n+  brackets `[] {} %{`, line comments (`# …`), and the full operator set\n+  (`-> .. := |> <> <~ <- . | : &`) — every operator unified as one\n+  `TOp(String)`, mirroring the reference's `@multi`/`@single` longest-match. The\n+  port now agrees with **`Rian.Lexer.tokenize/1`** (not just `expr_tokens/1`) over\n+  a corpus of multi-line declarations, punctuation, annotations, and operators\n+  (`Fixpoint.check/4` takes the reference tokenizer; slice-5 corpus + a\n+  `{:nl}`-divergence teeth test). **This is the gateway rung: the Rian lexer now\n+  produces the declaration token stream the parser will consume.**\n+\n+**Next:** port the **declaration parser** (`Rian.Pratt` + `Rian.Decl`) in Rian and\n+fixpoint-diff its AST/IR against the reference — turning the parser spike from a\n+demo into a checked, equivalence-locked stage (the same method, AST projection\n+instead of token projection). Remaining lexer gaps (heredocs `\"\"\"`, string-body\n+escapes, `\\u{…}` char escapes) are small and unforced by the parser corpus.\n \n ## The whole compiler as one Rian artifact\n ","raw_url":"https://github.com/wstein/rian-lab/raw/c4b593aab4a8630e9e116e4141724e206713195f/SELFHOST.md","sha":"8ec3a142dbd6f55687203cd99b7e1bc22f572eca","status":"modified"},{"additions":98,"blob_url":"https://github.com/wstein/rian-lab/blob/c4b593aab4a8630e9e116e4141724e206713195f/examples%2Frian%2Fselfhost_lexer_v2.rian","changes":164,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_lexer_v2.rian?ref=c4b593aab4a8630e9e116e4141724e206713195f","deletions":66,"filename":"examples/rian/selfhost_lexer_v2.rian","patch":"@@ -1,82 +1,121 @@\n # ===========================================================================\n-# SELF-HOSTING — porting the REAL Rian lexer to Rian (slices 1-2)\n+# SELF-HOSTING — porting the REAL Rian lexer to Rian (slices 1-5)\n # ===========================================================================\n-# The toy lexer (selfhost_lexer.rian) tokenized arithmetic only. This grows the\n-# Rian-written lexer toward the *real* `Rian.Lexer` token vocabulary, one slice\n-# at a time, checked for agreement against the reference by `Rian.Fixpoint`.\n+# Grows the Rian-written lexer toward the *real* `Rian.Lexer`, one slice at a\n+# time, checked for agreement against the reference by `Rian.Fixpoint`.\n #\n-# Slice 1 added **identifiers and keywords** on top of numbers/operators/parens —\n-# writable because `Char` is a real ordinal type (ADR-0036): the scanner\n-# classifies bytes with `Char` comparisons (`c >= 'a' and c <= 'z'`) and folds\n-# digit runs with ordinal arithmetic (`acc * 10 + c - '0'`).\n+#   Slice 1  identifiers + keywords (Char ordinals, digit runs)\n+#   Slice 2  comparison + word operators (`< > <= >= == !=`, and/or/not/in/rem/div)\n+#   Slice 3  string (`\"…\"`) and char (`'X'`) literals\n+#   Slice 4  number lexemes — `_` separators, decimals, exponents (`norm_num`)\n+#   Slice 5  **`tokenize/1` parity** — the full declaration stream `Rian.Decl`\n+#            consumes (not just the expression stream): **significant newlines**\n+#            (`{:nl}`, runs collapsed, leading/trailing dropped), `;` `,`,\n+#            **`@annot`**, brackets `[] {} %{`, line comments `# …`, and the full\n+#            operator set (`-> .. := |> <> <~ <- . | : &`, plus those above) —\n+#            every operator now a single `TOp(String)`, mirroring the reference's\n+#            `@multi`/`@single` longest-match.\n #\n-# Slice 2 adds **comparison operators** (`< > <= >= == !=`, with two-char\n-# longest-match via `[a, b | rest]` peeking), the **word-operators**\n-# (`and or not in rem div`, which the reference lexes as `{:op, w}` not keywords),\n-# and the **full 16-keyword set** of `Rian.Lexer.@keywords`.\n-#\n-# Slice 3 adds **string** (`\"…\"`) and **char** (`'X'`) literals — scanned\n-# char-by-char in Rian (`scan_str`/`scan_char`) over `Char` comparisons, with the\n-# string body rebuilt via `Prim.str_from_chars` and the char's codepoint via\n-# `Prim.char_code`.\n-#\n-# Slice 4 adds **number lexemes** — `_` separators, decimals, and exponents. The\n-# model changed: `TNum` now carries the source *lexeme* (a `String`), not a folded\n-# `Int64`, because the reference keeps the text (`1_000` stays `1_000`) and applies\n-# `norm_num` (a bare exponent gains `.0`: `1e9` -> `1.0e9`). The scanner\n-# accumulates the lexeme char-by-char (`lex_int`/`lex_frac`/`lex_exp`) mirroring\n-# `Rian.Lexer.@num_re`. Still out of slice: escapes inside literals, heredocs,\n-# `@annot`, brackets `[] {} %{`, the assignment/arrow operators, and significant\n-# newlines (the jump to the full `tokenize/1` stream `Rian.Decl` consumes).\n-#\n-# Char-class tests are *inlined* in `when` guards (BEAM guards cannot call a\n-# user function — this leaks the host's guard constraint into source and will\n-# be lifted once protocol-bounded predicates land per ADR-0042 part 2), and the\n-# name is built by accumulating `Char`s and `Prim.str_from_chars`-ing them\n-# (ADR-0047 §2 portable prelude; `Prim` is the target-internal namespace).\n+# Still out of slice: heredocs (`\"\"\"`), escapes inside string bodies, and the\n+# `\\u{…}` char escape. Char-class tests are inlined in `when` guards (BEAM guards\n+# cannot call a user function); names/numbers are accumulated as `Vec(Char)` and\n+# `Prim.str_from_chars`-ed (ADR-0047 portable prelude).\n # ---------------------------------------------------------------------------\n \n mod SelfhostLexerV2 do\n-  # The lexer's own token stream — projected onto `Rian.Lexer`'s tokens by the\n-  # fixpoint harness (TNum -> {:num}, TId -> {:id}, TKw -> {:kw}, TOp -> {:op}, …).\n-  type Tok := TNum(String) | TPlus | TMinus | TStar | TSlash | TLP | TRP | TId(String) | TKw(String) | TOp(String) | TStr(String) | TChar(Int64)\n+  # The lexer's own token sum — projected onto `Rian.Lexer.tokenize/1` by the\n+  # fixpoint harness. Operators (single/multi/word) are all `TOp`; structural\n+  # punctuation and the significant newline get their own nullary variants.\n+  type Tok :=\n+      TNum(String) | TId(String) | TKw(String) | TOp(String) | TStr(String)\n+    | TChar(Int64) | TAnnot(String)\n+    | TLP | TRP | TLBracket | TRBracket | TLBrace | TRBrace | TMapOpen\n+    | TComma | TSemi | TNL\n \n-  pub def tokenize(src String) Vec(Tok) := lex(Prim.str_chars(src))\n+  # Full token stream with significant newlines collapsed — matches `tokenize/1`.\n+  pub def tokenize(src String) Vec(Tok) := collapse(lex(Prim.str_chars(src)))\n \n   def lex(cs Vec(Char)) Vec(Tok)\n   def lex([]) := []\n-  def lex([c | rest]) when c == ' ' := lex(rest)\n-  # string literal: scan to the closing `\"` (slice 3)\n+  # whitespace (space, tab, carriage-return) — skipped\n+  def lex([c | rest]) when c == ' ' or c == '\\t' or c == '\\r' := lex(rest)\n+  # significant newline (collapsed later)\n+  def lex(['\\n' | rest]) := [TNL | lex(rest)]\n+  # line comment to end-of-line (the newline survives, lexed next as TNL)\n+  def lex(['#' | rest]) := lex(skip_comment(rest))\n+  # map-open `%{` (the only use of `%`)\n+  def lex(['%', '{' | rest]) := [TMapOpen | lex(rest)]\n+  # string / char literals\n   def lex(['\"' | rest]) := scan_str(rest, [])\n-  # char literal `'X'`: one codepoint between single quotes (slice 3, no escapes)\n   def lex(['\\'' | rest]) := scan_char(rest)\n-  # two-char comparison operators FIRST (longest match: `<=` before `<`)\n+  # `@name` annotation\n+  def lex(['@' | rest]) := scan_annot(rest, [])\n+  # brackets & punctuation\n+  def lex(['(' | rest]) := [TLP | lex(rest)]\n+  def lex([')' | rest]) := [TRP | lex(rest)]\n+  def lex(['[' | rest]) := [TLBracket | lex(rest)]\n+  def lex([']' | rest]) := [TRBracket | lex(rest)]\n+  def lex(['{' | rest]) := [TLBrace | lex(rest)]\n+  def lex(['}' | rest]) := [TRBrace | lex(rest)]\n+  def lex([',' | rest]) := [TComma | lex(rest)]\n+  def lex([';' | rest]) := [TSemi | lex(rest)]\n+  # multi-char operators (longest match first)\n+  def lex(['-', '>' | rest]) := [TOp(\"->\") | lex(rest)]\n+  def lex(['.', '.' | rest]) := [TOp(\"..\") | lex(rest)]\n+  def lex([':', '=' | rest]) := [TOp(\":=\") | lex(rest)]\n+  def lex(['|', '>' | rest]) := [TOp(\"|>\") | lex(rest)]\n+  def lex(['<', '>' | rest]) := [TOp(\"<>\") | lex(rest)]\n+  def lex(['<', '~' | rest]) := [TOp(\"<~\") | lex(rest)]\n+  def lex(['<', '-' | rest]) := [TOp(\"<-\") | lex(rest)]\n   def lex(['<', '=' | rest]) := [TOp(\"<=\") | lex(rest)]\n   def lex(['>', '=' | rest]) := [TOp(\">=\") | lex(rest)]\n   def lex(['=', '=' | rest]) := [TOp(\"==\") | lex(rest)]\n   def lex(['!', '=' | rest]) := [TOp(\"!=\") | lex(rest)]\n-  def lex(['+' | rest]) := [TPlus | lex(rest)]\n-  def lex(['-' | rest]) := [TMinus | lex(rest)]\n-  def lex(['*' | rest]) := [TStar | lex(rest)]\n-  def lex(['/' | rest]) := [TSlash | lex(rest)]\n+  # single-char operators\n+  def lex(['+' | rest]) := [TOp(\"+\") | lex(rest)]\n+  def lex(['-' | rest]) := [TOp(\"-\") | lex(rest)]\n+  def lex(['*' | rest]) := [TOp(\"*\") | lex(rest)]\n+  def lex(['/' | rest]) := [TOp(\"/\") | lex(rest)]\n   def lex(['<' | rest]) := [TOp(\"<\") | lex(rest)]\n   def lex(['>' | rest]) := [TOp(\">\") | lex(rest)]\n-  def lex(['(' | rest]) := [TLP | lex(rest)]\n-  def lex([')' | rest]) := [TRP | lex(rest)]\n+  def lex(['.' | rest]) := [TOp(\".\") | lex(rest)]\n+  def lex(['|' | rest]) := [TOp(\"|\") | lex(rest)]\n+  def lex([':' | rest]) := [TOp(\":\") | lex(rest)]\n+  def lex(['&' | rest]) := [TOp(\"&\") | lex(rest)]\n+  # numbers (slice 4): accumulate the source lexeme\n   def lex([c | rest]) when c >= '0' and c <= '9' := lex_int(rest, [c])\n+  # identifiers / keywords / word-operators (slices 1-2)\n   def lex([c | rest]) when (c >= 'a' and c <= 'z') or (c >= 'A' and c <= 'Z') or c == '_' := lex_id([c | rest], [])\n+  # anything else: skip (the reference raises; the corpus avoids unknown bytes)\n   def lex([_ | rest]) := lex(rest)\n \n-  # NUMBERS (slice 4) — accumulate the *lexeme* (reversed) rather than fold a\n-  # value, because the reference keeps the source text (underscores included).\n-  # The grammar mirrors `Rian.Lexer.@num_re`:\n-  #     \\d[\\d_]*  ( . \\d[\\d_]* )?  ( [eE][+-]? \\d+ )?\n-  # and bakes in the `norm_num` rule: a BARE exponent (no decimal) gets `.0`\n-  # inserted and the marker lowercased (`1e9`/`1E9` -> `1.0e9`); a decimal keeps\n-  # its marker as written (`1.0E9` stays `1.0E9`). `acc` is the reversed lexeme;\n-  # `rev` + `Prim.str_from_chars` rebuild it in source order (portable, no FFI).\n+  # ── significant-newline collapse (matches Rian.Lexer.collapse_nl) ─────────\n+  # collapse runs to one TNL, then drop a leading and a trailing TNL.\n+  def collapse(toks Vec(Tok)) Vec(Tok) := drop_trailing_nl(drop_leading_nl(dedup_nl(toks)))\n+\n+  def dedup_nl(toks Vec(Tok)) Vec(Tok)\n+  def dedup_nl([]) := []\n+  def dedup_nl([TNL, TNL | rest]) := dedup_nl([TNL | rest])\n+  def dedup_nl([t | rest]) := [t | dedup_nl(rest)]\n+\n+  def drop_leading_nl(toks Vec(Tok)) Vec(Tok)\n+  def drop_leading_nl([TNL | rest]) := rest\n+  def drop_leading_nl(toks) := toks\n+\n+  # at most one trailing TNL survives dedup; drop it via reverse / drop-lead / reverse\n+  def drop_trailing_nl(toks Vec(Tok)) Vec(Tok) := rev(drop_leading_nl(rev(toks, [])), [])\n+\n+  # ── comment / annotation scanners ────────────────────────────────────────\n+  def skip_comment(cs Vec(Char)) Vec(Char)\n+  def skip_comment([]) := []\n+  def skip_comment(['\\n' | rest]) := ['\\n' | rest]\n+  def skip_comment([_ | rest]) := skip_comment(rest)\n+\n+  def scan_annot(cs Vec(Char), chars Vec(Char)) Vec(Tok)\n+  def scan_annot([c | rest], chars) when (c >= 'a' and c <= 'z') or (c >= 'A' and c <= 'Z') or (c >= '0' and c <= '9') or c == '_' := scan_annot(rest, [c | chars])\n+  def scan_annot(cs, chars) := [TAnnot(Prim.str_from_chars(rev(chars, []))) | lex(cs)]\n \n-  # integer part (no decimal point seen yet)\n+  # ── numbers (slice 4): accumulate the lexeme (reversed), then str_from_chars ─\n   def lex_int(cs Vec(Char), acc Vec(Char)) Vec(Tok)\n   def lex_int([c | rest], acc) when (c >= '0' and c <= '9') or c == '_' := lex_int(rest, [c | acc])\n   def lex_int(['.', d | rest], acc) when d >= '0' and d <= '9' := lex_frac(rest, [d, '.' | acc])\n@@ -86,39 +125,32 @@ mod SelfhostLexerV2 do\n   def lex_int(['E', d | rest], acc) when d >= '0' and d <= '9' := lex_exp(rest, [d, 'e', '0', '.' | acc])\n   def lex_int(cs, acc) := [TNum(Prim.str_from_chars(rev(acc, []))) | lex(cs)]\n \n-  # fractional part (decimal point already consumed) — exponent marker case kept\n   def lex_frac(cs Vec(Char), acc Vec(Char)) Vec(Tok)\n   def lex_frac([c | rest], acc) when (c >= '0' and c <= '9') or c == '_' := lex_frac(rest, [c | acc])\n   def lex_frac([e, s, d | rest], acc) when (e == 'e' or e == 'E') and (s == '+' or s == '-') and (d >= '0' and d <= '9') := lex_exp(rest, [d, s, e | acc])\n   def lex_frac([e, d | rest], acc) when (e == 'e' or e == 'E') and (d >= '0' and d <= '9') := lex_exp(rest, [d, e | acc])\n   def lex_frac(cs, acc) := [TNum(Prim.str_from_chars(rev(acc, []))) | lex(cs)]\n \n-  # exponent digits (\\d+ only — no underscores, no decimal point)\n   def lex_exp(cs Vec(Char), acc Vec(Char)) Vec(Tok)\n   def lex_exp([c | rest], acc) when c >= '0' and c <= '9' := lex_exp(rest, [c | acc])\n   def lex_exp(cs, acc) := [TNum(Prim.str_from_chars(rev(acc, []))) | lex(cs)]\n \n-  # a string literal: accumulate chars (reversed) until the closing `\"`, then\n-  # rebuild in source order (portable, no host FFI). (Slice 3 — no escapes yet.)\n+  # ── string / char literals (slice 3) ─────────────────────────────────────\n   def scan_str(cs Vec(Char), acc Vec(Char)) Vec(Tok)\n   def scan_str(['\"' | rest], acc) := [TStr(Prim.str_from_chars(rev(acc, []))) | lex(rest)]\n   def scan_str([c | rest], acc) := scan_str(rest, [c | acc])\n   def scan_str([], acc) := [TStr(Prim.str_from_chars(rev(acc, [])))]\n \n-  # a char literal `'X'`: the codepoint of the single char, then the closing `'`.\n   def scan_char(cs Vec(Char)) Vec(Tok)\n   def scan_char([c, q | rest]) when q == '\\'' := [TChar(Prim.char_code(c)) | lex(rest)]\n \n-  # a run of identifier chars -> the name string, classified as keyword / word-op\n-  # / id. `chars` accumulates in reverse, then `rev` + `Prim.str_from_chars`\n-  # rebuild the name in source order (portable: no host FFI).\n+  # ── identifiers / keyword classification (slices 1-2) ─────────────────────\n   def lex_id(cs Vec(Char), chars Vec(Char)) Vec(Tok)\n   def lex_id([c | rest], chars) when (c >= 'a' and c <= 'z') or (c >= 'A' and c <= 'Z') or (c >= '0' and c <= '9') or c == '_' := lex_id(rest, [c | chars])\n   def lex_id(cs, chars) := [classify(Prim.str_from_chars(rev(chars, []))) | lex(cs)]\n \n-  # the full keyword set (Rian.Lexer.@keywords); the word-operators\n-  # (and/or/not/in/rem/div) are `{:op, w}` in the reference, not keywords; the\n-  # rest is a plain identifier.\n+  # word-operators are `{:op, w}` in the reference; the 16 keywords are `{:kw, w}`;\n+  # everything else is an identifier.\n   def classify(name String) Tok\n   def classify(\"and\") := TOp(\"and\")\n   def classify(\"or\") := TOp(\"or\")","raw_url":"https://github.com/wstein/rian-lab/raw/c4b593aab4a8630e9e116e4141724e206713195f/examples%2Frian%2Fselfhost_lexer_v2.rian","sha":"f4531b4dff8cc9277c69e5b8638e6c9781868348","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/c4b593aab4a8630e9e116e4141724e206713195f/lib%2Frian%2Ffixpoint.ex","changes":12,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Ffixpoint.ex?ref=c4b593aab4a8630e9e116e4141724e206713195f","deletions":5,"filename":"lib/rian/fixpoint.ex","patch":"@@ -37,16 +37,18 @@ defmodule Rian.Fixpoint do\n \n   @doc \"\"\"\n   For each `input` in `corpus`, compare the Rian lexer's projected output against\n-  `Rian.Lexer.expr_tokens/1`. Returns `:ok`, or the first\n-  `{:mismatch, input, expected, got}` so the failing case is reported precisely.\n+  the `reference` tokenizer (default `Rian.Lexer.expr_tokens/1`; pass\n+  `&Rian.Lexer.tokenize/1` to check the full declaration stream incl. `{:nl}`).\n+  Returns `:ok`, or the first `{:mismatch, input, expected, got}` so the failing\n+  case is reported precisely.\n \n   `project` maps one Rian token (tagged tuple/atom) to its reference-token shape.\n   \"\"\"\n-  @spec check(lexer_mod(), [String.t()], (term() -> term())) ::\n+  @spec check(lexer_mod(), [String.t()], (term() -> term()), (String.t() -> [term()])) ::\n           :ok | {:mismatch, String.t(), [term()], [term()]}\n-  def check(mod, corpus, project) do\n+  def check(mod, corpus, project, reference \\\\ &Rian.Lexer.expr_tokens/1) do\n     Enum.reduce_while(corpus, :ok, fn input, _acc ->\n-      expected = Rian.Lexer.expr_tokens(input)\n+      expected = reference.(input)\n       got = mod.tokenize(input) |> Enum.map(project)\n \n       if got == expected,","raw_url":"https://github.com/wstein/rian-lab/raw/c4b593aab4a8630e9e116e4141724e206713195f/lib%2Frian%2Ffixpoint.ex","sha":"4b04be8e11992433576f021888d030ae9fa9de43","status":"modified"},{"additions":70,"blob_url":"https://github.com/wstein/rian-lab/blob/c4b593aab4a8630e9e116e4141724e206713195f/test%2Frian%2Ffixpoint_test.exs","changes":70,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ffixpoint_test.exs?ref=c4b593aab4a8630e9e116e4141724e206713195f","deletions":0,"filename":"test/rian/fixpoint_test.exs","patch":"@@ -67,8 +67,19 @@ defmodule Rian.FixpointTest do\n   defp project_v2({:t_op, s}), do: {:op, s}\n   defp project_v2({:t_str, s}), do: {:str, s}\n   defp project_v2({:t_char, cp}), do: {:char, cp}\n+  # slice 5 — the full `tokenize/1` vocabulary: annotations, brackets, punctuation,\n+  # and the significant newline (`{:nl}`).\n+  defp project_v2({:t_annot, s}), do: {:annot, s}\n   defp project_v2(:tlp), do: {:lparen}\n   defp project_v2(:trp), do: {:rparen}\n+  defp project_v2(:tl_bracket), do: {:lbracket}\n+  defp project_v2(:tr_bracket), do: {:rbracket}\n+  defp project_v2(:tl_brace), do: {:lbrace}\n+  defp project_v2(:tr_brace), do: {:rbrace}\n+  defp project_v2(:t_map_open), do: {:mapopen}\n+  defp project_v2(:t_comma), do: {:comma}\n+  defp project_v2(:t_semi), do: {:semi}\n+  defp project_v2(:tnl), do: {:nl}\n   defp project_v2(other), do: project(other)\n \n   # within slices 1-4: integers, identifiers, all 16 keywords, the comparison\n@@ -158,4 +169,63 @@ defmodule Rian.FixpointTest do\n       assert {:num, \"1e9\"} in got\n     end\n   end\n+\n+  # Slice 5 corpus — the FULL declaration stream `Rian.Decl` consumes: significant\n+  # newlines (runs collapsed, leading/trailing dropped), `;` `,`, `@annot`,\n+  # brackets `[] {} %{`, line comments, and the extra operators (`-> .. := |> <>\n+  # <~ <- . | : &`). Checked against `Rian.Lexer.tokenize/1` (not `expr_tokens/1`).\n+  @corpus_tok [\n+    # significant newlines: collapsed runs, leading/trailing dropped\n+    \"a\\nb\",\n+    \"\\n\\nx\\n\\n\",\n+    \"a\\n\\n\\nb\\nc\",\n+    # punctuation + brackets + map-open\n+    \"[1, 2, 3]\",\n+    \"%{k: v, j: w}\",\n+    \"f(a, b) ; g(c)\",\n+    # the new operators\n+    \"x := y\",\n+    \"p |> q.r\",\n+    \"ok <- e\",\n+    \"total <~ total + n\",\n+    \"lo .. hi\",\n+    \"a <> b\",\n+    \"&f | x\",\n+    # line comment is skipped; the newline after it survives as {:nl}\n+    \"a # trailing comment\\nb\",\n+    # a realistic multi-line declaration (newlines, @annot, keywords, do/end, `:=`)\n+    \"@doc\\nmod M do\\n  pub def f(n Int64) Int64 := n + 1\\nend\"\n+  ]\n+\n+  describe \"lexer port slice 5 — tokenize/1 parity (newlines, punctuation, @annot, full ops)\" do\n+    setup do\n+      mod =\n+        Fixpoint.load_lexer(\n+          File.read!(\"examples/rian/selfhost_lexer_v2.rian\"),\n+          :rian_fixpoint_lexer_tok\n+        )\n+\n+      {:ok, mod: mod}\n+    end\n+\n+    test \"the v2 Rian lexer agrees with Rian.Lexer.tokenize/1 (full declaration stream)\",\n+         %{mod: mod} do\n+      assert Fixpoint.check(mod, @corpus_tok, &project_v2/1, &Rian.Lexer.tokenize/1) == :ok\n+    end\n+\n+    # slice-5 teeth: a wrong newline projection (dropping `{:nl}`) MUST be caught —\n+    # significant newlines are real tokens in the declaration stream.\n+    test \"divergence in the significant newline is caught (the diff has teeth)\", %{mod: mod} do\n+      wrong = fn\n+        :tnl -> {:semi}\n+        other -> project_v2(other)\n+      end\n+\n+      assert {:mismatch, \"a\\nb\", expected, got} =\n+               Fixpoint.check(mod, @corpus_tok, wrong, &Rian.Lexer.tokenize/1)\n+\n+      assert {:nl} in expected\n+      assert {:semi} in got\n+    end\n+  end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/c4b593aab4a8630e9e116e4141724e206713195f/test%2Frian%2Ffixpoint_test.exs","sha":"d397b5dfae16be36ac0d73f2bc29b5c643cae9d9","status":"modified"}]}