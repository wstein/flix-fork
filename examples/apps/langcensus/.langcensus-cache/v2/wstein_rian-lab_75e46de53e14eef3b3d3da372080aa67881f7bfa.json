{"files":[{"additions":9,"blob_url":"https://github.com/wstein/rian-lab/blob/75e46de53e14eef3b3d3da372080aa67881f7bfa/SELFHOST.md","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=75e46de53e14eef3b3d3da372080aa67881f7bfa","deletions":0,"filename":"SELFHOST.md","patch":"@@ -242,6 +242,15 @@ means accumulating codepoints and `List.to_string`-ing them (BEAM FFI) — the s\n class of crutch the lexer already used; a portable `String` builder is the\n multi-target fix.\n \n+The surface then gained top-level **`def` declarations** — a program is a run of\n+`def name = expr;` declarations followed by a result expression, exactly how a\n+real Rian module is shaped. Each declaration desugars to a `let` wrapping the\n+rest, so the existing fold/codegen/VM handle it unchanged:\n+`run(\"def a = 2; def b = 3; a * b + a\") = 8`. Declarations and inner `let`s\n+compose, and (being still arithmetic + binding) the whole thing lowers to JS and\n+runs under node too. (User-defined *functions* — `def f(x) := …` with call\n+frames — are the next, larger language step; they need CALL/RET in the VM.)\n+\n ## Module system — a compiler is many modules\n \n A real compiler is split across files; [`examples/rian/selfhost_modules.rian`](examples/rian/selfhost_modules.rian)","raw_url":"https://github.com/wstein/rian-lab/raw/75e46de53e14eef3b3d3da372080aa67881f7bfa/SELFHOST.md","sha":"8cc1a99c6d86f3c19472f970d27079f43da33ff9","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/75e46de53e14eef3b3d3da372080aa67881f7bfa/examples%2Frian%2FREADME.md","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2FREADME.md?ref=75e46de53e14eef3b3d3da372080aa67881f7bfa","deletions":1,"filename":"examples/rian/README.md","patch":"@@ -79,7 +79,11 @@ compile + run on real BEAM bytecode:\n   It also parses **`let`-bindings and variables** from source — the lexer scans\n   identifiers and the `let`/`in` keywords, the parser builds `Let`/`Var`, and the\n   codegen allocates slots: `run(\"let x = 5 in x + 1\") → 6`, with correct lexical\n-  shadowing.\n+  shadowing. It also parses top-level **`def` declarations** — a program is a run\n+  of `def name = expr;` declarations then a result, desugared to nested `let`s:\n+  `run(\"def a = 2; def b = 3; a * b + a\") → 8`. The whole pipeline also **lowers\n+  to JavaScript and runs under node** (`Rian.JS.compile`), so the calc runs on\n+  two targets.\n - [selfhost_modules.rian](selfhost_modules.rian) — **the same calc split across\n   many modules.** `mod CalcLex` / `CalcParse` / `CalcGen` / `Calc` each compile\n   to their own BEAM module (`Elixir.CalcLex`, …); the driver `Calc.run` calls","raw_url":"https://github.com/wstein/rian-lab/raw/75e46de53e14eef3b3d3da372080aa67881f7bfa/examples%2Frian%2FREADME.md","sha":"c6f30b0013c60031110a3fba47f1423babd4d4fa","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/rian-lab/blob/75e46de53e14eef3b3d3da372080aa67881f7bfa/examples%2Frian%2Fselfhost_calc.rian","changes":18,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_calc.rian?ref=75e46de53e14eef3b3d3da372080aa67881f7bfa","deletions":3,"filename":"examples/rian/selfhost_calc.rian","patch":"@@ -16,7 +16,7 @@ mod SelfhostCalc do\n   # ── tokens ────────────────────────────────────────────────────────────\n   type Token := TNum(Int64)\n               | TPlus | TMinus | TStar | TSlash | TLParen | TRParen\n-              | TIdent(String) | TLet | TIn | TEq\n+              | TIdent(String) | TLet | TIn | TEq | TDef | TSemi\n \n   # ── the expression tree (the parser builds the arithmetic subset; the\n   #    optimizer/codegen also handle variables and `let`) ─────────────────\n@@ -51,6 +51,7 @@ mod SelfhostCalc do\n   def lex([40 | rest]) := [TLParen | lex(rest)]\n   def lex([41 | rest]) := [TRParen | lex(rest)]\n   def lex([61 | rest]) := [TEq | lex(rest)]\n+  def lex([59 | rest]) := [TSemi | lex(rest)]\n   def lex([c | rest]) when c >= 48 and c <= 57 := lex_num([c | rest], 0)\n   def lex([c | rest]) when c >= 97 and c <= 122 := lex_ident([c | rest], [])\n   def lex([_ | rest]) := lex(rest)\n@@ -69,11 +70,22 @@ mod SelfhostCalc do\n   def keyword(s String) Token\n   def keyword(\"let\") := TLet\n   def keyword(\"in\") := TIn\n+  def keyword(\"def\") := TDef\n   def keyword(s) := TIdent(s)\n \n   # ── parser:  precedence climbing over `+ - * /` with parentheses ───────\n-  pub def parse(ts Vec(Token)) Expr\n-  pub def parse(ts)\n+  pub def parse(ts Vec(Token)) Expr := parse_program(ts)\n+\n+  # a program is a run of `def name = <rhs>;` declarations followed by a result\n+  # expression; each declaration desugars to a `let` wrapping the rest (so the\n+  # existing fold/codegen/VM handle it unchanged — declarations are nested lets)\n+  def parse_program(ts Vec(Token)) Expr\n+  def parse_program([TDef, TIdent(name), TEq | rest])\n+    case parse_expr(rest) do\n+      P(rhs, [TSemi | rest2]) -> Let(name, rhs, parse_program(rest2))\n+    end\n+  end\n+  def parse_program(ts)\n     case parse_expr(ts) do\n       P(e, _) -> e\n     end","raw_url":"https://github.com/wstein/rian-lab/raw/75e46de53e14eef3b3d3da372080aa67881f7bfa/examples%2Frian%2Fselfhost_calc.rian","sha":"0e9b2cc64851a083a6b37a8d1a6f8e80bef8d6c6","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/rian-lab/blob/75e46de53e14eef3b3d3da372080aa67881f7bfa/test%2Frian%2Fbeam_test.exs","changes":12,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fbeam_test.exs?ref=75e46de53e14eef3b3d3da372080aa67881f7bfa","deletions":0,"filename":"test/rian/beam_test.exs","patch":"@@ -251,6 +251,18 @@ defmodule Rian.BeamTest do\n       assert calc.run(\"let x = 1 in (let x = 2 in x) + x\") == 3\n     end\n \n+    test \"the calc parses top-level `def` declarations (a program is decls + a result)\" do\n+      {:ok, calc} = Beam.load(File.read!(\"examples/rian/selfhost_calc.rian\"), :rian_beam_calc_def)\n+\n+      # `def name = expr;` declarations desugar to nested lets, reusing the pipeline\n+      assert calc.run(\"def a = 2; def b = 3; a * b + a\") == 8\n+      assert calc.run(\"def x = 10; def y = x + 5; y * 2\") == 30\n+      # declarations and an inner `let` compose\n+      assert calc.run(\"def base = 100; let t = 1 in base + t\") == 101\n+      # a bare expression (no declarations) still works\n+      assert calc.run(\"2 + 3 * 4\") == 14\n+    end\n+\n     test \"higher-order: a `&name/arity` capture applied through a fun-typed param (ADR-0042)\" do\n       {:ok, mod} =\n         Beam.load(","raw_url":"https://github.com/wstein/rian-lab/raw/75e46de53e14eef3b3d3da372080aa67881f7bfa/test%2Frian%2Fbeam_test.exs","sha":"248b4d04f97e47c4d3b439079240f9bca5f12dd4","status":"modified"}]}