{"files":[{"additions":188,"blob_url":"https://github.com/wstein/flatbars/blob/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","changes":262,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs?ref=ea62b218f87b20be4e32dafe5b24f8904e9b437e","deletions":74,"filename":"trussbars/crates/trussbars-template/src/emit.rs","patch":"@@ -25,6 +25,39 @@ pub fn emit(ctx_type: &str, src: &str) -> Result<String, String> {\n /// # Errors\n /// The located `line:col: message` reason (see [`emit`]).\n pub fn emit_named(fn_name: &str, ctx_type: &str, src: &str) -> Result<String, String> {\n+    Ok(emit_body(fn_name, ctx_type, src)?.0)\n+}\n+\n+/// One **source→Rust tile**: a template byte span and the byte range of the emitted\n+/// Rust it produced. The AOT analog of the VM's `inspect` provenance — it powers the\n+/// Studio's template↔Rust pane and \"which template line is this generated Rust\" lookups\n+/// (docs/10). Tiles nest: an `each` block's tile spans its whole Rust, with each inner\n+/// interpolation's tile inside it.\n+#[derive(Debug, Clone, Copy, PartialEq, Eq)]\n+pub struct Tile {\n+    /// The template span's start byte.\n+    pub src_start: usize,\n+    /// The template span's end byte.\n+    pub src_end: usize,\n+    /// The start byte of the emitted Rust this node produced.\n+    pub rust_start: usize,\n+    /// The end byte of the emitted Rust this node produced.\n+    pub rust_end: usize,\n+}\n+\n+/// As [`emit`], also returning the **source→Rust map** ([`Tile`]s) — every template\n+/// node's span paired with the byte range of Rust it emitted, in the returned string's\n+/// coordinates.\n+///\n+/// # Errors\n+/// The located `line:col: message` reason (see [`emit`]).\n+pub fn emit_mapped(ctx_type: &str, src: &str) -> Result<(String, Vec<Tile>), String> {\n+    emit_body(\"render\", ctx_type, src)\n+}\n+\n+/// The shared core: parse → hoist → emit the body into a buffer (recording tiles) →\n+/// wrap in the render-fn header/footer, shifting the tiles into final coordinates.\n+fn emit_body(fn_name: &str, ctx_type: &str, src: &str) -> Result<(String, Vec<Tile>), String> {\n     let nodes = parse(src).map_err(|e| located(e.at, src, &e.message))?;\n     let (registry, top) = hoist(nodes);\n     let env = Env {\n@@ -38,8 +71,20 @@ pub fn emit_named(fn_name: &str, ctx_type: &str, src: &str) -> Result<String, St\n         yield_code: None,\n         depth: 0,\n     };\n-    let body = emit_nodes(&env, src, &top)?;\n-    Ok(render_fn(fn_name, ctx_type, estimate_bytes(&top), &body))\n+    let mut body = String::new();\n+    let mut map = Vec::new();\n+    emit_nodes(&env, src, &top, &mut body, &mut map)?;\n+    let (header, footer) = render_fn_parts(fn_name, ctx_type, estimate_bytes(&top));\n+    let shift = header.len();\n+    for t in &mut map {\n+        t.rust_start += shift;\n+        t.rust_end += shift;\n+    }\n+    let mut rust = String::with_capacity(header.len() + body.len() + footer.len());\n+    rust.push_str(&header);\n+    rust.push_str(&body);\n+    rust.push_str(&footer);\n+    Ok((rust, map))\n }\n \n /// Prefix a message with its `line:col:` template coordinates (class-A diagnostics,\n@@ -126,16 +171,18 @@ fn hoist_into(nodes: Vec<Node>, reg: &mut BTreeMap<String, Vec<Node>>) -> Vec<No\n \n // ── the render-fn wrapper + capacity seed ─────────────────────────────────────\n \n-fn render_fn(fn_name: &str, ctx_type: &str, cap: usize, body: &str) -> String {\n-    format!(\n+/// The render-fn wrapper split into the prefix (before the body) and the suffix, so the\n+/// body's tiles can be shifted by `header.len()` into final coordinates. Concatenated\n+/// `header + body + footer` is byte-identical to the previous single-`format!` wrapper.\n+fn render_fn_parts(fn_name: &str, ctx_type: &str, cap: usize) -> (String, String) {\n+    let header = format!(\n         \"#[doc = \\\"Rendered from a MaxBars template (generated by Trussbars).\\\"]\\n\\\n          pub fn {fn_name}(ctx: &{ctx_type}) -> String {{\\n\\\n          static __CAP: trussbars_core::SizeHint = trussbars_core::SizeHint::new({cap});\\n\\\n          let __root = ctx;\\n\\\n-         let mut out = String::with_capacity(__CAP.suggest());\\n\\\n-         {body}__CAP.record(out.len());\\n\\\n-         out\\n}}\\n\"\n-    )\n+         let mut out = String::with_capacity(__CAP.suggest());\\n\"\n+    );\n+    (header, \"__CAP.record(out.len());\\nout\\n}\\n\".to_string())\n }\n \n fn estimate_bytes(nodes: &[Node]) -> usize {\n@@ -161,58 +208,74 @@ fn estimate_node(n: &Node) -> usize {\n \n // ── nodes ─────────────────────────────────────────────────────────────────────\n \n-fn emit_nodes(env: &Env, src: &str, nodes: &[Node]) -> Result<String, String> {\n-    let mut out = String::new();\n+fn emit_nodes(env: &Env, src: &str, nodes: &[Node], out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n     for n in nodes {\n-        out.push_str(&emit_node(env, src, n)?);\n+        emit_node(env, src, n, out, map)?;\n     }\n-    Ok(out)\n+    Ok(())\n }\n \n-/// Emit one node, attaching its tag's `line:col` to any leaf error that does not\n-/// already carry one (so a class-A failure points at the offending tag).\n-fn emit_node(env: &Env, src: &str, n: &Node) -> Result<String, String> {\n-    emit_node_inner(env, src, n)\n-        .map_err(|m| if is_located(&m) { m } else { located(n.span().start, src, &m) })\n+/// Emit one node into `out`, recording its source→Rust tile and attaching its tag's\n+/// `line:col` to any leaf error that does not already carry one (class-A diagnostics).\n+fn emit_node(env: &Env, src: &str, n: &Node, out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n+    let start = out.len();\n+    emit_node_inner(env, src, n, out, map)\n+        .map_err(|m| if is_located(&m) { m } else { located(n.span().start, src, &m) })?;\n+    // Text has no source span; Inline emits nothing — neither tiles usefully.\n+    if !matches!(n, Node::Text(_) | Node::Inline { .. }) {\n+        let span = n.span();\n+        map.push(Tile { src_start: span.start, src_end: span.end, rust_start: start, rust_end: out.len() });\n+    }\n+    Ok(())\n }\n \n-fn emit_node_inner(env: &Env, src: &str, n: &Node) -> Result<String, String> {\n+fn emit_node_inner(env: &Env, src: &str, n: &Node, out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n     match n {\n-        Node::Text(s) => Ok(format!(\"out.push_str({});\\n\", rust_str(s))),\n+        Node::Text(s) => out.push_str(&format!(\"out.push_str({});\\n\", rust_str(s))),\n         Node::Output { expr, raw, .. } => {\n             let e = emit_expr(env, expr)?;\n             if *raw {\n-                Ok(format!(\"trussbars_core::ToText::write_text(&({e}), &mut out);\\n\"))\n+                out.push_str(&format!(\"trussbars_core::ToText::write_text(&({e}), &mut out);\\n\"));\n             } else {\n-                Ok(format!(\"trussbars_core::esc(&({e}), &mut out);\\n\"))\n+                out.push_str(&format!(\"trussbars_core::esc(&({e}), &mut out);\\n\"));\n             }\n         }\n-        Node::RawBlock { body, .. } => Ok(format!(\"out.push_str({});\\n\", rust_str(body))),\n-        Node::Yield { .. } => yield_here(env),\n+        Node::RawBlock { body, .. } => out.push_str(&format!(\"out.push_str({});\\n\", rust_str(body))),\n+        Node::Yield { .. } => return yield_here(env, out),\n         Node::Partial { name, ctx, .. } => {\n-            inline_partial(env, src, name, ctx.clone().unwrap_or_else(|| Expr::nullary(\"this\")), None)\n+            return inline_partial(env, src, name, ctx.clone().unwrap_or_else(|| Expr::nullary(\"this\")), None, out, map);\n         }\n         Node::PartialBlock { name, ctx, body, .. } => {\n-            let yield_code = emit_nodes(env, src, body)?;\n-            inline_partial(\n+            // The block body renders into a separate buffer (its yield); its inner tiles\n+            // are not mapped (a block-partial edge case).\n+            let mut yield_buf = String::new();\n+            let mut discard = Vec::new();\n+            emit_nodes(env, src, body, &mut yield_buf, &mut discard)?;\n+            return inline_partial(\n                 env,\n                 src,\n                 name,\n                 ctx.clone().unwrap_or_else(|| Expr::nullary(\"this\")),\n-                Some(yield_code),\n-            )\n+                Some(yield_buf),\n+                out,\n+                map,\n+            );\n         }\n-        Node::Inline { .. } => Ok(String::new()), // hoisted away\n-        Node::Each(e) => each_block(env, src, e),\n-        Node::Cond(c) => cond_block(env, src, c),\n-        Node::With(w) => with_block(env, src, w),\n-        Node::Let { bindings, body, .. } => let_block(env, src, bindings, body),\n+        Node::Inline { .. } => {} // hoisted away\n+        Node::Each(e) => return each_block(env, src, e, out, map),\n+        Node::Cond(c) => return cond_block(env, src, c, out, map),\n+        Node::With(w) => return with_block(env, src, w, out, map),\n+        Node::Let { bindings, body, .. } => return let_block(env, src, bindings, body, out, map),\n     }\n+    Ok(())\n }\n \n-fn yield_here(env: &Env) -> Result<String, String> {\n+fn yield_here(env: &Env, out: &mut String) -> Result<(), String> {\n     match &env.yield_code {\n-        Some(code) => Ok(code.clone()),\n+        Some(code) => {\n+            out.push_str(code);\n+            Ok(())\n+        }\n         None => Err(\"unsupported: '{{yield}}' outside a block partial\".into()),\n     }\n }\n@@ -223,7 +286,9 @@ fn inline_partial(\n     name: &str,\n     ctx_e: Expr,\n     yield_code: Option<String>,\n-) -> Result<String, String> {\n+    out: &mut String,\n+    map: &mut Vec<Tile>,\n+) -> Result<(), String> {\n     let Some(body) = env.partials.get(name) else {\n         return Err(format!(\"unsupported: unknown partial '{name}'\"));\n     };\n@@ -238,35 +303,39 @@ fn inline_partial(\n     child.parents = Vec::new();\n     child.expanding.push(name.to_string());\n     child.yield_code = yield_code;\n-    emit_nodes(&child, src, body)\n+    emit_nodes(&child, src, body, out, map)\n }\n \n // ── blocks ────────────────────────────────────────────────────────────────────\n \n-fn cond_block(env: &Env, src: &str, c: &Cond) -> Result<String, String> {\n+fn cond_block(env: &Env, src: &str, c: &Cond, out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n     let prefix = if c.negated { \"if !\" } else { \"if \" };\n     let cond = truthy_of(env, &c.cond)?;\n-    let body = emit_nodes(env, src, &c.body)?;\n-    let mut out = format!(\"{prefix}trussbars_core::truthy(&({cond})) {{\\n{body}}}\");\n+    out.push_str(prefix);\n+    out.push_str(&format!(\"trussbars_core::truthy(&({cond})) {{\\n\"));\n+    emit_nodes(env, src, &c.body, out, map)?;\n+    out.push('}');\n     for (econd, ebody) in &c.elifs {\n         let ec = truthy_of(env, econd)?;\n-        let eb = emit_nodes(env, src, ebody)?;\n-        out.push_str(&format!(\" else if trussbars_core::truthy(&({ec})) {{\\n{eb}}}\"));\n+        out.push_str(&format!(\" else if trussbars_core::truthy(&({ec})) {{\\n\"));\n+        emit_nodes(env, src, ebody, out, map)?;\n+        out.push('}');\n     }\n     if !c.otherwise.is_empty() {\n-        let ob = emit_nodes(env, src, &c.otherwise)?;\n-        out.push_str(&format!(\" else {{\\n{ob}}}\"));\n+        out.push_str(\" else {\\n\");\n+        emit_nodes(env, src, &c.otherwise, out, map)?;\n+        out.push('}');\n     }\n     out.push('\\n');\n-    Ok(out)\n+    Ok(())\n }\n \n-fn with_block(env: &Env, src: &str, w: &With) -> Result<String, String> {\n+fn with_block(env: &Env, src: &str, w: &With, out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n     // An Option-aware `with` over a `find` subject.\n     if let Expr::App(name, args) = &w.subject\n         && name == \"find\"\n     {\n-        return with_find(env, src, args, &w.body, &w.otherwise);\n+        return with_find(env, src, args, &w.body, &w.otherwise, out, map);\n     }\n     let subj = emit_expr(env, &w.subject)?;\n     let d = env.depth + 1;\n@@ -275,27 +344,46 @@ fn with_block(env: &Env, src: &str, w: &With) -> Result<String, String> {\n     child.scope = cvar.clone();\n     child.parents.insert(0, env.scope.clone());\n     child.depth = d;\n-    let body = emit_nodes(&child, src, &w.body)?;\n-    let other = emit_nodes(env, src, &w.otherwise)?;\n-    Ok(format!(\n-        \"{{\\nlet {cvar} = &({subj});\\nif trussbars_core::truthy({cvar}) {{\\n{body}}} else {{\\n{other}}}\\n}}\\n\"\n-    ))\n+    out.push_str(&format!(\"{{\\nlet {cvar} = &({subj});\\nif trussbars_core::truthy({cvar}) {{\\n\"));\n+    emit_nodes(&child, src, &w.body, out, map)?;\n+    out.push_str(\"} else {\\n\");\n+    emit_nodes(env, src, &w.otherwise, out, map)?;\n+    out.push_str(\"}\\n}\\n\");\n+    Ok(())\n }\n \n-fn with_find(env: &Env, src: &str, find_args: &[Expr], body: &[Node], otherwise: &[Node]) -> Result<String, String> {\n+fn with_find(\n+    env: &Env,\n+    src: &str,\n+    find_args: &[Expr],\n+    body: &[Node],\n+    otherwise: &[Node],\n+    out: &mut String,\n+    map: &mut Vec<Tile>,\n+) -> Result<(), String> {\n     let found = coll_find(env, find_args)?;\n     let d = env.depth + 1;\n     let cvar = format!(\"__c{d}\");\n     let mut child = env.clone();\n     child.scope = cvar.clone();\n     child.parents.insert(0, env.scope.clone());\n     child.depth = d;\n-    let b = emit_nodes(&child, src, body)?;\n-    let other = emit_nodes(env, src, otherwise)?;\n-    Ok(format!(\"{{\\nif let Some({cvar}) = {found} {{\\n{b}}} else {{\\n{other}}}\\n}}\\n\"))\n+    out.push_str(&format!(\"{{\\nif let Some({cvar}) = {found} {{\\n\"));\n+    emit_nodes(&child, src, body, out, map)?;\n+    out.push_str(\"} else {\\n\");\n+    emit_nodes(env, src, otherwise, out, map)?;\n+    out.push_str(\"}\\n}\\n\");\n+    Ok(())\n }\n \n-fn let_block(env: &Env, src: &str, bindings: &[(String, Expr)], body: &[Node]) -> Result<String, String> {\n+fn let_block(\n+    env: &Env,\n+    src: &str,\n+    bindings: &[(String, Expr)],\n+    body: &[Node],\n+    out: &mut String,\n+    map: &mut Vec<Tile>,\n+) -> Result<(), String> {\n     let mut cur = env.clone();\n     let mut lets = String::new();\n     for (name, value) in bindings {\n@@ -304,11 +392,14 @@ fn let_block(env: &Env, src: &str, bindings: &[(String, Expr)], body: &[Node]) -\n         lets.push_str(&format!(\"let {rust} = {ve};\\n\"));\n         cur.params.insert(name.clone(), rust);\n     }\n-    let b = emit_nodes(&cur, src, body)?;\n-    Ok(format!(\"{{\\n{lets}{b}}}\\n\"))\n+    out.push_str(\"{\\n\");\n+    out.push_str(&lets);\n+    emit_nodes(&cur, src, body, out, map)?;\n+    out.push_str(\"}\\n\");\n+    Ok(())\n }\n \n-fn each_block(env: &Env, src: &str, e: &Each) -> Result<String, String> {\n+fn each_block(env: &Env, src: &str, e: &Each, out: &mut String, map: &mut Vec<Tile>) -> Result<(), String> {\n     let subj = emit_expr(env, &e.subject)?;\n     let d = env.depth + 1;\n     let (cvar, lvar, ivar, kvar, subvar, lenvar) = (\n@@ -341,8 +432,6 @@ fn each_block(env: &Env, src: &str, e: &Each) -> Result<String, String> {\n     child.depth = d;\n     let parent_loop = env.loop_var.as_ref().map_or_else(|| \"None\".to_string(), |pl| format!(\"Some(&{pl})\"));\n     let need_index = needs_frame || e.index.is_some();\n-    let body = emit_nodes(&child, src, &e.body)?;\n-    let other = emit_nodes(env, src, &e.otherwise)?;\n     let for_head = if need_index {\n         format!(\"for ({ivar}, ({kvar}, {cvar})) in trussbars_core::Each::each({subvar}).enumerate() {{\\n\")\n     } else {\n@@ -353,17 +442,19 @@ fn each_block(env: &Env, src: &str, e: &Each) -> Result<String, String> {\n     } else {\n         String::new()\n     };\n-    let mut s = String::from(\"{\\n\");\n-    s.push_str(&format!(\"let {subvar} = &({subj});\\n\"));\n-    s.push_str(&format!(\"let {lenvar} = trussbars_core::Each::each_len({subvar});\\n\"));\n-    s.push_str(&format!(\"if {lenvar} == 0 {{\\n{other}}} else {{\\n\"));\n-    s.push_str(&for_head); // ends with `{`\n-    s.push_str(&frame);\n-    s.push_str(&body);\n-    s.push_str(\"}\\n\"); // close the for\n-    s.push_str(\"}\\n\"); // close the else\n-    s.push_str(\"}\\n\"); // close the outer block\n-    Ok(s)\n+    out.push_str(\"{\\n\");\n+    out.push_str(&format!(\"let {subvar} = &({subj});\\n\"));\n+    out.push_str(&format!(\"let {lenvar} = trussbars_core::Each::each_len({subvar});\\n\"));\n+    out.push_str(&format!(\"if {lenvar} == 0 {{\\n\"));\n+    emit_nodes(env, src, &e.otherwise, out, map)?;\n+    out.push_str(\"} else {\\n\");\n+    out.push_str(&for_head); // ends with `{`\n+    out.push_str(&frame);\n+    emit_nodes(&child, src, &e.body, out, map)?;\n+    out.push_str(\"}\\n\"); // close the for\n+    out.push_str(\"}\\n\"); // close the else\n+    out.push_str(\"}\\n\"); // close the outer block\n+    Ok(())\n }\n \n /// Whether `App name …` (a bare reference) appears anywhere in a node subtree.\n@@ -747,12 +838,35 @@ fn rust_str(s: &str) -> String {\n \n #[cfg(test)]\n mod tests {\n-    use super::emit;\n+    use super::{emit, emit_mapped};\n \n     fn e(src: &str) -> String {\n         emit(\"Ctx\", src).unwrap_or_else(|err| panic!(\"{src:?}: {err}\"))\n     }\n \n+    #[test]\n+    fn mapped_tiles_link_template_to_rust() {\n+        let src = \"<h1>{{name}}</h1>{{#each items}}{{this}}{{/each}}\";\n+        let (rust, tiles) = emit_mapped(\"Ctx\", src).unwrap();\n+        // Every tile's rust range is in-bounds and its src range slices the template.\n+        for t in &tiles {\n+            assert!(t.rust_end <= rust.len() && t.rust_start <= t.rust_end);\n+            let _ = &src[t.src_start..t.src_end];\n+        }\n+        // The {{name}} interpolation: its Rust is the esc call, its src is the tag.\n+        let name = tiles\n+            .iter()\n+            .find(|t| rust[t.rust_start..t.rust_end].contains(\"ctx.name\"))\n+            .expect(\"a tile for {{name}}\");\n+        assert_eq!(&src[name.src_start..name.src_end], \"{{name}}\");\n+        // The each block's tile spans its whole emitted loop, and src is the `#each` tag.\n+        let each = tiles\n+            .iter()\n+            .find(|t| rust[t.rust_start..t.rust_end].contains(\"Each::each\"))\n+            .expect(\"a tile for the each\");\n+        assert!(src[each.src_start..each.src_end].starts_with(\"{{#each\"));\n+    }\n+\n     #[test]\n     fn output_escaped_and_raw() {\n         assert!(e(\"{{name}}\").contains(\"trussbars_core::esc(&(ctx.name), &mut out)\"));","raw_url":"https://github.com/wstein/flatbars/raw/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Femit.rs","sha":"e385a17a21ae124e331b4f93cacbb88e89186322","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/flatbars/blob/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs","changes":2,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs?ref=ea62b218f87b20be4e32dafe5b24f8904e9b437e","deletions":1,"filename":"trussbars/crates/trussbars-template/src/lib.rs","patch":"@@ -27,7 +27,7 @@ pub mod parse_expr;\n pub mod span;\n \n pub use ast::{Cond, Each, Expr, Node, Value, With};\n-pub use emit::{emit, emit_named};\n+pub use emit::{Tile, emit, emit_mapped, emit_named};\n pub use lex::{Lexeme, Sigil, lex};\n pub use parse::parse;\n pub use parse_expr::{ParseError, Scope, parse_expr};","raw_url":"https://github.com/wstein/flatbars/raw/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-template%2Fsrc%2Flib.rs","sha":"03e2adf37b3960334849fee4f6cc348bbfe00b29","status":"modified"},{"additions":17,"blob_url":"https://github.com/wstein/flatbars/blob/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-vm%2Fsrc%2Flib.rs","changes":20,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-vm%2Fsrc%2Flib.rs?ref=ea62b218f87b20be4e32dafe5b24f8904e9b437e","deletions":3,"filename":"trussbars/crates/trussbars-vm/src/lib.rs","patch":"@@ -24,7 +24,7 @@ use std::collections::BTreeMap;\n use std::rc::Rc;\n \n use trussbars_core::{ToText, escape_html};\n-use trussbars_template::{Cond, Each, Expr, Node, Value as Lit, With, parse};\n+use trussbars_template::{Cond, Each, Expr, Node, Tile, Value as Lit, With, parse};\n \n /// A dynamic runtime value. Heap variants (`Str`/`Array`/`Object`) are **`Rc`-backed**\n /// so [`Clone`] is a refcount bump, not a deep copy — the env can then hold values\n@@ -429,6 +429,9 @@ pub struct Inspection {\n     pub emitted_rust: Result<String, String>,\n     /// data→output provenance, one entry per interpolation, in output order.\n     pub interpolations: Vec<Interp>,\n+    /// template→Rust provenance (the AOT source map): each node's template span paired\n+    /// with the byte range of `emitted_rust` it produced.\n+    pub rust_map: Vec<Tile>,\n }\n \n impl Inspection {\n@@ -455,8 +458,14 @@ impl Inspection {\n             Err(e) => (\"null\".to_string(), jstr(e)),\n         };\n         let compat_err = self.aot_compat_error.as_deref().map_or_else(|| \"null\".to_string(), jstr);\n+        let rust_map = self\n+            .rust_map\n+            .iter()\n+            .map(|t| format!(\"{{\\\"src\\\":[{},{}],\\\"rust\\\":[{},{}]}}\", t.src_start, t.src_end, t.rust_start, t.rust_end))\n+            .collect::<Vec<_>>()\n+            .join(\",\");\n         format!(\n-            \"{{\\\"template\\\":{},\\\"output\\\":{},\\\"rendered\\\":{},\\\"aotCompatOk\\\":{},\\\"aotCompatError\\\":{},\\\"emittedRust\\\":{},\\\"emitError\\\":{},\\\"interpolations\\\":[{}]}}\",\n+            \"{{\\\"template\\\":{},\\\"output\\\":{},\\\"rendered\\\":{},\\\"aotCompatOk\\\":{},\\\"aotCompatError\\\":{},\\\"emittedRust\\\":{},\\\"emitError\\\":{},\\\"interpolations\\\":[{}],\\\"rustMap\\\":[{}]}}\",\n             jstr(&self.template),\n             jstr(&self.output),\n             self.rendered,\n@@ -465,6 +474,7 @@ impl Inspection {\n             rust,\n             emit_err,\n             interps,\n+            rust_map,\n         )\n     }\n }\n@@ -514,7 +524,10 @@ pub fn inspect(template: &str, data: &Value, helpers: &Rc<Helpers>) -> Inspectio\n         },\n         Err(e) => (false, Some(e.clone())),\n     };\n-    let emitted_rust = trussbars_template::emit(\"Ctx\", template);\n+    let (emitted_rust, rust_map) = match trussbars_template::emit_mapped(\"Ctx\", template) {\n+        Ok((r, tiles)) => (Ok(r), tiles),\n+        Err(e) => (Err(e), Vec::new()),\n+    };\n     Inspection {\n         template: template.to_string(),\n         output,\n@@ -523,6 +536,7 @@ pub fn inspect(template: &str, data: &Value, helpers: &Rc<Helpers>) -> Inspectio\n         aot_compat_error,\n         emitted_rust,\n         interpolations,\n+        rust_map,\n     }\n }\n ","raw_url":"https://github.com/wstein/flatbars/raw/ea62b218f87b20be4e32dafe5b24f8904e9b437e/trussbars%2Fcrates%2Ftrussbars-vm%2Fsrc%2Flib.rs","sha":"fc5345c69e72f5a4b6dd60b9459dce269b7e6a21","status":"modified"}]}