{"files":[{"additions":29,"blob_url":"https://github.com/wstein/rian-lab/blob/b662f0500944d87bbac2ebc04674a5eab7c23acb/purs%2Fscripts%2Fjs-bundle.sh","changes":29,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/purs%2Fscripts%2Fjs-bundle.sh?ref=b662f0500944d87bbac2ebc04674a5eab7c23acb","deletions":0,"filename":"purs/scripts/js-bundle.sh","patch":"@@ -0,0 +1,29 @@\n+#!/bin/sh\n+# Bundle the stock-purs JS build (`output-js/`) into a single browser ESM artifact (ADR-0090) — the\n+# compiler the live playground loads. Runs the JS build first (so `output-js/` is fresh), then\n+# esbuild-bundles `playground-entry.mjs` (which re-exports `compile`/`compileTypes`) + all its\n+# transitive deps into `site/public/rian-compiler.mjs`. The Astro site serves that at `/`.\n+set -eu\n+\n+cd \"$(dirname \"$0\")/..\"          # purs/\n+export PATH=\"$PWD/node_modules/.bin:$PATH\"\n+\n+ESBUILD=\"../site/node_modules/.bin/esbuild\"\n+OUT=\"../site/public/rian-compiler.mjs\"\n+[ -x \"$ESBUILD\" ] || { echo \"✗ esbuild not found at $ESBUILD — run: cd site && npm install\"; exit 1; }\n+\n+echo \"→ JS build (output-js/)\"\n+./scripts/js-build.sh\n+\n+echo \"→ esbuild: bundle → $OUT\"\n+mkdir -p ../site/public\n+\"$ESBUILD\" scripts/playground-entry.mjs --bundle --format=esm --minify --outfile=\"$OUT\" --log-level=warning\n+\n+echo \"→ smoke: load the bundle + compile a sample (node)\"\n+node --input-type=module -e '\n+  import { compile } from \"../site/public/rian-compiler.mjs\";\n+  const js = compile(\"pub def add(x Int53, y Int53) Int53 := x + y\");\n+  if (!js.includes(\"export function add\")) { console.error(\"✗ bundle compile failed\\n\" + js); process.exit(1); }\n+  console.log(\"✓ rian-compiler.mjs bundles + compiles\");\n+'\n+echo \"✓ bundle: $(wc -c < \"$OUT\" | tr -d ' ') bytes → $OUT\"","raw_url":"https://github.com/wstein/rian-lab/raw/b662f0500944d87bbac2ebc04674a5eab7c23acb/purs%2Fscripts%2Fjs-bundle.sh","sha":"f86399ff6a7206c69a1563954f460b7e7a2d15dc","status":"added"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/b662f0500944d87bbac2ebc04674a5eab7c23acb/purs%2Fscripts%2Fplayground-entry.mjs","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/purs%2Fscripts%2Fplayground-entry.mjs?ref=b662f0500944d87bbac2ebc04674a5eab7c23acb","deletions":0,"filename":"purs/scripts/playground-entry.mjs","patch":"@@ -0,0 +1,5 @@\n+// Browser-bundle entry (ADR-0090): re-export the Rian compiler's public JS API from the\n+// stock-purs JS build (`output-js/`). `esbuild` inlines this + its transitive deps into one ESM\n+// artifact the playground loads. `compile :: String -> String` lowers Rian source to an ECMAScript\n+// module string; `compileTypes` produces the `.d.mts` TypeScript sidecar.\n+export { compile, compileTypes } from \"../output-js/Rian.JS/index.js\";","raw_url":"https://github.com/wstein/rian-lab/raw/b662f0500944d87bbac2ebc04674a5eab7c23acb/purs%2Fscripts%2Fplayground-entry.mjs","sha":"b1b88eba9c37adc291b2c43347af0ecbd9bb4cd6","status":"added"},{"additions":201,"blob_url":"https://github.com/wstein/rian-lab/blob/b662f0500944d87bbac2ebc04674a5eab7c23acb/site%2Fpublic%2Frian-compiler.mjs","changes":201,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fpublic%2Frian-compiler.mjs?ref=b662f0500944d87bbac2ebc04674a5eab7c23acb","deletions":0,"filename":"site/public/rian-compiler.mjs","raw_url":"https://github.com/wstein/rian-lab/raw/b662f0500944d87bbac2ebc04674a5eab7c23acb/site%2Fpublic%2Frian-compiler.mjs","sha":"956204f46f6721b155c2b730fc13f907b8f8dbcb","status":"added"},{"additions":74,"blob_url":"https://github.com/wstein/rian-lab/blob/b662f0500944d87bbac2ebc04674a5eab7c23acb/site%2Fsrc%2Fpages%2Fplayground.astro","changes":86,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fplayground.astro?ref=b662f0500944d87bbac2ebc04674a5eab7c23acb","deletions":12,"filename":"site/src/pages/playground.astro","patch":"@@ -117,6 +117,15 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   const statusLn = $(\"#pg-status-ln\");\n \n   let current: Cell = cells[0];\n+  // the live JS the in-browser compiler emits for the current buffer (null = show the reference pane)\n+  let liveJs: string | null = null;\n+\n+  // the real Rian compiler, compiled to JS by stock purs (ADR-0090) — lazy-loaded once on first Run.\n+  // Served from `public/` at `/rian-compiler.mjs`; the URL is a variable so the bundler treats it as\n+  // a runtime import (a static literal would make Rollup try to resolve the public asset at build).\n+  let compilerP: Promise<{ compile: (src: string) => string }> | null = null;\n+  const compilerUrl = \"/rian-compiler.mjs\";\n+  const getCompiler = () => (compilerP ??= import(/* @vite-ignore */ compilerUrl));\n \n   function renderEditor() {\n     const code = src.value;\n@@ -130,7 +139,11 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   function renderPanes() {\n     for (const t of [\"ex\", \"rs\", \"js\", \"jvm\"]) {\n       const el = document.querySelector(`.pg-pane-code[data-pane=\"${t}\"]`);\n-      if (el) el.innerHTML = highlight((current.panes as Record<string, string>)[t]);\n+      if (!el) continue;\n+      // the JS pane is LIVE — the in-browser compiler's real emission for the current buffer once\n+      // Run has compiled it; the other targets show the reference (their emitters aren't in-browser).\n+      const code = t === \"js\" && liveJs != null ? liveJs : (current.panes as Record<string, string>)[t];\n+      el.innerHTML = highlight(code);\n     }\n     const reach = Object.values(current.reach)[0] as string[];\n     const order = [\"ex\", \"rs\", \"js\", \"jvm\"];\n@@ -147,23 +160,72 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   function loadFile(id: string) {\n     current = byId.get(id)!;\n     src.value = current.source;\n+    liveJs = null;\n     document.querySelectorAll(\".pg-tab\").forEach((b) => b.classList.toggle(\"on\", (b as HTMLElement).dataset.file === id));\n     renderEditor();\n     renderPanes();\n-    out.textContent = \"Run to see the compiler's gate + reachability report.\";\n+    out.textContent = \"Run to compile this buffer in your browser (the real Rian → JS compiler).\";\n+  }\n+\n+  // Execute an emitted ECMAScript module in a sandboxed iframe (`allow-scripts`, opaque origin — no\n+  // page access). Imports it from a blob URL; calls `main()` if exported, else reports it loaded.\n+  function runInSandbox(jsModule: string, timeoutMs = 4000): Promise<{ ok: boolean; text: string }> {\n+    return new Promise((resolve) => {\n+      const frame = document.createElement(\"iframe\");\n+      frame.sandbox.add(\"allow-scripts\");\n+      frame.style.display = \"none\";\n+      let done = false;\n+      const finish = (r: { ok: boolean; text: string }) => {\n+        if (done) return;\n+        done = true;\n+        window.removeEventListener(\"message\", onMsg);\n+        frame.remove();\n+        resolve(r);\n+      };\n+      const onMsg = (ev: MessageEvent) => {\n+        if (ev.source === frame.contentWindow && ev.data && ev.data.__rian) finish(ev.data.r);\n+      };\n+      window.addEventListener(\"message\", onMsg);\n+      const runner =\n+        '<!doctype html><script type=\"module\">' +\n+        \"const code = \" + JSON.stringify(jsModule) + \";\" +\n+        \"(async () => { let r; try {\" +\n+        \"  const url = URL.createObjectURL(new Blob([code], { type: 'text/javascript' }));\" +\n+        \"  const mod = await import(url);\" +\n+        \"  if (typeof mod.main === 'function') r = { ok: true, text: 'main() = ' + JSON.stringify(mod.main()) };\" +\n+        \"  else r = { ok: true, text: 'module loaded · exports: ' + Object.keys(mod).join(', ') + ' (define `main()` to see a value)' };\" +\n+        \"} catch (e) { r = { ok: false, text: String((e && e.message) || e) }; }\" +\n+        \"  parent.postMessage({ __rian: true, r }, '*'); })();\" +\n+        \"<\\/script>\";\n+      frame.srcdoc = runner;\n+      document.body.appendChild(frame);\n+      setTimeout(() => finish({ ok: false, text: \"timed out (\" + timeoutMs + \"ms) — a runaway loop?\" }), timeoutMs);\n+    });\n   }\n \n-  function run() {\n-    const reach = (Object.values(current.reach)[0] as string[]).map((t) => \":\" + t).join(\" · \");\n-    const fn = Object.keys(current.reach)[0];\n-    const dirty = src.value !== current.source;\n+  async function run() {\n+    const source = src.value;\n+    out.textContent = \"$ rian build --js  (compiling in your browser…)\";\n+    let js: string;\n+    try {\n+      const { compile } = await getCompiler();\n+      js = compile(source);\n+    } catch (e: any) {\n+      // a gate failure (type/exhaustiveness/capability) or an unsupported construct throws here.\n+      liveJs = null;\n+      renderPanes();\n+      out.textContent = \"$ rian build --js\\n  ✗ \" + String((e && e.message) || e).replace(/^\\w+: /, \"\");\n+      return;\n+    }\n+    // the JS pane is now live (real in-browser emission); switch the panel to it.\n+    liveJs = js;\n+    document.documentElement.dataset.target = \"js\";\n+    renderPanes();\n+    out.textContent = \"$ rian build --js\\n  ✓ compiled to ECMAScript (live, in your browser)\\n  ▷ running the emitted module in a sandbox…\";\n+    const res = await runInSandbox(js);\n     out.textContent =\n-      `$ rian check ${current.file}\\n` +\n-      `  ✓ parsed\\n  ✓ type-checked\\n  ✓ exhaustive (Maranget)\\n  ✓ capability\\n` +\n-      `  ${fn} reaches ${reach}\\n` +\n-      (dirty\n-        ? `\\n  note: the buffer was edited — the panes show the reference\\n  emission for ${current.file}; in-browser re-lowering is not wired\\n  (the hosted Lab recompiles server-side).`\n-        : `\\n  exit 0`);\n+      \"$ rian build --js\\n  ✓ compiled to ECMAScript (live, in your browser)\\n\" +\n+      (res.ok ? \"  ✓ \" + res.text + \"\\n  exit 0\" : \"  ✗ runtime: \" + res.text);\n   }\n \n   // wire up","raw_url":"https://github.com/wstein/rian-lab/raw/b662f0500944d87bbac2ebc04674a5eab7c23acb/site%2Fsrc%2Fpages%2Fplayground.astro","sha":"30432435755c035825c931efb94a4df8722a8ffe","status":"modified"}]}