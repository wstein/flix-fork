{"files":[{"additions":8,"blob_url":"https://github.com/wstein/gramaire/blob/b0074fd2390cf29e72abd0b5e48cf411f871ff11/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala?ref=b0074fd2390cf29e72abd0b5e48cf411f871ff11","deletions":0,"filename":"cli/jvm/src/test/scala/gramaire/cli/MainSuite.scala","patch":"@@ -6,6 +6,7 @@ import gramaire.{\n   BackendIr,\n   BackendJs,\n   BackendRegistry,\n+  BackendScalaPeg,\n   BackendTs,\n   Grammar,\n   IR,\n@@ -112,12 +113,19 @@ class MainSuite extends munit.FunSuite:\n     // atn-ts is the mirror image of js/ts: it reads only the ATN, so it's ll-star-only.\n     assert(!Main.backendSupportsStrategy(BackendAtnTs.backend, \"lr\"))\n     assert(Main.backendSupportsStrategy(BackendAtnTs.backend, \"ll-star\"))\n+    // scala-peg reads only IR.rewritten (the ll-star-only CST-fold-back section).\n+    assert(!Main.backendSupportsStrategy(BackendScalaPeg.backend, \"lr\"))\n+    assert(Main.backendSupportsStrategy(BackendScalaPeg.backend, \"ll-star\"))\n   }\n \n   test(\"BackendRegistry: atn-ts is registered and reachable by name\") {\n     assertEquals(BackendRegistry.findBackend(\"atn-ts\"), Some(BackendAtnTs.backend))\n   }\n \n+  test(\"BackendRegistry: scala-peg is registered and reachable by name\") {\n+    assertEquals(BackendRegistry.findBackend(\"scala-peg\"), Some(BackendScalaPeg.backend))\n+  }\n+\n   test(\n     \"strategyIgnoresPredicates: an lr build of a `{%? %}` grammar is flagged, ll-star is not\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/b0074fd2390cf29e72abd0b5e48cf411f871ff11/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","sha":"3ee4380af7053ffb592cd86a90ee19c625869acf","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/gramaire/blob/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","changes":16,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala?ref=b0074fd2390cf29e72abd0b5e48cf411f871ff11","deletions":0,"filename":"core/.jvm/src/test/scala/gramaire/BackendGoldenSuite.scala","patch":"@@ -95,6 +95,22 @@ class BackendGoldenSuite extends munit.FunSuite:\n             assertEquals(BackendAtnTs.emit(irLl), readFile(\"test/golden/Calc.atn.ts\"))\n   }\n \n+  // A golden-text pin, same convention as every other backend above — but see\n+  // site/scripts/check-scala-peg-parity.mjs for the test that actually matters for this backend:\n+  // a golden diff alone can't tell a correctly-translated IRProv/IRRuleBody from one that merely\n+  // still emits stable-looking text, only compiling and running the emitted Scala can.\n+  test(\"scala-peg: examples/calc.gram.md under --strategy ll-star matches the committed golden\") {\n+    val md = readFile(\"examples/calc.gram.md\")\n+    Lr.parse(md) match\n+      case Left(e) => fail(s\"could not parse examples/calc.gram.md: $e\")\n+      case Right(g) =>\n+        IR.buildIR(Method.Canonical, \"Calc\", g) match\n+          case Left(_) => fail(\"could not build IR for calc\")\n+          case Right(ir) =>\n+            val irLl = IR.withStrategy(\"ll-star\", g, ir, Lr.precedenceOf(md))\n+            assertEquals(BackendScalaPeg.emit(irLl), readFile(\"test/golden/Calc.scala\"))\n+  }\n+\n   test(\n     \"js: calc-js bakes its inline actions into one evaluate(cst), matching the committed golden\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","sha":"f5076fcc9d0d897d98e23d16938321cf3bbe384d","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/gramaire/blob/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","changes":3,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala?ref=b0074fd2390cf29e72abd0b5e48cf411f871ff11","deletions":1,"filename":"core/src/main/scala/gramaire/BackendRegistry.scala","patch":"@@ -12,7 +12,8 @@ object BackendRegistry:\n       BackendTs.backend,\n       BackendAntlr.backend,\n       BackendJs.backend,\n-      BackendAtnTs.backend\n+      BackendAtnTs.backend,\n+      BackendScalaPeg.backend\n     )\n \n   /** Find a backend by its `--backend` name. */","raw_url":"https://github.com/wstein/gramaire/raw/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","sha":"89311b7ce61fa6979b6f77682309b823642b0449","status":"modified"},{"additions":199,"blob_url":"https://github.com/wstein/gramaire/blob/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala","changes":199,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala?ref=b0074fd2390cf29e72abd0b5e48cf411f871ff11","deletions":0,"filename":"core/src/main/scala/gramaire/BackendScalaPeg.scala","patch":"@@ -0,0 +1,199 @@\n+package gramaire\n+\n+// A `Cst`-capable, dependency-free codegen backend: `gramaire-ir` in, a self-contained Scala PEG\n+// (ordered-choice recursive-descent) parser out — `IR.rewritten`'s first *executable* consumer.\n+//\n+// Unlike `atn-ts` (deliberately recognizer-only, `docs/all-star-port-plan.md` Phase 5), this\n+// reaches real CST parity with `Ll.parse`: `IR.rewritten` already carries every alt's resolved\n+// `IRProv` (see `IR.scala`), so codegen only has to translate that static tree into Scala\n+// expressions, never interpret it at runtime.\n+//\n+// Each `IRRewrittenRule` becomes one `parse<Name>` function, dispatching on its `IRRuleBody`:\n+//   - `Plain(alts)`: ordered choice, tried in the IR's own order (already `Unwrap`-last — see\n+//     `IR.rewrittenGrammarOf`'s doc for why re-sorting here would reintroduce the PEG \"hiding\n+//     problem\" `IRRuleBody.Folded` and the reordering both exist to avoid).\n+//   - `Folded(bases, operators)`: match one of `bases` once, then a real greedy loop over\n+//     `operators`, left-folding each match onto the accumulator — the direct Scala translation of\n+//     `IRRewrittenSuite`'s own reference-walker `loop` function, which is this backend's design\n+//     proof; the parity gate (`ScalaPegParityMain`/`check-scala-peg-parity.mjs`) proves the\n+//     translation is faithful by actually compiling and running the emitted code.\n+object BackendScalaPeg:\n+  val backend: Backend = Backend(\n+    name = \"scala-peg\",\n+    capabilities = Vector(Capability.Cst),\n+    strategies = Vector(\"ll-star\"),\n+    emit = ir => Vector(Output(s\"${ir.grammar.name}.scala\", emit(ir)))\n+  )\n+\n+  private def banner(ir: IR, what: String): String =\n+    s\"// GENERATED by Gramaire from the `${ir.grammar.name}` grammar — $what.\\n\" +\n+      \"// Do not edit; regenerate with `gramaire emit --backend scala-peg --strategy ll-star`.\"\n+\n+  private def strLit(s: String): String =\n+    \"\\\"\" + s.flatMap {\n+      case '\"'  => \"\\\\\\\"\"\n+      case '\\\\' => \"\\\\\\\\\"\n+      case '\\n' => \"\\\\n\"\n+      case '\\t' => \"\\\\t\"\n+      case c    => c.toString\n+    } + \"\\\"\"\n+\n+  // A grammar rule name is a `.gram.md` identifier already (letters/digits/underscore) — this is\n+  // a defensive normalization, not something the real corpus ever exercises, so an arbitrary\n+  // (deterministic) substitution for anything else is fine.\n+  private def ident(name: String): String =\n+    val safe = name.map(c => if c.isLetterOrDigit || c == '_' then c else '_')\n+    if safe.headOption.exists(_.isDigit) then s\"_$safe\" else safe\n+\n+  private def parseFnName(rule: String): String = s\"parse_${ident(rule)}\"\n+\n+  private def originExpr(origin: IRAltOrigin, kidsExpr: String): String = origin match\n+    case IRAltOrigin.Unwrap           => s\"$kidsExpr.head\"\n+    case IRAltOrigin.Original(prodId) => s\"Cst.Branch($prodId, $kidsExpr)\"\n+\n+  // Translates one alt's `IRProv` to a Scala expression computing its tagged `Cst`, given\n+  // `kidsVar` (a `Vector[Cst]` already bound in scope) and — only meaningful inside a `Folded`\n+  // rule's own operator alts — `accVar` (the fold's running accumulator). Mirrors\n+  // `IRRewrittenSuite.resolveProv` exactly, just emitting Scala source instead of interpreting.\n+  private def tagExpr(prov: IRProv, kidsVar: String, accVar: Option[String], depth: Int): String =\n+    prov match\n+      case IRProv.Leaf(origin) => originExpr(origin, kidsVar)\n+      case IRProv.OpLeaf(origin) =>\n+        val acc = accVar.getOrElse(\n+          throw new IllegalStateException(\"BackendScalaPeg: OpLeaf outside an operator context\")\n+        )\n+        originExpr(origin, s\"($acc +: $kidsVar)\")\n+      case IRProv.Wrap(origin, innerSpan, inner) =>\n+        val innerKids = s\"innerKids$depth\"\n+        val ownKids = s\"ownKids$depth\"\n+        val innerExpr = tagExpr(inner, innerKids, accVar, depth + 1)\n+        s\"{ val ($innerKids, $ownKids) = $kidsVar.splitAt($innerSpan); \" +\n+          s\"${originExpr(origin, s\"($innerExpr +: $ownKids)\")} }\"\n+\n+  // One alt's full match-and-tag body: a brace-delimited for-comprehension over its own symbols\n+  // (each a terminal match or a recursive rule call), starting from `startPos`, yielding\n+  // `(taggedCst, posAfter)`. Every block below uses explicit `{ }` rather than relying on Scala\n+  // 3's significant-indentation rule — the codegen only tracks string content, not column\n+  // position, so off-side-rule indentation is not safely derivable here.\n+  private def altBody(alt: IRRewrittenAlt, startPos: String, accVar: Option[String]): String =\n+    val n = alt.syms.length\n+    val posName: Int => String = i => if i == 0 then startPos else s\"p$i\"\n+    val steps = alt.syms.zipWithIndex.map { case (sym, i) =>\n+      val kid = s\"k$i\"\n+      val posIn = posName(i)\n+      val posOut = posName(i + 1)\n+      sym match\n+        case IRRewrittenSym.Terminal(label) =>\n+          s\"($kid, $posOut) <- matchTerminal(toks, $posIn, ${strLit(label)})\"\n+        case IRRewrittenSym.NonTerminal(rule) =>\n+          s\"($kid, $posOut) <- ${parseFnName(rule)}(toks, $posIn)\"\n+    }\n+    val kidsVec = s\"Vector(${(0 until n).map(i => s\"k$i\").mkString(\", \")})\"\n+    val finalPos = posName(n)\n+    val tagged = tagExpr(alt.prov, kidsVec, accVar, depth = 0)\n+    if n == 0 then s\"Some(($tagged, $finalPos))\"\n+    else s\"for { ${steps.mkString(\"; \")} } yield ($tagged, $finalPos)\"\n+\n+  // No leading parenthesis on the result: this string is always embedded as the first token of\n+  // its own line, immediately after a previous statement — a leading `(` there is a real bug, not\n+  // a style nit. Scala's newline-vs-continuation rule attaches a line starting with `(` to the\n+  // PRECEDING line's trailing token as a function application (`None` \\n `(x)` parses as\n+  // `None(x)`, only failing at type-check time with a confusing \"does not take parameters\"\n+  // error) — verified by compiling the generated output directly, not just reasoned about.\n+  private def orElseChain(exprs: Vector[String]): String =\n+    exprs.reduceLeft((acc, e) => s\"$acc.orElse($e)\")\n+\n+  // Every generated function starts at column 0 — the caller (`emit`, via `indented`) shifts the\n+  // whole module uniformly by one level when nesting it inside `object $name:`, so this only ever\n+  // has to get ITS OWN relative nesting right, not guess the column it will end up printed at.\n+  private def ruleFn(r: IRRewrittenRule): String =\n+    val fnName = parseFnName(r.name)\n+    r.body match\n+      case IRRuleBody.Plain(alts) =>\n+        val altDefs = alts.zipWithIndex.map { case (a, i) =>\n+          s\"  def alt$i: Option[(Cst, Int)] = ${altBody(a, \"pos\", None)}\"\n+        }\n+        val chain = orElseChain(alts.indices.map(i => s\"alt$i\").toVector)\n+        s\"\"\"def $fnName(toks: Vector[Token], pos: Int): Option[(Cst, Int)] = {\n+${altDefs.mkString(\"\\n\")}\n+  $chain\n+}\"\"\"\n+      case IRRuleBody.Folded(bases, operators) =>\n+        val baseDefs = bases.zipWithIndex.map { case (a, i) =>\n+          s\"  def base$i: Option[(Cst, Int)] = ${altBody(a, \"pos\", None)}\"\n+        }\n+        val baseChain = orElseChain(bases.indices.map(i => s\"base$i\").toVector)\n+        val opDefs = operators.zipWithIndex.map { case (a, i) =>\n+          s\"    def op$i(p: Int): Option[(Cst, Int)] = ${altBody(a, \"p\", Some(\"acc\"))}\"\n+        }\n+        val opChain = orElseChain(operators.indices.map(i => s\"op$i(p)\").toVector)\n+        s\"\"\"def $fnName(toks: Vector[Token], pos: Int): Option[(Cst, Int)] = {\n+${baseDefs.mkString(\"\\n\")}\n+  $baseChain.map { case (baseCst, basePos) =>\n+    @scala.annotation.tailrec\n+    def loop(acc: Cst, p: Int): (Cst, Int) = {\n+${opDefs.mkString(\"\\n\")}\n+      $opChain match {\n+        case None             => (acc, p)\n+        case Some((next, p2)) => loop(next, p2)\n+      }\n+    }\n+    loop(baseCst, basePos)\n+  }\n+}\"\"\"\n+\n+  private val preamble: String =\n+    Vector(\n+      \"final case class Token(terminal: String, text: String)\",\n+      \"\",\n+      \"enum Cst derives CanEqual:\",\n+      \"  case Branch(rule: Int, kids: Vector[Cst])\",\n+      \"  case Token(terminal: String, text: String)\",\n+      \"\",\n+      \"def matchTerminal(toks: Vector[Token], pos: Int, label: String): Option[(Cst, Int)] =\",\n+      \"  toks.lift(pos).filter(_.terminal == label).map(t => (Cst.Token(t.terminal, t.text), pos + 1))\"\n+    ).mkString(\"\\n\")\n+\n+  // Every line of `body` shifted one indent level deeper, for nesting inside `object $name:`\n+  // below — the ONE place this backend relies on Scala's off-side rule (a `object name:` block's\n+  // own top-level members just need consistent indentation, not any exact column), since wrapping\n+  // the whole module keeps each generated grammar in its own namespace: a `Token`/`Cst`/`parse`\n+  // defined by one grammar's generated file can never collide with another's on the same\n+  // classpath (`codegen-scratch`'s parity driver may compile more than one grammar's output at\n+  // once — see Phase 3 of the PEG-engine plan).\n+  private def indented(body: String): String =\n+    body.linesIterator.map(l => if l.isEmpty then l else s\"  $l\").mkString(\"\\n\")\n+\n+  /** Emit the `<Name>.scala` module (a single `object <Name>:` — see `indented`'s doc for why), or\n+    * an error-commented stub if `ir.rewritten` is absent (shouldn't happen via `gramaire emit`,\n+    * which gates on `--strategy ll-star` before calling any backend — defensive for a caller that\n+    * constructs/invokes this backend directly, or whose grammar `IR.rewrittenGrammarOf` declined,\n+    * e.g. a genuinely mutually left-recursive one).\n+    */\n+  def emit(ir: IR): String =\n+    val objName = ident(ir.grammar.name)\n+    ir.rewritten match\n+      case None =>\n+        banner(ir, \"PEG parser\") +\n+          s\"\\nobject $objName:\\n\" +\n+          \"  // ERROR: no rewritten grammar on this IR — build with --strategy ll-star,\\n\" +\n+          \"  // or this grammar's own shape isn't yet supported (see IR.rewrittenGrammarOf's doc).\\n\" +\n+          \"  def recognize(toks: Vector[Nothing]): Boolean = throw new RuntimeException(\\\"unavailable\\\")\\n\"\n+      case Some(rg) =>\n+        val fns = rg.rules.map(ruleFn)\n+        val startFn = parseFnName(rg.start)\n+        val body = Vector(\n+          preamble,\n+          \"\",\n+          fns.mkString(\"\\n\\n\"),\n+          s\"\"\"def parse(toks: Vector[Token]): Option[Cst] =\n+  $startFn(toks, 0).collect { case (cst, pos) if pos == toks.length => cst }\n+\"\"\",\n+          \"\"\"def recognize(toks: Vector[Token]): Boolean = parse(toks).isDefined\"\"\"\n+        ).mkString(\"\\n\")\n+        Vector(\n+          banner(ir, \"PEG parser (strategy ll-star)\"),\n+          \"\",\n+          s\"object $objName:\",\n+          indented(body)\n+        ).mkString(\"\\n\")","raw_url":"https://github.com/wstein/gramaire/raw/b0074fd2390cf29e72abd0b5e48cf411f871ff11/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendScalaPeg.scala","sha":"85f8c78519eb9b96b46561870aba4be7a3088cdf","status":"added"},{"additions":54,"blob_url":"https://github.com/wstein/gramaire/blob/b0074fd2390cf29e72abd0b5e48cf411f871ff11/test%2Fgolden%2FCalc.scala","changes":54,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/test%2Fgolden%2FCalc.scala?ref=b0074fd2390cf29e72abd0b5e48cf411f871ff11","deletions":0,"filename":"test/golden/Calc.scala","patch":"@@ -0,0 +1,54 @@\n+// GENERATED by Gramaire from the `Calc` grammar — PEG parser (strategy ll-star).\n+// Do not edit; regenerate with `gramaire emit --backend scala-peg --strategy ll-star`.\n+\n+object Calc:\n+  final case class Token(terminal: String, text: String)\n+\n+  enum Cst derives CanEqual:\n+    case Branch(rule: Int, kids: Vector[Cst])\n+    case Token(terminal: String, text: String)\n+\n+  def matchTerminal(toks: Vector[Token], pos: Int, label: String): Option[(Cst, Int)] =\n+    toks.lift(pos).filter(_.terminal == label).map(t => (Cst.Token(t.terminal, t.text), pos + 1))\n+\n+  def parse_Expr(toks: Vector[Token], pos: Int): Option[(Cst, Int)] = {\n+    def base0: Option[(Cst, Int)] = for { (k0, p1) <- parse_Term(toks, pos) } yield (Cst.Branch(2, Vector(k0)), p1)\n+    base0.map { case (baseCst, basePos) =>\n+      @scala.annotation.tailrec\n+      def loop(acc: Cst, p: Int): (Cst, Int) = {\n+      def op0(p: Int): Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, p, \"+\"); (k1, p2) <- parse_Term(toks, p1) } yield (Cst.Branch(0, (acc +: Vector(k0, k1))), p2)\n+      def op1(p: Int): Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, p, \"-\"); (k1, p2) <- parse_Term(toks, p1) } yield (Cst.Branch(1, (acc +: Vector(k0, k1))), p2)\n+        op0(p).orElse(op1(p)) match {\n+          case None             => (acc, p)\n+          case Some((next, p2)) => loop(next, p2)\n+        }\n+      }\n+      loop(baseCst, basePos)\n+    }\n+  }\n+\n+  def parse_Term(toks: Vector[Token], pos: Int): Option[(Cst, Int)] = {\n+    def base0: Option[(Cst, Int)] = for { (k0, p1) <- parse_Factor(toks, pos) } yield (Cst.Branch(5, Vector(k0)), p1)\n+    base0.map { case (baseCst, basePos) =>\n+      @scala.annotation.tailrec\n+      def loop(acc: Cst, p: Int): (Cst, Int) = {\n+      def op0(p: Int): Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, p, \"*\"); (k1, p2) <- parse_Factor(toks, p1) } yield (Cst.Branch(3, (acc +: Vector(k0, k1))), p2)\n+      def op1(p: Int): Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, p, \"/\"); (k1, p2) <- parse_Factor(toks, p1) } yield (Cst.Branch(4, (acc +: Vector(k0, k1))), p2)\n+        op0(p).orElse(op1(p)) match {\n+          case None             => (acc, p)\n+          case Some((next, p2)) => loop(next, p2)\n+        }\n+      }\n+      loop(baseCst, basePos)\n+    }\n+  }\n+\n+  def parse_Factor(toks: Vector[Token], pos: Int): Option[(Cst, Int)] = {\n+    def alt0: Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, pos, \"(\"); (k1, p2) <- parse_Expr(toks, p1); (k2, p3) <- matchTerminal(toks, p2, \")\") } yield (Cst.Branch(6, Vector(k0, k1, k2)), p3)\n+    def alt1: Option[(Cst, Int)] = for { (k0, p1) <- matchTerminal(toks, pos, \"NUMBER\") } yield (Cst.Branch(7, Vector(k0)), p1)\n+    alt0.orElse(alt1)\n+  }\n+  def parse(toks: Vector[Token]): Option[Cst] =\n+    parse_Expr(toks, 0).collect { case (cst, pos) if pos == toks.length => cst }\n+\n+  def recognize(toks: Vector[Token]): Boolean = parse(toks).isDefined\n\\ No newline at end of file","raw_url":"https://github.com/wstein/gramaire/raw/b0074fd2390cf29e72abd0b5e48cf411f871ff11/test%2Fgolden%2FCalc.scala","sha":"afacb689135f272cc389f6a8d2d6af4a59a07846","status":"added"}]}