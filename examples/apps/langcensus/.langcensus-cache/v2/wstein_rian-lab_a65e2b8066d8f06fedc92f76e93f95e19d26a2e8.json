{"files":[{"additions":38,"blob_url":"https://github.com/wstein/rian-lab/blob/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/lib%2Frian%2Ftranspile.ex","changes":38,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Ftranspile.ex?ref=a65e2b8066d8f06fedc92f76e93f95e19d26a2e8","deletions":0,"filename":"lib/rian/transpile.ex","patch":"@@ -181,6 +181,7 @@ defmodule Rian.Transpile do\n     inner =\n       body\n       |> block_stmts()\n+      |> Enum.flat_map(&expand_defaults/1)\n       |> render_items(sigmap, name, struct_anns, referenced)\n       |> Enum.map(&indent/1)\n \n@@ -189,6 +190,43 @@ defmodule Rian.Transpile do\n     [\"mod #{name} do\" | type_lines ++ inner] ++ [\"end\"]\n   end\n \n+  # Rian has no default arguments, so a `def f(a, opts \\\\ [])` expands into a real\n+  # clause `def f(a, opts) := body` plus one delegating clause per trailing default\n+  # (`def f(a) := f(a, [])`) — the same desugaring Elixir performs. Only expanded\n+  # when every parameter is a plain variable (so forwarding by name is sound);\n+  # otherwise the defaults are stripped to a single clause.\n+  defp expand_defaults({df, m, [{name, hm, params}, kw]})\n+       when df in [:def, :defp] and is_atom(name) and is_list(params) and\n+              kw != [] do\n+    defaulted = Enum.count(params, &match?({:\\\\, _, _}, &1))\n+    plain = Enum.map(params, &strip_default/1)\n+\n+    cond do\n+      defaulted == 0 ->\n+        [{df, m, [{name, hm, params}, kw]}]\n+\n+      Enum.all?(plain, &var?/1) ->\n+        n = length(params)\n+\n+        delegators =\n+          for keep <- (n - defaulted)..(n - 1) do\n+            taken = Enum.take(plain, keep)\n+            call_args = taken ++ Enum.map(Enum.drop(params, keep), fn {:\\\\, _, [_, d]} -> d end)\n+            {df, m, [{name, hm, taken}, [do: {name, [], call_args}]]}\n+          end\n+\n+        delegators ++ [{df, m, [{name, hm, plain}, kw]}]\n+\n+      true ->\n+        [{df, m, [{name, hm, plain}, kw]}]\n+    end\n+  end\n+\n+  defp expand_defaults(stmt), do: [stmt]\n+\n+  defp strip_default({:\\\\, _, [p, _]}), do: p\n+  defp strip_default(p), do: p\n+\n   # split `@rian` annotation strings into `{def sigmap, struct-by-name, [type decl]}`.\n   # Whitespace is collapsed so a heredoc multi-line decl parses/emits as one line.\n   defp classify_annotations(strings) do","raw_url":"https://github.com/wstein/rian-lab/raw/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/lib%2Frian%2Ftranspile.ex","sha":"f1ace118dbfce0e5a7b702d1af2a41039a68a174","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/rian-lab/blob/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/test%2Frian%2Fdecl_test.exs","changes":37,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_test.exs?ref=a65e2b8066d8f06fedc92f76e93f95e19d26a2e8","deletions":0,"filename":"test/rian/decl_test.exs","patch":"@@ -719,6 +719,43 @@ defmodule Rian.DeclTest do\n         \"\"\")\n       end\n     end\n+\n+    test \"same-name different-arity overloads load and dispatch as distinct functions\" do\n+      {:ok, m} =\n+        Rian.Beam.load(\n+          \"\"\"\n+          mod Ov do\n+            pub def f(x Int53) Int53 := x\n+            pub def f(x Int53, y Int53) Int53 := x + y\n+          end\n+          \"\"\",\n+          :rian_decl_overload\n+        )\n+\n+      assert m.f(7) == 7\n+      assert m.f(10, 20) == 30\n+    end\n+\n+    test \"delegating clauses run (the shape default-args desugar to: f/1 -> f/2 -> f/3)\" do\n+      # `def f(a, opts \\\\ [], n \\\\ 0)` lowers to one full clause plus a delegating\n+      # clause per default; arity overloading makes all three distinct and the\n+      # by-name forwarding resolves at the call site.\n+      {:ok, m} =\n+        Rian.Beam.load(\n+          \"\"\"\n+          mod Dlg do\n+            pub def f(a Int53) Tuple := f(a, [], 0)\n+            pub def f(a Int53, opts Vec(Int53)) Tuple := f(a, opts, 0)\n+            pub def f(a Int53, opts Vec(Int53), n Int53) Tuple := {a, opts, n}\n+          end\n+          \"\"\",\n+          :rian_decl_delegate\n+        )\n+\n+      assert m.f(1) == {1, [], 0}\n+      assert m.f(1, [2]) == {1, [2], 0}\n+      assert m.f(1, [2], 3) == {1, [2], 3}\n+    end\n   end\n \n   describe \"struct declarations (product types)\" do","raw_url":"https://github.com/wstein/rian-lab/raw/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/test%2Frian%2Fdecl_test.exs","sha":"18d463b0e4f5e3f622d604c355fd646561759921","status":"modified"},{"additions":58,"blob_url":"https://github.com/wstein/rian-lab/blob/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/test%2Frian%2Ftranspile_test.exs","changes":58,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Ftranspile_test.exs?ref=a65e2b8066d8f06fedc92f76e93f95e19d26a2e8","deletions":0,"filename":"test/rian/transpile_test.exs","patch":"@@ -445,6 +445,64 @@ end|) =~ ~S|\"v=${x}!\"|\n     end\n   end\n \n+  describe \"default arguments desugar into delegating clauses (arity overloading)\" do\n+    test \"a `\\\\\\\\`-default function expands to one delegating clause per default\" do\n+      out =\n+        rian(\"\"\"\n+        defmodule M do\n+          def f(a, opts \\\\\\\\ [], n \\\\\\\\ 0) do\n+            {a, opts, n}\n+          end\n+        end\n+        \"\"\")\n+\n+      # the full clause plus one delegating clause per trailing default, exactly\n+      # like Elixir's desugaring — f/1 -> f/2 -> f/3\n+      assert out =~ \"pub def f(a _Unk) _Unk := f(a, [], 0)\"\n+      assert out =~ \"pub def f(a _Unk, opts _Unk) _Unk := f(a, opts, 0)\"\n+      assert out =~ \"pub def f(a _Unk, opts _Unk, n _Unk) _Unk := {a, opts, n}\"\n+      # no leftover `\\\\` and no port marker on the emitted body (the header always\n+      # mentions TODO_PORT generically — check the code after `mod M do`)\n+      body = out |> String.split(\"mod M do\") |> List.last()\n+      refute body =~ \"\\\\\\\\\"\n+      refute body =~ \"TODO\"\n+    end\n+\n+    test \"a single trailing default yields exactly one delegator\" do\n+      out =\n+        rian(\"\"\"\n+        defmodule M do\n+          def greet(name, greeting \\\\\\\\ \"hi\") do\n+            greeting\n+          end\n+        end\n+        \"\"\")\n+\n+      assert out =~ ~S|pub def greet(name _Unk) _Unk := greet(name, \"hi\")|\n+      assert out =~ \"pub def greet(name _Unk, greeting _Unk) _Unk := greeting\"\n+    end\n+\n+    test \"a non-variable parameter alongside a default strips defaults to one clause\" do\n+      # forwarding by name is unsound when a param is a pattern (not a plain var),\n+      # so the defaults are dropped to a single clause rather than mis-delegated.\n+      out =\n+        rian(\"\"\"\n+        defmodule M do\n+          def f({x, y}, opts \\\\\\\\ []) do\n+            {x, y, opts}\n+          end\n+        end\n+        \"\"\")\n+\n+      body = out |> String.split(\"mod M do\") |> List.last()\n+      assert body =~ \"pub def f(\"\n+      refute body =~ \"\\\\\\\\\"\n+      # no delegating clause was synthesised (a pattern param can't be forwarded by\n+      # name) — there is no `:= f(` self-call\n+      refute body =~ \":= f(\"\n+    end\n+  end\n+\n   describe \"transpile_with_stats\" do\n     test \"counts def groups and unresolved markers\" do\n       {_text, stats} =","raw_url":"https://github.com/wstein/rian-lab/raw/a65e2b8066d8f06fedc92f76e93f95e19d26a2e8/test%2Frian%2Ftranspile_test.exs","sha":"50e6813e56839f81cafd5b6eed28a02015b6b940","status":"modified"}]}