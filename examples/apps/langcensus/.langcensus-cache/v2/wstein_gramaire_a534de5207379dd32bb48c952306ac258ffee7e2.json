{"files":[{"additions":18,"blob_url":"https://github.com/wstein/gramaire/blob/a534de5207379dd32bb48c952306ac258ffee7e2/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":19,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=a534de5207379dd32bb48c952306ac258ffee7e2","deletions":1,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -128,7 +128,7 @@ a build error.\n A `name:X` prefix names that right-hand position. The name is carried onto the\n IR's `rhs[].field` and feeds CST accessors and generated visitors; it does not\n change the recognized language. A bare-body action then binds the field names: a\n-production `left:NUM `+` right:NUM {% Add left right %}` rewrites to\n+production `left:NUM '+' right:NUM {% Add left right %}` rewrites to\n `\\left _ right -> Add left right`.\n \n ### 4.5 Inline folding — `#[inline]`\n@@ -143,6 +143,23 @@ through a wrapper so values still flow.\n A `# Name` label names an alternative. It surfaces in the IR as an optional\n `rules[].label` and drives per-alternative visitor method names and CST accessors.\n \n+For a structured AST, **prefer labels over inline `{% … %}` actions** (ADR\n+D37). A label like `# MulDiv` generates a typed `MulDiv(children)` method on the\n+emitted `Visitor<T>` — exactly ANTLR's `visitMulDiv` pattern, and\n+host-language-agnostic. Inline lambdas, by contrast, pin the grammar to one host\n+(PureScript). So:\n+\n+```grammark\n+expr\n+  : expr '*' term   # MulDiv\n+  | expr '+' term   # AddSub\n+  | NUM             # Int\n+```\n+\n+emits `interface Visitor<T> { MulDiv(c): T; AddSub(c): T; Int(c): T; … }` — you\n+fill in the methods in your host language, no inline lambda required. Inline\n+`{% … %}` stays available for one-liners where a visitor class is overkill.\n+\n ## 5. The IR contract — `grammark-ir`\n \n The compiler emits the analyzed grammar and its parse tables as a versioned JSON","raw_url":"https://github.com/wstein/gramaire/raw/a534de5207379dd32bb48c952306ac258ffee7e2/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"da92aa43ffad9779a09f006deb16990d546391d7","status":"modified"},{"additions":24,"blob_url":"https://github.com/wstein/gramaire/blob/a534de5207379dd32bb48c952306ac258ffee7e2/spec%2Fincremental-spec.md","changes":24,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/spec%2Fincremental-spec.md?ref=a534de5207379dd32bb48c952306ac258ffee7e2","deletions":0,"filename":"spec/incremental-spec.md","patch":"@@ -106,6 +106,30 @@ keystrokes, so recovery is mandatory, not optional.\n   the point of failure (for diagnostics) and the skipped tokens (for R1).\n - **R12 (determinism).** Recovery MUST be deterministic: identical input MUST\n   yield an identical error tree, so error placement is conformance-testable.\n+- **R13 (`error`-token recovery, spec-first — not yet implemented).** A\n+  production MAY use the reserved `error` pseudo-token to declare a\n+  **grammar-directed** sync point (yacc/Bison style), the explicit complement to\n+  R10's automatic panic-mode. On an error inside such a rule the runtime resyncs\n+  to the token that follows `error` in the production and reduces that\n+  alternative — instead of panic-popping to a global `syncToken`. This lets the\n+  grammar author place recovery where it makes sense (e.g. at a statement\n+  terminator). The conformance vector below specifies the target behavior\n+  **before** the engine implements it (the debate's spec-first rule); it joins\n+  the corpus when R13 ships, and until then is documented here, not asserted.\n+\n+  ```grammark\n+  Stmts : Stmt | Stmts Stmt\n+  Stmt  : Expr ';'        # Ok\n+        | error ';'       # Recovered   {% \\_ _ -> Recovered %}\n+  ```\n+\n+  Vector — input `1 + ; 2 ;` → outcome **Recover**, tree (sketch)\n+  `Stmts[ Stmt#Recovered(Error \"1 +\"), Stmt#Ok 2 ]`.\n+\n+  The first statement is malformed; `error ';'` consumes through the `;`,\n+  emits one `Error` node (R11) over the skipped `1 +`, and parsing **continues**\n+  so the well-formed `2 ;` still parses. Per R12 the placement is deterministic,\n+  so this is a conformance vector, not a hand-check.\n \n ## 5. The `edit()` contract\n ","raw_url":"https://github.com/wstein/gramaire/raw/a534de5207379dd32bb48c952306ac258ffee7e2/spec%2Fincremental-spec.md","sha":"d9fe9744826f49cb2cc8b6503951b4a15951c647","status":"modified"}]}