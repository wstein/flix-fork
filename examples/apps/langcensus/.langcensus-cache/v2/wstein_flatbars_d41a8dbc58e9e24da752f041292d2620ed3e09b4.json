{"files":[{"additions":15,"blob_url":"https://github.com/wstein/flatbars/blob/d41a8dbc58e9e24da752f041292d2620ed3e09b4/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0041-per-surface-lexer-parser.mdx","changes":23,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0041-per-surface-lexer-parser.mdx?ref=d41a8dbc58e9e24da752f041292d2620ed3e09b4","deletions":8,"filename":"spec/src/content/docs/adr/adr-0041-per-surface-lexer-parser.mdx","patch":"@@ -168,13 +168,20 @@ built:\n   better than a pure erasure would give. `{{{ … }}}` raw output and `{{! … }}` comments **are** erased\n   (`bracesOutputOnly` baked in — `{{{` is a lex error, `{{!` is `not` output, the comment is\n   `{# … #}`).\n-- **Phase 4 slimmed the parser seam, not the lexer-interior knobs.** The dead\n-  `ParseOptions.parseExpr`/`parseHead` seam, the `ExprParser` type, and the `clauseSeps` gate were\n-  removed from the shared parser (no remaining dialect distinguishes head from output grammar). The\n-  `LexOptions` knobs (`operatorChars`/`rangeOperator`/`collectionLiterals`) were **kept**: MaxBars\n-  *highlighting* still runs the shared `tokenizeSpans` over the shared lexer with MaxBars's (now\n-  inlined) config — highlighting is a separate axis from the parser coupling §1.1 targets, so owning a\n-  MaxBars `tokenizeSpans` is a deferred follow-up, not part of this freeze.\n+- **MaxBars owns its *entire* front-end — four modules, not two.** Beyond `MaxBars.Lexer` +\n+  `MaxBars.Parser`, the fork extended to `MaxBars.Token` (the interior expression tokenizer, with the\n+  infix alphabet / `..` range / `[…]`·`{k:v}` collections baked in) and `MaxBars.Highlight` (spans from\n+  the owned lexer). Nothing in MaxBars's `String → Template`/spans path runs shared lexing code; it\n+  shares only the AST *types* (`Syntax`, `Token`/`PosToken`/`Interior`, `Value`, `Span`) and the\n+  back-end (desugar / engine / compile).\n+- **Phase 4 fully slimmed the shared core.** Removed from the shared front-end, all now-dead: the\n+  `ParseOptions.parseExpr`/`parseHead` seam + `ExprParser` type + `clauseSeps` gate (no remaining\n+  dialect distinguishes head from output grammar), and the `LexOptions` knobs\n+  (`operatorChars`/`rangeOperator`/`collectionLiterals`) + `defaultLexOptions` + `infixOperatorChars`\n+  (no remaining dialect sets them non-default). `FlatBars.Token`'s `tokenizeInterior` is now the fixed\n+  path/name-only grammar, and `FlatBars.Lexer.tokenizeTemplate` / `ParseOptions` / `HighlightConfig`\n+  shed their `lexOptions`. The shared front-end is exactly what RawBars/ClassicBars/MinBars need.\n - The Phase 1–2 equivalence gates were retired with `maxOptions` once the fork became the production\n   path; the full conformance suite (`test:compile`, `check:parity`, `examples`, `check:provenance`,\n-  `check:parse-views`, the differential handlebars/mustache proofs) now covers it.\n+  `check:parse-views`, the highlight/vocab/tmgrammar gates, the differential handlebars/mustache\n+  proofs) now covers it.","raw_url":"https://github.com/wstein/flatbars/raw/d41a8dbc58e9e24da752f041292d2620ed3e09b4/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0041-per-surface-lexer-parser.mdx","sha":"27cc359128a5fc0064e2e098eca960c2bbb6d98d","status":"modified"}]}