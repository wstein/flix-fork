{"files":[{"additions":13,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/docs%2Fadr%2F0035-no-hidden-control-flow.md","changes":13,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0035-no-hidden-control-flow.md?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":0,"filename":"docs/adr/0035-no-hidden-control-flow.md","patch":"@@ -42,6 +42,18 @@ Rian adopts **No Hidden Control Flow** as a standing design principle. Concretel\n    single-expression body. This is the ML-family discipline — OCaml/Haskell/F#/Rust all require a\n    trailing expression, never a bare `let`.\n \n+   > **The same rule for `if` — `else` is mandatory in value position (enforced 2026-06-17).**\n+   > `if` is an expression: where its **value is used** (return, binding RHS, function argument, or a\n+   > branch that itself feeds a used value) it **must** have an `else`, and both branches must yield\n+   > the same type. An `else`-less `if` is legal *only* as an **effect statement** — a non-final\n+   > statement of a block whose value is discarded (unit-typed, as in Rust). A value-position\n+   > `else`-less `if` is a **compile error** (`Rian.Check.check_if_else`), which threads a value/effect\n+   > *position* through the body: a block's non-final statements are effects, its final statement\n+   > inherits the block's position, and `if`/`case`/`with` branches inherit the position of the\n+   > construct they belong to. The position context is why this is a `Check` gate, not a Core or\n+   > parser rule. (OCaml/Haskell require `else` on *every* `if` because they have no statement\n+   > position at all; Rian keeps the effect form, matching Rust.)\n+\n    > **Amended 2026-06-14 — enforced for Int↔Float arithmetic.** `+`/`-`/`*` whose two operands are\n    > concretely one integer-kind and one float-kind is a **compile error** (`Rian.Check.check_numeric_mix`),\n    > not a silent widen — e.g. `10.2 * a` with `a : Int64`. A value never silently becomes a float\n@@ -100,6 +112,7 @@ ADR-0036 `unreachable!()` shim — is loud, not hidden, and is the sole sanction\n | No silent partiality | 5/5 |\n | No implicit coercions | 4/5 |\n | Block value is the final expression, never a trailing binding | 5/5 |\n+| `if` is an expression; `else` is mandatory in value position | 5/5 |\n \n ## Consequences\n ","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/docs%2Fadr%2F0035-no-hidden-control-flow.md","sha":"8f7e8bac32553b1ed0aaafd9b00cd7959766e292","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/docs%2Fspec%2Fexpressions.md","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fspec%2Fexpressions.md?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":2,"filename":"docs/spec/expressions.md","patch":"@@ -36,8 +36,10 @@ grade :=\n   would return its RHS, but Rust lowers `let x = e;` to a `()`-typed block, a silent\n   cross-target divergence. Make the value the final line (add `x`), or use the `:= expr`\n   one-liner. (ML-family rule: OCaml/Haskell/F#/Rust all require a trailing expression.)\n-- A value-position `if` **must** have `else`. An `else`-less `if` is a unit-typed effect\n-  statement (matches Rust's `if` typing).\n+- A value-position `if` **must** have `else` — **enforced** by `Rian.Check` (ADR-0035 §6): an\n+  `if` whose value is used (return / binding RHS / argument / a branch feeding a used value) is a\n+  compile error without `else`. An `else`-less `if` is legal only as a unit-typed effect statement\n+  — a non-final statement of a block whose value is discarded (matches Rust's `if` typing).\n - `:=` bindings are **single-assignment** and **irrefutable** (tuple/struct destructuring is\n   fine; `Some(x) := opt` is refutable → compile error, use `match`). Rebinding a name is\n   shadowing, not mutation. Mutation is `<~` (capability-gated; BEAM-illegal unless local).","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/docs%2Fspec%2Fexpressions.md","sha":"9583009dccaa040a4b13504d61e0d33e33cac316","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/examples%2Frian%2F10_function_forms.rian","changes":3,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2F10_function_forms.rian?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":1,"filename":"examples/rian/10_function_forms.rian","patch":"@@ -58,7 +58,8 @@ def max2(a, b)             := b\n # --- (3) lambdas / closures: `(x) -> e` -----------------------------------\n # Anonymous, single-expression. Passed to higher-order functions (usually via\n # FFI). For multi-step logic inside a lambda, the body can be a block\n-# EXPRESSION (`if .. do a := ..; .. end`), or factor it into a named `def`.\n+# EXPRESSION (`if .. do a := ..; .. else .. end` — a lambda body is value\n+# position, so the `if` needs `else`), or factor it into a named `def`.\n def map_double(xs val Vec(Int53)) Vec(Int53) := Enum.map(xs, (x) -> x * 2)\n def clamp_each(xs val Vec(Int53)) Vec(Int53) :=\n   Enum.map(xs, (x) -> if x < 0 do 0 else x end)   # block-expression lambda body","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/examples%2Frian%2F10_function_forms.rian","sha":"b8eff8f772c2dc7a87b48f4c31676a636f1cd1ef","status":"modified"},{"additions":58,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/lib%2Frian%2Fcheck.ex","changes":58,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcheck.ex?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":0,"filename":"lib/rian/check.ex","patch":"@@ -669,9 +669,67 @@ defmodule Rian.Check do\n          :ok <- check_binds(f, ic),\n          :ok <- check_bounds(f, ic),\n          :ok <- check_numeric_mix(f, ic),\n+         :ok <- check_if_else(f),\n          do: check_error_set(f, eset)\n   end\n \n+  # ADR-0035 §6 — `if` is an expression; in **value position** (return / binding\n+  # RHS / argument / a branch feeding a used value) it must yield a value, so `else`\n+  # is mandatory there. An `else`-less `if` (empty else block) is legal only as an\n+  # **effect statement**: a non-final statement of a block, whose value is discarded.\n+  # The walk threads a position (`:value`/`:effect`) — a block's non-final statements\n+  # are effects, its final statement inherits the block's position, and `if`/`case`/\n+  # `with` branches inherit the position of the construct they belong to. (Guards\n+  # cannot contain `if`, so they are not walked.)\n+  defp check_if_else(%Func{clauses: clauses}) do\n+    Enum.find_value(clauses, :ok, fn c ->\n+      if_walk(Pratt.parse_body(c.body), :value)\n+    end)\n+  end\n+\n+  # value-position `if` with an empty else branch — the proven error\n+  defp if_walk({:if, cnd, _then, {:block, []}}, :value),\n+    do: if_walk(cnd, :value) || {:error, if_else_msg()}\n+\n+  defp if_walk({:if, cnd, then_b, else_b}, pos),\n+    do: if_walk(cnd, :value) || if_walk(then_b, pos) || if_walk(else_b, pos)\n+\n+  defp if_walk({:block, stmts}, pos), do: block_walk(stmts, pos)\n+\n+  defp if_walk({:case, scrut, arms}, pos),\n+    do: if_walk(scrut, :value) || Enum.find_value(arms, fn {_p, _g, b} -> if_walk(b, pos) end)\n+\n+  defp if_walk({:with, cls, body, els}, pos) do\n+    Enum.find_value(cls, fn {_p, e} -> if_walk(e, :value) end) ||\n+      if_walk(body, pos) ||\n+      Enum.find_value(els, fn {_p, _g, b} -> if_walk(b, pos) end)\n+  end\n+\n+  # any other node is a value-context container — its children are all used values\n+  defp if_walk(node, _pos) when is_tuple(node),\n+    do: node |> Tuple.to_list() |> Enum.find_value(&if_walk(&1, :value))\n+\n+  defp if_walk(list, _pos) when is_list(list),\n+    do: Enum.find_value(list, &if_walk(&1, :value))\n+\n+  defp if_walk(_leaf, _pos), do: nil\n+\n+  # a block: non-final statements are effects; the final statement keeps the block's pos\n+  defp block_walk([], _pos), do: nil\n+  defp block_walk([last], pos), do: stmt_walk(last, pos)\n+  defp block_walk([s | rest], pos), do: stmt_walk(s, :effect) || block_walk(rest, pos)\n+\n+  # a binding's RHS is always a used value; an `:expr` statement keeps the stmt's pos\n+  defp stmt_walk({:bind, _n, e}, _pos), do: if_walk(e, :value)\n+  defp stmt_walk({:typed_bind, _n, _t, e}, _pos), do: if_walk(e, :value)\n+  defp stmt_walk({:expr, e}, pos), do: if_walk(e, pos)\n+\n+  defp if_else_msg do\n+    \"`if` in value position must have an `else` branch (ADR-0035): it is an expression \" <>\n+      \"that yields a value. Add `else …` — or, if the value is unused, make the `if` a \" <>\n+      \"statement (not the final/returned expression of the block).\"\n+  end\n+\n   # ADR-0035 / ADR-0034 §1 — **no implicit Int↔Float coercion.** An arithmetic\n   # operator (`+`/`-`/`*`) whose two operands are *concretely* one integer-kind\n   # and one float-kind is a proven error: a value never silently changes numeric","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/lib%2Frian%2Fcheck.ex","sha":"9a4828693be78d64c86519d5d4a7b02cabd13ec5","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/lib%2Frian%2Fpratt.ex","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fpratt.ex?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":0,"filename":"lib/rian/pratt.ex","patch":"@@ -315,6 +315,11 @@ defmodule Rian.Pratt do\n     tokens = expect_kw(tokens, \"do\")\n     {then_b, tokens} = parse_block(tokens)\n \n+    # An `else`-less `if` parses to an empty else block: it is a unit-typed *effect*\n+    # statement (Rust's `if` typing). In **value** position (return / binding RHS /\n+    # argument / a branch feeding a used value) `else` is mandatory — that is an\n+    # expression that must yield a value — and is enforced by `Rian.Check`\n+    # (`check_if_else`, ADR-0035 §6), not here, since the parser lacks position context.\n     {else_b, tokens} =\n       case tokens do\n         [{:kw, \"else\"} | r] -> parse_block(r)","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/lib%2Frian%2Fpratt.ex","sha":"71836737b16ddba6f0ed16ac7bb7597c2a6823bf","status":"modified"},{"additions":44,"blob_url":"https://github.com/wstein/rian-lab/blob/93fd242594abf818546383acb7e2fb356f9ca9cc/test%2Frian%2Fcheck_test.exs","changes":44,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcheck_test.exs?ref=93fd242594abf818546383acb7e2fb356f9ca9cc","deletions":0,"filename":"test/rian/check_test.exs","patch":"@@ -637,6 +637,50 @@ defmodule Rian.CheckTest do\n     end\n   end\n \n+  describe \"`if` requires `else` in value position (ADR-0035 §6)\" do\n+    test \"an else-less `if` as the returned value is rejected\" do\n+      assert_raise Check.Error, ~r/value position must have an `else`/, fn ->\n+        Rian.Decl.compile(\"def f(n Int64) Int64 := if n > 0 do 1 end\")\n+      end\n+    end\n+\n+    test \"an else-less `if` on a binding's right-hand side is rejected\" do\n+      assert_raise Check.Error, ~r/value position must have an `else`/, fn ->\n+        Rian.Decl.compile(\"\"\"\n+        def f(n Int64) Int64\n+        def f(n)\n+          x := if n > 0 do 1 end\n+          x\n+        end\n+        \"\"\")\n+      end\n+    end\n+\n+    test \"an else-less `if` as a function argument is rejected\" do\n+      assert_raise Check.Error, ~r/value position must have an `else`/, fn ->\n+        Rian.Decl.compile(\"\"\"\n+        def g(x Int64) Int64 := x\n+        def f(n Int64) Int64 := g(if n > 0 do 1 end)\n+        \"\"\")\n+      end\n+    end\n+\n+    test \"an else-less `if` as a non-final effect statement is allowed\" do\n+      assert [{\"f\", _}] =\n+               Rian.Decl.compile(\"\"\"\n+               def f(n Int64) Int64\n+               def f(n)\n+                 if n > 0 do dbg(n) end\n+                 n\n+               end\n+               \"\"\")\n+    end\n+\n+    test \"a value-position `if` with both branches compiles\" do\n+      assert [{\"f\", _}] = Rian.Decl.compile(\"def f(n Int64) Int64 := if n > 0 do 1 else 0 end\")\n+    end\n+  end\n+\n   describe \"join lattice — least-upper-bound (ADR-0059)\" do\n     test \"numeric LUB: same kind widens; differing widths climb\" do\n       assert Check.join(\"Int32\", \"Int64\") == \"Int64\"","raw_url":"https://github.com/wstein/rian-lab/raw/93fd242594abf818546383acb7e2fb356f9ca9cc/test%2Frian%2Fcheck_test.exs","sha":"f5553463d290638233afba8d01ae6f839cf5615e","status":"modified"}]}