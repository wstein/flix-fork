{"files":[{"additions":34,"blob_url":"https://github.com/wstein/gramaire/blob/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Fsrc%2Fshared%2Fgramaire-topbar.mjs","changes":36,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fshared%2Fgramaire-topbar.mjs?ref=ef2484db9ce702dc01041d5d903722e52af1683b","deletions":2,"filename":"site/src/shared/gramaire-topbar.mjs","patch":"@@ -152,8 +152,40 @@ const TEMPLATE = `\n \n   @media (max-width: 720px) {\n     nav.ctx { display: none; }\n-    ::slotted([slot=\"tools\"]) { width: auto; }\n-    .tools { flex: 1; }\n+    /* .tools is itself display:flex, so its slotted child (a plain <div>)\n+       becomes a flex ITEM of it — width:auto on a flex item does NOT\n+       mean \"fill the container\" the way it does on a block element; without\n+       flex-grow it resolves via the item's own content size instead,\n+       shrinking only as far as flex-shrink's negotiation forces it to. That\n+       negotiation is sensitive to the item's OWN intrinsic content width,\n+       which is why this rendered inconsistently between pages even though\n+       the CSS is identical on both — an explicit flex:1 fills .tools's\n+       actual available width deterministically, regardless of content.\n+       min-width, not just flex:1, on .tools itself: Starlight's own\n+       sidebar-toggle hamburger (rendered outside this element, in\n+       PageFrame.astro's sidebar nav — not something this component\n+       controls) reserves extra space on its own .header wrapper below its\n+       ~50rem breakpoint, which Landing's bare page never has to reserve (no\n+       sidebar there at all) — without a floor, the search box was the only\n+       flexible item competing for whatever's left and collapsed to a\n+       near-invisible size on Starlight pages specifically. */\n+    ::slotted([slot=\"tools\"]) { width: auto; min-width: 0; flex: 1; }\n+    .tools { flex: 1 1 auto; min-width: 88px; }\n+  }\n+\n+  @media (max-width: 460px) {\n+    .bar { gap: 0.625em; padding-inline: 0.875em; }\n+    .seg button { padding: 0.25em 0.5em; font-size: 0.6875em; }\n+    /* Brand's wordmark alone is wider than the search box's own min-width\n+       floor. On a Starlight page, this width band is also where its\n+       sidebar-toggle hamburger's reserved space (see the comment above)\n+       bites hardest — without reclaiming room somewhere, the segmented\n+       theme control had nowhere left to go and rendered completely\n+       off-screen (clipped by Starlight's own overflow handling, not even\n+       scrollable to) rather than just visually tight like on Landing.\n+       Dropping to icon-only keeps the brand recognizable without costing\n+       everything else that needs the room more at this width. */\n+    .wm { display: none; }\n   }\n </style>\n ","raw_url":"https://github.com/wstein/gramaire/raw/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Fsrc%2Fshared%2Fgramaire-topbar.mjs","sha":"4b0ac6f6d5d6aaa7bbef62f13b8e07f736acf03e","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/gramaire/blob/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Fsrc%2Fstyles%2Ftokens.css","changes":16,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fstyles%2Ftokens.css?ref=ef2484db9ce702dc01041d5d903722e52af1683b","deletions":0,"filename":"site/src/styles/tokens.css","patch":"@@ -49,6 +49,22 @@\n     border: 0 solid;\n     padding: 0;\n   }\n+  /*\n+   * Browsers don't inherit font-family into form controls by default —\n+   * they get the UA's own control font (Arial, in most) instead. Matches\n+   * Starlight's own reset.css exactly (same property, same selector, no\n+   * exclusion — safe everywhere, including Pagefind's own <input>, since\n+   * an explicit font-family there would still win via normal cascade).\n+   * Landing's own search trigger button rendered in Arial, not IBM Plex\n+   * Sans, without this — Starlight's reset.css has it, but (as with\n+   * everything else in this rule) only loads on Starlight-templated pages.\n+   */\n+  input,\n+  button,\n+  textarea,\n+  select {\n+    font: inherit;\n+  }\n }\n \n @font-face {","raw_url":"https://github.com/wstein/gramaire/raw/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Fsrc%2Fstyles%2Ftokens.css","sha":"536cd2a6e71a7a189e604ad707f3a93bb10d49b7","status":"modified"},{"additions":44,"blob_url":"https://github.com/wstein/gramaire/blob/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Ftests%2Fvisual%2Ftopbar-consistency.spec.ts","changes":44,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Ftests%2Fvisual%2Ftopbar-consistency.spec.ts?ref=ef2484db9ce702dc01041d5d903722e52af1683b","deletions":0,"filename":"site/tests/visual/topbar-consistency.spec.ts","patch":"@@ -115,6 +115,50 @@ test(\"search results render with identical text metrics across page pipelines\",\n   }\n });\n \n+for (const { name, path } of REPRESENTATIVE_PAGES) {\n+  test(`topbar controls stay reachable at mobile width — ${name}`, async ({\n+    page,\n+  }) => {\n+    // Starlight's own sidebar-toggle hamburger (rendered outside this\n+    // element — not something it controls) reserves ~48px on its own\n+    // .header at narrow widths, which Landing's bare page never has to.\n+    // Before this test existed, that reservation pushed the theme\n+    // segmented control fully off-screen on Starlight pages specifically —\n+    // not just visually tight like on Landing, genuinely unreachable,\n+    // clipped rather than scrollable to. 320px (roughly iPhone SE) is the\n+    // narrowest width this site is expected to support.\n+    await page.setViewportSize({ width: 320, height: 400 });\n+    await page.goto(path);\n+    await page.locator(\"gramaire-topbar\").waitFor();\n+    const info = await page.evaluate(() => {\n+      const topbar = document.querySelector(\"gramaire-topbar\")!;\n+      const seg = topbar.shadowRoot!.querySelector(\".seg\")!;\n+      const r = seg.getBoundingClientRect();\n+      return {\n+        segRight: r.right,\n+        segWidth: r.width,\n+        bodyScrollWidth: document.body.scrollWidth,\n+      };\n+    });\n+    expect(\n+      info.segWidth,\n+      `${name}: theme control should have a real rendered width`,\n+    ).toBeGreaterThan(0);\n+    expect(\n+      info.segRight,\n+      `${name}: theme control's right edge should stay within the viewport`,\n+    ).toBeLessThanOrEqual(320);\n+    // A few px of overflow is a rounding/sub-pixel tolerance at this width,\n+    // not the bug this guards against (a fully off-screen, unreachable\n+    // control) — draw the line well below \"needs its own horizontal\n+    // scrollbar to reach anything.\"\n+    expect(\n+      info.bodyScrollWidth,\n+      `${name}: page should not need significant horizontal scroll at 320px`,\n+    ).toBeLessThan(340);\n+  });\n+}\n+\n for (const { name, path } of REPRESENTATIVE_PAGES) {\n   test(`search dialog renders styled (border/padding) when open — ${name}`, async ({\n     page,","raw_url":"https://github.com/wstein/gramaire/raw/ef2484db9ce702dc01041d5d903722e52af1683b/site%2Ftests%2Fvisual%2Ftopbar-consistency.spec.ts","sha":"4dba83e926f9bd0445137be5c474da9a679e3c2d","status":"modified"}]}