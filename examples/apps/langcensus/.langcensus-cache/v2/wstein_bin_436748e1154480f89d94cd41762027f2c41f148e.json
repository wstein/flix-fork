{"files":[{"additions":19,"blob_url":"https://github.com/wstein/bin/blob/436748e1154480f89d94cd41762027f2c41f148e/histlog","changes":35,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=436748e1154480f89d94cd41762027f2c41f148e","deletions":16,"filename":"histlog","patch":"@@ -1112,7 +1112,7 @@ def query_interactive(args)\n     opts.banner = \"Usage: histlog query [search] [options]\"\n   opts.on(\"--delete\", \"Delete matching commands after confirmation\") { options[:delete] = true }\n   opts.on(\"--force-delete\", \"Delete matching commands without confirmation\") { options[:force_delete] = true }\n-  opts.on(\"--asc\", \"Sort results in ascending order (oldest first)\") { options[:asc] = true }\n+  opts.on(\"--asc\", \"Sort results in ascending order (oldest first) - DEFAULT\") { options[:asc] = true }\n   opts.on(\"--desc\", \"Sort results in descending order (newest first)\") { options[:desc] = true }\n \n     # Search and basic filters\n@@ -1437,19 +1437,20 @@ def execute_interactive_query(options)\n   # Add GROUP BY to handle potential duplicates from JOINs, but preserve individual command executions\n   full_query += \" GROUP BY c.id\"\n \n-  # Order by creation time - when limit is used, get most recent N first, then reverse for display\n-    if options[:limit]\n-      # For limited results, get the most recent N commands first\n-      full_query += \" ORDER BY c.created_at DESC\"\n+  # Order by start_time (actual execution time), then created_at, then command_id for strict chronological order\n+  # For limited results, we need to get the most recent N commands first, then apply user's sort preference\n+  if options[:limit]\n+    # Always get the most recent N commands first\n+    full_query += \" ORDER BY c.start_time DESC, c.created_at DESC, c.id DESC\"\n+  else\n+    # For unlimited results, apply user's sort preference directly (default: oldest first - chronological)\n+    if options[:desc]\n+      order = \"DESC\"\n     else\n-      # For unlimited results, respect user's sort preference (default: most recent first)\n-      if options[:asc]\n-        order = \"ASC\"\n-      else\n-        order = \"DESC\"\n-      end\n-      full_query += \" ORDER BY c.created_at #{order}\"\n+      order = \"ASC\"  # Default behavior - chronological order\n     end\n+    full_query += \" ORDER BY c.start_time #{order}, c.created_at #{order}, c.id #{order}\"\n+  end\n \n     # Add limit if specified\n     if options[:limit]\n@@ -1512,14 +1513,16 @@ def execute_interactive_query(options)\n \n     # Output results\n     if options[:format]\n-      # For formatted output, ensure chronological order (oldest to newest)\n-      if options[:limit]\n+      # For limited results, reverse to get chronological order (oldest first) by default\n+      # Only don't reverse if --desc is explicitly specified\n+      if options[:limit] && !options[:desc]\n         results.reverse!\n       end\n       output_formatted_results(results, options)\n     else\n-      # For interactive output, ensure chronological order (oldest to newest)\n-      if options[:limit]\n+      # For limited results, reverse to get chronological order (oldest first) by default\n+      # Only don't reverse if --desc is explicitly specified\n+      if options[:limit] && !options[:desc]\n         results.reverse!\n       end\n       output_interactive_results(results, options)","raw_url":"https://github.com/wstein/bin/raw/436748e1154480f89d94cd41762027f2c41f148e/histlog","sha":"f2f794b06f233e2b754a1aa73204eed6be1f1a5d","status":"modified"}]}