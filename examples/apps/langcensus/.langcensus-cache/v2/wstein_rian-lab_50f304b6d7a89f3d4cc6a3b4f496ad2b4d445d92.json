{"files":[{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/.github%2Fworkflows%2Fci.yml","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/.github%2Fworkflows%2Fci.yml?ref=50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92","deletions":5,"filename":".github/workflows/ci.yml","patch":"@@ -52,16 +52,15 @@ jobs:\n       # BEAM-pinned application code stays opt-in (it declares `@targets`, or nothing).\n       # `prelude_int.rian`'s explicit `Int64` overflow ops (`wrapping_add`/`saturating_add`/\n       # `checked_add`) carry 64-bit overflow prims and so are honestly off `:js` (ADR-0064\n-      # §2a). `prelude_list.rian`'s higher-order combinators (`map`/`filter`/`reduce`/…) take\n-      # an `Fn(...)` parameter, and Rust closure-as-value lowering is the open residual\n-      # (ADR-0061), so they are honestly off `:rs`. Both files are gated against the targets\n-      # they *can* reach, never silently dropped (the gate matches the honest reach report).\n+      # §2a) — gated against the targets they *can* reach, never silently dropped.\n+      # `prelude_list.rian` (incl. its `Fn(...)`-callback combinators `map`/`filter`/`reduce`/…)\n+      # is back to the full `ex,rs,js`: closure-as-value lowering landed (ADR-0061 — `&impl Fn`\n+      # params, `Box<dyn Fn>` returns).\n       - name: Gate shared-core portability (ex,rs,js)\n         run: |\n           for f in examples/rian/prelude_*.rian; do\n             req=ex,rs,js\n             [ \"$f\" = examples/rian/prelude_int.rian ] && req=ex,rs\n-            [ \"$f\" = examples/rian/prelude_list.rian ] && req=ex,js\n             echo \"== $f (require $req) ==\"\n             mix rian.targets \"$f\" --require \"$req\"\n           done","raw_url":"https://github.com/wstein/rian-lab/raw/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/.github%2Fworkflows%2Fci.yml","sha":"65df3a09d43822ad4de3b26be847f186c50c4eb7","status":"modified"},{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/CLAUDE.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/CLAUDE.md?ref=50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92","deletions":2,"filename":"CLAUDE.md","patch":"@@ -145,8 +145,12 @@ architectural cost.\n   builders infer concrete args from the body). All gated on generic functions, so non-generic code is\n   untouched. The integer Eq/Ord/Dict stdlib compiles to and runs on Rust (`reach_rust_honesty_test`,\n   `rustc --test`). `Option(T)`/`T | E`/user-sum-over-`T` generic returns also reach `:rs` (the payload is\n-  cloned at construction). The only Rust-generic residual is an **`Fn(...)`-typed** return mentioning a\n-  tvar (a returned closure — needs `impl Fn`/`Box<dyn Fn>`), still honestly pinned off `:rs` (ADR-0061).\n+  cloned at construction). **`Fn(...)` closures now reach `:rs`** (ADR-0061): a callback **parameter**\n+  lowers to `&impl Fn(...)` (by-reference, so a recursive HOF calls + re-passes it; closure-call args\n+  `.clone()` into owned), and a **concrete** returned closure to `Box<dyn Fn(...)>` + `Box::new(move …)`\n+  — so `map`/`filter`/`reduce` and `adder() Fn(Int53,Int53)` are `:rs`-portable (rustc-verified). The\n+  one narrow residual still pinned off `:rs` is a returned closure mentioning a **tvar** (`mk(x T)\n+  Fn(Int53,T)`) or **nested** (`Option(Fn(…,T))`) — it needs owned capture + a `T: 'static` bound.\n - **Self-hosting** (`SELFHOST.md`, `compiler/*.rian`): a compiler pipeline written in\n   Rian that compiles to `.beam`. `Rian.Fixpoint` diffs a Rian-written lexer's tokens against the\n   reference `Rian.Lexer` — that's how a ported slice becomes a regression test, not a demo.","raw_url":"https://github.com/wstein/rian-lab/raw/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/CLAUDE.md","sha":"bc36dd894007bb64069a2cffa1c12fd20c2cc2cd","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/rian-lab/blob/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/docs%2Fadr%2F0061-multi-target-protocol-lowering.md","changes":24,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0061-multi-target-protocol-lowering.md?ref=50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92","deletions":8,"filename":"docs/adr/0061-multi-target-protocol-lowering.md","patch":"@@ -172,14 +172,22 @@ Consequences of target-relativity:\n        binding** (`y := x; Some(y)`), where the rebind clones the borrowed `&T` to an owned `T`. Nested\n        generic returns (`Vec(Option(T))` → `Vec<Option<T>>`) lower correctly too (`Rian.Capability.owned`\n        strips exactly the one closing paren per layer). Verified on rustc (`reach_rust_honesty_test`).\n-  The remaining Rust-generic residual is an **`Fn(...)` function type anywhere in a signature** — a\n-  parameter *or* the return, concrete *or* generic. The emitter has no closure-as-value lowering (it\n-  spells the bare trait `Fn<…>`, rustc E0782, and an `Fn` parameter mangles to an undeclared type,\n-  E0425), so `sig_uses_fn_type?` pins any `Fn(`-bearing signature off `:rs` — a returned closure\n-  (`adder() Fn(Int53, Int53)`), a higher-order parameter (`apply_twice(f Fn(Int53, Int53), …)`,\n-  `map(f Fn(T, U), …)`), and a nested `Option(Fn(Int53, T))` alike. Plus every **parametric** shape\n-  outside the monomorphic subset above. (A Copy-primitive *protocol-impl receiver* used as a value —\n-  `Show for Bool`'s `if b` — now derefs correctly and reaches `:rs`; it was a silent over-claim.)\n+  **Closure-as-value lowering landed (2026-06-17).** An `Fn(args.., ret)` callback **parameter** lowers\n+  to argument-position `&impl Fn(<lowered args>) -> <lowered ret>` (`Rian.Capability.rust_param`) — by\n+  *reference* so a recursive higher-order fn (`map`/`filter`/`reduce`) can both call it and re-pass it\n+  without a use-after-move; a closure-call's bare-variable args are `.clone()`d (the callback takes its\n+  args by value, so a reused element survives and a borrowed `&U` coerces to owned `U`). A **concrete**\n+  returned closure lowers to `Box<dyn Fn(...)>` + `Box::new(move …)` (`Rian.Capability.owned` +\n+  `Rian.Lower`). So `map(f Fn(T, U), …)`, `apply_twice(f Fn(Int53, Int53), …)`, and `adder(n Int53)\n+  Fn(Int53, Int53)` all reach `:rs` (rustc-verified, `reach_rust_honesty_test`); the whole\n+  `prelude_list.rian` is back on the full `ex,rs,js` CI gate.\n+\n+  The remaining `Fn` residual is **narrow**: a returned closure that mentions a **type variable**\n+  (`mk(x T) Fn(Int53, T)`) or is **nested** in another type (`Option(Fn(Int53, T))`). The boxed\n+  `dyn Fn` there needs owned capture of a borrowed param plus a `T: 'static` bound, not yet emitted, so\n+  `sig_uses_fn_type?` still pins exactly those off `:rs` (and nothing else `Fn`-shaped). Plus every\n+  **parametric** shape outside the monomorphic subset above. (A Copy-primitive *protocol-impl receiver*\n+  used as a value — `Show for Bool`'s `if b` — now derefs correctly and reaches `:rs`.)\n - **`Self` and associated types.** This ADR maps `Self` as the receiver only; protocols with\n   `Self`-returning methods (`def add(a Self, b Self) Self`) and associated types are a further Rust\n   mapping question (return-position `Self`, generic associated types).","raw_url":"https://github.com/wstein/rian-lab/raw/50f304b6d7a89f3d4cc6a3b4f496ad2b4d445d92/docs%2Fadr%2F0061-multi-target-protocol-lowering.md","sha":"4b2172d619929cdb5d22028651718228a051e919","status":"modified"}]}