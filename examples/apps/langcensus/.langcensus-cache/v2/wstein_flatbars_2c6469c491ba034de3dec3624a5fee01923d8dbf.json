{"files":[{"additions":8,"blob_url":"https://github.com/wstein/flatbars/blob/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2FPLAN.md","changes":10,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2FPLAN.md?ref=2c6469c491ba034de3dec3624a5fee01923d8dbf","deletions":2,"filename":"lab/PLAN.md","patch":"@@ -59,7 +59,13 @@ Inspector snapshots the render context at a clicked run (one per loop iteration)\n All pinned by `check:provenance` + the `test:lab:browser` smoke. MinBars backs\n neither, so its provenance surfaces gate off honestly.\n \n+The boot is defended: a failed engine / example-catalog fetch paints a visible\n+error overlay (built from the inline design tokens) instead of a blank page —\n+`test:lab:browser` asserts both the happy path and the missing-asset overlay.\n+\n ## Roadmap\n \n-1. **A defended boot path** — wrap the engine/examples fetch in a visible\n-   fallback instead of a blank page on a missing asset.\n+The provenance arc (ADR-035) is complete for the FlatBars dialects. Open ideas:\n+context inspection / source maps already cover core / FullBars / MaxBars; the\n+natural next surfaces are an embeddable Lab component and a per-example metrics\n+feed into the quality dashboards.","raw_url":"https://github.com/wstein/flatbars/raw/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2FPLAN.md","sha":"f74c6cea754ff5acf4f347ee21564524c036019b","status":"modified"},{"additions":52,"blob_url":"https://github.com/wstein/flatbars/blob/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2Findex.html","changes":65,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=2c6469c491ba034de3dec3624a5fee01923d8dbf","deletions":13,"filename":"lab/index.html","patch":"@@ -1731,6 +1731,39 @@\n       const byId = (id) => document.getElementById(id);\n       const qs   = (sel) => document.querySelector(sel);\n \n+      // Boot defence: if a required asset fails to load (the engine bundle, the\n+      // example catalog), show a visible message instead of a blank page. Styled\n+      // with the inline design tokens — no external CSS, so it works even when a\n+      // fetch is what failed.\n+      function showBootError(err) {\n+        if (document.querySelector(\".boot-error\")) return; // show once\n+        const el = (tag, css, text) => {\n+          const n = document.createElement(tag);\n+          if (css) n.style.cssText = css;\n+          if (text != null) n.textContent = text;\n+          return n;\n+        };\n+        const wrap = el(\"div\", \"position:fixed;inset:0;z-index:99999;display:flex;align-items:center;justify-content:center;padding:24px;background:var(--bg);color:var(--fg);font:14px/1.6 var(--font-ui,system-ui,sans-serif)\");\n+        wrap.className = \"boot-error\";\n+        wrap.setAttribute(\"role\", \"alert\");\n+        const card = el(\"div\", \"max-width:560px\");\n+        card.append(\n+          el(\"div\", \"font-size:18px;font-weight:600;margin-bottom:8px;color:var(--danger)\", \"The FlatBars Lab failed to start\"),\n+          el(\"div\", \"color:var(--fg-muted);margin-bottom:12px\", \"A required asset could not load. Build the engine bundle (npm run gen:bundle) and serve from the repo root (npm run lab) so /lab/ resolves.\"),\n+          el(\"pre\", \"white-space:pre-wrap;color:var(--danger);background:var(--bg);border:1px solid var(--border);padding:10px;border-radius:6px;margin:0;font-size:12px\", String((err && err.message) || err)),\n+        );\n+        wrap.append(card);\n+        document.body.appendChild(wrap);\n+      }\n+      // Stop a fatal boot step with a visible message (the rest of the page depends\n+      // on it, so re-throw after painting the overlay).\n+      const bootFail = (err) => { showBootError(err); throw err; };\n+      // Fetch JSON, treating a non-OK response (a 404 HTML page) as an error.\n+      const fetchJson = (url) => fetch(url).then((res) => {\n+        if (!res.ok) throw new Error(`${url}: ${res.status} ${res.statusText}`);\n+        return res.json();\n+      });\n+\n       // Pick the engine adapter. The lab exposes the FlatBars dialects as\n       // first-class engines — RawBars (no sugar) / FullBars (Handlebars-like) /\n       // MaxBars (operators & pipes) — alongside MinBars (Mustache-like). The\n@@ -1754,7 +1787,7 @@\n         : \"fullbars\";\n       // One adapter backs every engine; createRenderer maps the engine name to its\n       // dialect (rawbars→core, fullbars→surface, maxbars, minbars).\n-      const renderer = await createRenderer(ENGINE);\n+      const renderer = await createRenderer(ENGINE).catch(bootFail);\n       // Show the engine + its version (from the bundled FlatBars/MinBars engine).\n       const ENGINE_LABEL =\n         ENGINE === \"rawbars\" ? \"RawBars\"\n@@ -1817,7 +1850,7 @@\n       // Loading one engine's templates under another would produce syntax the\n       // active engine can't parse, so the base follows the engine.\n       const EXAMPLES_BASE = `./examples/${ENGINE}/`;\n-      const examples = await (await fetch(EXAMPLES_BASE + \"examples.json\")).json();\n+      const examples = await fetchJson(EXAMPLES_BASE + \"examples.json\").catch(bootFail);\n \n       // ── State ─────────────────────────────────────────────────────────\n       let tabs        = [{ name: \"main\", source: \"\" }];\n@@ -6580,17 +6613,23 @@\n       // examples.json catalog (e.g. simple -> advanced) never breaks it.\n       // A `?vendored=<id>` deep-link takes precedence over both the shared-state\n       // hash and the default example.\n-      const vendoredParam = new URLSearchParams(location.search).get(\"vendored\");\n-      if (vendoredParam) {\n-        await loadVendoredById(vendoredParam);\n-      } else if (!(await restoreHash())) {\n-        // Landing example: \"greeting\" (FullBars/MaxBars catalogs) or \"hello\"\n-        // (MinBars), else the first entry.\n-        const def = examples.findIndex((e) => e.id === \"greeting\" || e.id === \"hello\");\n-        await loadExample(def < 0 ? 0 : def);\n-        // On a fresh open (no share link), default the output to Plain Text rather\n-        // than the example's own view. A shared link keeps its saved view.\n-        setOutputView(validView(\"source\"));\n+      // The app shell + handlers are wired by here, so a failed example load shows\n+      // the overlay but does not re-throw — the cosmetic init below still runs.\n+      try {\n+        const vendoredParam = new URLSearchParams(location.search).get(\"vendored\");\n+        if (vendoredParam) {\n+          await loadVendoredById(vendoredParam);\n+        } else if (!(await restoreHash())) {\n+          // Landing example: \"greeting\" (FullBars/MaxBars catalogs) or \"hello\"\n+          // (MinBars), else the first entry.\n+          const def = examples.findIndex((e) => e.id === \"greeting\" || e.id === \"hello\");\n+          await loadExample(def < 0 ? 0 : def);\n+          // On a fresh open (no share link), default the output to Plain Text rather\n+          // than the example's own view. A shared link keeps its saved view.\n+          setOutputView(validView(\"source\"));\n+        }\n+      } catch (err) {\n+        showBootError(err);\n       }\n       if (savedOutLang && savedOutLang !== \"Auto\") {\n         outLangChoice = savedOutLang;","raw_url":"https://github.com/wstein/flatbars/raw/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2Findex.html","sha":"3225517a1644f6026cb32732008fbb8eaba1196f","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/flatbars/blob/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2Ftest%2Fprovenance_smoke.mjs","changes":16,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Ftest%2Fprovenance_smoke.mjs?ref=2c6469c491ba034de3dec3624a5fee01923d8dbf","deletions":0,"filename":"lab/test/provenance_smoke.mjs","patch":"@@ -117,6 +117,22 @@ try {\n   console.log(\"  ✓ caret — template caret lights up the output run it produced\");\n \n   if (consoleErrors.length) fail(\"console errors during the run:\\n    \" + consoleErrors.join(\"\\n    \"));\n+\n+  // boot defence: a missing asset shows a visible overlay, not a blank page. Fail\n+  // the examples catalog fetch and assert the `.boot-error` banner appears.\n+  const badPage = await browser.newPage();\n+  await badPage.setRequestInterception(true);\n+  badPage.on(\"request\", (req) =>\n+    req.url().endsWith(\"examples.json\") ? req.respond({ status: 404, body: \"nope\" }) : req.continue(),\n+  );\n+  await badPage.goto(URL_, { waitUntil: \"domcontentloaded\", timeout: 30000 });\n+  const overlay = await badPage\n+    .waitForSelector(\".boot-error\", { timeout: 10000 })\n+    .then(() => true)\n+    .catch(() => false);\n+  assert.ok(overlay, \"a missing example catalog shows the boot-error overlay (not a blank page)\");\n+  console.log(\"  ✓ boot defence — a missing asset shows a visible error, not a blank page\");\n+  await badPage.close();\n } catch (e) {\n   fail(e.message);\n } finally {","raw_url":"https://github.com/wstein/flatbars/raw/2c6469c491ba034de3dec3624a5fee01923d8dbf/lab%2Ftest%2Fprovenance_smoke.mjs","sha":"efb504a127209ebf1c8e70049b1fa598b5890a20","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/flatbars/blob/2c6469c491ba034de3dec3624a5fee01923d8dbf/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0035-source-map-provenance.mdx","changes":4,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0035-source-map-provenance.mdx?ref=2c6469c491ba034de3dec3624a5fee01923d8dbf","deletions":2,"filename":"spec/src/content/docs/adr/adr-0035-source-map-provenance.mdx","patch":"@@ -87,5 +87,5 @@ tile the output: ordered, contiguous, each `len > 0`, covering `[0, output.len)`\n   honestly via their capability vector.\n - Templates whose output is reshaped by a transforming helper get no map for that\n   render (degrade to `[]`), which is correct, not broken.\n-- Follow-up: a defended Lab boot path (the `§9` review item) — wrap the\n-  engine/examples fetch in a visible fallback instead of a blank page.\n+- The Lab boot is defended: a failed engine / example-catalog fetch paints a\n+  visible error overlay instead of a blank page (`test:lab:browser` covers it).","raw_url":"https://github.com/wstein/flatbars/raw/2c6469c491ba034de3dec3624a5fee01923d8dbf/spec%2Fsrc%2Fcontent%2Fdocs%2Fadr%2Fadr-0035-source-map-provenance.mdx","sha":"20a203774684ac70136ca85de2a50a983ade6f38","status":"modified"}]}