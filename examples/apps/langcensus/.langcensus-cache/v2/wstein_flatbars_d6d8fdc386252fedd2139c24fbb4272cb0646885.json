{"files":[{"additions":53,"blob_url":"https://github.com/wstein/flatbars/blob/d6d8fdc386252fedd2139c24fbb4272cb0646885/lab%2Findex.html","changes":107,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=d6d8fdc386252fedd2139c24fbb4272cb0646885","deletions":54,"filename":"lab/index.html","patch":"@@ -1871,7 +1871,6 @@\n         },\n         state: createAppState(ENGINE),\n       };\n-      let tabs        = [{ name: \"main\", source: \"\" }];\n       // What the bottom (data) pane currently DISPLAYS: \"data\" | \"config\" |\n       // \"catalog\". The pane is shared, so focusing it must restore ctx.state.activeView to\n       // what's shown — not assume \"data\" — or config/catalog edits route into\n@@ -1944,11 +1943,11 @@\n         typeof id === \"string\" && id !== \"data\" && id !== \"transform\" &&\n         !isInspectTab(id) && !!overlayByName(id);\n \n-      const activeTab = () => tabs[ctx.state.activeTabIdx];\n+      const activeTab = () => ctx.state.tabs[ctx.state.activeTabIdx];\n \n       function partialsMap() {\n         const map = {};\n-        for (let i = 1; i < tabs.length; i++) map[tabs[i].name] = tabs[i].source;\n+        for (let i = 1; i < ctx.state.tabs.length; i++) map[ctx.state.tabs[i].name] = ctx.state.tabs[i].source;\n         return map;\n       }\n \n@@ -2134,7 +2133,7 @@\n                   ctx.state.helpersDirty = true;\n                 } else {\n                   activeTab().source = text;\n-                  tabs[ctx.state.activeTabIdx].dirty = true;\n+                  ctx.state.tabs[ctx.state.activeTabIdx].dirty = true;\n                 }\n                 renderTabs();\n                 scheduleRun();\n@@ -2290,7 +2289,7 @@\n \n       // ── Virtual file explorer ───────────────────────────────────────────\n       // A type-grouped navigator over every editable file. It does not own any\n-      // state — it reflects tabs[] / ctx.state.dataOverlays[] / transform / config and\n+      // state — it reflects ctx.state.tabs[] / ctx.state.dataOverlays[] / transform / config and\n       // drives the existing select* functions + render-config popover, so the\n       // tab strips stay authoritative (rename / add / inspectors live there).\n       // Re-rendered from renderTabs() + renderDataTabs() (every structural and\n@@ -2361,10 +2360,10 @@\n         const groups = [\n           { key: \"template\", label: \"TEMPLATE\", files: [\n             { icon: \"stem\", name: \"main.\" + TEMPLATE_EXT, active: tmplActive && ctx.state.activeTabIdx === 0,\n-              dirty: !!(tabs[0] && tabs[0].dirty), title: \"entry template\", on: () => selectTemplateTab(0) },\n+              dirty: !!(ctx.state.tabs[0] && ctx.state.tabs[0].dirty), title: \"entry template\", on: () => selectTemplateTab(0) },\n           ]},\n           { key: \"partials\", label: \"PARTIALS\", add: addPartial, addTitle: \"New partial\",\n-            files: tabs.slice(1).map((t, idx) => {\n+            files: ctx.state.tabs.slice(1).map((t, idx) => {\n               const i = idx + 1;\n               return { icon: \"stem\", name: (t.name || \"(unnamed)\") + \".\" + TEMPLATE_EXT,\n                 active: tmplActive && ctx.state.activeTabIdx === i, dirty: !!t.dirty,\n@@ -2373,7 +2372,7 @@\n                 renameValue: t.name, onDelete: () => removeTab(i),\n                 onRename: (v) => {\n                   v = v.trim();\n-                  if (!v || tabs.some((o, j) => j !== i && o.name === v)) return false;\n+                  if (!v || ctx.state.tabs.some((o, j) => j !== i && o.name === v)) return false;\n                   const prev = t.name; t.name = v;\n                   const k = ctx.state.openEditors.indexOf(prev); if (k >= 0) ctx.state.openEditors[k] = v;\n                   markCustom(); renderTabs(); scheduleRun(); return true;\n@@ -2616,9 +2615,9 @@\n             continue;\n           }\n           if (key === \"config\" || key === \"catalog\" || key === \"data\" || overlayByName(key)) continue; // bottom strip\n-          const i = key === \"main\" ? 0 : tabs.findIndex((t) => t.name === key);\n+          const i = key === \"main\" ? 0 : ctx.state.tabs.findIndex((t) => t.name === key);\n           if (i < 0) continue; // a partial that no longer exists\n-          const tab = tabs[i];\n+          const tab = ctx.state.tabs[i];\n           const isActive = ctx.state.activeView === \"tmpl\" && templateMode === \"tmpl\" && i === ctx.state.activeTabIdx;\n           const t = document.createElement(\"div\");\n           t.className = \"tab\" + (isActive ? \" active\" : \"\") + (i === 0 ? \" is-main\" : \"\");\n@@ -2679,8 +2678,8 @@\n       // Diagnostics dock (see `dockTabs`); `renderDataInspect` re-renders the\n       // dock so the active inspector follows the file / latest render.\n \n-      // ── Template open-editor helpers (VS Code-style tabs) ───────────────\n-      const tmplKeyForIndex = (i) => (i === 0 ? \"main\" : tabs[i].name);\n+      // ── Template open-editor helpers (VS Code-style ctx.state.tabs) ───────────────\n+      const tmplKeyForIndex = (i) => (i === 0 ? \"main\" : ctx.state.tabs[i].name);\n       function ensureOpen(key) { if (!ctx.state.openEditors.includes(key)) ctx.state.openEditors.push(key); }\n \n       // Auto-open every example file that carries content — \"open all the files,\n@@ -2694,7 +2693,7 @@\n       function autoOpenEditors() {\n         const has = (s) => typeof s === \"string\" && s.trim() !== \"\";\n         const open = [\"main\", \"data\"];\n-        tabs.forEach((t, i) => { if (i > 0 && has(t.source)) open.push(t.name); });\n+        ctx.state.tabs.forEach((t, i) => { if (i > 0 && has(t.source)) open.push(t.name); });\n         if (has(ctx.state.transformSrc)) open.push(\"transform\");\n         if (has(ctx.state.helpersSrc)) open.push(\"helpers\");\n         for (const o of ctx.state.dataOverlays) if (has(o.source)) open.push(o.name);\n@@ -2712,7 +2711,7 @@\n         if (key === \"main\")      return ctx.state.activeView === \"tmpl\" && templateMode === \"tmpl\" && ctx.state.activeTabIdx === 0;\n         // partial or overlay name\n         if (overlayByName(key))  return ctx.state.activeView === \"data\" && ctx.state.dataTab === \"edit\" && ctx.state.dataFile === key;\n-        return ctx.state.activeView === \"tmpl\" && templateMode === \"tmpl\" && tabs[ctx.state.activeTabIdx] && tabs[ctx.state.activeTabIdx].name === key;\n+        return ctx.state.activeView === \"tmpl\" && templateMode === \"tmpl\" && ctx.state.tabs[ctx.state.activeTabIdx] && ctx.state.tabs[ctx.state.activeTabIdx].name === key;\n       }\n \n       // Activate an editor by key (used when closing the active one).\n@@ -2724,7 +2723,7 @@\n         if (key === \"main\") return selectTemplateTab(0);\n         if (key === \"data\") return selectDataTab(\"data\");\n         if (overlayByName(key)) return selectDataTab(key);\n-        const i = tabs.findIndex((t) => t.name === key);\n+        const i = ctx.state.tabs.findIndex((t) => t.name === key);\n         selectTemplateTab(i > 0 ? i : 0);\n       }\n \n@@ -2823,7 +2822,7 @@\n       // name). \"transform\" routes to the template pane (kept for older callers).\n       function selectDataTab(name = \"data\") {\n         if (name === \"transform\") { selectTransformTab(); return; }\n-        if (name === \"context\" || name === \"ast\") { // inspectors are dock tabs now\n+        if (name === \"context\" || name === \"ast\") { // inspectors are dock ctx.state.tabs now\n           dockActiveTab = name; if (byId(\"dock\").hidden) setDockVisible(true); else renderDock();\n           return;\n         }\n@@ -2886,9 +2885,9 @@\n       function renderDataInspect() { renderDock(); }\n       function addPartial() {\n         let n = 1, name;\n-        do { name = \"partial\" + n++; } while (tabs.some((t) => t.name === name));\n-        tabs.push({ name, source: \"\" });\n-        ctx.state.activeView = \"tmpl\"; ctx.state.activeTabIdx = tabs.length - 1;\n+        do { name = \"partial\" + n++; } while (ctx.state.tabs.some((t) => t.name === name));\n+        ctx.state.tabs.push({ name, source: \"\" });\n+        ctx.state.activeView = \"tmpl\"; ctx.state.activeTabIdx = ctx.state.tabs.length - 1;\n         ensureOpen(name);\n         markCustom();\n         // Set before `showActiveTab` so the renderExplorer it triggers picks\n@@ -2899,25 +2898,25 @@\n       // Open an existing partial tab by name, or create a new one if absent.\n       // Used by the double-click handler on {{> name}} tags in the template editor.\n       function openOrCreatePartial(name) {\n-        const idx = tabs.findIndex((t, i) => i > 0 && t.name === name);\n+        const idx = ctx.state.tabs.findIndex((t, i) => i > 0 && t.name === name);\n         if (idx >= 0) {\n           selectTemplateTab(idx);\n         } else {\n-          tabs.push({ name, source: \"\", dirty: false });\n-          ctx.state.activeView = \"tmpl\"; ctx.state.activeTabIdx = tabs.length - 1;\n+          ctx.state.tabs.push({ name, source: \"\", dirty: false });\n+          ctx.state.activeView = \"tmpl\"; ctx.state.activeTabIdx = ctx.state.tabs.length - 1;\n           ensureOpen(name);\n           markCustom();\n           showActiveTab(); run();\n         }\n       }\n       function removeTab(i) {\n         if (i === 0) return;\n-        if (!confirm(`Delete the \"${tabs[i].name}\" partial? This can't be undone.`)) return;\n-        const name = tabs[i].name;\n+        if (!confirm(`Delete the \"${ctx.state.tabs[i].name}\" partial? This can't be undone.`)) return;\n+        const name = ctx.state.tabs[i].name;\n         const wasActive = ctx.state.activeView === \"tmpl\" && templateMode === \"tmpl\" && ctx.state.activeTabIdx === i;\n         const k = ctx.state.openEditors.indexOf(name); if (k >= 0) ctx.state.openEditors.splice(k, 1);\n         if (ctx.state.activeTabIdx > i) ctx.state.activeTabIdx -= 1;\n-        tabs.splice(i, 1);\n+        ctx.state.tabs.splice(i, 1);\n         markCustom();\n         // Closing the active file falls back to the pinned entry template.\n         if (wasActive) { ctx.state.activeTabIdx = 0; templateMode = \"tmpl\"; ctx.state.activeView = \"tmpl\"; }\n@@ -2961,7 +2960,7 @@\n       }\n \n       // Activate the transform tab in the template pane. Symmetric with\n-      // selectTemplateTab(i) for normal template tabs.\n+      // selectTemplateTab(i) for normal template ctx.state.tabs.\n       function selectTransformTab() {\n         templateMode = \"transform\";\n         ctx.state.activeView = \"tmpl\";\n@@ -3088,7 +3087,7 @@\n           `[Open in the Stem playground](${location.href})`,\n           \"\",\n         ];\n-        for (const t of tabs) {\n+        for (const t of ctx.state.tabs) {\n           out.push(`## ${t.name === \"main\" ? \"Template — main\" : \"Partial — \" + t.name}`, \"\");\n           out.push(mdFence(t.source, TEMPLATE_FENCE), \"\");\n         }\n@@ -3103,7 +3102,7 @@\n       // The workspace as separate files for the zip: each template, the data, the\n       // transform (if any), the output, and the Markdown doc as a README.\n       async function buildFiles() {\n-        const files = tabs.map((t) => ({\n+        const files = ctx.state.tabs.map((t) => ({\n           name: t.name === \"main\" ? `main.${TEMPLATE_EXT}` : `partials/${t.name}.${TEMPLATE_EXT}`,\n           content: t.source,\n         }));\n@@ -3269,11 +3268,11 @@\n \n       // The file behind a segment (\"main\" is tab 0; partials are the rest).\n       function tabIndexByFile(file) {\n-        return file === \"main\" ? 0 : tabs.findIndex((t, i) => i > 0 && t.name === file);\n+        return file === \"main\" ? 0 : ctx.state.tabs.findIndex((t, i) => i > 0 && t.name === file);\n       }\n       function fileSource(file) {\n         const i = tabIndexByFile(file);\n-        return i >= 0 ? tabs[i].source : \"\";\n+        return i >= 0 ? ctx.state.tabs[i].source : \"\";\n       }\n \n       // True between a source edit and the debounced render that follows it: in\n@@ -3286,7 +3285,7 @@\n       // instances). renderPending is set via the injected setter; caretLinkKey via\n       // resetCaretLink (the caret subsystem owns it).\n       const { run, scheduleRun } = createRun(ctx, {\n-        getYamlSrc: () => ctx.state.yamlSrc, getTabs: () => tabs, getActiveTabIdx: () => ctx.state.activeTabIdx,\n+        getYamlSrc: () => ctx.state.yamlSrc, getTabs: () => ctx.state.tabs, getActiveTabIdx: () => ctx.state.activeTabIdx,\n         getStandalone: () => standalone, getTransformSrc: () => ctx.state.transformSrc,\n         getHelpersSrc: () => ctx.state.helpersSrc, getCatalogSource: () => ctx.state.catalogSource,\n         getAllowList: () => ctx.state.allowList, getEscapeMode: () => ctx.state.escapeMode,\n@@ -3348,13 +3347,13 @@\n       // `bytecode-wire`) — falls back to the always-present iframe Preview.\n       const validView = (v) => ovValidView(v, engineFeatures);\n \n-      // The output view tabs + the visible/valid decisions live in\n+      // The output view ctx.state.tabs + the visible/valid decisions live in\n       // lab/output-view.mjs (extracted for testability). These thin wrappers bind\n       // the engine's advertised features so existing call sites stay arg-free.\n       const visibleViews = () => ovVisibleViews(engineFeatures);\n \n       function templateSourceFor(file) {\n-        if (file === \"main\") return tabs[0]?.source || \"\";\n+        if (file === \"main\") return ctx.state.tabs[0]?.source || \"\";\n         return partialsMap()[file] || \"\";\n       }\n \n@@ -3523,7 +3522,7 @@\n         if (linkedTabEl) { linkedTabEl.classList.remove(\"tab-linked\"); linkedTabEl = null; }\n \n         if (active) {\n-          const [from, to] = spanRange(tabs[idx].source, start, end);\n+          const [from, to] = spanRange(ctx.state.tabs[idx].source, start, end);\n           templateView.dispatch({ effects: setLinkHighlight.of({ from, to }) });\n         } else {\n           templateView.dispatch({ effects: setLinkHighlight.of(null) });\n@@ -3584,7 +3583,7 @@\n         selectTemplateTab(idx);\n         templateView.focus();\n         if (start !== null) {\n-          const [from, to] = spanRange(tabs[idx].source, start, end);\n+          const [from, to] = spanRange(ctx.state.tabs[idx].source, start, end);\n           templateView.dispatch({ selection: { anchor: from, head: to }, scrollIntoView: true });\n         }\n         hideSegTip();\n@@ -3830,8 +3829,8 @@\n           ...ex.partials.map((name) =>\n             files.readText(`${dir}${name}.${ex.main.split(\".\").pop()}`)),\n         ]);\n-        tabs = [{ name: \"main\", source: mainSrc }];\n-        ex.partials.forEach((name, i) => tabs.push({ name, source: partialSrcs[i] }));\n+        ctx.state.tabs = [{ name: \"main\", source: mainSrc }];\n+        ex.partials.forEach((name, i) => ctx.state.tabs.push({ name, source: partialSrcs[i] }));\n         ctx.state.activeTabIdx = 0; ctx.state.activeView = \"tmpl\";\n         // Examples ship their data as YAML; an optional transform is the Controller.\n         ctx.state.yamlSrc = dataText;\n@@ -3869,7 +3868,7 @@\n         { const s = byId(\"vendored-source\"); if (s) s.hidden = true; } // catalog example: no vendored source\n         ctx.state.loadedExampleIdx = idx;\n         ctx.state.exampleModified = false;\n-        tabs.forEach(t => { t.dirty = false; });\n+        ctx.state.tabs.forEach(t => { t.dirty = false; });\n         ctx.state.yamlDirty = false; ctx.state.transformDirty = false;\n         outLangChoice = \"Auto\";\n         persistOutLang();\n@@ -3911,8 +3910,8 @@\n       // Mirrors loadExample's state setup, but from the fixture (no per-file fetch).\n       function loadVendored(fx) {\n         const ws = vendoredWorkspace(fx);\n-        tabs = [{ name: \"main\", source: ws.template, dirty: false }];\n-        ws.partials.forEach((p) => tabs.push({ name: p.name, source: p.source, dirty: false }));\n+        ctx.state.tabs = [{ name: \"main\", source: ws.template, dirty: false }];\n+        ws.partials.forEach((p) => ctx.state.tabs.push({ name: p.name, source: p.source, dirty: false }));\n         ctx.state.activeTabIdx = 0; ctx.state.activeView = \"tmpl\";\n         ctx.state.yamlSrc = ws.dataText;\n         ctx.state.transformSrc = \"\";\n@@ -3968,7 +3967,7 @@\n       // ── URL state ─────────────────────────────────────────────────────\n       function snapshotState() {\n         return {\n-          tabs: tabs.map((t) => ({ n: t.name, s: t.source })),\n+          tabs: ctx.state.tabs.map((t) => ({ n: t.name, s: t.source })),\n           d: ctx.state.yamlSrc,\n           t: ctx.state.transformSrc,\n           // Custom-helper source (ADR-018), a separate field from the transform.\n@@ -4020,10 +4019,10 @@\n             return true;\n           }\n           // Otherwise it is a genuine custom edit (x === -1): restore verbatim.\n-          tabs = Array.isArray(s.tabs) && s.tabs.length\n-            ? s.tabs.map((t, i) => ({ name: i === 0 ? \"main\" : (t.n ?? \"partial\"), source: t.s ?? \"\", dirty: false }))\n+          ctx.state.tabs = Array.isArray(s.ctx.state.tabs) && s.ctx.state.tabs.length\n+            ? s.ctx.state.tabs.map((t, i) => ({ name: i === 0 ? \"main\" : (t.n ?? \"partial\"), source: t.s ?? \"\", dirty: false }))\n             : [{ name: \"main\", source: \"\", dirty: false }];\n-          ctx.state.activeTabIdx = Math.max(0, Math.min(tabs.length - 1, s.ai ?? 0));\n+          ctx.state.activeTabIdx = Math.max(0, Math.min(ctx.state.tabs.length - 1, s.ai ?? 0));\n           ctx.state.activeView = s.av === \"data\" ? \"data\" : \"tmpl\";\n           ctx.state.yamlSrc = s.d ?? \"\";\n           ctx.state.transformSrc = s.t ?? \"\";\n@@ -4310,11 +4309,11 @@\n       let searchMount = null;\n       let searchRefresh = null;\n \n-      // Every searchable source: the template tabs (main + partials), the YAML\n+      // Every searchable source: the template ctx.state.tabs (main + partials), the YAML\n       // data, and the JSONata transform (when present). The rendered output is\n       // generated, not a source, so it's excluded.\n       function searchSources() {\n-        const out = tabs.map((t) => ({ file: t.name, kind: \"tmpl\", source: t.source }));\n+        const out = ctx.state.tabs.map((t) => ({ file: t.name, kind: \"tmpl\", source: t.source }));\n         out.push({ file: \"data.yaml\", kind: \"data\", source: ctx.state.yamlSrc });\n         if (ctx.state.transformSrc.trim()) out.push({ file: \"transform\", kind: \"transform\", source: ctx.state.transformSrc });\n         return out;\n@@ -4375,7 +4374,7 @@\n         if (kind === \"transform\") return ctx.state.transformSrc;\n         if (kind === \"helpers\") return ctx.state.helpersSrc;\n         const i = tabIndexByFile(file);\n-        return i >= 0 ? tabs[i].source : \"\";\n+        return i >= 0 ? ctx.state.tabs[i].source : \"\";\n       }\n \n       // Write a source back and keep the visible editor in sync.\n@@ -4393,7 +4392,7 @@\n         } else {\n           const i = tabIndexByFile(file);\n           if (i < 0) return;\n-          tabs[i].source = next;\n+          ctx.state.tabs[i].source = next;\n           if (i === ctx.state.activeTabIdx && ctx.state.activeView === \"tmpl\") setEditorText(templateView, next);\n         }\n       }\n@@ -4658,7 +4657,7 @@\n       // Parse main + every partial to its pre-expansion AST, then derive the DAG.\n       function currentDependencyGraph() {\n         const asts = {};\n-        const parsed = renderer.parseAst(tabs[0].source);\n+        const parsed = renderer.parseAst(ctx.state.tabs[0].source);\n         asts.main = parsed.ast ? parsed.ast.nodes : [];\n         for (const [name, src] of Object.entries(partialsMap())) {\n           const p = renderer.parseAst(src);\n@@ -4796,7 +4795,7 @@\n \n       function dockTabById(id) { return dockTabs.find((t) => t.id === id); }\n \n-      // Dock tabs the loaded engine actually exposes (ADR-0020 gate applied).\n+      // Dock ctx.state.tabs the loaded engine actually exposes (ADR-0020 gate applied).\n       // `problems`/`perf`/`coverage` declare no `requires`, so they are always\n       // present; the analysis panels appear only when their backing capability\n       // is advertised. Under the Stem adapter every tab is visible.\n@@ -4834,7 +4833,7 @@\n       };\n \n       // MinBars is a logic-less engine without the lowered-AST / source-map seams the\n-      // FlatBars panels read. Rather than silently showing fewer tabs, surface a\n+      // FlatBars panels read. Rather than silently showing fewer ctx.state.tabs, surface a\n       // \"Why fewer panels?\" popover listing each absent panel and its reason.\n       function paintWhyFewerPanels(strip) {\n         if (ENGINE !== \"minbars\") return;\n@@ -4911,9 +4910,9 @@\n       // both for capturing a confirmed bug fix (vector matches future fix) and\n       // for pinning current behaviour when a refactor is imminent.\n       function saveAsVector(row) {\n-        const tmpl = tabs[0].source;\n+        const tmpl = ctx.state.tabs[0].source;\n         const partials = {};\n-        for (const t of tabs.slice(1)) partials[t.name] = t.source;\n+        for (const t of ctx.state.tabs.slice(1)) partials[t.name] = t.source;\n         const name = row ? `data access: ${row.status} ${row.path}` : \"playground capture\";\n         const vector = {\n           name,\n@@ -5244,7 +5243,7 @@\n           g.append(text);\n           g.style.cursor = \"pointer\";\n           g.addEventListener(\"click\", () => {\n-            const idx = tabs.findIndex((t) => t.name === n.id);\n+            const idx = ctx.state.tabs.findIndex((t) => t.name === n.id);\n             if (idx >= 0) selectTemplateTab(idx);\n           });\n           svg.append(g);","raw_url":"https://github.com/wstein/flatbars/raw/d6d8fdc386252fedd2139c24fbb4272cb0646885/lab%2Findex.html","sha":"7c51fd9d1e4541280b30985caecbdebb1f5b46db","status":"modified"}]}