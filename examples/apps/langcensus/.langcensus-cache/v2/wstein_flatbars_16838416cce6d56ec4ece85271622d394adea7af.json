{"files":[{"additions":13,"blob_url":"https://github.com/wstein/flatbars/blob/16838416cce6d56ec4ece85271622d394adea7af/lab%2Findex.html","changes":26,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=16838416cce6d56ec4ece85271622d394adea7af","deletions":13,"filename":"lab/index.html","patch":"@@ -3013,9 +3013,9 @@\n           lastSegments: [],\n           lastData: null,    // the post-transform data the template rendered (Render Data view)\n           lastProgram: null, // the compiled wire program, for the Bytecode view\n+          lastSegViews: [],  // [{from,to,file,kind,start,end}] — segment char ranges in the output editor\n         },\n       };\n-      let lastSegViews = []; // [{from,to,file,kind,start,end}] — segment char ranges in the output editor\n       let lastUsedTransformers = []; // ADR-0013 report: transformer names called by the compiled program\n       let dockProblems = []; // diagnostics for the Problems tab, refreshed each run()\n       let hadProblems = false; // tracks the 0->>0 transition so the dock auto-opens once\n@@ -3203,21 +3203,21 @@\n       // language, with the source-map provenance as decorations.\n       function paintTextView() {\n         if (outputView === \"bytecode\") {\n-          lastSegViews = [];\n+          ctx.caches.lastSegViews = [];\n           setOutputDoc(ctx.caches.lastProgram ? disassemble(ctx.caches.lastProgram) : \"\", Decoration.none);\n         } else if (outputView === \"data\") {\n           let text = \"\";\n           if (ctx.caches.lastData != null) {\n             try { text = dumpYaml(ctx.caches.lastData, { indent: 2, lineWidth: -1, noRefs: true }); }\n             catch { text = String(ctx.caches.lastData); }\n           }\n-          lastSegViews = [];\n+          ctx.caches.lastSegViews = [];\n           setOutputDoc(text, Decoration.none);\n         } else if (outputView === \"compiled\") {\n           // The template compiled to a JS module (the `compile-js` engine\n           // extension). Errors come back as a `// …` comment so the pane stays\n           // valid-looking JS.\n-          lastSegViews = [];\n+          ctx.caches.lastSegViews = [];\n           let text = \"// this engine has no compiler\";\n           if (typeof renderer.compileToJs === \"function\") {\n             const r = renderer.compileToJs(templateSourceFor(\"main\"), partialsMap(), { compat: minbarsCompat });\n@@ -3229,7 +3229,7 @@\n           // `migrate` engine extension). Residuals the migrator could not safely\n           // rewrite are surfaced as leading `{{! … }}` MaxBars comments, so the\n           // pane stays valid MaxBars and a clean migration shows pure source.\n-          lastSegViews = [];\n+          ctx.caches.lastSegViews = [];\n           let text = \"{{! this engine has no migrator }}\";\n           if (typeof renderer.migrate === \"function\") {\n             const r = renderer.migrate(templateSourceFor(\"main\"));\n@@ -3245,7 +3245,7 @@\n           setOutputDoc(text, Decoration.none);\n         } else {\n           const { segViews, deco } = buildProvenance(ctx.caches.lastOutput, ctx.caches.lastSegments);\n-          lastSegViews = segViews;\n+          ctx.caches.lastSegViews = segViews;\n           setOutputDoc(ctx.caches.lastOutput, deco);\n         }\n         updateOutLang();\n@@ -3341,7 +3341,7 @@\n       // output offsets are UTF-16 code units (JS string indices), so `segmentRanges`\n       // uses them directly — NO byte↔char conversion (which drifts every mark after a\n       // multibyte char, the misalignment bug). Each mark carries its index in\n-      // lastSegViews via data-i so hover/click can recover the segment.\n+      // ctx.caches.lastSegViews via data-i so hover/click can recover the segment.\n       function buildProvenance(output, segments) {\n         if (!segments.length) return { segViews: [], deco: Decoration.none };\n         const segViews = segmentRanges(output, segments);\n@@ -3944,7 +3944,7 @@\n         if (key === caretLinkKey) return;\n         caretLinkKey = key;\n         const ranges = [];\n-        for (const sv of lastSegViews) {\n+        for (const sv of ctx.caches.lastSegViews) {\n           const hit = partial\n             ? sv.file === partial\n             : (sv.file === file && sv.start !== null && sv.start <= pos && pos < sv.end);\n@@ -3971,22 +3971,22 @@\n       // Keyboard parity: Enter on the output editor jumps from the run under the\n       // cursor to its source (arrows/Home/End navigate via the default keymap).\n       function jumpToSourceAtCursor(view) {\n-        if (outputView !== \"source\" || !lastSegViews.length)\n+        if (outputView !== \"source\" || !ctx.caches.lastSegViews.length)\n           return false;\n         const pos = view.state.selection.main.head;\n-        const sv = lastSegViews.find((s) => pos >= s.from && pos < s.to)\n-          || lastSegViews.find((s) => pos === s.to);\n+        const sv = ctx.caches.lastSegViews.find((s) => pos >= s.from && pos < s.to)\n+          || ctx.caches.lastSegViews.find((s) => pos === s.to);\n         if (!sv) return false;\n         openSourceLocation(sv.file, sv.start, sv.end);\n         return true;\n       }\n \n       // Hover/click on the output editor's runs. The provenance marks render as\n-      // `.seg` spans carrying data-i (index into lastSegViews); event delegation\n+      // `.seg` spans carrying data-i (index into ctx.caches.lastSegViews); event delegation\n       // on the stable contentDOM survives CM's viewport re-rendering.\n       (() => {\n         const content = outputEditor.contentDOM;\n-        const svOf = (el) => { const s = el?.closest?.(\".seg\"); return s ? lastSegViews[Number(s.dataset.i)] : null; };\n+        const svOf = (el) => { const s = el?.closest?.(\".seg\"); return s ? ctx.caches.lastSegViews[Number(s.dataset.i)] : null; };\n         // Debounce hover so sweeping the pointer across many runs only shows the\n         // tooltip + link-highlight for the run the pointer settles on.\n         let hoverTimer = null;","raw_url":"https://github.com/wstein/flatbars/raw/16838416cce6d56ec4ece85271622d394adea7af/lab%2Findex.html","sha":"aedc57e877235d36977a8a40325475faa81c0590","status":"modified"}]}