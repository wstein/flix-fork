{"files":[{"additions":31,"blob_url":"https://github.com/wstein/bin/blob/94977d75a38e91ff373500a44ff2a961fd3e0ab9/histlog","changes":88,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=94977d75a38e91ff373500a44ff2a961fd3e0ab9","deletions":57,"filename":"histlog","patch":"@@ -1194,11 +1194,12 @@ def query_interactive(args)\n     opts.on(\"--no-assisted\", \"Exclude AI-assisted commands\") { options[:no_assisted] = true }\n \n     # Output format\n-    opts.on(\"--json\", \"Output as JSON with full metadata\") { options[:format] = 'json' }\n-    opts.on(\"--yaml\", \"Output as YAML with full metadata\") { options[:format] = 'yaml' }\n-    opts.on(\"--format FORMAT\", \"Output format (json, yaml, fish, zsh, nu, bash, powershell, plain)\") { |f| options[:format] = f.downcase }\n-    opts.on(\"--time\", \"Show human-readable start_time with each command in interactive output\") { options[:show_time] = true }\n-    opts.on(\"--limit N\", Integer, \"Limit number of results (default: no limit)\") { |n| options[:limit] = n }\n+  opts.on(\"--json\", \"Output as JSON with full metadata\") { options[:format] = 'json' }\n+  opts.on(\"--yaml\", \"Output as YAML with full metadata\") { options[:format] = 'yaml' }\n+  opts.on(\"--plain\", \"Output only commands, one per line\") { options[:format] = 'plain' }\n+  opts.on(\"--format FORMAT\", \"Output format (json, yaml, fish, zsh, nu, bash, powershell, plain)\") { |f| options[:format] = f.downcase }\n+  opts.on(\"--time\", \"Show human-readable start_time with each command in interactive output\") { options[:show_time] = true }\n+  opts.on(\"--limit N\", Integer, \"Limit number of results (default: no limit)\") { |n| options[:limit] = n }\n \n     opts.on(\"-h\", \"--help\", \"Show this help\") do\n       puts opts\n@@ -1477,7 +1478,11 @@ def execute_interactive_query(options)\n \n     # Execute query\n     begin\n-      results = db.execute(full_query, params)\n+      all_results = db.execute(full_query, params)\n+      results = all_results.reject do |row|\n+        # Exclude if duration is null (still running)\n+        row['duration'].nil?\n+      end\n     rescue SQLite3::SQLException => e\n       # Fallback: if REGEXP is not supported, filter in Ruby\n       if options[:regex] && options[:search_term] && e.message =~ /no such function: REGEXP/\n@@ -1710,15 +1715,9 @@ def output_plain_results(results, options)\n   begin\n     results.each do |row|\n       command = row['cmd_text']\n-      if row['start_time']\n-        timestamp = Time.at(row['start_time'].to_f).strftime('%Y-%m-%d %H:%M:%S')\n-        puts \"[#{timestamp}] #{command}\"\n-      else\n-        puts command\n-      end\n+      puts command\n     end\n   rescue Errno::EPIPE\n-    # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n     exit 0\n   end\n end\n@@ -1762,36 +1761,20 @@ def output_interactive_results(results, options)\n     return\n   end\n \n-  # Simple output: just print commands (optionally prefixed by ISO8601 start_time)\n+  # Dracula color codes\n+  # Timestamp: #6272a4, Command: default, Result: ✓ #50fa7b, ✗ #ff5555, Duration: #bd93f9\n   begin\n     results.each do |row|\n-    command = row['cmd_text'] || row['command']\n-      lines = command.split(\"\\n\")\n-      time_prefix = \"\"\n-      if options && options[:show_time] && row['start_time']\n-        begin\n-          t = Time.at(row['start_time'].to_f)\n-          # Simplified format: YYYY-MM-DD HH:MM\n-          time_prefix = t.strftime(\"%Y-%m-%d %H:%M:%S\") + \" \"\n-        rescue\n-          time_prefix = \"\"\n-        end\n-      end\n-\n-      if results.length == 1\n-        # Single result: show full command, even if multiline\n-        puts time_prefix + command\n-      elsif lines.length > 1\n-        # Multiple results with multiline command: show first line + \"...\"\n-        puts time_prefix + \"#{lines.first}...\"\n-      else\n-        # Multiple results with single line command: show as-is\n-        puts time_prefix + command\n-      end\n+      timestamp = row['start_time'] ? Time.at(row['start_time']).strftime('%Y-%m-%d %H:%M:%S') : 'Unknown'\n+      exit_code = row['exit_code'] || 0\n+      duration = row['duration'] ? sprintf('%8.3fs', row['duration']) : '-,---s'\n+      status = exit_code == 0 ? \"\\e[38;5;84m✓\\e[0m   \" : sprintf(\"\\e[38;5;203m✗%-3d\\e[0m\", exit_code)\n+\tts_col = \"\\e[38;5;14m\"\n+\tdur_col = \"\\e[33m\"\n+      command = row['cmd_text'].gsub(\"\\n\", \"\\n                                   \")\n+      puts \"#{ts_col}#{timestamp} #{dur_col}#{duration} #{status}\\e[0m #{command}\"\n     end\n   rescue Errno::EPIPE\n-    # Broken pipe - output was closed (e.g., piped to head/less and user quit)\n-    # This is normal, just exit quietly\n     exit 0\n   end\n end\n@@ -3432,27 +3415,18 @@ def mcp_query_commands(args)\n       return \"No commands found matching the criteria.\"\n     end\n \n-    output = \"Found #{results.length} commands:\\n\\n\"\n+    # Dracula color codes\n+    # Timestamp: #6272a4, Command: default, Result: ✓ #50fa7b, ✗ #ff5555, Duration: #bd93f9\n+    output = \"\"\n     results.each do |row|\n       timestamp = row['start_time'] ? Time.at(row['start_time']).strftime('%Y-%m-%d %H:%M:%S') : 'Unknown'\n-      exit_code = row['exit_code'] || 'Unknown'\n-      duration = row['duration'] ? \"#{row['duration']}s\" : 'Unknown'\n-      status = row['exit_code'] == 0 ? '✓' : '✗'\n-\n-      # Build status indicators\n-      indicators = []\n-      indicators << \"private\" if row['is_private'] == 1\n-      indicators << \"assisted\" if row['is_assisted'] == 1\n-      indicators << \"session:#{row['session_id']}\" if row['session_id']\n-      indicators << \"shell:#{File.basename(row['shell_path'])}\" if row['shell_path']\n-      indicators << \"dir:#{row['old_path']}\" if row['old_path'] && row['old_path'] != row['new_path']\n-\n-      output << \"#{status} [#{timestamp}] #{row['cmd_text']}\"\n-      output << \" (exit: #{exit_code}, duration: #{duration})\" if row['exit_code'] || row['duration']\n-      output << \" [#{indicators.join(', ')}]\" unless indicators.empty?\n-      output << \"\\n\"\n+      exit_code = row['exit_code'] || 0\n+      duration = row['duration'] ? sprintf('%.3fs', row['duration']) : 'Unknown'\n+      status = exit_code == 0 ? \"\\e[38;5;84m✓\\e[0m\" : \"\\e[38;5;203m✗#{exit_code}\\e[0m\"\n+      ts_col = \"\\e[38;5;61m\"\n+      dur_col = \"\\e[38;5;141m\"\n+      output << \"[#{ts_col}#{timestamp}\\e[0m] #{row['cmd_text']} #{status} #{dur_col}#{duration}\\e[0m\\n\"\n     end\n-\n     return output\n   ensure\n     db.close if db","raw_url":"https://github.com/wstein/bin/raw/94977d75a38e91ff373500a44ff2a961fd3e0ab9/histlog","sha":"b6c6f883b7dc052b4baee57b33270dbd56ff00eb","status":"modified"}]}