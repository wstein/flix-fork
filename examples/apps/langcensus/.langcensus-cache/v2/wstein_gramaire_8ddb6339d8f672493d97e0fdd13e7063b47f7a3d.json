{"files":[{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/.scalafmt.conf","changes":2,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.scalafmt.conf?ref=8ddb6339d8f672493d97e0fdd13e7063b47f7a3d","deletions":1,"filename":".scalafmt.conf","patch":"@@ -3,5 +3,5 @@ runner.dialect = scala3\n maxColumn = 100\n \n project.excludeFilters = [\n-  \"core/shared/src/main/scala/gramaire/generated/.*\"\n+  \"core/src/main/scala/gramaire/generated/.*\"\n ]","raw_url":"https://github.com/wstein/gramaire/raw/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/.scalafmt.conf","sha":"8ea77bb1a1c0e917f2ddee8a339f4872e3af1a4e","status":"modified"},{"additions":55,"blob_url":"https://github.com/wstein/gramaire/blob/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala","changes":55,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala?ref=8ddb6339d8f672493d97e0fdd13e7063b47f7a3d","deletions":0,"filename":"core/.jvm/src/test/scala/gramaire/ScalaSelfHostSuite.scala","patch":"@@ -0,0 +1,55 @@\n+package gramaire\n+\n+// The Scala-retargeted half of the self-hosting proof (the migration\n+// plan's deferred task): `CodegenScala` emits a genuinely callable,\n+// compiled Scala module (`gramaire.generated.LrReduce`) instead of just\n+// PureScript source text to diff.\n+//\n+// Two checks, mirroring `Test.Codegen.purs` exactly:\n+//   1. Drift lock — the committed `generated/LrReduce.scala` matches a\n+//      fresh generation from `CodegenScala`'s own Scala-syntax action\n+//      profile (`CodegenScala.lrActionsScala`, not `bootstrapGrammar`'s\n+//      `.action` field, which stays PureScript text so every existing\n+//      PureScript-oracle test — `CodegenSuite`, `SelfHostSuite` — keeps\n+//      passing unmodified, and `grammar/lr.gram.md`, still read by the\n+//      live PureScript self-hosting proof, stays untouched).\n+//   2. Oracle — the SAME real `grammar/lr.gram.md` document, parsed with\n+//      the SAME table built from `bootstrapGrammar`, but driven by the\n+//      *generated* Scala reduce instead of `Lr.reduce`'s hand-written\n+//      one, still reconstructs `bootstrapGrammar` exactly. Two different\n+//      reduce implementations, same document, same result.\n+class ScalaSelfHostSuite extends munit.FunSuite:\n+  private def readFile(path: String): String =\n+    java.nio.file.Files.readString(java.nio.file.Path.of(path))\n+\n+  test(\"the committed generated/LrReduce.scala matches a fresh generation\") {\n+    IR.buildIR(Method.Canonical, \"Lr\", Bootstrap.bootstrapGrammar) match\n+      case Left(_) => fail(\"bootstrapGrammar should build\")\n+      case Right(ir) =>\n+        val scalaIr = CodegenScala.withScalaActions(ir)\n+        val committed = readFile(CodegenScala.lrReduceModulePath)\n+        assertEquals(CodegenScala.generateLrReduce(scalaIr), committed)\n+  }\n+\n+  test(\"self-host holds with the generated Scala reduce\") {\n+    val md = readFile(\"grammar/lr.gram.md\")\n+    val src = Lr.lrBlocks(md).mkString(\"\\n\") + \"\\n\"\n+    val items = Scanner.buildItems(\n+      Tokens.parseTokens(Bootstrap.lrTokensSource).getOrElse(Vector.empty),\n+      Vector(\":\", \"|\", \"(\", \")\", \".\", \"~\")\n+    )\n+    val raw = Scanner.scan(items, src)\n+    assert(!Scanner.hasError(raw), \"grammar/lr.gram.md should scan cleanly\")\n+    Table.buildTablesFor(Method.Canonical, Bootstrap.bootstrapGrammar) match\n+      case Left(_) => fail(\"lr tables should build\")\n+      case Right(table) =>\n+        Parser.run[SemVal](\n+          table,\n+          Lr.tokenVal,\n+          gramaire.generated.LrReduce.reduce,\n+          Lexer.normalizeNewlines(raw)\n+        ) match\n+          case Right(SemVal.VGrammar(g)) => assertEquals(g, Bootstrap.bootstrapGrammar)\n+          case Right(_)                  => fail(\"parse should yield a Grammar\")\n+          case Left(e)                   => fail(s\"parse failed: ${e.render}\")\n+  }","raw_url":"https://github.com/wstein/gramaire/raw/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FScalaSelfHostSuite.scala","sha":"88527ea9bad99f317e435cd8b5f12692a7539262","status":"added"},{"additions":179,"blob_url":"https://github.com/wstein/gramaire/blob/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FCodegenScala.scala","changes":179,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FCodegenScala.scala?ref=8ddb6339d8f672493d97e0fdd13e7063b47f7a3d","deletions":0,"filename":"core/src/main/scala/gramaire/CodegenScala.scala","patch":"@@ -0,0 +1,179 @@\n+package gramaire\n+\n+// Scala-emitting codegen: generate the `lr` reduce function from the IR,\n+// targeting real, compiled Scala source rather than PureScript text.\n+//\n+// This is the Scala-retargeted half of the self-hosting proof (the\n+// migration plan's deferred task). `Codegen` stays PureScript-emitting —\n+// it is the drift-lock oracle for the still-live `src/Gramaire/Generated/\n+// LrReduce.purs`, driven by `Bootstrap.bootstrapGrammar`'s own\n+// PureScript-lambda action text, and neither of those change here.\n+//\n+// The Scala action bodies below are a SEPARATE, independent profile —\n+// production id -> Scala-arrow-syntax action text, hand-derived from\n+// `Gramaire.Lr.reduce`'s own case-for-case logic (not from\n+// `bootstrapGrammar`'s `.action` field, which stays PureScript text so\n+// every existing PureScript-oracle test keeps passing unmodified). This\n+// keeps `grammar/lr.gram.md` — read by the still-live PureScript\n+// self-hosting proof — untouched too.\n+// Structurally parallel to `Codegen`, Scala-arrow-syntax-aware and\n+// emitting Scala `case` syntax instead of PureScript's.\n+object CodegenScala:\n+\n+  /** Where the generated module is written and read back from. */\n+  val lrReduceModulePath: String = \"core/src/main/scala/gramaire/generated/LrReduce.scala\"\n+\n+  /** The `lr` typed-AST profile: each grammar symbol mapped to the `SemVal` case that carries its\n+    * value. Identical to `Codegen.lrConMap` — the profile is language-independent.\n+    */\n+  val lrConMap: Map[String, String] = Codegen.lrConMap\n+\n+  /** The `lr` grammar's semantic actions, in Scala-arrow syntax, keyed by production id (the same\n+    * order `bootstrapGrammar`'s rules/alts flatten to). Mirrors `Gramaire.Lr.reduce`'s own\n+    * case-for-case logic exactly — a hand-derived, independent Scala profile, not a rewrite of\n+    * `Bootstrap.bootstrapGrammar`'s PureScript-syntax `.action` field.\n+    */\n+  val lrActionsScala: Map[Int, String] = Map(\n+    0 -> \"(rs) => Grammar(rs)\", // Grammar : RuleList\n+    1 -> \"(r) => Vector(r)\", // RuleList : Rule\n+    2 -> \"(rs, _, r) => rs :+ r\", // RuleList : RuleList NL Rule\n+    3 -> \"(attr, lhs, _, _, alts) => Rule(lhs, Vector(attr), alts)\", // Rule : ATTR IDENT NL ':' Body\n+    4 -> \"(lhs, _, _, alts) => Rule(lhs, Vector.empty, alts)\", // Rule : IDENT NL ':' Body\n+    5 -> \"(a) => Vector(a)\", // Body : Alt\n+    6 -> \"(bs, _, a) => bs :+ a\", // Body : Body '|' Alt\n+    7 -> \"(syms, lbl, act) => Alt(syms, lbl, act)\", // Alt : SymList Label Action\n+    8 -> \"(syms, lbl) => Alt(syms, lbl, None)\", // Alt : SymList Label\n+    9 -> \"(syms, act) => Alt(syms, None, act)\", // Alt : SymList Action\n+    10 -> \"(syms) => Alt(syms, None, None)\", // Alt : SymList\n+    11 -> \"(s) => Vector(s)\", // SymList : Sym\n+    12 -> \"(ss, s) => ss :+ s\", // SymList : SymList Sym\n+    13 -> \"(i) => Ref(i)\", // Sym : IDENT\n+    14 -> \"(t) => Lit(t)\", // Sym : TERM_LIT\n+    15 -> \"(i, _) => Rep(Ref(i))\", // Sym : IDENT PLUS\n+    16 -> \"(t, _) => Rep(Lit(t))\", // Sym : TERM_LIT PLUS\n+    17 -> \"(i, _) => Star(Ref(i))\", // Sym : IDENT STAR\n+    18 -> \"(t, _) => Star(Lit(t))\", // Sym : TERM_LIT STAR\n+    19 -> \"(i, _) => Opt(Ref(i))\", // Sym : IDENT QUESTION\n+    20 -> \"(t, _) => Opt(Lit(t))\", // Sym : TERM_LIT QUESTION\n+    21 -> \"(name, _, args, _) => Macro(name, args)\", // Sym : IDENT LANGLE Args RANGLE\n+    22 -> \"(name, _, s) => Field(name, s)\", // Sym : IDENT ':' Sym\n+    23 -> \"(_, g, _) => Group(g)\", // Sym : '(' GroupBody ')'\n+    24 -> \"(_, g, _, _) => Rep(Group(g))\", // Sym : '(' GroupBody ')' PLUS\n+    25 -> \"(_, g, _, _) => Star(Group(g))\", // Sym : '(' GroupBody ')' STAR\n+    26 -> \"(_, g, _, _) => Opt(Group(g))\", // Sym : '(' GroupBody ')' QUESTION\n+    27 -> \"(a) => a\", // Sym : Atom\n+    28 -> \"(a, _) => Rep(a)\", // Sym : Atom PLUS\n+    29 -> \"(a, _) => Star(a)\", // Sym : Atom STAR\n+    30 -> \"(a, _) => Opt(a)\", // Sym : Atom QUESTION\n+    31 -> \"(s) => Vector(s)\", // Args : Sym\n+    32 -> \"(args2, _, s) => args2 :+ s\", // Args : Args COMMA Sym\n+    33 -> \"(a) => Some(a)\", // Action : ACTION\n+    34 -> \"(l) => Some(l)\", // Label : LABEL\n+    35 -> \"(syms) => Vector(syms)\", // GroupBody : SymList\n+    36 -> \"(alts, _, syms) => alts :+ syms\", // GroupBody : GroupBody '|' SymList\n+    37 -> \"(_) => Any\", // Atom : '.'\n+    38 -> \"(_, s) => Not(s)\", // Atom : '~' NotArg\n+    39 -> \"(i) => Vector(i)\", // NotArg : SetItem\n+    40 -> \"(_, s, _) => s\", // NotArg : '(' SetBody ')'\n+    41 -> \"(i) => Vector(i)\", // SetBody : SetItem\n+    42 -> \"(s, _, i) => s :+ i\", // SetBody : SetBody '|' SetItem\n+    43 -> \"(i) => Ref(i)\", // SetItem : IDENT\n+    44 -> \"(t) => Lit(t)\" // SetItem : TERM_LIT\n+  )\n+\n+  /** Retag an IR's rule actions with the Scala-syntax profile above, keyed by production id (rather\n+    * than by whatever the IR's own `.action` field carried in — always \"scala\", ignoring whatever\n+    * language its source document declared).\n+    */\n+  def withScalaActions(ir: IR): IR =\n+    ir.copy(grammar =\n+      ir.grammar.copy(rules =\n+        ir.grammar.rules.map(r =>\n+          r.copy(actions = Map(\"scala\" -> lrActionsScala.getOrElse(r.id, \"\")))\n+        )\n+      )\n+    )\n+\n+  private def termId(t: IRTerminal): Int = t match\n+    case IRTerminal.IRLiteral(i, _) => i\n+    case IRTerminal.IRClass(i, _)   => i\n+\n+  private def termName(t: IRTerminal): String = t match\n+    case IRTerminal.IRLiteral(_, s) => s\n+    case IRTerminal.IRClass(_, n)   => n\n+\n+  private final case class Names(ref: IRRef => String, lhs: Int => String)\n+\n+  private def symbolNames(ir: IR): Names =\n+    val ntById: Map[Int, String] = ir.grammar.nonterminals.map(n => n.id -> n.name).toMap\n+    val termById: Map[Int, String] = ir.grammar.terminals.map(t => termId(t) -> termName(t)).toMap\n+    def look(m: Map[Int, String], i: Int): String = m.getOrElse(i, \"?\")\n+    Names(\n+      ref = {\n+        case IRRef.IRRefNT(i, _) => look(ntById, i)\n+        case IRRef.IRRefT(i, _)  => look(termById, i)\n+      },\n+      lhs = i => look(ntById, i)\n+    )\n+\n+  private def actionOf(rule: IRRule): String = rule.actions.getOrElse(\"scala\", \"\")\n+\n+  private final case class Lambda(params: Vector[String], body: String)\n+\n+  // Split `(p1, p2) => body` (or the empty-param `() => body`) into its parameters and body text.\n+  private def parseLambda(s: String): Option[Lambda] =\n+    val trimmed = s.trim\n+    if !trimmed.startsWith(\"(\") then None\n+    else\n+      val closeIdx = trimmed.indexOf(')')\n+      if closeIdx < 0 then None\n+      else\n+        val paramsPart = trimmed.substring(1, closeIdx).trim\n+        val rest = trimmed.substring(closeIdx + 1).trim\n+        if !rest.startsWith(\"=>\") then None\n+        else\n+          val params =\n+            if paramsPart.isEmpty then Vector.empty\n+            else paramsPart.split(\",\", -1).toVector.map(_.trim)\n+          Some(Lambda(params, rest.stripPrefix(\"=>\").trim))\n+\n+  // A kid pattern: `_` for an ignored parameter, else `SemVal.<Con>(<name>)`.\n+  private def mkPat(conMap: Map[String, String], symName: String, param: String): String =\n+    if param == \"_\" then \"_\" else s\"SemVal.${conMap.getOrElse(symName, s\"?$symName\")}($param)\"\n+\n+  // One case branch for a production.\n+  private def genBranch(conMap: Map[String, String], names: Names, rule: IRRule): String =\n+    parseLambda(actionOf(rule)) match\n+      case None =>\n+        s\"\"\"    case (${rule.id}, _) => SemVal.VErr(\"no action for production ${rule.id}\")\"\"\"\n+      case Some(Lambda(params, body)) =>\n+        val symNames = rule.rhs.map(names.ref)\n+        val pats = symNames.zip(params).map { case (sn, p) => mkPat(conMap, sn, p) }\n+        val wrap = conMap.getOrElse(names.lhs(rule.lhs), \"VErr\")\n+        s\"    case (${rule.id}, Vector(${pats.mkString(\", \")})) => SemVal.$wrap($body)\"\n+\n+  private val header: String =\n+    Vector(\n+      \"package gramaire.generated\",\n+      \"\",\n+      \"import gramaire.Sym.*\",\n+      \"import gramaire.{Alt, Grammar, Rule, SemVal}\",\n+      \"\",\n+      \"// GENERATED by Gramaire codegen from the `lr` grammar. Do not edit.\",\n+      \"//\",\n+      \"// This is the reduce `Gramaire.Lr` writes by hand, emitted from the IR\",\n+      \"// and the `lr` typed-AST profile. The self-host oracle proves it: the\",\n+      \"// parser driven by this reduce reconstructs `Bootstrap.bootstrapGrammar`.\",\n+      \"// Regenerate with `CodegenScala.generateLrReduce`.\",\n+      \"object LrReduce:\",\n+      \"  def reduce(p: Int, kids: Vector[SemVal]): SemVal = (p, kids) match\"\n+    ).mkString(\"\\n\")\n+\n+  private val fallback: String =\n+    \"\"\"    case _ => SemVal.VErr(s\"unexpected reduce shape for production $p\")\"\"\"\n+\n+  /** Emit the full `gramaire.generated.LrReduce` module text. */\n+  def generateLrReduce(ir: IR): String =\n+    val names = symbolNames(ir)\n+    val body = ir.grammar.rules.map(genBranch(lrConMap, names, _)).mkString(\"\\n\")\n+    header + \"\\n\" + body + \"\\n\" + fallback + \"\\n\"","raw_url":"https://github.com/wstein/gramaire/raw/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FCodegenScala.scala","sha":"3c5c84127893a9b3d64ebe628d93f7cf27834cec","status":"added"},{"additions":59,"blob_url":"https://github.com/wstein/gramaire/blob/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fgenerated%2FLrReduce.scala","changes":59,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fgenerated%2FLrReduce.scala?ref=8ddb6339d8f672493d97e0fdd13e7063b47f7a3d","deletions":0,"filename":"core/src/main/scala/gramaire/generated/LrReduce.scala","patch":"@@ -0,0 +1,59 @@\n+package gramaire.generated\n+\n+import gramaire.Sym.*\n+import gramaire.{Alt, Grammar, Rule, SemVal}\n+\n+// GENERATED by Gramaire codegen from the `lr` grammar. Do not edit.\n+//\n+// This is the reduce `Gramaire.Lr` writes by hand, emitted from the IR\n+// and the `lr` typed-AST profile. The self-host oracle proves it: the\n+// parser driven by this reduce reconstructs `Bootstrap.bootstrapGrammar`.\n+// Regenerate with `CodegenScala.generateLrReduce`.\n+object LrReduce:\n+  def reduce(p: Int, kids: Vector[SemVal]): SemVal = (p, kids) match\n+    case (0, Vector(SemVal.VRules(rs))) => SemVal.VGrammar(Grammar(rs))\n+    case (1, Vector(SemVal.VRule(r))) => SemVal.VRules(Vector(r))\n+    case (2, Vector(SemVal.VRules(rs), _, SemVal.VRule(r))) => SemVal.VRules(rs :+ r)\n+    case (3, Vector(SemVal.VStr(attr), SemVal.VStr(lhs), _, _, SemVal.VAlts(alts))) => SemVal.VRule(Rule(lhs, Vector(attr), alts))\n+    case (4, Vector(SemVal.VStr(lhs), _, _, SemVal.VAlts(alts))) => SemVal.VRule(Rule(lhs, Vector.empty, alts))\n+    case (5, Vector(SemVal.VAlt(a))) => SemVal.VAlts(Vector(a))\n+    case (6, Vector(SemVal.VAlts(bs), _, SemVal.VAlt(a))) => SemVal.VAlts(bs :+ a)\n+    case (7, Vector(SemVal.VSyms(syms), SemVal.VMaybeStr(lbl), SemVal.VMaybeStr(act))) => SemVal.VAlt(Alt(syms, lbl, act))\n+    case (8, Vector(SemVal.VSyms(syms), SemVal.VMaybeStr(lbl))) => SemVal.VAlt(Alt(syms, lbl, None))\n+    case (9, Vector(SemVal.VSyms(syms), SemVal.VMaybeStr(act))) => SemVal.VAlt(Alt(syms, None, act))\n+    case (10, Vector(SemVal.VSyms(syms))) => SemVal.VAlt(Alt(syms, None, None))\n+    case (11, Vector(SemVal.VSym(s))) => SemVal.VSyms(Vector(s))\n+    case (12, Vector(SemVal.VSyms(ss), SemVal.VSym(s))) => SemVal.VSyms(ss :+ s)\n+    case (13, Vector(SemVal.VStr(i))) => SemVal.VSym(Ref(i))\n+    case (14, Vector(SemVal.VStr(t))) => SemVal.VSym(Lit(t))\n+    case (15, Vector(SemVal.VStr(i), _)) => SemVal.VSym(Rep(Ref(i)))\n+    case (16, Vector(SemVal.VStr(t), _)) => SemVal.VSym(Rep(Lit(t)))\n+    case (17, Vector(SemVal.VStr(i), _)) => SemVal.VSym(Star(Ref(i)))\n+    case (18, Vector(SemVal.VStr(t), _)) => SemVal.VSym(Star(Lit(t)))\n+    case (19, Vector(SemVal.VStr(i), _)) => SemVal.VSym(Opt(Ref(i)))\n+    case (20, Vector(SemVal.VStr(t), _)) => SemVal.VSym(Opt(Lit(t)))\n+    case (21, Vector(SemVal.VStr(name), _, SemVal.VSyms(args), _)) => SemVal.VSym(Macro(name, args))\n+    case (22, Vector(SemVal.VStr(name), _, SemVal.VSym(s))) => SemVal.VSym(Field(name, s))\n+    case (23, Vector(_, SemVal.VGroupBody(g), _)) => SemVal.VSym(Group(g))\n+    case (24, Vector(_, SemVal.VGroupBody(g), _, _)) => SemVal.VSym(Rep(Group(g)))\n+    case (25, Vector(_, SemVal.VGroupBody(g), _, _)) => SemVal.VSym(Star(Group(g)))\n+    case (26, Vector(_, SemVal.VGroupBody(g), _, _)) => SemVal.VSym(Opt(Group(g)))\n+    case (27, Vector(SemVal.VSym(a))) => SemVal.VSym(a)\n+    case (28, Vector(SemVal.VSym(a), _)) => SemVal.VSym(Rep(a))\n+    case (29, Vector(SemVal.VSym(a), _)) => SemVal.VSym(Star(a))\n+    case (30, Vector(SemVal.VSym(a), _)) => SemVal.VSym(Opt(a))\n+    case (31, Vector(SemVal.VSym(s))) => SemVal.VSyms(Vector(s))\n+    case (32, Vector(SemVal.VSyms(args2), _, SemVal.VSym(s))) => SemVal.VSyms(args2 :+ s)\n+    case (33, Vector(SemVal.VStr(a))) => SemVal.VMaybeStr(Some(a))\n+    case (34, Vector(SemVal.VStr(l))) => SemVal.VMaybeStr(Some(l))\n+    case (35, Vector(SemVal.VSyms(syms))) => SemVal.VGroupBody(Vector(syms))\n+    case (36, Vector(SemVal.VGroupBody(alts), _, SemVal.VSyms(syms))) => SemVal.VGroupBody(alts :+ syms)\n+    case (37, Vector(_)) => SemVal.VSym(Any)\n+    case (38, Vector(_, SemVal.VSyms(s))) => SemVal.VSym(Not(s))\n+    case (39, Vector(SemVal.VSym(i))) => SemVal.VSyms(Vector(i))\n+    case (40, Vector(_, SemVal.VSyms(s), _)) => SemVal.VSyms(s)\n+    case (41, Vector(SemVal.VSym(i))) => SemVal.VSyms(Vector(i))\n+    case (42, Vector(SemVal.VSyms(s), _, SemVal.VSym(i))) => SemVal.VSyms(s :+ i)\n+    case (43, Vector(SemVal.VStr(i))) => SemVal.VSym(Ref(i))\n+    case (44, Vector(SemVal.VStr(t))) => SemVal.VSym(Lit(t))\n+    case _ => SemVal.VErr(s\"unexpected reduce shape for production $p\")","raw_url":"https://github.com/wstein/gramaire/raw/8ddb6339d8f672493d97e0fdd13e7063b47f7a3d/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fgenerated%2FLrReduce.scala","sha":"c213bc6af43f9d0199f49d531ed9919a6ed1a0fe","status":"added"}]}