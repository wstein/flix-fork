{"files":[{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/docs%2Fadr%2F0083-union-types.md","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0083-union-types.md?ref=eb1f9b412ba64622267ad96096868f8e0e7f0aa3","deletions":2,"filename":"docs/adr/0083-union-types.md","patch":"@@ -1,7 +1,7 @@\n # ADR-0083 — Union types: anonymous structural unions `A | B`, narrowed by type\n \n **Status:** Accepted (partial)\n-**Implemented:** **Phases 1–2 (primitive-member unions).** The parser accepts `A | B` in param/field positions → canonical `Union(...)` (`Rian.TypeStr.normalize`, flatten/dedup/sort); the type-pattern `name Type` parses → Core `PTyped` (`Rian.Pratt`/`Rian.Core`). BEAM (`Rian.Beam`) desugars a `PTyped` arm to a var bind + a runtime type-test (`is_integer`/`is_binary`/…), and JS (`Rian.JS`) to a `typeof` test — so a union of **primitive** members (`Int53 | String`) narrows and **runs on `:ex` and `:js`** (rustc/node-verified: `describe(41)/describe(\"hi\")` → `42/0`). `Rian.Reach` pins a primitive union off the nominal `:rs`/`:jvm` only; a union with a **non-primitive** member (sum/struct — no discriminator wired yet) or nested in a generic still pins off **every** target, honestly (ADR-0000). **Not yet:** checker narrowing/coherence (Phase 3), Rust enum synthesis (Phase 4), JVM `Any` (Phase 5), sum/struct discriminators in a type-pattern, the `T | E` sugar retirement.\n+**Implemented:** **Phases 1–3 (primitive-member unions).** The parser accepts `A | B` in param/field positions → canonical `Union(...)` (`Rian.TypeStr.normalize`, flatten/dedup/sort); the type-pattern `name Type` parses → Core `PTyped` (`Rian.Pratt`/`Rian.Core`). BEAM (`Rian.Beam`) desugars a `PTyped` arm to a var bind + a runtime type-test (`is_integer`/`is_binary`/…), and JS (`Rian.JS`) to a `typeof` test — so a union of **primitive** members (`Int53 | String`) narrows and **runs on `:ex` and `:js`** (BEAM-`apply`/node-verified: `describe(41)/describe(\"hi\")` → `42/0`). `Rian.Check` narrows a `PTyped` binding to its member type and makes a member assignable into a union (conservatively). `Rian.Reach` pins a *distinct-discriminator* primitive union off the nominal `:rs`/`:jvm` only; a union with a **non-primitive** member (sum/struct — no discriminator wired yet), a **discriminator clash** (`Int32 | Char`), or one nested in a generic still pins off **every** target, honestly (ADR-0000). **Not yet:** Rust enum synthesis (Phase 4), JVM `Any` (Phase 5), sum/struct discriminators in a type-pattern, a hard `Check` clash-rejection, the `T | E` sugar retirement.\n **Depends on:** ADR-0034 (type-system foundations — *defers* union types), ADR-0042 (protocol dispatch / `when is` machinery this reuses), ADR-0040 (`T | E` return sugar — the `|` this must reconcile with)\n **Refs:** ADR-0035 (errors are values; no hidden control flow), ADR-0049/0050 (per-target emitters + typed Core IR), ADR-0041 (idiomatic-per-target lowering), ADR-0057/0058 (reach is inferred over a target set), ADR-0000 (the honesty bar — the matrix matches the emitters)\n **Owners:** Arthur Pendelton (inference / narrowing) · Elena Rostova (Rust enum synthesis / coherence) · Maya Lin (emitters) · Mira (totality / conservative checker) · Kira Neri (Reach honesty) · Samir Patel (exhaustiveness) · Chloe Bennett (parser) · Rachel Okafor (PM)\n@@ -90,7 +90,7 @@ The Rust **enum synthesis** is the load-bearing new machinery (mangling, the wra\n \n 1. **Parse + represent** — ✅ **done.** `Rian.TypeStr.normalize` canonicalizes `A | B | C` (flatten/dedup/sort) → `Union(...)`; `param`/`field` accept it (rejecting garbage). The **type-pattern** `name Type` parses (`Rian.Pratt`) → Core `PTyped`. Reach's `:union` blocker keeps the matrix honest with no emit.\n 2. **BEAM + JS** — ✅ **done for primitive members.** `Rian.Beam` desugars a `PTyped` arm to a var bind + a runtime type-test BIF (`is_integer`/`is_binary`/`is_boolean`/`is_float`); `Rian.JS` to the `typeof` discriminator (whole-program int-mode aware). A primitive union (`Int53 | String`) narrows and **runs on `:ex`/`:js`** (BEAM `apply` + node-verified). **Deferred:** a sum/struct member in a type-pattern needs the tag/`__struct__` discriminator (the dispatcher's `sum_guard`/`struct_guard`) wired in — until then such a union pins off every target.\n-3. **Checker narrowing + coherence** — refine the arm binding to the member type; reject discriminator clashes for unions required to reach a runtime-dispatch target (reuse ADR-0042 §5); assignability `A ⊑ A | B` (a member is assignable into the union).\n+3. **Checker narrowing + coherence** — ✅ **done.** `Rian.Check.narrow` binds a `PTyped` arm to its **member** type (`s String ->` ⇒ `s : String`, verified via `annotate`), so member operations type-check; `assignable?` gains union clauses — a member flows **into** a union (`Int53 ⊑ Int53 | String`) and a union flows where **all** members do — both conservative (an `:unknown` member stays assignable, per CLAUDE.md). Coherence: a discriminator **clash** (`Int32 | Char`, both `is_integer`) is not narrowable, so `Rian.Reach` declines to claim it reaches `:ex`/`:js` (a hard `Check` rejection with a message is a future refinement).\n 4. **Rust** — synthesize the name-mangled `enum`, the wrap-at-boundary coercion, and the `match` narrowing (a coercion pass sibling to the associated-type cast). Unions reach `:rs`.\n 5. **JVM** — `Any` parameter + `when is` narrowing. Unions reach `:jvm`. Update the reach matrix, ADR-0034 (retire the \"deferred\" note), and SELFHOST.md.\n ","raw_url":"https://github.com/wstein/rian-lab/raw/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/docs%2Fadr%2F0083-union-types.md","sha":"f7028d94edf188ac89dd39eb09a29a57afec1014","status":"modified"},{"additions":19,"blob_url":"https://github.com/wstein/rian-lab/blob/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/lib%2Frian%2Fcheck.ex","changes":20,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fcheck.ex?ref=eb1f9b412ba64622267ad96096868f8e0e7f0aa3","deletions":1,"filename":"lib/rian/check.ex","patch":"@@ -74,7 +74,7 @@ defmodule Rian.Check do\n   }\n \n   alias Rian.Core.{ECaptureNamed, ELambda}\n-  alias Rian.Core.{PChar, PCtor, PLit, PVar}\n+  alias Rian.Core.{PChar, PCtor, PLit, PTyped, PVar}\n   alias Rian.IR.Func\n \n   defmodule Error do\n@@ -702,6 +702,10 @@ defmodule Rian.Check do\n   # and narrows each argument in turn. Unknown ctor / no `tdefs` -> `:unknown`.\n   defp narrow(%PVar{name: name}, type, _ic, env), do: Map.put(env, name, concretize(type))\n \n+  # a type-pattern `n Type` (ADR-0083) narrows its binding to the MATCHED type, not\n+  # the scrutinee's (union) type — so member operations type-check inside the arm.\n+  defp narrow(%PTyped{name: name, tname: tname}, _type, _ic, env), do: Map.put(env, name, tname)\n+\n   defp narrow(%PCtor{ctor: ctor, args: args}, _type, ic, env) do\n     field_types = Map.get(Map.get(ic, :tdefs, %{}), ctor, [])\n \n@@ -1422,6 +1426,16 @@ defmodule Rian.Check do\n   defp assignable?(:unknown, _to), do: true\n   defp assignable?(_from, :unknown), do: true\n \n+  # value unions (`Union(...)`, ADR-0083). A union flows where ALL its members do;\n+  # a value of a member type flows INTO a union. (The from-union clause is first so\n+  # union→union takes the all-members reading, not any-member.) An `:unknown` member\n+  # keeps it assignable via the base clauses — the conservative bar (CLAUDE.md).\n+  defp assignable?(\"Union(\" <> _ = from, to),\n+    do: Enum.all?(union_members_of(from), &assignable?(&1, to))\n+\n+  defp assignable?(from, \"Union(\" <> _ = to),\n+    do: Enum.any?(union_members_of(to), &assignable?(from, &1))\n+\n   defp assignable?(from, to) do\n     cond do\n       # a constructor whose payload type the inference didn't track yields the **bare\n@@ -1449,6 +1463,10 @@ defmodule Rian.Check do\n \n   defp bare_head_of?(_from, _to), do: false\n \n+  # the member type strings of a canonical `Union(...)` (ADR-0083).\n+  defp union_members_of(\"Union(\" <> rest),\n+    do: rest |> binary_part(0, byte_size(rest) - 1) |> Rian.TypeStr.split_top_commas()\n+\n   # a numeric type string -> {:int | :uint | :float, bit-width}, else nil\n   defp num_kind(\"UInt\" <> w), do: num_bits(:uint, w)\n   defp num_kind(\"Int\" <> w), do: num_bits(:int, w)","raw_url":"https://github.com/wstein/rian-lab/raw/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/lib%2Frian%2Fcheck.ex","sha":"b67e044f0bdb78ba0fd91ef3cfc61a29fb44127f","status":"modified"},{"additions":22,"blob_url":"https://github.com/wstein/rian-lab/blob/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/lib%2Frian%2Freach.ex","changes":30,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Freach.ex?ref=eb1f9b412ba64622267ad96096868f8e0e7f0aa3","deletions":8,"filename":"lib/rian/reach.ex","patch":"@@ -632,19 +632,33 @@ defmodule Rian.Reach do\n   defp union_kill_targets(_), do: []\n \n   # the type is EXACTLY a top-level `Union(...)` whose every member is a primitive\n-  # with a runtime discriminator (`is_integer`/`typeof`) — the narrowable shape.\n+  # with a runtime discriminator (`is_integer`/`typeof`) AND those discriminators are\n+  # all DISTINCT — `Int32 | Char` (both `is_integer`) can't narrow at runtime (its\n+  # second arm would be dead), so it is not narrowable and must not claim `:ex`/`:js`.\n   defp primitive_union?(\"Union(\" <> rest) do\n-    String.ends_with?(rest, \")\") and\n-      rest\n-      |> binary_part(0, byte_size(rest) - 1)\n-      |> Rian.TypeStr.split_top_commas()\n-      |> Enum.all?(&primitive_discriminable?/1)\n+    if String.ends_with?(rest, \")\") do\n+      members = rest |> binary_part(0, byte_size(rest) - 1) |> Rian.TypeStr.split_top_commas()\n+      discs = Enum.map(members, &discriminator/1)\n+      Enum.all?(discs, &(&1 != nil)) and length(Enum.uniq(discs)) == length(discs)\n+    else\n+      false\n+    end\n   end\n \n   defp primitive_union?(_), do: false\n \n-  defp primitive_discriminable?(t),\n-    do: t in [\"Bool\", \"String\", \"Char\"] or Regex.match?(~r/^(U?Int\\d*|Float\\d*)$/, t)\n+  # the runtime discriminator a primitive type tests under (`is_integer`/`typeof`),\n+  # or nil for a type with none. `Char`/`Int*`/`UInt*` share `:integer` (a clash).\n+  defp discriminator(t) do\n+    cond do\n+      t == \"Bool\" -> :boolean\n+      t == \"String\" -> :binary\n+      t == \"Char\" -> :integer\n+      Regex.match?(~r/^U?Int\\d*$/, t) -> :integer\n+      Regex.match?(~r/^Float\\d*$/, t) -> :float\n+      true -> nil\n+    end\n+  end\n \n   defp width_blocker,\n     do: %{","raw_url":"https://github.com/wstein/rian-lab/raw/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/lib%2Frian%2Freach.ex","sha":"77f0cdcd56545aec04ae7275d9720e1a57c1768d","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/rian-lab/blob/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/test%2Frian%2Fcheck_test.exs","changes":15,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcheck_test.exs?ref=eb1f9b412ba64622267ad96096868f8e0e7f0aa3","deletions":0,"filename":"test/rian/check_test.exs","patch":"@@ -362,6 +362,21 @@ defmodule Rian.CheckTest do\n              end\n              \"\"\") == :ok\n     end\n+\n+    test \"a value-union type-pattern narrows its binding to the matched member type (ADR-0083)\" do\n+      alias Rian.Core\n+\n+      typed =\n+        Check.annotate(\n+          Pratt.parse(\"case x do\\n  n Int53 -> n\\n  s String -> s\\nend\"),\n+          %{\"x\" => \"Union(Int53, String)\"}\n+        )\n+\n+      assert %Core.ECase{arms: [{_, _, b1}, {_, _, b2}]} = typed\n+      # each arm body is the bound var, narrowed to its arm's member type\n+      assert b1.type == \"Int53\"\n+      assert b2.type == \"String\"\n+    end\n   end\n \n   describe \"error-set composition (ADR-0040 §4)\" do","raw_url":"https://github.com/wstein/rian-lab/raw/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/test%2Frian%2Fcheck_test.exs","sha":"df2d4813cad5edbad7f4fceca9bb977680abe037","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/test%2Frian%2Freach_test.exs","changes":7,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Freach_test.exs?ref=eb1f9b412ba64622267ad96096868f8e0e7f0aa3","deletions":0,"filename":"test/rian/reach_test.exs","patch":"@@ -49,6 +49,13 @@ defmodule Rian.ReachTest do\n              )\n     end\n \n+    test \"a discriminator-CLASH union (`Int32 | Char`, both is_integer) is not claimed narrowable (ADR-0083)\" do\n+      # both members test `is_integer`/`typeof === number`, so the second arm would be\n+      # dead — the union can't narrow at runtime, so it must reach no dynamic target.\n+      rep = reach(\"def f(x Int32 | Char) Int32 := x\")\n+      assert targets(rep, \"f\") == []\n+    end\n+\n     test \"a NON-primitive value union (no discriminator wired) stays pinned off every target (ADR-0083)\" do\n       rep = reach(\"def f(x A | B) Int64 := x\")\n ","raw_url":"https://github.com/wstein/rian-lab/raw/eb1f9b412ba64622267ad96096868f8e0e7f0aa3/test%2Frian%2Freach_test.exs","sha":"c9ddd676165ae56af396a27d8001dcc185c80651","status":"modified"}]}