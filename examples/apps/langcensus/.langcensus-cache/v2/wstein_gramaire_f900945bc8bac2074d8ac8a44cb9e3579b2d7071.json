{"files":[{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/docs%2Fmulti-backend-implementation-plan.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":1,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -713,7 +713,7 @@ everything target-specific is independent.\n | D33 | %external post-lex pass    | **`%external(pass)` names a host-supplied, pure `Array Token -> Array Token` pass, registered by name and run after the regular scan; it is scalar-only (cf. D31). It handles the genuinely non-regular fraction (offside, the `lr` continuation keep/drop), not payload extraction — that is capture's job (D32).**                                                                       | Pins the contract in lexer-spec §6 rather than scattering it. With capture owning token text (D32), the `NL` external pass shrinks to just the keep/drop continuation decision (line-continuation §3); the rest of lexing stays regular. Block-level `%external` (bring-your-own scanner: classes with no patterns) is preserved for fully context-sensitive languages, with in-file regular lexing the default. Companion resolutions (lexer-spec §13): Unicode `\\p{…}` is reserved-but-rejected for v0, and token fragments are deferred.                                                                                                                                                                                                                                                                                                                                             |\n | D34 | Terminal literal quoting   | **A terminal literal may be written `'x'` or `\"x\"` — two interchangeable delimiters, one meaning; the author picks whichever avoids escaping (`'\"'`, `\"'\"`). The chosen quote is escaped with a backslash inside the literal (`'\\''`, `\"\\\"\"`). Backtick is NOT a delimiter — it collides with Markdown inline code / fences. `'+'` and `\"+\"` are the same terminal.**                      | A grammar whose terminals are quotes is unreadable forced into one delimiter; two styles let the spelling dodge the escape. Backtick was dropped (it was a third option) because inside a `.gram.md` it clashes with Markdown's own code spans and fences. Stays regular: `TERM_LIT` is one class — a two-branch alternation with NO capture (D32 forbids per-branch captures), so the lexer emits the whole lexeme and the consumer (`Lr.tokenVal`) strips delimiters and unescapes. The CST gains from whole-match too (a leaf shows the concrete `'+'`; the AST gets `Lit \"+\"`). Rejected: a char/string split between the quote styles (Gramaire terminals are uninterpreted spellings) and escapes beyond the delimiter.                                                                                                                                                            |\n | D35 | Case-insensitive terminals | **A token class folds ASCII case when its regex carries a glued `i` flag (`/…/i`) or the class has the `%caseless` modifier — the two are identical. Folding applies to every `Lit` and `Class` in the pattern, and to an exact `\"string\"`; the matched text keeps its source casing. ASCII only — Unicode case folding is deferred with `\\p{…}` (§13).**                                  | Case-insensitive keywords (SQL, HTTP, many DSLs) are common enough to deserve a first-class spelling, not hand-written `[Ss][Ee]…` classes. Two spellings: the `i` flag is the familiar regex idiom, while `%caseless` reads naturally on an exact string (`\"select\" %caseless`) where there is no regex to flag. Stays regular and ReDoS-safe (§3): folding widens the per-char predicate in the existing DFA simulation (match `x` or its case-swap), not a new construct. The IR carries a `caseless` boolean, emitted only when true so existing goldens are byte-unchanged. Rejected: inline `(?i)` flags (a `(?…)` form §3 forbids) and Unicode folding now (scoped out with `\\p{…}`).                                                                                                                                                                                            |\n-| D36 | Raw .gram projection       | **`.gram.md` is the SINGLE source of truth. `.gram` is a DERIVED, FENCE-FREE projection (`gramaire strip`), ANTLR-style: ALL-CAPS lexer + Mixed-case parser rules told apart by case, `%left`/`%right` kept. Prose → `..` comment lines (simplified reStructuredText; skipped on parse); headings, errors, diagrams & tables dropped. Author only in `.gram.md`. `parse(strip(x)) == parse(x)`.**           | Gramaire's differentiator is that the grammar IS lint-clean Markdown rendering as docs — a second editable format is the sync/trust nightmare the bet avoids. But the ecosystem (editors, linguist, generators, review) expects a plain grammar file that does NOT read as Markdown, so a one-way EXPORT serves it. Fence-free and marker-free because the ```gramaire fences made `.gram` look like Markdown and Gramaire, like ANTLR, tells lexer from parser rules by case. Prose is preserved as `..` comment lines — a banner for the title/intro, one per section — so the docs travel with the projection; `toFenced` skips them, and only sections carrying a gramaire block survive. Rejected: raw as co-canonical / canonical input; yacc `%%` sections; `$N` positional actions. The reverse (`.gram` comments → `.gram.md` prose) is a future scaffold.                         |\n+| D36 | Raw .gram projection       | **`.gram.md` is the SINGLE source of truth. `.gram` is a DERIVED, FENCE-FREE projection (`gramaire strip`), ANTLR-style: ALL-CAPS lexer + Mixed-case parser rules told apart by case, `%left`/`%right` kept. Prose → `/** */` banner + `//` comments (skipped on parse); headings, errors, diagrams & tables dropped. Author only in `.gram.md`. `parse(strip(x)) == parse(x)`.**           | Gramaire's differentiator is that the grammar IS lint-clean Markdown rendering as docs — a second editable format is the sync/trust nightmare the bet avoids. But the ecosystem (editors, linguist, generators, review) expects a plain grammar file that does NOT read as Markdown, so a one-way EXPORT serves it. Fence-free and marker-free because the ```gramaire fences made `.gram` look like Markdown and Gramaire, like ANTLR, tells lexer from parser rules by case. Prose is preserved as comments — a `/** */` banner for the title/intro, `//` per section — so the docs travel with the projection; `toFenced` skips them, and only sections carrying a gramaire block survive. Rejected: raw as co-canonical / canonical input; yacc `%%` sections; `$N` positional actions. The reverse (`.gram` comments → `.gram.md` prose) is a future scaffold.                         |\n | D37 | LR precedence + bans       | **Shift/reduce conflicts resolved the yacc/LR way: `## Precedence`'s `%left`/`%right`/`%nonassoc` set terminal precedence (later binds tighter), a production's is its last terminal's; on a tie `%left` reduces, `%right` shifts. HARD RULE: a conflict no declaration covers — incl. reduce/reduce — still surfaces. `%prec` per-rule override deferred.**                               | Gramaire is LR, so precedence resolution is native and proven (the yacc family); ANTLR's precedence-climbing is an LL technique foreign to a bottom-up engine. Stratification (expr/term/factor) is O(levels) and unreadable past a few; `%left` is one line per level. The yacc footgun — SILENT conflict resolution — is closed by keeping `explain-conflict` watching: declared precedence resolves, undeclared ambiguity throws (buildTablesForP; Test.Conflict). REJECTED borrows (so they aren't relitigated): ANTLR structural precedence (alt-order/climbing, LL-only); yacc `%%` section structure (Markdown `##` sections already do this); `$N` positional actions (unreadable, untyped, an injection temptation — `{% %}` lambdas and `# Label` visitors stay).                                                                                                              |\n | D38 | Bison `.y` interop         | **Roadmap. `gramaire import <file.y>` (yacc/Bison → `.gram.md`) and `gramaire emit --backend bison`, mirroring the ANTLR converter (`Gramaire.Convert.Antlr` / `Gramaire.Backend.Antlr`). Precedence reuses D37 (`%left`/`%right`/`%nonassoc`); the prologue/epilogue (`%{ … %}`, `%code`, `%union`) and `$N` actions are flagged and dropped, exactly as the ANTLR import drops predicates/actions. Not built in this cycle.**                                                                                                    | Bison/yacc is the other grammar lingua franca; a converter widens Gramaire's reach and, alongside the ANTLR one, makes cross-format round-tripping possible. It rides existing seams: the IR is the interchange, precedence is already yacc-shaped (D37), and the \"skip the untranslatable prelude, flag it\" import strategy is proven on ANTLR (D3/D4). Full parity (union types, error productions, `%glr-parser`) is out of scope; the goal is the Core — rules, alternatives, `%`-precedence — round-tripping cleanly.                                                                                                                                                                                                                                        |\n | D39 | Cross-format comments      | **Roadmap. The `..` convention (D36) is the canonical comment form; round-tripping `{.gram, .g4, .y}` preserves comments by mapping each format's line/block comments to and from Gramaire `..`. Requires a future OPTIONAL IR prose field (IR stays version 0 — additive, omitted when empty); until then import stays prose-lossy (today's behaviour). First concrete step: carry ANTLR import/export comments through that field. Not built in this cycle.**                                                              | Comments die at the IR boundary today (the IR carries no prose), so every conversion is documentation-lossy. One optional prose field, plus the `..` canonical form, lets docs survive `.g4`/`.y` ↔ `.gram` round trips — the \"best-practice conversion\" goal. Kept additive so existing goldens stay byte-stable, and one-way until the field exists so nothing regresses. Rejected: a second, format-specific comment channel per target (the sync nightmare D36 avoids).                                                                                                                                                                                                                                                                                       |","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/docs%2Fmulti-backend-implementation-plan.md","sha":"0cf5e8ae5310ddc1e6cb8a43fcd7fd97aa854dbe","status":"modified"},{"additions":68,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Fgenerated%2Fgramaire-engine.mjs","changes":100,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fgenerated%2Fgramaire-engine.mjs?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":32,"filename":"site/src/generated/gramaire-engine.mjs","patch":"@@ -8897,8 +8897,8 @@ var $$unescape = /* @__PURE__ */ (function() {\n     ;\n     throw new Error(\"Failed pattern match at Gramaire.Lr (line 81, column 11 - line 86, column 53): \" + [v.constructor.name]);\n   };\n-  return function($274) {\n-    return fromCharArray(go(toCharArray($274)));\n+  return function($282) {\n+    return fromCharArray(go(toCharArray($282)));\n   };\n })();\n var unquoteLit = function(s) {\n@@ -8918,8 +8918,8 @@ var trimBlankEnds = /* @__PURE__ */ (function() {\n   var dropBlank = dropWhile(function(l) {\n     return trim(l) === \"\";\n   });\n-  return function($275) {\n-    return reverse(dropBlank(reverse(dropBlank($275))));\n+  return function($283) {\n+    return reverse(dropBlank(reverse(dropBlank($283))));\n   };\n })();\n var tokenVal = function(tok) {\n@@ -9145,15 +9145,15 @@ var normalizeLang = function(raw) {\n         return false;\n       }\n       ;\n-      throw new Error(\"Failed pattern match at Gramaire.Lr (line 411, column 26 - line 413, column 21): \" + [v.constructor.name]);\n+      throw new Error(\"Failed pattern match at Gramaire.Lr (line 412, column 26 - line 414, column 21): \" + [v.constructor.name]);\n     };\n   };\n   var isJs = function(l2) {\n     return elem6(l2)([\"js\", \"javascript\", \"jsx\", \"mjs\", \"cjs\", \"ecmascript\", \"esnext\"]) || (prefixThenDigits(\"es\")(l2) || prefixThenDigits(\"ecmascript\")(l2));\n   };\n   var l = toLower(trim(raw));\n-  var $249 = isJs(l);\n-  if ($249) {\n+  var $252 = isJs(l);\n+  if ($252) {\n     return \"js\";\n   }\n   ;\n@@ -9164,8 +9164,8 @@ var lrBlocks = function(md) {\n   var scan2 = function(acc) {\n     return function(line) {\n       if (acc.inside) {\n-        var $251 = trim(line) === \"```\";\n-        if ($251) {\n+        var $254 = trim(line) === \"```\";\n+        if ($254) {\n           return {\n             inside: false,\n             cur: [],\n@@ -9180,8 +9180,8 @@ var lrBlocks = function(md) {\n         };\n       }\n       ;\n-      var $252 = trim(line) === \"```gramaire\";\n-      if ($252) {\n+      var $255 = trim(line) === \"```gramaire\";\n+      if ($255) {\n         return {\n           blocks: acc.blocks,\n           inside: true,\n@@ -9212,7 +9212,7 @@ var isUpperName = function(name) {\n       return false;\n     }\n     ;\n-    throw new Error(\"Failed pattern match at Gramaire.Lr (line 313, column 8 - line 315, column 23): \" + [v.constructor.name]);\n+    throw new Error(\"Failed pattern match at Gramaire.Lr (line 312, column 8 - line 314, column 23): \" + [v.constructor.name]);\n   })());\n };\n var isTokenDef = function(l) {\n@@ -9226,7 +9226,7 @@ var isTokenDef = function(l) {\n       return isUpperName(trim(take3(v.value0)(l)));\n     }\n     ;\n-    throw new Error(\"Failed pattern match at Gramaire.Lr (line 305, column 8 - line 307, column 46): \" + [v.constructor.name]);\n+    throw new Error(\"Failed pattern match at Gramaire.Lr (line 304, column 8 - line 306, column 46): \" + [v.constructor.name]);\n   })());\n };\n var isSettingDecl = function(l) {\n@@ -9242,8 +9242,8 @@ var gramaireBlocks = function(md) {\n   var step = function(acc) {\n     return function(line) {\n       if (acc.inside) {\n-        var $258 = trim(line) === \"```\";\n-        if ($258) {\n+        var $261 = trim(line) === \"```\";\n+        if ($261) {\n           return {\n             info: acc.info,\n             cur: acc.cur,\n@@ -9299,20 +9299,56 @@ var precedenceOf = function(md) {\n     return emptyPrec;\n   }\n   ;\n-  throw new Error(\"Failed pattern match at Gramaire.Lr (line 382, column 19 - line 384, column 23): \" + [v.constructor.name]);\n+  throw new Error(\"Failed pattern match at Gramaire.Lr (line 383, column 19 - line 385, column 23): \" + [v.constructor.name]);\n };\n-var decomment = /* @__PURE__ */ (function() {\n-  var isComment = function(line) {\n-    var t = trim(line);\n-    return t === \"..\" || isJust(stripPrefix(\".. \")(t));\n+var decomment = function(ls) {\n+  var step = function(acc) {\n+    return function(line) {\n+      var t = trim(line);\n+      if (acc.inBlock) {\n+        var $267 = contains(\"*/\")(t);\n+        if ($267) {\n+          return {\n+            out: acc.out,\n+            inBlock: false\n+          };\n+        }\n+        ;\n+        return acc;\n+      }\n+      ;\n+      var $268 = isJust(stripPrefix(\"//\")(t));\n+      if ($268) {\n+        return acc;\n+      }\n+      ;\n+      var $269 = isJust(stripPrefix(\"/*\")(t));\n+      if ($269) {\n+        var $270 = contains(\"*/\")(t);\n+        if ($270) {\n+          return acc;\n+        }\n+        ;\n+        return {\n+          out: acc.out,\n+          inBlock: true\n+        };\n+      }\n+      ;\n+      return {\n+        inBlock: acc.inBlock,\n+        out: cons(line)(acc.out)\n+      };\n+    };\n   };\n-  return filter(function($276) {\n-    return !isComment($276);\n-  });\n-})();\n+  return reverse(foldl9(step)({\n+    inBlock: false,\n+    out: []\n+  })(ls).out);\n+};\n var toFenced = function(src) {\n-  var $263 = contains(\"```gramaire\")(src);\n-  if ($263) {\n+  var $271 = contains(\"```gramaire\")(src);\n+  if ($271) {\n     return src;\n   }\n   ;\n@@ -9325,8 +9361,8 @@ var toFenced = function(src) {\n   var block = function(info) {\n     return function(body) {\n       var trimmed = trimBlankEnds(body);\n-      var $264 = $$null(trimmed);\n-      if ($264) {\n+      var $272 = $$null(trimmed);\n+      if ($272) {\n         return [];\n       }\n       ;\n@@ -9339,8 +9375,8 @@ var parseWith = function(method) {\n   return function(md) {\n     var src = joinWith(\"\\n\")(lrBlocks(toFenced(md))) + \"\\n\";\n     var raw = scan(lrScanItems)(src);\n-    var $265 = hasError(raw);\n-    if ($265) {\n+    var $273 = hasError(raw);\n+    if ($273) {\n       return new Left(\"lexical error in grammar source\");\n     }\n     ;\n@@ -9363,10 +9399,10 @@ var parseWith = function(method) {\n         return new Left(\"parse did not yield a Grammar\");\n       }\n       ;\n-      throw new Error(\"Failed pattern match at Gramaire.Lr (line 368, column 22 - line 371, column 56): \" + [v1.constructor.name]);\n+      throw new Error(\"Failed pattern match at Gramaire.Lr (line 369, column 22 - line 372, column 56): \" + [v1.constructor.name]);\n     }\n     ;\n-    throw new Error(\"Failed pattern match at Gramaire.Lr (line 366, column 10 - line 371, column 56): \" + [v.constructor.name]);\n+    throw new Error(\"Failed pattern match at Gramaire.Lr (line 367, column 10 - line 372, column 56): \" + [v.constructor.name]);\n   };\n };\n var parse = /* @__PURE__ */ (function() {","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Fgenerated%2Fgramaire-engine.mjs","sha":"4b4b7a0d7b7986f1fc17ad3447e8d6d2b3b11544","status":"modified"},{"additions":8,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Flib%2Fdiagrams.ts","changes":10,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flib%2Fdiagrams.ts?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":2,"filename":"site/src/lib/diagrams.ts","patch":"@@ -31,8 +31,14 @@ function ruleBlocks(\n       .split(\"\\n\")\n       .filter((line) => {\n         const t = line.trim();\n-        if (t === \"\" || t === \"..\" || t.startsWith(\".. \")) return false; // `.. ` comment\n-        if (/^%(left|right|nonassoc|lang)\\b/.test(t)) return false;\n+        if (\n+          t === \"\" ||\n+          t.startsWith(\"//\") ||\n+          t.startsWith(\"/*\") ||\n+          t.startsWith(\"*\")\n+        )\n+          return false;\n+        if (/^%(left|right|nonassoc)\\b/.test(t)) return false;\n         if (/^[A-Z][A-Z0-9_]*\\s*:/.test(line)) return false; // token-class def\n         return true;\n       })","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Flib%2Fdiagrams.ts","sha":"489dd73e5c8cfed7ac505358559ee83222b0812f","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Flib%2Fgramaire-runtime.ts","changes":32,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flib%2Fgramaire-runtime.ts?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":16,"filename":"site/src/lib/gramaire-runtime.ts","patch":"@@ -69,39 +69,39 @@ function formatReport(result: {\n }\n \n // The lab's sample grammar — a live arithmetic calculator in the raw, fence-free\n-// `.gram` projection (ADR D36): `.. ` comment lines (simplified reStructuredText)\n-// carry the docs, ALL-CAPS `NAME : /regex/` lines declare the token classes, and\n-// Mixed-case productions carry inline `{% … %}` actions. The `%lang javascript`\n-// setting (the `## General settings` block in the fenced form) declares those\n-// actions as JavaScript, so the engine bakes them into one `evaluate(cst)` the Lab\n-// runs in its sandbox. Each action gets its children as a namedtuple `c` — here\n-// read positionally (`c[0]`, `c[2]`); a `name:` field would also allow `c.left`.\n-// The same engine reads this as it reads a fenced `.gram.md` — `toFenced` re-fences\n+// `.gram` projection (ADR D36): a `/** */` banner and `//` comments carry the\n+// docs, ALL-CAPS `NAME : /regex/` lines declare the token classes, and Mixed-case\n+// productions carry inline `{% … %}` actions. The `%lang javascript` setting\n+// (the `## General settings` block in the fenced form) declares those actions as\n+// JavaScript, so the engine bakes them into one `evaluate(cst)` the Lab runs in\n+// its sandbox. Each action gets its children as a namedtuple `c` — here read\n+// positionally (`c[0]`, `c[2]`); a `name:` field would also allow `c.left`. The\n+// same engine reads this as it reads a fenced `.gram.md` — `toFenced` re-fences\n // it and `decomment` skips the comments — so the preview lexes NUMBER natively.\n-const DEFAULT_GRAMAR = `.. Calc-js\n-..\n-.. A small arithmetic calculator. Inline actions carry the meaning; \\`*\\` and \\`/\\`\n-.. bind tighter than \\`+\\` and \\`-\\` because the grammar is stratified into\n-.. Expr / Term / Factor. Press \"Evaluate\" to run the baked evaluator.\n+const DEFAULT_GRAMAR = `/**\n+ * Calc-js\n+ *\n+ * An arithmetic calculator that evaluates its own input — a demonstration of inline \\`{% … %}\\` actions.\n+ */\n \n %lang javascript\n \n NUMBER : /[0-9]+(?:\\\\.[0-9]+)?/\n WS     : /[ \\\\t\\\\r\\\\n]+/   %skip\n \n-.. An expression is a sum or difference of terms.\n+// An expression is a sum or difference of terms.\n Expr\n   : Expr '+' Term   {% (c) => c[0] + c[2] %}\n   | Expr '-' Term   {% (c) => c[0] - c[2] %}\n   | Term\n \n-.. A term is a product or quotient of factors.\n+// A term is a product or quotient of factors.\n Term\n   : Term '*' Factor {% (c) => c[0] * c[2] %}\n   | Term '/' Factor {% (c) => c[0] / c[2] %}\n   | Factor\n \n-.. A factor is a number or a parenthesised expression.\n+// A factor is a number or a parenthesised expression.\n Factor\n   : '(' Expr ')'    {% (c) => c[1] %}\n   | NUMBER          {% (c) => parseFloat(c[0]) %}","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/site%2Fsrc%2Flib%2Fgramaire-runtime.ts","sha":"1c4e658e27b9ad42db4a9995cd010c669af1d1ef","status":"modified"},{"additions":17,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/src%2FGramaire%2FLr.purs","changes":33,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/src%2FGramaire%2FLr.purs?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":16,"filename":"src/Gramaire/Lr.purs","patch":"@@ -195,24 +195,23 @@ strip md =\n   ls = split (Pattern \"\\n\") md\n   { preamble, sections } = sectionize ls\n \n-  -- The leading `# Title` + intro paragraph → `.. ` comment lines (simplified\n-  -- reStructuredText). Internal blank lines are kept (as a bare `..`) so the\n-  -- title stays set off from the description; only diagram images and the blank\n-  -- ends are dropped.\n+  -- The leading `# Title` + intro paragraph → a `/** … */` banner comment.\n+  -- Internal blank lines are kept (as ` *`) so the title stays set off from the\n+  -- description; only diagram images and the blank ends are dropped.\n   banner :: Array String -> String\n   banner pre =\n     let\n       body = trimBlankEnds (map unHead (Array.filter notImage pre))\n     in\n       if Array.null body then \"\"\n-      else joinWith \"\\n\" (map dotdot body)\n+      else \"/**\\n\" <> joinWith \"\\n\" (map star body) <> \"\\n */\"\n     where\n     notImage l = not (isJust (stripPrefix (Pattern \"![\") (trim l)))\n     unHead l = fromMaybe l (stripPrefix (Pattern \"# \") l)\n-    dotdot l = if trim l == \"\" then \"..\" else \".. \" <> l\n+    star l = if trim l == \"\" then \" *\" else \" * \" <> l\n \n   -- A `## ` section survives only if it carries a keepable gramaire block; its\n-  -- prose becomes `.. ` comments, its block becomes fence-free content.\n+  -- prose becomes `//` comments, its block becomes fence-free content.\n   section :: Array String -> Maybe String\n   section sec =\n     if Array.any keepableOpen sec then\n@@ -237,7 +236,7 @@ strip md =\n             Just _ -> acc { keep = Just false } -- some other fence: skip its body\n             Nothing\n               | not (keepProse line) -> acc -- diagram image / blank: dropped\n-              | otherwise -> acc { out = Array.cons (\".. \" <> line) acc.out }\n+              | otherwise -> acc { out = Array.cons (\"// \" <> line) acc.out }\n \n   keepableOpen line = case stripPrefix (Pattern \"```gramaire\") (trim line) of\n     Just rest -> keepInfo (trim rest)\n@@ -329,20 +328,22 @@ isPrecDecl l =\n isSettingDecl :: String -> Boolean\n isSettingDecl l = isJust (stripPrefix (Pattern \"%lang \") (trim l))\n \n--- | Drop `..` comment lines (simplified reStructuredText — the prose `strip`\n+-- | Drop `//` line comments and `/* … */` block comments (the prose `strip`\n -- | writes into a `.gram`), so the grammar lexer never sees them. Whole-line\n--- | only: a comment is a line whose first non-blank content is `..` (`.. text`\n--- | or a bare `..`), which never collides with a production (rules start with a\n--- | Mixed-case head, `:`, or `|`; the `.` wildcard only appears mid-line). A\n--- | recognized directive such as `.. note::` is just a comment and is dropped.\n+-- | only: a `//` mid-line (e.g. inside a `{% … %}` action) is left alone.\n decomment :: Array String -> Array String\n-decomment = Array.filter (not <<< isComment)\n+decomment ls = Array.reverse (foldl step { inBlock: false, out: [] } ls).out\n   where\n-  isComment line =\n+  step acc line =\n     let\n       t = trim line\n     in\n-      t == \"..\" || isJust (stripPrefix (Pattern \".. \") t)\n+      if acc.inBlock then\n+        if contains (Pattern \"*/\") t then acc { inBlock = false } else acc\n+      else if isJust (stripPrefix (Pattern \"//\") t) then acc\n+      else if isJust (stripPrefix (Pattern \"/*\") t) then\n+        (if contains (Pattern \"*/\") t then acc else acc { inBlock = true })\n+      else acc { out = Array.cons line acc.out }\n \n -- | The production lexer for `lr` grammar source: the scanner built from the\n -- | notation's own `## Tokens` block (`lrTokensSource`), with `:` and `|` as","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/src%2FGramaire%2FLr.purs","sha":"d5f4fa98913a16618d54f107f138ed9f4bfefae9","status":"modified"},{"additions":12,"blob_url":"https://github.com/wstein/gramaire/blob/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/test%2FTest%2FStrip.purs","changes":24,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/test%2FTest%2FStrip.purs?ref=f900945bc8bac2074d8ac8a44cb9e3579b2d7071","deletions":12,"filename":"test/Test/Strip.purs","patch":"@@ -1,10 +1,9 @@\n -- | The raw `.gram` projection (ADR D36): `strip` carries a `.gram.md`'s docs as\n--- | `.. ` comment lines (simplified reStructuredText) — a banner for the\n--- | title/intro and one per section — alongside the fence-free grammar. It is a\n--- | derived, non-authoritative export; the safety property is that it carries\n--- | exactly the grammar the parser sees: `parse (strip md) == parse md` for every\n--- | grammar (the comments are skipped on parse), so `.gram` can never be a second\n--- | source of truth.\n+-- | comments — a `/** */` banner for the title/intro and `//` lines per section —\n+-- | alongside the fence-free grammar. It is a derived, non-authoritative export;\n+-- | the safety property is that it carries exactly the grammar the parser sees:\n+-- | `parse (strip md) == parse md` for every grammar (the comments are skipped on\n+-- | parse), so `.gram` can never be a second source of truth.\n module Test.Strip (tests) where\n \n import Prelude\n@@ -29,21 +28,22 @@ tests = do\n     ]\n     check\n \n-  log \"  strip: `..` comments are skipped on parse\"\n+  log \"  strip: // and /* … */ comments are skipped on parse\"\n   let\n-    withComments = \".. a comment line\\n.. note:: an annotation\\nS\\n  : 'x'\\n\"\n+    withComments = \"// a line comment\\n/* a\\n   block comment */\\nS\\n  : 'x'\\n\"\n     without = \"S\\n  : 'x'\\n\"\n-  assert' \"`..` comments are transparent to the parser\"\n+  assert' \"both comment styles are transparent to the parser\"\n     (parse withComments == parse without && isRight (parse without))\n \n check :: String -> Effect Unit\n check path = do\n   log (\"  strip: parse(strip(x)) == parse(x) for \" <> path)\n   md <- readTextFile UTF8 path\n   let stripped = strip md\n-  -- Docs travel as `.. ` comment lines (a banner for the title/intro, one per\n-  -- section). Headings are dropped (no bare `## `).\n-  assert' (path <> \": carries a `.. ` banner/prose\") (contains (Pattern \".. \") stripped)\n+  -- Docs travel as comments: a `/** */` banner for the title/intro and `//`\n+  -- lines per section. Headings are dropped (no bare `## `).\n+  assert' (path <> \": carries a /** */ banner\") (contains (Pattern \"/**\") stripped)\n+  assert' (path <> \": section prose survives as // comments\") (contains (Pattern \"// \") stripped)\n   assert' (path <> \": no bare ## headings\") (not (contains (Pattern \"\\n## \") stripped))\n   -- … and the grammar still parses to the same value (toFenced skips comments).\n   assert' (path <> \": stripped form should still parse\") (isRight (parse stripped))","raw_url":"https://github.com/wstein/gramaire/raw/f900945bc8bac2074d8ac8a44cb9e3579b2d7071/test%2FTest%2FStrip.purs","sha":"a187c254f0c76e6f5155f78dfe550e4183a55f17","status":"modified"}]}