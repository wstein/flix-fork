{"files":[{"additions":10,"blob_url":"https://github.com/wstein/rian-lab/blob/a87da66217e256d225846c982c74f9f653a405a0/lib%2Frian%2Fbeam.ex","changes":10,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fbeam.ex?ref=a87da66217e256d225846c982c74f9f653a405a0","deletions":0,"filename":"lib/rian/beam.ex","patch":"@@ -276,6 +276,12 @@ defmodule Rian.Beam do\n   @rian_sig \"pub def load_program(src String) Vec(Symbol)\"\n   @spec load_program(String.t()) :: [module()]\n   def load_program(src) do\n+    # link the portable prelude so a `List.sum`/`Dict.get`/… call — which the emitter\n+    # redirects to its `Rian.Prelude.*` module — resolves at runtime (mirrors the\n+    # single-module `load/2`; idempotent). A top-level `def` calling the prelude\n+    # alongside a `mod` needs it now that those top-level funcs are compiled.\n+    :ok = Rian.Prelude.load()\n+\n     src\n     |> compile_program()\n     |> Enum.map(fn {atom, bin} ->\n@@ -379,6 +385,10 @@ defmodule Rian.Beam do\n   @rian_sig \"pub def load_program_ir(prog Prog) Vec(Symbol)\"\n   @spec load_program_ir(map()) :: [module()]\n   def load_program_ir(prog) do\n+    # see `load_program/1`: link the portable prelude so redirected `List.*`/`Dict.*`\n+    # calls resolve at runtime (idempotent).\n+    :ok = Rian.Prelude.load()\n+\n     prog\n     |> compile_program_ir()\n     |> Enum.map(fn {atom, bin} ->","raw_url":"https://github.com/wstein/rian-lab/raw/a87da66217e256d225846c982c74f9f653a405a0/lib%2Frian%2Fbeam.ex","sha":"ed16837536feff4c587b96d1b82e540a7dc589c3","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/a87da66217e256d225846c982c74f9f653a405a0/test%2Frian%2Fbeam_test.exs","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fbeam_test.exs?ref=a87da66217e256d225846c982c74f9f653a405a0","deletions":2,"filename":"test/rian/beam_test.exs","patch":"@@ -596,10 +596,12 @@ defmodule Rian.BeamTest do\n       # regression: `compile_program` mapped over `mods` only, so a top-level `def`\n       # alongside a `mod` was silently dropped — `Rian.Run` then found no entry. The\n       # top-level funcs now compile into `RianCompiled`; the qualified `Lib.twice`\n-      # call resolves to the `Elixir.Lib` module.\n+      # call resolves to the `Elixir.Lib` module, and `List.sum` (a top-level call to\n+      # the portable prelude) resolves because `load_program/1` now links the prelude.\n       mods =\n         Beam.load_program(\n-          \"mod Lib do\\n  pub def twice(n Int53) Int53 := n * 2\\nend\\n\\npub def main() Int53 := Lib.twice(21)\"\n+          \"mod Lib do\\n  pub def twice(n Int53) Int53 := n * 2\\nend\\n\\n\" <>\n+            \"pub def main() Int53 := Lib.twice(List.sum([10, 11]))\"\n         )\n \n       assert Lib in mods","raw_url":"https://github.com/wstein/rian-lab/raw/a87da66217e256d225846c982c74f9f653a405a0/test%2Frian%2Fbeam_test.exs","sha":"1090a522eed492164de838af504c018f33a0f235","status":"modified"}]}