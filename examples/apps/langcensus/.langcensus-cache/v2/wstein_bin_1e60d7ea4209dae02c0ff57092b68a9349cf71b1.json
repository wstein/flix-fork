{"files":[{"additions":126,"blob_url":"https://github.com/wstein/bin/blob/1e60d7ea4209dae02c0ff57092b68a9349cf71b1/histlog","changes":328,"contents_url":"https://api.github.com/repos/wstein/bin/contents/histlog?ref=1e60d7ea4209dae02c0ff57092b68a9349cf71b1","deletions":202,"filename":"histlog","patch":"@@ -1101,7 +1101,7 @@ def query_interactive(args)\n     no_args: false,\n     arg_count: nil,\n     limit: nil,\n-    json: false\n+    format: nil\n   }\n \n   OptionParser.new do |opts|\n@@ -1116,7 +1116,7 @@ def query_interactive(args)\n   opts.on(\"--desc\", \"Sort results in descending order (newest first)\") { options[:desc] = true }\n \n     # Search and basic filters\n-    opts.on(\"--session [ID]\", \"Commands from current/most recent or specific session\") do |id|\n+    opts.on(\"--session [ID]\", \"Commands from current session (requires __histlog_session_id) or specific session ID\") do |id|\n       options[:session] = true\n       options[:session_id] = id.to_i if id\n     end\n@@ -1152,7 +1152,7 @@ def query_interactive(args)\n     # Frequency filters\n     opts.on(\"--rare\", \"Commands used < 3 times\") { options[:rare] = true }\n     opts.on(\"--frequent\", \"Commands used > 10 times\") { options[:frequent] = true }\n-    opts.on(\"--unique\", \"Commands used exactly once\") { options[:unique] = true }\n+    opts.on(\"--unique\", \"Remove duplicate commands (show only first occurrence)\") { options[:unique] = true }\n \n     # Context filters\n     opts.on(\"--shell SHELL\", \"Commands from specific shell\") { |shell| options[:shell] = shell }\n@@ -1173,9 +1173,12 @@ def query_interactive(args)\n \n     # Environment filters\n     opts.on(\"--assisted\", \"Only show AI-assisted commands\") { options[:assisted] = true }\n-    opts.on(\"--no-assisted\", \"Exclude AI-assisted commands\") { options[:no_assisted] = true }    # Output format\n-    opts.on(\"--json\", \"Output as JSON with full metadata\") { options[:json] = true }\n-  opts.on(\"--time\", \"Show human-readable start_time with each command in interactive output\") { options[:show_time] = true }\n+    opts.on(\"--no-assisted\", \"Exclude AI-assisted commands\") { options[:no_assisted] = true }\n+\n+    # Output format\n+    opts.on(\"--json\", \"Output as JSON with full metadata\") { options[:format] = 'json' }\n+    opts.on(\"--format FORMAT\", \"Output format (json, fish, zsh, nu, bash, powershell, plain)\") { |f| options[:format] = f.downcase }\n+    opts.on(\"--time\", \"Show human-readable start_time with each command in interactive output\") { options[:show_time] = true }\n     opts.on(\"--limit N\", Integer, \"Limit number of results (default: no limit)\") { |n| options[:limit] = n }\n \n     opts.on(\"-h\", \"--help\", \"Show this help\") do\n@@ -1259,15 +1262,8 @@ def execute_interactive_query(options)\n             where_conditions << \"s.id = ?\"\n             params << current_session_id.to_i\n         else\n-          # Fallback: use the most recent session\n-            recent_session_result = db.execute(\"SELECT id FROM sessions ORDER BY created_at DESC LIMIT 1\")\n-            if recent_session_result && recent_session_result[0]\n-              where_conditions << \"s.id = ?\"\n-              params << recent_session_result[0][0]\n-          else\n-            # If no sessions exist, this will return no results\n-            where_conditions << \"s.id IS NULL\"\n-          end\n+          # If no current session ID, return no results\n+          where_conditions << \"s.id IS NULL\"\n         end\n       end\n     end\n@@ -1414,7 +1410,7 @@ def execute_interactive_query(options)\n     end\n \n     # Frequency filters require subqueries\n-    if options[:rare] || options[:frequent] || options[:unique]\n+    if options[:rare] || options[:frequent]\n       frequency_subquery = <<-SQL\n   (SELECT COUNT(*) FROM commands c2 WHERE c2.cmd_text_id = c.cmd_text_id)\n       SQL\n@@ -1423,11 +1419,14 @@ def execute_interactive_query(options)\n         where_conditions << \"#{frequency_subquery} < 3\"\n       elsif options[:frequent]\n         where_conditions << \"#{frequency_subquery} > 10\"\n-      elsif options[:unique]\n-        where_conditions << \"#{frequency_subquery} = 1\"\n       end\n     end\n \n+    # Unique filter - show only first occurrence of each command (deduplication)\n+    if options[:unique]\n+      where_conditions << \"c.id = (SELECT MIN(c2.id) FROM commands c2 WHERE c2.cmd_text_id = c.cmd_text_id)\"\n+    end\n+\n   # Combine query parts\n     full_query = base_query\n     if where_conditions.any?\n@@ -1437,13 +1436,19 @@ def execute_interactive_query(options)\n   # Add GROUP BY to handle potential duplicates from JOINs, but preserve individual command executions\n   full_query += \" GROUP BY c.id\"\n \n-  # Order by creation time, user-controlled\n-    if options[:desc]\n-      order = \"DESC\"\n-\telse\n-\t  order = \"ASC\"\n-\tend\n-\tfull_query += \" ORDER BY c.created_at #{order}\"\n+  # Order by creation time - when limit is used, get most recent N first, then reverse for display\n+    if options[:limit]\n+      # For limited results, get the most recent N commands first\n+      full_query += \" ORDER BY c.created_at DESC\"\n+    else\n+      # For unlimited results, respect user's sort preference (default: most recent first)\n+      if options[:asc]\n+        order = \"ASC\"\n+      else\n+        order = \"DESC\"\n+      end\n+      full_query += \" ORDER BY c.created_at #{order}\"\n+    end\n \n     # Add limit if specified\n     if options[:limit]\n@@ -1504,9 +1509,14 @@ def execute_interactive_query(options)\n       return\n     end\n \n+    # When using limit, reverse results to show oldest-to-newest order for display\n+    if options[:limit]\n+      results.reverse!\n+    end\n+\n     # Output results\n-    if options[:json]\n-      output_json_results(results)\n+    if options[:format]\n+      output_formatted_results(results, options)\n     else\n       output_interactive_results(results, options)\n     end\n@@ -1518,6 +1528,33 @@ def execute_interactive_query(options)\n   end\n end\n \n+# Output results in various formats\n+def output_formatted_results(results, options)\n+  format = options[:format]\n+\n+  # Validate format\n+  valid_formats = ['json', 'fish', 'zsh', 'nu', 'bash', 'powershell', 'plain']\n+  unless valid_formats.include?(format)\n+    STDERR.puts \"Error: Unsupported format '#{format}'\"\n+    STDERR.puts \"Supported formats: #{valid_formats.join(', ')}\"\n+    exit 1\n+  end\n+\n+  if results.empty?\n+    puts \"No commands found matching the criteria.\"\n+    return\n+  end\n+\n+  case format\n+  when 'json'\n+    output_json_results(results)\n+  when 'plain'\n+    output_plain_results(results, options)\n+  else\n+    output_shell_history_results(results, format, options)\n+  end\n+end\n+\n # Output results in JSON format\n def output_json_results(results)\n   # Get database connection to fetch path arguments\n@@ -1574,6 +1611,47 @@ def output_json_results(results)\n   end\n end\n \n+# Output results in plain format with timestamps\n+def output_plain_results(results, options)\n+  results.each do |row|\n+    command = row['cmd_text']\n+    if row['start_time']\n+      timestamp = Time.at(row['start_time'].to_f).strftime('%Y-%m-%d %H:%M:%S')\n+      puts \"[#{timestamp}] #{command}\"\n+    else\n+      puts command\n+    end\n+  end\n+end\n+\n+# Output results in shell history formats\n+def output_shell_history_results(results, format, options)\n+  # Convert query results to the format expected by format_commands_for_shell\n+  # We need to reverse for shell formats to get chronological order (oldest first)\n+  formatted_results = results.reverse.map do |row|\n+    result = {\n+      'command' => row['cmd_text'],\n+      'start_time' => row['start_time']\n+    }\n+\n+    # Add command_id for fish format to get paths\n+    if format == 'fish'\n+      result['command_id'] = row['command_id']\n+    end\n+\n+    result\n+  end\n+\n+  # Format results using the shared formatting function\n+  format_options = {\n+    format: format,\n+    include_timestamps: true\n+  }\n+\n+  formatted_commands = format_commands_for_shell(formatted_results, format, format_options)\n+  formatted_commands.each { |line| puts line }\n+end\n+\n # Output results for display\n def output_interactive_results(results, options)\n   if results.empty?\n@@ -2074,175 +2152,6 @@ def import_shell_history(args)\n   puts \"Successfully imported #{imported_count} commands\"\n end\n \n-# Export shell history function\n-def export_shell_history(args)\n-  # Parse command line options\n-  options = {\n-    output_file: nil,\n-    format: nil,\n-    unique: false,\n-    dry_run: false,\n-    verbose: false,\n-    limit: nil,\n-    include_timestamps: true\n-  }\n-\n-  OptionParser.new do |opts|\n-    opts.banner = \"Usage: histlog export [format] [options] [output_file]\"\n-\n-    opts.on(\"-f\", \"--file FILE\", \"Output file path (defaults to stdout)\") do |file|\n-      options[:output_file] = file\n-    end\n-\n-    opts.on(\"-u\", \"--unique\", \"Export only unique commands (remove duplicates)\") do\n-      options[:unique] = true\n-    end\n-\n-    opts.on(\"-n\", \"--dry-run\", \"Show what would be exported without writing\") do\n-      options[:dry_run] = true\n-    end\n-\n-    opts.on(\"-v\", \"--verbose\", \"Show detailed export progress\") do\n-      options[:verbose] = true\n-    end\n-\n-    opts.on(\"-l\", \"--limit COUNT\", Integer, \"Limit number of exported commands\") do |limit|\n-      options[:limit] = limit\n-    end\n-\n-    opts.on(\"--no-timestamps\", \"Don't include timestamps (plain commands only)\") do\n-      options[:include_timestamps] = false\n-    end\n-\n-    opts.on(\"-h\", \"--help\", \"Show this help\") do\n-      puts opts\n-      exit 0\n-    end\n-  end.parse!(args)\n-\n-  # Get format and output file from positional arguments\n-  if args.length > 0 && !args[0].start_with?('-')\n-    options[:format] = args.shift.downcase\n-  end\n-\n-  if args.length > 0 && !args[0].start_with?('-')\n-    options[:output_file] ||= args.shift\n-  end\n-\n-  # Determine format - priority: explicit format > auto-detect from filename > default\n-  if options[:format].nil?\n-    if options[:output_file] && options[:output_file] != '-'\n-      options[:format] = detect_format_from_filename(options[:output_file])\n-    else\n-      options[:format] = 'plain'  # Default for stdout\n-    end\n-  end\n-\n-  # Validate format\n-  unless ['fish', 'zsh', 'nu', 'nushell', 'plain'].include?(options[:format])\n-    STDERR.puts \"Error: Unsupported format '#{options[:format]}'\"\n-    STDERR.puts \"Supported formats: fish, zsh, nu, plain\"\n-    exit 1\n-  end\n-\n-  # Normalize nushell format name\n-  options[:format] = 'nu' if options[:format] == 'nushell'\n-\n-  db_path = get_db_path\n-  unless File.exist?(db_path)\n-    STDERR.puts \"No histlog.db found in current directory\"\n-    STDERR.puts \"Use 'histlog init' to set up shell integration and start logging commands\"\n-    return\n-  end\n-\n-  db = SQLite3::Database.new(db_path)\n-  db.results_as_hash = true\n-\n-  # Build query to get commands with paths\n-  if options[:format] == 'fish'\n-    # For fish format, we need to get paths as well\n-    query = \"SELECT ct.command, c.start_time, c.id as command_id\"\n-    query += \" FROM commands c\"\n-    query += \" JOIN cmd_texts ct ON c.cmd_text_id = ct.id\"\n-    query += \" WHERE c.cmd_text_id IS NOT NULL\"\n-  else\n-    # For other formats, simple query without paths\n-    query = \"SELECT ct.command, c.start_time\"\n-    query += \" FROM commands c\"\n-    query += \" JOIN cmd_texts ct ON c.cmd_text_id = ct.id\"\n-    query += \" WHERE c.cmd_text_id IS NOT NULL\"\n-  end\n-\n-  if options[:unique]\n-    query += \" GROUP BY ct.command\"\n-  end\n-\n-  query += \" ORDER BY c.start_time DESC\"\n-\n-  if options[:limit]\n-    query += \" LIMIT #{options[:limit]}\"\n-  end\n-\n-  puts \"Exporting commands in #{options[:format]} format...\" if options[:verbose]\n-\n-  begin\n-    rows = db.execute(query)\n-  rescue SQLite3::SQLException => e\n-    STDERR.puts \"Database error: #{e.message}\"\n-    exit 1\n-  end\n-\n-  if rows.empty?\n-    puts \"No commands found to export\"\n-    return\n-  end\n-\n-  puts \"Found #{rows.length} commands to export\" if options[:verbose]\n-\n-  # All shell formats (fish, zsh, nu) use chronological order (oldest first)\n-  # We query DESC to get the most recent N commands, then reverse to chronological order\n-  if ['fish', 'zsh', 'nu'].include?(options[:format])\n-    rows = rows.reverse\n-  end\n-\n-  # Format commands according to target shell format\n-  formatted_commands = format_commands_for_shell(rows, options[:format], options)\n-\n-  if options[:dry_run]\n-    puts \"\\nSample export output (first 10 entries):\"\n-    formatted_commands.first(10).each_with_index do |line, i|\n-      puts \"  #{i+1}. #{line}\"\n-    end\n-    puts \"...\"\n-    puts \"\\nTotal: #{formatted_commands.length} entries would be exported\"\n-    return\n-  end\n-\n-  # Write to file or stdout\n-  if options[:output_file] && options[:output_file] != '-'\n-    File.open(options[:output_file], 'w') do |file|\n-      formatted_commands.each { |line| file.puts line }\n-    end\n-    puts \"Exported #{formatted_commands.length} commands to #{options[:output_file]}\"\n-  else\n-    formatted_commands.each { |line| puts line }\n-  end\n-end\n-\n-# Detect output format from filename\n-def detect_format_from_filename(filename)\n-  case filename.downcase\n-  when /fish_history/, /\\.fish$/\n-    'fish'\n-  when /\\.zsh_history$/, /\\.zhistory$/, /\\.zsh$/\n-    'zsh'\n-  when /nushell/, /\\.nu$/, /history\\.txt$/\n-    'nu'\n-  else\n-    'plain'\n-  end\n-end\n-\n # Format commands for specific shell format\n def format_commands_for_shell(rows, format, options)\n   formatted = []\n@@ -2284,6 +2193,24 @@ def format_commands_for_shell(rows, format, options)\n       formatted << row['command']\n     end\n \n+  when 'bash'\n+    # Bash history format - simple commands, timestamps optional\n+    rows.each do |row|\n+      if options[:include_timestamps] && row['start_time']\n+        # Bash can use timestamps with HISTTIMEFORMAT, stored as comments\n+        formatted << \"##{row['start_time'].to_i}\"\n+        formatted << row['command']\n+      else\n+        formatted << row['command']\n+      end\n+    end\n+\n+  when 'powershell'\n+    # PowerShell history is typically just commands, one per line\n+    rows.each do |row|\n+      formatted << row['command']\n+    end\n+\n   when 'plain'\n     rows.each do |row|\n       if options[:include_timestamps] && row['start_time']\n@@ -2655,14 +2582,12 @@ def show_help(show_plumbing = false)\n   puts \"  paths           List all unique file or directory paths (--files, --dirs)\"\n   puts \"  sessions        Show session information\"\n   puts \"  import [FILE]   Import history from shell history files\"\n-  puts \"  export [FILE]   Export commands to shell history format (--unique)\"\n   puts \"  cleanup         Remove orphaned entries from database\"\n   puts \"  info            Show database and system information\"\n   puts \"  optimize        Optimize database performance\"\n   puts \"\"\n   puts \"Query Filters:\"\n-  puts \"  --session [ID]  Commands from current or specific session\"\n-  puts \"  --current        Commands from the current shell session (requires __histlog_session_id env)\"\n+  puts \"  --session [ID]  Commands from current session (requires __histlog_session_id) or specific session ID\"\n   puts \"  --dir [PATH]    Commands from current or specific directory\"\n   puts \"  --failed        Only failed commands (exit != 0)\"\n   puts \"  --success       Only successful commands (exit = 0)\"\n@@ -2674,6 +2599,7 @@ def show_help(show_plumbing = false)\n   puts \"  --slow          Slow commands (> 10s)\"\n   puts \"  --has-paths     Commands that accessed filesystem paths\"\n   puts \"  --json          Output as JSON\"\n+  puts \"  --format FORMAT Output in shell history format (json, fish, zsh, nu, bash, powershell, plain)\"\n   puts \"\"\n \n   if show_plumbing\n@@ -2796,7 +2722,7 @@ class HistlogApp\n     end\n \n     # Define known commands\n-  known_commands = ['query', 'stats', 'history', 'sessions', 'import', 'export', 'optimize', 'help', 'init', 'paths', 'cleanup', 'info', '_on_session', '_on_preexec', '_on_postcmd']\n+  known_commands = ['query', 'stats', 'history', 'sessions', 'import', 'optimize', 'help', 'init', 'paths', 'cleanup', 'info', '_on_session', '_on_preexec', '_on_postcmd']\n \n     if !known_commands.include?(@command) && !@command.start_with?('-')\n       STDERR.puts \"Error: Unknown command '#{@command}'\"\n@@ -2819,8 +2745,6 @@ class HistlogApp\n       query_sessions(@argv)\n     when 'import'\n       import_shell_history(@argv)\n-    when 'export'\n-      export_shell_history(@argv)\n     when 'optimize'\n       handle_optimize\n     when 'paths'","raw_url":"https://github.com/wstein/bin/raw/1e60d7ea4209dae02c0ff57092b68a9349cf71b1/histlog","sha":"5b8361d1faea0d1d33a7db6c3c9830068e0d5000","status":"modified"}]}