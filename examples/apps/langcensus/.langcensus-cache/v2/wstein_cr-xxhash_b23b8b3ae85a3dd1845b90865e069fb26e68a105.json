{"files":[{"additions":22,"blob_url":"https://github.com/wstein/cr-xxhash/blob/b23b8b3ae85a3dd1845b90865e069fb26e68a105/.github%2Fworkflows%2Fci-fast.yml","changes":36,"contents_url":"https://api.github.com/repos/wstein/cr-xxhash/contents/.github%2Fworkflows%2Fci-fast.yml?ref=b23b8b3ae85a3dd1845b90865e069fb26e68a105","deletions":14,"filename":".github/workflows/ci-fast.yml","patch":"@@ -123,24 +123,32 @@ jobs:\n           fetch-depth: 1\n           submodules: recursive\n \n-      - name: Install Crystal\n+      - name: Ensure Crystal is installed\n         shell: bash\n         run: |\n           set -euo pipefail\n-          echo \"Installing Crystal for \"/\"$(uname)\"\"\"\n-          if [ \"$(uname)\" = \"Darwin\" ]; then\n-            # macOS: use Homebrew (runner already has brew)\n-            brew update || true\n-            brew install crystal-lang || true\n-          elif [ \"$(uname)\" = \"Linux\" ]; then\n-            # Linux: installer script needs root\n-            curl -fsSL https://crystal-lang.org/install.sh | sudo bash\n-          elif [[ \"$(uname)\" == MINGW* || \"$(uname)\" == MSYS* ]]; then\n-            # Windows: use chocolatey (preinstalled on hosted runners)\n-            choco install -y crystal\n+          if command -v crystal >/dev/null 2>&1; then\n+            echo \"Crystal already available: $(crystal --version)\"\n           else\n-            # Fallback (unknown/unix-like)\n-            curl -fsSL https://crystal-lang.org/install.sh | bash\n+            echo \"Installing Crystal for \"/\"$(uname)\"\"\"\n+            if [ \"$(uname)\" = \"Darwin\" ]; then\n+              # macOS: use Homebrew (runner already has brew)\n+              brew update || true\n+              brew install crystal-lang || true\n+            elif [ \"$(uname)\" = \"Linux\" ]; then\n+              # Container image already provides crystal in most cases;\n+              # if not, fall back to installer but assume root access.\n+              if ! command -v curl >/dev/null 2>&1; then\n+                apt-get update && apt-get install -y curl\n+              fi\n+              curl -fsSL https://crystal-lang.org/install.sh | bash\n+            elif [[ \"$(uname)\" == MINGW* || \"$(uname)\" == MSYS* ]]; then\n+              # Windows: use chocolatey (preinstalled on hosted runners)\n+              choco install -y crystal\n+            else\n+              # Fallback (unknown/unix-like)\n+              curl -fsSL https://crystal-lang.org/install.sh | bash\n+            fi\n           fi\n           export PATH=\"$HOME/.local/bin:$PATH\"\n ","raw_url":"https://github.com/wstein/cr-xxhash/raw/b23b8b3ae85a3dd1845b90865e069fb26e68a105/.github%2Fworkflows%2Fci-fast.yml","sha":"03d9c01528e5b4ac6dfd4cf81dcbcde11a93882d","status":"modified"}]}