{"files":[{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/9dbcfa7178f637eb5671612ade0af70a4015426c/examples%2Frian%2Fselfhost_beam.rian","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_beam.rian?ref=9dbcfa7178f637eb5671612ade0af70a4015426c","deletions":0,"filename":"examples/rian/selfhost_beam.rian","patch":"@@ -29,6 +29,7 @@ mod SelfhostBeam do\n              | CRemote(String, String, Vec(Core))\n              | CDot(Core, String)\n              | CStruct(String, Vec(SFld))\n+             | CMap(Vec(SFld))\n              | CTuple(Vec(Core))\n              | CList(Vec(Core), Tail)\n              | CIf(Core, Block, Block)\n@@ -69,6 +70,7 @@ mod SelfhostBeam do\n              | FRemote(String, String, Vec(Form))\n              | FTuple(Vec(Form))\n              | FStruct(String, Vec(FFld))\n+             | FMap(Vec(FFld))\n              | FNil\n              | FCons(Form, Form)\n              | FIf(Form, Vec(Form), Vec(Form))\n@@ -182,6 +184,8 @@ mod SelfhostBeam do\n   # named struct construction `Name(f: v, …)` — a tagged map (the driver's inflater\n   # adds the `__struct__` field + snake tag, matching Rian.Beam.expr_form).\n   def expr_form(CStruct(name, fields)) := FStruct(name, fld_forms(fields))\n+  # map literal -> a plain Erlang map form (no `__struct__` tag); reuses fld_forms.\n+  def expr_form(CMap(fields)) := FMap(fld_forms(fields))\n   def expr_form(CList(es, TClose)) := list_form(expr_forms(es), FNil)\n   def expr_form(CList(es, TTail(t))) := list_form(expr_forms(es), expr_form(t))\n   def expr_form(CIf(c, t, e)) := FIf(expr_form(c), block_forms(t), block_forms(e))","raw_url":"https://github.com/wstein/rian-lab/raw/9dbcfa7178f637eb5671612ade0af70a4015426c/examples%2Frian%2Fselfhost_beam.rian","sha":"4d336a13abe945d4bfa3405bd6dab29ac4daf220","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/9dbcfa7178f637eb5671612ade0af70a4015426c/examples%2Frian%2Fselfhost_compose_real_sum.rian","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_compose_real_sum.rian?ref=9dbcfa7178f637eb5671612ade0af70a4015426c","deletions":0,"filename":"examples/rian/selfhost_compose_real_sum.rian","patch":"@@ -268,6 +268,9 @@ mod SelfhostComposeRealSum do\n   # (A call's args are all-labeled or none; matching the first label suffices.)\n   def lower_surface({:call, {:id, ctor}, [{:label, k, v} | more]}) := CStruct(ctor, lower_flds([{:label, k, v} | more]))\n   def lower_surface({:call, callee, args}) := CCall(callee_name(callee), lower_all(args))\n+  # map literal `%{k: v, …}` — `MapE` of `Label` fields -> a Core map; the fields\n+  # reuse the labeled-construction lowering (`lower_flds`), but no `__struct__` tag.\n+  def lower_surface({:map_e, flds}) := CMap(lower_flds(flds))\n   # selfhost_decl's `If(cond, then, else)` (single-expr branches) -> selfhost_beam's\n   # CIf with single-statement blocks; the backend lowers it to a `case` on the bool.\n   def lower_surface({:\"if\", c, t, e}) := CIf(lower_surface(c), Block([SExpr(lower_surface(t))]), Block([SExpr(lower_surface(e))]))\n@@ -412,6 +415,8 @@ mod SelfhostComposeRealSum do\n   # named struct construction -> a tagged map `%{__struct__: <snake>, field: v, …}`\n   # (Rian.Beam.expr_form): the `__struct__` field carries the snake-cased type name.\n   def inflate(FStruct(name, fields)) := {:map, 0, [{:map_field_assoc, 0, {:atom, 0, Prim.str_to_atom(\"__struct__\")}, {:atom, 0, to_snake(name)}} | inflate_flds(fields)]}\n+  # a map literal -> a plain Erlang map (no `__struct__` tag); fields reuse inflate_flds.\n+  def inflate(FMap(fields)) := {:map, 0, inflate_flds(fields)}\n   # `if` lowers (via selfhost_beam's FIf) to a `case` on the boolean condition.\n   def inflate(FIf(c, t, e)) := {:\"case\", 0, inflate(c), [{:clause, 0, [{:atom, 0, :true}], [], inflate_all(t)}, {:clause, 0, [{:atom, 0, :false}], [], inflate_all(e)}]}\n   # `case` -> a real Erlang `case`; each arm is an FClause (single pattern, guard, body).","raw_url":"https://github.com/wstein/rian-lab/raw/9dbcfa7178f637eb5671612ade0af70a4015426c/examples%2Frian%2Fselfhost_compose_real_sum.rian","sha":"22cd82847da4f599881296e272fcc1732f4cd0e9","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/rian-lab/blob/9dbcfa7178f637eb5671612ade0af70a4015426c/test%2Frian%2Fselfhost_build_test.exs","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fselfhost_build_test.exs?ref=9dbcfa7178f637eb5671612ade0af70a4015426c","deletions":2,"filename":"test/rian/selfhost_build_test.exs","patch":"@@ -13,7 +13,6 @@ defmodule Rian.SelfhostBuildTest do\n   # Known gaps (the backlog — programs `build` cannot yet compile, so they are not\n   # asserted here; add them as they land):\n   #   * lambdas / function captures — selfhost_beam emits no fun forms yet.\n-  #   * maps — no map-literal lowering in the driver / backend.\n \n   setup_all do\n     for {mod, file} <- [\n@@ -76,7 +75,13 @@ defmodule Rian.SelfhostBuildTest do\n      :f, [{:ok, 3}, {:ok, 4}], 7},\n     {\"with (multi-clause, 2nd fails -> else)\",\n      \"def f(p Tuple, q Tuple) Int53 := with {:ok, a} <- p, {:ok, b} <- q do a + b else _ -> 0 end\",\n-     :f, [{:ok, 3}, {:error, :x}], 0}\n+     :f, [{:ok, 3}, {:error, :x}], 0},\n+    # map literal (ADR-0063 #2): the parser keeps `%{…}` as a `MapE` node; the\n+    # driver lowers it to a Core map and the backend emits a plain Erlang map\n+    # (atom keys, no `__struct__` tag). Values may be literals or expressions.\n+    {\"map literal\", \"def m() Map := %{a: 1, b: 2}\", :m, [], %{a: 1, b: 2}},\n+    {\"map with expression values\", \"def wrap(n Int53) Map := %{ok: n, double: n * 2}\", :wrap, [7],\n+     %{ok: 7, double: 14}}\n   ]\n \n   describe \"the self-hosted Rian compiler compiles + runs real programs (no Elixir oracle)\" do","raw_url":"https://github.com/wstein/rian-lab/raw/9dbcfa7178f637eb5671612ade0af70a4015426c/test%2Frian%2Fselfhost_build_test.exs","sha":"86437ab874a0b36c41a5750ae41279a455bdd6b1","status":"modified"}]}