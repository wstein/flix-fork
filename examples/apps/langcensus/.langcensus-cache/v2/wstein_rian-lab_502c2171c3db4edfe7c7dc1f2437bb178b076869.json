{"files":[{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/502c2171c3db4edfe7c7dc1f2437bb178b076869/docs%2Fadr%2F0049-backend-target-roadmap.md","changes":8,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0049-backend-target-roadmap.md?ref=502c2171c3db4edfe7c7dc1f2437bb178b076869","deletions":3,"filename":"docs/adr/0049-backend-target-roadmap.md","patch":"@@ -95,9 +95,11 @@ the **`Str`/`Char` codepoint prims**, **`Symbol`/atoms** (→ a Kotlin `String`,\n functions** (`forall T` → `fun <T : Any>`), and **protocols** (a `dispatch: :dispatcher` → a\n `when (a0)` over `is <Type>`; bounded-generic consumers call it, ADR-0042), and **lambdas**\n (`(a) -> body` → a Kotlin lambda `{ a -> body }`, `Fn(arg…, ret)` → a function type `(arg…) -> ret`,\n-capturing natively — ADR-0061), and **captures** (`&(&1 * 2)` → `{ _1 -> … }`, `&name/arity` →\n-`::name`) now lower too. Still raising\n-`Rian.JVM.Unsupported` (the next increments): tuples, maps, structs, `with`,\n+capturing natively — ADR-0061), **captures** (`&(&1 * 2)` → `{ _1 -> … }`, `&name/arity` →\n+`::name`), and **tuples** (`{a, b}`/`{a, b, c}` → Kotlin `Pair`/`Triple`, type `(A, B)` →\n+`Pair<A, B>`, destructured via `componentN()`) now lower too. Still raising\n+`Rian.JVM.Unsupported` (the next increments): arity-≥4 tuples (use a struct), tagged tuples\n+(`{:ok, v}` — a Result, BEAM-only), maps, structs, `with`,\n general FFI, and a dispatcher returning an associated type (ADR-0074 — no concrete Kotlin return). Per ADR-0026\n parity, JVM CI stays **non-blocking** until promoted: CI installs `kotlinc` and runs the JVM\n execution tests in a dedicated `continue-on-error` lane (the blocking `mix test.all` gate keeps","raw_url":"https://github.com/wstein/rian-lab/raw/502c2171c3db4edfe7c7dc1f2437bb178b076869/docs%2Fadr%2F0049-backend-target-roadmap.md","sha":"38cbc0847e9fd24b6147991a7bbf35d01a1cef84","status":"modified"},{"additions":63,"blob_url":"https://github.com/wstein/rian-lab/blob/502c2171c3db4edfe7c7dc1f2437bb178b076869/lib%2Frian%2Fjvm.ex","changes":67,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fjvm.ex?ref=502c2171c3db4edfe7c7dc1f2437bb178b076869","deletions":4,"filename":"lib/rian/jvm.ex","patch":"@@ -57,9 +57,12 @@ defmodule Rian.JVM do\n   lowers to a Kotlin lambda `{ a -> body }` and a `Fn(arg…, ret)` type to a Kotlin\n   function type `(arg…) -> ret`, capturing the environment natively (ADR-0061).\n   A **capture** `&(&1 * 2)` lowers to a Kotlin lambda over generated args\n-  (`{ _1 -> … }`) and `&name/arity` to a Kotlin function reference `::name`.\n-  **Not yet** (raise `Rian.JVM.Unsupported`): tuples, maps, structs, `with`,\n-  general FFI; and an associated type in a *non*-covariant position (a bare `Elem`\n+  (`{ _1 -> … }`) and `&name/arity` to a Kotlin function reference `::name`. A\n+  **tuple** `{a, b}` / `{a, b, c}` lowers to a Kotlin `Pair`/`Triple` (type\n+  `(A, B)` → `Pair<A, B>`), destructured in a pattern via `componentN()`.\n+  **Not yet** (raise `Rian.JVM.Unsupported`): arity-≥4 tuples (use a struct),\n+  tagged tuples (`{:ok, v}` — a Result, BEAM-only), maps, structs, `with`, general\n+  FFI; and an associated type in a *non*-covariant position (a bare `Elem`\n   return / an `Elem` parameter), which stays off `:jvm`. A **value union** `A | B` (ADR-0083) erases to\n   `Any` — a member value *is-a* `Any`, so construction needs no wrapping — and a\n   type-pattern `n Int53 ->` narrows it back with `is Long`/`is String` (Kotlin\n@@ -95,12 +98,14 @@ defmodule Rian.JVM do\n     EList,\n     ENum,\n     EStr,\n+    ETuple,\n     EUnary,\n     PAtom,\n     PChar,\n     PCtor,\n     PList,\n     PLit,\n+    PTuple,\n     PTyped,\n     PVar,\n     PWild\n@@ -119,7 +124,6 @@ defmodule Rian.JVM do\n   # `Core.reject_unsupported!` runs the shared walk; this map is the JVM-specific set.\n   @jvm_unsupported %{\n     Core.EWith => \"a `with` expression\",\n-    Core.ETuple => \"a tuple\",\n     Core.EMap => \"a map\",\n     Core.EMapUpdate => \"a map update\",\n     Core.EBitstr => \"a bitstring (BEAM-only, ADR-0078)\",\n@@ -615,6 +619,24 @@ defmodule Rian.JVM do\n     {size_test ++ elem_tests ++ tail_tests, elem_binds ++ tail_binds}\n   end\n \n+  # a tuple pattern destructures a `Pair`/`Triple` via `componentN()` — no shape\n+  # test (the static type guarantees arity). Tagged/≥4 tuples are unsupported, as in\n+  # construction.\n+  defp pat_match(%PTuple{elems: [%PAtom{} | _]}, _acc),\n+    do: raise(Unsupported, \"jvm: a tagged tuple pattern (`{:tag, …}`) is BEAM-only\")\n+\n+  defp pat_match(%PTuple{elems: ps}, acc) when length(ps) in [2, 3] do\n+    ps\n+    |> Enum.with_index(1)\n+    |> Enum.reduce({[], []}, fn {p, i}, {ts, bs} ->\n+      {t, b} = pat_match(p, \"(#{acc}).component#{i}()\")\n+      {ts ++ t, bs ++ b}\n+    end)\n+  end\n+\n+  defp pat_match(%PTuple{elems: ps}, _acc),\n+    do: raise(Unsupported, \"jvm: a #{length(ps)}-tuple pattern (Pair/Triple cover 2/3)\")\n+\n   defp pat_match(other, _acc), do: raise(Unsupported, \"jvm: clause pattern #{inspect(other)}\")\n \n   defp bind_str([]), do: \"\"\n@@ -895,6 +917,25 @@ defmodule Rian.JVM do\n     \"run rcase@{\\n\" <> Enum.join(decl ++ arm_lines ++ tail, \"\\n\") <> \"\\n}\"\n   end\n \n+  # a tuple `{a, b}` / `{a, b, c}` → a Kotlin `Pair`/`Triple` (the idiomatic 2-/3-\n+  # tuple, destructured via `componentN`). A tagged tuple `{:ok, v}` is a Result/AST\n+  # node (BEAM-only on :jvm, ADR-0040); arity ≥4 has no Kotlin form yet.\n+  defp expr_kt(%ETuple{elems: [%EAtom{} | _]}),\n+    do:\n+      raise(Unsupported, \"a tagged tuple (`{:tag, …}` — a Result/AST node) is BEAM-only on :jvm\")\n+\n+  defp expr_kt(%ETuple{elems: [a, b]}), do: \"Pair(#{expr_kt(a)}, #{expr_kt(b)})\"\n+\n+  defp expr_kt(%ETuple{elems: [a, b, c]}),\n+    do: \"Triple(#{expr_kt(a)}, #{expr_kt(b)}, #{expr_kt(c)})\"\n+\n+  defp expr_kt(%ETuple{elems: es}),\n+    do:\n+      raise(\n+        Unsupported,\n+        \"a #{length(es)}-tuple has no Kotlin form (Pair/Triple cover 2/3; use a struct)\"\n+      )\n+\n   # a type-directed cast inserted by `coerce_casts` (ADR-0074): bridge an erased\n   # dispatcher's `List<Any>` to the concrete `List<T>` a callee expects.\n   defp expr_kt({:jvm_cast, inner, t}), do: \"(#{expr_kt(inner)} as #{t})\"\n@@ -1036,6 +1077,24 @@ defmodule Rian.JVM do\n         {params, [ret]} = Rian.TypeStr.split_top_commas(Enum.at(m, 1)) |> Enum.split(-1)\n         \"(#{Enum.map_join(params, \", \", &kt_type/1)}) -> #{kt_type(ret)}\"\n \n+      # a tuple type `(A, B)` / `(A, B, C)` → a Kotlin `Pair`/`Triple` (the idiomatic\n+      # 2-/3-tuple). A tuple type starts with `(` — distinct from `Fn(`/`Vec(`/a\n+      # nominal type. Arity ≥4 has no Kotlin form yet (use a struct).\n+      m = Regex.run(~r/^\\((.+)\\)$/, t) ->\n+        case Rian.TypeStr.split_top_commas(Enum.at(m, 1)) do\n+          [a, b] ->\n+            \"Pair<#{kt_type(a)}, #{kt_type(b)}>\"\n+\n+          [a, b, c] ->\n+            \"Triple<#{kt_type(a)}, #{kt_type(b)}, #{kt_type(c)}>\"\n+\n+          parts ->\n+            raise(\n+              Unsupported,\n+              \"jvm: a #{length(parts)}-tuple type `#{t}` (Pair/Triple cover 2/3)\"\n+            )\n+        end\n+\n       # a value union `Union(A, B)` (ADR-0083) erases to `Any` — a member value\n       # *is-a* `Any` (no wrapping needed, unlike Rust); a type-pattern narrows it back.\n       String.starts_with?(t, \"Union(\") ->","raw_url":"https://github.com/wstein/rian-lab/raw/502c2171c3db4edfe7c7dc1f2437bb178b076869/lib%2Frian%2Fjvm.ex","sha":"591f28fb04efcdb4c36e309c66e5e27618efd4ff","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/502c2171c3db4edfe7c7dc1f2437bb178b076869/lib%2Frian%2Freach.ex","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Freach.ex?ref=502c2171c3db4edfe7c7dc1f2437bb178b076869","deletions":1,"filename":"lib/rian/reach.ex","patch":"@@ -34,7 +34,7 @@ defmodule Rian.Reach do\n   Reach models **architectural** reachability (what a target *can* run — `ref` off\n   the BEAM, `Int64` off JS, FFI off non-BEAM). It deliberately does NOT track an\n   emitter's **implementation status** (`with` not *yet* lowered on JS;\n-  tuples/maps not *yet* on the Tier-2 JVM) — those are portable by\n+  maps/structs/arity-≥4 tuples not *yet* on the Tier-2 JVM) — those are portable by\n   design (ADR-0041/0040/0049) and will land. That gap is reported by a per-emitter\n   capability pre-check (`Rian.JS`/`Rian.JVM` `reject_unsupported!`), which fails fast\n   with a clear \"not yet supported on :js/:jvm\" message — keeping this matrix honest","raw_url":"https://github.com/wstein/rian-lab/raw/502c2171c3db4edfe7c7dc1f2437bb178b076869/lib%2Frian%2Freach.ex","sha":"1ff88c86b4b940e8a9c534aee00cee4723c9e00c","status":"modified"},{"additions":49,"blob_url":"https://github.com/wstein/rian-lab/blob/502c2171c3db4edfe7c7dc1f2437bb178b076869/test%2Frian%2Fjvm_test.exs","changes":63,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fjvm_test.exs?ref=502c2171c3db4edfe7c7dc1f2437bb178b076869","deletions":14,"filename":"test/rian/jvm_test.exs","patch":"@@ -326,6 +326,19 @@ defmodule Rian.JVMTest do\n       def named(x Int64) Int64 := apply2(&neg/1, x)\n       \"\"\",\n       probe: ~s|println(\"${anon(5L)},${named(5L)}\")|\n+    },\n+    %{\n+      id: :tuple,\n+      src: \"\"\"\n+      def swap(a Int64, b Int64) (Int64, Int64) := {b, a}\n+      def fst_of(a Int64, b Int64) Int64 := case swap(a, b) do\n+        {x, y} -> x\n+      end\n+      def sum3(a Int64, b Int64, c Int64) Int64 := case {a, b, c} do\n+        {x, y, z} -> x + y + z\n+      end\n+      \"\"\",\n+      probe: ~s|println(\"${fst_of(1L, 2L)},${sum3(1L, 2L, 3L)}\")|\n     }\n   ]\n \n@@ -496,17 +509,22 @@ defmodule Rian.JVMTest do\n     end\n \n     test \"a not-yet-implemented construct fails early with a clear message (naming the fn)\" do\n-      # the emitter-capability pre-check: tuples aren't on the Tier-2 JVM subset\n-      # yet, so a tuple raises ONE clear error up front (naming `f`). The return is an\n-      # opaque nominal type (`Pair`) so the tuple body type-checks and the failure is\n-      # the JVM emitter's, not the return-type gate (a structural tuple vs `Int64` is now\n-      # a Check error — ADR-0050 tuple inference).\n+      # the emitter-capability pre-check: a `with` expression isn't on the Tier-2 JVM\n+      # subset yet, so it raises ONE clear error up front (naming `f`) rather than a\n+      # deep inspect-dump mid-emission (Reach stays architectural per ADR-0041).\n       err =\n         assert_raise JVM.Unsupported, fn ->\n-          JVM.compile(\"def f() Pair := {1, 2}\")\n+          JVM.compile(\"def f(x Int64) Int64 := with {:ok, v} <- g(x) do v end\")\n         end\n \n-      assert Exception.message(err) =~ \"`f`: a tuple is not yet supported on :jvm\"\n+      assert Exception.message(err) =~ \"`f`: a `with` expression is not yet supported on :jvm\"\n+    end\n+\n+    test \"an arity-≥4 tuple has no idiomatic Kotlin form and raises a clear error\" do\n+      # Pair/Triple cover 2/3; ≥4 should use a struct. The body type-checks against an\n+      # opaque nominal return, so the failure is the emitter's, not the return gate.\n+      err = assert_raise JVM.Unsupported, fn -> JVM.compile(\"def f() Quad := {1, 2, 3, 4}\") end\n+      assert Exception.message(err) =~ \"4-tuple has no Kotlin form\"\n     end\n \n     @tag :jvm\n@@ -568,6 +586,18 @@ defmodule Rian.JVMTest do\n       expect_jvm(jvm, :capture, \"10,-5\")\n     end\n \n+    @tag :jvm\n+    test \"tuples lower to Pair/Triple, destructured via componentN (ADR-0049 Tier 2)\", %{\n+      jvm_batch: jvm\n+    } do\n+      kt = jvm_kt(jvm, :tuple)\n+      assert kt =~ \"): Pair<Long, Long> {\"\n+      assert kt =~ \"Pair(b, a)\"\n+      assert kt =~ \"Triple(a, b, c)\"\n+      assert kt =~ \"(__s).component1()\"\n+      expect_jvm(jvm, :tuple, \"2,6\")\n+    end\n+\n     @tag :jvm\n     test \"`:=` shadowing renames to a backtick-quoted fresh `val` (no Kotlin redeclaration)\", %{\n       jvm_batch: jvm\n@@ -709,13 +739,18 @@ defmodule Rian.JVMTest do\n       expect_jvm(jvm, :list_head_lit, \"yes\\nno\")\n     end\n \n-    test \"a tuple clause pattern (outside the Tier-2 subset) raises\" do\n-      assert_raise JVM.Unsupported, fn ->\n-        JVM.compile(\"\"\"\n-        def fst(p Int64) Int64\n-        def fst({x, y}) := x\n-        \"\"\")\n-      end\n+    test \"an arity-≥4 tuple clause pattern (outside the Tier-2 subset) raises\" do\n+      # Pair/Triple cover 2-/3-tuple patterns (destructured via `componentN`); a\n+      # ≥4 tuple has no idiomatic Kotlin form and raises a clear error.\n+      err =\n+        assert_raise JVM.Unsupported, fn ->\n+          JVM.compile(\"\"\"\n+          def fst(p Int64) Int64\n+          def fst({a, b, c, d}) := a\n+          \"\"\")\n+        end\n+\n+      assert Exception.message(err) =~ \"4-tuple pattern\"\n     end\n   end\n ","raw_url":"https://github.com/wstein/rian-lab/raw/502c2171c3db4edfe7c7dc1f2437bb178b076869/test%2Frian%2Fjvm_test.exs","sha":"e2ddf004c837cc5fd3232617249c461fef03e929","status":"modified"}]}