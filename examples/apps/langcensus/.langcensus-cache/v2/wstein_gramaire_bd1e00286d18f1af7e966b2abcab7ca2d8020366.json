{"files":[{"additions":12,"blob_url":"https://github.com/wstein/gramaire/blob/bd1e00286d18f1af7e966b2abcab7ca2d8020366/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala?ref=bd1e00286d18f1af7e966b2abcab7ca2d8020366","deletions":1,"filename":"core/src/main/scala/gramaire/Lr.scala","patch":"@@ -246,9 +246,20 @@ object Lr:\n     def dropBlank(v: Vector[String]): Vector[String] = v.dropWhile(_.trim == \"\")\n     dropBlank(dropBlank(ls).reverse).reverse\n \n+  // Blank, an image link, or one of the three raw-HTML lines `fmt`'s source-collapsing wraps a\n+  // fence in (`<details>`, `<summary>...</summary>`, `</details>`) — none of these are prose, but\n+  // none of them open a FENCE either (they're plain text, not ` ``` `-delimited), so nothing else\n+  // here would otherwise skip them: a rule's leading prose extraction (`sectionLeadingProse`) and\n+  // `strip`'s prose-to-`///`-comment walk (`walk`) both call this, and both used to fold the\n+  // disclosure markup straight into the \"prose\" they kept when a collapsed rule's image sat before\n+  // its own fence — caught by `BackendGoldenSuite`'s Bison golden once collapsing became the\n+  // default `fmt` layout for every checked-in example, not by either round-trip-only test that was\n+  // already exercising `strip` on a collapsed file.\n   private def keepProse(line: String): Boolean =\n     val t = line.trim\n-    t != \"\" && !t.startsWith(\"![\")\n+    t != \"\" && !t.startsWith(\"![\") && t != \"<details>\" && t != \"</details>\" && !t.startsWith(\n+      \"<summary>\"\n+    )\n \n   private def keepableOpen(line: String): Boolean = line.trim == \"```gramaire\"\n ","raw_url":"https://github.com/wstein/gramaire/raw/bd1e00286d18f1af7e966b2abcab7ca2d8020366/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","sha":"87eb8ce2d5550007488cb4d18540211481555917","status":"modified"},{"additions":34,"blob_url":"https://github.com/wstein/gramaire/blob/bd1e00286d18f1af7e966b2abcab7ca2d8020366/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDocCommentsSuite.scala","changes":34,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDocCommentsSuite.scala?ref=bd1e00286d18f1af7e966b2abcab7ca2d8020366","deletions":0,"filename":"core/src/test/scala/gramaire/DocCommentsSuite.scala","patch":"@@ -38,6 +38,40 @@ class DocCommentsSuite extends munit.FunSuite:\n     assertEquals(Lr.docCommentsOf(bare), Map.empty[String, String])\n   }\n \n+  // Regression: `fmt`'s default source-collapsing puts a rule's image link, then\n+  // `<details><summary>Source</summary>`, before the fence itself — none of those three lines is\n+  // inside a markdown fence (they're raw HTML/an image link, not ` ``` `-delimited), so nothing\n+  // previously stopped `sectionLeadingProse` from folding them straight into the \"prose\" it kept.\n+  // Caught via `BackendGoldenSuite`'s Bison golden once every checked-in example started using\n+  // this layout by default, not by this suite — added here directly so the underlying extraction\n+  // bug has its own minimal, fast repro instead of relying on a backend-specific golden diff.\n+  test(\n+    \"docCommentsOf skips a collapsed rule's image link and <details>/<summary>/</details> lines, keeping only the real prose\"\n+  ) {\n+    val collapsed =\n+      \"\"\"## Expr\n+        |\n+        |This rule parses arithmetic expressions.\n+        |\n+        |![Railroad diagram for the Expr rule](diagrams-t/expr.svg)\n+        |\n+        |<details>\n+        |<summary>Source</summary>\n+        |\n+        |```gramaire\n+        |Expr\n+        |  : Expr '+' Term\n+        |  | Term\n+        |```\n+        |\n+        |</details>\n+        |\"\"\".stripMargin\n+    assertEquals(\n+      Lr.docCommentsOf(collapsed),\n+      Map(\"Expr\" -> \"This rule parses arithmetic expressions.\")\n+    )\n+  }\n+\n   test(\n     \"docCommentsOf still finds a rule's own name past a leading `#[attr]` tag (ADR D28), \" +\n       \"not just a bare rule name\"","raw_url":"https://github.com/wstein/gramaire/raw/bd1e00286d18f1af7e966b2abcab7ca2d8020366/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FDocCommentsSuite.scala","sha":"785e257868fdf55427695bdf8cdee247646f18f7","status":"modified"}]}