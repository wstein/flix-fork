{"files":[{"additions":77,"blob_url":"https://github.com/wstein/gramaire/blob/d35206a8b174170fe6e2a913dc55bdc5aee832f8/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","changes":138,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx?ref=d35206a8b174170fe6e2a913dc55bdc5aee832f8","deletions":61,"filename":"site/src/content/docs/tutorials/intro.mdx","patch":"@@ -16,10 +16,12 @@ Grammark's whole idea is in its name — _gram + mark_, grammar plus Markdown. A\n railroad diagrams, FIRST/FOLLOW tables — and is **at the same time** the exact\n input the parser generator reads. Your grammar _is_ your documentation.\n \n-This tutorial builds a small calculator grammar from scratch, then parses input\n-against it. The `grammark` CLI and the [Lab](/grammark/lab) run the **same**\n-PureScript core — the Lab is that core compiled to WebAssembly-class browser ESM\n-— so a verdict in the browser matches the CLI.\n+This tutorial builds a small calculator grammar in two passes: **Part 1** writes\n+a _recognizer_ that decides whether input is valid, and **Part 2** attaches\n+_actions_ that turn a valid parse into a value. The `grammark` CLI and the\n+[Lab](/grammark/lab) run the **same** PureScript core — the Lab is that core\n+compiled to WebAssembly-class browser ESM — so a verdict in the browser matches\n+the CLI.\n \n <Aside type=\"tip\" title=\"The Lab runs the real engine\">\n   The browser Lab is not a stub: it bundles the compiled Grammark core and lexes\n@@ -119,8 +121,8 @@ WS     : /[ \\t\\r\\n]+/   %skip\n ```\n ````\n \n-Now the grammar is complete — the Lab can lex `12 + 3` and the CLI can emit a\n-parser.\n+That is the whole recognizer's vocabulary. We will run it in §5; first, two\n+conveniences.\n \n ## 3. Surface sugar\n \n@@ -156,42 +158,7 @@ Block\n   : '{' Sep<Stmt, ';'> '}'\n ```\n \n-## 4. Attaching actions\n-\n-A recognizer only says yes or no. To build a value or an AST, put a host-language\n-action between `{%` and `%}`. The default action language is **PureScript** (the\n-core's own language):\n-\n-```grammark\n-Expr\n-  : Expr '+' Term   {% \\l _ r -> Add l r %}\n-  | Expr '-' Term   {% \\l _ r -> Sub l r %}\n-  | Term            {% \\t -> t %}\n-```\n-\n-Actions are **positional**: one parameter per right-hand symbol, left to right.\n-In `Expr '+' Term` the three parameters are the left value, the `+` token (which\n-we discard with `_`), and the right value.\n-\n-If you name your fields, you can skip the lambda entirely — a **bare body** binds\n-the field names for you:\n-\n-```grammark\n-Expr\n-  : left:Expr '+' right:Term   {% Add left right %}\n-```\n-\n-Grammark rewrites that to `\\left _ right -> Add left right`, so the action reads\n-in terms of your grammar, not positions.\n-\n-<Aside title=\"Actions are data, backends are consumers\">\n-  Actions are carried verbatim into the [IR](../../specs/ir-schema/); each\n-  backend reads only the IR. The TypeScript backend, for instance, emits a\n-  runnable parser and a typed Visitor over the CST without ever seeing your\n-  Markdown.\n-</Aside>\n-\n-## 5. Precedence and conflicts\n+## 4. Precedence and conflicts\n \n Recursive operators make a grammar ambiguous, which surfaces as shift/reduce\n conflicts. Declare operator precedence in a reserved section — earlier\n@@ -222,32 +189,81 @@ can tell you _what kind_ of conflict it is:\n grammark explain-conflict calc.gram.md\n ```\n \n-## 6. Run it\n+## 5. Run the recognizer\n \n-Two feedback loops:\n+That is a complete grammar — and as it stands it is a **recognizer**: it answers\n+exactly one question, _does this input fit?_ Paste it into the\n+[Lab](/grammark/lab) and try it (this calculator is the Lab's default):\n \n-<Steps>\n+- `12 + 3 * 4` → **accepted**\n+- `12 +` → **rejected** — a `Term` has to follow `+`\n \n-1. **At the CLI (full evaluation).** Check structure, formatting, and conflicts;\n-   then emit:\n+The Lab runs the real engine — the `NUMBER` token class lexes client-side — and\n+re-evaluates live as you type, so you can feel the grammar accept and reject\n+without leaving the page.\n \n-   ```sh\n-   # verify the file (structure, formatting, no conflicts)\n-   grammark fmt calc.gram.md\n+<Aside type=\"tip\" title=\"A recognizer is already useful\">\n+  Linters, validators, and syntax highlighters often need nothing more than\n+  yes/no. If that is all you want, you are done. The rest of this tutorial is\n+  **Part 2** — turning an accepted parse into a value.\n+</Aside>\n \n-   # emit artifacts from the same grammar\n-   grammark emit --backend ebnf calc.gram.md   # W3C-style EBNF\n-   grammark emit --backend ts   calc.gram.md   # a runnable TS parser + Visitor\n-   grammark emit --backend ir   calc.gram.md   # the grammark-ir JSON\n-   ```\n+## 6. From recognition to a value\n \n-2. **In the browser (preview).** Paste the same grammar into the\n-   [Lab](/grammark/lab) for an instant accept/reject. It runs the real engine, so\n-   the `NUMBER` token class lexes there too — type `12 + 3 * 4` and watch the\n-   verdict update live as you edit. (This calculator is the Lab's default\n-   grammar.)\n+A recognizer throws its work away. To keep it — to build a number, or an AST —\n+attach a host-language **action** between `{%` and `%}`. The default action\n+language is **PureScript** (the core's own language):\n \n-</Steps>\n+```grammark\n+Expr\n+  : Expr '+' Term   {% \\l _ r -> Add l r %}\n+  | Expr '-' Term   {% \\l _ r -> Sub l r %}\n+  | Term            {% \\t -> t %}\n+```\n+\n+Actions are **positional**: one parameter per right-hand symbol, left to right.\n+In `Expr '+' Term` the three parameters are the left value, the `+` token (which\n+we discard with `_`), and the right value.\n+\n+<Aside title=\"From CST to AST\">\n+  The parser first builds a **concrete** syntax tree — every token it matched,\n+  the `+` and the parentheses included. Your actions project that onto the\n+  **abstract** tree you actually want: `\\l _ r -> Add l r` keeps the two operands\n+  and drops the `+`; `'(' Expr ')'` returns the inner `Expr` and drops both\n+  brackets. Same recognized language, smaller tree.\n+</Aside>\n+\n+If you name your fields, you can skip the lambda entirely — a **bare body** binds\n+the field names for you:\n+\n+```grammark\n+Expr\n+  : left:Expr '+' right:Term   {% Add left right %}\n+```\n+\n+Grammark rewrites that to `\\left _ right -> Add left right`, so the action reads\n+in terms of your grammar, not positions.\n+\n+<Aside title=\"Actions are data, backends are consumers\">\n+  Actions are carried verbatim into the [IR](../../specs/ir-schema/); each\n+  backend reads only the IR. The TypeScript backend, for instance, emits a\n+  runnable parser and a typed Visitor over the CST without ever seeing your\n+  Markdown.\n+</Aside>\n+\n+## 7. Emit a parser\n+\n+At the CLI, check the file and emit artifacts from the same grammar:\n+\n+```sh\n+# verify the file (structure, formatting, no conflicts)\n+grammark fmt calc.gram.md\n+\n+# emit artifacts from the same grammar\n+grammark emit --backend ebnf calc.gram.md   # W3C-style EBNF\n+grammark emit --backend ts   calc.gram.md   # a runnable TS parser + Visitor\n+grammark emit --backend ir   calc.gram.md   # the grammark-ir JSON\n+```\n \n `grammark fmt` also writes the railroad diagrams and the FIRST/FOLLOW table back\n into the document, so the file you commit renders as complete documentation.","raw_url":"https://github.com/wstein/gramaire/raw/d35206a8b174170fe6e2a913dc55bdc5aee832f8/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","sha":"a5d7c12c8e7fc9e1a2176e80fed10b972955dad8","status":"modified"}]}