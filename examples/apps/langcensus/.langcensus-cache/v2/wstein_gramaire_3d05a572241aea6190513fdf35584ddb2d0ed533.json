{"files":[{"additions":15,"blob_url":"https://github.com/wstein/gramaire/blob/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Flib%2Fcst-view.ts","changes":22,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flib%2Fcst-view.ts?ref=3d05a572241aea6190513fdf35584ddb2d0ed533","deletions":7,"filename":"site/src/lib/cst-view.ts","patch":"@@ -38,24 +38,32 @@ export function renderCstHtml(\n   path = \"0\",\n ): string {\n   if (isLeaf(node)) {\n-    return `<div class=\"cst-leaf\"><span class=\"cst-term\">${esc(\n-      node.token,\n-    )}</span> <span class=\"cst-text\">${esc(JSON.stringify(node.text))}</span></div>`;\n+    return (\n+      `<div class=\"cst-leaf\" data-path=\"${path}\" role=\"treeitem\" tabindex=\"-1\">` +\n+      `<span class=\"cst-term\">${esc(node.token)}</span> ` +\n+      `<span class=\"cst-text\">${esc(JSON.stringify(node.text))}</span></div>`\n+    );\n   }\n   const name = nameOf(node, prodLhs);\n   const isCollapsed = collapsed.has(path);\n-  const glyph = node.children.length === 0 ? \"\" : isCollapsed ? \"▶\" : \"▼\";\n+  const hasKids = node.children.length > 0;\n+  const glyph = !hasKids ? \"\" : isCollapsed ? \"▶\" : \"▼\";\n+  // role=treeitem + aria-expanded so a screen reader announces the node and\n+  // its fold state; tabindex=-1 makes it programmatically focusable for the\n+  // roving keyboard nav the Lab wires on the enclosing role=tree.\n   const head =\n-    `<div class=\"cst-head\" data-path=\"${path}\">` +\n-    `<span class=\"cst-toggle\">${glyph}</span>` +\n+    `<div class=\"cst-head\" data-path=\"${path}\" role=\"treeitem\"` +\n+    (hasKids ? ` aria-expanded=\"${!isCollapsed}\"` : \"\") +\n+    ` tabindex=\"-1\">` +\n+    `<span class=\"cst-toggle\" aria-hidden=\"true\">${glyph}</span>` +\n     `<span class=\"cst-name\">${esc(name)}</span>` +\n     (isCollapsed\n       ? ` <span class=\"cst-count\">… ${node.children.length}</span>`\n       : \"\") +\n     `</div>`;\n   const kids = isCollapsed\n     ? \"\"\n-    : `<div class=\"cst-kids\">${node.children\n+    : `<div class=\"cst-kids\" role=\"group\">${node.children\n         .map((c, i) => renderCstHtml(c, prodLhs, collapsed, `${path}.${i}`))\n         .join(\"\")}</div>`;\n   return `<div class=\"cst-branch\">${head}${kids}</div>`;","raw_url":"https://github.com/wstein/gramaire/raw/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Flib%2Fcst-view.ts","sha":"40400c7d50e9e50bcd1a8ae2733013da81963178","status":"modified"},{"additions":91,"blob_url":"https://github.com/wstein/gramaire/blob/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Fpages%2Flab.astro","changes":101,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fpages%2Flab.astro?ref=3d05a572241aea6190513fdf35584ddb2d0ed533","deletions":10,"filename":"site/src/pages/lab.astro","patch":"@@ -450,6 +450,15 @@ const TABS = [\n         font-family: var(--font-mono);\n         font-size: 0.8rem;\n       }\n+      .cst-head:focus-visible,\n+      .cst-leaf:focus-visible {\n+        outline: 2px solid var(--accent);\n+        outline-offset: 1px;\n+        border-radius: 3px;\n+      }\n+      .cst-tree:focus {\n+        outline: none;\n+      }\n       .cst-term {\n         color: var(--t-term);\n       }\n@@ -523,6 +532,9 @@ const TABS = [\n         lrStep: 0,\n         result: null as Result | null,\n       };\n+      // The CST tree-path last focused, so keyboard nav survives the re-render\n+      // that an expand/collapse triggers.\n+      let treeFocusPath: string | null = null;\n \n       // Preload from an Open-in-Lab link (private hash or ?grammar= preset).\n       const link = readLabLink(window.location);\n@@ -664,7 +676,8 @@ const TABS = [\n             return `<p class=\"muted\">no tree (input rejected)</p>`;\n           const node = JSON.parse(r.cstJson) as Cst;\n           const tree = renderCstHtml(node, r.prodLhs, state.collapsed);\n-          return `<button type=\"button\" class=\"btn-sm\" id=\"copy-lisp\">copy LISP</button>${tree}`;\n+          return `<button type=\"button\" class=\"btn-sm\" id=\"copy-lisp\">copy LISP</button>\n+            <div class=\"cst-tree\" role=\"tree\" aria-label=\"Parse tree\">${tree}</div>`;\n         },\n         \"Parse trace\"(r) {\n           return r.trace\n@@ -769,15 +782,83 @@ const TABS = [\n         }\n \n         if (state.tab === \"Parse tree\") {\n-          bodyEl.querySelectorAll<HTMLElement>(\".cst-head\").forEach((h) =>\n-            h.addEventListener(\"click\", () => {\n-              const p = h.dataset.path!;\n-              state.collapsed.has(p)\n-                ? state.collapsed.delete(p)\n-                : state.collapsed.add(p);\n-              renderTab();\n-            }),\n-          );\n+          const tree = bodyEl.querySelector<HTMLElement>(\".cst-tree\");\n+          const items = () =>\n+            tree\n+              ? [...tree.querySelectorAll<HTMLElement>('[role=\"treeitem\"]')]\n+              : [];\n+          const setFocus = (el?: HTMLElement) => {\n+            if (!el) return;\n+            items().forEach((i) => (i.tabIndex = -1));\n+            el.tabIndex = 0;\n+            el.focus();\n+            treeFocusPath = el.dataset.path ?? null;\n+          };\n+          const toggle = (p: string) => {\n+            state.collapsed.has(p)\n+              ? state.collapsed.delete(p)\n+              : state.collapsed.add(p);\n+            treeFocusPath = p;\n+            renderTab(); // rebuilds the tab; focus is restored below on re-entry\n+          };\n+          // Restore the roving tabstop after a re-render (or seed the first\n+          // node as the single tabbable entry point).\n+          const list0 = items();\n+          const restore = list0.find((i) => i.dataset.path === treeFocusPath);\n+          if (restore) {\n+            list0.forEach((i) => (i.tabIndex = -1));\n+            restore.tabIndex = 0;\n+            if (tree?.contains(document.activeElement)) restore.focus();\n+          } else if (list0[0]) list0[0].tabIndex = 0;\n+\n+          bodyEl\n+            .querySelectorAll<HTMLElement>(\".cst-head\")\n+            .forEach((h) =>\n+              h.addEventListener(\"click\", () => toggle(h.dataset.path!)),\n+            );\n+\n+          // ARIA tree keyboard model: ↑/↓ move, →/← expand/collapse (or move),\n+          // Enter/Space toggle.\n+          tree?.addEventListener(\"keydown\", (e) => {\n+            const list = items();\n+            const cur = (\n+              document.activeElement as HTMLElement | null\n+            )?.closest<HTMLElement>('[role=\"treeitem\"]');\n+            let idx = cur ? list.indexOf(cur) : 0;\n+            if (idx < 0) idx = 0;\n+            const el = list[idx];\n+            const expanded = el?.getAttribute(\"aria-expanded\");\n+            switch (e.key) {\n+              case \"ArrowDown\":\n+                e.preventDefault();\n+                setFocus(list[Math.min(idx + 1, list.length - 1)]);\n+                break;\n+              case \"ArrowUp\":\n+                e.preventDefault();\n+                setFocus(list[Math.max(idx - 1, 0)]);\n+                break;\n+              case \"ArrowRight\":\n+                e.preventDefault();\n+                if (expanded === \"false\" && el?.dataset.path)\n+                  toggle(el.dataset.path);\n+                else setFocus(list[Math.min(idx + 1, list.length - 1)]);\n+                break;\n+              case \"ArrowLeft\":\n+                e.preventDefault();\n+                if (expanded === \"true\" && el?.dataset.path)\n+                  toggle(el.dataset.path);\n+                else setFocus(list[Math.max(idx - 1, 0)]);\n+                break;\n+              case \"Enter\":\n+              case \" \":\n+                if (expanded != null && el?.dataset.path) {\n+                  e.preventDefault();\n+                  toggle(el.dataset.path);\n+                }\n+                break;\n+            }\n+          });\n+\n           $(\"copy-lisp\")?.addEventListener(\"click\", (e) => {\n             const btn = e.currentTarget as HTMLButtonElement;\n             navigator.clipboard","raw_url":"https://github.com/wstein/gramaire/raw/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Fpages%2Flab.astro","sha":"d63a0b3e2f1b5e3179c426a9833b95ac05a70008","status":"modified"},{"additions":13,"blob_url":"https://github.com/wstein/gramaire/blob/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Fstyles%2Fcustom.css","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fstyles%2Fcustom.css?ref=3d05a572241aea6190513fdf35584ddb2d0ed533","deletions":0,"filename":"site/src/styles/custom.css","patch":"@@ -164,6 +164,19 @@ samp {\n   background: color-mix(in srgb, var(--accent) 26%, transparent);\n }\n \n+/* Respect a reduced-motion preference across the whole site — the theme-switch\n+   knob slide, the diagram flash/zoom, and any transition drop to instant. */\n+@media (prefers-reduced-motion: reduce) {\n+  *,\n+  *::before,\n+  *::after {\n+    transition-duration: 0.01ms !important;\n+    animation-duration: 0.01ms !important;\n+    animation-iteration-count: 1 !important;\n+    scroll-behavior: auto !important;\n+  }\n+}\n+\n .site-title {\n   font-weight: 700;\n   letter-spacing: -0.02em;","raw_url":"https://github.com/wstein/gramaire/raw/3d05a572241aea6190513fdf35584ddb2d0ed533/site%2Fsrc%2Fstyles%2Fcustom.css","sha":"df8adb76db61eefcd5e05fcc0e511330b7f5c0b3","status":"modified"}]}