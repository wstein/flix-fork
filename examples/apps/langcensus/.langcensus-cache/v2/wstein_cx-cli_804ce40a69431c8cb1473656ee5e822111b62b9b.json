{"files":[{"additions":4,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/.github%2Fworkflows%2Fpages-smoke.yml","changes":8,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/.github%2Fworkflows%2Fpages-smoke.yml?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":4,"filename":".github/workflows/pages-smoke.yml","patch":"@@ -15,10 +15,10 @@ on:\n       - \"scripts/assemble-pages-site.js\"\n       - \"scripts/check-pages-site.js\"\n       - \"tests/contracts/pagesPublish.contract.test.ts\"\n-      - \"tests/unit/buildAntoraSite.test.ts\"\n+      - \"tests/integration/buildAntoraSite.integration.test.ts\"\n       - \"tests/unit/syncAntoraDocs.test.ts\"\n       - \"tests/unit/assemblePagesSite.test.ts\"\n-      - \"tests/unit/checkPagesSite.test.ts\"\n+      - \"tests/integration/checkPagesSite.integration.test.ts\"\n   push:\n     branches: [main]\n     paths:\n@@ -34,10 +34,10 @@ on:\n       - \"scripts/assemble-pages-site.js\"\n       - \"scripts/check-pages-site.js\"\n       - \"tests/contracts/pagesPublish.contract.test.ts\"\n-      - \"tests/unit/buildAntoraSite.test.ts\"\n+      - \"tests/integration/buildAntoraSite.integration.test.ts\"\n       - \"tests/unit/syncAntoraDocs.test.ts\"\n       - \"tests/unit/assemblePagesSite.test.ts\"\n-      - \"tests/unit/checkPagesSite.test.ts\"\n+      - \"tests/integration/checkPagesSite.integration.test.ts\"\n   workflow_dispatch:\n \n env:","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/.github%2Fworkflows%2Fpages-smoke.yml","sha":"ebb19ae6c007a1bae3eabb47f7ab5bb0a9c8ace4","status":"modified"},{"additions":14,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/docs%2Fmodules%2FROOT%2Fpages%2Fmanual%2Foperator-manual.adoc","changes":14,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/docs%2Fmodules%2FROOT%2Fpages%2Fmanual%2Foperator-manual.adoc?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":0,"filename":"docs/modules/ROOT/pages/manual/operator-manual.adoc","patch":"@@ -58,6 +58,20 @@ Typical minimal paths:\n . `cx verify --against .`\n . `cx extract dist/bundle --to ./restore`\n \n+=== Extraction Safety\n+\n+When you extract bundle contents, treat `intact` and `copied` outputs as the ordinary safe path.\n+\n+If extraction reports `degraded`, stop and decide whether you need approximate human review or deterministic machine-safe recovery:\n+\n+* use `cx extract ... --allow-degraded` only for human inspection\n+* do not use degraded output as automation input\n+* rebuild the bundle instead of forcing degraded fallback when coordinates, hashes, or downstream edits matter\n+\n+Why this protects you: degraded extraction means the recovered packed content no longer matches the manifest contract, so line coordinates and file identity may no longer be trustworthy enough for safe automation.\n+\n+For the full failure model, recovery statuses, and detailed examples, see xref:page$repository/docs/extraction_safety.adoc[Extraction Safety].\n+\n === Repository Commands\n \n Use the repository-local `make` targets for day-to-day development:","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/docs%2Fmodules%2FROOT%2Fpages%2Fmanual%2Foperator-manual.adoc","sha":"e6e720ac4d512dc770820e69ac9cfdced9f6ee29","status":"modified"},{"additions":164,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/docs%2Fmodules%2FROOT%2Fpages%2Frepository%2Fdocs%2Fextraction_safety.adoc","changes":307,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/docs%2Fmodules%2FROOT%2Fpages%2Frepository%2Fdocs%2Fextraction_safety.adoc?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":143,"filename":"docs/modules/ROOT/pages/repository/docs/extraction_safety.adoc","patch":"@@ -1,147 +1,168 @@\n = Extraction Safety\n :page-layout: default\n \n+== Why This Document Exists\n \n-++++\n-<h2>Why This Document Exists</h2>\n-<p><code>cx</code> is a deterministic bundler, not an archiver.</p>\n-<p>For the broader document map and architecture context, see the <a href=\"../../start-here/docs-index/\">Documentation Index</a> and the <a href=\"../../architecture/implementation-reference/\">Implementation Reference</a>.</p>\n-<p>See the <a href=\"../../architecture/mental-model/\">Mental Model</a> for the canonical artifact lifecycle that extraction sits inside.</p>\n-<p>For packed text files, the bundle contract is the normalized packed representation emitted by the renderer, not the original source file bytes. Verification and extraction therefore work against the rendered bundle content, which is what downstream automation actually consumes.</p>\n-<p>Text extraction uses the manifest&#39;s <code>outputStartLine</code> and <code>outputEndLine</code> values as the primary locator for XML, Markdown, and plain sections. JSON sections use direct object lookup because the packed content is already stored as structured values in a single JSON object. A bundle that contains text sections without span metadata is rejected at bundle time.</p>\n-<p>Extraction safety sits downstream of bundle safety. The bundle-time dirty-state taxonomy determines whether a bundle may exist at all, while this document explains what happens after a valid bundle has already been written.</p>\n-<p>In practical terms:</p>\n-<ul>\n-<li><code>clean</code> and <code>safe_dirty</code> bundles are ordinary inputs to extraction.</li>\n-<li><code>unsafe_dirty</code> never reaches extraction because bundling aborts first.</li>\n-<li><code>forced_dirty</code> is a recorded audit state, not a relaxation of extraction rules.</li>\n-</ul>\n-<p>If a bundle was forced dirty, extraction still uses the manifest as written. The presence of <code>forced_dirty</code> does not weaken hash checking or span validation; it simply tells reviewers that the bundle came from an explicitly overridden working tree.</p>\n-<p>If your promotion pipeline forbids operator overrides, quarantine the bundle\n-before extraction by parsing the manifest <code>dirtyState</code> field in CI. The\n-quarantine logic belongs upstream of extraction, not inside the extractor.</p>\n-<h2>Status Meanings</h2>\n-<p><code>cx</code> uses four statuses during recovery:</p>\n-<table>\n-<thead>\n-<tr>\n-<th>Status</th>\n-<th>Meaning</th>\n-</tr>\n-</thead>\n-<tbody><tr>\n-<td><code>intact</code></td>\n-<td>Reconstructed content matches the packed-content hash in the manifest</td>\n-</tr>\n-<tr>\n-<td><code>copied</code></td>\n-<td>Asset is restored directly from stored bundle content</td>\n-</tr>\n-<tr>\n-<td><code>degraded</code></td>\n-<td>Content can be reconstructed, but the packed slice does not match the manifest hash</td>\n-</tr>\n-<tr>\n-<td><code>blocked</code></td>\n-<td>Deterministic reconstruction is not possible from the stored section output or its span metadata</td>\n-</tr>\n-</tbody></table>\n-<h2>The Default Safety Rule</h2>\n-<p>By default, <code>cx extract</code> writes:</p>\n-<ul>\n-<li><code>intact</code> text files</li>\n-<li><code>copied</code> assets</li>\n-</ul>\n-<p>It refuses to write <code>degraded</code> text files unless you explicitly pass:</p>\n-<pre><code class=\"language-bash\">cx extract dist/demo-bundle --to /tmp/restore --allow-degraded\n-</code></pre>\n-<p>Why this stops you: once the recovered packed content no longer matches the manifest hash, the extractor can no longer guarantee that line coordinates and file identity still mean what the rest of the bundle says they mean.</p>\n-<h2>What The Hash Means</h2>\n-<p>The manifest <code>sha256</code> for a packed text file is the hash of the normalized packed content stored in the section output.</p>\n-<p>That is deliberate:</p>\n-<ul>\n-<li><code>cx verify --against</code> re-renders the selected source files through the Repomix adapter and compares packed-content hashes</li>\n-<li><code>cx extract</code> uses output spans to recover the packed slice and checks it against the packed-content hash recorded in the manifest</li>\n-<li><code>cx bundle</code> records the same packed-content hash that later commands rely on</li>\n-</ul>\n-<p>If the bundle output changes, the hash changes. If the original source file bytes differ in a way that does not change the packed output, the bundle hash stays the same.</p>\n-<h2>Troubleshooting Hash Mismatches</h2>\n-<p>When extraction reports <code>manifest_hash_mismatch</code>, the error table shows a short checksum prefix for both the expected and actual content. Use that as the first diagnostic signal, then decide whether the file should be reviewed or the bundle should be rebuilt.</p>\n-<p>Practical checks:</p>\n-<ul>\n-<li>compare the expected and actual checksum prefixes in the error table</li>\n-<li>re-run <code>cx list --json</code> or <code>cx extract --json</code> if you need the full <code>expectedSha256</code> and <code>actualSha256</code> values</li>\n-<li>confirm whether the section output changed because of a Repomix rendering option, a span mismatch, or an actual content edit</li>\n-<li>rebuild the bundle if the packed content is supposed to be stable</li>\n-</ul>\n-<p>If the mismatch is intentional and approximate recovery is acceptable, use <code>--allow-degraded</code>. Otherwise, treat it as a bundle integrity failure and regenerate the bundle.</p>\n-<p>Why this stops you: a hash mismatch is not cosmetic drift. It is evidence that the recovered slice is no longer the same packed content the manifest promised to downstream tooling.</p>\n-<p>Operational extract failures now surface the same guidance in both human and\n-JSON output: a suggested command, a docs reference, and next steps under\n-<code>error.remediation</code>. Treat that payload as the machine-readable recovery path\n-for dashboards and CI summaries.</p>\n-<h2>What &quot;Degraded&quot; Usually Means</h2>\n-<p>A degraded file usually means the stored section output and the parsed recovery path no longer agree on the normalized packed content.</p>\n-<p>That can happen if:</p>\n-<ul>\n-<li>the section wrapper no longer matches the manifest span metadata</li>\n-<li>the stored output was edited after bundling</li>\n-<li>the packed slice itself differs from the manifest hash</li>\n-</ul>\n-<p>It should not be caused by ordinary source-byte differences that do not affect the packed output.</p>\n-<h2>Why <code>--allow-degraded</code> Is Dangerous</h2>\n-<blockquote>\n-<p>[!CAUTION]</p>\n-<h3>THE DISASTER SCENARIO</h3>\n-<p>Imagine your bundle is degraded by a single missing newline at the top of a file. Every line number in the manifest is now shifted by +1. </p>\n-<ol>\n-<li>You run an LLM agent that reads the degraded bundle.</li>\n-<li>The agent identifies a vulnerability it thinks is on line 42.</li>\n-<li>Because of the shift, line 42 in the agent&#39;s view is actually line 41 in the real file.</li>\n-<li>The agent issues a patch to &quot;overwrite line 42&quot;.</li>\n-<li><strong>Result:</strong> The agent blindly overwrites a critical database connection string on the real line 42 that it never even saw.</li>\n-</ol>\n-<p>Your production build breaks silently because the mechanical chain of causality was severed by a &quot;minor&quot; formatting difference.</p>\n-</blockquote>\n-<h3>The Mechanical Chain of Causality</h3>\n-<ol>\n-<li><strong>Byte Shift:</strong> A missing newline or encoding error shifts the character offsets of the entire file.</li>\n-<li><strong>Integer Drift:</strong> The manifest records the <code>outputStartLine</code> based on the <em>expected</em> rendering, but the <em>actual</em> recovered slice starts one line earlier/later.</li>\n-<li><strong>Hash Mismatch:</strong> <code>cx</code> detects that the recovered slice hash doesn&#39;t match the manifest and marks it <code>degraded</code>.</li>\n-<li><strong>Coordinate Failure:</strong> If you bypass this with <code>--allow-degraded</code>, you are handing the LLM a map where every coordinate is off by one.</li>\n-<li><strong>Silent Corruption:</strong> The LLM executes a tool (like <code>replace_repomix_span</code>) against these wrong coordinates, corrupting the file.</li>\n-</ol>\n-<p>In other words, <code>--allow-degraded</code> can turn one localized representation issue into a broader mapping error. Treat a <code>degraded</code> signal as a hard stop.</p>\n-<h2>Safe And Unsafe Use Cases</h2>\n-<p>Safe enough:</p>\n-<ul>\n-<li>a human wants to inspect the recovered file manually</li>\n-<li>an operator needs approximate content for review, not automation</li>\n-<li>the output is being treated as advisory, not authoritative</li>\n-</ul>\n-<p>Unsafe:</p>\n-<ul>\n-<li>an automated runner depends on manifest hashes</li>\n-<li>downstream tooling uses <code>outputStartLine</code> and <code>outputEndLine</code></li>\n-<li>another system slices code from the rendered bundle by absolute coordinates</li>\n-<li>the extracted file will be treated as a source-of-truth artifact</li>\n-</ul>\n-<h2>Recommended Operator Decision</h2>\n-<p>If extraction reports degraded text:</p>\n-<ol>\n-<li>stop the pipeline</li>\n-<li>inspect which files are degraded</li>\n-<li>decide whether you need human-readable recovery or deterministic machine-safe recovery</li>\n-<li>only use <code>--allow-degraded</code> if approximate output is acceptable</li>\n-</ol>\n-<p>If deterministic recovery matters, rebuild the bundle from the original source tree instead of forcing degraded fallback.</p>\n-<h2>Practical Examples</h2>\n-<p>Human review:</p>\n-<pre><code class=\"language-bash\">cx extract dist/demo-bundle --to /tmp/review --file docs/summary.md --allow-degraded\n-</code></pre>\n-<p>This is acceptable only if the file is being read by a person.</p>\n-<p>Automation:</p>\n-<pre><code class=\"language-bash\">cx extract dist/demo-bundle --to /tmp/runner --verify\n-</code></pre>\n-<p>If this reports degraded text, do not append <code>--allow-degraded</code> just to make the job pass. Rebuild or re-bundle from the trusted source instead.</p>\n-++++\n+`cx` is a deterministic bundler, not an archiver.\n+\n+For the broader document map and architecture context, see the xref:start-here/docs-index.adoc[Documentation Index] and the xref:architecture/implementation-reference.adoc[Implementation Reference].\n+See the xref:architecture/mental-model.adoc[Mental Model] for the canonical artifact lifecycle that extraction sits inside.\n+\n+For packed text files, the bundle contract is the normalized packed representation emitted by the renderer, not the original source file bytes. Verification and extraction therefore work against the rendered bundle content, which is what downstream automation actually consumes.\n+\n+Text extraction uses the manifest's `outputStartLine` and `outputEndLine` values as the primary locator for XML, Markdown, and plain sections. JSON sections use direct object lookup because the packed content is already stored as structured values in a single JSON object. A bundle that contains text sections without span metadata is rejected at bundle time.\n+\n+Extraction safety sits downstream of bundle safety. The bundle-time dirty-state taxonomy determines whether a bundle may exist at all, while this document explains what happens after a valid bundle has already been written.\n+\n+In practical terms:\n+\n+* `clean` and `safe_dirty` bundles are ordinary inputs to extraction.\n+* `unsafe_dirty` never reaches extraction because bundling aborts first.\n+* `forced_dirty` is a recorded audit state, not a relaxation of extraction rules.\n+\n+If a bundle was forced dirty, extraction still uses the manifest as written. The presence of `forced_dirty` does not weaken hash checking or span validation; it simply tells reviewers that the bundle came from an explicitly overridden working tree.\n+\n+If your promotion pipeline forbids operator overrides, quarantine the bundle before extraction by parsing the manifest `dirtyState` field in CI. The quarantine logic belongs upstream of extraction, not inside the extractor.\n+\n+== Status Meanings\n+\n+`cx` uses four statuses during recovery:\n+\n+[cols=\"1,3\",options=\"header\"]\n+|===\n+|Status |Meaning\n+|`intact` |Reconstructed content matches the packed-content hash in the manifest\n+|`copied` |Asset is restored directly from stored bundle content\n+|`degraded` |Content can be reconstructed, but the packed slice does not match the manifest hash\n+|`blocked` |Deterministic reconstruction is not possible from the stored section output or its span metadata\n+|===\n+\n+== The Default Safety Rule\n+\n+By default, `cx extract` writes:\n+\n+* `intact` text files\n+* `copied` assets\n+\n+It refuses to write `degraded` text files unless you explicitly pass:\n+\n+[source,bash]\n+----\n+cx extract dist/demo-bundle --to /tmp/restore --allow-degraded\n+----\n+\n+Why this stops you: once the recovered packed content no longer matches the manifest hash, the extractor can no longer guarantee that line coordinates and file identity still mean what the rest of the bundle says they mean.\n+\n+== What The Hash Means\n+\n+The manifest `sha256` for a packed text file is the hash of the normalized packed content stored in the section output.\n+\n+That is deliberate:\n+\n+* `cx verify --against` re-renders the selected source files through the reference oracle and compares packed-content hashes\n+* `cx extract` uses output spans to recover the packed slice and checks it against the packed-content hash recorded in the manifest\n+* `cx bundle` records the same packed-content hash that later commands rely on\n+\n+If the bundle output changes, the hash changes. If the original source file bytes differ in a way that does not change the packed output, the bundle hash stays the same.\n+\n+== Troubleshooting Hash Mismatches\n+\n+When extraction reports `manifest_hash_mismatch`, the error table shows a short checksum prefix for both the expected and actual content. Use that as the first diagnostic signal, then decide whether the file should be reviewed or the bundle should be rebuilt.\n+\n+Practical checks:\n+\n+* compare the expected and actual checksum prefixes in the error table\n+* re-run `cx list --json` or `cx extract --json` if you need the full `expectedSha256` and `actualSha256` values\n+* confirm whether the section output changed because of a rendering option, a span mismatch, or an actual content edit\n+* rebuild the bundle if the packed content is supposed to be stable\n+\n+If the mismatch is intentional and approximate recovery is acceptable, use `--allow-degraded`. Otherwise, treat it as a bundle integrity failure and regenerate the bundle.\n+\n+Why this stops you: a hash mismatch is not cosmetic drift. It is evidence that the recovered slice is no longer the same packed content the manifest promised to downstream tooling.\n+\n+Operational extract failures now surface the same guidance in both human and JSON output: a suggested command, a docs reference, and next steps under `error.remediation`. Treat that payload as the machine-readable recovery path for dashboards and CI summaries.\n+\n+== What \"Degraded\" Usually Means\n+\n+A degraded file usually means the stored section output and the parsed recovery path no longer agree on the normalized packed content.\n+\n+That can happen if:\n+\n+* the section wrapper no longer matches the manifest span metadata\n+* the stored output was edited after bundling\n+* the packed slice itself differs from the manifest hash\n+\n+It should not be caused by ordinary source-byte differences that do not affect the packed output.\n+\n+== Why `--allow-degraded` Is Dangerous\n+\n+[CAUTION]\n+====\n+THE DISASTER SCENARIO\n+\n+Imagine your bundle is degraded by a single missing newline at the top of a file. Every line number in the manifest is now shifted by `+1`.\n+\n+. You run an LLM agent that reads the degraded bundle.\n+. The agent identifies a vulnerability it thinks is on line 42.\n+. Because of the shift, line 42 in the agent's view is actually line 41 in the real file.\n+. The agent issues a patch to “overwrite line 42”.\n+. *Result:* the agent blindly overwrites a critical database connection string on the real line 42 that it never even saw.\n+\n+Your production build breaks silently because the mechanical chain of causality was severed by a “minor” formatting difference.\n+====\n+\n+=== The Mechanical Chain of Causality\n+\n+. *Byte shift:* a missing newline or encoding error shifts the character offsets of the entire file.\n+. *Integer drift:* the manifest records the `outputStartLine` based on the expected rendering, but the actual recovered slice starts one line earlier or later.\n+. *Hash mismatch:* `cx` detects that the recovered slice hash does not match the manifest and marks it `degraded`.\n+. *Coordinate failure:* if you bypass this with `--allow-degraded`, you are handing the LLM a map where every coordinate is off by one.\n+. *Silent corruption:* the LLM executes a tool such as `replace_repomix_span` against those wrong coordinates, corrupting the file.\n+\n+In other words, `--allow-degraded` can turn one localized representation issue into a broader mapping error. Treat a `degraded` signal as a hard stop.\n+\n+== Safe And Unsafe Use Cases\n+\n+Safe enough:\n+\n+* a human wants to inspect the recovered file manually\n+* an operator needs approximate content for review, not automation\n+* the output is being treated as advisory, not authoritative\n+\n+Unsafe:\n+\n+* an automated runner depends on manifest hashes\n+* downstream tooling uses `outputStartLine` and `outputEndLine`\n+* another system slices code from the rendered bundle by absolute coordinates\n+* the extracted file will be treated as a source-of-truth artifact\n+\n+== Recommended Operator Decision\n+\n+If extraction reports degraded text:\n+\n+. stop the pipeline\n+. inspect which files are degraded\n+. decide whether you need human-readable recovery or deterministic machine-safe recovery\n+. only use `--allow-degraded` if approximate output is acceptable\n+\n+If deterministic recovery matters, rebuild the bundle from the original source tree instead of forcing degraded fallback.\n+\n+== Practical Examples\n+\n+Human review:\n+\n+[source,bash]\n+----\n+cx extract dist/demo-bundle --to /tmp/review --file docs/summary.md --allow-degraded\n+----\n+\n+This is acceptable only if the file is being read by a person.\n+\n+Automation:\n+\n+[source,bash]\n+----\n+cx extract dist/demo-bundle --to /tmp/runner --verify\n+----\n+\n+If this reports degraded text, do not append `--allow-degraded` just to make the job pass. Rebuild or re-bundle from the trusted source instead.","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/docs%2Fmodules%2FROOT%2Fpages%2Frepository%2Fdocs%2Fextraction_safety.adoc","sha":"c3960aa97d6c091f52aac1f2f269e0c955facece","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fcontracts%2FdocsAssurance.contract.test.ts","changes":3,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/tests%2Fcontracts%2FdocsAssurance.contract.test.ts?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":0,"filename":"tests/contracts/docsAssurance.contract.test.ts","patch":"@@ -316,6 +316,9 @@ describe(\"docs assurance contract\", () => {\n     expect(manualNormalized).toContain(\n       \"Dirty-state gating stops tracked-file drift\",\n     );\n+    expect(manualNormalized).toContain(\n+      \"use `cx extract ... --allow-degraded` only for human inspection\",\n+    );\n     expect(extractionSafety).toContain(\n       \"Why this stops you: once the recovered packed content no longer matches\",\n     );","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fcontracts%2FdocsAssurance.contract.test.ts","sha":"7c7122e8df5ff043c90a924f71808951400c81fa","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fcontracts%2FrepositoryReferencePassthrough.contract.test.ts","changes":1,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/tests%2Fcontracts%2FrepositoryReferencePassthrough.contract.test.ts?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":1,"filename":"tests/contracts/repositoryReferencePassthrough.contract.test.ts","patch":"@@ -15,7 +15,6 @@ const REPOSITORY_DOCS_ROOT = path.join(\n );\n const ALLOWED_PASSTHROUGH_FILES = [\n   \"agent_integration.adoc\",\n-  \"extraction_safety.adoc\",\n   \"governance.adoc\",\n   \"init_template_contract.adoc\",\n   \"notes_module_spec.adoc\",","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fcontracts%2FrepositoryReferencePassthrough.contract.test.ts","sha":"5549e5690e7df40f38b93f629e51f3e3641787c8","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fintegration%2FbuildAntoraSite.integration.test.ts","changes":2,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/tests%2Fintegration%2FbuildAntoraSite.integration.test.ts?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":1,"filename":"tests/integration/buildAntoraSite.integration.test.ts","patch":"@@ -1,4 +1,4 @@\n-// test-lane: unit\n+// test-lane: integration\n \n import fs from \"node:fs/promises\";\n import os from \"node:os\";","previous_filename":"tests/unit/buildAntoraSite.test.ts","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fintegration%2FbuildAntoraSite.integration.test.ts","sha":"5a6d44cc43d134932ada3153ed9413c68aeaef12","status":"renamed"},{"additions":1,"blob_url":"https://github.com/wstein/cx-cli/blob/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fintegration%2FcheckPagesSite.integration.test.ts","changes":2,"contents_url":"https://api.github.com/repos/wstein/cx-cli/contents/tests%2Fintegration%2FcheckPagesSite.integration.test.ts?ref=804ce40a69431c8cb1473656ee5e822111b62b9b","deletions":1,"filename":"tests/integration/checkPagesSite.integration.test.ts","patch":"@@ -1,4 +1,4 @@\n-// test-lane: unit\n+// test-lane: integration\n \n import fs from \"node:fs/promises\";\n import os from \"node:os\";","previous_filename":"tests/unit/checkPagesSite.test.ts","raw_url":"https://github.com/wstein/cx-cli/raw/804ce40a69431c8cb1473656ee5e822111b62b9b/tests%2Fintegration%2FcheckPagesSite.integration.test.ts","sha":"7dbb6b2a02d34063627c2ed23fae615ab956c053","status":"renamed"}]}