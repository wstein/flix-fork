{"files":[{"additions":18,"blob_url":"https://github.com/wstein/rian-lab/blob/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/SELFHOST.md","changes":29,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be","deletions":11,"filename":"SELFHOST.md","patch":"@@ -325,17 +325,24 @@ over time. The proof is a four-stage ladder:\n   sources; the rest use heredoc doc-comments, and the lexer cannot yet lex *its own*\n   source (char escapes `'\\n'`) — the documented frontier.\n   See [`test/rian/selfhost_fixedpoint_test.exs`](test/rian/selfhost_fixedpoint_test.exs).\n-- **Stage 2 — front-end self-host** (future): the Rian front-end produces the *same\n-  Core IR* the Elixir front-end builds, then the existing backend compiles it — the\n-  first genuinely self-hosted compile. Gated on the parser→IR port + the portable\n-  stdlib breadth (ADR-0047).\n-- **Stage 3 — bootstrap fixed point** (future): the whole compiler in Rian; compile\n-  its source with the Elixir host → v1, compile with v1 → v2, assert **v1 == v2**\n-  (bit-identical `.beam`). The canonical terminus.\n-\n-**Next:** widen the parser slice (calls, comparisons, pipes, then patterns) toward\n-`Rian.Decl` coverage and Stage 2; port the `Enum`/`Map`/`String` stdlib breadth the\n-real front-end needs. Remaining lexer gaps (heredocs `\"\"\"`, string-body escapes,\n+- **Stage 2 — front-end self-host** (**partial — the expression front-end self-hosts**):\n+  the Rian-written parser (full binary precedence table + prefix `-`/`not` + function\n+  calls) produces an AST that **term-equals `Rian.Pratt.parse`**, and that output is\n+  consumed by the **real Elixir backend** (`Rian.Lower`/`Rian.Core`) to **actually run**\n+  — `(2 + 3) * 4` → `20`, `a + b * c` (a=2,b=3,c=4) → `14`, proven in\n+  [`parse_fixpoint_test.exs`](test/rian/parse_fixpoint_test.exs). This is \"Rian\n+  front-end + reused backend = working program\" for expressions. **Remaining:**\n+  declaration parsing (`Rian.Decl` → Core IR for `def`/`type`/`mod`) and the portable\n+  `Enum`/`Map`/`String` stdlib breadth (ADR-0047) — the gate on the *full* front-end.\n+- **Stage 3 — bootstrap fixed point** (future; **determinism prerequisite verified**):\n+  the whole compiler in Rian; compile its source with the Elixir host → v1, compile\n+  with v1 → v2, assert **v1 == v2** (bit-identical `.beam`). The canonical terminus.\n+  Its precondition already holds — `Rian.Beam` emits **byte-identical** bytecode on\n+  recompilation (the optimizer/parser/module sources round-trip identically).\n+\n+**Next:** the *full* Stage 2 — port `Rian.Decl`'s declaration parsing in Rian\n+(producing the Core IR, diffed against `Rian.Decl.parse`) and the stdlib breadth the\n+real front-end leans on. Remaining lexer gaps (heredocs `\"\"\"`, string-body escapes,\n `\\u{…}` char escapes — which block self-lexing) are small and unforced by Stage 2.\n \n ## The whole compiler as one Rian artifact","raw_url":"https://github.com/wstein/rian-lab/raw/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/SELFHOST.md","sha":"8b13a47563d909864825fc267916e59326ee61e7","status":"modified"},{"additions":5,"blob_url":"https://github.com/wstein/rian-lab/blob/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be","deletions":4,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -42,8 +42,8 @@ gating library work and blocks Stage 2 below.\n |---|---|---|---|\n | **0 — Equivalence** | each ported stage matches the reference on a corpus | fixpoint diff (lexer vs `tokenize/1`; parser vs `Pratt.parse`) | **done** (lexer slices 1-5; parser slice) |\n | **1 — Self-application** | a ported stage processes **real toolchain source** (incl. the self-hosting sources themselves) and agrees with the reference | run the Rian lexer over `selfhost_parse.rian` / preludes, diff vs `Rian.Lexer.tokenize/1` | **done for the lexer** (`test/rian/selfhost_fixedpoint_test.exs`) |\n-| **2 — Front-end self-host** | the Rian front-end produces the **same Core IR** the Elixir front-end builds; the existing backend compiles it | diff the Rian front-end's IR against `Rian.Decl.parse`/`Core` over a corpus, then `Rian.Beam` it | **future** (needs the parser→IR port + portable stdlib) |\n-| **3 — Bootstrap fixed point** | the **whole compiler**, written in Rian, compiles its own source; doing so twice is stable | compile the Rian compiler source with the Elixir-hosted compiler → v1; compile the *same source* with v1 → v2; assert **v1 == v2** (bit-identical `.beam`) | **future** (the canonical terminus) |\n+| **2 — Front-end self-host** | the Rian front-end produces the **same AST/IR** the Elixir front-end builds; the existing backend compiles it | parser output term-equals `Rian.Pratt.parse`; that output runs through `Rian.Lower`/`Rian.Core` to a value | **partial** — the **expression** front-end self-hosts: the Rian parser (full binary table + prefix + calls) feeds the real backend and **runs** (`parse_fixpoint_test.exs`); declaration parsing (`Rian.Decl` → Core IR) + the portable stdlib remain |\n+| **3 — Bootstrap fixed point** | the **whole compiler**, written in Rian, compiles its own source; doing so twice is stable | compile the Rian compiler source with the Elixir-hosted compiler → v1; compile the *same source* with v1 → v2; assert **v1 == v2** (bit-identical `.beam`) | **future** (the canonical terminus); its **determinism prerequisite is verified** — `Rian.Beam` emits byte-identical bytecode on recompilation (`selfhost_fixedpoint_test.exs`) |\n \n **\"Real self-hosting\" = Stage 2** (the front-end genuinely self-hosts, reusing a trusted backend);\n **Stage 3** is the canonical bootstrap fixed point that retires the Elixir host entirely.\n@@ -77,5 +77,6 @@ and requires the compiler in Rian. Each stage's claim is bounded to what its pro\n   define the canonical/normalized equality the front-end diff uses.\n - **Portable-stdlib breadth** — enumerate the `Enum`/`Map`/`String` ops the real front-end needs and\n   port them (ADR-0047), the true gate on Stage 2.\n-- **Determinism for Stage 3** — bit-identical `.beam` requires deterministic emission (atom/name\n-  ordering, no timestamps); audit `Rian.Beam` for nondeterminism before the fixed-point test.\n+- ~~**Determinism for Stage 3**~~ — **verified**: `Rian.Beam` already emits byte-identical bytecode on\n+  recompilation (the optimizer/parser/module sources round-trip identically — `selfhost_fixedpoint_test.exs`).\n+  Re-audit if the emitter gains atom-table or name-ordering nondeterminism.","raw_url":"https://github.com/wstein/rian-lab/raw/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"36bb84fce77e0a8b2971866c18bf61a31cc890e4","status":"modified"},{"additions":73,"blob_url":"https://github.com/wstein/rian-lab/blob/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/examples%2Frian%2Fselfhost_parse.rian","changes":114,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_parse.rian?ref=eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be","deletions":41,"filename":"examples/rian/selfhost_parse.rian","patch":"@@ -1,38 +1,35 @@\n # ===========================================================================\n-# SELF-HOSTING — porting Rian.Pratt to Rian, fixpoint-locked (rung 2, widened)\n+# SELF-HOSTING — porting Rian.Pratt to Rian, fixpoint-locked (Stage 2 front-end)\n # ===========================================================================\n # The parser's output is diffed against the reference `Rian.Pratt.parse` over a\n-# corpus (test/rian/parse_fixpoint_test.exs) — the parser analog of the lexer\n-# fixpoint. The diff needs NO projection: the AST constructors lower to Pratt's\n-# exact surface tuples — Num(s)->{:num,s}, Id(s)->{:id,s}, Bin(op,l,r)->\n-# {:bin,op,l,r} — so `parse(tokens)` term-EQUALS `Rian.Pratt.parse(source)`.\n+# corpus (test/rian/parse_fixpoint_test.exs). The diff needs NO projection: the\n+# AST constructors lower to Pratt's exact surface tuples —\n+#     Num(s)->{:num,s}  Id(s)->{:id,s}  Bin(op,l,r)->{:bin,op,l,r}\n+#     Unary(op,x)->{:unary,op,x}        Call(f,args)->{:call,f,args}\n+# — so `parse(tokens)` term-EQUALS `Rian.Pratt.parse(source)`. This is the\n+# Stage-2 front-end (ADR-0063): its output is consumed by the real Elixir backend\n+# (`Rian.Lower`/`Rian.Core`) to actually run — proven in the parse fixpoint test.\n #\n-# This widens the first slice (which hardcoded `+ - * /` in two levels) to the\n-# FULL binary-operator precedence table via **precedence climbing** — the same\n-# algorithm `Rian.Pratt` uses, with the same binding powers (`opinfo`/`bp`):\n+# Grammar (Rian.Pratt's binding powers; precedence climbing):\n+#   expr    := prefix (binop expr)*          binop levels per Rian.Pratt.opinfo\n+#   prefix  := ('-' | 'not') prefix@110 | primary\n+#   primary := Num | Id | '(' expr ')'  then postfix\n+#   postfix := '(' args ')'                  function call\n #\n-#   level  3  * / rem div     (left)        base = (13 - level) * 10\n-#   level  4  + -             (left)        left/none: lbp=base,  rbp=base+1\n-#   level  5  <>              (right)       right:     lbp=base+1, rbp=base\n-#   level  8  < <= > >=       (none)\n-#   level  9  == !=           (none)\n-#   level 10  and             (left)\n-#   level 11  or              (left)\n-#\n-# BEAM guards cannot call user functions, so every precedence decision is made in\n-# the body (an `if` over `lbp(op)`), never in a guard. Atoms are Num / Id /\n-# parenthesized expressions. Prefix operators, calls, pipes, and the non-assoc\n-# *raise* (Pratt rejects `a < b < c`) are later slices — the corpus stays within\n-# this grammar (each non-assoc operator used at most once per level).\n+# Out of slice (later): labeled args (`k: v`), `.field`, `&` captures, `if`/`case`/\n+# `with`, lists/maps/tuples, and Pratt's non-assoc *raise* (`a < b < c`).\n # ---------------------------------------------------------------------------\n \n mod SelfhostParse do\n-  type Tok := TNum(String) | TId(String) | TOp(String) | TLP | TRP\n+  type Tok := TNum(String) | TId(String) | TOp(String) | TLP | TRP | TComma\n \n   # constructor names chosen so the lowered term matches Rian.Pratt's tuples.\n-  type Expr := Num(String) | Id(String) | Bin(String, Expr, Expr) | EErr\n+  type Expr :=\n+      Num(String) | Id(String) | Bin(String, Expr, Expr)\n+    | Unary(String, Expr) | Call(Expr, Vec(Expr)) | EErr\n \n   type Parse := P(Expr, Vec(Tok))\n+  type Args := AP(Vec(Expr), Vec(Tok))\n \n   pub def parse(ts Vec(Tok)) Expr\n   pub def parse(ts)\n@@ -41,17 +38,16 @@ mod SelfhostParse do\n     end\n   end\n \n-  # parse an expression whose operators all bind at least as tight as `min_bp`.\n+  # an expression whose operators all bind at least as tight as `min_bp`.\n   def parse_bp(min_bp Int64, ts Vec(Tok)) Parse\n   def parse_bp(min_bp, ts)\n-    case parse_atom(ts) do\n+    case parse_prefix(ts) do\n       P(lhs, rest) -> climb(min_bp, lhs, rest)\n     end\n   end\n \n   # while the next token is a binary operator whose left binding power is >=\n-  # min_bp, consume it and fold; the right-hand side is parsed at `rbp(op)` (so\n-  # left-assoc stops at the same level, right-assoc re-enters it).\n+  # min_bp, consume it and fold; the RHS is parsed at `rbp(op)`.\n   def climb(min_bp Int64, lhs Expr, ts Vec(Tok)) Parse\n   def climb(min_bp, lhs, [TOp(op) | rest])\n     if lbp(op) >= min_bp do\n@@ -64,28 +60,65 @@ mod SelfhostParse do\n   end\n   def climb(min_bp, lhs, ts) := P(lhs, ts)\n \n-  # atom := Num | Id | '(' expr ')'  — anything else (or a missing `)`) is a\n-  # total `EErr` (the reference raises; the corpus is well-formed).\n-  def parse_atom(ts Vec(Tok)) Parse\n-  def parse_atom([TNum(n) | rest]) := P(Num(n), rest)\n-  def parse_atom([TId(x) | rest]) := P(Id(x), rest)\n-  def parse_atom([TLP | rest])\n+  # prefix := ('-' | 'not') prefix | primary  — unary operands bind at 110\n+  # (tighter than any binary op), matching Rian.Pratt.parse_prefix.\n+  def parse_prefix(ts Vec(Tok)) Parse\n+  def parse_prefix([TOp(\"-\") | rest])\n+    case parse_bp(110, rest) do\n+      P(x, rest2) -> P(Unary(\"-\", x), rest2)\n+    end\n+  end\n+  def parse_prefix([TOp(\"not\") | rest])\n+    case parse_bp(110, rest) do\n+      P(x, rest2) -> P(Unary(\"not\", x), rest2)\n+    end\n+  end\n+  def parse_prefix(ts) := parse_primary(ts)\n+\n+  # primary := Num | Id | '(' expr ')'  then postfix (calls)\n+  def parse_primary(ts Vec(Tok)) Parse\n+  def parse_primary([TNum(n) | rest]) := parse_postfix(Num(n), rest)\n+  def parse_primary([TId(x) | rest]) := parse_postfix(Id(x), rest)\n+  def parse_primary([TLP | rest])\n     case parse_bp(0, rest) do\n-      P(e, [TRP | rest2]) -> P(e, rest2)\n+      P(e, [TRP | rest2]) -> parse_postfix(e, rest2)\n       P(_, other) -> P(EErr, other)\n     end\n   end\n-  def parse_atom(ts) := P(EErr, ts)\n+  def parse_primary(ts) := P(EErr, ts)\n+\n+  # postfix := '(' args ')'  — a function call `node(args)`.\n+  def parse_postfix(node Expr, ts Vec(Tok)) Parse\n+  def parse_postfix(node, [TLP | rest])\n+    case parse_args(rest) do\n+      AP(args, rest2) -> parse_postfix(Call(node, args), rest2)\n+    end\n+  end\n+  def parse_postfix(node, ts) := P(node, ts)\n+\n+  # args := ε | expr (',' expr)*\n+  def parse_args(ts Vec(Tok)) Args\n+  def parse_args([TRP | rest]) := AP([], rest)\n+  def parse_args(ts)\n+    case parse_bp(0, ts) do\n+      P(a, rest) -> finish_arg(a, rest)\n+    end\n+  end\n+\n+  def finish_arg(a Expr, ts Vec(Tok)) Args\n+  def finish_arg(a, [TComma | rest])\n+    case parse_args(rest) do\n+      AP(more, rest2) -> AP([a | more], rest2)\n+    end\n+  end\n+  def finish_arg(a, [TRP | rest]) := AP([a], rest)\n+  def finish_arg(a, ts) := AP([a], ts)\n \n   # ── binding powers (mirror Rian.Pratt.bp) ────────────────────────────────\n-  # left binding power: where the operator sits when deciding to bind leftward.\n   def lbp(op String) Int64 := bp_base(level(op)) + left_off(op)\n-  # right binding power: the min_bp the right operand is parsed at.\n   def rbp(op String) Int64 := bp_base(level(op)) + right_off(op)\n-\n   def bp_base(lvl Int64) Int64 := (13 - lvl) * 10\n \n-  # only `<>` is right-associative among the operators in this slice.\n   def left_off(op String) Int64\n   def left_off(\"<>\") := 1\n   def left_off(op) := 0\n@@ -95,8 +128,7 @@ mod SelfhostParse do\n   def right_off(op) := 1\n \n   # operator precedence level (lower binds tighter); an unknown token gets a level\n-  # so loose that `lbp(op) >= min_bp` is always false — climbing stops, the token\n-  # is left unconsumed (never a binary fold).\n+  # so loose that climbing always stops (the token is left unconsumed).\n   def level(op String) Int64\n   def level(\"*\") := 3\n   def level(\"/\") := 3","raw_url":"https://github.com/wstein/rian-lab/raw/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/examples%2Frian%2Fselfhost_parse.rian","sha":"30d1fc587b14e19e4ffe50fa99f0debdac00eb2a","status":"modified"},{"additions":46,"blob_url":"https://github.com/wstein/rian-lab/blob/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/test%2Frian%2Fparse_fixpoint_test.exs","changes":47,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fparse_fixpoint_test.exs?ref=eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be","deletions":1,"filename":"test/rian/parse_fixpoint_test.exs","patch":"@@ -26,6 +26,7 @@ defmodule Rian.ParseFixpointTest do\n   defp inject({:op, o}), do: {:t_op, o}\n   defp inject({:lparen}), do: :tlp\n   defp inject({:rparen}), do: :trp\n+  defp inject({:comma}), do: :t_comma\n \n   defp toks(src), do: src |> Lexer.expr_tokens() |> Enum.map(&inject/1)\n \n@@ -56,7 +57,21 @@ defmodule Rian.ParseFixpointTest do\n     \"a <> b <> c\",\n     \"x rem y div z\",\n     \"n * n + 1 < limit and ok\",\n-    \"a <= b and c >= d or e\"\n+    \"a <= b and c >= d or e\",\n+    # prefix operators and function calls\n+    \"-x\",\n+    \"not a\",\n+    \"-a + b\",\n+    \"a + -b * c\",\n+    \"not a and b\",\n+    \"f(x)\",\n+    \"f(x, y)\",\n+    \"g()\",\n+    \"f(x, y) + 1\",\n+    \"h(a + b, c * d)\",\n+    \"f(g(x), y)\",\n+    \"-f(x)\",\n+    \"1 + f(2, 3) * g(4)\"\n   ]\n \n   describe \"self-hosting parser fixpoint (ADR-0027/0031) — Rian parser vs Rian.Pratt\" do\n@@ -100,5 +115,35 @@ defmodule Rian.ParseFixpointTest do\n       assert mod.parse(toks(\"a <> b <> c\")) ==\n                {:bin, \"<>\", {:id, \"a\"}, {:bin, \"<>\", {:id, \"b\"}, {:id, \"c\"}}}\n     end\n+\n+    test \"prefix `-` binds tighter than `+`: `-a + b` ⇒ `(-a) + b`\", %{mod: mod} do\n+      assert mod.parse(toks(\"-a + b\")) ==\n+               {:bin, \"+\", {:unary, \"-\", {:id, \"a\"}}, {:id, \"b\"}}\n+    end\n+\n+    test \"a call's arguments are full expressions: `f(a + b, c)`\", %{mod: mod} do\n+      assert mod.parse(toks(\"f(a + b, c)\")) ==\n+               {:call, {:id, \"f\"}, [{:bin, \"+\", {:id, \"a\"}, {:id, \"b\"}}, {:id, \"c\"}]}\n+    end\n+  end\n+\n+  # Stage 2 of the bootstrap ladder (ADR-0063): the Rian-written front-end's output\n+  # is consumed by the **real Elixir backend** (`Rian.Lower`) and actually runs —\n+  # \"Rian front-end + reused backend = working program.\" (Calls to bound functions\n+  # are exercised by the fixpoint above; here we run arithmetic/unary so emission is\n+  # unambiguous.)\n+  describe \"Stage 2 — the Rian front-end feeds the real backend and runs\" do\n+    test \"a Rian-parsed expression compiles through Rian.Lower and executes\", %{mod: mod} do\n+      run = fn src, binds ->\n+        ast = mod.parse(toks(src))\n+        {value, _} = Code.eval_string(Rian.Lower.emit_ast(ast, :elixir), binds)\n+        value\n+      end\n+\n+      assert run.(\"(2 + 3) * 4\", []) == 20\n+      assert run.(\"a + b * c\", a: 2, b: 3, c: 4) == 14\n+      assert run.(\"-a + b\", a: 5, b: 1) == -4\n+      assert run.(\"a * b - c * d\", a: 2, b: 3, c: 4, d: 5) == -14\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/test%2Frian%2Fparse_fixpoint_test.exs","sha":"751d6b8421a132282ad03ddf045fa66b075d680d","status":"modified"},{"additions":15,"blob_url":"https://github.com/wstein/rian-lab/blob/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/test%2Frian%2Fselfhost_fixedpoint_test.exs","changes":15,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fselfhost_fixedpoint_test.exs?ref=eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be","deletions":0,"filename":"test/rian/selfhost_fixedpoint_test.exs","patch":"@@ -74,4 +74,19 @@ defmodule Rian.SelfhostFixedpointTest do\n       assert length(got) > 500\n     end\n   end\n+\n+  # Stage 3 prerequisite (ADR-0063 §\"Open items\"): a bit-identical bootstrap fixed\n+  # point (compile the compiler with itself, twice, and get the same bytes) is only\n+  # possible if emission is **deterministic**. Verify `Rian.Beam` already is — the\n+  # same source compiles to byte-identical bytecode on every run.\n+  describe \"Stage 3 prerequisite — deterministic emission (ADR-0063)\" do\n+    test \"Rian.Beam emits byte-identical bytecode on recompilation\" do\n+      for f <- ~w(selfhost_opt.rian selfhost_modules.rian selfhost_parse.rian) do\n+        src = File.read!(Path.join([\"examples\", \"rian\", f]))\n+\n+        assert Beam.compile_program(src) == Beam.compile_program(src),\n+               \"Rian.Beam emission is nondeterministic for #{f} — blocks a bit-identical bootstrap\"\n+      end\n+    end\n+  end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/eb5b903bb7cd4531bb103e778f2bfd5c6bbac4be/test%2Frian%2Fselfhost_fixedpoint_test.exs","sha":"87368753cbfe6cc4a1a93f290d3776a29e29651a","status":"modified"}]}