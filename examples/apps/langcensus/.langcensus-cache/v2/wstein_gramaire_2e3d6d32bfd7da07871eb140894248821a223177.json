{"files":[{"additions":39,"blob_url":"https://github.com/wstein/gramaire/blob/2e3d6d32bfd7da07871eb140894248821a223177/.github%2Fworkflows%2Fdeploy-docs.yml","changes":41,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.github%2Fworkflows%2Fdeploy-docs.yml?ref=2e3d6d32bfd7da07871eb140894248821a223177","deletions":2,"filename":".github/workflows/deploy-docs.yml","patch":"@@ -88,6 +88,43 @@ jobs:\n       name: github-pages\n       url: ${{ steps.deployment.outputs.page_url }}\n     steps:\n-      - name: Deploy to GitHub Pages\n-        id: deployment\n+      # actions/deploy-pages occasionally fails its post-creation status poll with a\n+      # generic \"Deployment failed, try again later\" — a transient GitHub Pages\n+      # backend hiccup (observed 2026-07-04 and 2026-07-05), not anything this\n+      # workflow or repo controls. It has always self-healed on the next push, but\n+      # retrying here means a single flaky attempt doesn't force a hollow follow-up\n+      # commit just to redeploy the same content.\n+      - name: Deploy to GitHub Pages (attempt 1)\n+        id: deploy1\n+        continue-on-error: true\n         uses: actions/deploy-pages@v5\n+\n+      - name: Back off before retry 1\n+        if: steps.deploy1.outcome == 'failure'\n+        run: sleep 15\n+\n+      - name: Deploy to GitHub Pages (attempt 2)\n+        id: deploy2\n+        if: steps.deploy1.outcome == 'failure'\n+        continue-on-error: true\n+        uses: actions/deploy-pages@v5\n+\n+      - name: Back off before retry 2\n+        if: steps.deploy1.outcome == 'failure' && steps.deploy2.outcome == 'failure'\n+        run: sleep 30\n+\n+      - name: Deploy to GitHub Pages (attempt 3)\n+        id: deploy3\n+        if: steps.deploy1.outcome == 'failure' && steps.deploy2.outcome == 'failure'\n+        uses: actions/deploy-pages@v5\n+\n+      - name: Resolve deployment result\n+        id: deployment\n+        if: always()\n+        run: |\n+          url=\"${{ steps.deploy3.outputs.page_url || steps.deploy2.outputs.page_url || steps.deploy1.outputs.page_url }}\"\n+          echo \"page_url=$url\" >> \"$GITHUB_OUTPUT\"\n+          if [ -z \"$url\" ]; then\n+            echo \"::error::All deploy-pages attempts failed\"\n+            exit 1\n+          fi","raw_url":"https://github.com/wstein/gramaire/raw/2e3d6d32bfd7da07871eb140894248821a223177/.github%2Fworkflows%2Fdeploy-docs.yml","sha":"1329e659c299f64fde57f94adece1258a1cabe3a","status":"modified"}]}