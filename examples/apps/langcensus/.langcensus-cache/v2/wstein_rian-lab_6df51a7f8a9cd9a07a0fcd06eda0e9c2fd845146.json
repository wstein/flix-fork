{"files":[{"additions":60,"blob_url":"https://github.com/wstein/rian-lab/blob/6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146/compiler%2Fchecker.rian","changes":60,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/compiler%2Fchecker.rian?ref=6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146","deletions":0,"filename":"compiler/checker.rian","patch":"@@ -933,4 +933,64 @@ mod Checker do\n   def cat_str(Vec(String), Vec(String)) Vec(String)\n   def cat_str([], ys) := ys\n   def cat_str([x | xs], ys) := [x | cat_str(xs, ys)]\n+\n+  # ── error set #1: numeric mix (ADR-0035) — the first SELF-CHECKING gate ──────────\n+  # The inference above never rejects; it infers `unknown` and moves on. This is the\n+  # first port of a Rian.Check ERROR SET — the boundary check that turns the compiler\n+  # from self-COMPILING into (partially) self-CHECKING. `Rian.Check.check_numeric_mix`:\n+  # a `+`/`-`/`*` whose two operands are concretely a different numeric KIND (one\n+  # int/uint, the other float) is REJECTED — a value never silently becomes a float.\n+  # `num_mix` walks one clause body's Core and returns true iff such a mix exists.\n+  # CONSERVATIVE: an operand inferred `unknown` (or non-numeric) never triggers — only a\n+  # PROVABLE int↔float mix. The env is FIXED across the walk (no case-narrow / block-bind\n+  # / lambda-extend as we descend), mirroring the reference's `scan_num_mix`.\n+  pub def num_mix(Core, Dict(String, String), Dict(Symbol, Dict(String, String))) Bool\n+  pub def num_mix(CNum(_), _, _) := false\n+  pub def num_mix(CStr(_), _, _) := false\n+  pub def num_mix(CChar(_), _, _) := false\n+  pub def num_mix(CId(_), _, _) := false\n+  pub def num_mix(CUnary(_, a), env, ic) := num_mix(a, env, ic)\n+  pub def num_mix(CBin(op, l, r), env, ic)\n+    if mix_op(op) and mixed_kind(ordinal_base(infer_ic(l, env, ic)), ordinal_base(infer_ic(r, env, ic))) do\n+      true\n+    else\n+      num_mix(l, env, ic) or num_mix(r, env, ic)\n+    end\n+  end\n+  pub def num_mix(CCall(_, args), env, ic) := num_mix_any(args, env, ic)\n+  pub def num_mix(CIf(c, t, e), env, ic) := num_mix(c, env, ic) or num_mix(t, env, ic) or num_mix(e, env, ic)\n+  pub def num_mix(CBlock(stmts), env, ic) := num_mix_stmts(stmts, env, ic)\n+  pub def num_mix(CCase(scrut, arms), env, ic) := num_mix(scrut, env, ic) or num_mix_arms(arms, env, ic)\n+  pub def num_mix(CList(elems, tail), env, ic) := num_mix_any(elems, env, ic) or num_mix_tail(tail, env, ic)\n+  pub def num_mix(CLambda(_, body), env, ic) := num_mix(body, env, ic)\n+\n+  # the mix arithmetic ops are EXACTLY Rian.Check's @arith = `+ - *` (NOT div/rem, which\n+  # are integer-only; `arith_op`'s wider set is for inference, not this gate).\n+  def mix_op(op String) Bool := op == \"+\" or op == \"-\" or op == \"*\"\n+\n+  # one operand int/uint-kind, the other float-kind, both concrete (Rian.Check.mixed_num?\n+  # / num_mix?). NkNone (a non-numeric type, or `unknown`) never mixes.\n+  def mixed_kind(lt String, rt String) Bool := km(num_kind(lt), num_kind(rt))\n+  def km(NumKind, NumKind) Bool\n+  def km(Nk(lk, _), Nk(rk, _)) := kind_mix(lk, rk)\n+  def km(_, _) := false\n+  def kind_mix(lk String, rk String) Bool := (lk == \"float\" and int_kind(rk)) or (rk == \"float\" and int_kind(lk))\n+  def int_kind(k String) Bool := k == \"int\" or k == \"uint\"\n+\n+  def num_mix_any(Vec(Core), Dict(String, String), Dict(Symbol, Dict(String, String))) Bool\n+  def num_mix_any([], _, _) := false\n+  def num_mix_any([c | rest], env, ic) := num_mix(c, env, ic) or num_mix_any(rest, env, ic)\n+\n+  def num_mix_stmts(Vec(Stmt), Dict(String, String), Dict(Symbol, Dict(String, String))) Bool\n+  def num_mix_stmts([], _, _) := false\n+  def num_mix_stmts([SBind(_, e) | rest], env, ic) := num_mix(e, env, ic) or num_mix_stmts(rest, env, ic)\n+  def num_mix_stmts([SExpr(e) | rest], env, ic) := num_mix(e, env, ic) or num_mix_stmts(rest, env, ic)\n+\n+  def num_mix_arms(Vec(Arm), Dict(String, String), Dict(Symbol, Dict(String, String))) Bool\n+  def num_mix_arms([], _, _) := false\n+  def num_mix_arms([CArm(_, body) | rest], env, ic) := num_mix(body, env, ic) or num_mix_arms(rest, env, ic)\n+\n+  def num_mix_tail(Tail, Dict(String, String), Dict(Symbol, Dict(String, String))) Bool\n+  def num_mix_tail(TClose, _, _) := false\n+  def num_mix_tail(TCons(e), env, ic) := num_mix(e, env, ic)\n end","raw_url":"https://github.com/wstein/rian-lab/raw/6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146/compiler%2Fchecker.rian","sha":"eac0da9a803c7e8af7f3ac736c79bf1461d92362","status":"modified"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_beam.rian","changes":427,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/compiler%2Fcompose_real_beam.rian?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":427,"filename":"compiler/compose_real_beam.rian","patch":"@@ -1,427 +0,0 @@\n-# ===========================================================================\n-# SELF-HOSTING — COMPOSITION rung 7: wire the VERIFIED beam backend into the driver\n-# ===========================================================================\n-# Rungs 1-6 used a TOY backend: the driver had its own hand-rolled `forms`\n-# (Core → abstract-form tuples), separate from the equivalence-locked\n-# `beam.rian`. That made two disconnected successes — a per-stage 100%\n-# (selfhost_beam verified vs `Rian.Beam` in ISOLATION, with its `Form` sum\n-# inflated to real forms by Elixir glue in the fixpoint test) and a composition\n-# loop over a toy surface. This rung connects them: the driver's backend IS now\n-# the verified port, called across modules.\n-#\n-#   parse(src) → selfhost_beam Core/Func  →  Beam.compile_forms(funcs)\n-#                                            └─ the REAL, equivalence-locked backend\n-#                          → inflate Form → real abstract-form tuples (IN RIAN)\n-#                          → :compile.forms / :code.load_binary (the driver loop)\n-#\n-# Two things move out of \"glue\" and into Rian:\n-#   * the backend call is a genuine cross-module call to `Beam` (loaded\n-#     under its `:\"Elixir.Beam\"` atom — a Pascal-qualified call, ADR-0041),\n-#     so stage N's Rian output (a `Func`) is stage N+1's Rian input with no\n-#     projection — the shared-type reconciliation the composition axis is about.\n-#   * the `Form` → abstract-form inflation that the beam fixpoint test did in\n-#     Elixir (`erl_op`/`var_atom`/`to_snake`) is ported here to Rian.\n-#\n-# Scope: still the arithmetic + `if`/comparison/boolean surface (the driver's\n-# front-end is unchanged) — but the BACKEND is no longer a toy. Widening the\n-# front-end to the real parser/core stages is the next cut.\n-#\n-# `test/rian/compose_real_beam_fixpoint_test.exs` loads `Beam`, then calls\n-# only `build/2` and runs the result, identical to `Rian.Beam`.\n-# ---------------------------------------------------------------------------\n-\n-mod ComposeRealBeam do\n-  # ── selfhost_beam's INPUT IR (structurally identical — the shared contract) ──\n-  type Core := CNum(String)\n-             | CId(String)\n-             | CAtom(String)\n-             | CUnary(String, Core)\n-             | CBin(String, Core, Core)\n-             | CCall(String, Vec(Core))\n-             | CTuple(Vec(Core))\n-             | CList(Vec(Core), Tail)\n-             | CIf(Core, Block, Block)\n-             | CCase(Core, Vec(Arm))\n-\n-  type Tail := TClose | TTail(Core)\n-  type Block := Block(Vec(Stmt))\n-  type Stmt := SBind(String, Core) | STypedBind(String, String, Core) | SExpr(Core)\n-  type Arm := Arm(Pat, Guard, Block)\n-\n-  type Pat := PWild\n-            | PVar(String)\n-            | PInt(Int53)\n-            | PAtom(String)\n-            | PCtor(String, Vec(Pat))\n-            | PTuple(Vec(Pat))\n-            | PList(Vec(Pat), PTail)\n-\n-  type PTail := PtlClose | PtlTail(Pat)\n-  type Guard := GNone | GSome(Core)\n-  type Clause := Clause(Vec(Pat), Guard, Block)\n-  type Func := Func(String, Int53, Vec(Clause))\n-\n-  # ── selfhost_beam's OUTPUT (the string-carrying Form sum we inflate) ──\n-  type Form := FInt(Int53)\n-             | FVar(String)\n-             | FAtom(String)\n-             | FCtorN(String)\n-             | FCtor(String, Vec(Form))\n-             | FOp1(String, Form)\n-             | FOp2(String, Form, Form)\n-             | FCall(String, Vec(Form))\n-             | FTuple(Vec(Form))\n-             | FNil\n-             | FCons(Form, Form)\n-             | FIf(Form, Vec(Form), Vec(Form))\n-             | FCase(Form, Vec(FClause))\n-             | FMatch(String, Form)\n-\n-  type GForm := GfNone | GfSome(Form)\n-  type FClause := FClause(Vec(Form), GForm, Vec(Form))\n-  type FFunc := FFunc(String, Int53, Vec(FClause))\n-\n-  # ── driver-local types ──\n-  type Tok := TNum(String) | TId(String) | TOp(String)\n-            | TLP | TRP | TComma | TDef | TIf | TDo | TElse | TEnd\n-\n-  type Decl := Def(String, Vec(Pat), Core)\n-  type Grp := Grp(String, Int53, Vec(Decl))\n-  type PR := PR(Core, Vec(Tok))\n-  type CR := CR(Decl, Vec(Tok))\n-\n-  # ── the BEAM toolchain boundary: two @external(:ex) host calls (counted FFI) ──\n-  @external(:ex, \":compile.forms(forms, [:return_errors])\")\n-  def compile_forms_host(forms Vec(Tuple)) Tuple\n-\n-  @external(:ex, \":code.load_binary(name, chars, bin)\")\n-  def load_binary(name Symbol, chars Vec(Int53), bin String) Tuple\n-\n-  # ── the driver: source -> a loaded, runnable module ──\n-  pub def build(src String, modname Symbol) Symbol\n-  pub def build(src, modname)\n-    case compile_forms_host(compile_module(src, modname)) do\n-      {:ok, m, bin} -> loaded(load_binary(m, Prim.str_chars(\"nofile\"), bin))\n-    end\n-  end\n-\n-  def loaded(Tuple) Symbol\n-  def loaded({:module, m}) := m\n-\n-  # ── source -> a full :compile.forms input, via the REAL beam backend ──\n-  # `Beam.compile_forms` is the equivalence-locked port (cross-module call);\n-  # its `Form` output is inflated to real abstract-form tuples here in Rian.\n-  pub def compile_module(src String, modname Symbol) Vec(Tuple)\n-  pub def compile_module(src, modname) := assemble(modname, funcs(group(parse_clauses(lex(src)))))\n-\n-  def assemble(modname Symbol, fs Vec(Func)) Vec(Tuple) := [{:attribute, 0, :module, modname}, {:attribute, 0, :export, exports(fs)} | inflate_funcs(Beam.compile_forms(fs))]\n-\n-  def exports(Vec(Func)) Vec(Tuple)\n-  def exports([]) := []\n-  def exports([Func(name, arity, _) | rest]) := [{Prim.str_to_atom(name), arity} | exports(rest)]\n-\n-  # ── stage 1: lex (String -> tokens) — unchanged from rung 6 ──\n-  pub def lex(s String) Vec(Tok) := lex_chars(Prim.str_chars(s))\n-\n-  def lex_chars(Vec(Int53)) Vec(Tok)\n-  def lex_chars([]) := []\n-  def lex_chars([c | rest])\n-    if c == 32 or c == 10 or c == 13 do\n-      lex_chars(rest)\n-    else\n-      classify(c, rest)\n-    end\n-  end\n-\n-  def classify(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def classify(c, rest)\n-    if is_digit(c) do\n-      take_num([c | rest], [])\n-    else\n-      lex_alpha_or_op(c, rest)\n-    end\n-  end\n-\n-  def lex_alpha_or_op(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def lex_alpha_or_op(c, rest)\n-    if is_alpha(c) do\n-      take_id([c | rest], [])\n-    else\n-      take_op([c | rest])\n-    end\n-  end\n-\n-  def take_num(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_num([], acc) := [TNum(text(acc))]\n-  def take_num([c | rest], acc)\n-    if is_digit(c) do\n-      take_num(rest, [c | acc])\n-    else\n-      [TNum(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  def take_id(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_id([], acc) := [id_tok(text(acc))]\n-  def take_id([c | rest], acc)\n-    if is_alnum(c) do\n-      take_id(rest, [c | acc])\n-    else\n-      [id_tok(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  def id_tok(String) Tok\n-  def id_tok(\"def\") := TDef\n-  def id_tok(\"if\") := TIf\n-  def id_tok(\"do\") := TDo\n-  def id_tok(\"else\") := TElse\n-  def id_tok(\"end\") := TEnd\n-  def id_tok(\"and\") := TOp(\"and\")\n-  def id_tok(\"or\") := TOp(\"or\")\n-  def id_tok(s) := TId(s)\n-\n-  def take_op(Vec(Int53)) Vec(Tok)\n-  def take_op([58, 61 | rest]) := [TOp(\":=\") | lex_chars(rest)]\n-  def take_op([61, 61 | rest]) := [TOp(\"==\") | lex_chars(rest)]\n-  def take_op([62, 61 | rest]) := [TOp(\">=\") | lex_chars(rest)]\n-  def take_op([60, 61 | rest]) := [TOp(\"<=\") | lex_chars(rest)]\n-  def take_op([44 | rest]) := [TComma | lex_chars(rest)]\n-  def take_op([40 | rest]) := [TLP | lex_chars(rest)]\n-  def take_op([41 | rest]) := [TRP | lex_chars(rest)]\n-  def take_op([c | rest]) := [TOp(Prim.str_from_chars([c])) | lex_chars(rest)]\n-\n-  def text(acc Vec(Int53)) String := Prim.str_from_chars(rev_codes(acc, []))\n-\n-  def rev_codes(Vec(Int53), Vec(Int53)) Vec(Int53)\n-  def rev_codes([], out) := out\n-  def rev_codes([c | rest], out) := rev_codes(rest, [c | out])\n-\n-  def is_digit(c Int53) Bool := c >= 48 and c <= 57\n-  def is_alpha(c Int53) Bool := (c >= 65 and c <= 90) or (c >= 97 and c <= 122) or c == 95\n-  def is_alnum(c Int53) Bool := is_alpha(c) or is_digit(c)\n-\n-  # ── stage 2a: parse the clause sequence — heads are beam `Pat`, body is `Core` ──\n-  pub def parse_clauses(Vec(Tok)) Vec(Decl)\n-  pub def parse_clauses([]) := []\n-  pub def parse_clauses(ts)\n-    case parse_one(ts) do\n-      CR(d, rest) -> [d | parse_clauses(rest)]\n-    end\n-  end\n-\n-  def parse_one(Vec(Tok)) CR\n-  def parse_one([_ | rest])\n-    case rest do\n-      [TId(name) | r2] -> parse_head(name, r2)\n-    end\n-  end\n-\n-  def parse_head(name String, Vec(Tok)) CR\n-  def parse_head(name, [TLP | rest]) := collect_pats(name, [], rest)\n-\n-  # number head -> PInt pattern, identifier head -> PVar pattern.\n-  def collect_pats(name String, acc Vec(Pat), Vec(Tok)) CR\n-  def collect_pats(name, acc, [TRP | rest]) := finish_clause(name, rev_pats(acc, []), rest)\n-  def collect_pats(name, acc, [TComma | rest]) := collect_pats(name, acc, rest)\n-  def collect_pats(name, acc, [TNum(n) | rest]) := collect_pats(name, [PInt(parse_int(n)) | acc], rest)\n-  def collect_pats(name, acc, [TId(p) | rest]) := collect_pats(name, [PVar(p) | acc], rest)\n-\n-  def finish_clause(name String, pats Vec(Pat), Vec(Tok)) CR\n-  def finish_clause(name, pats, [_ | rest])\n-    case parse_bp(0, rest) do\n-      PR(body, leftover) -> CR(Def(name, pats, body), leftover)\n-    end\n-  end\n-\n-  def rev_pats(Vec(Pat), Vec(Pat)) Vec(Pat)\n-  def rev_pats([], out) := out\n-  def rev_pats([p | rest], out) := rev_pats(rest, [p | out])\n-\n-  # ── stage 2b: parse a body expression directly into beam `Core` ──\n-  def parse_bp(min_bp Int53, ts Vec(Tok)) PR\n-  def parse_bp(min_bp, ts)\n-    case primary(ts) do\n-      PR(lhs, rest) -> climb(min_bp, lhs, rest)\n-    end\n-  end\n-\n-  def climb(min_bp Int53, lhs Core, ts Vec(Tok)) PR\n-  def climb(min_bp, lhs, [TOp(op) | rest])\n-    if lbp(op) >= min_bp do\n-      case parse_bp(rbp(op), rest) do\n-        PR(rhs, rest2) -> climb(min_bp, CBin(op, lhs, rhs), rest2)\n-      end\n-    else\n-      PR(lhs, [TOp(op) | rest])\n-    end\n-  end\n-  def climb(min_bp, lhs, ts) := PR(lhs, ts)\n-\n-  def primary(Vec(Tok)) PR\n-  def primary([TNum(n) | rest]) := PR(CNum(n), rest)\n-  def primary([TId(x) | rest]) := call_or_var(x, rest)\n-  def primary([TIf | rest]) := parse_if(rest)\n-  def primary([TLP | rest])\n-    case parse_bp(0, rest) do\n-      PR(e, [TRP | rest2]) -> PR(e, rest2)\n-      PR(e, other) -> PR(e, other)\n-    end\n-  end\n-\n-  # `if cond do then else els end` -> CIf(cond, Block([then]), Block([els])).\n-  def parse_if(Vec(Tok)) PR\n-  def parse_if(ts)\n-    case parse_bp(0, ts) do\n-      PR(c, [TDo | r2]) -> parse_then(c, r2)\n-    end\n-  end\n-\n-  def parse_then(c Core, Vec(Tok)) PR\n-  def parse_then(c, ts)\n-    case parse_bp(0, ts) do\n-      PR(t, [TElse | r3]) -> parse_else(c, t, r3)\n-    end\n-  end\n-\n-  def parse_else(c Core, t Core, Vec(Tok)) PR\n-  def parse_else(c, t, ts)\n-    case parse_bp(0, ts) do\n-      PR(e, [TEnd | r4]) -> PR(CIf(c, Block([SExpr(t)]), Block([SExpr(e)])), r4)\n-    end\n-  end\n-\n-  def call_or_var(name String, Vec(Tok)) PR\n-  def call_or_var(name, [TLP | rest]) := parse_args(name, [], rest)\n-  def call_or_var(name, ts) := PR(CId(name), ts)\n-\n-  def parse_args(name String, acc Vec(Core), Vec(Tok)) PR\n-  def parse_args(name, acc, [TRP | rest]) := PR(CCall(name, rev_core(acc, [])), rest)\n-  def parse_args(name, acc, [TComma | rest]) := parse_args(name, acc, rest)\n-  def parse_args(name, acc, ts)\n-    case parse_bp(0, ts) do\n-      PR(arg, rest) -> parse_args(name, [arg | acc], rest)\n-    end\n-  end\n-\n-  def rev_core(Vec(Core), Vec(Core)) Vec(Core)\n-  def rev_core([], out) := out\n-  def rev_core([e | rest], out) := rev_core(rest, [e | out])\n-\n-  def lbp(op String) Int53 := bp_base(level(op))\n-  def rbp(op String) Int53 := bp_base(level(op)) + 1\n-  def bp_base(lvl Int53) Int53 := (13 - lvl) * 10\n-\n-  def level(String) Int53\n-  def level(\"*\") := 3\n-  def level(\"+\") := 4\n-  def level(\"-\") := 4\n-  def level(\"==\") := 5\n-  def level(\"<\") := 5\n-  def level(\">\") := 5\n-  def level(\"<=\") := 5\n-  def level(\">=\") := 5\n-  def level(\"and\") := 6\n-  def level(\"or\") := 7\n-\n-  # ── stage 2c: group clauses, then wrap each group as a beam `Func` ──\n-  def group(Vec(Decl)) Vec(Grp)\n-  def group([]) := []\n-  def group([Def(name, pats, body) | rest]) := group_run(name, plen(pats), [Def(name, pats, body)], rest)\n-\n-  def group_run(name String, arity Int53, acc Vec(Decl), Vec(Decl)) Vec(Grp)\n-  def group_run(name, arity, acc, []) := [Grp(name, arity, rev_decls(acc, []))]\n-  def group_run(name, arity, acc, [Def(n2, p2, b2) | rest])\n-    if name == n2 do\n-      group_run(name, arity, [Def(n2, p2, b2) | acc], rest)\n-    else\n-      [Grp(name, arity, rev_decls(acc, [])) | group([Def(n2, p2, b2) | rest])]\n-    end\n-  end\n-\n-  def rev_decls(Vec(Decl), Vec(Decl)) Vec(Decl)\n-  def rev_decls([], out) := out\n-  def rev_decls([d | rest], out) := rev_decls(rest, [d | out])\n-\n-  # a group -> selfhost_beam's Func(name, arity, [Clause(pats, GNone, Block([body]))]).\n-  def funcs(Vec(Grp)) Vec(Func)\n-  def funcs([]) := []\n-  def funcs([Grp(name, arity, decls) | rest]) := [Func(name, arity, clauses_of(decls)) | funcs(rest)]\n-\n-  def clauses_of(Vec(Decl)) Vec(Clause)\n-  def clauses_of([]) := []\n-  def clauses_of([Def(_, pats, body) | rest]) := [Clause(pats, GNone, Block([SExpr(body)])) | clauses_of(rest)]\n-\n-  def plen(Vec(Pat)) Int53\n-  def plen([]) := 0\n-  def plen([_ | rest]) := 1 + plen(rest)\n-\n-  # ── inflate: selfhost_beam's Form sum -> real Erlang abstract-form tuples ──\n-  # the conventions the beam fixpoint test applied in Elixir, ported to Rian.\n-  def inflate_funcs(Vec(FFunc)) Vec(Tuple)\n-  def inflate_funcs([]) := []\n-  def inflate_funcs([f | rest]) := [inflate_func(f) | inflate_funcs(rest)]\n-\n-  def inflate_func(FFunc) Tuple\n-  def inflate_func(FFunc(name, arity, clauses)) := {:function, 0, Prim.str_to_atom(name), arity, inflate_clauses(clauses)}\n-\n-  def inflate_clauses(Vec(FClause)) Vec(Tuple)\n-  def inflate_clauses([]) := []\n-  def inflate_clauses([c | rest]) := [inflate_clause(c) | inflate_clauses(rest)]\n-\n-  def inflate_clause(FClause) Tuple\n-  def inflate_clause(FClause(pats, guard, body)) := {:clause, 0, inflate_all(pats), guard_seq(guard), inflate_all(body)}\n-\n-  def guard_seq(GForm) Vec(Vec(Tuple))\n-  def guard_seq(GfNone) := []\n-  def guard_seq(GfSome(g)) := [[inflate(g)]]\n-\n-  def inflate_all(Vec(Form)) Vec(Tuple)\n-  def inflate_all([]) := []\n-  def inflate_all([f | rest]) := [inflate(f) | inflate_all(rest)]\n-\n-  def inflate(Form) Tuple\n-  def inflate(FInt(v)) := {:integer, 0, v}\n-  def inflate(FVar(n)) := {:var, 0, var_atom(n)}\n-  def inflate(FAtom(a)) := {:atom, 0, Prim.str_to_atom(a)}\n-  def inflate(FOp1(op, x)) := {:op, 0, op_atom(op), inflate(x)}\n-  def inflate(FOp2(op, l, r)) := {:op, 0, op_atom(op), inflate(l), inflate(r)}\n-  def inflate(FCall(f, args)) := {:call, 0, {:atom, 0, Prim.str_to_atom(f)}, inflate_all(args)}\n-  def inflate(FIf(c, t, e)) := {:\"case\", 0, inflate(c), [{:clause, 0, [{:atom, 0, :true}], [], inflate_all(t)}, {:clause, 0, [{:atom, 0, :false}], [], inflate_all(e)}]}\n-\n-  # Erlang variable atom: capitalise the first character (`a` -> `:A`, `_` stays).\n-  def var_atom(n String) Symbol := Prim.str_to_atom(cap(n))\n-\n-  # operator string -> Erlang operator atom (`<=` -> `=<`, `and` -> `andalso`).\n-  def op_atom(String) Symbol\n-  def op_atom(\"+\") := :\"+\"\n-  def op_atom(\"-\") := :\"-\"\n-  def op_atom(\"*\") := :\"*\"\n-  def op_atom(\"==\") := :\"==\"\n-  def op_atom(\"<\") := :\"<\"\n-  def op_atom(\">\") := :\">\"\n-  def op_atom(\">=\") := :\">=\"\n-  def op_atom(\"<=\") := :\"=<\"\n-  def op_atom(\"and\") := :andalso\n-  def op_atom(\"or\") := :orelse\n-\n-  def parse_int(s String) Int53 := digits(Prim.str_chars(s), 0)\n-  def digits(Vec(Int53), Int53) Int53\n-  def digits([], acc) := acc\n-  def digits([c | rest], acc) := digits(rest, acc * 10 + c - 48)\n-\n-  def cap(s String) String\n-    case Prim.str_chars(s) do\n-      [c | rest] -> Prim.str_from_chars([up(c) | rest])\n-      [] -> s\n-    end\n-  end\n-\n-  def up(c Int53) Int53\n-    if c >= 97 and c <= 122 do\n-      c - 32\n-    else\n-      c\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_beam.rian","sha":"0513478bd7c75e9d90e031e6a3ad43f4bf452c87","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_decl.rian","changes":349,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/compiler%2Fcompose_real_decl.rian?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":349,"filename":"compiler/compose_real_decl.rian","patch":"@@ -1,349 +0,0 @@\n-# ===========================================================================\n-# SELF-HOSTING — COMPOSITION rung 9: verified DECLARATION front-end + lowering\n-# ===========================================================================\n-# Rung 8 used the verified `selfhost_parse` for clause *bodies* but still split\n-# declarations (def heads / clauses) with driver-local code. This rung replaces\n-# that last toy front-end piece with the equivalence-locked `selfhost_decl` port,\n-# which parses a WHOLE program (def heads, params, multi-clause patterns incl.\n-# cons-lists, expressions) into a `Decl` IR. The driver then lowers that IR to\n-# `selfhost_beam` Core and runs it through the verified backend (rung 7):\n-#\n-#   lex(src) → Decl.parse_program(toks)   ← the REAL declaration parser\n-#            → normalize/group clauses by name\n-#            → lower Decl IR → selfhost_beam Core/Pat   (the surface→Core lowering)\n-#            → Beam.compile_forms(funcs)        ← the REAL backend\n-#            → inflate Form → abstract-form tuples (in Rian)\n-#            → :compile.forms / :code.load_binary\n-#\n-# Now BOTH the front-end (parsing: lex→Decl) and the back-end (codegen:\n-# Beam) are equivalence-locked ports. The only driver-local glue is the\n-# lexer, the surface→Core lowering, and the Form inflater — none of which\n-# reimplements a verified stage. Two verified ports, composed.\n-#\n-# selfhost_decl's surface is arithmetic + calls + multi-clause patterns + cons\n-# LISTS (no `if` — that was rung 8's selfhost_parse). So this rung compiles e.g.\n-#   def sum([]) := 0\n-#   def sum([h | t]) := h + sum(t)\n-# — list-pattern recursion, beyond rung 8's surface. Both `Decl` and\n-# `Beam` load under their :\"Elixir.*\" atoms (Pascal calls,\n-# ADR-0041); both are sibling ports, so the calls are composition, not host\n-# crutches (excluded from the FFI ledger). Only host FFI: :compile.forms /\n-# :code.load_binary.\n-# ---------------------------------------------------------------------------\n-\n-mod ComposeRealDecl do\n-  # ── selfhost_decl's Tok (the subset the driver emits; tags must match) ──\n-  type Tok := TNum(String) | TId(String) | TKw(String) | TOp(String)\n-            | TLP | TRP | TLBracket | TRBracket | TComma | TNL\n-\n-  # ── selfhost_beam's INPUT IR (the shared contract — structurally identical) ──\n-  type Core := CNum(String)\n-             | CId(String)\n-             | CAtom(String)\n-             | CUnary(String, Core)\n-             | CBin(String, Core, Core)\n-             | CCall(String, Vec(Core))\n-             | CTuple(Vec(Core))\n-             | CList(Vec(Core), Tail)\n-             | CIf(Core, Block, Block)\n-             | CCase(Core, Vec(Arm))\n-\n-  type Tail := TClose | TTail(Core)\n-  type Block := Block(Vec(Stmt))\n-  type Stmt := SBind(String, Core) | STypedBind(String, String, Core) | SExpr(Core)\n-  type Arm := Arm(Pat, Guard, Block)\n-\n-  type Pat := PWild\n-            | PVar(String)\n-            | PInt(Int53)\n-            | PAtom(String)\n-            | PCtor(String, Vec(Pat))\n-            | PTuple(Vec(Pat))\n-            | PList(Vec(Pat), PTail)\n-\n-  type PTail := PtlClose | PtlTail(Pat)\n-  type Guard := GNone | GSome(Core)\n-  type Clause := Clause(Vec(Pat), Guard, Block)\n-  type Func := Func(String, Int53, Vec(Clause))\n-\n-  # ── selfhost_beam's OUTPUT (the string-carrying Form sum we inflate) ──\n-  type Form := FInt(Int53)\n-             | FVar(String)\n-             | FAtom(String)\n-             | FCtorN(String)\n-             | FCtor(String, Vec(Form))\n-             | FOp1(String, Form)\n-             | FOp2(String, Form, Form)\n-             | FCall(String, Vec(Form))\n-             | FTuple(Vec(Form))\n-             | FNil\n-             | FCons(Form, Form)\n-             | FIf(Form, Vec(Form), Vec(Form))\n-             | FCase(Form, Vec(FClause))\n-             | FMatch(String, Form)\n-\n-  type GForm := GfNone | GfSome(Form)\n-  type FClause := FClause(Vec(Form), GForm, Vec(Form))\n-  type FFunc := FFunc(String, Int53, Vec(FClause))\n-\n-  # ── driver-local types ──\n-  # a normalized clause (name, lowered head patterns, lowered body) + a per-fn group.\n-  type NClause := NC(String, Vec(Pat), Core)\n-  type Grp := Grp(String, Int53, Vec(NClause))\n-\n-  # ── the BEAM toolchain boundary: two @external(:ex) host calls (counted FFI) ──\n-  @external(:ex, \":compile.forms(forms, [:return_errors])\")\n-  def compile_forms_host(forms Vec(Tuple)) Tuple\n-\n-  @external(:ex, \":code.load_binary(name, chars, bin)\")\n-  def load_binary(name Symbol, chars Vec(Int53), bin String) Tuple\n-\n-  # ── the driver: source -> a loaded, runnable module ──\n-  pub def build(src String, modname Symbol) Symbol\n-  pub def build(src, modname)\n-    case compile_forms_host(compile_module(src, modname)) do\n-      {:ok, m, bin} -> loaded(load_binary(m, Prim.str_chars(\"nofile\"), bin))\n-    end\n-  end\n-\n-  def loaded(Tuple) Symbol\n-  def loaded({:module, m}) := m\n-\n-  # ── source -> a full :compile.forms input, via the REAL decl parser + backend ──\n-  pub def compile_module(src String, modname Symbol) Vec(Tuple)\n-  pub def compile_module(src, modname) := assemble(modname, funcs(group(normalize(Decl.parse_program(lex(src))))))\n-\n-  def assemble(modname Symbol, fs Vec(Func)) Vec(Tuple) := [{:attribute, 0, :module, modname}, {:attribute, 0, :export, exports(fs)} | inflate_funcs(Beam.compile_forms(fs))]\n-\n-  def exports(Vec(Func)) Vec(Tuple)\n-  def exports([]) := []\n-  def exports([Func(name, arity, _) | rest]) := [{Prim.str_to_atom(name), arity} | exports(rest)]\n-\n-  # ── stage 1: lex (String -> selfhost_decl Tok, with significant newlines) ──\n-  pub def lex(s String) Vec(Tok) := lex_chars(Prim.str_chars(s))\n-\n-  def lex_chars(Vec(Int53)) Vec(Tok)\n-  def lex_chars([]) := []\n-  def lex_chars([c | rest])\n-    if c == 32 or c == 13 do\n-      lex_chars(rest)\n-    else\n-      lex_nl_or(c, rest)\n-    end\n-  end\n-\n-  # newline is a SIGNIFICANT token (TNL) — selfhost_decl splits declarations on it.\n-  def lex_nl_or(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def lex_nl_or(c, rest)\n-    if c == 10 do\n-      [TNL | lex_chars(rest)]\n-    else\n-      classify(c, rest)\n-    end\n-  end\n-\n-  def classify(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def classify(c, rest)\n-    if is_digit(c) do\n-      take_num([c | rest], [])\n-    else\n-      lex_alpha_or_op(c, rest)\n-    end\n-  end\n-\n-  def lex_alpha_or_op(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def lex_alpha_or_op(c, rest)\n-    if is_alpha(c) do\n-      take_id([c | rest], [])\n-    else\n-      take_op([c | rest])\n-    end\n-  end\n-\n-  def take_num(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_num([], acc) := [TNum(text(acc))]\n-  def take_num([c | rest], acc)\n-    if is_digit(c) do\n-      take_num(rest, [c | acc])\n-    else\n-      [TNum(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  def take_id(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_id([], acc) := [id_tok(text(acc))]\n-  def take_id([c | rest], acc)\n-    if is_alnum(c) do\n-      take_id(rest, [c | acc])\n-    else\n-      [id_tok(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  # `def`/`pub` are keywords; `_` is an identifier (selfhost_decl maps TId(\"_\") to\n-  # the wildcard pattern); everything else is an identifier.\n-  def id_tok(String) Tok\n-  def id_tok(\"def\") := TKw(\"def\")\n-  def id_tok(\"pub\") := TKw(\"pub\")\n-  def id_tok(s) := TId(s)\n-\n-  # `:=` then punctuation; `[`/`]` for lists, `|` (the cons bar) falls to TOp.\n-  def take_op(Vec(Int53)) Vec(Tok)\n-  def take_op([58, 61 | rest]) := [TOp(\":=\") | lex_chars(rest)]\n-  def take_op([44 | rest]) := [TComma | lex_chars(rest)]\n-  def take_op([40 | rest]) := [TLP | lex_chars(rest)]\n-  def take_op([41 | rest]) := [TRP | lex_chars(rest)]\n-  def take_op([91 | rest]) := [TLBracket | lex_chars(rest)]\n-  def take_op([93 | rest]) := [TRBracket | lex_chars(rest)]\n-  def take_op([c | rest]) := [TOp(Prim.str_from_chars([c])) | lex_chars(rest)]\n-\n-  def text(acc Vec(Int53)) String := Prim.str_from_chars(rev_codes(acc, []))\n-\n-  def rev_codes(Vec(Int53), Vec(Int53)) Vec(Int53)\n-  def rev_codes([], out) := out\n-  def rev_codes([c | rest], out) := rev_codes(rest, [c | out])\n-\n-  def is_digit(c Int53) Bool := c >= 48 and c <= 57\n-  def is_alpha(c Int53) Bool := (c >= 65 and c <= 90) or (c >= 97 and c <= 122) or c == 95\n-  def is_alnum(c Int53) Bool := is_alpha(c) or is_digit(c)\n-\n-  # ── normalize: selfhost_decl's Decl IR -> a flat list of (name, pats, body) ──\n-  # only function clauses matter; DSig/DType/DStruct/DMod/guarded clauses are skipped\n-  # (the corpus has none). `p`/`ds` are untyped — the cross-module values are raw\n-  # tuples/atoms the driver pattern-matches directly.\n-  def normalize(ds) Vec(NClause)\n-  def normalize([]) := []\n-  def normalize([{:d_clause, name, pats, expr} | rest]) := [NC(name, lower_pats(pats), lower_surface(expr)) | normalize(rest)]\n-  def normalize([_ | rest]) := normalize(rest)\n-\n-  # ── the surface→Core lowering: selfhost_decl Pat/Expr -> selfhost_beam Pat/Core ──\n-  def lower_pats(ps) Vec(Pat)\n-  def lower_pats([]) := []\n-  def lower_pats([p | rest]) := [lower_pat(p) | lower_pats(rest)]\n-\n-  def lower_pat(p) Pat\n-  def lower_pat(:wild) := PWild\n-  def lower_pat(:nil_p) := PList([], PtlClose)\n-  def lower_pat({:var, n}) := PVar(n)\n-  def lower_pat({:lit, k}) := PInt(k)\n-  def lower_pat({:ctor, c, ps}) := PCtor(c, lower_pats(ps))\n-  def lower_pat({:cons_p, h, t}) := cons_pat([lower_pat(h)], t)\n-\n-  # flatten a cons-pattern chain `[a, b | tl]` into PList([a, b], tail).\n-  def cons_pat(acc Vec(Pat), t) Pat\n-  def cons_pat(acc, :nil_p) := PList(rev_pats(acc, []), PtlClose)\n-  def cons_pat(acc, {:cons_p, h, t}) := cons_pat([lower_pat(h) | acc], t)\n-  def cons_pat(acc, other) := PList(rev_pats(acc, []), PtlTail(lower_pat(other)))\n-\n-  def rev_pats(Vec(Pat), Vec(Pat)) Vec(Pat)\n-  def rev_pats([], out) := out\n-  def rev_pats([p | rest], out) := rev_pats(rest, [p | out])\n-\n-  # expressions: `{:num,s}` `{:id,x}` `{:bin,op,l,r}` `{:unary,op,x}`\n-  # `{:call,{:id,f},args}` — the subset selfhost_decl bodies produce here.\n-  def lower_surface(e) Core\n-  def lower_surface({:num, s}) := CNum(s)\n-  def lower_surface({:id, x}) := CId(x)\n-  def lower_surface({:bin, op, l, r}) := CBin(op, lower_surface(l), lower_surface(r))\n-  def lower_surface({:unary, op, x}) := CUnary(op, lower_surface(x))\n-  def lower_surface({:call, callee, args}) := CCall(callee_name(callee), lower_all(args))\n-\n-  def callee_name(c) String\n-  def callee_name({:id, f}) := f\n-\n-  def lower_all(es) Vec(Core)\n-  def lower_all([]) := []\n-  def lower_all([e | rest]) := [lower_surface(e) | lower_all(rest)]\n-\n-  # ── group consecutive same-name clauses, wrap each as a beam Func ──\n-  def group(Vec(NClause)) Vec(Grp)\n-  def group([]) := []\n-  def group([NC(name, pats, body) | rest]) := group_run(name, plen(pats), [NC(name, pats, body)], rest)\n-\n-  def group_run(name String, arity Int53, acc Vec(NClause), Vec(NClause)) Vec(Grp)\n-  def group_run(name, arity, acc, []) := [Grp(name, arity, rev_ncs(acc, []))]\n-  def group_run(name, arity, acc, [NC(n2, p2, b2) | rest])\n-    if name == n2 do\n-      group_run(name, arity, [NC(n2, p2, b2) | acc], rest)\n-    else\n-      [Grp(name, arity, rev_ncs(acc, [])) | group([NC(n2, p2, b2) | rest])]\n-    end\n-  end\n-\n-  def rev_ncs(Vec(NClause), Vec(NClause)) Vec(NClause)\n-  def rev_ncs([], out) := out\n-  def rev_ncs([c | rest], out) := rev_ncs(rest, [c | out])\n-\n-  def funcs(Vec(Grp)) Vec(Func)\n-  def funcs([]) := []\n-  def funcs([Grp(name, arity, ncs) | rest]) := [Func(name, arity, clauses_of(ncs)) | funcs(rest)]\n-\n-  def clauses_of(Vec(NClause)) Vec(Clause)\n-  def clauses_of([]) := []\n-  def clauses_of([NC(_, pats, body) | rest]) := [Clause(pats, GNone, Block([SExpr(body)])) | clauses_of(rest)]\n-\n-  def plen(Vec(Pat)) Int53\n-  def plen([]) := 0\n-  def plen([_ | rest]) := 1 + plen(rest)\n-\n-  # ── inflate selfhost_beam's Form sum -> real abstract-form tuples ──\n-  # (rung 7's inflater, extended with FNil/FCons for list patterns).\n-  def inflate_funcs(Vec(FFunc)) Vec(Tuple)\n-  def inflate_funcs([]) := []\n-  def inflate_funcs([f | rest]) := [inflate_func(f) | inflate_funcs(rest)]\n-\n-  def inflate_func(FFunc) Tuple\n-  def inflate_func(FFunc(name, arity, clauses)) := {:function, 0, Prim.str_to_atom(name), arity, inflate_clauses(clauses)}\n-\n-  def inflate_clauses(Vec(FClause)) Vec(Tuple)\n-  def inflate_clauses([]) := []\n-  def inflate_clauses([c | rest]) := [inflate_clause(c) | inflate_clauses(rest)]\n-\n-  def inflate_clause(FClause) Tuple\n-  def inflate_clause(FClause(pats, guard, body)) := {:clause, 0, inflate_all(pats), guard_seq(guard), inflate_all(body)}\n-\n-  def guard_seq(GForm) Vec(Vec(Tuple))\n-  def guard_seq(GfNone) := []\n-  def guard_seq(GfSome(g)) := [[inflate(g)]]\n-\n-  def inflate_all(Vec(Form)) Vec(Tuple)\n-  def inflate_all([]) := []\n-  def inflate_all([f | rest]) := [inflate(f) | inflate_all(rest)]\n-\n-  def inflate(Form) Tuple\n-  def inflate(FInt(v)) := {:integer, 0, v}\n-  def inflate(FVar(n)) := {:var, 0, var_atom(n)}\n-  def inflate(FAtom(a)) := {:atom, 0, Prim.str_to_atom(a)}\n-  def inflate(FNil) := {:nil, 0}\n-  def inflate(FCons(h, t)) := {:cons, 0, inflate(h), inflate(t)}\n-  def inflate(FOp1(op, x)) := {:op, 0, op_atom(op), inflate(x)}\n-  def inflate(FOp2(op, l, r)) := {:op, 0, op_atom(op), inflate(l), inflate(r)}\n-  def inflate(FCall(f, args)) := {:call, 0, {:atom, 0, Prim.str_to_atom(f)}, inflate_all(args)}\n-\n-  def var_atom(n String) Symbol := Prim.str_to_atom(cap(n))\n-\n-  def op_atom(String) Symbol\n-  def op_atom(\"+\") := :\"+\"\n-  def op_atom(\"-\") := :\"-\"\n-  def op_atom(\"*\") := :\"*\"\n-\n-  def parse_int(s String) Int53 := digits(Prim.str_chars(s), 0)\n-  def digits(Vec(Int53), Int53) Int53\n-  def digits([], acc) := acc\n-  def digits([c | rest], acc) := digits(rest, acc * 10 + c - 48)\n-\n-  def cap(s String) String\n-    case Prim.str_chars(s) do\n-      [c | rest] -> Prim.str_from_chars([up(c) | rest])\n-      [] -> s\n-    end\n-  end\n-\n-  def up(c Int53) Int53\n-    if c >= 97 and c <= 122 do\n-      c - 32\n-    else\n-      c\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_decl.rian","sha":"05b68982ab14a9bf2f09fdd8f11d4dab81a67af6","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_front.rian","changes":374,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/compiler%2Fcompose_real_front.rian?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":374,"filename":"compiler/compose_real_front.rian","patch":"@@ -1,374 +0,0 @@\n-# ===========================================================================\n-# SELF-HOSTING — COMPOSITION rung 8: wire the VERIFIED parser AND backend in\n-# ===========================================================================\n-# Rung 7 made the driver's BACKEND the equivalence-locked `selfhost_beam` port.\n-# This rung does the same for the FRONT-END's hardest piece: clause bodies are\n-# now parsed by the equivalence-locked `selfhost_parse` port (the full Rian.Pratt\n-# grammar), called cross-module — not by a toy expression parser.\n-#\n-#   lex(src) → driver decl-parse (def head + clause split)\n-#            → Parse.parse(body tokens)   ← the REAL parser (cross-module)\n-#            → lower its raw surface tuple → selfhost_beam Core\n-#            → Beam.compile_forms(funcs)  ← the REAL backend (cross-module)\n-#            → inflate Form → abstract-form tuples (in Rian)\n-#            → :compile.forms / :code.load_binary (the driver loop)\n-#\n-# TWO verified stages are now composed end to end, connected only by driver-local\n-# glue that itself contains no toy reimplementation of either: a lexer producing\n-# `selfhost_parse`'s exact `Tok`, a declaration splitter, and a raw-surface→Core\n-# lowering (`{:bin,op,l,r}` → `CBin`, `{:if,c,{:block,_},{:block,_}}` → `CIf`, …).\n-#\n-# Both `Parse` and `Beam` must be loaded under their\n-# `:\"Elixir.*\"` atoms so the Pascal-qualified calls resolve (ADR-0041);\n-# both are sibling self-host ports, so the calls are composition, not host crutches\n-# (excluded from the FFI ledger). The only host FFI is still :compile.forms /\n-# :code.load_binary.\n-#\n-# Scope: arithmetic + `if`/comparison/boolean over multi-clause defs — but the\n-# expression grammar is now the REAL one (real precedence, real surface). The\n-# remaining toy piece is the declaration layer (selfhost_decl is :partial).\n-# ---------------------------------------------------------------------------\n-\n-mod ComposeRealFront do\n-  # ── selfhost_parse's Tok (the subset the driver emits; tags must match) ──\n-  type Tok := TNum(String) | TId(String) | TKw(String) | TOp(String)\n-            | TLP | TRP | TComma\n-\n-  # ── selfhost_beam's INPUT IR (the shared contract — structurally identical) ──\n-  type Core := CNum(String)\n-             | CId(String)\n-             | CAtom(String)\n-             | CUnary(String, Core)\n-             | CBin(String, Core, Core)\n-             | CCall(String, Vec(Core))\n-             | CTuple(Vec(Core))\n-             | CList(Vec(Core), Tail)\n-             | CIf(Core, Block, Block)\n-             | CCase(Core, Vec(Arm))\n-\n-  type Tail := TClose | TTail(Core)\n-  type Block := Block(Vec(Stmt))\n-  type Stmt := SBind(String, Core) | STypedBind(String, String, Core) | SExpr(Core)\n-  type Arm := Arm(Pat, Guard, Block)\n-\n-  type Pat := PWild\n-            | PVar(String)\n-            | PInt(Int53)\n-            | PAtom(String)\n-            | PCtor(String, Vec(Pat))\n-            | PTuple(Vec(Pat))\n-            | PList(Vec(Pat), PTail)\n-\n-  type PTail := PtlClose | PtlTail(Pat)\n-  type Guard := GNone | GSome(Core)\n-  type Clause := Clause(Vec(Pat), Guard, Block)\n-  type Func := Func(String, Int53, Vec(Clause))\n-\n-  # ── selfhost_beam's OUTPUT (the string-carrying Form sum we inflate) ──\n-  type Form := FInt(Int53)\n-             | FVar(String)\n-             | FAtom(String)\n-             | FCtorN(String)\n-             | FCtor(String, Vec(Form))\n-             | FOp1(String, Form)\n-             | FOp2(String, Form, Form)\n-             | FCall(String, Vec(Form))\n-             | FTuple(Vec(Form))\n-             | FNil\n-             | FCons(Form, Form)\n-             | FIf(Form, Vec(Form), Vec(Form))\n-             | FCase(Form, Vec(FClause))\n-             | FMatch(String, Form)\n-\n-  type GForm := GfNone | GfSome(Form)\n-  type FClause := FClause(Vec(Form), GForm, Vec(Form))\n-  type FFunc := FFunc(String, Int53, Vec(FClause))\n-\n-  # ── driver-local types ──\n-  type Decl := Def(String, Vec(Pat), Core)\n-  type Grp := Grp(String, Int53, Vec(Decl))\n-  type CR := CR(Decl, Vec(Tok))\n-  type BR := BR(Vec(Tok), Vec(Tok))\n-\n-  # ── the BEAM toolchain boundary: two @external(:ex) host calls (counted FFI) ──\n-  @external(:ex, \":compile.forms(forms, [:return_errors])\")\n-  def compile_forms_host(forms Vec(Tuple)) Tuple\n-\n-  @external(:ex, \":code.load_binary(name, chars, bin)\")\n-  def load_binary(name Symbol, chars Vec(Int53), bin String) Tuple\n-\n-  # ── the driver: source -> a loaded, runnable module ──\n-  pub def build(src String, modname Symbol) Symbol\n-  pub def build(src, modname)\n-    case compile_forms_host(compile_module(src, modname)) do\n-      {:ok, m, bin} -> loaded(load_binary(m, Prim.str_chars(\"nofile\"), bin))\n-    end\n-  end\n-\n-  def loaded(Tuple) Symbol\n-  def loaded({:module, m}) := m\n-\n-  # ── source -> a full :compile.forms input, via the REAL parser + backend ──\n-  pub def compile_module(src String, modname Symbol) Vec(Tuple)\n-  pub def compile_module(src, modname) := assemble(modname, funcs(group(parse_clauses(lex(src)))))\n-\n-  def assemble(modname Symbol, fs Vec(Func)) Vec(Tuple) := [{:attribute, 0, :module, modname}, {:attribute, 0, :export, exports(fs)} | inflate_funcs(Beam.compile_forms(fs))]\n-\n-  def exports(Vec(Func)) Vec(Tuple)\n-  def exports([]) := []\n-  def exports([Func(name, arity, _) | rest]) := [{Prim.str_to_atom(name), arity} | exports(rest)]\n-\n-  # ── stage 1: lex (String -> selfhost_parse Tok) ──\n-  pub def lex(s String) Vec(Tok) := lex_chars(Prim.str_chars(s))\n-\n-  def lex_chars(Vec(Int53)) Vec(Tok)\n-  def lex_chars([]) := []\n-  def lex_chars([c | rest])\n-    if c == 32 or c == 10 or c == 13 do\n-      lex_chars(rest)\n-    else\n-      classify(c, rest)\n-    end\n-  end\n-\n-  def classify(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def classify(c, rest)\n-    if is_digit(c) do\n-      take_num([c | rest], [])\n-    else\n-      lex_alpha_or_op(c, rest)\n-    end\n-  end\n-\n-  def lex_alpha_or_op(c Int53, rest Vec(Int53)) Vec(Tok)\n-  def lex_alpha_or_op(c, rest)\n-    if is_alpha(c) do\n-      take_id([c | rest], [])\n-    else\n-      take_op([c | rest])\n-    end\n-  end\n-\n-  def take_num(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_num([], acc) := [TNum(text(acc))]\n-  def take_num([c | rest], acc)\n-    if is_digit(c) do\n-      take_num(rest, [c | acc])\n-    else\n-      [TNum(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  def take_id(Vec(Int53), Vec(Int53)) Vec(Tok)\n-  def take_id([], acc) := [id_tok(text(acc))]\n-  def take_id([c | rest], acc)\n-    if is_alnum(c) do\n-      take_id(rest, [c | acc])\n-    else\n-      [id_tok(text(acc)) | lex_chars([c | rest])]\n-    end\n-  end\n-\n-  # `def`/`if`/`do`/`else`/`end` are keywords (TKw); `and`/`or` are operators (TOp,\n-  # per selfhost_parse's level table); everything else is an identifier.\n-  def id_tok(String) Tok\n-  def id_tok(\"def\") := TKw(\"def\")\n-  def id_tok(\"if\") := TKw(\"if\")\n-  def id_tok(\"do\") := TKw(\"do\")\n-  def id_tok(\"else\") := TKw(\"else\")\n-  def id_tok(\"end\") := TKw(\"end\")\n-  def id_tok(\"and\") := TOp(\"and\")\n-  def id_tok(\"or\") := TOp(\"or\")\n-  def id_tok(s) := TId(s)\n-\n-  def take_op(Vec(Int53)) Vec(Tok)\n-  def take_op([58, 61 | rest]) := [TOp(\":=\") | lex_chars(rest)]\n-  def take_op([61, 61 | rest]) := [TOp(\"==\") | lex_chars(rest)]\n-  def take_op([62, 61 | rest]) := [TOp(\">=\") | lex_chars(rest)]\n-  def take_op([60, 61 | rest]) := [TOp(\"<=\") | lex_chars(rest)]\n-  def take_op([44 | rest]) := [TComma | lex_chars(rest)]\n-  def take_op([40 | rest]) := [TLP | lex_chars(rest)]\n-  def take_op([41 | rest]) := [TRP | lex_chars(rest)]\n-  def take_op([c | rest]) := [TOp(Prim.str_from_chars([c])) | lex_chars(rest)]\n-\n-  def text(acc Vec(Int53)) String := Prim.str_from_chars(rev_codes(acc, []))\n-\n-  def rev_codes(Vec(Int53), Vec(Int53)) Vec(Int53)\n-  def rev_codes([], out) := out\n-  def rev_codes([c | rest], out) := rev_codes(rest, [c | out])\n-\n-  def is_digit(c Int53) Bool := c >= 48 and c <= 57\n-  def is_alpha(c Int53) Bool := (c >= 65 and c <= 90) or (c >= 97 and c <= 122) or c == 95\n-  def is_alnum(c Int53) Bool := is_alpha(c) or is_digit(c)\n-\n-  # ── stage 2: parse declarations; delegate each body to the REAL parser ──\n-  pub def parse_clauses(Vec(Tok)) Vec(Decl)\n-  pub def parse_clauses([]) := []\n-  pub def parse_clauses(ts)\n-    case parse_one(ts) do\n-      CR(d, rest) -> [d | parse_clauses(rest)]\n-    end\n-  end\n-\n-  # consume `def NAME ( pats ) :=`, then the body up to the next `def`.\n-  def parse_one(Vec(Tok)) CR\n-  def parse_one([_ | rest])\n-    case rest do\n-      [TId(name) | r2] -> parse_head(name, r2)\n-    end\n-  end\n-\n-  def parse_head(name String, Vec(Tok)) CR\n-  def parse_head(name, [TLP | rest]) := collect_pats(name, [], rest)\n-\n-  def collect_pats(name String, acc Vec(Pat), Vec(Tok)) CR\n-  def collect_pats(name, acc, [TRP | rest]) := finish_clause(name, rev_pats(acc, []), rest)\n-  def collect_pats(name, acc, [TComma | rest]) := collect_pats(name, acc, rest)\n-  def collect_pats(name, acc, [TNum(n) | rest]) := collect_pats(name, [PInt(parse_int(n)) | acc], rest)\n-  def collect_pats(name, acc, [TId(p) | rest]) := collect_pats(name, [PVar(p) | acc], rest)\n-\n-  # after `)` comes `:=`; the body is everything up to the next `def`. The body\n-  # token slice is handed to the REAL parser, whose raw surface is lowered to Core.\n-  def finish_clause(name String, pats Vec(Pat), Vec(Tok)) CR\n-  def finish_clause(name, pats, [_ | rest])\n-    case take_body(rest, []) do\n-      BR(body, leftover) -> CR(Def(name, pats, lower_surface(Parse.parse(body))), leftover)\n-    end\n-  end\n-\n-  # collect body tokens until the next `def` keyword (kept out of the body).\n-  def take_body(Vec(Tok), Vec(Tok)) BR\n-  def take_body([], acc) := BR(rev_toks(acc, []), [])\n-  def take_body([TKw(\"def\") | rest], acc) := BR(rev_toks(acc, []), [TKw(\"def\") | rest])\n-  def take_body([t | rest], acc) := take_body(rest, [t | acc])\n-\n-  def rev_toks(Vec(Tok), Vec(Tok)) Vec(Tok)\n-  def rev_toks([], out) := out\n-  def rev_toks([t | rest], out) := rev_toks(rest, [t | out])\n-\n-  def rev_pats(Vec(Pat), Vec(Pat)) Vec(Pat)\n-  def rev_pats([], out) := out\n-  def rev_pats([p | rest], out) := rev_pats(rest, [p | out])\n-\n-  # ── lower the REAL parser's raw surface tuples -> selfhost_beam Core ──\n-  # `{:num,s}` `{:id,x}` `{:bin,op,l,r}` `{:call,{:id,f},args}`\n-  # `{:if,c,{:block,_},{:block,_}}` — the subset the corpus produces.\n-  def lower_surface(Tuple) Core\n-  def lower_surface({:num, s}) := CNum(s)\n-  def lower_surface({:id, x}) := CId(x)\n-  def lower_surface({:bin, op, l, r}) := CBin(op, lower_surface(l), lower_surface(r))\n-  def lower_surface({:call, callee, args}) := CCall(callee_name(callee), lower_all(args))\n-  def lower_surface({:\"if\", c, t, e}) := CIf(lower_surface(c), lower_block(t), lower_block(e))\n-\n-  def callee_name(Tuple) String\n-  def callee_name({:id, f}) := f\n-\n-  def lower_all(Vec(Tuple)) Vec(Core)\n-  def lower_all([]) := []\n-  def lower_all([e | rest]) := [lower_surface(e) | lower_all(rest)]\n-\n-  def lower_block(Tuple) Block\n-  def lower_block({:block, stmts}) := Block(lower_stmts(stmts))\n-\n-  def lower_stmts(Vec(Tuple)) Vec(Stmt)\n-  def lower_stmts([]) := []\n-  def lower_stmts([{:expr, e} | rest]) := [SExpr(lower_surface(e)) | lower_stmts(rest)]\n-  def lower_stmts([{:bind, n, e} | rest]) := [SBind(n, lower_surface(e)) | lower_stmts(rest)]\n-\n-  # ── group clauses, wrap each group as a beam Func (as in rung 7) ──\n-  def group(Vec(Decl)) Vec(Grp)\n-  def group([]) := []\n-  def group([Def(name, pats, body) | rest]) := group_run(name, plen(pats), [Def(name, pats, body)], rest)\n-\n-  def group_run(name String, arity Int53, acc Vec(Decl), Vec(Decl)) Vec(Grp)\n-  def group_run(name, arity, acc, []) := [Grp(name, arity, rev_decls(acc, []))]\n-  def group_run(name, arity, acc, [Def(n2, p2, b2) | rest])\n-    if name == n2 do\n-      group_run(name, arity, [Def(n2, p2, b2) | acc], rest)\n-    else\n-      [Grp(name, arity, rev_decls(acc, [])) | group([Def(n2, p2, b2) | rest])]\n-    end\n-  end\n-\n-  def rev_decls(Vec(Decl), Vec(Decl)) Vec(Decl)\n-  def rev_decls([], out) := out\n-  def rev_decls([d | rest], out) := rev_decls(rest, [d | out])\n-\n-  def funcs(Vec(Grp)) Vec(Func)\n-  def funcs([]) := []\n-  def funcs([Grp(name, arity, decls) | rest]) := [Func(name, arity, clauses_of(decls)) | funcs(rest)]\n-\n-  def clauses_of(Vec(Decl)) Vec(Clause)\n-  def clauses_of([]) := []\n-  def clauses_of([Def(_, pats, body) | rest]) := [Clause(pats, GNone, Block([SExpr(body)])) | clauses_of(rest)]\n-\n-  def plen(Vec(Pat)) Int53\n-  def plen([]) := 0\n-  def plen([_ | rest]) := 1 + plen(rest)\n-\n-  # ── inflate selfhost_beam's Form sum -> real abstract-form tuples (as rung 7) ──\n-  def inflate_funcs(Vec(FFunc)) Vec(Tuple)\n-  def inflate_funcs([]) := []\n-  def inflate_funcs([f | rest]) := [inflate_func(f) | inflate_funcs(rest)]\n-\n-  def inflate_func(FFunc) Tuple\n-  def inflate_func(FFunc(name, arity, clauses)) := {:function, 0, Prim.str_to_atom(name), arity, inflate_clauses(clauses)}\n-\n-  def inflate_clauses(Vec(FClause)) Vec(Tuple)\n-  def inflate_clauses([]) := []\n-  def inflate_clauses([c | rest]) := [inflate_clause(c) | inflate_clauses(rest)]\n-\n-  def inflate_clause(FClause) Tuple\n-  def inflate_clause(FClause(pats, guard, body)) := {:clause, 0, inflate_all(pats), guard_seq(guard), inflate_all(body)}\n-\n-  def guard_seq(GForm) Vec(Vec(Tuple))\n-  def guard_seq(GfNone) := []\n-  def guard_seq(GfSome(g)) := [[inflate(g)]]\n-\n-  def inflate_all(Vec(Form)) Vec(Tuple)\n-  def inflate_all([]) := []\n-  def inflate_all([f | rest]) := [inflate(f) | inflate_all(rest)]\n-\n-  def inflate(Form) Tuple\n-  def inflate(FInt(v)) := {:integer, 0, v}\n-  def inflate(FVar(n)) := {:var, 0, var_atom(n)}\n-  def inflate(FAtom(a)) := {:atom, 0, Prim.str_to_atom(a)}\n-  def inflate(FOp1(op, x)) := {:op, 0, op_atom(op), inflate(x)}\n-  def inflate(FOp2(op, l, r)) := {:op, 0, op_atom(op), inflate(l), inflate(r)}\n-  def inflate(FCall(f, args)) := {:call, 0, {:atom, 0, Prim.str_to_atom(f)}, inflate_all(args)}\n-  def inflate(FIf(c, t, e)) := {:\"case\", 0, inflate(c), [{:clause, 0, [{:atom, 0, :true}], [], inflate_all(t)}, {:clause, 0, [{:atom, 0, :false}], [], inflate_all(e)}]}\n-\n-  def var_atom(n String) Symbol := Prim.str_to_atom(cap(n))\n-\n-  def op_atom(String) Symbol\n-  def op_atom(\"+\") := :\"+\"\n-  def op_atom(\"-\") := :\"-\"\n-  def op_atom(\"*\") := :\"*\"\n-  def op_atom(\"==\") := :\"==\"\n-  def op_atom(\"<\") := :\"<\"\n-  def op_atom(\">\") := :\">\"\n-  def op_atom(\">=\") := :\">=\"\n-  def op_atom(\"<=\") := :\"=<\"\n-  def op_atom(\"and\") := :andalso\n-  def op_atom(\"or\") := :orelse\n-\n-  def parse_int(s String) Int53 := digits(Prim.str_chars(s), 0)\n-  def digits(Vec(Int53), Int53) Int53\n-  def digits([], acc) := acc\n-  def digits([c | rest], acc) := digits(rest, acc * 10 + c - 48)\n-\n-  def cap(s String) String\n-    case Prim.str_chars(s) do\n-      [c | rest] -> Prim.str_from_chars([up(c) | rest])\n-      [] -> s\n-    end\n-  end\n-\n-  def up(c Int53) Int53\n-    if c >= 97 and c <= 122 do\n-      c - 32\n-    else\n-      c\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_front.rian","sha":"b80ccc5e5c0cb4f4289a2a955c99c3a205a24dcd","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_lex.rian","changes":244,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/compiler%2Fcompose_real_lex.rian?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":244,"filename":"compiler/compose_real_lex.rian","patch":"@@ -1,244 +0,0 @@\n-# ===========================================================================\n-# SELF-HOSTING — COMPOSITION rung 10: the WHOLE front-end is verified ports\n-# ===========================================================================\n-# Rung 9 parsed declarations with the verified `selfhost_decl` port but still\n-# tokenized with a driver-local lexer. This rung replaces that last front-end\n-# glue with the equivalence-locked `selfhost_lexer_v2` port. The driver now owns\n-# NO part of lexing or parsing — the full front-end is verified ports, called\n-# cross-module, and so is the backend:\n-#\n-#   LexerV2.tokenize(src)        ← the REAL lexer        (cross-module)\n-#     → Decl.parse_program(toks) ← the REAL decl parser  (cross-module)\n-#     → normalize/group + surface→Core lowering   (driver glue — lowers, doesn't parse)\n-#     → Beam.compile_forms(funcs)← the REAL backend      (cross-module)\n-#     → inflate Form → abstract-form tuples (driver glue)\n-#     → :compile.forms / :code.load_binary\n-#\n-# THREE verified ports (lexer, declaration parser, backend) composed end to end,\n-# with identical token tags across the lexer→parser boundary (no projection): the\n-# lexer's Tok sum is a superset of the parser's, same tags. The only driver-local\n-# code is the surface→Core lowering and the Form inflater — neither reimplements a\n-# verified stage.\n-#\n-# Surface: selfhost_decl's slice (arithmetic + calls + multi-clause patterns incl.\n-# cons-LISTS; no `if`/strings/sum-types yet — widening selfhost_decl off :partial\n-# is the path to compiling a real selfhost_*.rian slice / the v1==v2 fixed point).\n-# All three ports load under their :\"Elixir.*\" atoms (Pascal calls,\n-# ADR-0041); sibling-port calls are composition, not host crutches (excluded from\n-# the FFI ledger). Only host FFI: :compile.forms / :code.load_binary.\n-# ---------------------------------------------------------------------------\n-\n-mod ComposeRealLex do\n-  # ── selfhost_beam's INPUT IR (the shared contract — structurally identical) ──\n-  type Core := CNum(String)\n-             | CId(String)\n-             | CAtom(String)\n-             | CUnary(String, Core)\n-             | CBin(String, Core, Core)\n-             | CCall(String, Vec(Core))\n-             | CTuple(Vec(Core))\n-             | CList(Vec(Core), Tail)\n-             | CIf(Core, Block, Block)\n-             | CCase(Core, Vec(Arm))\n-\n-  type Tail := TClose | TTail(Core)\n-  type Block := Block(Vec(Stmt))\n-  type Stmt := SBind(String, Core) | STypedBind(String, String, Core) | SExpr(Core)\n-  type Arm := Arm(Pat, Guard, Block)\n-\n-  type Pat := PWild\n-            | PVar(String)\n-            | PInt(Int53)\n-            | PAtom(String)\n-            | PCtor(String, Vec(Pat))\n-            | PTuple(Vec(Pat))\n-            | PList(Vec(Pat), PTail)\n-\n-  type PTail := PtlClose | PtlTail(Pat)\n-  type Guard := GNone | GSome(Core)\n-  type Clause := Clause(Vec(Pat), Guard, Block)\n-  type Func := Func(String, Int53, Vec(Clause))\n-\n-  # ── selfhost_beam's OUTPUT (the string-carrying Form sum we inflate) ──\n-  type Form := FInt(Int53)\n-             | FVar(String)\n-             | FAtom(String)\n-             | FCtorN(String)\n-             | FCtor(String, Vec(Form))\n-             | FOp1(String, Form)\n-             | FOp2(String, Form, Form)\n-             | FCall(String, Vec(Form))\n-             | FTuple(Vec(Form))\n-             | FNil\n-             | FCons(Form, Form)\n-             | FIf(Form, Vec(Form), Vec(Form))\n-             | FCase(Form, Vec(FClause))\n-             | FMatch(String, Form)\n-\n-  type GForm := GfNone | GfSome(Form)\n-  type FClause := FClause(Vec(Form), GForm, Vec(Form))\n-  type FFunc := FFunc(String, Int53, Vec(FClause))\n-\n-  # ── driver-local types ──\n-  type NClause := NC(String, Vec(Pat), Core)\n-  type Grp := Grp(String, Int53, Vec(NClause))\n-\n-  # ── the BEAM toolchain boundary: two @external(:ex) host calls (counted FFI) ──\n-  @external(:ex, \":compile.forms(forms, [:return_errors])\")\n-  def compile_forms_host(forms Vec(Tuple)) Tuple\n-\n-  @external(:ex, \":code.load_binary(name, chars, bin)\")\n-  def load_binary(name Symbol, chars Vec(Int53), bin String) Tuple\n-\n-  # ── the driver: source -> a loaded, runnable module ──\n-  pub def build(src String, modname Symbol) Symbol\n-  pub def build(src, modname)\n-    case compile_forms_host(compile_module(src, modname)) do\n-      {:ok, m, bin} -> loaded(load_binary(m, Prim.str_chars(\"nofile\"), bin))\n-    end\n-  end\n-\n-  def loaded(Tuple) Symbol\n-  def loaded({:module, m}) := m\n-\n-  # ── source -> a full :compile.forms input, via the REAL lexer + parser + backend ──\n-  # the front-end is entirely verified ports: LexerV2.tokenize feeds\n-  # Decl.parse_program directly (same token tags, no projection).\n-  pub def compile_module(src String, modname Symbol) Vec(Tuple)\n-  pub def compile_module(src, modname) := assemble(modname, funcs(group(normalize(Decl.parse_program(LexerV2.tokenize(src))))))\n-\n-  def assemble(modname Symbol, fs Vec(Func)) Vec(Tuple) := [{:attribute, 0, :module, modname}, {:attribute, 0, :export, exports(fs)} | inflate_funcs(Beam.compile_forms(fs))]\n-\n-  def exports(Vec(Func)) Vec(Tuple)\n-  def exports([]) := []\n-  def exports([Func(name, arity, _) | rest]) := [{Prim.str_to_atom(name), arity} | exports(rest)]\n-\n-  # ── normalize: selfhost_decl's Decl IR -> a flat list of (name, pats, body) ──\n-  def normalize(ds) Vec(NClause)\n-  def normalize([]) := []\n-  def normalize([{:d_clause, name, pats, expr} | rest]) := [NC(name, lower_pats(pats), lower_surface(expr)) | normalize(rest)]\n-  def normalize([_ | rest]) := normalize(rest)\n-\n-  # ── the surface→Core lowering: selfhost_decl Pat/Expr -> selfhost_beam Pat/Core ──\n-  def lower_pats(ps) Vec(Pat)\n-  def lower_pats([]) := []\n-  def lower_pats([p | rest]) := [lower_pat(p) | lower_pats(rest)]\n-\n-  def lower_pat(p) Pat\n-  def lower_pat(:wild) := PWild\n-  def lower_pat(:nil_p) := PList([], PtlClose)\n-  def lower_pat({:var, n}) := PVar(n)\n-  def lower_pat({:lit, k}) := PInt(k)\n-  def lower_pat({:ctor, c, ps}) := PCtor(c, lower_pats(ps))\n-  def lower_pat({:cons_p, h, t}) := cons_pat([lower_pat(h)], t)\n-\n-  # flatten a cons-pattern chain `[a, b | tl]` into PList([a, b], tail).\n-  def cons_pat(acc Vec(Pat), t) Pat\n-  def cons_pat(acc, :nil_p) := PList(rev_pats(acc, []), PtlClose)\n-  def cons_pat(acc, {:cons_p, h, t}) := cons_pat([lower_pat(h) | acc], t)\n-  def cons_pat(acc, other) := PList(rev_pats(acc, []), PtlTail(lower_pat(other)))\n-\n-  def rev_pats(Vec(Pat), Vec(Pat)) Vec(Pat)\n-  def rev_pats([], out) := out\n-  def rev_pats([p | rest], out) := rev_pats(rest, [p | out])\n-\n-  def lower_surface(e) Core\n-  def lower_surface({:num, s}) := CNum(s)\n-  def lower_surface({:id, x}) := CId(x)\n-  def lower_surface({:bin, op, l, r}) := CBin(op, lower_surface(l), lower_surface(r))\n-  def lower_surface({:unary, op, x}) := CUnary(op, lower_surface(x))\n-  def lower_surface({:call, callee, args}) := CCall(callee_name(callee), lower_all(args))\n-\n-  def callee_name(c) String\n-  def callee_name({:id, f}) := f\n-\n-  def lower_all(es) Vec(Core)\n-  def lower_all([]) := []\n-  def lower_all([e | rest]) := [lower_surface(e) | lower_all(rest)]\n-\n-  # ── group consecutive same-name clauses, wrap each as a beam Func ──\n-  def group(Vec(NClause)) Vec(Grp)\n-  def group([]) := []\n-  def group([NC(name, pats, body) | rest]) := group_run(name, plen(pats), [NC(name, pats, body)], rest)\n-\n-  def group_run(name String, arity Int53, acc Vec(NClause), Vec(NClause)) Vec(Grp)\n-  def group_run(name, arity, acc, []) := [Grp(name, arity, rev_ncs(acc, []))]\n-  def group_run(name, arity, acc, [NC(n2, p2, b2) | rest])\n-    if name == n2 do\n-      group_run(name, arity, [NC(n2, p2, b2) | acc], rest)\n-    else\n-      [Grp(name, arity, rev_ncs(acc, [])) | group([NC(n2, p2, b2) | rest])]\n-    end\n-  end\n-\n-  def rev_ncs(Vec(NClause), Vec(NClause)) Vec(NClause)\n-  def rev_ncs([], out) := out\n-  def rev_ncs([c | rest], out) := rev_ncs(rest, [c | out])\n-\n-  def funcs(Vec(Grp)) Vec(Func)\n-  def funcs([]) := []\n-  def funcs([Grp(name, arity, ncs) | rest]) := [Func(name, arity, clauses_of(ncs)) | funcs(rest)]\n-\n-  def clauses_of(Vec(NClause)) Vec(Clause)\n-  def clauses_of([]) := []\n-  def clauses_of([NC(_, pats, body) | rest]) := [Clause(pats, GNone, Block([SExpr(body)])) | clauses_of(rest)]\n-\n-  def plen(Vec(Pat)) Int53\n-  def plen([]) := 0\n-  def plen([_ | rest]) := 1 + plen(rest)\n-\n-  # ── inflate selfhost_beam's Form sum -> real abstract-form tuples ──\n-  def inflate_funcs(Vec(FFunc)) Vec(Tuple)\n-  def inflate_funcs([]) := []\n-  def inflate_funcs([f | rest]) := [inflate_func(f) | inflate_funcs(rest)]\n-\n-  def inflate_func(FFunc) Tuple\n-  def inflate_func(FFunc(name, arity, clauses)) := {:function, 0, Prim.str_to_atom(name), arity, inflate_clauses(clauses)}\n-\n-  def inflate_clauses(Vec(FClause)) Vec(Tuple)\n-  def inflate_clauses([]) := []\n-  def inflate_clauses([c | rest]) := [inflate_clause(c) | inflate_clauses(rest)]\n-\n-  def inflate_clause(FClause) Tuple\n-  def inflate_clause(FClause(pats, guard, body)) := {:clause, 0, inflate_all(pats), guard_seq(guard), inflate_all(body)}\n-\n-  def guard_seq(GForm) Vec(Vec(Tuple))\n-  def guard_seq(GfNone) := []\n-  def guard_seq(GfSome(g)) := [[inflate(g)]]\n-\n-  def inflate_all(Vec(Form)) Vec(Tuple)\n-  def inflate_all([]) := []\n-  def inflate_all([f | rest]) := [inflate(f) | inflate_all(rest)]\n-\n-  def inflate(Form) Tuple\n-  def inflate(FInt(v)) := {:integer, 0, v}\n-  def inflate(FVar(n)) := {:var, 0, var_atom(n)}\n-  def inflate(FAtom(a)) := {:atom, 0, Prim.str_to_atom(a)}\n-  def inflate(FNil) := {:nil, 0}\n-  def inflate(FCons(h, t)) := {:cons, 0, inflate(h), inflate(t)}\n-  def inflate(FOp1(op, x)) := {:op, 0, op_atom(op), inflate(x)}\n-  def inflate(FOp2(op, l, r)) := {:op, 0, op_atom(op), inflate(l), inflate(r)}\n-  def inflate(FCall(f, args)) := {:call, 0, {:atom, 0, Prim.str_to_atom(f)}, inflate_all(args)}\n-\n-  def var_atom(n String) Symbol := Prim.str_to_atom(cap(n))\n-\n-  def op_atom(String) Symbol\n-  def op_atom(\"+\") := :\"+\"\n-  def op_atom(\"-\") := :\"-\"\n-  def op_atom(\"*\") := :\"*\"\n-\n-  def cap(s String) String\n-    case Prim.str_chars(s) do\n-      [c | rest] -> Prim.str_from_chars([up(c) | rest])\n-      [] -> s\n-    end\n-  end\n-\n-  def up(c Int53) Int53\n-    if c >= 97 and c <= 122 do\n-      c - 32\n-    else\n-      c\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/compiler%2Fcompose_real_lex.rian","sha":"e1bd1221e3f2b1a18c0013480b59a1329356936c","status":"removed"},{"additions":126,"blob_url":"https://github.com/wstein/rian-lab/blob/6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146/test%2Frian%2Fchecker_nummix_fixpoint_test.exs","changes":126,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fchecker_nummix_fixpoint_test.exs?ref=6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146","deletions":0,"filename":"test/rian/checker_nummix_fixpoint_test.exs","patch":"@@ -0,0 +1,126 @@\n+defmodule Rian.CheckerNumMixFixpointTest do\n+  # async: false — loads compiler/checker.rian into the VM via :code.load_binary.\n+  use ExUnit.Case, async: false\n+\n+  alias Rian.{Beam, Core, Check, Pratt}\n+\n+  # Self-hosting fixpoint (ADR-0063) for the type checker's FIRST ERROR SET — numeric\n+  # mix (ADR-0035). `compiler/checker.rian`'s inference (`infer_ic`) never rejects; it\n+  # infers `unknown` and moves on. `num_mix/3` is the first port of a Rian.Check\n+  # BOUNDARY check — the thing that turns the self-built compiler from self-COMPILING\n+  # into (partially) self-CHECKING: it returns `true` iff a clause body contains a\n+  # `+`/`-`/`*` whose two operands are concretely a different numeric KIND (one\n+  # int/uint, the other float) — a value never silently becomes a float.\n+  #\n+  # LOCK: for each corpus function, the port's `num_mix` (over the body's Core, the\n+  # clause's param env, an empty `ic`) must AGREE with whether the REAL\n+  # `Rian.Check.check/1` reports the numeric-mix error. The corpus uses param/literal\n+  # operands only, so the empty `ic` is irrelevant (an `ic`-dependent operand — a call\n+  # to a float-returning function — is the documented tail, where the port is merely\n+  # MORE conservative, never wrong). The reference's inference is itself already\n+  # equivalence-locked (checker_infer_fixpoint_test), so agreeing operand types compose\n+  # into an agreeing mix decision.\n+\n+  setup_all do\n+    {:ok, mod} = Beam.load(File.read!(\"compiler/checker.rian\"), :rian_checker_nummix_fixpoint)\n+    {:ok, mod: mod}\n+  end\n+\n+  # {label, body expression, param env, the full function (for the reference)}.\n+  # NEGATIVE = a provable int↔float mix → BOTH the port and Rian.Check must flag it.\n+  @negative [\n+    {\"Int + float-lit\", \"a + 1.5\", %{\"a\" => \"Int64\"}, \"def f(a Int64) Float64 := a + 1.5\"},\n+    {\"Int - float-lit\", \"a - 2.5\", %{\"a\" => \"Int64\"}, \"def f(a Int64) Float64 := a - 2.5\"},\n+    {\"Int * Float param\", \"a * b\", %{\"a\" => \"Int64\", \"b\" => \"Float64\"},\n+     \"def f(a Int64, b Float64) Float64 := a * b\"},\n+    {\"float-lit + Int\", \"1 + x\", %{\"x\" => \"Float64\"}, \"def f(x Float64) Float64 := 1 + x\"},\n+    {\"UInt * float-lit\", \"u * 2.0\", %{\"u\" => \"UInt32\"}, \"def f(u UInt32) Float64 := u * 2.0\"},\n+    {\"Char + float-lit\", \"c + 1.5\", %{\"c\" => \"Char\"}, \"def f(c Char) Float64 := c + 1.5\"},\n+    # the mix sits inside a comparison so the body is well-typed `Bool` (no return-type\n+    # error to fire first) — isolating the gate while proving the walk recurses.\n+    {\"mix nested in comparison\", \"(a + 1.5) > a\", %{\"a\" => \"Int64\"},\n+     \"def f(a Int64) Bool := (a + 1.5) > a\"}\n+  ]\n+\n+  # POSITIVE = NO provable mix → BOTH must pass. Covers same-kind arithmetic, non-arith\n+  # operators, conservative `unknown` operands, and the div/rem exclusion.\n+  @positive [\n+    {\"Int + Int\", \"a + b\", %{\"a\" => \"Int64\", \"b\" => \"Int64\"},\n+     \"def f(a Int64, b Int64) Int64 := a + b\"},\n+    {\"Float - Float\", \"a - b\", %{\"a\" => \"Float64\", \"b\" => \"Float64\"},\n+     \"def f(a Float64, b Float64) Float64 := a - b\"},\n+    {\"Int * int-lit\", \"a * 2\", %{\"a\" => \"Int64\"}, \"def f(a Int64) Int64 := a * 2\"},\n+    {\"Float + float-lit\", \"a + 1.0\", %{\"a\" => \"Float64\"}, \"def f(a Float64) Float64 := a + 1.0\"},\n+    {\"untyped operands (unknown)\", \"a + b\", %{}, \"def f(a, b) Int64 := a + b\"},\n+    {\"bare var, no arith\", \"a\", %{\"a\" => \"Int64\"}, \"def f(a Int64) Int64 := a\"},\n+    # div/rem are NOT @arith — an Int÷Float is never a num-mix error (ADR-0035 is about\n+    # the implicit-widening operators); the port's `mix_op` excludes them to match.\n+    {\"Int div int-lit\", \"a div 2\", %{\"a\" => \"Int64\"}, \"def f(a Int64) Int64 := a div 2\"}\n+  ]\n+\n+  defp inj(%Core.ENum{text: t}), do: {:c_num, t}\n+  defp inj(%Core.EStr{value: v}), do: {:c_str, v}\n+  defp inj(%Core.EChar{value: c}), do: {:c_char, c}\n+  defp inj(%Core.EId{name: n}), do: {:c_id, n}\n+  defp inj(%Core.EUnary{op: op, arg: a}), do: {:c_unary, op, inj(a)}\n+  defp inj(%Core.EBin{op: op, left: l, right: r}), do: {:c_bin, op, inj(l), inj(r)}\n+  defp inj(%Core.ECall{fun: %Core.EId{name: f}, args: as}), do: {:c_call, f, Enum.map(as, &inj/1)}\n+  defp inj(%Core.EIf{cond: c, then: t, else: e}), do: {:c_if, inj(c), inj(t), inj(e)}\n+  defp inj(%Core.EBlock{stmts: stmts}), do: {:c_block, Enum.map(stmts, &inj_stmt/1)}\n+  defp inj(%Core.ECase{scrut: s, arms: arms}), do: {:c_case, inj(s), Enum.map(arms, &inj_arm/1)}\n+\n+  defp inj(%Core.EList{elems: elems, tail: tail}),\n+    do: {:c_list, Enum.map(elems, &inj/1), inj_tail(tail)}\n+\n+  defp inj(%Core.ELambda{params: ps, body: body}),\n+    do: {:c_lambda, Enum.map(ps, &inj_param/1), inj(body)}\n+\n+  defp inj_tail(nil), do: :t_close\n+  defp inj_tail(:close), do: :t_close\n+  defp inj_tail(tail), do: {:t_cons, inj(tail)}\n+  defp inj_param({n, nil}), do: {:lp, n, :pt_none}\n+  defp inj_param({n, t}), do: {:lp, n, {:pt_some, t}}\n+  defp inj_stmt({:expr, e}), do: {:s_expr, inj(e)}\n+  defp inj_stmt({:bind, n, e}), do: {:s_bind, n, inj(e)}\n+  defp inj_arm({pat, _guard, body}), do: {:c_arm, inj_pat(pat), inj(body)}\n+  defp inj_pat(%Core.PVar{name: n}), do: {:p_var, n}\n+  defp inj_pat(%Core.PCtor{ctor: c, args: args}), do: {:p_ctor, c, Enum.map(args, &inj_pat/1)}\n+  defp inj_pat(_), do: :p_other\n+\n+  defp port_mix(mod, body, env), do: mod.num_mix(inj(Core.from_expr(Pratt.parse(body))), env, %{})\n+\n+  # the reference's verdict, isolated to the numeric-mix gate by its message. The corpus\n+  # is built so num-mix is the ONLY error a function can carry (well-typed otherwise), so\n+  # `:ok` ⇒ no mix and the mix message ⇒ mix.\n+  defp ref_mix(src) do\n+    case Check.check(src) do\n+      :ok -> false\n+      {:error, msg} -> String.contains?(msg, \"no implicit Int↔Float\")\n+    end\n+  end\n+\n+  describe \"num_mix agrees with Rian.Check.check_numeric_mix (the lock)\" do\n+    test \"every NEGATIVE case is flagged by BOTH the port and the reference\", %{mod: mod} do\n+      for {label, body, env, src} <- @negative do\n+        assert ref_mix(src), \"reference did not flag the mix in #{label}: #{inspect(src)}\"\n+\n+        assert port_mix(mod, body, env),\n+               \"port num_mix missed the mix in #{label}: #{inspect(body)}\"\n+      end\n+    end\n+\n+    test \"every POSITIVE case is passed by BOTH the port and the reference\", %{mod: mod} do\n+      for {label, body, env, src} <- @positive do\n+        refute ref_mix(src), \"reference flagged a non-mix in #{label}: #{inspect(src)}\"\n+        refute port_mix(mod, body, env), \"port num_mix false-flagged #{label}: #{inspect(body)}\"\n+      end\n+    end\n+\n+    test \"port and reference agree on every corpus entry (the equivalence lock)\", %{mod: mod} do\n+      for {label, body, env, src} <- @negative ++ @positive do\n+        assert port_mix(mod, body, env) == ref_mix(src),\n+               \"num_mix DIVERGED from Rian.Check on #{label}: #{inspect(src)}\"\n+      end\n+    end\n+  end\n+end","raw_url":"https://github.com/wstein/rian-lab/raw/6df51a7f8a9cd9a07a0fcd06eda0e9c2fd845146/test%2Frian%2Fchecker_nummix_fixpoint_test.exs","sha":"cd17b9f5c9bf5fcfbbb4e426556585f9506764b1","status":"added"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_beam_fixpoint_test.exs","changes":116,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcompose_real_beam_fixpoint_test.exs?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":116,"filename":"test/rian/compose_real_beam_fixpoint_test.exs","patch":"@@ -1,116 +0,0 @@\n-defmodule Rian.ComposeRealBeamFixpointTest do\n-  # async: false — loads modules into the VM (incl. the verified backend by its\n-  # natural module atom) and the driver loads compiled modules at runtime.\n-  use ExUnit.Case, async: false\n-\n-  alias Rian.Beam\n-\n-  # COMPOSITION fixpoint rung 7 (ADR-0063 Step 3 / §4): the first cut that connects\n-  # the two previously-disconnected successes — per-stage equivalence and the\n-  # composition loop. Rungs 1-6 used a TOY backend (the driver's own `forms`).\n-  # `compose_real_beam.rian` instead calls the EQUIVALENCE-LOCKED\n-  # `beam.rian` backend across modules:\n-  #\n-  #     parse(src) → selfhost_beam Func → Beam.compile_forms(funcs)\n-  #                                       └ the REAL port (cross-module call)\n-  #                  → inflate its Form sum → real abstract-form tuples (in Rian)\n-  #                  → :compile.forms / :code.load_binary (the driver loop)\n-  #\n-  # `Beam` must be loaded under its natural `:\"Elixir.Beam\"` atom so\n-  # the Pascal-qualified call resolves (ADR-0041). The `Form → abstract-form`\n-  # inflation the beam fixpoint test did in Elixir is now ported into the driver.\n-  #\n-  # The test calls only `build/2` and runs the result, identical to `Rian.Beam` —\n-  # now with the driver's backend being the verified port, not a toy.\n-\n-  setup_all do\n-    # the verified backend, under the atom a Pascal-qualified call lowers to.\n-    {:ok, _} = Beam.load(File.read!(\"compiler/beam.rian\"), :\"Elixir.Beam\")\n-\n-    {:ok, drv} =\n-      Beam.load(\n-        File.read!(\"compiler/compose_real_beam.rian\"),\n-        :rian_compose_real_beam\n-      )\n-\n-    {:ok, drv: drv}\n-  end\n-\n-  defp ref_module(src) do\n-    {:ok, m} = Beam.load(src, :\"cmp_rb_ref_#{System.unique_integer([:positive])}\")\n-    m\n-  end\n-\n-  # {pipeline source (untyped), reference source (typed sigs), [{fn, args}] to run}.\n-  @corpus [\n-    {\n-      \"def fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      \"def fib(n Int53) Int53\\ndef fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      [{:fib, [0]}, {:fib, [1]}, {:fib, [10]}, {:fib, [15]}]\n-    },\n-    {\n-      \"def max(a, b) := if a > b do a else b end\\ndef min(a, b) := if a < b do a else b end\",\n-      \"def max(a Int53, b Int53) Int53 := if a > b do a else b end\\ndef min(a Int53, b Int53) Int53 := if a < b do a else b end\",\n-      [{:max, [3, 7]}, {:max, [9, 2]}, {:min, [3, 7]}]\n-    },\n-    {\n-      \"def gcd(a, 0) := a\\ndef gcd(a, b) := step(a, b)\\ndef step(a, b) := if a > b do gcd(a - b, b) else gcd(a, b - a) end\",\n-      \"def gcd(a Int53, b Int53) Int53\\ndef gcd(a, 0) := a\\ndef gcd(a, b) := step(a, b)\\ndef step(a Int53, b Int53) Int53 := if a > b do gcd(a - b, b) else gcd(a, b - a) end\",\n-      [{:gcd, [48, 18]}, {:gcd, [17, 5]}, {:gcd, [100, 100]}]\n-    },\n-    {\n-      \"def inrange(x, lo, hi) := if x >= lo and x <= hi do 1 else 0 end\",\n-      \"def inrange(x Int53, lo Int53, hi Int53) Int53 := if x >= lo and x <= hi do 1 else 0 end\",\n-      [{:inrange, [5, 1, 10]}, {:inrange, [15, 1, 10]}]\n-    }\n-  ]\n-\n-  describe \"real-backend fixpoint — driver via Beam runs identically to Elixir\" do\n-    test \"build/2 (verified backend) loads modules equal to Rian.Beam's\", %{drv: drv} do\n-      for {rian_src, ref_src, calls} <- @corpus do\n-        modname = :\"cmp_rb_#{System.unique_integer([:positive])}\"\n-        loaded = drv.build(rian_src, modname)\n-        assert loaded == modname\n-\n-        ref = ref_module(ref_src)\n-\n-        for {fname, args} <- calls do\n-          assert apply(loaded, fname, args) == apply(ref, fname, args),\n-                 \"driver(real-backend) `#{fname}` diverged from Elixir toolchain on #{inspect(args)}\"\n-        end\n-      end\n-    end\n-  end\n-\n-  describe \"teeth — the backend really is the cross-module verified port\" do\n-    test \"compile_module routes through Beam.compile_forms (inflated in Rian)\", %{\n-      drv: drv\n-    } do\n-      # the abstract form is exactly what the verified backend + Rian inflater produce.\n-      [_m, _exp, func] =\n-        drv.compile_module(\"def pick(a, b) := if a > b do a else b end\", :pick_rb)\n-\n-      assert {:function, 0, :pick, 2,\n-              [\n-                {:clause, 0, [{:var, 0, :A}, {:var, 0, :B}], [],\n-                 [\n-                   {:case, 0, {:op, 0, :>, {:var, 0, :A}, {:var, 0, :B}},\n-                    [\n-                      {:clause, 0, [{:atom, 0, true}], [], [{:var, 0, :A}]},\n-                      {:clause, 0, [{:atom, 0, false}], [], [{:var, 0, :B}]}\n-                    ]}\n-                 ]}\n-              ]} = func\n-    end\n-\n-    test \"without Beam loaded the call would fail — proving it's a real dependency\", %{\n-      drv: drv\n-    } do\n-      # Beam IS loaded (setup_all), so this succeeds; the point of the rung is\n-      # that the backend is an external verified module, not inlined toy code.\n-      assert Code.ensure_loaded?(:\"Elixir.Beam\")\n-      m = drv.build(\"def double(n) := n + n\", :rb_double)\n-      assert apply(m, :double, [21]) == 42\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_beam_fixpoint_test.exs","sha":"0c0740a13d92a75ac773a1f9833d8807101ec9cb","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_decl_fixpoint_test.exs","changes":115,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcompose_real_decl_fixpoint_test.exs?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":115,"filename":"test/rian/compose_real_decl_fixpoint_test.exs","patch":"@@ -1,115 +0,0 @@\n-defmodule Rian.ComposeRealDeclFixpointTest do\n-  # async: false — loads verified ports (by their natural atoms) + the driver, and\n-  # the driver loads compiled modules at runtime.\n-  use ExUnit.Case, async: false\n-\n-  alias Rian.Beam\n-\n-  # COMPOSITION fixpoint rung 9 (ADR-0063 Step 3): the front-end's LAST toy piece —\n-  # declaration splitting — is replaced by the equivalence-locked `selfhost_decl`\n-  # port. `compose_real_decl.rian` parses the whole program with\n-  # `Decl.parse_program` (cross-module), lowers its Decl IR to `selfhost_beam`\n-  # Core/Pat (the surface→Core lowering), and compiles via the verified `selfhost_beam`\n-  # backend (cross-module). BOTH front-end (lex→Decl) and back-end\n-  # (Beam) are now verified ports.\n-  #\n-  #   lex → Decl.parse_program → normalize/group → lower → Beam.compile_forms\n-  #        → inflate (in Rian) → :compile.forms / :code.load_binary\n-  #\n-  # selfhost_decl's surface has no `if` (that was rung 8's selfhost_parse) but DOES\n-  # have cons-list patterns — so this rung compiles list-pattern recursion\n-  # (`sum`/`len`), beyond rung 8. The test loads both ports under their\n-  # :\"Elixir.*\" atoms, then calls only `build/2` and runs the result,\n-  # identical to `Rian.Beam`.\n-\n-  setup_all do\n-    {:ok, _} = Beam.load(File.read!(\"compiler/decl.rian\"), :\"Elixir.Decl\")\n-    {:ok, _} = Beam.load(File.read!(\"compiler/beam.rian\"), :\"Elixir.Beam\")\n-\n-    {:ok, drv} =\n-      Beam.load(\n-        File.read!(\"compiler/compose_real_decl.rian\"),\n-        :rian_compose_real_decl\n-      )\n-\n-    {:ok, drv: drv}\n-  end\n-\n-  defp ref_module(src) do\n-    {:ok, m} = Beam.load(src, :\"cmp_rd_ref_#{System.unique_integer([:positive])}\")\n-    m\n-  end\n-\n-  # {pipeline source (untyped), reference source (typed sigs), [{fn, args}] to run}.\n-  @corpus [\n-    {\n-      \"def fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      \"def fib(n Int53) Int53\\ndef fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      [{:fib, [0]}, {:fib, [1]}, {:fib, [10]}, {:fib, [15]}]\n-    },\n-    {\n-      \"def fact(0) := 1\\ndef fact(n) := n * fact(n - 1)\",\n-      \"def fact(n Int53) Int53\\ndef fact(0) := 1\\ndef fact(n) := n * fact(n - 1)\",\n-      [{:fact, [0]}, {:fact, [6]}]\n-    },\n-    {\n-      \"def even(0) := 1\\ndef even(n) := odd(n - 1)\\ndef odd(0) := 0\\ndef odd(n) := even(n - 1)\",\n-      \"def even(n Int53) Int53\\ndef even(0) := 1\\ndef even(n) := odd(n - 1)\\ndef odd(n Int53) Int53\\ndef odd(0) := 0\\ndef odd(n) := even(n - 1)\",\n-      [{:even, [8]}, {:even, [7]}, {:odd, [8]}, {:odd, [5]}]\n-    },\n-    # list-pattern recursion — beyond rung 8's (if-based) surface\n-    {\n-      \"def sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\",\n-      \"def sum(xs Vec(Int53)) Int53\\ndef sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\",\n-      [{:sum, [[]]}, {:sum, [[1, 2, 3, 4]]}, {:sum, [[10, -3, 5]]}]\n-    },\n-    {\n-      \"def len([]) := 0\\ndef len([_ | t]) := 1 + len(t)\",\n-      \"def len(xs Vec(Int53)) Int53\\ndef len([]) := 0\\ndef len([_ | t]) := 1 + len(t)\",\n-      [{:len, [[]]}, {:len, [[1, 2, 3]]}, {:len, [[9]]}]\n-    }\n-  ]\n-\n-  describe \"verified front-end + back-end fixpoint — runs identically to Elixir\" do\n-    test \"build/2 (Decl + Beam) loads modules equal to Rian.Beam's\", %{drv: drv} do\n-      for {rian_src, ref_src, calls} <- @corpus do\n-        modname = :\"cmp_rd_#{System.unique_integer([:positive])}\"\n-        loaded = drv.build(rian_src, modname)\n-        assert loaded == modname\n-\n-        ref = ref_module(ref_src)\n-\n-        for {fname, args} <- calls do\n-          assert apply(loaded, fname, args) == apply(ref, fname, args),\n-                 \"driver(real decl+back) `#{fname}` diverged from Elixir toolchain on #{inspect(args)}\"\n-        end\n-      end\n-    end\n-  end\n-\n-  describe \"teeth — the declaration parser AND backend are the verified ports\" do\n-    test \"list-pattern clauses lower to cons forms and run (sum over a list)\", %{drv: drv} do\n-      m = drv.build(\"def sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\", :rd_sum_teeth)\n-      assert apply(m, :sum, [[1, 2, 3, 4, 5]]) == 15\n-    end\n-\n-    test \"compile_module groups multi-clause defs via the real decl parser\", %{drv: drv} do\n-      [_m, {:attribute, 0, :export, exports} | funcs] =\n-        drv.compile_module(\n-          \"def fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-          :rd_fibs\n-        )\n-\n-      assert exports == [{:fib, 1}]\n-      assert [{:function, 0, :fib, 1, clauses}] = funcs\n-      assert length(clauses) == 3\n-    end\n-\n-    test \"an empty-list pattern lowers to the nil form `{nil, 0}`\", %{drv: drv} do\n-      [_m, _e, {:function, 0, :f, 1, [c0 | _]}] =\n-        drv.compile_module(\"def f([]) := 0\\ndef f([_ | t]) := t\", :rd_nil)\n-\n-      assert {:clause, 0, [{nil, 0}], [], [{:integer, 0, 0}]} = c0\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_decl_fixpoint_test.exs","sha":"4c90fd22e9edc94f41514964bb2fb04e1c6ccf78","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_front_fixpoint_test.exs","changes":111,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcompose_real_front_fixpoint_test.exs?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":111,"filename":"test/rian/compose_real_front_fixpoint_test.exs","patch":"@@ -1,111 +0,0 @@\n-defmodule Rian.ComposeRealFrontFixpointTest do\n-  # async: false — loads verified ports (by their natural atoms) + the driver, and\n-  # the driver loads compiled modules at runtime.\n-  use ExUnit.Case, async: false\n-\n-  alias Rian.Beam\n-\n-  # COMPOSITION fixpoint rung 8 (ADR-0063 Step 3): extends rung 7 (verified BACKEND)\n-  # to the FRONT-END. `compose_real_front.rian` parses each clause body with\n-  # the equivalence-locked `selfhost_parse` port (the full Rian.Pratt grammar), called\n-  # cross-module, then lowers its raw surface tuple to `selfhost_beam` Core and runs it\n-  # through the equivalence-locked `selfhost_beam` backend (also cross-module):\n-  #\n-  #   lex → decl-parse → Parse.parse(body) → lower → Beam.compile_forms\n-  #        → inflate (in Rian) → :compile.forms / :code.load_binary\n-  #\n-  # TWO verified stages composed end to end. This test loads both ports under their\n-  # :\"Elixir.*\" atoms (so the Pascal-qualified calls resolve), then calls only\n-  # `build/2` and runs the result — identical to `Rian.Beam`, now with the REAL parser\n-  # (real precedence, real surface) AND the real backend.\n-\n-  setup_all do\n-    {:ok, _} = Beam.load(File.read!(\"compiler/parse.rian\"), :\"Elixir.Parse\")\n-    {:ok, _} = Beam.load(File.read!(\"compiler/beam.rian\"), :\"Elixir.Beam\")\n-\n-    {:ok, drv} =\n-      Beam.load(\n-        File.read!(\"compiler/compose_real_front.rian\"),\n-        :rian_compose_real_front\n-      )\n-\n-    {:ok, drv: drv}\n-  end\n-\n-  defp ref_module(src) do\n-    {:ok, m} = Beam.load(src, :\"cmp_rf_ref_#{System.unique_integer([:positive])}\")\n-    m\n-  end\n-\n-  # {pipeline source (untyped), reference source (typed sigs), [{fn, args}] to run}.\n-  @corpus [\n-    {\n-      \"def fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      \"def fib(n Int53) Int53\\ndef fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      [{:fib, [0]}, {:fib, [1]}, {:fib, [10]}, {:fib, [15]}]\n-    },\n-    {\n-      \"def max(a, b) := if a > b do a else b end\\ndef min(a, b) := if a < b do a else b end\",\n-      \"def max(a Int53, b Int53) Int53 := if a > b do a else b end\\ndef min(a Int53, b Int53) Int53 := if a < b do a else b end\",\n-      [{:max, [3, 7]}, {:max, [9, 2]}, {:min, [3, 7]}]\n-    },\n-    {\n-      \"def gcd(a, 0) := a\\ndef gcd(a, b) := step(a, b)\\ndef step(a, b) := if a > b do gcd(a - b, b) else gcd(a, b - a) end\",\n-      \"def gcd(a Int53, b Int53) Int53\\ndef gcd(a, 0) := a\\ndef gcd(a, b) := step(a, b)\\ndef step(a Int53, b Int53) Int53 := if a > b do gcd(a - b, b) else gcd(a, b - a) end\",\n-      [{:gcd, [48, 18]}, {:gcd, [17, 5]}, {:gcd, [100, 100]}]\n-    },\n-    {\n-      \"def inrange(x, lo, hi) := if x >= lo and x <= hi do 1 else 0 end\",\n-      \"def inrange(x Int53, lo Int53, hi Int53) Int53 := if x >= lo and x <= hi do 1 else 0 end\",\n-      [{:inrange, [5, 1, 10]}, {:inrange, [15, 1, 10]}]\n-    },\n-    {\n-      \"def poly(a, b, c) := a + b * c - 1\",\n-      \"def poly(a Int53, b Int53, c Int53) Int53 := a + b * c - 1\",\n-      [{:poly, [2, 3, 4]}, {:poly, [10, 0, 5]}]\n-    }\n-  ]\n-\n-  describe \"real front-end + back-end fixpoint — runs identically to Elixir\" do\n-    test \"build/2 (verified parser + backend) loads modules equal to Rian.Beam's\", %{drv: drv} do\n-      for {rian_src, ref_src, calls} <- @corpus do\n-        modname = :\"cmp_rf_#{System.unique_integer([:positive])}\"\n-        loaded = drv.build(rian_src, modname)\n-        assert loaded == modname\n-\n-        ref = ref_module(ref_src)\n-\n-        for {fname, args} <- calls do\n-          assert apply(loaded, fname, args) == apply(ref, fname, args),\n-                 \"driver(real front+back) `#{fname}` diverged from Elixir toolchain on #{inspect(args)}\"\n-        end\n-      end\n-    end\n-  end\n-\n-  describe \"teeth — the REAL parser's precedence and surface flow through\" do\n-    test \"real Pratt precedence: comparison binds looser than arithmetic\", %{drv: drv} do\n-      # a + b * c - 1 must nest as (a + (b*c)) - 1, per selfhost_parse's level table.\n-      m = drv.build(\"def f(a, b, c) := a + b * c - 1\", :rf_prec_teeth)\n-      # 2 + 3*4 - 1 = 13, not e.g. (2+3)*4-1=19\n-      assert apply(m, :f, [2, 3, 4]) == 13\n-    end\n-\n-    test \"compile_module routes bodies through Parse then Beam\", %{drv: drv} do\n-      [_m, _exp, func] =\n-        drv.compile_module(\"def pick(a, b) := if a > b do a else b end\", :pick_rf)\n-\n-      assert {:function, 0, :pick, 2,\n-              [\n-                {:clause, 0, [{:var, 0, :A}, {:var, 0, :B}], [],\n-                 [\n-                   {:case, 0, {:op, 0, :>, {:var, 0, :A}, {:var, 0, :B}},\n-                    [\n-                      {:clause, 0, [{:atom, 0, true}], [], [{:var, 0, :A}]},\n-                      {:clause, 0, [{:atom, 0, false}], [], [{:var, 0, :B}]}\n-                    ]}\n-                 ]}\n-              ]} = func\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_front_fixpoint_test.exs","sha":"c0169d8eaa5a8804141eba807ead688d8df9c76f","status":"removed"},{"additions":0,"blob_url":"https://github.com/wstein/rian-lab/blob/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_lex_fixpoint_test.exs","changes":103,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcompose_real_lex_fixpoint_test.exs?ref=26ff057e19daf6e84f5cba95905382253caf6b8b","deletions":103,"filename":"test/rian/compose_real_lex_fixpoint_test.exs","patch":"@@ -1,103 +0,0 @@\n-defmodule Rian.ComposeRealLexFixpointTest do\n-  # async: false — loads verified ports (by their natural atoms) + the driver, and\n-  # the driver loads compiled modules at runtime.\n-  use ExUnit.Case, async: false\n-\n-  alias Rian.Beam\n-\n-  # COMPOSITION fixpoint rung 10 (ADR-0063 Step 3): the front-end's LAST driver-local\n-  # piece — tokenizing — is replaced by the equivalence-locked `selfhost_lexer_v2`\n-  # port. `compose_real_lex.rian` now owns NO lexing or parsing: the whole\n-  # front-end is verified ports, called cross-module.\n-  #\n-  #   LexerV2.tokenize → Decl.parse_program → lower → Beam.compile_forms\n-  #        → inflate (in Rian) → :compile.forms / :code.load_binary\n-  #\n-  # THREE verified ports (lexer, declaration parser, backend) composed end to end,\n-  # with identical token tags across the lexer→parser boundary (no projection). The\n-  # only driver-local code is the surface→Core lowering and the Form inflater. The\n-  # test loads all three ports under their :\"Elixir.*\" atoms, then calls only\n-  # `build/2` and runs the result, identical to `Rian.Beam`.\n-\n-  setup_all do\n-    {:ok, _} =\n-      Beam.load(File.read!(\"compiler/lexer_v2.rian\"), :\"Elixir.LexerV2\")\n-\n-    {:ok, _} = Beam.load(File.read!(\"compiler/decl.rian\"), :\"Elixir.Decl\")\n-    {:ok, _} = Beam.load(File.read!(\"compiler/beam.rian\"), :\"Elixir.Beam\")\n-\n-    {:ok, drv} =\n-      Beam.load(\n-        File.read!(\"compiler/compose_real_lex.rian\"),\n-        :rian_compose_real_lex\n-      )\n-\n-    {:ok, drv: drv}\n-  end\n-\n-  defp ref_module(src) do\n-    {:ok, m} = Beam.load(src, :\"cmp_rl_ref_#{System.unique_integer([:positive])}\")\n-    m\n-  end\n-\n-  # {pipeline source (untyped), reference source (typed sigs), [{fn, args}] to run}.\n-  @corpus [\n-    {\n-      \"def fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      \"def fib(n Int53) Int53\\ndef fib(0) := 0\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\",\n-      [{:fib, [0]}, {:fib, [1]}, {:fib, [10]}, {:fib, [15]}]\n-    },\n-    {\n-      \"def fact(0) := 1\\ndef fact(n) := n * fact(n - 1)\",\n-      \"def fact(n Int53) Int53\\ndef fact(0) := 1\\ndef fact(n) := n * fact(n - 1)\",\n-      [{:fact, [0]}, {:fact, [6]}]\n-    },\n-    {\n-      \"def even(0) := 1\\ndef even(n) := odd(n - 1)\\ndef odd(0) := 0\\ndef odd(n) := even(n - 1)\",\n-      \"def even(n Int53) Int53\\ndef even(0) := 1\\ndef even(n) := odd(n - 1)\\ndef odd(n Int53) Int53\\ndef odd(0) := 0\\ndef odd(n) := even(n - 1)\",\n-      [{:even, [8]}, {:odd, [7]}]\n-    },\n-    {\n-      \"def sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\",\n-      \"def sum(xs Vec(Int53)) Int53\\ndef sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\",\n-      [{:sum, [[]]}, {:sum, [[1, 2, 3, 4]]}]\n-    },\n-    {\n-      \"def len([]) := 0\\ndef len([_ | t]) := 1 + len(t)\",\n-      \"def len(xs Vec(Int53)) Int53\\ndef len([]) := 0\\ndef len([_ | t]) := 1 + len(t)\",\n-      [{:len, [[]]}, {:len, [[1, 2, 3]]}]\n-    }\n-  ]\n-\n-  describe \"fully-verified front-end + back-end fixpoint — runs identically to Elixir\" do\n-    test \"build/2 (LexerV2 + Decl + Beam ports) loads modules equal to Rian.Beam's\", %{drv: drv} do\n-      for {rian_src, ref_src, calls} <- @corpus do\n-        modname = :\"cmp_rl_#{System.unique_integer([:positive])}\"\n-        loaded = drv.build(rian_src, modname)\n-        assert loaded == modname\n-\n-        ref = ref_module(ref_src)\n-\n-        for {fname, args} <- calls do\n-          assert apply(loaded, fname, args) == apply(ref, fname, args),\n-                 \"driver(real lex+decl+back) `#{fname}` diverged from Elixir toolchain on #{inspect(args)}\"\n-        end\n-      end\n-    end\n-  end\n-\n-  describe \"teeth — the driver owns no lexing or parsing\" do\n-    test \"tokenizing comes from the verified lexer (handles whitespace/comments)\", %{drv: drv} do\n-      # the real lexer collapses newlines and skips `#` comments — driver code never\n-      # touches characters. A comment line and extra blank lines must not break it.\n-      src = \"# fib\\ndef fib(0) := 0\\n\\ndef fib(1) := 1\\ndef fib(n) := fib(n - 1) + fib(n - 2)\\n\"\n-      m = drv.build(src, :rl_comment)\n-      assert Enum.map(0..7, &apply(m, :fib, [&1])) == [0, 1, 1, 2, 3, 5, 8, 13]\n-    end\n-\n-    test \"list-pattern recursion compiles + runs (sum over a list)\", %{drv: drv} do\n-      m = drv.build(\"def sum([]) := 0\\ndef sum([h | t]) := h + sum(t)\", :rl_sum_teeth)\n-      assert apply(m, :sum, [[5, 10, 15, 20]]) == 50\n-    end\n-  end\n-end","raw_url":"https://github.com/wstein/rian-lab/raw/26ff057e19daf6e84f5cba95905382253caf6b8b/test%2Frian%2Fcompose_real_lex_fixpoint_test.exs","sha":"b4fccdb6163ca7c128b902cecb4a1158c239bf69","status":"removed"}]}