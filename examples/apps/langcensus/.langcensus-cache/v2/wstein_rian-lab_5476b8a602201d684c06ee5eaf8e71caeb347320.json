{"files":[{"additions":9,"blob_url":"https://github.com/wstein/rian-lab/blob/5476b8a602201d684c06ee5eaf8e71caeb347320/lib%2Frian%2Fdecl.ex","changes":9,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fdecl.ex?ref=5476b8a602201d684c06ee5eaf8e71caeb347320","deletions":0,"filename":"lib/rian/decl.ex","patch":"@@ -970,6 +970,15 @@ defmodule Rian.Decl do\n   defp take_line([{close} = t | rest], acc, depth) when close in [:rparen, :rbracket, :rbrace],\n     do: take_line(rest, [t | acc], max(depth - 1, 0))\n \n+  # a `do … end` block inside a `:=` body (a multi-line `case`/`if`/`with`) deepens\n+  # like a bracket, so its arm/branch newlines continue the body to the matching\n+  # `end` instead of leaking each line as a bogus declaration (P1).\n+  defp take_line([{:kw, \"do\"} = t | rest], acc, depth),\n+    do: take_line(rest, [t | acc], depth + 1)\n+\n+  defp take_line([{:kw, \"end\"} = t | rest], acc, depth),\n+    do: take_line(rest, [t | acc], max(depth - 1, 0))\n+\n   defp take_line([{:nl} | rest], acc, depth) do\n     if depth > 0 or line_continues?(acc, rest),\n       do: take_line(rest, acc, depth),","raw_url":"https://github.com/wstein/rian-lab/raw/5476b8a602201d684c06ee5eaf8e71caeb347320/lib%2Frian%2Fdecl.ex","sha":"244e8538e74a9dbfc234663c299e203f9a15ee71","status":"modified"},{"additions":35,"blob_url":"https://github.com/wstein/rian-lab/blob/5476b8a602201d684c06ee5eaf8e71caeb347320/test%2Frian%2Fdecl_test.exs","changes":35,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fdecl_test.exs?ref=5476b8a602201d684c06ee5eaf8e71caeb347320","deletions":0,"filename":"test/rian/decl_test.exs","patch":"@@ -83,6 +83,41 @@ defmodule Rian.DeclTest do\n       assert a.name == \"p\" and b.name == \"q\"\n     end\n \n+    test \"a `:=` body holds a multi-line `do … end` block together (P1) — case/if/with\" do\n+      # Regression: before the fix `take_line` balanced only `(`/`[`/`{`, so a `:=`\n+      # body opening `case x do` ended at the next newline and each arm line leaked\n+      # as a bogus declaration (\"expected a declaration, got {pattern}\"). A `do … end`\n+      # block must continue the body to its matching `end`.\n+      one = fn src -> %{funcs: fs} = Decl.parse(src); fs end\n+\n+      assert [%{name: \"f\"}] =\n+               one.(\"def f(x Int53) Int53 := case x do\\n  0 -> 1\\n  _ -> 2\\nend\")\n+\n+      assert [%{name: \"g\"}] = one.(\"def g(x Bool) Int53 := if x do\\n  1\\nelse\\n  2\\nend\")\n+\n+      # nested blocks balance, and the body still TERMINATES at its `end` — a\n+      # following `def` stays a separate declaration, not swallowed.\n+      assert [%{name: \"h\"}, %{name: \"k\"}] =\n+               one.(\"\"\"\n+               def h(x Int53) Int53 := case x do\n+                 0 -> case x do\n+                   0 -> 9\n+                   _ -> 8\n+                 end\n+                 _ -> 2\n+               end\n+               def k(y Int53) Int53 := y + 1\n+               \"\"\")\n+    end\n+\n+    test \"a `:=` multi-line `case` body compiles and runs on the BEAM\" do\n+      src = \"def classify(n Int53) Int53 := case n do\\n  0 -> 100\\n  _ -> 200\\nend\"\n+      {:ok, mod, bin} = Rian.Beam.compile(src, :\"rian_case_body_#{System.unique_integer([:positive])}\")\n+      {:module, ^mod} = :code.load_binary(mod, ~c\"#{mod}.beam\", bin)\n+      assert apply(mod, :classify, [0]) == 100\n+      assert apply(mod, :classify, [7]) == 200\n+    end\n+\n     test \"multi-line declarations are joined by continuation\" do\n       %{types: [t]} =\n         Decl.parse(\"\"\"","raw_url":"https://github.com/wstein/rian-lab/raw/5476b8a602201d684c06ee5eaf8e71caeb347320/test%2Frian%2Fdecl_test.exs","sha":"0d8c1ddfd76f7308482f31ccab54141b8a15fbf1","status":"modified"}]}