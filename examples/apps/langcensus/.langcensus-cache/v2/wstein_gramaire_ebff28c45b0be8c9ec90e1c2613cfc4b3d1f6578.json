{"files":[{"additions":4,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/.gitignore","changes":4,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.gitignore?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":0,"filename":".gitignore","patch":"@@ -63,3 +63,7 @@ codegen-scratch/vectors.txt\n \n # Python\n __pycache__/\n+\n+# ANTLR4's own generated-code output directory, left behind by anything that shells out to a real\n+# antlr4 tool (e.g. a conformance/golden-fixture run) against a checked-in .g4 — never source.\n+.antlr/","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/.gitignore","sha":"df1a86b7b2d38f057852c8ef3783c6ced88bfc0c","status":"modified"},{"additions":20,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FMain.scala","changes":21,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FMain.scala?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":1,"filename":"cli/jvm/src/main/scala/gramaire/cli/Main.scala","patch":"@@ -243,13 +243,32 @@ object Main:\n       else stripped\n     if base.isEmpty then base else base.take(1).toUpperCase + base.drop(1)\n \n+  // A `.g4` being imported may carry a `<Name>.gramaire-names.json` sidecar (ADR D62) next to it —\n+  // `BackendAntlr.renameMap`'s own output from a prior `emit --backend antlr`, restoring a parser\n+  // rule's original Gramaire spelling that export had to force-lowercase. Absent for any `.g4`\n+  // Gramaire never touched (a hand-written ANTLR grammar, or one from `emit` that renamed\n+  // nothing) — silently `Map.empty` then, never an error, since the sidecar is optional provenance,\n+  // not a required companion file.\n+  private def renameMapSidecarOf(g4File: String): Either[String, Map[String, String]] =\n+    val sidecarPath = g4File.replaceFirst(\"(?i)\\\\.g4$\", \"\") + \".gramaire-names.json\"\n+    if !Files.exists(Path.of(sidecarPath)) then Right(Map.empty)\n+    else\n+      readFile(sidecarPath) match\n+        case Left(err) => Left(s\"cannot read rename-map sidecar $sidecarPath: $err\")\n+        case Right(json) =>\n+          ConvertAntlr\n+            .parseRenameMap(json)\n+            .left\n+            .map(e => s\"malformed rename-map sidecar $sidecarPath: $e\")\n+\n   // Dispatches on the input file's own extension — `.g4` (ANTLR4) or `.y`/`.yy` (Bison/yacc,\n   // ADR D38) — mirroring how `emit --backend <name>` itself is backend-name-driven, just keyed\n   // by the INPUT format here instead of the output one. Compared case-insensitively, matching a\n   // filesystem convention CLI users expect (`.G4`/`.Y` work the same as `.g4`/`.y`).\n   def importResult(file: String, src: String): Either[String, Imported] =\n     val lower = file.toLowerCase\n-    if lower.endsWith(\".g4\") then ConvertAntlr.importAntlr(src)\n+    if lower.endsWith(\".g4\") then\n+      renameMapSidecarOf(file).flatMap(renameMap => ConvertAntlr.importAntlr(src, renameMap))\n     else if lower.endsWith(\".y\") || lower.endsWith(\".yy\") then\n       ConvertBison.importBison(src, baseNameOf(file))\n     else Left(s\"unrecognized import format (expected a .g4 or .y/.yy file): $file\")","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FMain.scala","sha":"07a35197a307aecef489280d25d51479e5f9f50b","status":"modified"},{"additions":27,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala","changes":28,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":1,"filename":"core/src/main/scala/gramaire/BackendAntlr.scala","patch":"@@ -13,9 +13,35 @@ object BackendAntlr:\n     name = \"antlr\",\n     capabilities = Vector(Capability.Format),\n     strategies = Backend.allStrategies,\n-    emit = ir => Vector(Output(s\"${ir.grammar.name}.g4\", emit(ir)))\n+    emit = ir =>\n+      val g4 = Vector(Output(s\"${ir.grammar.name}.g4\", emit(ir)))\n+      val renames = renameMap(ir)\n+      // A `<Name>.gramaire-names.json` sidecar (ADR D62), only when something was actually\n+      // renamed — never an in-band `.g4` comment: ANTLR's own file format isn't Gramaire's to\n+      // annotate, and a sidecar keeps the exported grammar plain, ordinary ANTLR. Read back by\n+      // `ConvertAntlr.importAntlr`'s own `renameMap` parameter to restore the original spelling\n+      // (and every reference to it) on re-import.\n+      if renames.isEmpty then g4\n+      else g4 :+ Output(s\"${ir.grammar.name}.gramaire-names.json\", renameMapJson(renames))\n   )\n \n+  /** Every PARSER rule this export force-lowercased (ADR D62): ANTLR name -> original Gramaire\n+    * name. Empty when the grammar's own nonterminals were already all lowercase-initial (the common\n+    * case for a grammar imported FROM ANTLR in the first place, or authored lowercase to begin\n+    * with) — a real ANTLR parser rule can never start uppercase, so `Expr`/`expr` could never both\n+    * be valid ANTLR parser rules; only the direction Gramaire -> ANTLR ever renames.\n+    */\n+  def renameMap(ir: IR): Map[String, String] =\n+    ir.grammar.nonterminals.flatMap { nt =>\n+      val exported = ruleName(nt.name)\n+      if exported != nt.name then Some(exported -> nt.name) else None\n+    }.toMap\n+\n+  // `Json.stringify` already sorts an JObject's keys at render time (its own canonical-JSON\n+  // guarantee), so no separate sort is needed here.\n+  private def renameMapJson(renames: Map[String, String]): String =\n+    Json.stringify(Json.JObject(renames.toVector.map { case (k, v) => k -> Json.JString(v) }))\n+\n   private val reserved: Set[String] =\n     Set(\n       \"grammar\",","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAntlr.scala","sha":"79cc39e5dc141bb80876f65b5551a914d9592c3e","status":"modified"},{"additions":46,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala","changes":52,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":6,"filename":"core/src/main/scala/gramaire/ConvertAntlr.scala","patch":"@@ -18,6 +18,13 @@ package gramaire\n // distinct doc-comment token of its own — a `/** ... */` is lexically just an ordinary block\n // comment — so plain `//`/`/* */` capture, identical to what `ConvertBison` already does, is\n // sufficient here too.\n+//\n+// `importAntlr`'s optional `renameMap` (ADR D62) restores a parser rule's original Gramaire\n+// spelling when BackendAntlr force-lowercased it on export (`Expr` -> `expr`, since a real ANTLR\n+// parser rule can never start uppercase) — read from a separate `<Name>.gramaire-names.json`\n+// sidecar BackendAntlr writes alongside the `.g4`, never from an in-band `.g4` comment: ANTLR's\n+// own file format isn't Gramaire's to annotate, and a sidecar keeps the exported grammar plain,\n+// ordinary ANTLR any other tool can read with no special knowledge of Gramaire at all.\n \n /** The result of an import: the rendered `.gram.md` and any features that could not be represented\n   * and were dropped.\n@@ -657,7 +664,7 @@ object ConvertAntlr:\n   private def regexOfAlts(alts: Vector[Vector[Elem]]): String =\n     alts.map(els => els.map(regexOfElem).mkString).mkString(\"|\")\n \n-  private def render(p: Parsed): Imported =\n+  private def render(p: Parsed, externalRenameMap: Map[String, String]): Imported =\n     val parserRules = p.rules.filterNot(_.lexer)\n     val lexerRules = p.rules.filter(_.lexer)\n \n@@ -670,6 +677,17 @@ object ConvertAntlr:\n     // exact same rename, or it dangles.\n     val lexerRename: Map[String, String] = lexerRules.map(r => r.name -> r.name.toUpperCase).toMap\n \n+    // A parser rule named in the caller-supplied `externalRenameMap` (ADR D62 — the\n+    // `<Name>.gramaire-names.json` sidecar BackendAntlr writes next to a `.g4` it renamed rules in)\n+    // restores its ORIGINAL Gramaire spelling instead of the ANTLR name BackendAntlr force-lowercased\n+    // it to. Scoped to rules the map actually names — unlike lexerRename, which applies to every\n+    // lexer rule uniformly — since the overwhelming majority of parser rules never went through that\n+    // export/re-import round trip (no sidecar at all, the common case for a hand-written ANTLR\n+    // grammar) and should just keep their own, already-valid name.\n+    val parserRename: Map[String, String] =\n+      parserRules.flatMap(r => externalRenameMap.get(r.name).map(r.name -> _)).toMap\n+    val rename: Map[String, String] = lexerRename ++ parserRename\n+\n     def tokenLine(r: G4Rule): String =\n       val gramaireName = lexerRename(r.name)\n       val spelling = if gramaireName != r.name then s\"\"\" @spelling(\"${r.name}\")\"\"\" else \"\"\n@@ -692,9 +710,10 @@ object ConvertAntlr:\n         )\n \n     def ruleSection(r: G4Rule): String =\n+      val gramaireName = parserRename.getOrElse(r.name, r.name)\n       val doc = r.doc.map(d => d + \"\\n\\n\").getOrElse(\"\")\n-      s\"## ${r.name}\\n\\n$doc```gramaire\\n${r.name}\\n  : \" + r.alts\n-        .map(renderTopAlt(_, lexerRename))\n+      s\"## $gramaireName\\n\\n$doc```gramaire\\n$gramaireName\\n  : \" + r.alts\n+        .map(renderTopAlt(_, rename))\n         .mkString(\"\\n  | \") + \"\\n  ;\\n```\\n\"\n \n     val markdown =\n@@ -704,10 +723,31 @@ object ConvertAntlr:\n     Imported(markdown, p.warnings)\n \n   /** Import an ANTLR4 `.g4` grammar, producing a rendered `.gram.md` and any features that could\n-    * not be represented.\n+    * not be represented. `renameMap` (ADR D62) is this grammar's own `<Name>.gramaire-names.json`\n+    * sidecar, if BackendAntlr wrote one alongside the `.g4` — ANTLR name -> original Gramaire name\n+    * for every parser rule a prior export force-lowercased; empty for a `.g4` with no such sidecar\n+    * (any hand-written ANTLR grammar, or one Gramaire never touched).\n     */\n-  def importAntlr(src: String): Either[String, Imported] =\n+  def importAntlr(\n+      src: String,\n+      renameMap: Map[String, String] = Map.empty\n+  ): Either[String, Imported] =\n     for\n       toks <- lexG4(src)\n       parsed <- parseG4(toks)\n-    yield render(parsed)\n+    yield render(parsed, renameMap)\n+\n+  /** Decode a `<Name>.gramaire-names.json` sidecar (ADR D62, `BackendAntlr.renameMap`'s own output)\n+    * into the flat `Map[String, String]` `importAntlr`'s `renameMap` parameter expects — the CLI's\n+    * own job to call this (reading the sidecar file, if one sits next to the `.g4` being imported)\n+    * before calling `importAntlr`; this module has no filesystem access of its own.\n+    */\n+  def parseRenameMap(json: String): Either[String, Map[String, String]] =\n+    Json.parse(json).flatMap {\n+      case Json.JObject(kvs) =>\n+        kvs.foldLeft[Either[String, Map[String, String]]](Right(Map.empty)) {\n+          case (acc, (k, Json.JString(v))) => acc.map(_ + (k -> v))\n+          case (_, (k, _))                 => Left(s\"rename map entry `$k` must be a string\")\n+        }\n+      case _ => Left(\"rename map JSON must be an object\")\n+    }","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FConvertAntlr.scala","sha":"8849c8f4fac454c2e0f6dd681fe752ad30610636","status":"modified"},{"additions":21,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala","changes":21,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":0,"filename":"core/src/test/scala/gramaire/BackendAntlrSuite.scala","patch":"@@ -23,6 +23,27 @@ class BackendAntlrSuite extends munit.FunSuite:\n         )\n   }\n \n+  test(\n+    \"renameMap (D62): every renamed parser rule (S -> s, A -> a), keyed by its exported ANTLR name\"\n+  ) {\n+    IR.buildIR(Method.Canonical, \"Tiny\", tiny) match\n+      case Left(_) => fail(\"tiny grammar should build\")\n+      case Right(ir) =>\n+        assertEquals(BackendAntlr.renameMap(ir), Map(\"s\" -> \"S\", \"a\" -> \"A\"))\n+  }\n+\n+  test(\"renameMap (D62): empty when no nonterminal was actually renamed\") {\n+    val allLowercase = Grammar(\n+      Vector(\n+        Rule(\"s\", Vector.empty, Vector(Alt(Vector(Ref(\"a\"), Lit(\"+\"), Ref(\"a\")), None, None))),\n+        Rule(\"a\", Vector.empty, Vector(Alt(Vector(Ref(\"NUM\")), None, None)))\n+      )\n+    )\n+    IR.buildIR(Method.Canonical, \"Tiny\", allLowercase) match\n+      case Left(_)   => fail(\"grammar should build\")\n+      case Right(ir) => assertEquals(BackendAntlr.renameMap(ir), Map.empty[String, String])\n+  }\n+\n   test(\"IRNonterminal.comment is rendered as a leading /* ... */ block comment above its rule\") {\n     IR.buildIR(Method.Canonical, \"Tiny\", tiny) match\n       case Left(_) => fail(\"tiny grammar should build\")","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendAntlrSuite.scala","sha":"1cb78bf46a0def59b872036c95355398013adbfa","status":"modified"},{"additions":92,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala","changes":93,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":1,"filename":"core/src/test/scala/gramaire/ConvertAntlrSuite.scala","patch":"@@ -528,12 +528,103 @@ class ConvertAntlrSuite extends munit.FunSuite:\n                 )\n                 // `Digit+` desugars to a helper list rule before BackendAntlr ever sees it (D-token\n                 // sugar lowering) — so the reference to check is inside THAT rule's body, not `r`'s.\n+                // A standalone `DIGIT` reference must not survive; the desugared helper's own name\n+                // (`DIGIT_plus`) legitimately still contains the substring, in its own D62\n+                // `@gramaire-name` marker, which isn't what this assertion is about.\n                 assert(\n-                  !g4b.contains(\"DIGIT\"),\n+                  !\"(?<![A-Z_])DIGIT(?![A-Za-z_])\".r.findFirstIn(g4b).isDefined,\n                   s\"no parser rule may still reference the internal ALL-CAPS name once the lexer rule itself renders under its native spelling, got:\\n$g4b\"\n                 )\n                 assert(\n                   g4b.contains(\"Digit\\n  | \") || g4b.contains(\": Digit\\n\"),\n                   s\"the desugared helper rule's own reference to the renamed lexer rule must use its native spelling, got:\\n$g4b\"\n                 )\n   }\n+\n+  // Gramaire's own idiomatic nonterminal style (README.md's own worked example: `Expr`, `Term`) is\n+  // capitalized — but a real ANTLR parser rule can NEVER start uppercase (that's always a lexer\n+  // rule in ANTLR terms), so BackendAntlr.ruleName force-lowercases it on export. The mirror image\n+  // of the D61 lexer-name test above: this time the rename happens on EXPORT (not import), and the\n+  // round trip that matters is emit -> hand-edit -> re-import, not import -> re-export.\n+  private val capitalizedNonterminalGrammar =\n+    \"\"\"---\n+      |name: ExprLang\n+      |---\n+      |\n+      |## Tokens\n+      |\n+      |```gramaire\n+      |NUMBER : /[0-9]+/ ;\n+      |```\n+      |\n+      |## Expr\n+      |\n+      |```gramaire\n+      |Expr\n+      |  : Expr '+' Term\n+      |  | Term\n+      |  ;\n+      |```\n+      |\n+      |## Term\n+      |\n+      |```gramaire\n+      |Term\n+      |  : NUMBER\n+      |  ;\n+      |```\n+      |\"\"\".stripMargin\n+\n+  test(\n+    \"convert: a capitalized Gramaire nonterminal (Expr) exports plain ANTLR (no in-band annotation) plus a renameMap sidecar (D62), and re-imports its original spelling everywhere given that sidecar\"\n+  ) {\n+    Lr.parse(capitalizedNonterminalGrammar) match\n+      case Left(e) => fail(s\"capitalizedNonterminalGrammar should parse: $e\")\n+      case Right(g) =>\n+        IR.buildIRWithTokens(\n+          defsOf(capitalizedNonterminalGrammar),\n+          Method.Canonical,\n+          \"ExprLang\",\n+          g\n+        ) match\n+          case Left(_) => fail(\"capitalizedNonterminalGrammar should build an IR\")\n+          case Right(ir) =>\n+            val g4 = BackendAntlr.emit(ir)\n+            assert(\n+              !g4.contains(\"@gramaire-name\") && !g4.contains(\"@\"),\n+              s\"the exported .g4 must stay plain, ordinary ANTLR — no in-band Gramaire annotation, got:\\n$g4\"\n+            )\n+            assert(\n+              g4.contains(\"expr\\n  : expr '+' term\"),\n+              s\"expr's own reference to term must also be lowercased consistently, got:\\n$g4\"\n+            )\n+\n+            val renameMap = BackendAntlr.renameMap(ir)\n+            assertEquals(renameMap, Map(\"expr\" -> \"Expr\", \"term\" -> \"Term\"))\n+\n+            // Without the sidecar, re-import has nothing to restore the original spelling from —\n+            // the plain ANTLR names are all that's left, exactly like importing anyone else's\n+            // hand-written .g4 that never went through a Gramaire export at all.\n+            ConvertAntlr.importAntlr(g4) match\n+              case Left(e) => fail(s\"the exported .g4 should re-import: $e\")\n+              case Right(impNoSidecar) =>\n+                assert(\n+                  impNoSidecar.markdown.contains(\"## expr\\n\"),\n+                  s\"without the sidecar, re-import has no way to recover the original capitalization, got:\\n${impNoSidecar.markdown}\"\n+                )\n+\n+            // With the sidecar (as the CLI's own `import` reads it alongside the .g4, D62), the\n+            // original capitalized name is restored — in the rule's own heading AND in every\n+            // OTHER rule's reference to it, not just its own declaration.\n+            ConvertAntlr.importAntlr(g4, renameMap) match\n+              case Left(e) => fail(s\"the exported .g4 should re-import given its sidecar: $e\")\n+              case Right(imp2) =>\n+                assert(\n+                  imp2.markdown.contains(\"## Expr\\n\"),\n+                  s\"re-import must restore the original capitalized rule name, got:\\n${imp2.markdown}\"\n+                )\n+                assert(\n+                  imp2.markdown.contains(\"Expr '+' Term\"),\n+                  s\"re-import must restore the capitalized name in EVERY reference too, not just the rule's own heading, got:\\n${imp2.markdown}\"\n+                )\n+  }","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/core%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FConvertAntlrSuite.scala","sha":"8a3609912a29f538c8ed83e53d83a4cbe1307f01","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/docs%2Fmulti-backend-implementation-plan.md","changes":1,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578","deletions":0,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -761,6 +761,7 @@ everything target-specific is independent.\n | D59 | A rule head's `NL` before `:` is optional — single-line, ANTLR/Bison-style rules now parse | **`Lexer.normalizeNewlines`/`normalizeNewlinesSpanned` now synthesize a zero-width head `NL` whenever a rule can legitimately start — right after a `;` (the previous rule's own terminator) or at the very start of the document — and the real one is missing, so `Foo : 'x' ;` parses identically to the traditional `Foo\\n  : 'x' ;`. `Rule`'s own two productions in `bootstrapGrammar`/`grammar/Productions.gram.md` are untouched: the parser still only ever sees an `IDENT NL ':'` shape, real or synthesized, so every downstream production id in `CodegenScala.lrActionsScala` and `Lr.reduce`, every golden IR/DOT/`.g4` fixture, and `Productions.gram.md`'s own railroad diagrams and `.gram.lock` stay byte-for-byte unchanged. A `name:Sym` field (`IDENT ':' Sym`) is never mistaken for a head, because synthesis only fires at a position a field can never occupy — mid-`Body`, a field's `:` is never immediately preceded by a `;` or the document start.** | ANTLR handles single-line rules because it never reuses `:` for two jobs (`=`/`+=` bind element labels, `:` only ever separates a rule head); Gramaire reuses `:` for both a rule's own head and a `name:Sym` field, which is why the newline convention existed in the first place (fmt-output-contract.md's \"Option A\"). The mandatory trailing `;` already made \"the token right after a `;`, or the document's first token\" an unambiguous rule-start position (that's what let a prior revision drop the *boundary* NL between consecutive rules, D-numbered implicitly by that revision); this reuses the exact same boundary to also drop the *requirement* that a head's own `NL` be present in the source, without touching the grammar itself. Synthesizing in the lexer rather than adding new `Rule` alternatives was a deliberate choice: `Rule` is nonterminal #3 of the self-hosting bootstrap grammar, so any new alternative shifts every later production's global id, cascading through three independently hand-synchronized tables (`CodegenScala.lrActionsScala`, `Lr.reduce`, and every golden fixture keyed to the `lr` corpus) for no behavioral gain a lexer-level fix doesn't already deliver. |\n | D60 | An ALL-CAPS reference is checked against the document's real declared token set, not just its spelling | **`Diagnostics.undefinedNonterminals`/`checkDefined` gain a `declaredTokens: Set[String]` parameter — the document's own `## Tokens` block, parsed for real by `Tokens.parseTokens` and threaded through from `Lr.parseWith` (which already has the raw markdown to hand). When `declaredTokens` is non-empty, an ALL-CAPS reference that isn't among them is now a new, real diagnostic — `` undefined token class `X` `` — with a did-you-mean against the other declared tokens, mirroring the existing undefined-nonterminal check exactly. When `declaredTokens` is empty (the document has no `## Tokens` block at all — lexer-spec.md §6's whole-grammar \"external mode\", where the entire scanner is supplied out of band, e.g. `examples/readme.gram.md`'s `WORD`), an ALL-CAPS reference is left unchecked, exactly as before D60. The pre-existing spelling-based `terminalLikeRefs` heuristic survives only as the did-you-mean fallback for that external-mode case, where no real declared set exists to suggest from.** | Closes a real, previously-silent gap: a typo'd ALL-CAPS reference in a grammar that already declares its own tokens locally (the common case — most shipped examples) was never checked against anything and was silently treated as \"presumably a valid terminal\", the exact opposite of the mixed-case undefined-nonterminal check two lines above it in the same function, which has always been a hard error. `unusedTokenWarnings` (a declared-but-unreferenced token, the other direction of the same typo) already existed; this adds its missing complement. Scoped to only fire when the document has actually declared something locally, rather than unconditionally, because `## Tokens` is genuinely optional (whole-grammar external mode) and a real, checked-in example (`readme.gram.md`) depends on that leniency. |\n | D61 | `Tokens.validateName`'s ALL-CAPS requirement stays a hard gate; round-trip fidelity is solved by `@spelling(\"...\")` instead | **Investigated relaxing the ALL-CAPS-only requirement on a declared token's name to a lint (in response to a request to support importing non-ALL-CAPS names, e.g. ANTLR's `Digit` — ANTLR only requires an uppercase FIRST letter, not the whole word). Found it can't be relaxed in isolation: `Lr.classifyFenceContent`'s fence-role self-identification (\"case is law\", D-grammar-roles) requires *every* line in a `## Tokens` fence to independently look ALL-CAPS-before-`:` to be recognized as a Tokens fence at all; a lowercase-named line would silently fall through to `FenceKind.Rule` instead (and, post-D59, a single-line rule head is now `IDENT : ... ;`-shaped too — textually identical to a token definition line for a name that isn't ALL-CAPS). So the requirement is structural, not stylistic, and stays exactly as strict as before. Instead, `TokenDef`/`IRTokenClass` gain an optional `nativeSpelling: Option[String]` (the `@spelling(\"...\")` modifier, parsed by `Tokens.scala`'s own hand-rolled, non-bootstrap parser — no self-hosting/production-id risk): `ConvertAntlr` renames a non-ALL-CAPS ANTLR lexer rule (`Digit` → `DIGIT`) and records the original via `@spelling(\"Digit\")`, rewriting every parser-rule reference to the same renamed name (`renderAtom`'s `rename` map) so nothing dangles; `BackendAntlr` re-emits under the recorded original spelling when present, not Gramaire's internal ALL-CAPS name.** | The real, underlying need from the debate that prompted this (round-trip fidelity with ANTLR/Bison/EBNF, none of which share Gramaire's case convention) doesn't actually require relaxing Gramaire's own authoring format — it requires Gramaire to *remember* an import's original spelling. `@spelling` delivers exactly that, safely, without touching fence classification, the self-hosting bootstrap, or any existing golden fixture (the field is optional and omitted from JSON when absent, following the same convention as `sourceLayout`/`diagramView`/`name`, D56/D58). Rejected: relaxing `classifyFenceContent` to accept non-ALL-CAPS Tokens lines too — that would need a *new* disambiguation mechanism (contradicting D-grammar-roles' \"no keyword, ever\" stance) to replace the one case-based signal removes, for no benefit `@spelling` doesn't already provide. |\n+| D62 | A renamed nonterminal's original spelling round-trips via a `<Name>.gramaire-names.json` sidecar, never an in-band `.g4` comment | **The matching hazard on the opposite side of the ANTLR converter from D61: `BackendAntlr.ruleName` force-lowercases a nonterminal's first letter on export (`Expr` → `expr`), because a real ANTLR parser rule can never start uppercase (that's always a lexer rule in ANTLR, not a style choice — unlike D61's tokens, this can never arise on IMPORT, since any real ANTLR parser rule name is already lowercase-initial by ANTLR's own hard requirement). The hazard is real only in the `emit --backend antlr` → hand-edit → `import` workflow the CLI advertises side by side: without a fix, Gramaire's own capitalized house style (`Expr`, `Term`) silently and permanently becomes lowercase on that round trip. A first pass recorded the original name as an in-band `// @gramaire-name: Expr` comment in the exported `.g4` itself — rejected on review: ANTLR's own file format isn't Gramaire's to annotate, and it's the one export among all of Gramaire's backends that would carry a Gramaire-specific marker a plain ANTLR toolchain has no reason to understand. Replaced with `BackendAntlr.renameMap(ir): Map[String, String]` (ANTLR name → original Gramaire name, only for rules actually renamed) written to a **separate sidecar file**, `<Name>.gramaire-names.json`, alongside the plain, unannotated `.g4` — the `Backend.emit` `Vector[Output]` shape (already multi-file-capable) needed no changes to carry it. `ConvertAntlr.importAntlr` gained an optional `renameMap` parameter (default empty) it threads into the *same* `rename` map D61's `lexerRename` already passes through `renderTopAlt`, so every reference to a restored rule — not just its own heading — comes back consistently; `ConvertAntlr.parseRenameMap` decodes the sidecar's JSON, and the CLI's `import` command (`renameMapSidecarOf`) looks for and reads it automatically next to the `.g4` being imported, falling back to `Map.empty` (silently, not an error) when no sidecar exists — the common case for any hand-written ANTLR grammar, or a `.g4` Gramaire exported but renamed nothing in.** | Confirms and closes the exact gap Marcus/Devon named in the original ANTLR-vs-Gramaire debate (\"a workflow the CLI literally advertises side-by-side\") without polluting an output format Gramaire is a guest in — the `.g4` a `gramaire emit --backend antlr` user hands to a colleague, or feeds into a real `antlr4` toolchain, reads as completely ordinary ANTLR, with the provenance living in a file only Gramaire's own `import` ever needs to look for. Mirrors `.gram.lock`'s own established sidecar convention (a derived, drift-checkable artifact living beside its source) rather than inventing a second in-band-comment mechanism alongside D61's real-syntax `@spelling(...)` modifier (which stays as is: it extends *Gramaire's own* Tokens format, not someone else's). |\n \n ## Platform & tooling roadmap (D43)\n ","raw_url":"https://github.com/wstein/gramaire/raw/ebff28c45b0be8c9ec90e1c2613cfc4b3d1f6578/docs%2Fmulti-backend-implementation-plan.md","sha":"97239627c83b33220a0c442fd1e7b02e6dd52916","status":"modified"}]}