{"files":[{"additions":6,"blob_url":"https://github.com/wstein/rian-lab/blob/55e2b264751d1876e5986e349bae527bef48e32a/purs%2Fscripts%2Fplayground-entry.mjs","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/purs%2Fscripts%2Fplayground-entry.mjs?ref=55e2b264751d1876e5986e349bae527bef48e32a","deletions":0,"filename":"purs/scripts/playground-entry.mjs","patch":"@@ -3,3 +3,9 @@\n // artifact the playground loads. `compile` lowers Rian source to an ECMAScript module string;\n // `compileTypes` the `.d.mts` TypeScript declaration sidecar; `compileTs` a native typed `.ts` module.\n export { compile, compileTypes, compileTs } from \"../output-js/Rian.JS/index.js\";\n+// The non-JS *source* emitters also run in-browser (they are pure PureScript, no toolchain): the\n+// whole-program Rust assembly (`rustProgram`, == the `rs` tour pane's `Rian.Lower.rust_program`) and\n+// the Kotlin/JVM emitter (`Rian.JVM.compile`, == the `jvm` pane). Aliased to avoid the `compile`\n+// name clash. (BEAM stays out — `Rian.Beam` needs `:compile.forms`, an Erlang/OTP runtime API.)\n+export { rustProgram as compileRust } from \"../output-js/Rian.Lower.Rust/index.js\";\n+export { compile as compileJvm } from \"../output-js/Rian.JVM/index.js\";","raw_url":"https://github.com/wstein/rian-lab/raw/55e2b264751d1876e5986e349bae527bef48e32a/purs%2Fscripts%2Fplayground-entry.mjs","sha":"37e6d4a3d0a7ca4ee7158742347351aee2c22998","status":"modified"},{"additions":92,"blob_url":"https://github.com/wstein/rian-lab/blob/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fpublic%2Frian-compiler.mjs","changes":128,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fpublic%2Frian-compiler.mjs?ref=55e2b264751d1876e5986e349bae527bef48e32a","deletions":36,"filename":"site/public/rian-compiler.mjs","raw_url":"https://github.com/wstein/rian-lab/raw/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fpublic%2Frian-compiler.mjs","sha":"81515b757a50654a00cf7591114edf1ac9fa8327","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Flib%2Frian-live.ts","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Flib%2Frian-live.ts?ref=55e2b264751d1876e5986e349bae527bef48e32a","deletions":0,"filename":"site/src/lib/rian-live.ts","patch":"@@ -6,6 +6,8 @@\n export type Compiler = {\n   compile: (src: string) => string; // the runtime ECMAScript module (.mjs)\n   compileTs: (src: string) => string; // a native, typed TypeScript module (.ts)\n+  compileRust: (src: string) => string; // whole-program Rust (== the `rs` tour pane)\n+  compileJvm: (src: string) => string; // Kotlin/JVM source (== the `jvm` tour pane)\n };\n \n // The deploy base, slash-safe: `/rian-lab` on GitHub Pages (configure-pages emits","raw_url":"https://github.com/wstein/rian-lab/raw/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Flib%2Frian-live.ts","sha":"3e4ca28c946aaabd0c86cc56e2c3f490aa2ccf1f","status":"modified"},{"additions":40,"blob_url":"https://github.com/wstein/rian-lab/blob/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Fpages%2Fby-example.astro","changes":45,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fby-example.astro?ref=55e2b264751d1876e5986e349bae527bef48e32a","deletions":5,"filename":"site/src/pages/by-example.astro","patch":"@@ -160,20 +160,33 @@ const onThis = [\n \n <script>\n   import { highlight } from \"../lib/highlight\";\n+  import type { Compiler } from \"../lib/rian-live\";\n   import { loadCompiler, looksIncomplete, runInSandbox, cleanError } from \"../lib/rian-live\";\n \n   // Make every by-example cell a live editor: edit the source and it re-compiles +\n-  // runs in your browser (the same in-browser Rian → JS compiler the playground uses),\n+  // runs in your browser (the same in-browser Rian compiler the playground uses),\n   // forgiving of half-typed input (`looksIncomplete`) and keeping the last good JS\n-  // pane on a transient error. The other target panes stay the verified reference.\n+  // pane on a transient error. The JS/Rust/JVM panes all re-lower live (pure-PureScript\n+  // emitters); only the BEAM pane needs the Erlang toolchain, so on an edit it shows a note.\n   document.querySelectorAll<HTMLElement>(\"[data-cell-id]\").forEach((cell) => {\n     const ta = cell.querySelector<HTMLTextAreaElement>(\"[data-cell-src]\");\n     const hlEl = cell.querySelector<HTMLElement>(\"[data-cell-hl]\");\n     const outEl = cell.querySelector<HTMLElement>(\"[data-cell-out]\");\n     const jsPane = cell.querySelector<HTMLElement>('[data-cell-pane=\"js\"]');\n     if (!ta || !hlEl || !outEl || !jsPane) return;\n+    const rsPane = cell.querySelector<HTMLElement>('[data-cell-pane=\"rs\"]');\n+    const jvmPane = cell.querySelector<HTMLElement>('[data-cell-pane=\"jvm\"]');\n+    const exPane = cell.querySelector<HTMLElement>('[data-cell-pane=\"ex\"]');\n     const original = ta.value;\n-    const refJs = jsPane.innerHTML; // the verified reference emission, restored when unedited\n+    // the verified reference emissions, restored when the buffer matches the original\n+    const refJs = jsPane.innerHTML;\n+    const refRs = rsPane?.innerHTML ?? \"\";\n+    const refJvm = jvmPane?.innerHTML ?? \"\";\n+    const refEx = exPane?.innerHTML ?? \"\";\n+    const noteEx =\n+      \"// ⓘ BEAM · Elixir isn't lowered in-browser — it needs the Erlang\\n// toolchain (`:compile.forms`). Flip back to the original to see the reference.\";\n+    const noteUnsupported = (label: string) =>\n+      `// ⓘ the ${label} emitter doesn't lower this construct yet.`;\n \n     let timer = 0;\n     let gen = 0;\n@@ -183,7 +196,11 @@ const onThis = [\n       const g = ++gen;\n       const source = ta!.value;\n       if (source === original) {\n+        // unedited: restore every verified reference pane.\n         jsPane!.innerHTML = refJs;\n+        if (rsPane) rsPane.innerHTML = refRs;\n+        if (jvmPane) jvmPane.innerHTML = refJvm;\n+        if (exPane) exPane.innerHTML = refEx;\n         setOut(\"idle\", \"▷ edit the source to compile + run it live\");\n         return;\n       }\n@@ -192,16 +209,34 @@ const onThis = [\n         return;\n       }\n       let js: string;\n+      let compiler: Compiler;\n       try {\n-        const { compile } = await loadCompiler();\n+        compiler = await loadCompiler();\n         if (g !== gen) return; // a newer keystroke superseded this run\n-        js = compile(source);\n+        js = compiler.compile(source);\n       } catch (e) {\n         if (g !== gen) return;\n         setOut(\"soft\", \"… \" + cleanError(e)); // keep the last good JS pane\n         return;\n       }\n       jsPane!.innerHTML = highlight(js);\n+      // JS gated it (the buffer parses + checks), so re-lower the other source targets live; a\n+      // Rust/JVM failure is an honest \"this construct isn't covered yet\". BEAM can't lower here.\n+      if (rsPane) {\n+        try {\n+          rsPane.innerHTML = highlight(compiler.compileRust(source));\n+        } catch {\n+          rsPane.innerHTML = highlight(noteUnsupported(\"Rust\"));\n+        }\n+      }\n+      if (jvmPane) {\n+        try {\n+          jvmPane.innerHTML = highlight(compiler.compileJvm(source));\n+        } catch {\n+          jvmPane.innerHTML = highlight(noteUnsupported(\"JVM · Kotlin\"));\n+        }\n+      }\n+      if (exPane) exPane.innerHTML = highlight(noteEx);\n       setOut(\"ok\", \"✓ compiled to JS — running…\");\n       const res = await runInSandbox(js);\n       if (g !== gen) return;","raw_url":"https://github.com/wstein/rian-lab/raw/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Fpages%2Fby-example.astro","sha":"76635c3eaa9beb1caedebda64f8ec2a8a62377f0","status":"modified"},{"additions":52,"blob_url":"https://github.com/wstein/rian-lab/blob/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Fpages%2Fplayground.astro","changes":68,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/site%2Fsrc%2Fpages%2Fplayground.astro?ref=55e2b264751d1876e5986e349bae527bef48e32a","deletions":16,"filename":"site/src/pages/playground.astro","patch":"@@ -102,6 +102,7 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n \n <script>\n   import { highlight } from \"../lib/highlight\";\n+  import type { Compiler } from \"../lib/rian-live\";\n   import {\n     loadCompiler,\n     compilerLoaded,\n@@ -133,9 +134,13 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n   type AnyCell = Cell | { id: \"shared\"; file: string; source: string; panes: {}; reach: {} };\n   let current: AnyCell = cells[0];\n   let isShared = false;\n-  // the live JS/TS the in-browser compiler emits for the current buffer (null = reference / awaiting Run)\n+  // the live output the in-browser compiler emits for the current buffer (null = reference /\n+  // awaiting Run, or — for rs/jvm — a construct that target's emitter doesn't lower yet). All four\n+  // source targets lower live (JS/TS/Rust/JVM); only BEAM needs the Erlang toolchain.\n   let liveJs: string | null = null;\n   let liveTs: string | null = null;\n+  let liveRs: string | null = null;\n+  let liveJvm: string | null = null;\n \n   // the real Rian compiler + live-eval helpers are shared with the tutorial (`lib/rian-live`).\n \n@@ -152,33 +157,46 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n \n   function renderPanes() {\n     const panes = (current as any).panes as Record<string, string>;\n-    // Only JS/TS lower in-browser; BEAM/Rust/JVM ship as pre-generated reference for the\n-    // *original* cell source. So once the buffer diverges from that source (an edit) or is a\n-    // shared buffer with no reference at all, those panes would be SHOWING A STALE LIE — the\n-    // original cell's output mislabeled as this buffer's. Mark them explicitly instead.\n+    // All four SOURCE targets lower in-browser (JS/TS/Rust/JVM are pure-PureScript emitters);\n+    // only BEAM needs the Erlang toolchain (`:compile.forms`), so it stays a pre-generated\n+    // reference for the *original* cell. Once the buffer diverges (an edit) or is a shared buffer,\n+    // the BEAM pane would be SHOWING A STALE LIE — the original's output mislabeled as this\n+    // buffer's — so mark it explicitly. A live target with a `null` value on a diverged buffer\n+    // means its emitter doesn't lower this construct yet (Rust/JVM cover a subset of JS).\n     const stale = isShared || src.value !== current.source;\n+    const live: Record<string, string | null> = { js: liveJs, ts: liveTs, rs: liveRs, jvm: liveJvm };\n     for (const t of [\"ex\", \"rs\", \"js\", \"ts\", \"jvm\"]) {\n       const el = document.querySelector(`.pg-pane-code[data-pane=\"${t}\"]`);\n       if (!el) continue;\n       let code: string;\n       let isStale = false;\n-      if (t === \"js\") {\n+      if (t === \"ex\") {\n+        // BEAM: reference only — not lowered in-browser.\n+        if (stale) {\n+          isStale = true;\n+          code = isShared\n+            ? `// ⓘ BEAM · Elixir is not lowered in-browser.\\n// It needs the Erlang toolchain (\\`:compile.forms\\`); JS/TS/Rust/JVM lower live (ADR-0090).`\n+            : `// ⓘ BEAM · Elixir: reference for the ORIGINAL ${current.file}.\\n// BEAM needs the Erlang toolchain (\\`:compile.forms\\`), so it isn't in the browser —\\n// JS/TS/Rust/JVM lower live as you edit (ADR-0090).`;\n+        } else {\n+          code = panes.ex;\n+        }\n+      } else if (t === \"js\") {\n         code = liveJs != null ? liveJs : (panes.js ?? \"// editing — compiles live as you type.\");\n       } else if (t === \"ts\") {\n         code = liveTs != null ? liveTs : \"// Run to compile this buffer to native TypeScript.\";\n-      } else if (stale) {\n-        isStale = true;\n-        code = isShared\n-          ? `// ⓘ ${PANE_LABEL[t]} is not lowered in-browser.\\n// A shared/edited buffer compiles live to JS/TS only; BEAM/Rust/JVM\\n// need the toolchain — they ship here as pre-generated reference (ADR-0090).`\n-          : `// ⓘ ${PANE_LABEL[t]}: reference for the ORIGINAL ${current.file}.\\n// In-browser lowering covers JS/TS only — your edits update those panes;\\n// the BEAM/Rust/JVM emitters aren't in the browser yet (ADR-0090).`;\n       } else {\n-        code = panes[t];\n+        // rs / jvm: live when available; the reference for the untouched cell; else (a diverged\n+        // buffer whose live value is null) the emitter doesn't cover this construct yet.\n+        const lv = live[t];\n+        if (lv != null) code = lv;\n+        else if (!stale && panes[t] != null) code = panes[t];\n+        else code = `// ⓘ ${PANE_LABEL[t]}: lowers live as you edit.\\n// (Empty means this buffer uses a construct the ${PANE_LABEL[t]} emitter doesn't cover yet.)`;\n       }\n       el.innerHTML = highlight(code);\n       (el as HTMLElement).classList.toggle(\"pg-pane-stale\", isStale);\n     }\n     if (isShared) {\n-      statusReach.innerHTML = `<span style=\"opacity:1\">:js</span> · <span style=\"opacity:1\">:ts</span> live · <span style=\"opacity:0.45\">other targets need the toolchain</span>`;\n+      statusReach.innerHTML = `<span style=\"opacity:1\">:js</span> · <span style=\"opacity:1\">:ts</span> · <span style=\"opacity:1\">:rs</span> · <span style=\"opacity:1\">:jvm</span> live · <span style=\"opacity:0.45\">:ex needs the toolchain</span>`;\n       return;\n     }\n     const reach = (Object.values((current as any).reach)[0] as string[]) ?? [];\n@@ -202,6 +220,8 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     src.value = current.source;\n     liveJs = null;\n     liveTs = null;\n+    liveRs = null;\n+    liveJvm = null;\n     document.querySelectorAll(\".pg-tab\").forEach((b) => b.classList.toggle(\"on\", (b as HTMLElement).dataset.file === id));\n     renderEditor();\n     renderPanes();\n@@ -217,6 +237,8 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n     src.value = source;\n     liveJs = null;\n     liveTs = null;\n+    liveRs = null;\n+    liveJvm = null;\n     document.querySelectorAll(\".pg-tab\").forEach((b) => b.classList.remove(\"on\"));\n     sharedBadge.hidden = false;\n     renderEditor();\n@@ -248,18 +270,32 @@ const files = tour.cells.map((c) => ({ id: c.id, file: c.file }));\n       return;\n     }\n     let js: string;\n+    let compiler: Compiler;\n     try {\n-      const { compile, compileTs } = await loadCompiler();\n+      compiler = await loadCompiler();\n       if (gen !== evalGen) return; // a newer keystroke superseded this run\n-      js = compile(source);\n-      liveTs = compileTs(source); // the native typed `.ts` view, same gate\n+      js = compiler.compile(source);\n+      liveTs = compiler.compileTs(source); // the native typed `.ts` view, same gate\n     } catch (e: any) {\n       if (gen !== evalGen) return;\n       // forgiving: a gate/parse error keeps the last good panes; report it softly.\n       setOut(\"soft\", \"… \" + cleanError(e));\n       return;\n     }\n     liveJs = js;\n+    // JS is the gate: once it lowers, the buffer parses + type-checks, so a Rust/JVM failure is an\n+    // honest \"this target's emitter doesn't cover this construct yet\" — record it as `null` (the\n+    // pane shows that) rather than aborting, and keep the targets independent of each other.\n+    try {\n+      liveRs = compiler.compileRust(source);\n+    } catch {\n+      liveRs = null;\n+    }\n+    try {\n+      liveJvm = compiler.compileJvm(source);\n+    } catch {\n+      liveJvm = null;\n+    }\n     if (force) document.documentElement.dataset.target = \"js\";\n     renderPanes();\n     setOut(\"ok\", \"$ rian build --js\\n  ✓ compiled to ECMAScript (live, in your browser)\\n  ▷ running…\");","raw_url":"https://github.com/wstein/rian-lab/raw/55e2b264751d1876e5986e349bae527bef48e32a/site%2Fsrc%2Fpages%2Fplayground.astro","sha":"5976deee4f4359517450b9273b190c911ee1236b","status":"modified"}]}