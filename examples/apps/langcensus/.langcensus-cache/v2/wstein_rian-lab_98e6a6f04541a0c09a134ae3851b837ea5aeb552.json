{"files":[{"additions":10,"blob_url":"https://github.com/wstein/rian-lab/blob/98e6a6f04541a0c09a134ae3851b837ea5aeb552/SELFHOST.md","changes":14,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/SELFHOST.md?ref=98e6a6f04541a0c09a134ae3851b837ea5aeb552","deletions":4,"filename":"SELFHOST.md","patch":"@@ -298,10 +298,16 @@ reference `Rian.Lexer` tokens into the parser's `Tok` sum (the same stream Pratt\n consumes), and asserts precedence (`1 + 2 * 3` nests `*` under `+`) and\n left-associativity (`1 - 2 - 3` ⇒ `(1 - 2) - 3`) match Pratt exactly.\n \n-Slice: `+ - * /` (multiplicative tighter than additive, left-assoc — Pratt's levels\n-for these ops), identifiers, integer literals, parentheses. The method is proven;\n-later slices widen the grammar+corpus (calls, comparisons, pipes, then patterns and\n-`Rian.Decl`'s declaration forms), each a regression test rather than a fresh demo.\n+The parser ports **precedence climbing** — the same algorithm `Rian.Pratt` uses,\n+with the same binding powers (`opinfo`/`bp`) — so it now covers the **full binary\n+operator table**: `* / rem div` (level 3), `+ -` (4), `<>` (5, right-assoc),\n+`< <= > >=` (8), `== !=` (9), `and` (10), `or` (11), over identifiers, integer\n+literals, and parentheses. (BEAM guards can't call user functions, so every\n+precedence decision is made in the body — an `if` over `lbp(op)` — never in a\n+guard.) The corpus exercises the ladder (`a or b and c` ⇒ `a or (b and c)`,\n+`a + b == c * d`, right-assoc `a <> b <> c`) and matches Pratt term-for-term.\n+Prefix operators, calls, pipes, and Pratt's non-assoc *raise* (`a < b < c`) are\n+later slices; each widening is a regression test, not a fresh demo.\n \n ### Bootstrap plan + the fixed-point ladder (rungs 3-4, ADR-0063)\n ","raw_url":"https://github.com/wstein/rian-lab/raw/98e6a6f04541a0c09a134ae3851b837ea5aeb552/SELFHOST.md","sha":"3c3d4e54bb491958721fefb502b384d820b82b97","status":"modified"},{"additions":80,"blob_url":"https://github.com/wstein/rian-lab/blob/98e6a6f04541a0c09a134ae3851b837ea5aeb552/examples%2Frian%2Fselfhost_parse.rian","changes":146,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_parse.rian?ref=98e6a6f04541a0c09a134ae3851b837ea5aeb552","deletions":66,"filename":"examples/rian/selfhost_parse.rian","patch":"@@ -1,103 +1,117 @@\n # ===========================================================================\n-# SELF-HOSTING — porting Rian.Pratt to Rian, fixpoint-locked (rung 2)\n+# SELF-HOSTING — porting Rian.Pratt to Rian, fixpoint-locked (rung 2, widened)\n # ===========================================================================\n-# The earlier spike (selfhost_parser.rian) parsed arithmetic into its OWN `Expr`\n-# sum and only *ran*. This rung makes the port a **checked equivalence**: the\n-# parser's output is diffed against the reference `Rian.Pratt.parse` over a\n-# corpus (test/rian/parse_fixpoint_test.exs) — the same method as the lexer\n-# fixpoint, AST projection instead of token projection.\n+# The parser's output is diffed against the reference `Rian.Pratt.parse` over a\n+# corpus (test/rian/parse_fixpoint_test.exs) — the parser analog of the lexer\n+# fixpoint. The diff needs NO projection: the AST constructors lower to Pratt's\n+# exact surface tuples — Num(s)->{:num,s}, Id(s)->{:id,s}, Bin(op,l,r)->\n+# {:bin,op,l,r} — so `parse(tokens)` term-EQUALS `Rian.Pratt.parse(source)`.\n #\n-# The trick that makes the diff exact: the AST constructors are named so their\n-# BEAM lowering IS `Rian.Pratt`'s surface-tuple shape —\n-#     Num(s)       -> {:num, s}\n-#     Id(s)        -> {:id, s}\n-#     Bin(op,l,r)  -> {:bin, op, l, r}\n-# so `parse(tokens)` term-EQUALS `Rian.Pratt.parse(source)` with no projection.\n-# `EErr` keeps every clause total (the reference raises on a parse error instead;\n-# the corpus is well-formed, so `EErr` never appears in a checked run).\n+# This widens the first slice (which hardcoded `+ - * /` in two levels) to the\n+# FULL binary-operator precedence table via **precedence climbing** — the same\n+# algorithm `Rian.Pratt` uses, with the same binding powers (`opinfo`/`bp`):\n #\n-# Slice: precedence climbing over `+ - * /` (multiplicative tighter than additive,\n-# both left-associative — Rian.Pratt's levels for these ops), identifiers, integer\n-# literals, and parentheses. Calls / comparisons / pipes are later slices.\n+#   level  3  * / rem div     (left)        base = (13 - level) * 10\n+#   level  4  + -             (left)        left/none: lbp=base,  rbp=base+1\n+#   level  5  <>              (right)       right:     lbp=base+1, rbp=base\n+#   level  8  < <= > >=       (none)\n+#   level  9  == !=           (none)\n+#   level 10  and             (left)\n+#   level 11  or              (left)\n #\n-#   add  := mul (('+' | '-') mul)*\n-#   mul  := atom (('*' | '/') atom)*\n-#   atom := Num | Id | '(' add ')'\n+# BEAM guards cannot call user functions, so every precedence decision is made in\n+# the body (an `if` over `lbp(op)`), never in a guard. Atoms are Num / Id /\n+# parenthesized expressions. Prefix operators, calls, pipes, and the non-assoc\n+# *raise* (Pratt rejects `a < b < c`) are later slices — the corpus stays within\n+# this grammar (each non-assoc operator used at most once per level).\n # ---------------------------------------------------------------------------\n \n mod SelfhostParse do\n-  # The token stream this parser consumes — the reference `Rian.Lexer` tokens,\n-  # injected into this sum by the test (`{:num,s}` -> `TNum(s)`, `{:op,o}` ->\n-  # `TOp(o)`, `{:lparen}` -> `TLP`, …). `TOp` carries the operator string, so the\n-  # parser branches on `TOp(\"+\")` etc.\n   type Tok := TNum(String) | TId(String) | TOp(String) | TLP | TRP\n \n-  # Output AST — constructor names chosen so the lowered term matches Rian.Pratt.\n+  # constructor names chosen so the lowered term matches Rian.Pratt's tuples.\n   type Expr := Num(String) | Id(String) | Bin(String, Expr, Expr) | EErr\n \n-  # One parse step: the node built so far + the unconsumed tokens (a pair without\n-  # tuple types — a single-constructor sum).\n   type Parse := P(Expr, Vec(Tok))\n \n   pub def parse(ts Vec(Tok)) Expr\n   pub def parse(ts)\n-    case parse_add(ts) do\n+    case parse_bp(0, ts) do\n       P(e, _) -> e\n     end\n   end\n \n-  # add := mul (('+' | '-') mul)*  — fold additive operators left to right.\n-  def parse_add(ts Vec(Tok)) Parse\n-  def parse_add(ts)\n-    case parse_mul(ts) do\n-      P(left, rest) -> parse_add_tail(left, rest)\n-    end\n-  end\n-\n-  def parse_add_tail(left Expr, ts Vec(Tok)) Parse\n-  def parse_add_tail(left, [TOp(\"+\") | rest])\n-    case parse_mul(rest) do\n-      P(right, rest2) -> parse_add_tail(Bin(\"+\", left, right), rest2)\n-    end\n-  end\n-  def parse_add_tail(left, [TOp(\"-\") | rest])\n-    case parse_mul(rest) do\n-      P(right, rest2) -> parse_add_tail(Bin(\"-\", left, right), rest2)\n-    end\n-  end\n-  def parse_add_tail(left, ts) := P(left, ts)\n-\n-  # mul := atom (('*' | '/') atom)*  — fold multiplicative operators.\n-  def parse_mul(ts Vec(Tok)) Parse\n-  def parse_mul(ts)\n+  # parse an expression whose operators all bind at least as tight as `min_bp`.\n+  def parse_bp(min_bp Int64, ts Vec(Tok)) Parse\n+  def parse_bp(min_bp, ts)\n     case parse_atom(ts) do\n-      P(left, rest) -> parse_mul_tail(left, rest)\n+      P(lhs, rest) -> climb(min_bp, lhs, rest)\n     end\n   end\n \n-  def parse_mul_tail(left Expr, ts Vec(Tok)) Parse\n-  def parse_mul_tail(left, [TOp(\"*\") | rest])\n-    case parse_atom(rest) do\n-      P(right, rest2) -> parse_mul_tail(Bin(\"*\", left, right), rest2)\n+  # while the next token is a binary operator whose left binding power is >=\n+  # min_bp, consume it and fold; the right-hand side is parsed at `rbp(op)` (so\n+  # left-assoc stops at the same level, right-assoc re-enters it).\n+  def climb(min_bp Int64, lhs Expr, ts Vec(Tok)) Parse\n+  def climb(min_bp, lhs, [TOp(op) | rest])\n+    if lbp(op) >= min_bp do\n+      case parse_bp(rbp(op), rest) do\n+        P(rhs, rest2) -> climb(min_bp, Bin(op, lhs, rhs), rest2)\n+      end\n+    else\n+      P(lhs, [TOp(op) | rest])\n     end\n   end\n-  def parse_mul_tail(left, [TOp(\"/\") | rest])\n-    case parse_atom(rest) do\n-      P(right, rest2) -> parse_mul_tail(Bin(\"/\", left, right), rest2)\n-    end\n-  end\n-  def parse_mul_tail(left, ts) := P(left, ts)\n+  def climb(min_bp, lhs, ts) := P(lhs, ts)\n \n-  # atom := Num | Id | '(' add ')'  — a literal, a variable, or a parenthesized\n-  # subexpression; anything else (or a missing `)`) is a total `EErr`.\n+  # atom := Num | Id | '(' expr ')'  — anything else (or a missing `)`) is a\n+  # total `EErr` (the reference raises; the corpus is well-formed).\n   def parse_atom(ts Vec(Tok)) Parse\n   def parse_atom([TNum(n) | rest]) := P(Num(n), rest)\n   def parse_atom([TId(x) | rest]) := P(Id(x), rest)\n   def parse_atom([TLP | rest])\n-    case parse_add(rest) do\n+    case parse_bp(0, rest) do\n       P(e, [TRP | rest2]) -> P(e, rest2)\n       P(_, other) -> P(EErr, other)\n     end\n   end\n   def parse_atom(ts) := P(EErr, ts)\n+\n+  # ── binding powers (mirror Rian.Pratt.bp) ────────────────────────────────\n+  # left binding power: where the operator sits when deciding to bind leftward.\n+  def lbp(op String) Int64 := bp_base(level(op)) + left_off(op)\n+  # right binding power: the min_bp the right operand is parsed at.\n+  def rbp(op String) Int64 := bp_base(level(op)) + right_off(op)\n+\n+  def bp_base(lvl Int64) Int64 := (13 - lvl) * 10\n+\n+  # only `<>` is right-associative among the operators in this slice.\n+  def left_off(op String) Int64\n+  def left_off(\"<>\") := 1\n+  def left_off(op) := 0\n+\n+  def right_off(op String) Int64\n+  def right_off(\"<>\") := 0\n+  def right_off(op) := 1\n+\n+  # operator precedence level (lower binds tighter); an unknown token gets a level\n+  # so loose that `lbp(op) >= min_bp` is always false — climbing stops, the token\n+  # is left unconsumed (never a binary fold).\n+  def level(op String) Int64\n+  def level(\"*\") := 3\n+  def level(\"/\") := 3\n+  def level(\"rem\") := 3\n+  def level(\"div\") := 3\n+  def level(\"+\") := 4\n+  def level(\"-\") := 4\n+  def level(\"<>\") := 5\n+  def level(\"<\") := 8\n+  def level(\"<=\") := 8\n+  def level(\">\") := 8\n+  def level(\">=\") := 8\n+  def level(\"==\") := 9\n+  def level(\"!=\") := 9\n+  def level(\"and\") := 10\n+  def level(\"or\") := 11\n+  def level(other) := 100\n end","raw_url":"https://github.com/wstein/rian-lab/raw/98e6a6f04541a0c09a134ae3851b837ea5aeb552/examples%2Frian%2Fselfhost_parse.rian","sha":"6c3543abef90aa5d95e28401dd0cde5717379957","status":"modified"},{"additions":32,"blob_url":"https://github.com/wstein/rian-lab/blob/98e6a6f04541a0c09a134ae3851b837ea5aeb552/test%2Frian%2Fparse_fixpoint_test.exs","changes":35,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fparse_fixpoint_test.exs?ref=98e6a6f04541a0c09a134ae3851b837ea5aeb552","deletions":3,"filename":"test/rian/parse_fixpoint_test.exs","patch":"@@ -29,8 +29,9 @@ defmodule Rian.ParseFixpointTest do\n \n   defp toks(src), do: src |> Lexer.expr_tokens() |> Enum.map(&inject/1)\n \n-  # Within the slice: identifiers, integer literals, parens, and `+ - * /`\n-  # (multiplicative tighter than additive, both left-associative).\n+  # Within the slice: identifiers, integer literals, parens, and the full binary\n+  # operator precedence table (`* / rem div`, `+ -`, `<>` right-assoc, `< <= > >=`,\n+  # `== !=`, `and`, `or`) via precedence climbing — Rian.Pratt's exact levels.\n   @corpus [\n     \"1\",\n     \"x\",\n@@ -45,7 +46,17 @@ defmodule Rian.ParseFixpointTest do\n     \"a * (b + c) / d\",\n     \"(((7)))\",\n     \"a / b / c\",\n-    \"x * y + z * w\"\n+    \"x * y + z * w\",\n+    # widened: comparison / boolean / concat / rem / div\n+    \"a < b\",\n+    \"a + b == c\",\n+    \"x and y or z\",\n+    \"a or b and c\",\n+    \"a == b and c != d\",\n+    \"a <> b <> c\",\n+    \"x rem y div z\",\n+    \"n * n + 1 < limit and ok\",\n+    \"a <= b and c >= d or e\"\n   ]\n \n   describe \"self-hosting parser fixpoint (ADR-0027/0031) — Rian parser vs Rian.Pratt\" do\n@@ -71,5 +82,23 @@ defmodule Rian.ParseFixpointTest do\n       assert mod.parse(toks(\"1 - 2 - 3\")) ==\n                {:bin, \"-\", {:bin, \"-\", {:num, \"1\"}, {:num, \"2\"}}, {:num, \"3\"}}\n     end\n+\n+    test \"the full precedence ladder matches Pratt: `*` > `+` > comparison > `and` > `or`\",\n+         %{mod: mod} do\n+      # `and` binds tighter than `or`: a or (b and c)\n+      assert mod.parse(toks(\"a or b and c\")) ==\n+               {:bin, \"or\", {:id, \"a\"}, {:bin, \"and\", {:id, \"b\"}, {:id, \"c\"}}}\n+\n+      # comparison looser than arithmetic: (a + b) == (c * d)\n+      assert mod.parse(toks(\"a + b == c * d\")) ==\n+               {:bin, \"==\", {:bin, \"+\", {:id, \"a\"}, {:id, \"b\"}},\n+                {:bin, \"*\", {:id, \"c\"}, {:id, \"d\"}}}\n+    end\n+\n+    test \"`<>` is right-associative (Pratt level 5): `a <> b <> c` ⇒ `a <> (b <> c)`\",\n+         %{mod: mod} do\n+      assert mod.parse(toks(\"a <> b <> c\")) ==\n+               {:bin, \"<>\", {:id, \"a\"}, {:bin, \"<>\", {:id, \"b\"}, {:id, \"c\"}}}\n+    end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/98e6a6f04541a0c09a134ae3851b837ea5aeb552/test%2Frian%2Fparse_fixpoint_test.exs","sha":"ec6e14ee86fac81021bec233c9100eedda760212","status":"modified"}]}