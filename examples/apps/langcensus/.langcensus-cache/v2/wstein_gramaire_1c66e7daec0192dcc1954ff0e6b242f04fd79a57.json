{"files":[{"additions":40,"blob_url":"https://github.com/wstein/gramaire/blob/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FStripSuite.scala","changes":41,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FStripSuite.scala?ref=1c66e7daec0192dcc1954ff0e6b242f04fd79a57","deletions":1,"filename":"core/.jvm/src/test/scala/gramaire/StripSuite.scala","patch":"@@ -10,7 +10,8 @@ class StripSuite extends munit.FunSuite:\n       \"grammar/Productions.gram.md\",\n       \"examples/calc.gram.md\",\n       \"examples/json.gram.md\",\n-      \"examples/readme.gram.md\"\n+      \"examples/readme.gram.md\",\n+      \"examples/calc-delegate.gram.md\"\n     )\n   do\n     test(s\"parse(strip(x)) == parse(x) for $path\") {\n@@ -39,3 +40,41 @@ class StripSuite extends munit.FunSuite:\n       )\n       assert(Lr.parse(strippedTwice).isRight, s\"$path: twice-stripped form should still parse\")\n     }\n+\n+  // `## Externals`/`### name` (ADR D49) has no `` ```gramaire `` fence of its own (its fences are\n+  // real host-language code, e.g. ` ```javascript `) — `section`'s generic keepableOpen check would\n+  // silently drop the whole thing, losing every embedded delegate implementation, not merely\n+  // reformatting it. `nativeExternalsSection` renders it as `external NAME {% ... %}` blocks\n+  // instead, and `toFenced`'s `extractNativeExternals` reads that shape back into the exact\n+  // `## Externals` markdown `Lr.withExternals` already knows how to scan — so this needs no changes\n+  // to that machinery, only the synthesis in both directions.\n+  test(\n+    \"strip preserves ## Externals as `external NAME {% ... %}`, round-tripping through parseWithDocs\"\n+  ) {\n+    val md = readFile(\"examples/calc-delegate.gram.md\")\n+    val stripped = Lr.strip(md)\n+    assert(stripped.contains(\"external Pow {%\"), \"Pow has an embedded impl and must survive strip\")\n+    assert(\n+      stripped.contains(\"external Round {%\"),\n+      \"Round has an embedded impl and must survive strip\"\n+    )\n+    assert(\n+      !stripped.contains(\"external Call {%\"),\n+      \"Call has no embedded impl (deliberately unresolved) and must not appear as one\"\n+    )\n+    Lr.parseWithDocs(Method.Canonical, stripped) match\n+      case Left(e) => fail(s\"stripped native form should still parse with docs: $e\")\n+      case Right(g) =>\n+        val byName = g.externals.map(e => e.name -> e.impl).toMap\n+        assert(byName.contains(\"Pow\"), \"Pow external should round-trip\")\n+        assert(\n+          byName(\"Pow\").values.exists(_.contains(\"Math.pow(c.factor, c.power)\")),\n+          s\"Pow's embedded code should survive verbatim, got: ${byName(\"Pow\")}\"\n+        )\n+        assert(byName.contains(\"Round\"), \"Round external should round-trip\")\n+        assert(\n+          byName(\"Round\").values.exists(_.contains(\"Number(c.expr.toFixed(n))\")),\n+          s\"Round's embedded code should survive verbatim, got: ${byName(\"Round\")}\"\n+        )\n+        assert(!byName.contains(\"Call\"), \"Call must still have no embedded impl after round-trip\")\n+  }","raw_url":"https://github.com/wstein/gramaire/raw/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FStripSuite.scala","sha":"c6ce7100c85d926fded539cc17c7bf610c8a1162","status":"modified"},{"additions":84,"blob_url":"https://github.com/wstein/gramaire/blob/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","changes":88,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala?ref=1c66e7daec0192dcc1954ff0e6b242f04fd79a57","deletions":4,"filename":"core/src/main/scala/gramaire/Lr.scala","patch":"@@ -309,6 +309,25 @@ object Lr:\n       val rendered = trimBlankEnds(sec.drop(1).foldLeft(WalkAcc(None, Vector.empty))(walk).out)\n       if rendered.isEmpty then None else Some(rendered.mkString(\"\\n\"))\n \n+  // A `## Externals` section renders to `external NAME {% ... %}` blocks (the exact shape\n+  // `toFenced`'s `extractNativeExternals` reads back) instead of running through `section` above —\n+  // `section`/`keepableOpen` only ever look for a ` ```gramaire ` fence, and a `### name` subsection\n+  // never has one (its own fence is real host-language code, e.g. ` ```javascript `), so every\n+  // embedded implementation would otherwise be silently dropped rather than merely reformatted, the\n+  // one class of data `strip` must never lose even though everything else it emits is a lossy,\n+  // presentation-only projection. Reuses `externalsSections` (already scans exactly this `## `/`### `\n+  // shape for `withExternals`) against just this section's own lines, re-joined, rather than a\n+  // second, drifting parse of the same structure.\n+  private def nativeExternalsSection(sec: Vector[String]): Option[String] =\n+    if sec.headOption.map(_.trim) != Some(\"## Externals\") then None\n+    else\n+      val rendered = externalsSections(sec.mkString(\"\\n\")).flatMap { sub =>\n+        sub.fences\n+          .find(f => f.lang.nonEmpty && f.lang != \"gramaire\")\n+          .map(f => s\"external ${sub.name} {%\\n${f.content}\\n%}\")\n+      }\n+      if rendered.isEmpty then None else Some(rendered.mkString(\"\\n\\n\"))\n+\n   // The leading `# Title` + intro paragraph -> a `/** … */` banner\n   // comment.\n   private def banner(pre: Vector[String]): String =\n@@ -348,7 +367,8 @@ object Lr:\n       val sect = sectionize(ls)\n       val (bannerLines, preambleFence) = splitPreamble(sect.preamble)\n       val parts =\n-        (banner(bannerLines) +: preambleFence.toVector) ++ sect.sections.flatMap(section)\n+        (banner(bannerLines) +: preambleFence.toVector) ++\n+          sect.sections.flatMap(sec => nativeExternalsSection(sec).orElse(section(sec)))\n       parts.filter(_ != \"\").mkString(\"\\n\\n\") + \"\\n\"\n \n   // A rule fence's own first non-blank line always starts with that rule's own name (`RuleName`\n@@ -614,10 +634,67 @@ object Lr:\n       else acc.copy(out = acc.out :+ line)\n     }.out\n \n+  // A native top-level external declaration (ADR D49, extended to native `.gram`): `external NAME`\n+  // then `{%` alone ends the opening line — mirroring NAME's own `### NAME` markdown counterpart —\n+  // and a `%}` alone on its own line closes it, however many lines the body spans. Deliberately\n+  // stricter than an ordinary inline `{% %}` action (which may open/close mid-line): this shape only\n+  // needs simple whole-line matches to find and remove the block from the line pool before any other\n+  // `toFenced` classification runs on what's left, at the cost of a vanishingly unlikely false\n+  // positive if a grammar ever named a rule `external` and immediately followed it with a same-shaped\n+  // action opener — the same class of heuristic risk `isTokenDef`/`isSettingDecl` already accept\n+  // elsewhere in this file.\n+  private val nativeExternalOpenRe = \"^external\\\\s+(\\\\w+)\\\\s+\\\\{%\\\\s*$\".r\n+\n+  // Pull every native `external NAME {% ... %}` block out of `ls`, in document order — returned\n+  // alongside the lines with those blocks removed, so `decomment`/`isTokenDef`/`isPrecDecl`/\n+  // `isSettingDecl` never see a JS-internal `//`/`/* */` inside a body and misread it as a grammar\n+  // comment, and the body's own lines don't leak into the `prodLines` catch-all. An unterminated\n+  // block (`{%` with no closing `%}` before EOF) is left completely in place, untouched — the same\n+  // \"don't invent a shape a real syntax error wouldn't have\" restraint this file's diagnostics rely\n+  // on elsewhere; a later real parse of the reconstructed text surfaces it as an ordinary error\n+  // instead of this silently swallowing broken input.\n+  private def extractNativeExternals(\n+      ls: Vector[String]\n+  ): (Vector[(String, String)], Vector[String]) =\n+    val found = Vector.newBuilder[(String, String)]\n+    val remaining = Vector.newBuilder[String]\n+    var i = 0\n+    while i < ls.length do\n+      ls(i) match\n+        case nativeExternalOpenRe(name) =>\n+          ((i + 1) until ls.length).find(j => ls(j).trim == \"%}\") match\n+            case Some(j) =>\n+              found += name -> ls.slice(i + 1, j).mkString(\"\\n\")\n+              i = j + 1\n+            case None =>\n+              remaining += ls(i)\n+              i += 1\n+        case line =>\n+          remaining += line\n+          i += 1\n+    (found.result(), remaining.result())\n+\n+  // Every native `external` block, synthesized as the exact `## Externals`/`### name`/```` ```javascript ````\n+  // markdown shape `externalsSections` (and, through it, `withExternals`/`parseWithDocs`) already\n+  // knows how to scan for — so attaching externals to a native `.gram`'s `Grammar` needs no changes\n+  // to that machinery at all, only this one synthesis step. Always tagged `javascript`: every\n+  // consumer of `Grammar.externals` (`BackendJs.jsImplOf`) resolves a fence via `Lr.normalizeLang`,\n+  // which already folds \"javascript\" (among other spellings) to `\"js\"`, and `external { %} ` carries\n+  // no language tag of its own to preserve — same as an ordinary inline `{% %}` action, whose\n+  // language is always the document's own declared `lang:`, never spelled out at the action site.\n+  private def externalsMarkdown(externals: Vector[(String, String)]): Vector[String] =\n+    if externals.isEmpty then Vector.empty\n+    else\n+      val subsections = externals.map { case (name, code) =>\n+        s\"### $name\\n\\n```javascript\\n$code\\n```\"\n+      }\n+      Vector((\"## Externals\" +: subsections).mkString(\"\\n\\n\"))\n+\n   /** Read a fence-free `.gram` projection back to the fenced form the parser expects (a no-op on\n     * already-fenced `.gram.md`). Each classified line-group becomes its own bare ```gramaire fence\n     * — `fenceOrigins`/`classifyFenceContent` re-derive its role from content, exactly as for any\n-    * `.gram.md` fence.\n+    * `.gram.md` fence — except `external NAME {% ... %}` blocks, synthesized straight into the `##\n+    * Externals` markdown shape instead (see `externalsMarkdown`).\n     */\n   def toFenced(src: String): String =\n     // A leading `---` frontmatter block is blanked out (replaced by exactly as many empty\n@@ -634,7 +711,8 @@ object Lr:\n       case _                                         => src\n     if defrontmattered.contains(\"```gramaire\") then defrontmattered\n     else\n-      val ls = decomment(defrontmattered.split(\"\\n\", -1).toVector)\n+      val (externals, rawLs) = extractNativeExternals(defrontmattered.split(\"\\n\", -1).toVector)\n+      val ls = decomment(rawLs)\n       val settingLines = ls.filter(isSettingDecl)\n       val tokenLines = ls.filter(isTokenDef)\n       val precLines = ls.filter(isPrecDecl)\n@@ -643,7 +721,9 @@ object Lr:\n         val trimmed = trimBlankEnds(body)\n         if trimmed.isEmpty then Vector.empty\n         else Vector(s\"```gramaire\\n${trimmed.mkString(\"\\n\")}\\n```\")\n-      (block(settingLines) ++ block(tokenLines) ++ block(precLines) ++ block(prodLines))\n+      (block(settingLines) ++ block(tokenLines) ++ block(precLines) ++ block(\n+        prodLines\n+      ) ++ externalsMarkdown(externals))\n         .mkString(\"\\n\\n\")\n \n   /** `toFenced` plus whether it actually changed anything — i.e. whether `src` was a fence-free","raw_url":"https://github.com/wstein/gramaire/raw/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FLr.scala","sha":"14e78e075c9095166e53537d5939206b1636d4de","status":"modified"},{"additions":25,"blob_url":"https://github.com/wstein/gramaire/blob/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/docs%2Ffmt-output-contract.md","changes":32,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Ffmt-output-contract.md?ref=1c66e7daec0192dcc1954ff0e6b242f04fd79a57","deletions":7,"filename":"docs/fmt-output-contract.md","patch":"@@ -459,18 +459,36 @@ there's no Markdown here to satisfy guarantees 1–3 against in the first\n place: no headings, no fences, no `markdownlint-cli2` run at all.\n \n **Shape**: an optional `/** ... */` banner (the file's intro prose, one\n-` * ` per line), `%name`/`%lang` declarations as bare lines, token\n+` * ` per line), `name:`/`lang:` declarations as bare lines, token\n definitions such as `ALLCAPS : /regex/`, `Mixed-case` rule productions — each\n-optionally preceded by `///`-prefixed doc-comment lines — and any\n-`%left`/`%right`/`%nonassoc` declarations after the productions. This is\n-exactly the shape `gramaire strip` produces from a `.gram.md`, and `Lr.parse`\n-already treats it as fully valid grammar input via `Lr.toFenced`'s line-shape\n-reconstruction (`isSettingDecl`/`isTokenDef`/`isPrecDecl`, else rule content —\n+optionally preceded by `///`-prefixed doc-comment lines — any\n+`%left`/`%right`/`%nonassoc` declarations after the productions, and any\n+`external NAME {% ... %}` blocks (ADR D49, extended to this format) after\n+that. This is exactly the shape `gramaire strip` produces from a `.gram.md`,\n+and `Lr.parse`/`Lr.parseWithDocs` already treat it as fully valid grammar\n+input via `Lr.toFenced`'s line-shape reconstruction (`isSettingDecl`/\n+`isTokenDef`/`isPrecDecl`, else rule content —\n the same `classifyFenceContent` rules used everywhere, \"case is law\").\n \n+`external NAME {% ... %}` (a `-> NAME` delegate's embedded implementation,\n+the native counterpart of a `.gram.md`'s `## Externals`/`### NAME` fence) is\n+recognized as its own shape, not lumped into rule content: `NAME` then `{%`\n+alone ends the opening line, and a `%}` alone on its own line closes it,\n+however many lines the body spans — stricter than an ordinary inline\n+`{% %}` action (which may open/close mid-line) so the block can be found\n+and removed from the rest of the file with plain line matches before\n+anything else is classified. `Lr.toFenced` synthesizes it straight into the\n+`## Externals`/`### NAME`/` ```javascript ` shape `Lr.withExternals` already\n+scans for, so attaching it to a `Grammar` needs no changes beyond that one\n+synthesis step; `gramaire strip` emits the reverse direction, so a\n+`.gram.md`'s embedded delegate implementations survive the round trip\n+instead of silently vanishing (the gap `section`'s ` ```gramaire `-only\n+`keepableOpen` check would otherwise create, since a `### NAME` subsection's\n+own fence is real host code, never a `` ```gramaire `` one).\n+\n **`gramaire check <file.gram>`** verifies:\n \n-1. The grammar parses (`Lr.parse`) and has a `%name` directive — the only\n+1. The grammar parses (`Lr.parse`) and has a `name:` directive — the only\n    two things that were ever actually _required_ for correctness.\n 2. Plain-text hygiene: no CRLF, no trailing whitespace on any line, exactly\n    one trailing newline.","raw_url":"https://github.com/wstein/gramaire/raw/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/docs%2Ffmt-output-contract.md","sha":"8a27f28790ef6afe7041569aa3a950fc59e2bcd1","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/docs%2Fmulti-backend-implementation-plan.md","changes":1,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fmulti-backend-implementation-plan.md?ref=1c66e7daec0192dcc1954ff0e6b242f04fd79a57","deletions":0,"filename":"docs/multi-backend-implementation-plan.md","patch":"@@ -763,6 +763,7 @@ everything target-specific is independent.\n | D61 | `Tokens.validateName`'s ALL-CAPS requirement stays a hard gate; round-trip fidelity is solved by `@spelling(\"...\")` instead | **Investigated relaxing the ALL-CAPS-only requirement on a declared token's name to a lint (in response to a request to support importing non-ALL-CAPS names, e.g. ANTLR's `Digit` — ANTLR only requires an uppercase FIRST letter, not the whole word). Found it can't be relaxed in isolation: `Lr.classifyFenceContent`'s fence-role self-identification (\"case is law\", D-grammar-roles) requires *every* line in a `## Tokens` fence to independently look ALL-CAPS-before-`:` to be recognized as a Tokens fence at all; a lowercase-named line would silently fall through to `FenceKind.Rule` instead (and, post-D59, a single-line rule head is now `IDENT : ... ;`-shaped too — textually identical to a token definition line for a name that isn't ALL-CAPS). So the requirement is structural, not stylistic, and stays exactly as strict as before. Instead, `TokenDef`/`IRTokenClass` gain an optional `nativeSpelling: Option[String]` (the `@spelling(\"...\")` modifier, parsed by `Tokens.scala`'s own hand-rolled, non-bootstrap parser — no self-hosting/production-id risk): `ConvertAntlr` renames a non-ALL-CAPS ANTLR lexer rule (`Digit` → `DIGIT`) and records the original via `@spelling(\"Digit\")`, rewriting every parser-rule reference to the same renamed name (`renderAtom`'s `rename` map) so nothing dangles; `BackendAntlr` re-emits under the recorded original spelling when present, not Gramaire's internal ALL-CAPS name.** | The real, underlying need from the debate that prompted this (round-trip fidelity with ANTLR/Bison/EBNF, none of which share Gramaire's case convention) doesn't actually require relaxing Gramaire's own authoring format — it requires Gramaire to *remember* an import's original spelling. `@spelling` delivers exactly that, safely, without touching fence classification, the self-hosting bootstrap, or any existing golden fixture (the field is optional and omitted from JSON when absent, following the same convention as `sourceLayout`/`diagramView`/`name`, D56/D58). Rejected: relaxing `classifyFenceContent` to accept non-ALL-CAPS Tokens lines too — that would need a *new* disambiguation mechanism (contradicting D-grammar-roles' \"no keyword, ever\" stance) to replace the one case-based signal removes, for no benefit `@spelling` doesn't already provide. |\n | D62 | A renamed nonterminal's original spelling round-trips via a `<Name>.gramaire-names.json` sidecar, never an in-band `.g4` comment | **The matching hazard on the opposite side of the ANTLR converter from D61: `BackendAntlr.ruleName` force-lowercases a nonterminal's first letter on export (`Expr` → `expr`), because a real ANTLR parser rule can never start uppercase (that's always a lexer rule in ANTLR, not a style choice — unlike D61's tokens, this can never arise on IMPORT, since any real ANTLR parser rule name is already lowercase-initial by ANTLR's own hard requirement). The hazard is real only in the `emit --backend antlr` → hand-edit → `import` workflow the CLI advertises side by side: without a fix, Gramaire's own capitalized house style (`Expr`, `Term`) silently and permanently becomes lowercase on that round trip. A first pass recorded the original name as an in-band `// @gramaire-name: Expr` comment in the exported `.g4` itself — rejected on review: ANTLR's own file format isn't Gramaire's to annotate, and it's the one export among all of Gramaire's backends that would carry a Gramaire-specific marker a plain ANTLR toolchain has no reason to understand. Replaced with `BackendAntlr.renameMap(ir): Map[String, String]` (ANTLR name → original Gramaire name, only for rules actually renamed) written to a **separate sidecar file**, `<Name>.gramaire-names.json`, alongside the plain, unannotated `.g4` — the `Backend.emit` `Vector[Output]` shape (already multi-file-capable) needed no changes to carry it. `ConvertAntlr.importAntlr` gained an optional `renameMap` parameter (default empty) it threads into the *same* `rename` map D61's `lexerRename` already passes through `renderTopAlt`, so every reference to a restored rule — not just its own heading — comes back consistently; `ConvertAntlr.parseRenameMap` decodes the sidecar's JSON, and the CLI's `import` command (`renameMapSidecarOf`) looks for and reads it automatically next to the `.g4` being imported, falling back to `Map.empty` (silently, not an error) when no sidecar exists — the common case for any hand-written ANTLR grammar, or a `.g4` Gramaire exported but renamed nothing in.** | Confirms and closes the exact gap Marcus/Devon named in the original ANTLR-vs-Gramaire debate (\"a workflow the CLI literally advertises side-by-side\") without polluting an output format Gramaire is a guest in — the `.g4` a `gramaire emit --backend antlr` user hands to a colleague, or feeds into a real `antlr4` toolchain, reads as completely ordinary ANTLR, with the provenance living in a file only Gramaire's own `import` ever needs to look for. Mirrors `.gram.lock`'s own established sidecar convention (a derived, drift-checkable artifact living beside its source) rather than inventing a second in-band-comment mechanism alongside D61's real-syntax `@spelling(...)` modifier (which stays as is: it extends *Gramaire's own* Tokens format, not someone else's). |\n | D63 | Heading lines split out of a `.gram.md` prose gap into their own Notebook cell, Livebook-style | **A `.gram.md` document's markdown gaps (the text between/around `` ```gramaire `` fences) used to collapse into exactly one \"prose\" `DocBlock` regardless of how much structure they held — a `## Term` heading, several paragraphs, an image, all fused into one big click-to-edit textarea. `document.ts`'s `buildDocument` now takes an injected `splitProse` callback (the same injection pattern `sectionEndIndex`'s own `headingLevelOf` already uses, keeping this module markdown-agnostic per its own header) that further divides each gap into a new `\"heading\"` `DocBlockKind` (one `#`/`##`/`###` line, `headingLevel: 2 \\| 3 \\| 4`) and the `\"prose\"` runs between them. `markdown.ts`'s new `splitHeadingsFromProse` is the real implementation `GramaireNotebookIsland.tsx` supplies, sharing the exact same heading regex `parseMarkdownLite` already matches against; its descriptors always join back to the original gap text byte-for-byte, so no content is ever silently dropped, only reattributed to a different sibling block than before. A real local Livebook instance was fetched and inspected as UX reference during design: Livebook's own model is that a heading defines a section boundary and is itself a separately addressable element, while the body underneath (paragraphs, images, tables) stays one shared unit — the user explicitly confirmed this coarser scope over a more aggressive per-paragraph/per-table split. `headingLevelOf` (the callback `sectionEndIndex`/`moveBlock` need for Livebook-style section-aware reordering, which already existed for the old \"first heading inside a bigger prose block\" case) simplifies from a `parseMarkdownLite`/`leadingHeading` re-parse to a direct `block.headingLevel` field read; moving a heading cell now moves its own trailing content along with it via that same, unmodified mechanism — the user's own explicit requirement, verified end-to-end against a real multi-heading document (not just the pre-existing generic stub-driven tests). `isPaperBlock` widens to include `\"heading\"` so Paper view/PDF export keep showing every heading. Re-splitting a gap only ever happens at `buildDocument` time — the existing debounced-reshape-after-`scheduleEvaluate` flow, never synchronously on every keystroke — the same rule that already governs every other structural change in this file, for the same OOM-avoidance reason its own header comments document; a heading typed mid-edit inside an existing prose cell renders correctly immediately (embedded headings already render inline) and only splits into its own cell once the next reshape lands.** | The literal ask — \"like Gramaire fences are separate live-edit elements, each different markdown cell should be scoped\" — needed a real UX reference to size correctly: a naive reading could have meant splitting every paragraph and table into its own cell too, a much larger, more invasive change with no analogue in the reference the user actually pointed at. Fetching and inspecting a real Livebook instance resolved that ambiguity concretely rather than guessing, and confirming the narrower scope with the user before implementing kept the change small — no new synchronous re-split machinery, no new `\"table\"` kind, just one new block kind riding the SAME injected-callback, debounced-reshape, and section-move mechanisms this file already had for its other markdown-agnostic concerns. |\n+| D64 | `## Externals` extended to native `.gram` (`external NAME {% ... %}`) | **`gramaire strip` used to silently drop a `.gram.md`'s entire `## Externals` section when projecting to native `.gram` (ADR D36): `Lr.strip`'s `section` helper keeps a `##` section only if it contains a `` ```gramaire `` fence, and a `### name` subsection's own fence is real host code (` ```javascript `, never `` ```gramaire ``), so every embedded delegate implementation vanished on export — discovered while translating a genuinely delegate-heavy example grammar to native format, not a synthetic case. Fixed both directions: `Lr.strip`'s new `nativeExternalsSection` renders each `### name` as `external NAME {%\\n<code>\\n%}` (reusing `externalsSections` against just that section's own lines); `Lr.toFenced`'s new `extractNativeExternals` recognizes that exact shape — `NAME` then a bare `{%` ends the opening line, a bare `%}` on its own line closes it, however many lines the body spans, found and removed via plain line matches *before* `decomment`/`isTokenDef`/`isPrecDecl`/`isSettingDecl` ever see the pool of lines, so a JS-internal `//`/`/* */` inside the body is never misread as a grammar comment — then synthesizes the extracted `(name, code)` pairs straight into the same `## Externals`/`### name`/` ```javascript ` shape `Lr.withExternals`/`externalsSections` already scan for. That one synthesis step is the entire fix: `withExternals`/`parseWithDocs`/`IR.buildIRP`/`BackendJs` all consume `Grammar.externals` completely unchanged, native or markdown. Always tagged `javascript` on the synthesized fence, matching an ordinary inline `{% %}` action's own convention of never spelling out a language at the call site (the document's `lang:` implies it; `BackendJs.jsImplOf` resolves via `Lr.normalizeLang` regardless of which JS spelling produced the tag). Round-trip verified end-to-end: `StripSuite` strips `examples/calc-delegate.gram.md`, reparses the native output with `parseWithDocs`, and confirms `Pow`/`Round`'s embedded code survives verbatim while `Call` (deliberately unresolved) still carries none.** | Caught by actually exercising the feature on a real grammar rather than assuming `strip`'s existing per-`##`-section handling already covered every reserved heading — `## Externals` was added (D49) after `strip`/`toFenced` were last touched, and nothing forced the two to be revisited together. Reusing the existing markdown-shaped `externalsSections`/`withExternals` machinery as the synthesis TARGET (rather than teaching `Grammar`/`IR`/`BackendJs` a second, native-only externals representation) keeps this a notation-level, round-trip-only concern, the same architectural choice D49 itself made for keeping `externals` a document-structure attachment rather than a core-grammar production. |\n \n ## Platform & tooling roadmap (D43)\n ","raw_url":"https://github.com/wstein/gramaire/raw/1c66e7daec0192dcc1954ff0e6b242f04fd79a57/docs%2Fmulti-backend-implementation-plan.md","sha":"1dc5e0d4c3184c38f53347c6a3b0e77597c216f8","status":"modified"}]}