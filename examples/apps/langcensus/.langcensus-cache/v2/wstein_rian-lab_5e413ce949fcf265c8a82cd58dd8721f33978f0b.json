{"files":[{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/5e413ce949fcf265c8a82cd58dd8721f33978f0b/CLAUDE.md","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/CLAUDE.md?ref=5e413ce949fcf265c8a82cd58dd8721f33978f0b","deletions":1,"filename":"CLAUDE.md","patch":"@@ -96,7 +96,10 @@ Source flows through these stages; the **typed Core IR is the spine** that decou\n      limitation, see its moduledoc); **`Rian.Beam` is the real BEAM backend.** So BEAM has two\n      emitters — don't confuse them.\n    - **`Rian.JS`** — ECMAScript (ADR-0049 Tier 1); partial (`struct`/`with`/lambdas/FFI raise\n-     `Unsupported`).\n+     `Unsupported`). Has **two print modes** over one lowering (ADR-0086 §5): `compile/1` (the\n+     runtime `.mjs`) and `compile_types/1` (a TypeScript `.d.mts` declaration sidecar — the typed\n+     FFI-boundary *view*; doc-not-gate, capabilities erased, unmappable types → `unknown`; written\n+     beside the `.mjs` by `rian build --js`).\n    - **`Rian.JVM`** — Kotlin/JVM (ADR-0049 **Tier 2**); a direct source emitter on Core, like JS.\n      MVP: functions, primitives, operators, `if`, `case`, sum variants (→ `sealed interface` +\n      `data class` + smart-cast patterns), lists (`listOf` literals + cons/closed clause patterns →","raw_url":"https://github.com/wstein/rian-lab/raw/5e413ce949fcf265c8a82cd58dd8721f33978f0b/CLAUDE.md","sha":"44958b0fd28de66afa413a2438935660b6939780","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2FREADME.md","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2FREADME.md?ref=5e413ce949fcf265c8a82cd58dd8721f33978f0b","deletions":2,"filename":"docs/README.md","patch":"@@ -22,7 +22,7 @@ the normative behaviour the implementation must match).\n | Lexer + declaration parser | **Stage 0.1 (broad)** | [decl.ex](../lib/rian/decl.ex) — token-driven; `type`/`struct`/`alias`/`const`/`use`/`mod` + single/multi-param `def`, `:=`/block/`case` bodies, `when` guards, `pub` visibility. Struct & sum-variant construction (positional + named) and constant references lower per target. The modules tour ([05_modules.rian](../examples/rian/05_modules.rian)) parses and lowers end-to-end. Not yet: `macro` files |\n | Real type checker | **Compile gate + flow narrowing** | [check.ex](../lib/rian/check.ex) — ADR-0034 §1/§4: unification-based inference gates `compile`, rejecting only *provable* return-type mismatches; `case` arms and pattern clauses narrow bound variables to the matched variant's field types. Error sets (§2) are checked at the `T \\| E` boundary; concrete generics (`Vec(T)`) infer. Protocol bounds (§3) await their surface |\n | Erlang abstract-forms backend (Stage 0.5) | Implemented | [beam.ex](../lib/rian/beam.ex) — lowers to the Erlang abstract format + `:compile.forms` → loadable `.beam` (no `eval`); the default BEAM execution path. **Dialyzer-checkable**: every function emits a `-spec` and every sum/struct a named `-type` (kept via `:debug_info`); `Int*→integer()`, `Float*→float()`, `Bool→boolean()`, `String→binary()`, `Vec(T)→[t()]`, `Fn(A,R)→fun((a())->r())`, sum→union, struct→map\\|tuple, generic→`any()` |\n-| ECMAScript backend | Implemented (partial) | [js.ex](../lib/rian/js.ex) — ADR-0049 Tier 1 on the core IR; **protocol dispatch** (ADR-0061 §3, node-verified) incl. **struct dispatch**; **structs** as `__struct__`-tagged objects (construction/field-access/patterns); **atoms** (→ JS strings) and **`Result`** (`{:ok,_}`/`{:error,_}` → `[\"ok\", v]`) incl. tuple/atom `case` patterns, node-verified; gaps: `with`/lambdas/general FFI |\n+| ECMAScript backend | Implemented (partial) | [js.ex](../lib/rian/js.ex) — ADR-0049 Tier 1 on the core IR; **protocol dispatch** (ADR-0061 §3, node-verified) incl. **struct dispatch**; **structs** as `__struct__`-tagged objects (construction/field-access/patterns); **atoms** (→ JS strings) and **`Result`** (`{:ok,_}`/`{:error,_}` → `[\"ok\", v]`) incl. tuple/atom `case` patterns, node-verified; **TypeScript `.d.mts` sidecar** (`compile_types/1`, ADR-0086 §5 — the typed FFI-boundary view, `tsc`-verified) shipped in the npm package; gaps: `with`/lambdas/general FFI |\n | Self-hosting (compiler in Rian) | Started | six-layer pipeline in Rian compiles to `.beam`; real-lexer port at **slice 3** (ids, all 16 keywords, comparisons `< > <= >= == !=`, word-ops `and/or/not/in/rem/div`, and **string `\"…\"` + char `'X'` literals** scanned char-by-char), diffed vs reference by [fixpoint.ex](../lib/rian/fixpoint.ex) and **locked in CI** ([ci.yml](../.github/workflows/ci.yml)). Out of slice: floats/brackets/escapes |\n | Protocols & impls (ADR-0042, ADR-0061) | **multi-target** | [protocol.ex](../lib/rian/protocol.ex) — coherence-checked desugar; `forall T: Bound` enforced at call sites ([check.ex](../lib/rian/check.ex)). **BEAM** guarded dispatcher (primitive/sum/struct); **JS** native dispatcher (`typeof`/tag, node-verified); **Rust** fresh `trait Rian<P>` + `impl`s + `fn f<T: RianEq>` with UFCS calls ([lower.ex](../lib/rian/lower.ex), rustc-verified). Deferred: dynamic dispatch, multi-unit Rust dedup, target-relative coherence |\n | Rian-native tests (ADR-0057, ADR-0060 §3) | **multi-target** | [test.ex](../lib/rian/test.ex) — `@test def name() Bool` runs on the BEAM (`Rian.Test.exunit/1` → ExUnit), and lowers to a **Rust `#[test]`** module (`Rian.Test.rust/1`, `rustc --test`-verified) and a **`node:test`** module (`Rian.Test.js/1`, `node --test`-verified) — the same `@test` surface, three native harnesses ([14_test_framework.rian](../examples/rian/14_test_framework.rian)) |\n@@ -101,7 +101,7 @@ individual specs as *component-level* unless the tour exercises them end-to-end.\n | [0083](adr/0083-union-types.md) | **Union types — anonymous structural `A \\| B`, narrowed by type** — the principled resolution of ADR-0034's *deferred* \"introduce union types\" item (the verbose \"named sum per multi-shape param\" pain, e.g. self-host `lower_body`). A value of `A \\| B` is one of the members, carrying its own runtime identity; consumed by a new **type-pattern** `case x do n Int53 -> … ; s String -> …` that reuses the protocol-dispatch discriminator (ADR-0042). Resolves the `\\|` overload **by position** (param/field/binding = value union; **return** stays `Result` sugar, ADR-0040 — no break to `with`/`<-`). Per-target: BEAM/JS dynamic (free), **Rust a synthesized name-mangled `enum`**, JVM `Any`+`when is` (a primitive can't share a sealed interface, same constraint as dispatch). Reach pins a union off a nominal target until its emitter lands (ADR-0000). Staged: parse → BEAM/JS → checker narrowing → Rust enum synthesis → JVM. Fallback spelling `Union(A, B)` if `\\|` proves too subtle | Proposed (direction) |\n | [0084](adr/0084-purescript-port.md) | **Re-platform the reference compiler from Elixir to PureScript (purerl)** — port `lib/` file-by-file to PureScript so a real type system replaces the **Dialyzer** success-typing crutch (`@spec`/`@rian_sig` become types). Backend is **purerl** (PureScript→Erlang), not JS: the compiler keeps running on the BEAM, so the five `:compile.forms` sites + 26 `:erlang`/`:code`/`:file`/`:os` modules become a **small Erlang-FFI boundary at the leaves** and the self-host bootstrap (byte-identical `.beam`, v1==v2) survives — a JS port would have deleted it. Behaviour-preserving: the **language ADRs and Reach matrix are unchanged**; parity vs the Elixir reference fixtures is the gate (ADR-0000 — \"compiles\" ≠ \"correct\"). Migrate **leaf-first along the Core spine** (`Token`→`Lexer`→`Core`→parsers→gates→emitters), hybrid repo until each module reaches parity, then drop its Elixir twin. **Phase 4 closed:** the purerl chain is verified end-to-end on the BEAM and the **entire front-end + inference + gate stack is ported** at **byte-for-byte parity** (the full fixture corpus — [docs/purescript-migration.md](purescript-migration.md) tracks the live `N/N` count; lex → `Pratt`/`Decl` → `Core` → the refutation/portability/capability/expansion gates → `Check`/`InferLocal` inference → the `Assemble`/`Comptime` tail) via pure no-FFI ports + an Erlang parity harness; the `Beam`/`Lower`/`JS`/`JVM` emitters remain. The parity gate is **local** (`scripts/purerl-build.sh` per commit), not yet in CI. Toolchain: legacy spago 0.21 + the purerl *dhall* package set (`purs/README.md`). Plan: [docs/purescript-migration.md](purescript-migration.md). Open: retire vs port the Elixir transpiler once self-hosted | Accepted (in progress); Phase 4 closed, emitters remain |\n | [0085](adr/0085-haxe-style-dynamic-backends.md) | **Haxe-style dynamic backends (Lua, PHP, Neko) — candidate targets** — [Haxe](https://haxe.org) is the standing proof one typed source lowers to all three in production; this borrows that **reach as proof-of-viability** while refusing Haxe's **sprawl** (the very lesson ADR-0049 §5a cites). All three are dynamic/GC — a *re-skin* of the JS/Python emitter on Core (ADR-0050/0071): runtime-tagged sums + a trailing `raise` (hosts don't check exhaustiveness, the inverse of Swift), atoms → strings, capabilities erased, `Option` tagged (no `nil`). **Three divergent numeric floors** (ADR-0064): Lua/PHP native 64-bit → `Int53`+fixed-widths reach; **Neko 31-bit** → numerically a *structural* target only; `Int` (bignum) pins off all three. **No `:lua`/`:php`/`:neko` reach until the interpreter runs in CI** (ADR-0000/0058). Enter at **Tier 3** under §5a (target budget + frozen LCD); sequence **Neko-first** (cheapest CI canary) → Lua → PHP, never in parallel, after self-host (ADR-0063) | Proposed (exploratory); candidates only |\n-| [0086](adr/0086-competitive-strategy-and-reach-honesty-harness.md) | **Competitive strategy: prove the reach matrix, then depth before breadth** — names the field ([Gleam](https://gleam.run)/[Roc](https://www.roc-lang.org)/[Haxe](https://haxe.org)/[Scala.js](https://www.scala-js.org)/[Swift](https://swift.org)/TS) and frames the only defensible position as the **intersection none of them occupies**: one typed source → BEAM+Rust+JS+JVM with ownership **inferred** (not hidden like Roc, not hand-written like Swift/Rust) and reach **inferred + honestly refused** (not hand-picked like Gleam, not compile-and-pray like Haxe). Core fix: that moat is **asserted, not proven** — reach honesty rests on a *toy corpus* (selection-bias honesty). §2 gates on a **generative reach-honesty property** (*reach claims `:t` ⟹ the real `t` toolchain compiles+runs the emitted code*), the gate every new target (ADR-0071/0072/0085) passes through — its mechanism split to **ADR-0087**. §3 **depth before breadth** (close `:rs` gaps; self-host outranks targets); §4 records that the strategy *rests on* **protocol coherence**, owned and shipped in **ADR-0061 §5** (not re-decided here); §5 **TypeScript as a typed view** over the JS backend (FFI-boundary types, not a 5th emitter); §6 legible reach diagnostics; §7 written one-sentence competitor deltas; §8 effects-as-rows (Koka) parked as a north star | Proposed |\n+| [0086](adr/0086-competitive-strategy-and-reach-honesty-harness.md) | **Competitive strategy: prove the reach matrix, then depth before breadth** — names the field ([Gleam](https://gleam.run)/[Roc](https://www.roc-lang.org)/[Haxe](https://haxe.org)/[Scala.js](https://www.scala-js.org)/[Swift](https://swift.org)/TS) and frames the only defensible position as the **intersection none of them occupies**: one typed source → BEAM+Rust+JS+JVM with ownership **inferred** (not hidden like Roc, not hand-written like Swift/Rust) and reach **inferred + honestly refused** (not hand-picked like Gleam, not compile-and-pray like Haxe). Core fix: that moat is **asserted, not proven** — reach honesty rests on a *toy corpus* (selection-bias honesty). §2 gates on a **generative reach-honesty property** (*reach claims `:t` ⟹ the real `t` toolchain compiles+runs the emitted code*), the gate every new target (ADR-0071/0072/0085) passes through — its mechanism split to **ADR-0087**. §3 **depth before breadth** (close `:rs` gaps; self-host outranks targets); §4 records that the strategy *rests on* **protocol coherence**, owned and shipped in **ADR-0061 §5** (not re-decided here); §5 **TypeScript as a typed view** over the JS backend (FFI-boundary types, not a 5th emitter) — **shipped 2026-06-22** (`Rian.JS.compile_types/1` → a `.d.mts` sidecar in the npm package, `tsc`-verified; doc-not-gate, so safe ahead of §2); §6 legible reach diagnostics; §7 written one-sentence competitor deltas; §8 effects-as-rows (Koka) parked as a north star | Proposed (§5 implemented) |\n | [0087](adr/0087-generative-reach-honesty-harness.md) | **Generative reach-honesty harness — prove the matrix, don't assert it** — the load-bearing mechanism split out of ADR-0086 §2. Today reach honesty rests on a hand-curated corpus (*\"green by selection bias, NOT parity\"*); this **locks the invariant** — *if `Rian.Reach` claims `p` reaches `t`, the `t` toolchain compiles+runs the emitted code* (over-claim = P0; pins must carry a *modeled* reason) — and gates on a **property-based generator over Core** (sums/structs/generics/`Fn`/numeric-lattice/capabilities) driving both the reach verdict and the real `rustc`/`node`/`kotlinc`/BEAM backend, asserting they agree. Catches the *unmodeled* blocker a fixture can't. Generated programs are well-typed/total/terminating; failures **shrink** capability-preservingly to a minimal P0; the same harness also gates **coherence** (ADR-0061 §5). Fixtures demoted to regression. **Stage 1 landed** (`reach_honesty_property_test`, `@tag :rust`): a seeded generator of well-typed integer programs over a random width asserts the over-claim invariant on `:rs` (must compile under rustc) + the ADR-0064 width contract both ways. Type-directed generator, shrinking, `:js`/run lanes, coherence consumer = Stage 2 | Proposed (direction); Stage 1 implemented |\n | [0088](adr/0088-reference-interpreter-as-oracle.md) | **Reference interpreter as the executable spec & conformance oracle (not a runtime target)** — \"rian-vm\" was three proposals in one name (interpreter / runtime VM / playground engine); separating them *is* the decision. **Build P1:** `Rian.Eval`, a small **total tree-walking** evaluator over `Rian.Core` — a pure Core consumer like an emitter (no fork, no bytecode, no GC, sequential-only), **promoted to the conformance oracle** via an `:eval` lane in `Rian.ConformanceTest` (`eval == each emitter`). This **inverts the hierarchy**: a BEAM/`eval` disagreement is a BEAM bug, not the spec. **P3:** the playground engine = P1 → WASM/JS, shipping **ahead** of self-host → JS (amends ADR-0052). **Reject P2** (bytecode VM as a runtime target) on the charter — ADR-0000 no-separate-runtime + ADR-0050 no-fork + ADR-0057 native concurrency (removes the VM's purpose) + ADR-0058 (widens the honesty matrix) + ADR-0063 (competes with self-host); **defer-not-forbid** (Elena's dissent: a future VM's precondition *is* the oracle, so P1-first is path-independent). Reject P4 (status quo, too slow) / P5 (bytecode interchange, out of scope). Anti-drift gate: `:eval` may only run constructs ≥1 emitter supports. Naming: the debate's `Rian.Interp` is **already** the interpolation desugar (ADR-0069) → `Rian.Eval` | Proposed |\n ","raw_url":"https://github.com/wstein/rian-lab/raw/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2FREADME.md","sha":"1efe84a84cb288a38456afa6611bc3faeed5cffb","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/rian-lab/blob/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2Fadr%2F0049-backend-target-roadmap.md","changes":5,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0049-backend-target-roadmap.md?ref=5e413ce949fcf265c8a82cd58dd8721f33978f0b","deletions":1,"filename":"docs/adr/0049-backend-target-roadmap.md","patch":"@@ -246,6 +246,9 @@ ADR-0031 already identifies.\n - **JVM value-type story** — boxing vs Project Valhalla value classes for opaque types / ranges\n   (ADR-0043/0036) and `Int*` (ADR-0034) on the JVM.\n - **ECMAScript module/interop conventions** — ESM vs CJS output; npm interop (the JS analogue of the\n-  ADR-0026 Hex/rebar3 ecosystem work).\n+  ADR-0026 Hex/rebar3 ecosystem work). *Partly resolved:* output is **ESM** (`.mjs`, `\"type\":\n+  \"module\"`), and the npm package now ships a **TypeScript declaration sidecar** `<name>.d.mts`\n+  (`Rian.JS.compile_types/1`, `\"types\"` in `package.json`) — the typed FFI-boundary view of\n+  **ADR-0086 §5**. CJS interop remains open.\n - **Per-tier conformance-matrix contents** — the concrete cross-target test set each tier must pass.\n - **Promotion criteria** — what advances a target from Tier 2 → Tier 1 (or Tier 3 → Tier 2).","raw_url":"https://github.com/wstein/rian-lab/raw/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2Fadr%2F0049-backend-target-roadmap.md","sha":"aa51a8f8792057c57a1d141644f8cdd93431068e","status":"modified"},{"additions":30,"blob_url":"https://github.com/wstein/rian-lab/blob/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2Fadr%2F0086-competitive-strategy-and-reach-honesty-harness.md","changes":32,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0086-competitive-strategy-and-reach-honesty-harness.md?ref=5e413ce949fcf265c8a82cd58dd8721f33978f0b","deletions":2,"filename":"docs/adr/0086-competitive-strategy-and-reach-honesty-harness.md","patch":"@@ -1,7 +1,7 @@\n # ADR-0086 — Competitive strategy: prove the reach matrix, then depth before breadth\n \n-**Status:** Proposed\n-**Implemented:** no — a *strategy* (sequencing + positioning), whose one load-bearing mechanism (the generative reach-honesty harness) is split into ADR-0087. No code lands on the strength of this ADR until ADR-0087's gate (§2) is green; everything downstream is *ordered* by it, not by this document.\n+**Status:** Proposed · **§5 (TypeScript view) Accepted + implemented 2026-06-22**\n+**Implemented:** partial — the *strategy* (sequencing + positioning) is direction-only, and its one load-bearing reach mechanism (the generative reach-honesty harness) is split into ADR-0087; **no reach-affecting code lands on this ADR until ADR-0087's gate (§2) is green.** The exception is **§5, now shipped** (`Rian.JS.compile_types/1` + the `.d.mts` sidecar in the npm package): the TS view is *documentation, not a gate*, so it neither rests on §2 nor widens the portable core — it is the one part of this ADR safe to build ahead of the gate.\n **Refines:** ADR-0000 (honesty bar — strengthens *how* the matrix is verified: generative property, not a hand-picked corpus), ADR-0058 (reachability + CI parity — adds the property the parity lane must satisfy), ADR-0049 §5a (backend tiers / target budget — supplies the gate Swift/Python/Lua-PHP-Neko candidates pass *through*).\n **Depends on / delegates to:** ADR-0087 (generative reach-honesty harness — owns the §2 mechanism this strategy gates on), ADR-0061 §5 (target-set-relative coherence — owns the §4 precondition this strategy rests on; this ADR re-decides neither).\n **Refs:** ADR-0050 (typed Core IR is the emitter spine), ADR-0057 (concurrency native-per-target — the deliberate non-portability the thesis depends on), ADR-0064 (portable numeric contract — the largest existing honest-pin surface), ADR-0070 (`val` default capability), ADR-0071 (Python — *gated* behind §2/§3), ADR-0072 (Swift — *gated* behind §2/§3), ADR-0084 (PureScript port — the mid-flight cost §3 protects), ADR-0085 (Haxe-style dynamic triad — the §5a discipline this ADR generalizes).\n@@ -111,6 +111,34 @@ it is one backend with two print modes, it adds a bounded annotation cost, not a\n drift-tax row. This is also the direct answer to \"why not just write TypeScript\": *typed once, lowered\n to BEAM/Rust/JVM too — a claim TS cannot make by construction.*\n \n+**Amended 2026-06-22 — emit detail decided and shipped (resolves the §5 open item).**\n+The typed view is a **declaration sidecar**, not `.ts` source: `Rian.JS.compile_types/1` is\n+a **second print mode** of the JS backend (same `Decl.parse` → `Check.gate!` → `Opaque.erase`\n+prologue as `compile/1`) that emits a TypeScript declaration file for the module's **exported**\n+surface — `export function`/`export const` for every `pub` declaration plus `type`/`interface`/range\n+aliases for the user types they reference. The runtime `.mjs` is untouched.\n+\n+- **Filename: `<name>.d.mts`, not `<name>.d.ts`.** The runtime is ESM (`.mjs`), and TypeScript\n+  resolves an ESM module's declarations from the sibling `.d.mts` (a `.d.ts` does *not* resolve for\n+  a relative `import \"./m.mjs\"` — verified with `tsc`). The npm `package.json` also gains a\n+  `\"types\"` field pointing at it, so a package-name import is typed too. The build\n+  (`rian build --js -o`, ADR-0082 step 4) writes the sidecar beside the `.mjs`.\n+- **Capabilities leave no TS trace.** `val`/`iso`/`tag`/`ref` shape Rust/BEAM only (ADR-0055) and\n+  have no JS runtime meaning, so they are fully erased in the view (a `Vec(T)` param is `T[]` whether\n+  it arrived `val` or `iso`).\n+- **Reach surfaces only by omission.** The view declares only what the `.mjs` actually exports; a\n+  function that is not JS-reachable never reaches this print mode (`compile/1` fails first). The\n+  declarations *describe runtime values* — a sum is `[\"Ctor\", …]`, a struct `{__struct__: \"Name\", …}`,\n+  a `Result` an `[\"ok\", v]` / `[\"error\", e]` pair, an `Int` a `bigint`, an `Int53`/`Char` a `number`.\n+  A type outside the faithfully-mappable subset becomes `unknown` (honest), never a misleading `any`.\n+- **Types are documentation, not a gate** (unchanged): the reach gate stays `Rian.Reach` + §2.\n+\n+This sets the **pattern** a future typed view over another *dynamic* target (the ADR-0085 Lua/PHP/Neko\n+triad, or ADR-0071 Python — all type-erasing like JS) would reuse: a second print mode over the same\n+lowering, not a new emitter. It **adds no target and widens nothing** — the §5a admission gate and the\n+target budget (ADR-0049 §5a) stand; a typed view is a print mode of an *already-admitted* target, never\n+a way to smuggle one in.\n+\n ### 6. Reach blockers must read like diagnostics, not mysteries\n \n Inference is a moat for the compiler and a mystery for the user unless it is *legible*. Every reach","raw_url":"https://github.com/wstein/rian-lab/raw/5e413ce949fcf265c8a82cd58dd8721f33978f0b/docs%2Fadr%2F0086-competitive-strategy-and-reach-honesty-harness.md","sha":"290dd8fe3e692f56d732a9685448b214465da1a6","status":"modified"}]}