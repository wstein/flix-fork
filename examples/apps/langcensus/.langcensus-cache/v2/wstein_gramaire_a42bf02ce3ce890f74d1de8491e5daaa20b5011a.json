{"files":[{"additions":156,"blob_url":"https://github.com/wstein/gramaire/blob/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2FLabIsland.tsx","changes":157,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flab%2FLabIsland.tsx?ref=a42bf02ce3ce890f74d1de8491e5daaa20b5011a","deletions":1,"filename":"site/src/lab/LabIsland.tsx","patch":"@@ -8,6 +8,7 @@ import type {\n   CstToken,\n   DiagnosticInfo,\n   DiagramView,\n+  ForestResult,\n   LabRequest,\n   LabResponse,\n   LlStepInfo,\n@@ -1385,7 +1386,7 @@ function AllParsesPanel() {\n         </button>\n       </div>\n       {graphViewEnabled.value ? (\n-        <pre class=\"lab__tree\">{mermaidOfForest(forest)}</pre>\n+        <ForestGraph forest={forest} />\n       ) : (\n         forest.parses.map((p, i) => (\n           <div key={i} class=\"lab__forest-item\">\n@@ -1715,6 +1716,160 @@ function RailroadSvg({ svg }: { svg: string }) {\n   );\n }\n \n+// Native SVG rendering for \"Graph view\" — no external graphing library (no async load, no\n+// third-party layout engine): a small tidy-tree layout, drawn with the same `rr-nonterm`/`rr-term`\n+// visual language the server-rendered railroad diagrams already use (RailroadSvg above), styled by\n+// the matching rules in lab.css. `mermaidOfForest` (the Mermaid DSL text) still backs the separate\n+// \"copy graph (Mermaid)\" button — an export format for pasting elsewhere, independent of how this\n+// view renders inline.\n+const GRAPH_ROW_H = 56;\n+const GRAPH_BOX_H = 28;\n+const GRAPH_CHAR_W = 7.2;\n+const GRAPH_PAD_X = 14;\n+const GRAPH_GAP = 14;\n+const GRAPH_MIN_W = 40;\n+\n+function escXml(s: string): string {\n+  return s\n+    .replace(/&/g, \"&amp;\")\n+    .replace(/</g, \"&lt;\")\n+    .replace(/>/g, \"&gt;\")\n+    .replace(/\"/g, \"&quot;\");\n+}\n+\n+interface GraphBox {\n+  cx: number; // center x\n+  y: number; // top y\n+  width: number;\n+  label: string;\n+  term: boolean;\n+}\n+interface GraphEdge {\n+  x1: number;\n+  y1: number;\n+  x2: number;\n+  y2: number;\n+}\n+\n+// A tidy (if not fully collision-proof for pathological shapes) top-down tree layout: each leaf\n+// gets its own horizontal slot, each internal node centers over its children — the standard \"mean\n+// of children\" placement, good enough for the small parse trees this view is ever asked to show.\n+function layoutTree(\n+  root: CstNode,\n+  originX: number,\n+  boxes: GraphBox[],\n+  edges: GraphEdge[],\n+): number {\n+  function labelOf(node: CstNode): { label: string; term: boolean } {\n+    return \"token\" in node\n+      ? { label: node.text.length > 0 ? node.text : node.token, term: true }\n+      : { label: ruleName(node.rule), term: false };\n+  }\n+  function widthOf(label: string): number {\n+    return Math.max(GRAPH_MIN_W, label.length * GRAPH_CHAR_W + GRAPH_PAD_X * 2);\n+  }\n+  // Pass 1 (post-order): how much horizontal space each subtree needs.\n+  function extent(node: CstNode): number {\n+    const { label } = labelOf(node);\n+    const own = widthOf(label);\n+    if (\"token\" in node || node.children.length === 0) return own;\n+    const kidsExtent = node.children.reduce((sum, c) => sum + extent(c), 0);\n+    const gaps = GRAPH_GAP * (node.children.length - 1);\n+    return Math.max(own, kidsExtent + gaps);\n+  }\n+  // Pass 2 (pre-order): place this node's box, then lay out children left to right within the\n+  // extent already computed, and center this node over them.\n+  function place(node: CstNode, xStart: number, depth: number): number {\n+    const { label, term } = labelOf(node);\n+    const own = widthOf(label);\n+    const y = depth * GRAPH_ROW_H;\n+    const kids = \"token\" in node ? [] : node.children;\n+    if (kids.length === 0) {\n+      const cx = xStart + own / 2;\n+      boxes.push({ cx, y, width: own, label, term });\n+      return cx;\n+    }\n+    let cursor = xStart;\n+    const childCx: number[] = [];\n+    for (const c of kids) {\n+      const cExtent = extent(c);\n+      childCx.push(place(c, cursor, depth + 1));\n+      cursor += cExtent + GRAPH_GAP;\n+    }\n+    const cx = (childCx[0] + childCx[childCx.length - 1]) / 2;\n+    boxes.push({ cx, y, width: own, label, term });\n+    for (const ccx of childCx) {\n+      edges.push({ x1: cx, y1: y + GRAPH_BOX_H, x2: ccx, y2: y + GRAPH_ROW_H });\n+    }\n+    return cx;\n+  }\n+  place(root, originX, 0);\n+  return extent(root);\n+}\n+\n+function svgOfForest(forest: ForestResult): string {\n+  const boxes: GraphBox[] = [];\n+  const edges: GraphEdge[] = [];\n+  const labels: Array<{ x: number; y: number; text: string }> = [];\n+  let cursorX = 0;\n+  let maxDepthY = 0;\n+  forest.parses.forEach((p, i) => {\n+    const startX = cursorX;\n+    const before = boxes.length;\n+    const treeExtent = layoutTree(p, startX, boxes, edges);\n+    if (forest.parses.length > 1) {\n+      labels.push({\n+        x: startX + treeExtent / 2,\n+        y: -10,\n+        text: `Parse ${i + 1}`,\n+      });\n+    }\n+    for (let j = before; j < boxes.length; j++)\n+      maxDepthY = Math.max(maxDepthY, boxes[j].y);\n+    cursorX += treeExtent + GRAPH_GAP * 3;\n+  });\n+  const width = Math.max(GRAPH_MIN_W, cursorX - GRAPH_GAP * 3);\n+  const height = maxDepthY + GRAPH_ROW_H;\n+  const topPad = forest.parses.length > 1 ? 24 : 4;\n+  const parts: string[] = [\n+    `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"${width}\" height=\"${height + topPad + 4}\" viewBox=\"-4 ${-topPad} ${width + 8} ${height + topPad + 4}\" role=\"img\" aria-label=\"Parse forest graph\">`,\n+  ];\n+  for (const e of edges) {\n+    parts.push(\n+      `<path class=\"rr-track\" d=\"M${e.x1} ${e.y1} V${e.y1 + (e.y2 - e.y1) / 2} H${e.x2} V${e.y2}\"/>`,\n+    );\n+  }\n+  for (const b of boxes) {\n+    const x = b.cx - b.width / 2;\n+    if (b.term) {\n+      parts.push(\n+        `<rect class=\"rr-term\" x=\"${x}\" y=\"${b.y}\" width=\"${b.width}\" height=\"${GRAPH_BOX_H}\" rx=\"${GRAPH_BOX_H / 2}\"/>`,\n+      );\n+    } else {\n+      parts.push(\n+        `<rect class=\"rr-nonterm\" x=\"${x}\" y=\"${b.y}\" width=\"${b.width}\" height=\"${GRAPH_BOX_H}\" rx=\"8\"/>`,\n+      );\n+    }\n+    parts.push(\n+      `<text class=\"rr-text\" x=\"${b.cx}\" y=\"${b.y + GRAPH_BOX_H / 2}\" text-anchor=\"middle\" dominant-baseline=\"central\">${escXml(b.label)}</text>`,\n+    );\n+  }\n+  for (const l of labels) {\n+    parts.push(\n+      `<text class=\"lab__graph-label\" x=\"${l.x}\" y=\"${l.y}\" text-anchor=\"middle\">${escXml(l.text)}</text>`,\n+    );\n+  }\n+  parts.push(\"</svg>\");\n+  return parts.join(\"\");\n+}\n+\n+function ForestGraph({ forest }: { forest: ForestResult }) {\n+  const svg = svgOfForest(forest);\n+  return (\n+    <div class=\"lab__cst-graph\" dangerouslySetInnerHTML={{ __html: svg }} />\n+  );\n+}\n+\n function getTrace(): LrStepInfo[] | null {\n   return response.value?.parse?.trace ?? null;\n }","raw_url":"https://github.com/wstein/gramaire/raw/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2FLabIsland.tsx","sha":"5efa14bfacbf507871796c4397ae107e9db1f451","status":"modified"},{"additions":4,"blob_url":"https://github.com/wstein/gramaire/blob/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2Fexamples.ts","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flab%2Fexamples.ts?ref=a42bf02ce3ce890f74d1de8491e5daaa20b5011a","deletions":4,"filename":"site/src/lab/examples.ts","patch":"@@ -15,11 +15,11 @@ export interface LabExample {\n   input: string;\n }\n \n-export const DEFAULT_SOURCE = `# Expr\n-\n-\\`\\`\\`gramaire\n+export const DEFAULT_SOURCE = `---\n name: Expr\n-\\`\\`\\`\n+---\n+\n+# Expr\n \n ## Tokens\n ","raw_url":"https://github.com/wstein/gramaire/raw/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2Fexamples.ts","sha":"09f402bdfd19e9acc8b125e2f751d4fc6e0269e8","status":"modified"},{"additions":34,"blob_url":"https://github.com/wstein/gramaire/blob/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2Flab.css","changes":34,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Flab%2Flab.css?ref=a42bf02ce3ce890f74d1de8491e5daaa20b5011a","deletions":0,"filename":"site/src/lab/lab.css","patch":"@@ -713,6 +713,40 @@\n .lab__railroad-svg svg {\n   max-width: 100%;\n }\n+.lab__cst-graph {\n+  margin-top: 10px;\n+  overflow-x: auto;\n+}\n+.lab__cst-graph svg {\n+  max-width: 100%;\n+}\n+/* Reuses the railroad diagrams' own rr-track/rr-term/rr-nonterm/rr-text class names (a familiar\n+   look for the same term/nonterminal distinction), but scoped here to the site's own theme-aware\n+   tokens rather than Railroad.scala's fixed hex — this SVG is generated client-side purely for\n+   inline display, never extracted as a portable standalone file the way a sidecar diagram is. */\n+.lab__cst-graph .rr-track {\n+  fill: none;\n+  stroke: var(--border-strong);\n+  stroke-width: 2;\n+}\n+.lab__cst-graph .rr-term {\n+  fill: var(--bg-1);\n+  stroke: var(--t-term);\n+  stroke-width: 2;\n+}\n+.lab__cst-graph .rr-nonterm {\n+  fill: var(--bg-2);\n+  stroke: var(--t-nonterm);\n+  stroke-width: 2;\n+}\n+.lab__cst-graph .rr-text {\n+  fill: var(--fg);\n+  font: 12px var(--font-mono);\n+}\n+.lab__cst-graph .lab__graph-label {\n+  fill: var(--fg-faint);\n+  font: 11px var(--font-mono);\n+}\n \n .lab__walk-controls {\n   display: flex;","raw_url":"https://github.com/wstein/gramaire/raw/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Fsrc%2Flab%2Flab.css","sha":"4c34ed23bd0322cc999710dbbd59ac1c1fe0b87c","status":"modified"},{"additions":27,"blob_url":"https://github.com/wstein/gramaire/blob/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Ftests%2Fvisual%2Flab.spec.ts","changes":27,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Ftests%2Fvisual%2Flab.spec.ts?ref=a42bf02ce3ce890f74d1de8491e5daaa20b5011a","deletions":0,"filename":"site/tests/visual/lab.spec.ts","patch":"@@ -268,6 +268,33 @@ test(\"the Lab's All-parses tab shows every derivation of an ambiguous grammar\",\n   );\n });\n \n+test(\"the All-parses tab's Graph view renders an actual SVG tree, not raw Mermaid source text\", async ({\n+  page,\n+}) => {\n+  await gotoLabReady(page);\n+  await page.click('button[role=\"tab\"]:has-text(\"All parses\")');\n+  await expect(page.locator(\".lab__forest-item\")).toHaveCount(1);\n+\n+  await page.click('button.lab__copy-btn:has-text(\"Graph view\")');\n+  const graph = page.locator(\".lab__cst-graph\");\n+  await expect(graph.locator(\"svg\")).toHaveCount(1);\n+  // The regression this guards: the raw `graph TD\\n n0[...` Mermaid DSL text must never appear as\n+  // literal page content once Graph view is toggled on.\n+  await expect(graph).not.toContainText(\"graph TD\");\n+  // \"1+2*3\" against calc.gram.md's left-recursive Expr/Term/Factor derives several nested\n+  // Expr/Term nodes, not one apiece — assert presence/labeling, not an exact structural count.\n+  await expect(graph.locator(\"rect.rr-nonterm\").first()).toBeVisible();\n+  await expect(graph.locator(\"rect.rr-term\").first()).toBeVisible();\n+  await expect(\n+    graph.locator(\"text.rr-text\", { hasText: \"Expr\" }).first(),\n+  ).toBeVisible();\n+\n+  // Toggling back off returns to the plain per-parse tree view.\n+  await page.click('button.lab__copy-btn:has-text(\"Graph view\")');\n+  await expect(page.locator(\".lab__cst-graph\")).toHaveCount(0);\n+  await expect(page.locator(\".lab__forest-item\")).toHaveCount(1);\n+});\n+\n test(\"the Lab's Evaluate tab runs a grammar's real {% %} actions, not a passthrough\", async ({\n   page,\n }) => {","raw_url":"https://github.com/wstein/gramaire/raw/a42bf02ce3ce890f74d1de8491e5daaa20b5011a/site%2Ftests%2Fvisual%2Flab.spec.ts","sha":"0f245c56af3a4358ea7af980b15e4eac23fd657e","status":"modified"}]}