{"files":[{"additions":6,"blob_url":"https://github.com/wstein/flatbars/blob/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Flib.rs","changes":10,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Flib.rs?ref=7aefdae12f72a741d9dae9fe09262a80c692261a","deletions":4,"filename":"trussbars/crates/trussbars-core/src/lib.rs","patch":"@@ -11,8 +11,10 @@\n //!   ([`text`]). Stringification mirrors the reference engine's `stringify`, and\n //!   escaping mirrors its `escapeHtml`, byte-for-byte (one deliberate exception:\n //!   f64 formatting, which is out of scope for v1 — see the module docs).\n-//! - [`Truthy`] / [`truthy`] — the `nonEmpty` truthiness rule, minus numbers\n-//!   ([`truthy`](mod@truthy)). A bare-number condition does not compile.\n+//! - [`TruthyIn`] / [`truthy`] / [`truthy_in`] — truthiness under a named policy\n+//!   ([`truthy`](mod@truthy)). The default is `nonEmpty` minus numbers (a bare-number\n+//!   condition does not compile); [`Liquid`] / [`Handlebars`] and host-defined policies\n+//!   are selectable per render.\n //! - [`Loop`] — the borrowed-reference loop frame model ([`frame`]): per-iteration\n //!   `{{#each}}` metadata threaded by lexical nesting, no `Rc`, no heap frame.\n //! - [`SizeHint`] — the adaptive output-capacity hint ([`capacity`]): a warm\n@@ -46,9 +48,9 @@ pub use capacity::SizeHint;\n pub use each::Each;\n pub use frame::Loop;\n pub use text::{Safe, ToText, esc, escape_html};\n-pub use truthy::{Truthy, truthy};\n+pub use truthy::{Handlebars, Liquid, NonEmpty, TruthyIn, truthy, truthy_in};\n \n-/// `#[derive(Trussbars)]` — generates [`Truthy`] for a context struct or enum (and\n+/// `#[derive(Trussbars)]` — generates [`TruthyIn`] for a context struct or enum (and\n /// [`ToText`] for a fieldless enum, writing the variant name). Available with the\n /// `derive` feature; re-exported from `trussbars-derive`.\n #[cfg(feature = \"derive\")]","raw_url":"https://github.com/wstein/flatbars/raw/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Flib.rs","sha":"407c5f3f640d7a46d2496429042893aee80b40ff","status":"modified"},{"additions":247,"blob_url":"https://github.com/wstein/flatbars/blob/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Ftruthy.rs","changes":317,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Ftruthy.rs?ref=7aefdae12f72a741d9dae9fe09262a80c692261a","deletions":70,"filename":"trussbars/crates/trussbars-core/src/truthy.rs","patch":"@@ -1,29 +1,53 @@\n-//! Truthiness: the `nonEmpty` rule, minus numbers.\n+//! Truthiness: the `nonEmpty` rule by default, selectable per render to another\n+//! template family's policy.\n //!\n-//! Trussbars has one fixed truthiness rule (see `trussbars/docs/01-subset-spec.md`\n-//! §7), applied to **non-numeric** types only. Falsy: `false`, `None`/`()`, `\"\"`,\n-//! `[]`, `{}`. Truthy: every other non-numeric value.\n+//! [`TruthyIn<Mode>`] answers whether a value is truthy *under a named policy\n+//! `Mode`*. The default `Mode` is [`NonEmpty`] — the one fixed Trussbars rule\n+//! (`trussbars/docs/01-subset-spec.md` §7): falsy is `false`, `None`/`()`, `\"\"`,\n+//! `[]`, `{}`; truthy is every other non-numeric value; and **numbers have no\n+//! [`NonEmpty`] impl**, so a bare-number condition (`{{#if count}}`) does not\n+//! compile — write the comparison (`{{#if count > 0}}`, §5.3).\n //!\n-//! **Numbers deliberately have no [`Truthy`] impl.** A bare-number condition such\n-//! as `{{#if count}}` therefore fails to compile, forcing an explicit comparison\n-//! (`{{#if count > 0}}`). This is the typed escape from the `0`-truthy (MaxBars)\n-//! / `0`-falsy (Handlebars) dilemma — rather than pick a rule, the coercion is\n-//! rejected (subset spec §5.3).\n+//! Two further built-in policies ship for hosts that want a different family's\n+//! semantics (selected with `truss!(…, truthiness = Liquid)`; out of conformance,\n+//! §11): [`Liquid`] (only `false`/`nil` are falsy — `0`, `\"\"`, `[]`, `{}` are all\n+//! truthy) and [`Handlebars`] (`false`/`null`/`0`/`NaN`/`\"\"`/`[]` falsy, `{}`\n+//! truthy). Because `Mode` is a *type parameter* of the trait, a host can define\n+//! its **own** policy over the standard library's types and its own newtypes alike\n+//! — `impl TruthyIn<MyMode> for str` is permitted even though `str` is foreign,\n+//! because the marker `MyMode` is local (the orphan rule's covered-parameter case;\n+//! `docs/15-truthiness-modes.md`). Every policy is monomorphized, so selecting one\n+//! costs nothing at runtime.\n \n use alloc::collections::BTreeMap;\n use alloc::string::String;\n use alloc::vec::Vec;\n \n-/// The `nonEmpty` truthiness predicate. Implemented for the non-numeric types a\n-/// template can place in a boolean position; **not** for numbers (see the module\n-/// docs) or context structs.\n-pub trait Truthy {\n-    /// Whether `self` is truthy under the `nonEmpty` rule.\n+/// Truthiness under a named policy `Mode` — the per-type half of the rule the\n+/// compiler emits for an `{{#if}}` / `{{#unless}}` condition and the `&&` / `||` /\n+/// `!` operators. A type that has no `TruthyIn<Mode>` impl cannot appear in a\n+/// boolean position under that policy: the condition is a compile error, not a\n+/// silent `false` (numbers under [`NonEmpty`], for example).\n+pub trait TruthyIn<Mode> {\n+    /// Whether `self` is truthy under `Mode`.\n     fn truthy(&self) -> bool;\n }\n \n-/// Evaluate the `nonEmpty` truthiness of `v` — the emission for an `{{#if}}` /\n-/// `{{#unless}}` condition and the `&&` / `||` / `!` operators.\n+/// The default Trussbars policy (`nonEmpty`, minus numbers): `false`, `None`/`()`,\n+/// `\"\"`, `[]`, `{}` are falsy; numbers have no impl (a bare-number condition does\n+/// not compile). The only policy the conformance corpus is checked against.\n+pub struct NonEmpty;\n+\n+/// The Liquid policy: only `false` and `nil` (`None`/`()`) are falsy. `0`, `\"\"`,\n+/// `[]`, and `{}` are all truthy. Out of conformance (`docs/15-truthiness-modes.md`).\n+pub struct Liquid;\n+\n+/// The Handlebars policy: `false`, `null` (`None`/`()`), `0`, `NaN`, `\"\"`, and `[]`\n+/// are falsy; a non-empty value and an (empty or non-empty) object `{}` are truthy.\n+/// Out of conformance (`docs/15-truthiness-modes.md`).\n+pub struct Handlebars;\n+\n+/// Evaluate truthiness under the default [`NonEmpty`] policy.\n ///\n /// ```\n /// use trussbars_core::truthy;\n@@ -32,123 +56,218 @@ pub trait Truthy {\n /// assert!(!truthy(&Option::<&str>::None));\n /// ```\n ///\n-/// Numbers have no [`Truthy`] impl, so a bare-number condition does not compile —\n+/// Numbers have no [`NonEmpty`] impl, so a bare-number condition does not compile —\n /// write the comparison instead:\n ///\n /// ```compile_fail\n /// use trussbars_core::truthy;\n-/// let _ = truthy(&5_i64); // error[E0277]: `i64: Truthy` is not satisfied\n+/// let _ = truthy(&5_i64); // error[E0277]: `i64: TruthyIn<NonEmpty>` is not satisfied\n+/// ```\n+pub fn truthy<T: TruthyIn<NonEmpty> + ?Sized>(v: &T) -> bool {\n+    <T as TruthyIn<NonEmpty>>::truthy(v)\n+}\n+\n+/// Evaluate truthiness under an explicit policy `Mode` — the emission for a template\n+/// compiled with a non-default `truss!(…, truthiness = Mode)`.\n+///\n /// ```\n-pub fn truthy<T: Truthy + ?Sized>(v: &T) -> bool {\n-    v.truthy()\n+/// use trussbars_core::{truthy_in, Liquid, Handlebars};\n+/// assert!(truthy_in::<Liquid, _>(&\"\"));   // \"\" is truthy under Liquid\n+/// assert!(truthy_in::<Liquid, _>(&0_i64)); // 0 is truthy under Liquid\n+/// assert!(!truthy_in::<Handlebars, _>(&0_i64)); // 0 is falsy under Handlebars\n+/// assert!(!truthy_in::<Handlebars, _>(&\"\"));    // \"\" is falsy under Handlebars\n+/// ```\n+pub fn truthy_in<Mode, T: TruthyIn<Mode> + ?Sized>(v: &T) -> bool {\n+    <T as TruthyIn<Mode>>::truthy(v)\n }\n \n-impl Truthy for bool {\n+// ── mode-invariant types ──────────────────────────────────────────────────────\n+// `bool`, `null`, `Option`, and references answer the same under every policy, so\n+// they impl `TruthyIn<Mode>` for *all* `Mode` (one impl, no per-policy repetition).\n+\n+impl<Mode> TruthyIn<Mode> for bool {\n     fn truthy(&self) -> bool {\n         *self\n     }\n }\n \n-impl Truthy for str {\n+/// The `null` literal — falsy under every policy.\n+impl<Mode> TruthyIn<Mode> for () {\n     fn truthy(&self) -> bool {\n-        !self.is_empty()\n+        false\n     }\n }\n \n-impl Truthy for String {\n+impl<Mode, T: TruthyIn<Mode>> TruthyIn<Mode> for Option<T> {\n+    // `None` is falsy; `Some(v)` defers to `v` under the same policy — so under\n+    // `NonEmpty` `Some(\"\")` is falsy (absence and emptiness both fall through),\n+    // while under `Liquid` `Some(\"\")` is truthy.\n     fn truthy(&self) -> bool {\n-        !self.is_empty()\n+        self.as_ref().is_some_and(<T as TruthyIn<Mode>>::truthy)\n     }\n }\n \n-impl Truthy for crate::Safe {\n+impl<Mode, T: TruthyIn<Mode> + ?Sized> TruthyIn<Mode> for &T {\n     fn truthy(&self) -> bool {\n-        !self.0.is_empty()\n+        <T as TruthyIn<Mode>>::truthy(*self)\n     }\n }\n \n-/// The `null` literal — falsy.\n-impl Truthy for () {\n-    fn truthy(&self) -> bool {\n-        false\n-    }\n+// ── NonEmpty / Handlebars: emptiness types ────────────────────────────────────\n+// Both policies treat a string/sequence as truthy exactly when non-empty; they\n+// diverge only on maps (an empty `{}` is falsy under `NonEmpty`, truthy under\n+// Handlebars) and numbers (no impl vs. `!= 0`), handled below.\n+\n+macro_rules! impl_emptiness {\n+    ($mode:ty) => {\n+        impl TruthyIn<$mode> for str {\n+            fn truthy(&self) -> bool {\n+                !self.is_empty()\n+            }\n+        }\n+        impl TruthyIn<$mode> for String {\n+            fn truthy(&self) -> bool {\n+                !self.is_empty()\n+            }\n+        }\n+        impl TruthyIn<$mode> for crate::Safe {\n+            fn truthy(&self) -> bool {\n+                !self.0.is_empty()\n+            }\n+        }\n+        impl<T> TruthyIn<$mode> for [T] {\n+            fn truthy(&self) -> bool {\n+                !self.is_empty()\n+            }\n+        }\n+        impl<T> TruthyIn<$mode> for Vec<T> {\n+            fn truthy(&self) -> bool {\n+                !self.is_empty()\n+            }\n+        }\n+    };\n }\n+impl_emptiness!(NonEmpty);\n+impl_emptiness!(Handlebars);\n \n-impl<T> Truthy for [T] {\n+/// `NonEmpty`: an empty map is falsy (the `{}`-falsy half of `nonEmpty`).\n+impl<K, V> TruthyIn<NonEmpty> for BTreeMap<K, V> {\n     fn truthy(&self) -> bool {\n         !self.is_empty()\n     }\n }\n \n-impl<T> Truthy for Vec<T> {\n+/// Handlebars: an object is always truthy — Handlebars special-cases only empty\n+/// *arrays*, never objects.\n+impl<K, V> TruthyIn<Handlebars> for BTreeMap<K, V> {\n     fn truthy(&self) -> bool {\n-        !self.is_empty()\n+        true\n     }\n }\n \n-impl<K, V> Truthy for BTreeMap<K, V> {\n+// ── Handlebars: numbers ───────────────────────────────────────────────────────\n+// `0` is falsy; `NaN` is falsy (the JS falsy set); every other number is truthy.\n+\n+macro_rules! impl_handlebars_int {\n+    ($($t:ty),+ $(,)?) => {\n+        $(\n+            impl TruthyIn<Handlebars> for $t {\n+                fn truthy(&self) -> bool { *self != 0 }\n+            }\n+        )+\n+    };\n+}\n+impl_handlebars_int!(\n+    i8, i16, i32, i64, i128, isize, u8, u16, u32, u64, u128, usize\n+);\n+\n+impl TruthyIn<Handlebars> for f32 {\n     fn truthy(&self) -> bool {\n-        !self.is_empty()\n+        !self.is_nan() && *self != 0.0\n     }\n }\n-\n-impl<T: Truthy> Truthy for Option<T> {\n-    // `None` is falsy; `Some(v)` defers to `v` — so `Some(\"\")` is falsy too,\n-    // matching the interpreter (absence and emptiness both fall through).\n+impl TruthyIn<Handlebars> for f64 {\n     fn truthy(&self) -> bool {\n-        self.as_ref().is_some_and(Truthy::truthy)\n+        !self.is_nan() && *self != 0.0\n     }\n }\n \n-impl<T: Truthy + ?Sized> Truthy for &T {\n+// ── Liquid: everything but `false`/`nil` is truthy ────────────────────────────\n+// Strings, sequences, maps, and numbers — including `\"\"`, `[]`, `{}`, and `0` — are\n+// all truthy.\n+\n+macro_rules! impl_liquid_true {\n+    ($($t:ty),+ $(,)?) => {\n+        $(\n+            impl TruthyIn<Liquid> for $t {\n+                fn truthy(&self) -> bool { true }\n+            }\n+        )+\n+    };\n+}\n+impl_liquid_true!(\n+    str,\n+    String,\n+    crate::Safe,\n+    i8,\n+    i16,\n+    i32,\n+    i64,\n+    i128,\n+    isize,\n+    u8,\n+    u16,\n+    u32,\n+    u64,\n+    u128,\n+    usize,\n+    f32,\n+    f64\n+);\n+\n+impl<T> TruthyIn<Liquid> for [T] {\n+    fn truthy(&self) -> bool {\n+        true\n+    }\n+}\n+impl<T> TruthyIn<Liquid> for Vec<T> {\n     fn truthy(&self) -> bool {\n-        (**self).truthy()\n+        true\n+    }\n+}\n+impl<K, V> TruthyIn<Liquid> for BTreeMap<K, V> {\n+    fn truthy(&self) -> bool {\n+        true\n     }\n }\n \n #[cfg(test)]\n mod tests {\n-    use super::*;\n+    use super::{Handlebars, Liquid, TruthyIn, truthy, truthy_in};\n     use crate::Safe;\n \n     #[test]\n-    fn booleans_are_themselves() {\n+    fn nonempty_booleans_and_null() {\n         assert!(truthy(&true));\n         assert!(!truthy(&false));\n+        assert!(!truthy(&()));\n+        assert!(!truthy(&Option::<&str>::None));\n     }\n \n     #[test]\n-    fn strings_are_truthy_when_non_empty() {\n+    fn nonempty_strings_and_collections() {\n         assert!(truthy(&\"x\"));\n         assert!(!truthy(&\"\"));\n         assert!(truthy(&String::from(\"x\")));\n         assert!(!truthy(&String::new()));\n-    }\n-\n-    #[test]\n-    fn safe_tests_its_content() {\n         assert!(truthy(&Safe(String::from(\"x\"))));\n         assert!(!truthy(&Safe(String::new())));\n-    }\n-\n-    #[test]\n-    fn collections_are_truthy_when_non_empty() {\n         assert!(truthy(&vec![1]));\n         assert!(!truthy(&Vec::<i32>::new()));\n-        let mut m = BTreeMap::new();\n-        assert!(!truthy(&m));\n-        m.insert(\"k\", 1);\n-        assert!(truthy(&m));\n-    }\n-\n-    #[test]\n-    fn null_and_none_are_falsy() {\n-        assert!(!truthy(&()));\n-        assert!(!truthy(&Option::<&str>::None));\n     }\n \n     #[test]\n-    fn option_defers_to_its_inner_value() {\n+    fn nonempty_option_defers_to_inner() {\n         assert!(truthy(&Some(\"x\")));\n         // The key nonEmpty-through-Option case: Some(\"\") is falsy.\n         assert!(!truthy(&Some(\"\")));\n@@ -157,9 +276,67 @@ mod tests {\n     }\n \n     #[test]\n-    fn references_delegate() {\n-        let s = String::from(\"x\");\n-        let r = &s;\n-        assert!(truthy(r));\n+    fn liquid_only_false_and_nil_are_falsy() {\n+        // Everything present and non-false is truthy — including the empties.\n+        assert!(truthy_in::<Liquid, _>(&\"\"));\n+        assert!(truthy_in::<Liquid, _>(&String::new()));\n+        assert!(truthy_in::<Liquid, _>(&Vec::<i32>::new()));\n+        assert!(truthy_in::<Liquid, _>(&0_i64));\n+        assert!(truthy_in::<Liquid, _>(&0.0_f64));\n+        assert!(truthy_in::<Liquid, _>(&Safe(String::new())));\n+        // …but `false` and `nil` stay falsy.\n+        assert!(!truthy_in::<Liquid, _>(&false));\n+        assert!(!truthy_in::<Liquid, _>(&()));\n+        assert!(!truthy_in::<Liquid, _>(&Option::<&str>::None));\n+        // Option defers under the same policy: Some(\"\") is truthy under Liquid.\n+        assert!(truthy_in::<Liquid, _>(&Some(\"\")));\n+        assert!(!truthy_in::<Liquid, _>(&Some(false)));\n+    }\n+\n+    #[test]\n+    fn handlebars_zero_nan_and_empties_are_falsy() {\n+        assert!(!truthy_in::<Handlebars, _>(&0_i64));\n+        assert!(truthy_in::<Handlebars, _>(&5_i64));\n+        assert!(!truthy_in::<Handlebars, _>(&0.0_f64));\n+        assert!(truthy_in::<Handlebars, _>(&5.0_f64));\n+        assert!(!truthy_in::<Handlebars, _>(&f64::NAN));\n+        assert!(truthy_in::<Handlebars, _>(&f64::INFINITY));\n+        assert!(!truthy_in::<Handlebars, _>(&\"\"));\n+        assert!(!truthy_in::<Handlebars, _>(&Vec::<i32>::new()));\n+    }\n+\n+    #[test]\n+    fn handlebars_empty_object_is_truthy() {\n+        use alloc::collections::BTreeMap;\n+        // Handlebars special-cases empty arrays, not empty objects.\n+        let m: BTreeMap<&str, i32> = BTreeMap::new();\n+        assert!(truthy_in::<Handlebars, _>(&m));\n+    }\n+\n+    // The extension point: a host defines its *own* policy for a foreign type. The\n+    // marker is local, so `impl TruthyIn<NonZero> for i64` is orphan-rule-legal even\n+    // though both `TruthyIn` and `i64` are foreign here — the mechanism Option A buys\n+    // (docs/15). A bare `i64` has no `NonEmpty` impl, yet gains one under `NonZero`.\n+    struct NonZero;\n+    impl TruthyIn<NonZero> for i64 {\n+        fn truthy(&self) -> bool {\n+            *self != 0\n+        }\n+    }\n+    impl TruthyIn<NonZero> for str {\n+        fn truthy(&self) -> bool {\n+            self != \"skip\"\n+        }\n+    }\n+\n+    #[test]\n+    fn host_defined_policy_over_foreign_types() {\n+        assert!(truthy_in::<NonZero, _>(&7_i64));\n+        assert!(!truthy_in::<NonZero, _>(&0_i64));\n+        assert!(truthy_in::<NonZero, _>(&\"go\"));\n+        assert!(!truthy_in::<NonZero, _>(&\"skip\"));\n+        // The mode-invariant impls compose with the host policy for free.\n+        assert!(truthy_in::<NonZero, _>(&Some(7_i64)));\n+        assert!(!truthy_in::<NonZero, _>(&Option::<i64>::None));\n     }\n }","raw_url":"https://github.com/wstein/flatbars/raw/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-core%2Fsrc%2Ftruthy.rs","sha":"b77b330a7565cddbeefe5e56d9f86a81de078032","status":"modified"},{"additions":31,"blob_url":"https://github.com/wstein/flatbars/blob/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-derive%2Fsrc%2Flib.rs","changes":50,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-derive%2Fsrc%2Flib.rs?ref=7aefdae12f72a741d9dae9fe09262a80c692261a","deletions":19,"filename":"trussbars/crates/trussbars-derive/src/lib.rs","patch":"@@ -1,34 +1,46 @@\n //! `#[derive(Trussbars)]` — the companion derive for Trussbars context types.\n //!\n-//! **Struct.** Generates [`trussbars_core::Truthy`]: under the `nonEmpty` rule an\n-//! inhabited object is truthy, so a struct with ≥1 field derives `true` and a\n-//! field-less struct derives `false` (matching `Object.keys(v).length > 0`). It\n-//! deliberately does **not** generate [`trussbars_core::ToText`] — `{{struct}}`\n-//! stays a compile error, the typed counterpart of the interpreter's runtime\n-//! \"cannot stringify an object\" (subset spec §2, runtime API §3/§12).\n+//! A context type's truthiness is **policy-independent** — an inhabited object is\n+//! truthy under every rule — so the derive generates a blanket\n+//! [`trussbars_core::TruthyIn<__TruthMode>`] impl that holds for `NonEmpty`,\n+//! `Liquid`, `Handlebars`, and any host-defined policy alike.\n //!\n-//! **Enum.** Every value is an inhabited variant, so `Truthy` is always `true`\n-//! (matching a serde-tagged object being non-empty). A **fieldless** enum (all unit\n-//! variants) additionally derives `ToText` writing the variant *name* — matching\n-//! serde's unit-variant serialization (`Status::Active` → `\"Active\"`), so\n-//! `{{status}}` renders the name and `{{#if (eq status \"Active\")}}` works. An enum\n-//! with **data-carrying** variants gets `Truthy` only; field-access dispatch over\n-//! its variants is the §4.1 polymorphic-dispatch work (a `match`, type-aware → v2).\n+//! **Struct.** Under the `nonEmpty` rule an inhabited object is truthy, so a struct\n+//! with ≥1 field derives `true` and a field-less struct derives `false` (matching\n+//! `Object.keys(v).length > 0`). It deliberately does **not** generate\n+//! [`trussbars_core::ToText`] — `{{struct}}` stays a compile error, the typed\n+//! counterpart of the interpreter's runtime \"cannot stringify an object\" (subset\n+//! spec §2, runtime API §3/§12).\n+//!\n+//! **Enum.** Every value is an inhabited variant, so it is always truthy (matching a\n+//! serde-tagged object being non-empty). A **fieldless** enum (all unit variants)\n+//! additionally derives `ToText` writing the variant *name* — matching serde's\n+//! unit-variant serialization (`Status::Active` → `\"Active\"`), so `{{status}}`\n+//! renders the name and `{{#if (eq status \"Active\")}}` works. An enum with\n+//! **data-carrying** variants gets truthiness only; field-access dispatch over its\n+//! variants is the §4.1 polymorphic-dispatch work (a `match`, type-aware → v2).\n //!\n //! Deriving on a union is a compile error.\n \n use proc_macro::TokenStream;\n use quote::quote;\n-use syn::{Data, DeriveInput, Fields, parse_macro_input};\n+use syn::{Data, DeriveInput, Fields, parse_macro_input, parse_quote};\n \n-/// Derive Trussbars context support (`Truthy`, and `ToText` for a unit enum) — see\n-/// the crate docs.\n+/// Derive Trussbars context support (a policy-independent `TruthyIn`, and `ToText`\n+/// for a unit enum) — see the crate docs.\n #[proc_macro_derive(Trussbars)]\n pub fn derive_trussbars(input: TokenStream) -> TokenStream {\n     let input = parse_macro_input!(input as DeriveInput);\n     let name = &input.ident;\n     let (impl_generics, ty_generics, where_clause) = input.generics.split_for_impl();\n \n+    // A context type is truthy under *every* policy, so the `TruthyIn` impl is generic\n+    // over the mode marker — `impl<__TruthMode, …> TruthyIn<__TruthMode> for T`. The\n+    // extra parameter goes only on the impl, never on the Self type.\n+    let mut mode_generics = input.generics.clone();\n+    mode_generics.params.insert(0, parse_quote!(__TruthMode));\n+    let (mode_impl_generics, _, _) = mode_generics.split_for_impl();\n+\n     match &input.data {\n         Data::Struct(data) => {\n             // nonEmpty: a struct with ≥1 field is truthy; a unit struct is falsy.\n@@ -38,7 +50,7 @@ pub fn derive_trussbars(input: TokenStream) -> TokenStream {\n                 Fields::Unit => false,\n             };\n             quote! {\n-                impl #impl_generics ::trussbars_core::Truthy for #name #ty_generics #where_clause {\n+                impl #mode_impl_generics ::trussbars_core::TruthyIn<__TruthMode> for #name #ty_generics #where_clause {\n                     fn truthy(&self) -> bool { #truthy }\n                 }\n             }\n@@ -47,7 +59,7 @@ pub fn derive_trussbars(input: TokenStream) -> TokenStream {\n         Data::Enum(data) => {\n             // An enum value is always an inhabited variant → truthy.\n             let truthy_impl = quote! {\n-                impl #impl_generics ::trussbars_core::Truthy for #name #ty_generics #where_clause {\n+                impl #mode_impl_generics ::trussbars_core::TruthyIn<__TruthMode> for #name #ty_generics #where_clause {\n                     fn truthy(&self) -> bool { true }\n                 }\n             };\n@@ -73,7 +85,7 @@ pub fn derive_trussbars(input: TokenStream) -> TokenStream {\n                 }\n                 .into()\n             } else {\n-                // Data-carrying variants: Truthy only (dispatch deferred, §4.1).\n+                // Data-carrying variants: truthiness only (dispatch deferred, §4.1).\n                 truthy_impl.into()\n             }\n         }","raw_url":"https://github.com/wstein/flatbars/raw/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-derive%2Fsrc%2Flib.rs","sha":"1929df36bb69bf51253e3e709f4affc2e111fdb7","status":"modified"},{"additions":19,"blob_url":"https://github.com/wstein/flatbars/blob/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-derive%2Ftests%2Fderive.rs","changes":20,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fcrates%2Ftrussbars-derive%2Ftests%2Fderive.rs?ref=7aefdae12f72a741d9dae9fe09262a80c692261a","deletions":1,"filename":"trussbars/crates/trussbars-derive/tests/derive.rs","patch":"@@ -6,7 +6,7 @@\n //! dead-code is allowed for the whole fixture module.\n #![allow(dead_code)]\n \n-use trussbars_core::truthy;\n+use trussbars_core::{Handlebars, Liquid, truthy, truthy_in};\n use trussbars_derive::Trussbars;\n \n #[derive(Trussbars)]\n@@ -83,3 +83,21 @@ fn data_enum_is_truthy() {\n     assert!(truthy(&Shape::Circle { radius: 1.0 }));\n     assert!(truthy(&Shape::Square(2.0)));\n }\n+\n+#[test]\n+fn derived_truthiness_is_policy_independent() {\n+    // The generated `TruthyIn<__TruthMode>` impl holds under every policy: an\n+    // inhabited object is truthy whether the template renders under NonEmpty,\n+    // Liquid, or Handlebars, and a unit struct is falsy under all three.\n+    let v = WithFields {\n+        a: 0,\n+        b: String::new(),\n+    };\n+    assert!(truthy_in::<Liquid, _>(&v));\n+    assert!(truthy_in::<Handlebars, _>(&v));\n+    assert!(!truthy_in::<Liquid, _>(&Unit));\n+    assert!(!truthy_in::<Handlebars, _>(&Unit));\n+    // Generic context types keep the extra mode parameter off the Self type.\n+    assert!(truthy_in::<Liquid, _>(&Generic { x: 0_i64 }));\n+    assert!(truthy_in::<Handlebars, _>(&Status::Active));\n+}","raw_url":"https://github.com/wstein/flatbars/raw/7aefdae12f72a741d9dae9fe09262a80c692261a/trussbars%2Fcrates%2Ftrussbars-derive%2Ftests%2Fderive.rs","sha":"0f76952efdad9ed658a9917b2855c020e5724bcb","status":"modified"}]}