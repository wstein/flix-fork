{"files":[{"additions":111,"blob_url":"https://github.com/wstein/flatbars/blob/2985300e4dcbabe8faa97d249575fa3a115aa18a/lab%2Findex.html","changes":222,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/lab%2Findex.html?ref=2985300e4dcbabe8faa97d249575fa3a115aa18a","deletions":111,"filename":"lab/index.html","patch":"@@ -2786,7 +2786,7 @@\n         const res = parseConfigYaml(text);\n         if (!res.ok) {\n           configError = res.error;\n-          dockProblems = [{ severity: \"error\", file: \"config.yaml\", line: 1, col: 1, message: res.error }];\n+          ctx.caches.dockProblems = [{ severity: \"error\", file: \"config.yaml\", line: 1, col: 1, message: res.error }];\n           setError(\"config error: \" + res.error);\n           renderDock(); renderTabs();\n           return;\n@@ -2993,7 +2993,7 @@\n       function refreshCoverageGutter() {\n         if (!templateView) return;\n         const file = activeTab()?.name === undefined ? \"main\" : (activeTabIdx === 0 ? \"main\" : activeTab().name);\n-        const rows = lastCoverage.filter((r) => r.file === file);\n+        const rows = ctx.caches.lastCoverage.filter((r) => r.file === file);\n         const source = templateView.state.doc.toString();\n         templateView.dispatch({ effects: setCoverageEffect.of(buildCoverageDeco(source, rows)) });\n       }\n@@ -3016,8 +3016,8 @@\n           lastSegViews: [],  // [{from,to,file,kind,start,end}] — segment char ranges in the output editor\n         },\n       };\n-      let lastUsedTransformers = []; // ADR-0013 report: transformer names called by the compiled program\n-      let dockProblems = []; // diagnostics for the Problems tab, refreshed each run()\n+      ctx.caches.lastUsedTransformers = []; // ADR-0013 report: transformer names called by the compiled program\n+      ctx.caches.dockProblems = []; // diagnostics for the Problems tab, refreshed each run()\n       let hadProblems = false; // tracks the 0->>0 transition so the dock auto-opens once\n       // Dock tab id; the persisted activeTab (see panelState below) is folded\n       // in inside restorePanels(), which runs after panelState exists.\n@@ -3030,19 +3030,19 @@\n       // Any built-in missing from TRANSFORMER_RISK falls back to \"host\" tier at\n       // render time.\n \n-      let lastTimings = null;       // #6: { parseMs, compileMs, renderMs, totalMs, outBytes }\n-      let lastAst = null;           // shared cache: { main: nodes, partials: { name: nodes } }\n-      let lastDataAccess = [];      // #1: [{ path, status, file, start, end, value? }]\n-      let lastWhitespace = [];      // #2: [{ file, start, end, kind, line, col }]\n-      let lastDeps = null;          // #3: dependency graph\n-      let lastCalls = [];           // #4: [{ name, file, start, end }]\n-      let lastEscapeRuns = null;    // #5: { html, plain, raw, total }\n-      let lastCoverage = [];        // #7: [{ kind, file, start, end, line, col, executed, count }]\n-      let lastAnalyse = { ok: true, findings: [] }; // ADR-022: truthiness portability (Truthiness panel)\n-      let lastAnalyseSuppressed = 0; // ADR-030: count of potential/miss findings the pathSchema removed\n-      let lastLint = { ok: true, findings: [] }; // ADR-019: canonicalization lint findings (Lint panel)\n-      let lastRequiredAssigns = []; // engine reflection: assign names the program reads\n-      let lastMissingAssigns = [];  // intersection: requiredAssigns NOT in current data\n+      ctx.caches.lastTimings = null;       // #6: { parseMs, compileMs, renderMs, totalMs, outBytes }\n+      ctx.caches.lastAst = null;           // shared cache: { main: nodes, partials: { name: nodes } }\n+      ctx.caches.lastDataAccess = [];      // #1: [{ path, status, file, start, end, value? }]\n+      ctx.caches.lastWhitespace = [];      // #2: [{ file, start, end, kind, line, col }]\n+      ctx.caches.lastDeps = null;          // #3: dependency graph\n+      ctx.caches.lastCalls = [];           // #4: [{ name, file, start, end }]\n+      ctx.caches.lastEscapeRuns = null;    // #5: { html, plain, raw, total }\n+      ctx.caches.lastCoverage = [];        // #7: [{ kind, file, start, end, line, col, executed, count }]\n+      ctx.caches.lastAnalyse = { ok: true, findings: [] }; // ADR-022: truthiness portability (Truthiness panel)\n+      ctx.caches.lastAnalyseSuppressed = 0; // ADR-030: count of potential/miss findings the pathSchema removed\n+      ctx.caches.lastLint = { ok: true, findings: [] }; // ADR-019: canonicalization lint findings (Lint panel)\n+      ctx.caches.lastRequiredAssigns = []; // engine reflection: assign names the program reads\n+      ctx.caches.lastMissingAssigns = [];  // intersection: requiredAssigns NOT in current data\n \n       // outputHasContent (./app/render-helpers.mjs) destructures { lastData,\n       // lastProgram, lastOutput } from a cache snapshot — ctx.caches has exactly\n@@ -3302,13 +3302,13 @@\n         renderPending = false;\n         setRecomputing(false); // I-1 · debounce window ended; clear before sync work\n         ctx.caches.lastData = null; // reset; reassigned once data is parsed + transformed below\n-        dockProblems = []; // success leaves this empty; error paths populate it below\n+        ctx.caches.dockProblems = []; // success leaves this empty; error paths populate it below\n         // Reset per-run analyses so a stale value never leaks into the dock.\n-        lastTimings = null; lastAst = null; lastDataAccess = []; lastWhitespace = [];\n-        lastDeps = null; lastCalls = []; lastEscapeRuns = null; lastCoverage = [];\n-        lastAnalyse = { ok: true, findings: [] };\n-        lastLint = { ok: true, findings: [] };\n-        lastRequiredAssigns = []; lastMissingAssigns = [];\n+        ctx.caches.lastTimings = null; ctx.caches.lastAst = null; ctx.caches.lastDataAccess = []; ctx.caches.lastWhitespace = [];\n+        ctx.caches.lastDeps = null; ctx.caches.lastCalls = []; ctx.caches.lastEscapeRuns = null; ctx.caches.lastCoverage = [];\n+        ctx.caches.lastAnalyse = { ok: true, findings: [] };\n+        ctx.caches.lastLint = { ok: true, findings: [] };\n+        ctx.caches.lastRequiredAssigns = []; ctx.caches.lastMissingAssigns = [];\n         const dataSource = yamlSrc; // the YAML tab's source (editor may show the transform)\n         const mainSource = tabs[0].source;\n         const tCompileStart = performance.now();\n@@ -3319,7 +3319,7 @@\n \n         if (compiled.errors) {\n           ctx.caches.lastProgram = null;\n-          lastUsedTransformers = [];\n+          ctx.caches.lastUsedTransformers = [];\n           // The compiler reports every recoverable parse error in one pass, in\n           // source order. Each names the file (\"main\" or a partial) it occurred\n           // in; map its byte span into that file's source for line/column, and\n@@ -3344,14 +3344,14 @@\n           const more = problems.length > 1 ? ` (+${problems.length - 1} more)` : \"\";\n           const first = problems[0];\n           setError(`compile error: ${first.file} · ${first.message} (Ln ${first.line}, Col ${first.col})${more}`);\n-          dockProblems = problems;\n+          ctx.caches.dockProblems = problems;\n           renderDock();\n           setOutput(\"\"); saveHash(); return;\n         }\n \n         ctx.caches.lastProgram = compiled.program;\n-        lastUsedTransformers = renderer.usedTransformers(compiled.program);\n-        lastRequiredAssigns = renderer.requiredAssigns(compiled.program);\n+        ctx.caches.lastUsedTransformers = renderer.usedTransformers(compiled.program);\n+        ctx.caches.lastRequiredAssigns = renderer.requiredAssigns(compiled.program);\n         clearDiagnostics();\n \n         // Static analyses — every panel that doesn't need rendered output runs\n@@ -3365,13 +3365,13 @@\n           const a = renderer.parseAst(src);\n           astsByFile[name] = a.ast ? a.ast.nodes : [];\n         }\n-        lastAst = astsByFile;\n-        lastCalls = analyseCalls(astsByFile);\n-        lastEscapeRuns = analyseEscapeRuns(astsByFile);\n-        lastDeps = buildDependencyGraph(astsByFile);\n-        lastWhitespace = [];\n+        ctx.caches.lastAst = astsByFile;\n+        ctx.caches.lastCalls = analyseCalls(astsByFile);\n+        ctx.caches.lastEscapeRuns = analyseEscapeRuns(astsByFile);\n+        ctx.caches.lastDeps = buildDependencyGraph(astsByFile);\n+        ctx.caches.lastWhitespace = [];\n         for (const [file, src] of [[\"main\", mainSource]].concat(Object.entries(partialsMap()))) {\n-          for (const w of analyseWhitespace(src, file)) lastWhitespace.push(w);\n+          for (const w of analyseWhitespace(src, file)) ctx.caches.lastWhitespace.push(w);\n         }\n         const tParseEnd = performance.now();\n \n@@ -3381,7 +3381,7 @@\n           if (typeof data === \"undefined\") data = null;\n         } catch (err) {\n           setError(\"invalid YAML: \" + err.message);\n-          dockProblems = [{ severity: \"error\", file: \"data.yaml\", line: (err.mark?.line ?? 0) + 1, col: (err.mark?.column ?? 0) + 1, message: \"YAML · \" + err.message }];\n+          ctx.caches.dockProblems = [{ severity: \"error\", file: \"data.yaml\", line: (err.mark?.line ?? 0) + 1, col: (err.mark?.column ?? 0) + 1, message: \"YAML · \" + err.message }];\n           setOutput(\"\"); saveHash(); return;\n         }\n \n@@ -3398,7 +3398,7 @@\n             parsedOverlays.push({ name: ov.name, value });\n           } catch (err) {\n             setError(\"invalid YAML: \" + ov.name + \" · \" + err.message);\n-            dockProblems = [{\n+            ctx.caches.dockProblems = [{\n               severity: \"error\", file: ov.name,\n               line: (err.mark?.line ?? 0) + 1, col: (err.mark?.column ?? 0) + 1,\n               message: \"YAML · \" + err.message,\n@@ -3409,7 +3409,7 @@\n         const overlayResult = mergeDataOverlays(data, parsedOverlays);\n         data = overlayResult.merged;\n         for (const c of overlayResult.clashes) {\n-          dockProblems.push({\n+          ctx.caches.dockProblems.push({\n             severity: \"missing\", // re-use the warn-colour Problems styling\n             file: \"data.yaml\", line: 1, col: 1,\n             message: `overlay clash at \\`${c.path || \"(root)\"}\\` — ${c.was} replaced by ${c.now}`,\n@@ -3425,7 +3425,7 @@\n             ctx.caches.lastData = null;\n             const message = \"transform · \" + (err.message || err.code || \"invalid JSONata\");\n             setError(\"transform error: \" + (err.message || err.code || \"invalid JSONata\"));\n-            dockProblems = [{ severity: \"error\", file: \"transform\", line: 1, col: 1, message }];\n+            ctx.caches.dockProblems = [{ severity: \"error\", file: \"transform\", line: 1, col: 1, message }];\n             setOutput(\"\"); saveHash(); return;\n           }\n         }\n@@ -3442,50 +3442,50 @@\n \n         // Data-aware analyses, now that the view-model is finalized. Coverage\n         // uses the engine's default truthiness for dead-branch classification.\n-        lastDataAccess = analyseDataAccess(astsByFile, data);\n-        lastCoverage = analyseCoverage(astsByFile, data);\n+        ctx.caches.lastDataAccess = analyseDataAccess(astsByFile, data);\n+        ctx.caches.lastCoverage = analyseCoverage(astsByFile, data);\n         // ADR-022: trace the render and collect the truthiness portability\n         // findings for the Truthiness dock panel (FlatBars engine only). ADR-030:\n         // when config.yaml declares safe paths, route through analyzeWith so the\n         // panel suppresses those paths' potential + miss findings.\n-        lastAnalyseSuppressed = 0;\n+        ctx.caches.lastAnalyseSuppressed = 0;\n         if (typeof renderer.analyze !== \"function\") {\n-          lastAnalyse = { ok: true, findings: [] };\n+          ctx.caches.lastAnalyse = { ok: true, findings: [] };\n         } else if (labPathSchema.length && typeof renderer.analyzeWith === \"function\") {\n           // ADR-030: the schema'd pass is what the panel shows; an unschema'd pass\n           // counts how many potential/miss findings the schema removed (advisory\n           // findings only — observed findings are never suppressed).\n-          lastAnalyse = renderer.analyzeWith((p) => !labPathSchema.includes(p), { source: mainSource }, data);\n+          ctx.caches.lastAnalyse = renderer.analyzeWith((p) => !labPathSchema.includes(p), { source: mainSource }, data);\n           const unsch = renderer.analyze({ source: mainSource }, data);\n           const advisory = (r) => (r.findings || []).filter((f) => f.kind === \"potential\" || f.kind === \"miss\").length;\n-          if (lastAnalyse.ok && unsch.ok) lastAnalyseSuppressed = Math.max(0, advisory(unsch) - advisory(lastAnalyse));\n+          if (ctx.caches.lastAnalyse.ok && unsch.ok) ctx.caches.lastAnalyseSuppressed = Math.max(0, advisory(unsch) - advisory(ctx.caches.lastAnalyse));\n         } else {\n-          lastAnalyse = renderer.analyze({ source: mainSource }, data);\n+          ctx.caches.lastAnalyse = renderer.analyze({ source: mainSource }, data);\n         }\n         // ADR-019: deprecated-alias + non-canonical scoped-variable findings for\n         // the active dialect (the Lint dock panel; FlatBars engines only).\n-        lastLint = typeof renderer.lint === \"function\"\n+        ctx.caches.lastLint = typeof renderer.lint === \"function\"\n           ? renderer.lint(mainSource)\n           : { ok: true, findings: [] };\n         refreshCoverageGutter();\n         // Engine-reported assigns that aren't present at the data root — a\n         // tight, false-positive-free \"missing assigns\" signal (Data Access).\n         const dataKeys = (data && typeof data === \"object\" && !Array.isArray(data))\n           ? new Set(Object.keys(data)) : new Set();\n-        lastMissingAssigns = lastRequiredAssigns.filter((n) => !dataKeys.has(n));\n+        ctx.caches.lastMissingAssigns = ctx.caches.lastRequiredAssigns.filter((n) => !dataKeys.has(n));\n         // Promote each missing assign to a Problems row at warn severity, so\n         // the dock's auto-open behaviour fires for a missing key just like it\n         // does for compile errors. The first source span that references the\n         // assign is the jump target; locating it by re-using the data-access\n         // walk (which already classifies bare identifiers).\n-        for (const name of lastMissingAssigns) {\n-          const firstHit = lastDataAccess.find((r) => r.path === name || r.path.startsWith(name + \".\"));\n+        for (const name of ctx.caches.lastMissingAssigns) {\n+          const firstHit = ctx.caches.lastDataAccess.find((r) => r.path === name || r.path.startsWith(name + \".\"));\n           const file = firstHit?.file || \"main\";\n           const src = fileSource(file);\n           const pos = firstHit && typeof firstHit.start === \"number\"\n             ? charToLineColumn(src, spanRange(src, firstHit.start, firstHit.end)[0])\n             : { line: 1, column: 1 };\n-          dockProblems.push({\n+          ctx.caches.dockProblems.push({\n             severity: \"missing\",\n             file, line: pos.line, col: pos.column,\n             message: `missing assign '${name}' — referenced by the template but not present in data`,\n@@ -3500,17 +3500,17 @@\n         // refusal: amber \"cap\" Problems row, no output. Analyses above still\n         // ran, so the dock can show what was used and why it was blocked.\n         const denied = allowList !== null\n-          ? [...new Set(lastUsedTransformers.filter((n) => !allowList.includes(n)))]\n+          ? [...new Set(ctx.caches.lastUsedTransformers.filter((n) => !allowList.includes(n)))]\n           : [];\n         if (denied.length) {\n           const plural = denied.length > 1 ? \"s\" : \"\";\n           setError(`not in allow-list: transformer${plural} '${denied.join(\"', '\")}'`);\n-          dockProblems.push({\n+          ctx.caches.dockProblems.push({\n             severity: \"cap\", file: \"main\", line: 1, col: 1,\n             message: `transformer${plural} '${denied.join(\"', '\")}' not in the render-config allow-list — add to \\`transformers.allow\\` to render`,\n           });\n           setOutput(\"\"); renderDock(); saveHash();\n-          lastTimings = null;\n+          ctx.caches.lastTimings = null;\n           return;\n         }\n         try {\n@@ -3532,11 +3532,11 @@\n             const key = helperKey(mainSource, partialsMap(), data, helperSrc, catSrc, labLocale, ENGINE);\n             if (helperCache.key !== key) {\n               requestHelperRender(key, { dialect: ENGINE, template: mainSource, data, partials: partialsMap(), helperSrc, catalogSrc: catSrc, locale: labLocale });\n-              setOutput(\"rendering…\"); setError(\"\"); renderDock(); saveHash(); lastTimings = null;\n+              setOutput(\"rendering…\"); setError(\"\"); renderDock(); saveHash(); ctx.caches.lastTimings = null;\n               return;\n             }\n             if (helperCache.error) {\n-              setError(helperCache.error); setOutput(\"\"); renderDock(); saveHash(); lastTimings = null;\n+              setError(helperCache.error); setOutput(\"\"); renderDock(); saveHash(); ctx.caches.lastTimings = null;\n               return;\n             }\n             output = helperCache.output; segments = [];\n@@ -3556,11 +3556,11 @@\n           const at = /^(\\d+):(\\d+):\\s*/.exec(message);\n           if (at) { line = +at[1]; col = +at[2]; message = message.slice(at[0].length); }\n           setError(\"render error: \" + message);\n-          dockProblems = [{ severity: isPolicy ? \"cap\" : \"render\", file: \"main\", line, col, message }];\n+          ctx.caches.dockProblems = [{ severity: isPolicy ? \"cap\" : \"render\", file: \"main\", line, col, message }];\n           setOutput(\"\");\n         }\n         const tRenderEnd = performance.now();\n-        lastTimings = {\n+        ctx.caches.lastTimings = {\n           parseMs: tParseEnd - tParseStart,\n           compileMs: tCompileEnd - tCompileStart,\n           renderMs: tRenderEnd - tRenderStart,\n@@ -4968,25 +4968,25 @@\n       // the dock body. Adding a panel is one entry here — no DOM wiring.\n       const dockTabs = [\n         { id: \"problems\", label: \"Problems\",\n-          count: () => dockProblems.length,\n-          badge: () => dockProblems.length === 0 ? \"success\" :\n-            (dockProblems.some((p) => p.severity === \"error\" || p.severity === \"render\") ? \"danger\" : \"warn\"),\n-          render: (body) => renderProblems(body, dockProblems, { openOrCreatePartial, openProblem }) },\n+          count: () => ctx.caches.dockProblems.length,\n+          badge: () => ctx.caches.dockProblems.length === 0 ? \"success\" :\n+            (ctx.caches.dockProblems.some((p) => p.severity === \"error\" || p.severity === \"render\") ? \"danger\" : \"warn\"),\n+          render: (body) => renderProblems(body, ctx.caches.dockProblems, { openOrCreatePartial, openProblem }) },\n         { id: \"transformers\", label: \"Transformers\", requires: [\"catalog\"],\n-          count: () => lastUsedTransformers.length,\n-          badge: () => lastUsedTransformers.length ? \"\" : \"success\",\n+          count: () => ctx.caches.lastUsedTransformers.length,\n+          badge: () => ctx.caches.lastUsedTransformers.length ? \"\" : \"success\",\n           render: renderTransformers },\n         { id: \"data-access\", label: \"Data Access\", requires: [\"required-assigns\"],\n-          count: () => lastDataAccess.filter((r) => r.status !== \"hit\").length,\n+          count: () => ctx.caches.lastDataAccess.filter((r) => r.status !== \"hit\").length,\n           badge: () => {\n-            const misses = lastDataAccess.filter((r) => r.status === \"miss\").length;\n-            const mism = lastDataAccess.filter((r) => r.status !== \"hit\" && r.status !== \"miss\").length;\n+            const misses = ctx.caches.lastDataAccess.filter((r) => r.status === \"miss\").length;\n+            const mism = ctx.caches.lastDataAccess.filter((r) => r.status !== \"hit\" && r.status !== \"miss\").length;\n             if (mism) return \"danger\";\n             if (misses) return \"warn\";\n-            return lastDataAccess.length ? \"success\" : \"\";\n+            return ctx.caches.lastDataAccess.length ? \"success\" : \"\";\n           },\n           render: (body) => renderDataAccess(body,\n-            { rows: lastDataAccess, requiredAssigns: lastRequiredAssigns, missingAssigns: lastMissingAssigns },\n+            { rows: ctx.caches.lastDataAccess, requiredAssigns: ctx.caches.lastRequiredAssigns, missingAssigns: ctx.caches.lastMissingAssigns },\n             {\n               rowPosition: (r) => {\n                 const src = fileSource(r.file);\n@@ -4997,61 +4997,61 @@\n               openProblem, saveAsVector,\n             }) },\n         { id: \"trim\", label: \"Whitespace\", requires: [\"standalone\"],\n-          count: () => lastWhitespace.length,\n+          count: () => ctx.caches.lastWhitespace.length,\n           badge: () => \"\",\n-          render: (body) => renderWhitespace(body, lastWhitespace, { openProblem, standaloneOn: standalone }) },\n+          render: (body) => renderWhitespace(body, ctx.caches.lastWhitespace, { openProblem, standaloneOn: standalone }) },\n         { id: \"partials\", label: \"Partials\", requires: [\"partial-graph\"],\n           count: () => {\n-            if (!lastDeps) return 0;\n+            if (!ctx.caches.lastDeps) return 0;\n             // Count partials (everything except main) — main is the entry,\n             // not a partial. Empty count is what the user expects when a\n             // template uses no `{{> name}}`.\n-            return lastDeps.nodes.filter((n) => !n.isMain).length;\n+            return ctx.caches.lastDeps.nodes.filter((n) => !n.isMain).length;\n           },\n           badge: () => {\n-            if (!lastDeps) return \"\";\n-            if (lastDeps.nodes.some((n) => n.missing)) return \"warn\";\n-            const partials = lastDeps.nodes.filter((n) => !n.isMain);\n+            if (!ctx.caches.lastDeps) return \"\";\n+            if (ctx.caches.lastDeps.nodes.some((n) => n.missing)) return \"warn\";\n+            const partials = ctx.caches.lastDeps.nodes.filter((n) => !n.isMain);\n             if (partials.length === 0) return \"\"; // no partials in use — neutral\n-            return lastDeps.cycles.length ? \"\" : \"success\";\n+            return ctx.caches.lastDeps.cycles.length ? \"\" : \"success\";\n           },\n           render: renderPartials },\n         { id: \"capabilities\", label: \"Capabilities\",\n           count: () => {\n-            const evalCount = lastUsedTransformers.filter((n) => n === \"eval\").length;\n-            const xform = lastUsedTransformers.filter((n) => TRANSFORMER_RISK[n] === \"transform\").length;\n+            const evalCount = ctx.caches.lastUsedTransformers.filter((n) => n === \"eval\").length;\n+            const xform = ctx.caches.lastUsedTransformers.filter((n) => TRANSFORMER_RISK[n] === \"transform\").length;\n             return evalCount + xform;\n           },\n           badge: () => {\n-            if (lastUsedTransformers.includes(\"eval\")) return \"danger\";\n+            if (ctx.caches.lastUsedTransformers.includes(\"eval\")) return \"danger\";\n             if (allowScripts) return \"warn\";\n-            if (lastUsedTransformers.some((n) => TRANSFORMER_RISK[n] === \"transform\")) return \"warn\";\n+            if (ctx.caches.lastUsedTransformers.some((n) => TRANSFORMER_RISK[n] === \"transform\")) return \"warn\";\n             return \"success\";\n           },\n           render: (body) => renderCapabilities(body, {\n-            escapeRuns: lastEscapeRuns,\n-            usedTransformers: lastUsedTransformers,\n+            escapeRuns: ctx.caches.lastEscapeRuns,\n+            usedTransformers: ctx.caches.lastUsedTransformers,\n             stemAllowList: engineHas(\"stem-allow-list\"),\n             engineMeta,\n             escapeMode, standalone, allowList,\n           }) },\n         { id: \"perf\", label: \"Performance\",\n-          count: () => lastTimings ? Math.round(lastTimings.totalMs) : 0,\n+          count: () => ctx.caches.lastTimings ? Math.round(ctx.caches.lastTimings.totalMs) : 0,\n           badge: () => {\n-            if (!lastTimings) return \"\";\n-            if (lastTimings.totalMs > 100) return \"warn\";\n-            if (lastTimings.totalMs > 500) return \"danger\";\n+            if (!ctx.caches.lastTimings) return \"\";\n+            if (ctx.caches.lastTimings.totalMs > 100) return \"warn\";\n+            if (ctx.caches.lastTimings.totalMs > 500) return \"danger\";\n             return \"success\";\n           },\n-          render: (body) => renderPerformance(body, lastTimings) },\n+          render: (body) => renderPerformance(body, ctx.caches.lastTimings) },\n         { id: \"coverage\", label: \"Coverage\",\n-          count: () => lastCoverage.filter((r) => !r.executed).length,\n+          count: () => ctx.caches.lastCoverage.filter((r) => !r.executed).length,\n           badge: () => {\n-            if (!lastCoverage.length) return \"\";\n-            const dead = lastCoverage.filter((r) => !r.executed).length;\n+            if (!ctx.caches.lastCoverage.length) return \"\";\n+            const dead = ctx.caches.lastCoverage.filter((r) => !r.executed).length;\n             return dead ? \"warn\" : \"success\";\n           },\n-          render: (body) => renderCoverage(body, lastCoverage, {\n+          render: (body) => renderCoverage(body, ctx.caches.lastCoverage, {\n             rowPosition: (r) => {\n               const src = fileSource(r.file);\n               return typeof r.start === \"number\"\n@@ -5063,23 +5063,23 @@\n         // ADR-022: conditions whose branch would differ on another engine\n         // (Mustache/StringTemplate4/…), with a portable fix each.\n         { id: \"truthiness\", label: \"Truthiness\", requires: [\"analyse\"],\n-          count: () => lastAnalyse.findings.length,\n+          count: () => ctx.caches.lastAnalyse.findings.length,\n           badge: () => {\n-            if (!lastAnalyse.ok) return \"\";\n-            return lastAnalyse.findings.length ? \"warn\" : \"success\";\n+            if (!ctx.caches.lastAnalyse.ok) return \"\";\n+            return ctx.caches.lastAnalyse.findings.length ? \"warn\" : \"success\";\n           },\n           render: (body) => renderTruthiness(body,\n-            { analyse: lastAnalyse, suppressed: lastAnalyseSuppressed, pathSchema: labPathSchema },\n+            { analyse: ctx.caches.lastAnalyse, suppressed: ctx.caches.lastAnalyseSuppressed, pathSchema: labPathSchema },\n             { openProblem }) },\n         // ADR-019: deprecated aliases + non-canonical scoped variables, each with\n         // the canonical spelling the editor quick-fix / `flatbars lint` steers to.\n         { id: \"lint\", label: \"Lint\", requires: [\"lint\"],\n-          count: () => lastLint.findings.length,\n+          count: () => ctx.caches.lastLint.findings.length,\n           badge: () => {\n-            if (!lastLint.ok) return \"\";\n-            return lastLint.findings.length ? \"warn\" : \"success\";\n+            if (!ctx.caches.lastLint.ok) return \"\";\n+            return ctx.caches.lastLint.findings.length ? \"warn\" : \"success\";\n           },\n-          render: (body) => renderLint(body, lastLint, { openProblem }) },\n+          render: (body) => renderLint(body, ctx.caches.lastLint, { openProblem }) },\n         // The read-only inspectors (moved here from the Sources pane). They are\n         // not analyses with a count — `noBadge` suppresses the badge — and follow\n         // the active file / last render (re-rendered by renderDock after each run).\n@@ -5152,7 +5152,7 @@\n         paintDockTabs();\n \n         // Auto-open on the 0→>0 transition so problems surface without a click.\n-        const probCount = dockProblems.length;\n+        const probCount = ctx.caches.dockProblems.length;\n         if (probCount > 0 && !hadProblems && byId(\"dock\").hidden) {\n           byId(\"dock\").hidden = false;\n           byId(\"dock-toggle\").classList.add(\"active\");\n@@ -5180,8 +5180,8 @@\n       // helpers into the module's pure-renderer signature.\n       function renderTransformers(body) {\n         renderTransformersPanel(body, {\n-          usedTransformers: lastUsedTransformers,\n-          calls: lastCalls,\n+          usedTransformers: ctx.caches.lastUsedTransformers,\n+          calls: ctx.caches.lastCalls,\n           engineBuiltins,\n           makeEl,\n           fileSource,\n@@ -5197,7 +5197,7 @@\n       // Every data lookup the template makes (paths and bare identifiers),\n       // classified hit/miss/oob/type-mismatch/scoped. Jump-linked to source.\n       // Panel #1: Data Access now lives in ./app/dock/data-access.mjs — it takes\n-      // its three caches (lastDataAccess / lastRequiredAssigns / lastMissingAssigns)\n+      // its three caches (ctx.caches.lastDataAccess / ctx.caches.lastRequiredAssigns / ctx.caches.lastMissingAssigns)\n       // plus injected rowPosition, openProblem, and saveAsVector (the descriptor\n       // above passes them). dxSnippet moved into that module.\n \n@@ -5218,7 +5218,7 @@\n           data: ctx.caches.lastData == null ? {} : ctx.caches.lastData,\n           escape: escapeMode,\n           expected: ctx.caches.lastOutput,\n-          transformers: lastUsedTransformers,\n+          transformers: ctx.caches.lastUsedTransformers,\n         };\n         const json = JSON.stringify(vector);\n         navigator.clipboard?.writeText(json).then(\n@@ -5229,7 +5229,7 @@\n       // dxSnippet moved into ./app/dock/data-access.mjs (its only consumer).\n \n       // Panel #2: Whitespace trim trace now lives in ./app/dock/whitespace.mjs —\n-      // it takes its cache (lastWhitespace) plus the openProblem callback and the\n+      // it takes its cache (ctx.caches.lastWhitespace) plus the openProblem callback and the\n       // global standalone-trim flag (the descriptor above passes them).\n \n       // ── Panel #3: Partial graph (Dependencies, formerly the floating popup) ──\n@@ -5248,7 +5248,7 @@\n       // into the module's pure-renderer signature.\n       function renderPartials(body) {\n         renderPartialsPanel(body, {\n-          partialDeps: lastDeps,\n+          partialDeps: ctx.caches.lastDeps,\n           makeEl,\n           openOrCreatePartial,\n           tabIndexByFile,\n@@ -5266,24 +5266,24 @@\n       // emit-escape mix and the iframe+CSP sandbox are the engine-agnostic floor.\n       // Panel #5: Capabilities / escape provenance now lives in\n       // ./app/dock/capabilities.mjs — a pure DOM build taking the two caches\n-      // (lastEscapeRuns / lastUsedTransformers), the stem-allow-list capability,\n+      // (ctx.caches.lastEscapeRuns / ctx.caches.lastUsedTransformers), the stem-allow-list capability,\n       // engineMeta, and the render policy (escapeMode / standalone / allowList).\n       // The descriptor above passes them.\n \n       // Panel #6: Performance now lives in ./app/dock/performance.mjs — the first\n-      // dock panel migrated to the ctx form (it takes its one cache, lastTimings,\n+      // dock panel migrated to the ctx form (it takes its one cache, ctx.caches.lastTimings,\n       // as an argument; the descriptor above passes it).\n \n       // Panel #7: Coverage map now lives in ./app/dock/coverage.mjs — it takes its\n-      // cache (lastCoverage) plus an injected rowPosition closure (fileSource +\n+      // cache (ctx.caches.lastCoverage) plus an injected rowPosition closure (fileSource +\n       // charToLineColumn/spanRange) and openProblem (the descriptor passes them).\n \n       // Panel: Truthiness (ADR-022) now lives in ./app/dock/truthiness.mjs — it\n-      // takes the analyse cache (lastAnalyse), the suppressed count, and the host\n+      // takes the analyse cache (ctx.caches.lastAnalyse), the suppressed count, and the host\n       // pathSchema, plus openProblem (the descriptor above passes them).\n \n       // Lint panel (ADR-019) now lives in ./app/dock/lint.mjs — it takes the lint\n-      // cache (lastLint) and openProblem (the descriptor above passes them).\n+      // cache (ctx.caches.lastLint) and openProblem (the descriptor above passes them).\n \n       // Context Inspector: snapshots of the render context at the last-clicked\n       // output span, from the engine's `inspect_at` — one card per execution\n@@ -5353,7 +5353,7 @@\n       }\n \n       // Problems panel now lives in ./app/dock/problems.mjs — it takes the\n-      // dockProblems cache plus openOrCreatePartial + openProblem (the descriptor\n+      // ctx.caches.dockProblems cache plus openOrCreatePartial + openProblem (the descriptor\n       // above passes them).\n \n       // Jump from a Problems row to its source location.","raw_url":"https://github.com/wstein/flatbars/raw/2985300e4dcbabe8faa97d249575fa3a115aa18a/lab%2Findex.html","sha":"21b8f08c4d346956003154355fb22b81f981db2c","status":"modified"}]}