{"files":[{"additions":75,"blob_url":"https://github.com/wstein/flatbars/blob/9f51d8db9648d15170bfd0203389d8fe1f5bff38/spec%2Fsrc%2Fcontent%2Fdocs%2Fengine%2Fmaxbars.mdx","changes":149,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/spec%2Fsrc%2Fcontent%2Fdocs%2Fengine%2Fmaxbars.mdx?ref=9f51d8db9648d15170bfd0203389d8fe1f5bff38","deletions":74,"filename":"spec/src/content/docs/engine/maxbars.mdx","patch":"@@ -44,20 +44,20 @@ subexpression produces — so the prelude, engine, and emit rules need no change\n | `==` `!=` | `eq` `ne` | `{{ x == 1 }}` → `(eq x 1)` |\n | `<` `>` `<=` `>=` | `lt` `gt` `lte` `gte` | `{{ x >= 18 }}` → `(gte x 18)` |\n | `+` `-` `*` `/` `%` | `add` `subtract` `multiply` `divide` `modulo` | `{{ price * qty }}` → `(multiply price qty)` |\n-| `..` (range) | `range` | `{{#each 1..n}}` → `(range 1 n)` (the inclusive integer span) |\n+| `..` (range) | `range` | `{% for 1..n %}` → `(range 1 n)` (the inclusive integer span) |\n | `??` | `coalesce` | `{{ a ?? b }}` → `(coalesce a b)` (first non-null) |\n | `\\|` (pipe) | application | `{{ a \\| f x }}` → `(f a x)` (piped value first) |\n \n Precedence (loosest → tightest): pipe, `??`, `||`, `&&`, comparisons\n (non-associative), range `..` (non-associative), additive (`+` `-`),\n multiplicative (`*` `/` `%`), prefix `!`, then application/atom — so\n-`{{ n + 1 > 5 }}` is `(gt (add n 1) 5)` and `{{#each 1..n + 1}}` is\n+`{{ n + 1 > 5 }}` is `(gt (add n 1) 5)` and `{% for 1..n + 1 %}` is\n `(range 1 (add n 1))`.\n Pipe chains are left-associative: `a | f | g` ⇒ `(g (f a))`. Paths,\n literals, and `(parenthesised)` subexpressions are exactly as in ClassicBars.\n \n Infix reads identically in a **block head** and in its **clause separators**, so\n-`{{#if x >= 18}}…{{elif x < 5}}…{{/if}}` takes each condition bare — no parentheses\n+`{% if x >= 18 %}…{% elif x < 5 %}…{% endif %}` takes each condition bare — no parentheses\n around the `elif` test (it is one infix expression, parsed as a head: `elif (lt x 5)`).\n \n The operator characters are excluded from the identifier set so they tokenise as\n@@ -77,13 +77,13 @@ truthiness rule.\n ## Collection literals\n \n MaxBars adds two literal *atoms* — a `[…]` list and a `{k: v}` dict — usable\n-anywhere an expression is (`each` subjects, arguments, pipe sources, nested):\n+anywhere an expression is (loop subjects, arguments, pipe sources, nested):\n \n-```handlebars\n-{{#each [1, 2, 3]}}{{this}} {{/each}}            {{!-- ⇒ (list 1 2 3) --}}\n-{{#each [first, n + 1, {tag: x}]}}…{{/each}}     {{!-- elements are full expressions --}}\n-{{#with {name: who, age: 30} }}{{name}}{{/with}} {{!-- ⇒ (dict \"name\" who \"age\" 30) --}}\n-{{{ json {\"full name\": who} }}}                  {{!-- a string key, for spaces --}}\n+```maxbars\n+{% for [1, 2, 3] %}{{this}} {% endfor %}            {{!-- ⇒ (list 1 2 3) --}}\n+{% for [first, n + 1, {tag: x}] %}…{% endfor %}     {{!-- elements are full expressions --}}\n+{% scope {name: who, age: 30} %}{{name}}{% endscope %} {{!-- ⇒ (dict \"name\" who \"age\" 30) --}}\n+{{{ json {\"full name\": who} }}}                     {{!-- a string key, for spaces --}}\n ```\n \n They are pure sugar: `[e1, …]` desugars to the prelude `list` helper and\n@@ -98,7 +98,7 @@ The lexer carves `[` `]` `{` `}` `,` as their own punctuation only in MaxBars\n town]` path-bracket — is untouched; only a **leading** `[` opens a list. The\n structural scanner is *brace-aware* under the same flag: it balances `{ }` and\n skips string literals, so a dict's own `}` is consumed before the tag close and\n-`{{#with {a: 1}}}` (or `{{{json {x: 1}}}}`) needs **no** disambiguating space — a\n+`{% scope {a: 1} %}` (or `{{{json {x: 1}}}}`) needs **no** disambiguating space — a\n `}}` inside a dict-value string is not a tag close either.\n \n ## Collection filters\n@@ -107,13 +107,13 @@ The shared prelude carries five **key-based** filter operations\n ([ADR-036](/adr/adr-0036-collection-filter-operations/)) — pipe-able like every\n other collection op, no new surface:\n \n-```handlebars\n-{{#each row in (rows | where \"active\") }}…{{/each}}     {{!-- key is truthy --}}\n-{{#each u in (users | where \"tier\" \"gold\") }}…{{/each}} {{!-- key == value --}}\n-{{#each u in (users | reject \"archived\") }}…{{/each}}   {{!-- complement of where --}}\n-{{ users | find \"role\" \"admin\" | json }}                {{!-- first match, else null --}}\n-{{#if (orders | some \"open\") }}You have open orders.{{/if}}\n-{{#if (rows | every \"valid\") }}All valid.{{/if}}\n+```maxbars\n+{% for row in (rows | where \"active\") %}…{% endfor %}     {{!-- key is truthy --}}\n+{% for u in (users | where \"tier\" \"gold\") %}…{% endfor %} {{!-- key == value --}}\n+{% for u in (users | reject \"archived\") %}…{% endfor %}   {{!-- complement of where --}}\n+{{ users | find \"role\" \"admin\" | json }}                  {{!-- first match, else null --}}\n+{% if (orders | some \"open\") %}You have open orders.{% endif %}\n+{% if (rows | every \"valid\") %}All valid.{% endif %}\n ```\n \n | Operation | 2-arg (`coll key`) | 3-arg (`coll key value`) |\n@@ -125,7 +125,7 @@ other collection op, no new surface:\n The key is a dotted path (`where users \"profile.active\"`), resolved exactly as\n `sortBy` / `groupBy` / `pluck` resolve theirs. The 2-arg *truthy* form reads the\n engine's rule, so under MaxBars's `nonEmpty` a `0` field is **kept** (it is truthy\n-here) where ClassicBars's `handlebars` rule would drop it — the same split `{{#if}}`\n+here) where ClassicBars's `handlebars` rule would drop it — the same split `{% if %}`\n shows. There are **no lambdas** ([ADR-020](/adr/adr-0020-user-block-helpers/)), so\n the filter is key-based, not predicate-based: `where (gt x.score 50)` is *not*\n expressible — precompute the flag (`{ score, top }`) and filter on the key.\n@@ -159,9 +159,9 @@ construction. `@root` becomes `root`, `../x` becomes `parent.x`, and an\n `@`-prefixed name is a *located parse error* pointing at this model.\n \n ```handlebars\n-{{#each items}}{{loop.index1}}/{{loop.length}}: {{this.name}}{{#unless loop.last}}, {{/unless}}{{/each}}\n+{% for items %}{{loop.index1}}/{{loop.length}}: {{this.name}}{% unless loop.last %}, {% endunless %}{% endfor %}\n \n-{{#each rows}}{{#each this}}{{loop.parent.index0}}.{{loop.index0}} {{parent.label}} {{root.title}}{{/each}}{{/each}}\n+{% for rows %}{% for this %}{{loop.parent.index0}}.{{loop.index0}} {{parent.label}} {{root.title}}{% endfor %}{% endfor %}\n ```\n \n ## The block-partial body (`{% yield %}`)\n@@ -200,30 +200,30 @@ A block *open* parses its head as the helper name followed by arguments, each a\n full infix expression — so a condition needs no parentheses:\n \n ```handlebars\n-{{#if x > 0 && x < 10}}in range{{/if}}      {{!-- = {{#if (and (gt x 0) (lt x 10))}} --}}\n-{{#unless a || b}}neither{{/unless}}\n+{% if x > 0 && x < 10 %}in range{% endif %}      {{!-- = {% if (and (gt x 0) (lt x 10)) %} --}}\n+{% unless a || b %}neither{% endunless %}\n ```\n \n This is the core `parseHead` seam: only block opens use it. A *clause* separator\n-still takes a parenthesised condition — `{{elif (a && b)}}`,\n+still takes a parenthesised condition — `{% elif (a && b) %}`,\n `{{else if (a && b)}}` — because separators share their tag shape with\n output expressions.\n \n ## Multi-arm `case`\n \n-For a value with several branches, `{{#case}}` dispatches the subject against each\n+For a value with several branches, `{% case %}` dispatches the subject against each\n `{{when}}` arm by equality — clearer than a chain of `{{else if (eq s …)}}`:\n \n ```handlebars\n-{{#case status}}\n-{{when \"shipped\"}}On its way\n-{{when \"pending\" \"queued\"}}Waiting\n-{{else}}Unknown\n-{{/case}}\n+{% case status %}\n+{% when \"shipped\" %}On its way\n+{% when \"pending\" \"queued\" %}Waiting\n+{% else %}Unknown\n+{% endcase %}\n ```\n \n The subject is a full expression; each `{{when}}` carries one or more **value\n-expressions** and is taken when the subject equals **any** of them. `{{else}}` is the\n+expressions** and is taken when the subject equals **any** of them. `{% else %}` is the\n catch-all (with none, an unmatched subject renders nothing). `when`/`else` are\n clause separators (their standalone lines are trimmed like `else`/`elif`); only\n whitespace may precede the first `{{when}}` — leading content is a located error.\n@@ -232,7 +232,7 @@ whitespace may precede the first `{{when}}` — leading content is a located err\n once (`Kernel.Prelude.caseH`) and the Trussbars compiler lowers it to a Rust\n `match`. It belongs to the `nonEmpty`-truthiness family — **RawBars and MaxBars\n only**; ClassicBars (Handlebars-faithful) has no `case` and rejects it with a located\n-error pointing at the `{{#if (eq …)}}` form. The future typed-exhaustive variant\n+error pointing at the `{% if (eq …) %}` form. The future typed-exhaustive variant\n takes the `match` keyword with pattern arms.\n \n ## Loop and block bindings\n@@ -242,27 +242,27 @@ won't change further:\n \n | Construct | Operation | Spelling |\n | --- | --- | --- |\n-| `{{#each elem [i0] [i1] in coll}}` | iterate a collection | `… in coll` (names first) |\n-| `{{#with value as alias}}` | re-root the context + alias | `value as alias` |\n-| `{{#let name=value …}}` | alias, no re-root | `name=value` |\n+| `{% for elem [i0] [i1] in coll %}` | iterate a collection | `… in coll` (names first) |\n+| `{% scope value [as alias] %}` | re-root the context (optional alias) | `value [as alias]` |\n+| `{% local name=value … %}` | alias, no re-root | `name=value` |\n \n The keywords differ because the operations do: `in` reads \"drawn from a\n-collection\" (so it fits `each`, not `with`), `as` is the alias/re-root word, and\n-`=` is the binding word. `{{#each}}` binds its loop variables **Liquid-style** —\n-the names come *before* `in`, the collection after — and `{{#with}}` aliases the\n-shifted context with a *drop-pipes* `as` (no bars). `each` binds up to three\n-names: the element, the 0-based index (or object key), and the 1-based index —\n-one more than ClassicBars' two.\n+collection\" (so it fits `for`, not `scope`), `as` is the alias word, and\n+`=` is the binding word. `{% for %}` binds its loop variables **Liquid-style** —\n+the names come *before* `in`, the collection after — and `{% scope %}` re-roots the\n+context, optionally aliasing it with a *drop-pipes* `as` (no bars). `for` binds up\n+to three names: the element, the 0-based index (or object key), and the 1-based\n+index — one more than ClassicBars' two.\n \n-```handlebars\n-{{#each item in items}}{{item}}{{/each}}\n-{{#each item i in items}}{{i}}: {{item}}{{/each}}\n-{{#each item i0 i1 in items}}{{i0}}/{{i1}}: {{item}}{{/each}}\n-{{#each value key in obj}}{{key}}={{value}}{{/each}}\n-{{#with user as u}}{{u.name}}{{/with}}\n+```maxbars\n+{% for item in items %}{{item}}{% endfor %}\n+{% for item i in items %}{{i}}: {{item}}{% endfor %}\n+{% for item i0 i1 in items %}{{i0}}/{{i1}}: {{item}}{% endfor %}\n+{% for value key in obj %}{{key}}={{value}}{% endfor %}\n+{% scope user as u %}{{u.name}}{% endscope %}\n ```\n \n-`{{#each items}}` (no `in`) is the binding-less form (`this` is the element). The\n+`{% for items %}` (no `in`) is the binding-less form (`this` is the element). The\n head grammar omits the *pipe* rung and MaxBars rejects a bar in a block head\n outright (the Handlebars `as |a b|` form is a parse error here). The binding names\n are bare identifiers carried to the same surface desugar ClassicBars uses\n@@ -271,38 +271,39 @@ binding, no other machinery. The collection after `in` is a full expression —\n path, a `(subexpr)`, a `1..n` range, or a `[…]` list — so to *pipe* it,\n parenthesise:\n \n-```handlebars\n-{{#each p in (people | reverse)}}{{p.name}}{{/each}}\n+```maxbars\n+{% for p in (people | reverse) %}{{p.name}}{% endfor %}\n ```\n \n-## Let bindings\n+## Local bindings\n \n-`{{#let name=expr …}}…{{/let}}` ([ADR-024](/adr/adr-0024-let-and-dict-literal/))\n+`{% local name=expr … %}…{% endlocal %}` ([ADR-024](/adr/adr-0024-let-and-dict-literal/))\n installs *block-scoped aliases* — template-local constants for a deep path, a\n-computed value, a small lookup table — that vanish at `{{/let}}`. Each name is a\n-nullary operation returning its value:\n+computed value, a small lookup table — that vanish at `{% endlocal %}`. (The keyword\n+was `let` before docs-17; it is now `local` for the bounded binding, `set` for the\n+forward one.) Each name is a nullary operation returning its value:\n \n-```handlebars\n-{{#let total=(add price tax) label=\"Subtotal\"}}\n+```maxbars\n+{% local total=(add price tax) label=\"Subtotal\" %}\n   {{label}}: {{total}}\n-{{/let}}\n-{{#let u=order.customer}}{{u.name}} — {{u.email}}{{/let}}\n+{% endlocal %}\n+{% local u=order.customer %}{{u.name}} — {{u.email}}{% endlocal %}\n ```\n \n Two properties define it:\n \n * **Sequential.** Bindings evaluate left-to-right, each in the scope extended by\n-  those to its left, so `{{#let a=1 b=(add a 1)}}` binds `b` to `2`. There is no\n+  those to its left, so `{% local a=1 b=(add a 1) %}` binds `b` to `2`. There is no\n   self- or forward-reference (a sequential `let`, not a `letrec`).\n-* **Never re-roots.** Unlike `{{#with}}`, `let` only *aliases* — it does not shift\n+* **Never re-roots.** Unlike `{% scope %}`, `local` only *aliases* — it does not shift\n   the context, so a bare `{{name}}` in the body still resolves against the current\n-  context. `{{#let u=user}}{{name}}{{/let}}` reads the *root* `name`, not\n+  context. `{% local u=user %}{{name}}{% endlocal %}` reads the *root* `name`, not\n   `user.name` (write `{{u.name}}`). The complement holds:\n-  `{{#with x as p}}` ≡ `{{#with x}}{{#let p=this}}…{{/let}}{{/with}}`.\n+  `{% scope x as p %}` ≡ `{% scope x %}{% local p=this %}…{% endlocal %}{% endscope %}`.\n \n A binding whose name matches an in-scope operation shadows it for the body\n-(`{{#let add=\"…\"}}` reads the alias, not the `add` helper). Values are any\n-expression — including a dict or list literal (`{{#let cfg={theme: \"dark\"}}}`).\n+(`{% local add=\"…\" %}` reads the alias, not the `add` helper). Values are any\n+expression — including a dict or list literal (`{% local cfg={theme: \"dark\"} %}`).\n The surface desugars to nested single-binding lets, which the engine installs with\n `pushHelpers` (the context-preserving frame push); it is MaxBars-only sugar over\n that one engine primitive (ClassicBars stays Handlebars-faithful and uses\n@@ -317,18 +318,18 @@ shape); it binds the frame reified as an object whose fields are the loop\n variables (`index0`/`first`/`length`/…):\n \n ```handlebars\n-{{#each row in rows label outer}}\n-  {{#each cell in row}}{{outer.index1}}/{{outer.length}}: {{cell}}{{#if outer.first}} (first row){{/if}}\n-  {{/each}}\n-{{/each}}\n+{% for row in rows label outer %}\n+  {% for cell in row %}{{outer.index1}}/{{outer.length}}: {{cell}}{% if outer.first %} (first row){% endif %}\n+  {% endfor %}\n+{% endfor %}\n ```\n \n `outer` exposes `this` / `index0` / `index1` / `rindex0` / `rindex1` / `first` /\n `last` / `length` / `key` — the same values, and the same normative arithmetic, as\n the bare loop variables of that loop. It is an *immutable per-iteration snapshot*,\n reached by ordinary path lookup (`{{outer.length}}`), so the interpreter and\n compiler agree by construction. A label and loop bindings compose:\n-`{{#each row in rows label outer}}` binds both `row` (the element) and\n+`{% for row in rows label outer %}` binds both `row` (the element) and\n `outer` (the frame).\n \n Notes:\n@@ -356,7 +357,7 @@ renderWithOperations\n ```\n \n A *block* operation receives a Handlebars-style `options`: `options.fn(ctx)`\n-renders the body (shifting context), `options.inverse(ctx)` the `{{else}}`\n+renders the body (shifting context), `options.inverse(ctx)` the `{% else %}`\n clause, `options.hash` the surface `k=v` arguments, `options.fn(ctx, { data })`\n supplies scoped `@vars`, and `options.fn(ctx, { blockParams })` binds the names a\n custom block helper declares with a trailing drop-pipes `as a b` (the built-in\n@@ -369,11 +370,11 @@ plain functions into these operations (the FlatBars Lab and `flatbars-js`).\n \n ## v1 limitations\n \n-* **Clause separators need parenthesised conditions** — `{{elif (a && b)}}`\n+* **Clause separators need parenthesised conditions** — `{% elif (a && b) %}`\n   / `{{else if (a && b)}}`, since `{{ … }}` separators and output\n-  share a tag shape. Block opens (`{{#if a && b}}`) do not.\n+  share a tag shape. Block opens (`{% if a && b %}`) do not.\n * **A bar in a block head is a parse error** — to pipe a head argument,\n-  parenthesise it: `{{#each (xs | f) …}}`, not `{{#each xs | f}}`. The head\n+  parenthesise it: `{% for (xs | f) … %}`, not `{% for xs | f %}`. The head\n   grammar omits the top-level pipe rung, and MaxBars block params drop the pipes\n   (`as x`, not `as |x|`), so a bar in a head has no meaning and is rejected\n   outright — whether it was the Handlebars block-param delimiter or an\n@@ -399,9 +400,9 @@ plain functions into these operations (the FlatBars Lab and `flatbars-js`).\n \n MaxBars uses the `nonEmpty` rule (ADR-022): `false`/`null`/`\"\"`/`[]`/`{}`\n are falsy, but `0` is *truthy* — test magnitude with the built-in compare,\n-`{{#if val > 0}}`. This *differs* from ClassicBars (`handlebars`, where `0`\n+`{% if val > 0 %}`. This *differs* from ClassicBars (`handlebars`, where `0`\n and `{}` flip); MaxBars is not a Handlebars clone, so it leans on its\n compare operators rather than coercing `0`. There is no `@truthiness` directive\n-(removed in [ADR-022](/adr/adr-0022-analyse-mode/)). `{{#if x}}` and\n+(removed in [ADR-022](/adr/adr-0022-analyse-mode/)). `{% if x %}` and\n `{{ x && y }}` always agree (both test through the engine's one rule).\n `includeZero` is a no-op here (`0` is already truthy). See [directives](/engine/directives/).","raw_url":"https://github.com/wstein/flatbars/raw/9f51d8db9648d15170bfd0203389d8fe1f5bff38/spec%2Fsrc%2Fcontent%2Fdocs%2Fengine%2Fmaxbars.mdx","sha":"7dc588563b4e0a8c06ffbb47669bc45bf48af76a","status":"modified"}]}