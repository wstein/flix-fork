{"files":[{"additions":8,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/.github%2Fworkflows%2Fci.yml","changes":8,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/.github%2Fworkflows%2Fci.yml?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":".github/workflows/ci.yml","patch":"@@ -195,6 +195,14 @@ jobs:\n         # each against spec/lab-protocol-schema.json.\n         run: npm run check:lab-parity\n \n+      - name: atn-ts backend parity gate (all-star-port-plan.md Phase 5)\n+        # Runs the conformance corpus through `gramaire emit --backend atn-ts\n+        # --strategy ll-star` (sbt runMain), transpiles the emitted TS with\n+        # tsc, and runs it under Node — proving the emitted ATN recognizer,\n+        # not just its golden text, actually accepts/rejects like\n+        # Ll.recognize on every vector.\n+        run: npm run check:atn-ts-parity\n+\n       - name: Build\n         run: npm run build\n ","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/.github%2Fworkflows%2Fci.yml","sha":"58d6b627f93b9a54bb177fef8e52ac94587702fb","status":"modified"},{"additions":93,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FAtnTsParityMain.scala","changes":93,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FAtnTsParityMain.scala?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"cli/jvm/src/main/scala/gramaire/cli/AtnTsParityMain.scala","patch":"@@ -0,0 +1,93 @@\n+package gramaire.cli\n+\n+import java.nio.file.{Files, Path}\n+import gramaire.*\n+\n+// A thin JVM entry point for the `atn-ts` backend's parity gate (site/scripts/check-atn-ts-\n+// parity.mjs, mirroring gramaire.lab.LabParityMain/check-lab-parity.mjs's own architecture): for\n+// every conformance-corpus grammar, writes the `atn-ts` backend's emitted `.ts` recognizer to\n+// `outDir` and prints each vector's *already-lexed* tokens plus Ll.recognize's own accept/reject —\n+// the actual thing under test, computed live, not a hardcoded expectation restated here. The\n+// calling script transpiles and runs the emitted TS against those same tokens and diffs its\n+// accept/reject against what this process printed; tokens travel pre-lexed (not raw input text) so\n+// the Node side never needs to reimplement any of this corpus's five different lexers.\n+//\n+// Not a CLI feature — `gramaire.cli.Main` never dispatches to it — just a shell invocable via\n+// `sbt \"cli/runMain gramaire.cli.AtnTsParityMain <outDir>\"`.\n+object AtnTsParityMain:\n+  private val startMarker = \"===ATN-TS-PARITY-CORPUS-START===\"\n+  private val endMarker = \"===ATN-TS-PARITY-CORPUS-END===\"\n+\n+  // A corpus entry paired with the declared precedence its OWN file (if any) needs for a\n+  // successful table build — `calc-prec`'s `expr` is deliberately ambiguous without it (ADR D37),\n+  // so building with `Table.emptyPrec` the way a precedence-free corpus entry would use fails with\n+  // real conflicts; `IR.withStrategy`'s atn-building doesn't touch tables at all, but it still\n+  // needs a base `IR` from a successful `buildIRP` to attach the atn to.\n+  private final case class Entry(descriptor: Descriptor, prec: Precedence)\n+\n+  private def readFile(path: String): Either[String, String] =\n+    try Right(Files.readString(Path.of(path)))\n+    catch case e: Exception => Left(e.getMessage)\n+\n+  // Mirrors `Main.loadDescriptor`: a corpus grammar whose file is absent or unparseable is\n+  // skipped, not a hard failure — this gate only speaks for the corpora it can actually load.\n+  private def loadEntry(path: String, mk: (String, Grammar) => Descriptor): Option[Entry] =\n+    readFile(path).toOption.flatMap(md =>\n+      Lr.parse(md).toOption.map(g => Entry(mk(md, g), Lr.precedenceOf(md)))\n+    )\n+\n+  private def emitAtnTs(language: String, g: Grammar, prec: Precedence, outDir: String): Unit =\n+    IR.buildIRP(prec, Method.Canonical, language, g) match\n+      case Left(_) =>\n+        System.err.println(s\"$language: skipped — LR(1) conflicts, no table to derive atn from\")\n+      case Right(ir0) =>\n+        val ir = IR.withStrategy(\"ll-star\", g, ir0)\n+        BackendAtnTs.backend.emit(ir).foreach { out =>\n+          Files.writeString(Path.of(s\"$outDir/${out.path}\"), out.contents)\n+        }\n+\n+  // A vector's tokens as a JSON array of `{terminal, text}` — `d.lexer(input)`'s already-lexed\n+  // result, so the Node side never re-lexes anything.\n+  private def tokensJson(toks: Vector[Token]): String =\n+    toks\n+      .map(t =>\n+        s\"\"\"{ \"terminal\": ${Json.stringify(Json.JString(t.terminal))}, \"text\": ${Json\n+            .stringify(Json.JString(t.text))} }\"\"\"\n+      )\n+      .mkString(\"[\", \", \", \"]\")\n+\n+  private def vectorJson(d: Descriptor, v: TestVector): Option[String] =\n+    d.lexer(v.input) match\n+      case Left(_) => None // a lex failure has no token list to hand the TS side at all\n+      case Right(toks) =>\n+        val expected = Ll.recognize(d.grammar, toks)\n+        Some(\n+          s\"\"\"    { \"name\": ${Json.stringify(Json.JString(v.name))}, \"tokens\": ${tokensJson(\n+              toks\n+            )}, \"expected\": $expected }\"\"\"\n+        )\n+\n+  private def report(e: Entry, outDir: String): Unit =\n+    val d = e.descriptor\n+    emitAtnTs(d.language, d.grammar, e.prec, outDir)\n+    val vectors = d.vectors.flatMap(v => vectorJson(d, v))\n+    println(startMarker)\n+    println(s\"\"\"{ \"language\": ${Json.stringify(Json.JString(d.language))}, \"vectors\": [\"\"\")\n+    println(vectors.mkString(\",\\n\"))\n+    println(\"] }\")\n+    println(endMarker)\n+\n+  def main(args: Array[String]): Unit =\n+    args.headOption match\n+      case None =>\n+        System.err.println(\"usage: AtnTsParityMain <outDir>\")\n+        sys.exit(1)\n+      case Some(outDir) =>\n+        Files.createDirectories(Path.of(outDir))\n+        val lr = Entry(Conformance.lrDescriptor, Table.emptyPrec)\n+        val calc = loadEntry(\"examples/calc.gram.md\", (_, g) => Conformance.calcDescriptor(g))\n+        val json = loadEntry(\"examples/json.gram.md\", Conformance.jsonDescriptor)\n+        val ecma404 = loadEntry(\"examples/ECMA-404.gram.md\", Conformance.ecma404Descriptor)\n+        val calcPrec = loadEntry(\"examples/calc-prec.gram.md\", Conformance.calcPrecDescriptor)\n+        val entries = Vector(Some(lr), calc, json, ecma404, calcPrec).flatten\n+        entries.foreach(report(_, outDir))","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/cli%2Fjvm%2Fsrc%2Fmain%2Fscala%2Fgramaire%2Fcli%2FAtnTsParityMain.scala","sha":"48e75cd845b87b5cc4c27bd9e991917aef57b88a","status":"added"},{"additions":19,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","changes":20,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":1,"filename":"cli/jvm/src/test/scala/gramaire/cli/MainSuite.scala","patch":"@@ -1,6 +1,17 @@\n package gramaire.cli\n \n-import gramaire.{Alt, BackendIr, BackendJs, BackendTs, Grammar, IR, Method, Rule}\n+import gramaire.{\n+  Alt,\n+  BackendAtnTs,\n+  BackendIr,\n+  BackendJs,\n+  BackendRegistry,\n+  BackendTs,\n+  Grammar,\n+  IR,\n+  Method,\n+  Rule\n+}\n import gramaire.Sym.*\n \n // Covers `Main`'s pure argument-parsing and name-resolution helpers —\n@@ -98,6 +109,13 @@ class MainSuite extends munit.FunSuite:\n     // A structure-reading backend is agnostic to which table/ATN strategy produced the IR.\n     assert(Main.backendSupportsStrategy(BackendIr.backend, \"lr\"))\n     assert(Main.backendSupportsStrategy(BackendIr.backend, \"ll-star\"))\n+    // atn-ts is the mirror image of js/ts: it reads only the ATN, so it's ll-star-only.\n+    assert(!Main.backendSupportsStrategy(BackendAtnTs.backend, \"lr\"))\n+    assert(Main.backendSupportsStrategy(BackendAtnTs.backend, \"ll-star\"))\n+  }\n+\n+  test(\"BackendRegistry: atn-ts is registered and reachable by name\") {\n+    assertEquals(BackendRegistry.findBackend(\"atn-ts\"), Some(BackendAtnTs.backend))\n   }\n \n   test(","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/cli%2Fjvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2Fcli%2FMainSuite.scala","sha":"833b2e8d5d12d4272a84a1ce9a04bf2febd0522e","status":"modified"},{"additions":16,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","changes":16,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"core/.jvm/src/test/scala/gramaire/BackendGoldenSuite.scala","patch":"@@ -79,6 +79,22 @@ class BackendGoldenSuite extends munit.FunSuite:\n             assertEquals(dot, readFile(\"test/golden/calc-atn.dot\"))\n   }\n \n+  // A golden-text pin, same convention as every other backend above — but see\n+  // site/scripts/check-atn-ts-parity.mjs for the test that actually matters for this backend: a\n+  // golden diff alone can't tell a correct `closure`/`move`/`predict` port from one that merely\n+  // still emits stable-looking text, only running the emitted TS can.\n+  test(\"atn-ts: examples/calc.gram.md under --strategy ll-star matches the committed golden\") {\n+    val md = readFile(\"examples/calc.gram.md\")\n+    Lr.parse(md) match\n+      case Left(e) => fail(s\"could not parse examples/calc.gram.md: $e\")\n+      case Right(g) =>\n+        IR.buildIR(Method.Canonical, \"Calc\", g) match\n+          case Left(_) => fail(\"could not build IR for calc\")\n+          case Right(ir) =>\n+            val irLl = IR.withStrategy(\"ll-star\", g, ir)\n+            assertEquals(BackendAtnTs.emit(irLl), readFile(\"test/golden/Calc.atn.ts\"))\n+  }\n+\n   test(\n     \"js: calc-js bakes its inline actions into one evaluate(cst), matching the committed golden\"\n   ) {","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2F.jvm%2Fsrc%2Ftest%2Fscala%2Fgramaire%2FBackendGoldenSuite.scala","sha":"5600387d023de2dda1675ef65ef8ba5c1d996ba4","status":"modified"},{"additions":273,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAtnTs.scala","changes":273,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAtnTs.scala?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"core/src/main/scala/gramaire/BackendAtnTs.scala","patch":"@@ -0,0 +1,273 @@\n+package gramaire\n+\n+// A `recognizer`-only, `ll-star`-only codegen backend: `gramaire-ir` in, a self-contained\n+// TypeScript ATN-driven recognizer out — `IR.atn`'s first *executable* consumer (every other\n+// reader, `BackendDot`'s `--strategy ll-star` rendering, only ever displays it).\n+//\n+// **v1 scope, deliberately recognizer-only** (`Capability.Recognizer`, not `Capability.Cst`):\n+// reproducing `Ll.parse`'s exact Cst (matching production ids, folding the left-recursion/\n+// precedence-cascade rewrites back onto the original grammar) needs IR-level provenance for\n+// `LeftRec.Fold`/`PrecClimb.Tag` that does not exist yet — `IR.withStrategy` serializes only the\n+// already-rewritten `atn`, discarding the fold information `Ll.parse` itself needs for the same\n+// job. Shipping recognizer-only first and CST second mirrors this port's own Phase 1 -> Phase 2\n+// staging for the Scala engine itself, not a scope cut invented for this backend alone.\n+//\n+// The emitted module is a straight, SLL-only transliteration of `AtnSim.closure`/`move`/`predict`\n+// and `Ll.recognize`'s own `parseRule`/`walk` drive loop (`predict.scala`'s two-stage SLL-then-\n+// full-LL retry, its DFA cache, and its ambiguity reporting are NOT ported for v1 — a plain,\n+// uncached SLL walk is enough to prove `IR.atn` is a real, executable substrate; the full-LL\n+// fallback and diagnostics are follow-up work, same as the engine's own already-documented\n+// scope boundary in `AtnSim.scala`/`docs/all-star-port-plan.md` §6). No *runtime* left-recursion\n+// handling is needed either: the ATN this reads is already the `LeftRec`-eliminated one `IR.atn`\n+// serializes server-side, so the emitted TS never sees a left-recursive shape to rewrite.\n+object BackendAtnTs:\n+  val backend: Backend = Backend(\n+    name = \"atn-ts\",\n+    capabilities = Vector(Capability.Recognizer),\n+    strategies = Vector(\"ll-star\"),\n+    emit = ir => Vector(Output(s\"${ir.grammar.name}.atn.ts\", emit(ir)))\n+  )\n+\n+  private def banner(ir: IR, what: String): String =\n+    s\"// GENERATED by Gramaire from the `${ir.grammar.name}` grammar — $what.\\n\" +\n+      \"// Do not edit; regenerate with `gramaire emit --backend atn-ts --strategy ll-star`.\"\n+\n+  // A TS/JSON object key or string literal (canonical escaping).\n+  private def str(s: String): String = Json.stringify(Json.JString(s))\n+\n+  private def transJson(t: IRAtnTrans): String = t match\n+    case IRAtnTrans.IRAtnEps(target) =>\n+      s\"\"\"{ kind: \"eps\", target: $target }\"\"\"\n+    case IRAtnTrans.IRAtnAtom(label, target) =>\n+      s\"\"\"{ kind: \"atom\", label: ${str(label)}, target: $target }\"\"\"\n+    case IRAtnTrans.IRAtnRule(name, target, follow) =>\n+      s\"\"\"{ kind: \"rule\", name: ${str(name)}, target: $target, follow: $follow }\"\"\"\n+\n+  private def stateJson(s: IRAtnState): String =\n+    val decision = s.decision.fold(\"null\")(_.toString)\n+    val transitions = s.transitions.map(transJson).mkString(\", \")\n+    s\"\"\"  { id: ${s.id}, rule: ${str(s.rule)}, kind: ${str(\n+        s.kind\n+      )}, decision: $decision, transitions: [$transitions] },\"\"\"\n+\n+  // `IR.atn`'s states, dense by id — indexing directly into this array is `Atn.stateAt`'s own\n+  // \"id is the index\" contract, so the runtime below never needs a lookup map.\n+  private def statesConst(atn: IRAtn): String =\n+    \"const STATES: AtnState[] = [\\n\" + atn.states\n+      .sortBy(_.id)\n+      .map(stateJson)\n+      .mkString(\"\\n\") + \"\\n];\"\n+\n+  private val types: String =\n+    Vector(\n+      \"type TransKind = \\\"eps\\\" | \\\"atom\\\" | \\\"rule\\\";\",\n+      \"type Trans =\",\n+      \"  | { kind: \\\"eps\\\"; target: number }\",\n+      \"  | { kind: \\\"atom\\\"; label: string; target: number }\",\n+      \"  | { kind: \\\"rule\\\"; name: string; target: number; follow: number };\",\n+      \"type StateKind = \\\"ruleStart\\\" | \\\"ruleStop\\\" | \\\"basic\\\" | \\\"blockStart\\\" | \\\"blockEnd\\\";\",\n+      \"type AtnState = {\",\n+      \"  id: number;\",\n+      \"  rule: string;\",\n+      \"  kind: StateKind;\",\n+      \"  decision: number | null;\",\n+      \"  transitions: Trans[];\",\n+      \"};\",\n+      \"export type Token = { terminal: string; text: string };\"\n+    ).mkString(\"\\n\")\n+\n+  // A straight port of `AtnSim.Config` (`core/src/main/scala/gramaire/AtnSim.scala`): an ATN state,\n+  // the alternative it explores, and the `RuleCall` return stack (innermost first).\n+  private val configType: String = \"type Config = { state: number; alt: number; stack: number[] };\"\n+\n+  // Mirrors `AtnSim.scala`'s own `maxDepth` exactly — a closure recursing through this many nested\n+  // `RuleCall`s without consuming input is treated as non-terminating (left recursion) and pruned.\n+  private val maxDepthConst: String = \"const MAX_DEPTH = 80;\"\n+\n+  // `AtnSim.closure`/`move`/`uniqueAlt`/`completed`/`resolve`/`predict` (SLL only — see the module\n+  // header) and `Ll.recognize`'s `parseRule`/`walk` drive loop, transliterated line-for-line.\n+  private val runtime: String =\n+    Vector(\n+      \"function stateAt(id: number): AtnState {\",\n+      \"  return STATES[id]!;\",\n+      \"}\",\n+      \"\",\n+      \"// The ε-closure of one configuration: every configuration reachable without consuming\",\n+      \"// input that sits at a terminal edge (an atom) or has returned to an empty call stack.\",\n+      \"function closure(c0: Config, seedLen: number): Config[] {\",\n+      \"  const seen = new Set<string>();\",\n+      \"  const out: Config[] = [];\",\n+      \"  function key(c: Config): string {\",\n+      \"    return c.state + \\\"|\\\" + c.stack.join(\\\",\\\");\",\n+      \"  }\",\n+      \"  function go(c: Config): void {\",\n+      \"    if (c.stack.length - seedLen > MAX_DEPTH) return;\",\n+      \"    const k = key(c);\",\n+      \"    if (seen.has(k)) return;\",\n+      \"    seen.add(k);\",\n+      \"    const st = stateAt(c.state);\",\n+      \"    if (st.transitions.length === 1 && st.transitions[0]!.kind === \\\"atom\\\") {\",\n+      \"      out.push(c);\",\n+      \"      return;\",\n+      \"    }\",\n+      \"    if (st.transitions.length === 0) {\",\n+      \"      if (c.stack.length === 0) {\",\n+      \"        out.push(c);\",\n+      \"      } else {\",\n+      \"        const [ret, ...tail] = c.stack;\",\n+      \"        go({ state: ret!, alt: c.alt, stack: tail });\",\n+      \"      }\",\n+      \"      return;\",\n+      \"    }\",\n+      \"    for (const t of st.transitions) {\",\n+      \"      if (t.kind === \\\"eps\\\") go({ state: t.target, alt: c.alt, stack: c.stack });\",\n+      \"      else if (t.kind === \\\"rule\\\")\",\n+      \"        go({ state: t.target, alt: c.alt, stack: [t.follow, ...c.stack] });\",\n+      \"      // a lone \\\"atom\\\" transition is handled by the length-1 case above; a state never\",\n+      \"      // mixes an atom edge with any other edge (Atn's own invariant).\",\n+      \"    }\",\n+      \"  }\",\n+      \"  go(c0);\",\n+      \"  return out;\",\n+      \"}\",\n+      \"\",\n+      \"function closureAll(configs: Config[], seedLen: number): Config[] {\",\n+      \"  const seen = new Set<string>();\",\n+      \"  const out: Config[] = [];\",\n+      \"  for (const c of configs) {\",\n+      \"    for (const c2 of closure(c, seedLen)) {\",\n+      \"      const k = c2.state + \\\"#\\\" + c2.alt + \\\"#\\\" + c2.stack.join(\\\",\\\");\",\n+      \"      if (!seen.has(k)) {\",\n+      \"        seen.add(k);\",\n+      \"        out.push(c2);\",\n+      \"      }\",\n+      \"    }\",\n+      \"  }\",\n+      \"  return out;\",\n+      \"}\",\n+      \"\",\n+      \"// Advance a lookahead-leaf configuration over one input terminal; a configuration with no\",\n+      \"// matching edge dies.\",\n+      \"function move(term: string, c: Config): Config[] {\",\n+      \"  const st = stateAt(c.state);\",\n+      \"  const t = st.transitions[0];\",\n+      \"  if (st.transitions.length === 1 && t !== undefined && t.kind === \\\"atom\\\" && t.label === term)\",\n+      \"    return [{ ...c, state: t.target }];\",\n+      \"  return [];\",\n+      \"}\",\n+      \"\",\n+      \"// The lowest (earliest-declared) alt among `configs` — first-alternative-wins on a genuine\",\n+      \"// tie, ALL(*)'s disambiguation convention.\",\n+      \"function uniqueAlt(configs: Config[]): number | null {\",\n+      \"  if (configs.length === 0) return null;\",\n+      \"  const first = configs[0]!.alt;\",\n+      \"  let min = first;\",\n+      \"  for (const c of configs) if (c.alt < min) min = c.alt;\",\n+      \"  return configs.every((c) => c.alt === min) ? min : null;\",\n+      \"}\",\n+      \"\",\n+      \"// A configuration whose rule has run to its end with an empty call stack.\",\n+      \"function completed(c: Config): boolean {\",\n+      \"  return stateAt(c.state).transitions.length === 0 && c.stack.length === 0;\",\n+      \"}\",\n+      \"\",\n+      \"// Prefer a completed configuration over one merely pending on more input; first-alt-wins\",\n+      \"// among whatever's left. `null` only when `configs` itself is empty.\",\n+      \"function resolve(configs: Config[]): number | null {\",\n+      \"  const completedConfigs = configs.filter(completed);\",\n+      \"  const candidates = completedConfigs.length > 0 ? completedConfigs : configs;\",\n+      \"  if (candidates.length === 0) return null;\",\n+      \"  let min = candidates[0]!.alt;\",\n+      \"  for (const c of candidates) if (c.alt < min) min = c.alt;\",\n+      \"  return min;\",\n+      \"}\",\n+      \"\",\n+      \"// Predict the alternative to take at `decision` (a blockStart) for the input from `pos0` —\",\n+      \"// SLL only (seed always []), no DFA cache, no full-LL retry: see the module header for why.\",\n+      \"function predict(decision: number, input: Token[], pos0: number): number | null {\",\n+      \"  const st = stateAt(decision);\",\n+      \"  const starts: Config[] = st.transitions.map((t, i) =>\",\n+      \"    t.kind === \\\"eps\\\"\",\n+      \"      ? { state: t.target, alt: i, stack: [] }\",\n+      \"      : { state: decision, alt: i, stack: [] },\",\n+      \"  );\",\n+      \"  let configs = closureAll(starts, 0);\",\n+      \"  let pos = pos0;\",\n+      \"  for (;;) {\",\n+      \"    const u = uniqueAlt(configs);\",\n+      \"    if (u !== null) return u;\",\n+      \"    const tok = input[pos];\",\n+      \"    if (tok === undefined) return resolve(configs);\",\n+      \"    const moved = configs.flatMap((c) => move(tok.terminal, c));\",\n+      \"    const advanced = closureAll(moved, 0);\",\n+      \"    if (advanced.length === 0) return resolve(configs);\",\n+      \"    configs = advanced;\",\n+      \"    pos += 1;\",\n+      \"  }\",\n+      \"}\",\n+      \"\",\n+      \"// Parse the rule whose RuleStart is `ruleStart`, from `pos`; return the position after it,\",\n+      \"// or null if the input does not match the predicted alternative.\",\n+      \"function parseRule(ruleStart: number, input: Token[], pos: number): number | null {\",\n+      \"  const st = stateAt(ruleStart);\",\n+      \"  const only = st.transitions[0];\",\n+      \"  if (st.transitions.length !== 1 || only === undefined || only.kind !== \\\"eps\\\") return null;\",\n+      \"  const blockStart = only.target;\",\n+      \"  const i = predict(blockStart, input, pos);\",\n+      \"  if (i === null) return null;\",\n+      \"  const alt = stateAt(blockStart).transitions[i];\",\n+      \"  if (alt === undefined || alt.kind !== \\\"eps\\\") return null;\",\n+      \"  return walk(alt.target, input, pos);\",\n+      \"}\",\n+      \"\",\n+      \"// Follow one alternative's transition chain, consuming input, until its block end.\",\n+      \"function walk(state: number, input: Token[], pos: number): number | null {\",\n+      \"  const st = stateAt(state);\",\n+      \"  if (st.kind === \\\"blockEnd\\\") return pos;\",\n+      \"  const only = st.transitions[0];\",\n+      \"  if (st.transitions.length !== 1 || only === undefined) return null;\",\n+      \"  if (only.kind === \\\"atom\\\") {\",\n+      \"    const tok = input[pos];\",\n+      \"    if (tok !== undefined && tok.terminal === only.label) return walk(only.target, input, pos + 1);\",\n+      \"    return null;\",\n+      \"  }\",\n+      \"  if (only.kind === \\\"rule\\\") {\",\n+      \"    const result = parseRule(only.target, input, pos);\",\n+      \"    if (result === null) return null;\",\n+      \"    return walk(only.follow, input, result);\",\n+      \"  }\",\n+      \"  return walk(only.target, input, pos);\",\n+      \"}\",\n+      \"\",\n+      \"// Accept `input` iff the grammar's start rule recognizes the whole stream.\",\n+      \"export function recognize(input: Token[]): boolean {\",\n+      \"  const result = parseRule(START, input, 0);\",\n+      \"  return result !== null && result === input.length;\",\n+      \"}\"\n+    ).mkString(\"\\n\")\n+\n+  /** Emit the `<Name>.atn.ts` module, or an error-commented stub if `ir.atn` is absent (shouldn't\n+    * happen via `gramaire emit`, which gates on `--strategy ll-star` before calling any backend —\n+    * defensive for a caller that constructs/invokes this backend directly).\n+    */\n+  def emit(ir: IR): String = ir.atn match\n+    case None =>\n+      banner(ir, \"ATN recognizer\") +\n+        \"\\n// ERROR: no atn on this IR — build with --strategy ll-star.\\n\" +\n+        \"export function recognize(_input: { terminal: string; text: string }[]): boolean {\\n\" +\n+        \"  throw new Error(\\\"no ATN available\\\");\\n\" +\n+        \"}\\n\"\n+    case Some(atn) =>\n+      Vector(\n+        banner(ir, \"ATN recognizer (strategy ll-star)\"),\n+        \"\",\n+        types,\n+        \"\",\n+        configType,\n+        maxDepthConst,\n+        \"\",\n+        s\"const START = ${atn.start};\",\n+        statesConst(atn),\n+        \"\",\n+        runtime\n+      ).mkString(\"\\n\")","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendAtnTs.scala","sha":"8a8b18ea98a39862901e8262750f3a7db2e9c692","status":"added"},{"additions":2,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","changes":3,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":1,"filename":"core/src/main/scala/gramaire/BackendRegistry.scala","patch":"@@ -11,7 +11,8 @@ object BackendRegistry:\n       BackendDot.backend,\n       BackendTs.backend,\n       BackendAntlr.backend,\n-      BackendJs.backend\n+      BackendJs.backend,\n+      BackendAtnTs.backend\n     )\n \n   /** Find a backend by its `--backend` name. */","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/core%2Fsrc%2Fmain%2Fscala%2Fgramaire%2FBackendRegistry.scala","sha":"6509bcb35e2d45c4e47be3e211397e7340d4e10c","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/docs%2Fall-star-port-plan.md","changes":40,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Fall-star-port-plan.md?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":3,"filename":"docs/all-star-port-plan.md","patch":"@@ -514,9 +514,43 @@ surface is reached by **conversion**, not syntax expansion (§4).\n - ✅ **Test (`Test.IRDecode`, JVM `IRGoldenSuite`):** an `ll-star` IR validates,\n   its ATN survives the JSON round trip, `lr` leaves the IR byte-identical, and\n   a predicate-effect round trip leaves every other rule byte-unchanged.\n-- ⏳ **Deferred:** a TS/interpreter backend that actually _emits_ an ATN-driven\n-  parser (a port of the `antlr4ng` predictor, or `Gramaire.Ll` shipped as the\n-  runtime) — the serialized `atn` is the substrate it will consume.\n+- ✅ **A TS/interpreter backend — v1, recognizer-only.** `gramaire emit\n+  --backend atn-ts --strategy ll-star` (`BackendAtnTs.scala`) is `IR.atn`'s\n+  first _executable_ consumer — every other reader (`BackendDot`'s own\n+  `--strategy ll-star` rendering) only ever displays it. The emitted\n+  self-contained `<Name>.atn.ts` serializes `IR.atn`'s states/transitions as\n+  TS consts (the same \"data consts + one embedded runtime\" shape\n+  `BackendJs.scala`'s `runtime`/`tracedRuntime` already use) plus a straight,\n+  SLL-only transliteration of `AtnSim.closure`/`move`/`predict` and\n+  `Ll.recognize`'s own `parseRule`/`walk` drive loop.\n+  **v1 scope, deliberately recognizer-only** (`Capability.Recognizer`, not\n+  `Capability.Cst`): reproducing `Ll.parse`'s exact `Cst` needs IR-level\n+  provenance for `LeftRec.Fold`/`PrecClimb.Tag` that doesn't exist yet —\n+  `IR.withStrategy` serializes only the already-rewritten `atn`, discarding\n+  the fold information `Ll.parse` itself needs for the same job. Also not\n+  ported: the two-stage SLL-then-full-LL retry, the DFA cache, and ambiguity\n+  reporting — a plain, uncached SLL walk is enough to prove `IR.atn` is a\n+  real, executable substrate; no _runtime_ left-recursion handling is needed\n+  either, since the ATN it reads is already the `LeftRec`-eliminated one\n+  `IR.atn` serializes server-side. Recognizer-then-CST mirrors this port's\n+  own Phase 1 → Phase 2 staging for the Scala engine itself, not a scope cut\n+  invented for this backend alone.\n+  **New test infrastructure**, since no test anywhere in this repo executed\n+  _generated_ code before this — a golden-text diff alone can't tell a\n+  correctly-ported `closure`/`move`/`predict` from one that merely still\n+  emits stable-looking text: `AtnTsParityMain.scala` (JVM-only, mirroring\n+  `gramaire.lab.LabParityMain`'s own architecture) emits the `.ts` for every\n+  conformance-corpus grammar and prints each vector's already-lexed tokens\n+  plus `Ll.recognize`'s own accept/reject; `site/scripts/check-atn-ts-\n+  parity.mjs` (mirroring `check-lab-parity.mjs`) shells it, transpiles each\n+  `.ts` with `tsc`, runs it under Node, and diffs its `recognize()` answer\n+  against what the JVM process printed — wired into CI right after the\n+  Lab's own JVM↔JS parity gate. All 62 vectors across all five corpora\n+  (`lr`/`calc`/`json`/`ECMA-404`/`calc-prec`) pass: the emitted TypeScript,\n+  actually transpiled and run, agrees with `Ll.recognize` on every one. A\n+  golden-text pin (`BackendGoldenSuite`, `test/golden/Calc.atn.ts`) exists\n+  too, same convention as every other backend, but is explicitly the lesser\n+  of the two tests for this backend.\n \n ### Phase 6 — Diagnostics, profiling, conformance ✅ done (backend + Lab surface)\n ","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/docs%2Fall-star-port-plan.md","sha":"860d8137e3726ae1ace6bf97eeb35fa78360a3c8","status":"modified"},{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/site%2Fpackage.json","changes":1,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fpackage.json?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"site/package.json","patch":"@@ -11,6 +11,7 @@\n     \"gen:lab-types\": \"node scripts/gen-lab-types.mjs\",\n     \"build:engine\": \"node scripts/build-engine.mjs\",\n     \"check:lab-parity\": \"node scripts/check-lab-parity.mjs\",\n+    \"check:atn-ts-parity\": \"node scripts/check-atn-ts-parity.mjs\",\n     \"format\": \"prettier --write \\\"src/**/*.{ts,tsx,astro,md,mdx,css}\\\" \\\"scripts/**/*.mjs\\\" \\\"tests/**/*.ts\\\" \\\"*.{json,mjs}\\\"\",\n     \"lint\": \"prettier --check \\\"src/**/*.{ts,tsx,astro,md,mdx,css}\\\" \\\"scripts/**/*.mjs\\\" \\\"tests/**/*.ts\\\" \\\"*.{json,mjs}\\\"\"\n   },","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/site%2Fpackage.json","sha":"47b7f3013ccd61516616a77f20750b4465fe78f7","status":"modified"},{"additions":82,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/site%2Fscripts%2Fcheck-atn-ts-parity.mjs","changes":82,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fscripts%2Fcheck-atn-ts-parity.mjs?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"site/scripts/check-atn-ts-parity.mjs","patch":"@@ -0,0 +1,82 @@\n+#!/usr/bin/env node\n+// The `atn-ts` backend's parity gate: proves the emitted TypeScript ATN recognizer, once\n+// transpiled and actually RUN, accepts/rejects the same conformance-corpus vectors\n+// `Ll.recognize` does — the behavioral proof a golden-text diff alone can't give (a bug in the\n+// emitted `closure`/`move`/`predict` port could still emit text that happens to golden-diff clean).\n+//\n+// Architecture mirrors check-lab-parity.mjs: shells `sbt cli/runMain\n+// gramaire.cli.AtnTsParityMain <tmpDir>` (writes one `<language>.atn.ts` per corpus grammar to\n+// `tmpDir`, and prints each vector's already-lexed tokens + Ll.recognize's own accept/reject —\n+// see AtnTsParityMain.scala's own header for why tokens travel pre-lexed), transpiles each `.ts`\n+// with `tsc` (already a `site/` devDependency), dynamically imports the compiled `.js`, and calls\n+// its own `recognize(tokens)` against the same tokens, diffing the result against what the JVM\n+// process printed.\n+//\n+// Usage: node scripts/check-atn-ts-parity.mjs\n+\n+import { execFileSync } from \"node:child_process\";\n+import { mkdtempSync, rmSync } from \"node:fs\";\n+import { tmpdir } from \"node:os\";\n+import { fileURLToPath, pathToFileURL } from \"node:url\";\n+import path from \"node:path\";\n+\n+const siteDir = path.dirname(path.dirname(fileURLToPath(import.meta.url)));\n+const repoRoot = path.join(siteDir, \"..\");\n+const tscBin = path.join(siteDir, \"node_modules\", \".bin\", \"tsc\");\n+\n+function runCorpus(outDir) {\n+  const out = execFileSync(\n+    \"sbt\",\n+    [\"-batch\", `cli/runMain gramaire.cli.AtnTsParityMain ${outDir}`],\n+    { cwd: repoRoot, encoding: \"utf8\" },\n+  );\n+  const blockRe =\n+    /===ATN-TS-PARITY-CORPUS-START===\\n([\\s\\S]*?)\\n===ATN-TS-PARITY-CORPUS-END===/g;\n+  return [...out.matchAll(blockRe)].map((m) => JSON.parse(m[1]));\n+}\n+\n+function transpile(tsFile, outDir) {\n+  execFileSync(\n+    tscBin,\n+    [tsFile, \"--outDir\", outDir, \"--target\", \"es2020\", \"--module\", \"es2020\"],\n+    {\n+      cwd: outDir,\n+    },\n+  );\n+}\n+\n+const tmpDir = mkdtempSync(path.join(tmpdir(), \"atn-ts-parity-\"));\n+let failed = false;\n+try {\n+  console.log(\"running the conformance corpus through AtnTsParityMain ...\");\n+  const corpora = runCorpus(tmpDir);\n+\n+  for (const corpus of corpora) {\n+    const { language, vectors } = corpus;\n+    const tsFile = path.join(tmpDir, `${language}.atn.ts`);\n+    console.log(`transpiling ${language}.atn.ts ...`);\n+    transpile(tsFile, tmpDir);\n+    const jsFile = path.join(tmpDir, `${language}.atn.js`);\n+    const { recognize } = await import(pathToFileURL(jsFile).href);\n+\n+    for (const v of vectors) {\n+      const got = recognize(v.tokens);\n+      if (got === v.expected) {\n+        console.log(`  ok    ${language} / ${v.name}: recognize() = ${got}`);\n+      } else {\n+        failed = true;\n+        console.error(\n+          `  FAIL  ${language} / ${v.name}: recognize() = ${got}, Ll.recognize = ${v.expected}`,\n+        );\n+      }\n+    }\n+  }\n+} finally {\n+  rmSync(tmpDir, { recursive: true, force: true });\n+}\n+\n+if (failed) {\n+  console.error(\"\\natn-ts parity gate FAILED\");\n+  process.exit(1);\n+}\n+console.log(\"\\natn-ts parity gate passed\");","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/site%2Fscripts%2Fcheck-atn-ts-parity.mjs","sha":"2c36b5c8a03f634e2ee6376cce1df7b310efe384","status":"added"},{"additions":231,"blob_url":"https://github.com/wstein/gramaire/blob/c6ad1f71807794eeec9dea813244d00fe5001f1c/test%2Fgolden%2FCalc.atn.ts","changes":231,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/test%2Fgolden%2FCalc.atn.ts?ref=c6ad1f71807794eeec9dea813244d00fe5001f1c","deletions":0,"filename":"test/golden/Calc.atn.ts","patch":"@@ -0,0 +1,231 @@\n+// GENERATED by Gramaire from the `Calc` grammar — ATN recognizer (strategy ll-star).\n+// Do not edit; regenerate with `gramaire emit --backend atn-ts --strategy ll-star`.\n+\n+type TransKind = \"eps\" | \"atom\" | \"rule\";\n+type Trans =\n+  | { kind: \"eps\"; target: number }\n+  | { kind: \"atom\"; label: string; target: number }\n+  | { kind: \"rule\"; name: string; target: number; follow: number };\n+type StateKind = \"ruleStart\" | \"ruleStop\" | \"basic\" | \"blockStart\" | \"blockEnd\";\n+type AtnState = {\n+  id: number;\n+  rule: string;\n+  kind: StateKind;\n+  decision: number | null;\n+  transitions: Trans[];\n+};\n+export type Token = { terminal: string; text: string };\n+\n+type Config = { state: number; alt: number; stack: number[] };\n+const MAX_DEPTH = 80;\n+\n+const START = 0;\n+const STATES: AtnState[] = [\n+  { id: 0, rule: \"Expr\", kind: \"ruleStart\", decision: null, transitions: [{ kind: \"eps\", target: 10 }] },\n+  { id: 1, rule: \"Expr\", kind: \"ruleStop\", decision: null, transitions: [] },\n+  { id: 2, rule: \"Expr_tail\", kind: \"ruleStart\", decision: null, transitions: [{ kind: \"eps\", target: 15 }] },\n+  { id: 3, rule: \"Expr_tail\", kind: \"ruleStop\", decision: null, transitions: [] },\n+  { id: 4, rule: \"Term\", kind: \"ruleStart\", decision: null, transitions: [{ kind: \"eps\", target: 27 }] },\n+  { id: 5, rule: \"Term\", kind: \"ruleStop\", decision: null, transitions: [] },\n+  { id: 6, rule: \"Term_tail\", kind: \"ruleStart\", decision: null, transitions: [{ kind: \"eps\", target: 32 }] },\n+  { id: 7, rule: \"Term_tail\", kind: \"ruleStop\", decision: null, transitions: [] },\n+  { id: 8, rule: \"Factor\", kind: \"ruleStart\", decision: null, transitions: [{ kind: \"eps\", target: 44 }] },\n+  { id: 9, rule: \"Factor\", kind: \"ruleStop\", decision: null, transitions: [] },\n+  { id: 10, rule: \"Expr\", kind: \"blockStart\", decision: 0, transitions: [{ kind: \"eps\", target: 12 }, { kind: \"eps\", target: 13 }] },\n+  { id: 11, rule: \"Expr\", kind: \"blockEnd\", decision: null, transitions: [{ kind: \"eps\", target: 1 }] },\n+  { id: 12, rule: \"Expr\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 11 }] },\n+  { id: 13, rule: \"Expr\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 14 }] },\n+  { id: 14, rule: \"Expr\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Expr_tail\", target: 2, follow: 11 }] },\n+  { id: 15, rule: \"Expr_tail\", kind: \"blockStart\", decision: 1, transitions: [{ kind: \"eps\", target: 17 }, { kind: \"eps\", target: 19 }, { kind: \"eps\", target: 22 }, { kind: \"eps\", target: 24 }] },\n+  { id: 16, rule: \"Expr_tail\", kind: \"blockEnd\", decision: null, transitions: [{ kind: \"eps\", target: 3 }] },\n+  { id: 17, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"+\", target: 18 }] },\n+  { id: 18, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 16 }] },\n+  { id: 19, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"+\", target: 20 }] },\n+  { id: 20, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 21 }] },\n+  { id: 21, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Expr_tail\", target: 2, follow: 16 }] },\n+  { id: 22, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"-\", target: 23 }] },\n+  { id: 23, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 16 }] },\n+  { id: 24, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"-\", target: 25 }] },\n+  { id: 25, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term\", target: 4, follow: 26 }] },\n+  { id: 26, rule: \"Expr_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Expr_tail\", target: 2, follow: 16 }] },\n+  { id: 27, rule: \"Term\", kind: \"blockStart\", decision: 2, transitions: [{ kind: \"eps\", target: 29 }, { kind: \"eps\", target: 30 }] },\n+  { id: 28, rule: \"Term\", kind: \"blockEnd\", decision: null, transitions: [{ kind: \"eps\", target: 5 }] },\n+  { id: 29, rule: \"Term\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 28 }] },\n+  { id: 30, rule: \"Term\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 31 }] },\n+  { id: 31, rule: \"Term\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term_tail\", target: 6, follow: 28 }] },\n+  { id: 32, rule: \"Term_tail\", kind: \"blockStart\", decision: 3, transitions: [{ kind: \"eps\", target: 34 }, { kind: \"eps\", target: 36 }, { kind: \"eps\", target: 39 }, { kind: \"eps\", target: 41 }] },\n+  { id: 33, rule: \"Term_tail\", kind: \"blockEnd\", decision: null, transitions: [{ kind: \"eps\", target: 7 }] },\n+  { id: 34, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"*\", target: 35 }] },\n+  { id: 35, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 33 }] },\n+  { id: 36, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"*\", target: 37 }] },\n+  { id: 37, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 38 }] },\n+  { id: 38, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term_tail\", target: 6, follow: 33 }] },\n+  { id: 39, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"/\", target: 40 }] },\n+  { id: 40, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 33 }] },\n+  { id: 41, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"/\", target: 42 }] },\n+  { id: 42, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Factor\", target: 8, follow: 43 }] },\n+  { id: 43, rule: \"Term_tail\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Term_tail\", target: 6, follow: 33 }] },\n+  { id: 44, rule: \"Factor\", kind: \"blockStart\", decision: 4, transitions: [{ kind: \"eps\", target: 46 }, { kind: \"eps\", target: 49 }] },\n+  { id: 45, rule: \"Factor\", kind: \"blockEnd\", decision: null, transitions: [{ kind: \"eps\", target: 9 }] },\n+  { id: 46, rule: \"Factor\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"(\", target: 47 }] },\n+  { id: 47, rule: \"Factor\", kind: \"basic\", decision: null, transitions: [{ kind: \"rule\", name: \"Expr\", target: 0, follow: 48 }] },\n+  { id: 48, rule: \"Factor\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \")\", target: 45 }] },\n+  { id: 49, rule: \"Factor\", kind: \"basic\", decision: null, transitions: [{ kind: \"atom\", label: \"NUMBER\", target: 45 }] },\n+];\n+\n+function stateAt(id: number): AtnState {\n+  return STATES[id]!;\n+}\n+\n+// The ε-closure of one configuration: every configuration reachable without consuming\n+// input that sits at a terminal edge (an atom) or has returned to an empty call stack.\n+function closure(c0: Config, seedLen: number): Config[] {\n+  const seen = new Set<string>();\n+  const out: Config[] = [];\n+  function key(c: Config): string {\n+    return c.state + \"|\" + c.stack.join(\",\");\n+  }\n+  function go(c: Config): void {\n+    if (c.stack.length - seedLen > MAX_DEPTH) return;\n+    const k = key(c);\n+    if (seen.has(k)) return;\n+    seen.add(k);\n+    const st = stateAt(c.state);\n+    if (st.transitions.length === 1 && st.transitions[0]!.kind === \"atom\") {\n+      out.push(c);\n+      return;\n+    }\n+    if (st.transitions.length === 0) {\n+      if (c.stack.length === 0) {\n+        out.push(c);\n+      } else {\n+        const [ret, ...tail] = c.stack;\n+        go({ state: ret!, alt: c.alt, stack: tail });\n+      }\n+      return;\n+    }\n+    for (const t of st.transitions) {\n+      if (t.kind === \"eps\") go({ state: t.target, alt: c.alt, stack: c.stack });\n+      else if (t.kind === \"rule\")\n+        go({ state: t.target, alt: c.alt, stack: [t.follow, ...c.stack] });\n+      // a lone \"atom\" transition is handled by the length-1 case above; a state never\n+      // mixes an atom edge with any other edge (Atn's own invariant).\n+    }\n+  }\n+  go(c0);\n+  return out;\n+}\n+\n+function closureAll(configs: Config[], seedLen: number): Config[] {\n+  const seen = new Set<string>();\n+  const out: Config[] = [];\n+  for (const c of configs) {\n+    for (const c2 of closure(c, seedLen)) {\n+      const k = c2.state + \"#\" + c2.alt + \"#\" + c2.stack.join(\",\");\n+      if (!seen.has(k)) {\n+        seen.add(k);\n+        out.push(c2);\n+      }\n+    }\n+  }\n+  return out;\n+}\n+\n+// Advance a lookahead-leaf configuration over one input terminal; a configuration with no\n+// matching edge dies.\n+function move(term: string, c: Config): Config[] {\n+  const st = stateAt(c.state);\n+  const t = st.transitions[0];\n+  if (st.transitions.length === 1 && t !== undefined && t.kind === \"atom\" && t.label === term)\n+    return [{ ...c, state: t.target }];\n+  return [];\n+}\n+\n+// The lowest (earliest-declared) alt among `configs` — first-alternative-wins on a genuine\n+// tie, ALL(*)'s disambiguation convention.\n+function uniqueAlt(configs: Config[]): number | null {\n+  if (configs.length === 0) return null;\n+  const first = configs[0]!.alt;\n+  let min = first;\n+  for (const c of configs) if (c.alt < min) min = c.alt;\n+  return configs.every((c) => c.alt === min) ? min : null;\n+}\n+\n+// A configuration whose rule has run to its end with an empty call stack.\n+function completed(c: Config): boolean {\n+  return stateAt(c.state).transitions.length === 0 && c.stack.length === 0;\n+}\n+\n+// Prefer a completed configuration over one merely pending on more input; first-alt-wins\n+// among whatever's left. `null` only when `configs` itself is empty.\n+function resolve(configs: Config[]): number | null {\n+  const completedConfigs = configs.filter(completed);\n+  const candidates = completedConfigs.length > 0 ? completedConfigs : configs;\n+  if (candidates.length === 0) return null;\n+  let min = candidates[0]!.alt;\n+  for (const c of candidates) if (c.alt < min) min = c.alt;\n+  return min;\n+}\n+\n+// Predict the alternative to take at `decision` (a blockStart) for the input from `pos0` —\n+// SLL only (seed always []), no DFA cache, no full-LL retry: see the module header for why.\n+function predict(decision: number, input: Token[], pos0: number): number | null {\n+  const st = stateAt(decision);\n+  const starts: Config[] = st.transitions.map((t, i) =>\n+    t.kind === \"eps\"\n+      ? { state: t.target, alt: i, stack: [] }\n+      : { state: decision, alt: i, stack: [] },\n+  );\n+  let configs = closureAll(starts, 0);\n+  let pos = pos0;\n+  for (;;) {\n+    const u = uniqueAlt(configs);\n+    if (u !== null) return u;\n+    const tok = input[pos];\n+    if (tok === undefined) return resolve(configs);\n+    const moved = configs.flatMap((c) => move(tok.terminal, c));\n+    const advanced = closureAll(moved, 0);\n+    if (advanced.length === 0) return resolve(configs);\n+    configs = advanced;\n+    pos += 1;\n+  }\n+}\n+\n+// Parse the rule whose RuleStart is `ruleStart`, from `pos`; return the position after it,\n+// or null if the input does not match the predicted alternative.\n+function parseRule(ruleStart: number, input: Token[], pos: number): number | null {\n+  const st = stateAt(ruleStart);\n+  const only = st.transitions[0];\n+  if (st.transitions.length !== 1 || only === undefined || only.kind !== \"eps\") return null;\n+  const blockStart = only.target;\n+  const i = predict(blockStart, input, pos);\n+  if (i === null) return null;\n+  const alt = stateAt(blockStart).transitions[i];\n+  if (alt === undefined || alt.kind !== \"eps\") return null;\n+  return walk(alt.target, input, pos);\n+}\n+\n+// Follow one alternative's transition chain, consuming input, until its block end.\n+function walk(state: number, input: Token[], pos: number): number | null {\n+  const st = stateAt(state);\n+  if (st.kind === \"blockEnd\") return pos;\n+  const only = st.transitions[0];\n+  if (st.transitions.length !== 1 || only === undefined) return null;\n+  if (only.kind === \"atom\") {\n+    const tok = input[pos];\n+    if (tok !== undefined && tok.terminal === only.label) return walk(only.target, input, pos + 1);\n+    return null;\n+  }\n+  if (only.kind === \"rule\") {\n+    const result = parseRule(only.target, input, pos);\n+    if (result === null) return null;\n+    return walk(only.follow, input, result);\n+  }\n+  return walk(only.target, input, pos);\n+}\n+\n+// Accept `input` iff the grammar's start rule recognizes the whole stream.\n+export function recognize(input: Token[]): boolean {\n+  const result = parseRule(START, input, 0);\n+  return result !== null && result === input.length;\n+}\n\\ No newline at end of file","raw_url":"https://github.com/wstein/gramaire/raw/c6ad1f71807794eeec9dea813244d00fe5001f1c/test%2Fgolden%2FCalc.atn.ts","sha":"913e9aa35b8682ceaba3e9cfff778f823f2d952e","status":"added"}]}