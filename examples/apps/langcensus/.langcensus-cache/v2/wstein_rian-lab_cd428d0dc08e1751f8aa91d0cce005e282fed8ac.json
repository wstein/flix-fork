{"files":[{"additions":3,"blob_url":"https://github.com/wstein/rian-lab/blob/cd428d0dc08e1751f8aa91d0cce005e282fed8ac/docs%2Fpurescript-migration.md","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fpurescript-migration.md?ref=cd428d0dc08e1751f8aa91d0cce005e282fed8ac","deletions":3,"filename":"docs/purescript-migration.md","patch":"@@ -108,7 +108,7 @@ what the Core oracle confirms over the surface form.\n \n | Module       | LOC  | Notes / FFI                                          |\n | ------------ | ---- | --------------------------------------------------- |\n-| `Rian.JS`    | 1053 | 🟡 **both print modes ported** — `compile` (runtime module) + `compileTypes` (the `.d.mts` sidecar, ADR-0086 §5). A direct JS source emitter on the typed Core (works on plain `CExpr`; node types are unused by JS, only signature types + a whole-program number/BigInt mode, ADR-0064). `compile`: functions/clauses/patterns, operators (`div`→`Math.trunc`, `in`, `<>`→`+`), `if`/`case`(→IIFE)/sum/list/tuple/struct/map/string/lambda/capture/`with`(local `desugarWith`) + `__prim_*`/stdlib calls. `compileTypes`: the JS-valid type subset → faithful TS carriers (prims/`Vec`/`Fn`/`Option`/`Result`/`Map`/tuple/`forall T`; else `unknown`). `js`/`jsdts` streams (oracle = `Rian.JS.compile`/`compile_types`). **Module `const`s + references ported** (`resolveConsts`/`constJs` + the `EConstRef` Core node, ADR-0033). **Deferred**: protocol dispatch (no `Func.dispatch`), `@external` bodies, value-union discrimination over a user type (the `PTyped` disc field is in Core; the baking pass is unported), and struct construction (no `EStruct` — `struct` in `compile` above is field access + `case` patterns, not `Name(f: v)`). |\n+| `Rian.JS`    | 1053 | ✅ **Tier-1 subset complete** — `compile` (runtime module) + `compileTypes` (the `.d.mts` sidecar, ADR-0086 §5). A direct JS source emitter on the typed Core (works on plain `CExpr`; node types are unused by JS, only signature types + a whole-program number/BigInt mode, ADR-0064). `compile`: functions/clauses/patterns, operators (`div`→`Math.trunc`, `in`, `<>`→`+`), `if`/`case`(→IIFE)/sum/list/tuple/struct/map/string/lambda/capture/`with`(local `desugarWith`) + `__prim_*`/stdlib calls. `compileTypes`: the JS-valid type subset → faithful TS carriers (prims/`Vec`/`Fn`/`Option`/`Result`/`Map`/tuple/`forall T`; else `unknown`). `js`/`jsdts` streams (oracle = `Rian.JS.compile`/`compile_types`). All Tier-1 features lower: module `const`s + references (`resolveConsts`/`EConstRef`, ADR-0033), `@external` bodies (`externalFn`/`importsJs`, ADR-0068), value-union discrimination (`bakeUnionDisc`/`PTyped` disc, ADR-0083), protocol dispatch (`protocolDispatchersJs`/`Func.dispatch`, ADR-0061), struct construction (`bakeStructs`/`EStruct`, ADR-0050), and string interpolation (the program tail, ADR-0069). |\n | `Rian.JVM`   | 1249 | Kotlin source emitter (pure).                       |\n | `Rian.Lower` | 3275 | Elixir-text + Rust source emitter (pure).           |\n | `Rian.Beam`  | 1355 | **Erlang abstract forms → `.beam`**; the core FFI.  |\n@@ -188,8 +188,8 @@ parity-gated). What's left, by category — `lib/rian/*.ex` with **no** `purs/sr\n \n | Category | Elixir modules (unported) | Status / why |\n | --- | --- | --- |\n-| **⛔ Emitters — the compile-spine gap** | `jvm`, `lower`, `beam` | The value backend. **`Rian.JS` both print modes have LANDED** (🟡 Phase 5 begun, `js` + `jsdts` streams; ADR-0090's priority emitter) — `compile` (runtime module) and `compileTypes` (the `.d.mts` sidecar, ADR-0086 §5) — so a program now *emits JS*. Remaining JS sub-features deferred on Core/Func extensions (`const` refs, protocol dispatch, `@external`, value-union patterns, struct construction — no `EStruct` in PS Core). `Lower` (Rust+Elixir) and `JVM` are display-pane emitters; `Beam` is FFI-heavy → Phase 8. |\n-| **🔧 Pipeline desugars still unported** | `interp` (252, `${}` resolution), `show_stdlib` (29) | Pure Core→Core passes the emitters need. `Interp` rewrites `${expr}` → `<>` chains *before* emit (mis-filed under Phase 8 — it is **not** FFI). `ShowStdlib` blocked on `Decl.inject_stdlib` (no consumer yet). |\n+| **⛔ Emitters — the compile-spine gap** | `jvm`, `lower`, `beam` | The value backend. **`Rian.JS` is complete for its Tier-1 subset** (both print modes + const refs, `@external`, value-union discrimination, protocol dispatch, struct construction, string interpolation — `js`/`jsdts` streams). `Lower` (Rust+Elixir) and `JVM` are display-pane emitters; `Beam` is FFI-heavy → Phase 8. |\n+| **✅ Pipeline desugars — ported** | ~~`interp`~~, ~~`show_stdlib`~~ | `Interp` (`${}` resolution, ADR-0069) and `ShowStdlib` (the portable `Show.float`) are ported and wired into `Rian.Assemble.runProgramTail` (composed by `Rian.JS.compile` before the gate); `itp`/`shs` parity streams. |\n | **⚙️ Execution & self-host (FFI-heavy, Phase 8)** | `run`, `repl`, `fixpoint`, `self_host`, `roundtrip`, `forms_equiv`, `doctest`, `test` | Wrap `:compile.forms` / code-loading / `GenServer`. Expected-late: they need the BEAM emitter + Erlang FFI. For the **JS** build these are skipped in-browser. |\n | **📐 Formatter (Phase 9)** | `format` (+ `format/{Doc,CST,CLI}`), `lsp/*` | Zero-config formatter (ADR-0045). A real feature, off the compile spine; ports after the emitters. |\n | **📦 CLI / packaging / integrations (Phase 10)** | `cli`, `build`, `manifest`, `pkg/*`, `tour`, `livebook/*`, `application` | Host shims + build toolchain. `Manifest` (`rian.toml` reader) gates the Phase 7–10 build. Becomes a thin Elixir/escript or node-CLI shell over the ported core. |","raw_url":"https://github.com/wstein/rian-lab/raw/cd428d0dc08e1751f8aa91d0cce005e282fed8ac/docs%2Fpurescript-migration.md","sha":"730699b551f33d7a2bebbb11c1472ad80f0df408","status":"modified"}]}