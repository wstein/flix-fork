{"files":[{"additions":51,"blob_url":"https://github.com/wstein/bin/blob/c359c6a06896052e4498f1e744ed5de2fe97dcfd/list-vscode-extensions","changes":64,"contents_url":"https://api.github.com/repos/wstein/bin/contents/list-vscode-extensions?ref=c359c6a06896052e4498f1e744ed5de2fe97dcfd","deletions":13,"filename":"list-vscode-extensions","patch":"@@ -23,6 +23,7 @@ OptionParser.new do |opts|\n   opts.on('--workers=N', Integer, 'Number of parallel workers (default: 10)') { |n| options[:workers] = n }\n   opts.on('-o', '--output=FILE', 'Write output to FILE instead of stdout') { |file| options[:output] = file }\n   opts.on('-i', '--input=FILE', 'Read extension list from FILE') { |file| options[:input] = file }\n+  opts.on('--id-with-version', 'Include version in extension id in output') { options[:id_with_version] = true }\n end.parse!\n \n # Helper to read extension list from stdin, file, or VS Code CLI\n@@ -136,6 +137,15 @@ def format_last_updated(details)\n   last_updated ? Time.parse(last_updated).strftime('%Y-%m-%d') : 'unknown'\n end\n \n+def id_with_version(details, ext_versions, options)\n+  if options[:id_with_version]\n+    version = ext_versions[details['id']] || details['versions']&.first&.dig('version') || 'unknown'\n+    \"#{details['id']}@#{version}\"\n+  else\n+    details['id']\n+  end\n+end\n+\n # Parallel fetching with specified number of workers\n queue = Queue.new\n ext_ids.each_with_index { |ext, idx| queue << [idx, ext] }\n@@ -196,40 +206,68 @@ def output_close(io, options)\n end\n \n if options[:format] == 'recommend'\n-  output_puts(output_io, '{')\n-  output_puts(output_io, '  \"recommendations\": [')\n-  valid_results.each_with_index do |details, idx|\n+  valid_results.each do |details|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    output_puts(output_io, \"\\n    \\\"#{details['id']}\\\", // #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\")\n-    output_puts(output_io, \"    // #{details['shortDescription']}\")\n-    output_puts(output_io, \"    // Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings) on #{Time.now.strftime(\"%Y-%m-%d\")}\")\n-    output_puts(output_io, \"    // #{details['marketplace']}\")\n+    input_version = ext_versions[details['id']]\n+    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n+    id_with_ver = if options[:id_with_version]\n+      ver = input_version || fetched_version\n+      \"#{details['id']}@#{ver}\"\n+    else\n+      details['id']\n+    end\n+    flags = details['flags'] ? details['flags'].split(',').map(&:strip).map { |f| \"*#{f}*\" }.join(' ') : ''\n+    output_puts(output_io, \"    \\\"#{id_with_ver}\\\",  // #{flags}\\n    // #{details['shortDescription']}\\n    // Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings) on #{Time.now.strftime('%Y-%m-%d')}\\n    // #{details['marketplace']}\\n\")\n   end\n-  output_puts(output_io, '  ]')\n-  output_puts(output_io, '}')\n elsif options[:format] == 'markdown'\n   output_puts(output_io, \"# VS Code Extensions\\n\")\n   valid_results.each do |details|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    output_puts(output_io, \"- [#{details['id']}](#{details['marketplace']}) #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\\n  > #{details['shortDescription']}\\n  > Last Updated: #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n\")\n+    input_version = ext_versions[details['id']]\n+    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n+    id_with_ver = if options[:id_with_version]\n+      ver = input_version || fetched_version\n+      \"#{details['id']}@#{ver}\"\n+    else\n+      details['id']\n+    end\n+    flags = details['flags'] ? \" *#{details['flags'].gsub(/,\\s*/, '* *')}*\" : ''\n+    output_puts(output_io, \"- [#{id_with_ver}](#{details['marketplace']})#{flags}\\n  > #{details['shortDescription']}\\n  > Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n\")\n   end\n elsif options[:format] == 'json'\n-  output_puts(output_io, JSON.pretty_generate(valid_results))\n+  output_puts(output_io, JSON.pretty_generate(valid_results.map { |details|\n+    d = details.dup\n+    d['id'] = id_with_version(details, ext_versions, options)\n+    d\n+  }))\n elsif options[:format] == 'yaml'\n-  output_puts(output_io, valid_results.to_yaml)\n+  output_puts(output_io, valid_results.map { |details|\n+    d = details.dup\n+    d['id'] = id_with_version(details, ext_versions, options)\n+    d\n+  }.to_yaml)\n else\n   valid_results.each do |details|\n     last_updated_date = format_last_updated(details)\n     installs = extract_stat(details, :install).to_i\n     rating = extract_stat(details, :rating)&.round(1)\n     rating_count = extract_stat(details, :rating_count).to_i\n-    output_puts(output_io, \"#{details['id']} #{details['versions']&.first&.dig('version') || ext_versions[details['id']] || 'unknown'} [#{details['flags']}]\\n  Description: #{details['shortDescription']}\\n  Last Updated: #{last_updated_date} Installs: #{installs},  Rating: #{rating} (#{rating_count} ratings)\\n  Marketplace: #{details['marketplace']}\\n\")\n+    input_version = ext_versions[details['id']]\n+    fetched_version = details['versions']&.first&.dig('version') || 'unknown'\n+    id_with_ver = if options[:id_with_version]\n+      ver = input_version || fetched_version\n+      \"#{details['id']}@#{ver}\"\n+    else\n+      details['id']\n+    end\n+    flags = details['flags'] ? \" *#{details['flags'].gsub(/,\\s*/, '* *')}*\" : ''\n+    output_puts(output_io, \"- #{id_with_ver}#{flags}\\n  > #{details['shortDescription']}\\n  > Latest #{fetched_version} - #{last_updated_date}, Installs: #{installs}, Rating: #{rating} (#{rating_count} ratings)\\n  Marketplace: #{details['marketplace']}\\n\")\n   end\n end\n output_close(output_io, options)","raw_url":"https://github.com/wstein/bin/raw/c359c6a06896052e4498f1e744ed5de2fe97dcfd/list-vscode-extensions","sha":"25a1be1b85b69c75ea5bb36be4865f66f9dcf0c6","status":"modified"}]}