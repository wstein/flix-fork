{"files":[{"additions":1,"blob_url":"https://github.com/wstein/gramaire/blob/423c4d89490d706b8e85b3832f14c7bd84223046/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":2,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=423c4d89490d706b8e85b3832f14c7bd84223046","deletions":1,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -21,7 +21,7 @@ compiler emits. Keywords MUST / SHOULD / MAY per RFC 2119.\n \n A Grammark grammar is hosted in a GitHub-Flavored Markdown document. The compiler\n ignores prose, list markers, and images; it extracts the grammar by scanning ATX\n-headings and fenced code blocks whose info string begins with `lr`.\n+headings and fenced code blocks whose info string begins with `grammark`.\n \n - **Grammar name.** The single `# H1`. There MUST be exactly one.\n - **Nonterminal rules.** Every non-reserved `## H2` names a nonterminal; the","raw_url":"https://github.com/wstein/gramaire/raw/423c4d89490d706b8e85b3832f14c7bd84223046/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"af785d4f4f32e1d8a74405a564b209165ac0d15f","status":"modified"},{"additions":56,"blob_url":"https://github.com/wstein/gramaire/blob/423c4d89490d706b8e85b3832f14c7bd84223046/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","changes":91,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx?ref=423c4d89490d706b8e85b3832f14c7bd84223046","deletions":35,"filename":"site/src/content/docs/tutorials/intro.mdx","patch":"@@ -17,16 +17,16 @@ railroad diagrams, FIRST/FOLLOW tables — and is **at the same time** the exact\n input the parser generator reads. Your grammar _is_ your documentation.\n \n This tutorial builds a small calculator grammar from scratch, then parses input\n-against it. The `grammark` CLI runs the real PureScript core; the\n-[Lab](/grammark/lab) is an in-browser **preview** — handy for a quick look, but\n-limited (see the note below).\n-\n-<Aside type=\"caution\" title=\"What the Lab can preview\">\n-  The browser Lab lexes input from a grammar's **literal terminals** (backtick\n-  spellings) and recognizes it client-side. It cannot evaluate **token classes**\n-  (ALL-CAPS names like `NUMBER`), which need a per-language lexer — for those,\n-  use the `grammark` CLI. Wiring the compiled core into the browser is on the\n-  [Lab roadmap](../../../../../docs/playground-spec.md).\n+against it. The `grammark` CLI and the [Lab](/grammark/lab) run the **same**\n+PureScript core — the Lab is that core compiled to WebAssembly-class browser ESM\n+— so a verdict in the browser matches the CLI.\n+\n+<Aside type=\"tip\" title=\"The Lab runs the real engine\">\n+  The browser Lab is not a stub: it bundles the compiled Grammark core and lexes\n+  input from a grammar's literal terminals **and** its token classes (ALL-CAPS\n+  names like `NUMBER`, declared as `NUMBER : /[0-9]+/`), then parses with the LR\n+  tables — entirely client-side, with no server round-trip. Your grammar never\n+  leaves the page.\n </Aside>\n \n ## 1. The shape of a `.gram.md` file\n@@ -37,8 +37,8 @@ A grammar file is a canonical Markdown document with a fixed structure:\n \n 1. **One `# H1`** — the grammar's name.\n \n-2. **One `## H2` per nonterminal** — the heading names the rule; an `lr` fenced\n-   block under it holds its productions.\n+2. **One `## H2` per nonterminal** — the heading names the rule; a `grammark`\n+   fenced block under it holds its productions.\n \n 3. **Reserved `## H2` sections** — `Precedence`, `Error messages`, and\n    `Generated tables`, in that order, when present.\n@@ -49,7 +49,7 @@ A grammar file is a canonical Markdown document with a fixed structure:\n </Steps>\n \n One file is exactly one grammar: one start symbol, one namespace. Productions\n-live in fenced blocks whose info string is `lr`; everything outside them is\n+live in fenced blocks whose info string is `grammark`; everything outside them is\n documentation that travels with the grammar.\n \n ## 2. Your first rule\n@@ -67,19 +67,20 @@ An expression is a sum or difference of terms.\n \n ```grammark\n Expr\n-  : Expr `+` Term\n-  | Expr `-` Term\n+  : Expr '+' Term\n+  | Expr '-' Term\n   | Term\n ```\n ````\n \n-Reading the `lr` block:\n+Reading the `grammark` block:\n \n - `Expr` on its own line names the nonterminal; the `:` on the next line begins\n   its alternatives.\n - `|` separates alternatives — and it is the **only** separator. A line break\n   inside an alternative is insignificant, so a long alternative may wrap freely.\n-- Backtick spellings like `` `+` `` are **literal terminals**.\n+- Quoted spellings like `'+'` (single or double quotes) are **literal\n+  terminals** — the exact text to match.\n - A **mixed-case** name (`Expr`, `Term`) is a nonterminal; an **ALL-CAPS** name\n   (`NUMBER`) is a lexer token class. A mixed-case name you never define is a\n   missing rule — Grammark rejects it by name rather than treating it as a phantom\n@@ -96,15 +97,31 @@ Add the remaining rules — `Term`, then `Factor` for numbers and parentheses:\n \n ```grammark\n Term\n-  : Term `*` Factor\n-  | Term `/` Factor\n+  : Term '*' Factor\n+  | Term '/' Factor\n   | Factor\n \n Factor\n-  : `(` Expr `)`\n+  : '(' Expr ')'\n   | NUMBER\n ```\n \n+`Factor` mentions the `NUMBER` token class but nothing has said what a `NUMBER`\n+_is_ yet. Declare token classes in a reserved `## Tokens` section, each an\n+ALL-CAPS name bound to a `/regex/`; mark throwaway classes (whitespace) `%skip`:\n+\n+````md\n+## Tokens\n+\n+```grammark tokens\n+NUMBER : /[0-9]+/\n+WS     : /[ \\t\\r\\n]+/   %skip\n+```\n+````\n+\n+Now the grammar is complete — the Lab can lex `12 + 3` and the CLI can emit a\n+parser.\n+\n ## 3. Surface sugar\n \n Writing pure, left-recursive, epsilon-free productions by hand is wordy.\n@@ -119,7 +136,7 @@ Grammark gives you sugar that compiles down to that clean core automatically\n   </Card>\n   <Card title=\"Separated lists\">\n     `Comma<X>` and `Sep<X, S>` expand to one-or-more lists separated by `,` or by\n-    any symbol `S` — e.g. `` `{` Comma<Member> `}` ``.\n+    any symbol `S` — e.g. `'{' Comma<Member> '}'`.\n   </Card>\n   <Card title=\"Named fields\">\n     `left:Expr` names a right-hand position. The name rides onto the IR\n@@ -136,7 +153,7 @@ For example, a statement block is just:\n \n ```grammark\n Block\n-  : `{` Sep<Stmt, `;`> `}`\n+  : '{' Sep<Stmt, ';'> '}'\n ```\n \n ## 4. Attaching actions\n@@ -147,21 +164,21 @@ core's own language):\n \n ```grammark\n Expr\n-  : Expr `+` Term   {% \\l _ r -> Add l r %}\n-  | Expr `-` Term   {% \\l _ r -> Sub l r %}\n+  : Expr '+' Term   {% \\l _ r -> Add l r %}\n+  | Expr '-' Term   {% \\l _ r -> Sub l r %}\n   | Term            {% \\t -> t %}\n ```\n \n Actions are **positional**: one parameter per right-hand symbol, left to right.\n-In `Expr `+` Term` the three parameters are the left value, the `+` token (which\n+In `Expr '+' Term` the three parameters are the left value, the `+` token (which\n we discard with `_`), and the right value.\n \n If you name your fields, you can skip the lambda entirely — a **bare body** binds\n the field names for you:\n \n ```grammark\n Expr\n-  : left:Expr `+` right:Term   {% Add left right %}\n+  : left:Expr '+' right:Term   {% Add left right %}\n ```\n \n Grammark rewrites that to `\\left _ right -> Add left right`, so the action reads\n@@ -184,8 +201,8 @@ declarations bind more loosely:\n ## Precedence\n \n ```grammark precedence\n-%left `+` `-`\n-%left `*` `/`\n+%left '+' '-'\n+%left '*' '/'\n ```\n ````\n \n@@ -194,9 +211,12 @@ names the competing rules — \"shift `+` vs reduce `Expr -> Expr + Expr`\" — an\n can tell you _what kind_ of conflict it is:\n \n - an **LALR artifact** — canonical LR(1) or IELR(1) resolves it, so just build\n-  with a stronger method; or\n-- a **genuine** conflict — the grammar really is ambiguous or not LR(1), and\n-  needs a refactor, a precedence rule, or the GLR engine.\n+  with a stronger method;\n+- **resolved by declaration** — your `%left`/`%right` lines already clear it, so\n+  the grammar compiles as written; or\n+- a **genuine** conflict — the grammar really is ambiguous or not LR(1) _even\n+  with_ the declared precedence, and needs a refactor, another precedence rule,\n+  or the GLR engine.\n \n ```sh\n grammark explain-conflict calc.gram.md\n@@ -221,10 +241,11 @@ Two feedback loops:\n    grammark emit --backend ir   calc.gram.md   # the grammark-ir JSON\n    ```\n \n-2. **In the browser (preview).** The [Lab](/grammark/lab) gives an instant\n-   accept/reject for grammars whose terminals are literals. This calculator uses\n-   the `NUMBER` token class, so evaluate it with the CLI above; for a Lab demo,\n-   try a literal-only grammar like balanced brackets (the Lab's default).\n+2. **In the browser (preview).** Paste the same grammar into the\n+   [Lab](/grammark/lab) for an instant accept/reject. It runs the real engine, so\n+   the `NUMBER` token class lexes there too — type `12 + 3 * 4` and watch the\n+   verdict update live as you edit. (This calculator is the Lab's default\n+   grammar.)\n \n </Steps>\n ","raw_url":"https://github.com/wstein/gramaire/raw/423c4d89490d706b8e85b3832f14c7bd84223046/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","sha":"f3087a4bcbbab1424c14eb90059b0768833bfd1a","status":"modified"}]}