{"files":[{"additions":32,"blob_url":"https://github.com/wstein/flatbars/blob/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx","changes":34,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx?ref=82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd","deletions":2,"filename":"tutorials/src/components/OpenInLab.jsx","patch":"@@ -13,7 +13,7 @@ import { labHref, dataText } from \"../../../lab/open-in-lab.mjs\";\n import { load as loadYaml } from \"../../../lab/vendor/js-yaml.mjs\";\n import { createFlatBarsRenderer } from \"../../../lab/flatbars.mjs\";\n import { createMinBarsRenderer } from \"../../../lab/minbars.mjs\";\n-import { renderWith, renderRawWith, renderMaxWith, safe } from \"../../../lab/vendor/flatbars-engine.mjs\";\n+import { renderWith, renderRawWith, renderMaxWith, safe, lint as runLint } from \"../../../lab/vendor/flatbars-engine.mjs\";\n import { buildHelpers } from \"../../../lab/helpers.mjs\";\n import { buildI18nHelpers } from \"../../../lab/i18n.mjs\";\n import jsonata from \"../../../lab/vendor/jsonata.mjs\";\n@@ -74,7 +74,7 @@ function CodeEditor({ lang, value, onInput, dialect = \"fullbars\", label }) {\n   );\n }\n \n-export default function OpenInLab({ engine, template, data = {}, partials = {}, helpers = \"\", transform = \"\", catalog = \"\", locale = \"en\", labUrl = LAB_URL, compile = false }) {\n+export default function OpenInLab({ engine, template, data = {}, partials = {}, helpers = \"\", transform = \"\", catalog = \"\", locale = \"en\", labUrl = LAB_URL, compile = false, lint = false }) {\n   const initialData = dataText(data); // object → YAML; string → verbatim\n   const [tpl, setTpl] = useState(template);\n   const [dataStr, setDataStr] = useState(initialData);\n@@ -87,6 +87,7 @@ export default function OpenInLab({ engine, template, data = {}, partials = {},\n   const [renderer, setRenderer] = useState(null);\n   const [out, setOut] = useState({ ok: true, text: null }); // text === null ⇒ \"rendering…\"\n   const [js, setJs] = useState(null); // compiled-JS pane (opt-in via `compile`)\n+  const [lintRes, setLintRes] = useState(null); // live lint pane (opt-in via `lint`)\n   const [href, setHref] = useState(null);\n   // Only the compiling dialects (RawBars/FullBars/MaxBars) expose compileToJs;\n   // MinBars doesn't, so the pane is gated on the method actually existing.\n@@ -154,6 +155,21 @@ export default function OpenInLab({ engine, template, data = {}, partials = {},\n     setJs(r && r.ok ? { ok: true, text: r.value } : { ok: false, text: (r && r.error) || \"compile failed\" });\n   }, [canCompile, renderer, tpl]);\n \n+  // Optional: run `flatbars lint` live as the template is edited, so the canonical-\n+  // ization findings track every keystroke (the same `lint` the CLI/CI gate uses).\n+  // Pure analysis — no render needed, so it doesn't wait on the engine adapter.\n+  useEffect(() => {\n+    if (!lint) return;\n+    try {\n+      const r = runLint(tpl, engine);\n+      setLintRes(r && r.ok\n+        ? { ok: true, report: r.report, findings: r.findings || [] }\n+        : { ok: false, report: (r && r.error) || \"lint failed\", findings: [] });\n+    } catch (e) {\n+      setLintRes({ ok: false, report: String((e && e.message) || e), findings: [] });\n+    }\n+  }, [lint, tpl, engine]);\n+\n   // Rebuild the Open-in-Lab deep link from the (possibly edited) workspace. The\n   // i18n catalog + locale round-trip into the Lab's LOCALIZATION/catalog.yaml and\n   // config.yaml views (ADR-029).\n@@ -263,6 +279,20 @@ export default function OpenInLab({ engine, template, data = {}, partials = {},\n             : <pre class={\"oil-out-pre\" + (js.ok ? \"\" : \" err\")}><code>{js.text}</code></pre>}\n         </div>\n       )}\n+\n+      {lint && (\n+        <div class=\"oil-out oil-lint\">\n+          <div class=\"oil-cell-head oil-out-head\">\n+            <span class=\"oil-cap\">flatbars lint</span>\n+            <span class=\"oil-js-note\">— live canonicalization findings</span>\n+          </div>\n+          {lintRes == null\n+            ? <pre class=\"oil-out-pre\"><em>linting…</em></pre>\n+            : <pre class={\"oil-out-pre\" + (lintRes.ok ? \"\" : \" err\")}>\n+                <code class={lintRes.findings.length ? \"oil-lint-warn\" : \"oil-lint-ok\"}>{lintRes.report}</code>\n+              </pre>}\n+        </div>\n+      )}\n     </figure>\n   );\n }","raw_url":"https://github.com/wstein/flatbars/raw/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fcomponents%2FOpenInLab.jsx","sha":"8c175fb208c6b1e7195c2da8389447ee8c4b2dc2","status":"modified"},{"additions":25,"blob_url":"https://github.com/wstein/flatbars/blob/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Flint.mjs","changes":29,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Flint.mjs?ref=82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd","deletions":4,"filename":"tutorials/src/lint.mjs","patch":"@@ -12,24 +12,33 @@\n // `flatbars lint` findings: deprecated aliases and non-canonical scoped variables.\n // Both are warn-only (never block rendering) — a host/CI hygiene check. `report` is\n // the exact CLI output (the `warning: …` line, or the clean no-findings line).\n+//\n+// `data` (optional) feeds the page's interactive example card (../components/\n+// OpenInLab.jsx): the template renders LIVE through the engine bundle next to the\n+// lint warning, so the page shows BOTH that the template renders AND what the\n+// linter says about it. The `clean` example has no `data` — it is a pure CLI/exit-\n+// code demo (terminal block on the page), not a render.\n export const lintExamples = {\n   // A deprecated alias: `plus` renders identically to `add`, but `add` is canonical.\n-  // Flagged in every dialect.\n+  // Flagged in every dialect. Renders fine (2 + 3 = 5) — the warning is hygiene only.\n   alias: {\n     engine: \"maxbars\",\n     template: \"{{ plus a b }}\",\n+    data: { a: 2, b: 3 },\n     report: \"warning: `plus` is a deprecated alias of `add` — prefer `add` (the lift/migrate assist rewrites it)\",\n   },\n   // A non-canonical scoped variable: the loop index is `index0` in RawBars/MaxBars\n   // (bare `index` is the legacy spelling). Flagged in RawBars/MaxBars only — FullBars\n-  // keeps Handlebars' `@index`, so it is canonical there.\n+  // keeps Handlebars' `@index`, so it is canonical there. (Bare `index` does not\n+  // resolve to the position in the card — exactly why the linter steers you off it.)\n   scopedVariable: {\n     engine: \"maxbars\",\n     template: \"{{#each items}}{{index}}. {{this}}{{/each}}\",\n+    data: { items: [\"alpha\", \"beta\", \"gamma\"] },\n     report: \"warning: `index` is the non-canonical scoped variable — prefer `index0` (the native RawBars/MaxBars spelling)\",\n   },\n   // A clean template reports the CLI's no-findings line (exit 0 under any\n-  // `--max-warnings`).\n+  // `--max-warnings`). No `data`: this is the terminal/exit-code demo, not a render.\n   clean: {\n     engine: \"rawbars\",\n     template: \"{{{ name }}}\",\n@@ -40,22 +49,34 @@ export const lintExamples = {\n // `migrate` rewrites Handlebars source to MaxBars source (minimal-diff: untouched\n // tags keep their exact spacing). `source` is the exact migrated output; an\n // unrewritable construct carries a `residualKind` instead of a clean rewrite.\n+//\n+// `data` (and, for the yield example, `cardTemplate` — a self-contained scenario\n+// the bare `{{yield}}` fragment can't show standalone) feed the page's interactive\n+// card: it renders the MIGRATED MaxBars result LIVE, proving the rewrite produced a\n+// working template. The card runs `cardTemplate ?? source` under MaxBars. The\n+// `ambiguousSection` residual has no `data` — it is the can't-migrate case (left\n+// verbatim), shown as source-only, not rendered.\n export const migrateExamples = {\n   // `@`-data loop variables migrate to the MaxBars `loop` object (ADR-021).\n   loopVars: {\n     template: \"{{#each items}}{{@index}}. {{this}}\\n{{/each}}\",\n     source: \"{{#each items}}{{loop.index0}}. {{this}}\\n{{/each}}\",\n+    data: { items: [\"alpha\", \"beta\", \"gamma\"] },\n   },\n   // An inverted section `{{^x}}` becomes `{{#unless x}}` (its close pairs too).\n   inverted: {\n     template: \"{{^items}}nothing here{{/items}}\",\n     source: \"{{#unless items}}nothing here{{/unless}}\",\n+    data: { items: [] },\n   },\n   // The block-partial reference `{{> @partial-block}}` becomes `{{yield}}` (a direct\n-  // yield, not a partial named `@partial-block`).\n+  // yield, not a partial named `@partial-block`). The bare fragment can't render on\n+  // its own, so the card runs `{{yield}}` in a minimal inline-partial host.\n   partialBlock: {\n     template: \"{{> @partial-block}}\",\n     source: \"{{yield}}\",\n+    cardTemplate: '{{#inline \"card\"}}<section>{{yield}}</section>{{/inline}}{{#partial \"card\"}}Hello, {{name}}!{{/partial}}',\n+    data: { name: \"Ada\" },\n   },\n   // What migration CANNOT do: a bare Mustache section `{{#name}}` whose name is not a\n   // known block helper is ambiguous (`{{#if}}` guard vs `{{#each}}` iteration?) — left","raw_url":"https://github.com/wstein/flatbars/raw/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Flint.mjs","sha":"cc8c8f99776e772dd78e55e6cc76e6e116de3431","status":"modified"},{"additions":48,"blob_url":"https://github.com/wstein/flatbars/blob/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fpages%2Flint.astro","changes":105,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fpages%2Flint.astro?ref=82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd","deletions":57,"filename":"tutorials/src/pages/lint.astro","patch":"@@ -4,34 +4,19 @@\n // \"Linting & migration\" — the guide to keeping templates canonical and importing\n // Handlebars. Two user-facing tools: `flatbars lint` (warn-only canonicalization,\n // for editors and CI) and `flatbars migrate` (Handlebars → MaxBars source, with a\n-// residual report). Unlike the dialect references this is a CLI/CI guide, not an\n-// interactive playground — the linter has no Lab view, so the before/after here is\n-// the exact tool output, gate-validated against the engine (../lint.mjs, gated by\n-// scripts/check-tutorial-tooling.mjs). The normative text lives in docs/\n-// (adr-0019-operation-vocabulary, adr-0021-maxbars-variable-model).\n+// residual report). The examples are interactive cards (../components/OpenInLab.jsx,\n+// the same island the dialect references use): the template renders LIVE through the\n+// engine bundle, with the tool output (the lint warning, the migrated source) shown\n+// alongside — so the page proves BOTH that a template renders AND what the tool says\n+// about it. Every example, warning, and rewrite is gate-validated against the engine\n+// (../lint.mjs, gated by scripts/check-tutorial-tooling.mjs). The normative text lives\n+// in docs/ (adr-0019-operation-vocabulary, adr-0021-maxbars-variable-model).\n import Reference from \"../layouts/Reference.astro\";\n-import { lintExamples, migrateExamples, lintLabInput, migrateLabInput } from \"../lint.mjs\";\n-import { labHref } from \"../../../lab/open-in-lab.mjs\";\n+import OpenInLab from \"../components/OpenInLab.jsx\";\n+import { lintExamples, migrateExamples } from \"../lint.mjs\";\n \n const t = (s) => s; // readability helper for inline tag literals\n \n-// Open-in-Lab parity with the dialect pages: each example carries into the Lab —\n-// lint examples land with the Lint dock panel open, migrate examples in the\n-// Migrated MaxBars output view. The deep-links are built at build time from the\n-// same per-kind target the CI gate validates (lint.mjs). Same env var the\n-// OpenInLab island uses, so dev and the GitHub-Pages sub-path both resolve.\n-const LAB_URL = import.meta.env.PUBLIC_LAB_URL || \"/lab/index.html\";\n-const lintHref = {};\n-for (const [k, ex] of Object.entries(lintExamples)) {\n-  const { engine, example } = lintLabInput(ex);\n-  lintHref[k] = await labHref(engine, example, { labUrl: LAB_URL });\n-}\n-const migrateHref = {};\n-for (const [k, ex] of Object.entries(migrateExamples)) {\n-  const { engine, example } = migrateLabInput(ex);\n-  migrateHref[k] = await labHref(engine, example, { labUrl: LAB_URL });\n-}\n-\n const groups = [\n   {\n     label: \"Linting & migration\",\n@@ -73,19 +58,25 @@ const groups = [\n       color: var(--fg-faint); }\n     .io pre { margin: 0.15rem 0 0.6rem; }\n     .io .arr { color: var(--accent-2); font: 600 12px var(--font-mono); margin: 0.1rem 0; }\n-    pre.cli { background: var(--bg-0, #11131a); border: 1px solid var(--border); border-radius: 8px;\n+    /* The cli blocks are a fixed dark \"terminal\" pane — same in light and dark\n+       mode, so they use the design system's --out-bg/--out-fg screen tokens, NOT\n+       theme-following foregrounds. (The previous --bg-0 was undefined → a black\n+       fallback, while the text stayed theme-following: in light mode that was\n+       dark violet --fg-secondary/--accent-2 on near-black — invisible.) */\n+    pre.cli { background: var(--out-bg); border: 1px solid var(--border-strong); border-radius: 8px;\n       padding: 0.7rem 0.9rem; overflow-x: auto; }\n-    pre.cli code { color: var(--fg-secondary); font: 13px/1.5 var(--font-mono); }\n-    .warn { color: var(--accent-2); }\n+    pre.cli code { color: var(--out-fg); font: 13px/1.5 var(--font-mono); }\n+    /* warn text must be a FIXED light amber: on the always-dark pane a theme\n+       token would go dark (and vanish) in light mode. */\n+    .warn { color: #fbbf24; }\n     .resid { margin: 0.9rem 0 1.4rem; padding: 0.7rem 0.9rem; border-left: 3px solid var(--accent);\n       background: var(--accent-softer); border-radius: 0 6px 6px 0; font-size: 0.92rem; }\n     .resid b { color: var(--accent-2); }\n     .resid .sug { display: block; margin-top: 0.4rem; color: var(--fg-secondary); }\n-    .oil-link { display: inline-flex; align-items: center; gap: 0.3rem; margin: 0.1rem 0 0.4rem;\n-      font: 600 12px var(--font-mono); color: var(--accent-2); text-decoration: none;\n-      border: 1px solid var(--border-strong); border-radius: 6px; padding: 0.25rem 0.6rem;\n-      background: var(--bg-1); }\n-    .oil-link:hover { border-color: var(--accent); color: var(--accent); }\n+    /* Caption tying the tool-output block / live card to the example above it. */\n+    .tool-cap { font: 600 11px/1 var(--font-mono); letter-spacing: 0.04em; text-transform: uppercase;\n+      color: var(--fg-faint); margin: 1.1rem 0 0.35rem; }\n+    .tool-cap .ico { color: var(--accent-2); margin-right: 0.35rem; }\n   </style>\n \n   <!-- ─────────────────────────── Intro ─────────────────────────── -->\n@@ -111,15 +102,11 @@ const groups = [\n     <p>\n       An alias is a second name the prelude still installs but no longer prefers. The lint\n       points at the canonical name; the <a href=\"#migrate\">migrate/lift</a> assist can rewrite\n-      it for you.\n+      it for you. The template still renders fine — edit it live below (<code>plus</code> is\n+      just <code>add</code>), and the <b>flatbars lint</b> pane under the output updates with\n+      every keystroke (swap <code>plus</code> → <code>add</code> and the warning clears):\n     </p>\n-    <div class=\"io\">\n-      <span class=\"lbl\">template</span>\n-      <pre class=\"cli\"><code>{lintExamples.alias.template}</code></pre>\n-      <span class=\"lbl\">flatbars lint</span>\n-      <pre class=\"cli\"><code class=\"warn\">{lintExamples.alias.report}</code></pre>\n-      <a class=\"oil-link\" href={lintHref.alias}>Open in Lab ↗</a>\n-    </div>\n+    <OpenInLab client:visible engine={lintExamples.alias.engine} template={lintExamples.alias.template} data={lintExamples.alias.data} lint />\n   </section>\n \n   <!-- ───────────────────────── Scoped vars ───────────────────────── -->\n@@ -129,15 +116,11 @@ const groups = [\n       Inside a loop, the canonical names in RawBars/MaxBars are <code>index0</code>,\n       <code>rindex0</code>, <code>first</code>, <code>last</code>, <code>key</code>; the bare\n       <code>index</code>/<code>rindex</code>/<code>size</code> are legacy spellings. The lint\n-      steers you to the canonical one.\n+      steers you to the canonical one — see the live <b>flatbars lint</b> pane below the\n+      output. Notice too that the bare <code>index</code> renders <em>blank</em> — it never\n+      resolved to the position — which is exactly why it is non-canonical:\n     </p>\n-    <div class=\"io\">\n-      <span class=\"lbl\">template</span>\n-      <pre class=\"cli\"><code>{lintExamples.scopedVariable.template}</code></pre>\n-      <span class=\"lbl\">flatbars lint --maxbars</span>\n-      <pre class=\"cli\"><code class=\"warn\">{lintExamples.scopedVariable.report}</code></pre>\n-      <a class=\"oil-link\" href={lintHref.scopedVariable}>Open in Lab ↗</a>\n-    </div>\n+    <OpenInLab client:visible engine={lintExamples.scopedVariable.engine} template={lintExamples.scopedVariable.template} data={lintExamples.scopedVariable.data} lint />\n     <p>\n       This lint is <b>surface-scoped</b>: it runs for RawBars/MaxBars, where the bare native\n       name is canonical, but <em>not</em> for FullBars — there <code>{t(\"{{@index}}\")}</code>\n@@ -163,7 +146,6 @@ const groups = [\n {lintExamples.clean.report}\n \n $ flatbars lint src/email.maxbars --maxbars --max-warnings 0   # CI gate</code></pre>\n-    <a class=\"oil-link\" href={lintHref.clean}>Open a clean template in Lab ↗</a>\n   </section>\n \n   <!-- ─────────────────────────── Migrate ─────────────────────────── -->\n@@ -177,27 +159,36 @@ $ flatbars lint src/email.maxbars --maxbars --max-warnings 0   # CI gate</code><\n \n     <h3><code>@</code>-data → the <code>loop</code> object</h3>\n     <div class=\"io\">\n+      <span class=\"lbl\">handlebars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.loopVars.template}</code></pre>\n-      <span class=\"arr\">migrate ↓</span>\n+      <span class=\"arr\">flatbars migrate ↓</span>\n+      <span class=\"lbl\">maxbars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.loopVars.source}</code></pre>\n-      <a class=\"oil-link\" href={migrateHref.loopVars}>Open in Lab ↗</a>\n     </div>\n+    <p class=\"tool-cap\"><span class=\"ico\">▶</span>the migrated template, running</p>\n+    <OpenInLab client:visible engine=\"maxbars\" template={migrateExamples.loopVars.source} data={migrateExamples.loopVars.data} />\n \n     <h3>Inverted section → <code>{t(\"{{#unless}}\")}</code></h3>\n     <div class=\"io\">\n+      <span class=\"lbl\">handlebars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.inverted.template}</code></pre>\n-      <span class=\"arr\">migrate ↓</span>\n+      <span class=\"arr\">flatbars migrate ↓</span>\n+      <span class=\"lbl\">maxbars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.inverted.source}</code></pre>\n-      <a class=\"oil-link\" href={migrateHref.inverted}>Open in Lab ↗</a>\n     </div>\n+    <p class=\"tool-cap\"><span class=\"ico\">▶</span>the migrated template, running (empty list → the inverse body shows)</p>\n+    <OpenInLab client:visible engine=\"maxbars\" template={migrateExamples.inverted.source} data={migrateExamples.inverted.data} />\n \n     <h3>Block-partial reference → <code>{t(\"{{yield}}\")}</code></h3>\n     <div class=\"io\">\n+      <span class=\"lbl\">handlebars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.partialBlock.template}</code></pre>\n-      <span class=\"arr\">migrate ↓</span>\n+      <span class=\"arr\">flatbars migrate ↓</span>\n+      <span class=\"lbl\">maxbars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.partialBlock.source}</code></pre>\n-      <a class=\"oil-link\" href={migrateHref.partialBlock}>Open in Lab ↗</a>\n     </div>\n+    <p class=\"tool-cap\"><span class=\"ico\">▶</span><code>{t(\"{{yield}}\")}</code> in a minimal inline-partial host</p>\n+    <OpenInLab client:visible engine=\"maxbars\" template={migrateExamples.partialBlock.cardTemplate} data={migrateExamples.partialBlock.data} />\n   </section>\n \n   <!-- ───────────────────────── Residuals ───────────────────────── -->\n@@ -209,10 +200,10 @@ $ flatbars lint src/email.maxbars --maxbars --max-warnings 0   # CI gate</code><\n       for a human to resolve — it never guesses.\n     </p>\n     <div class=\"io\">\n+      <span class=\"lbl\">handlebars source</span>\n       <pre class=\"cli\"><code>{migrateExamples.ambiguousSection.template}</code></pre>\n-      <span class=\"arr\">migrate ↓ (unchanged)</span>\n+      <span class=\"arr\">flatbars migrate ↓ (unchanged — reported, not rewritten)</span>\n       <pre class=\"cli\"><code>{migrateExamples.ambiguousSection.source}</code></pre>\n-      <a class=\"oil-link\" href={migrateHref.ambiguousSection}>Open in Lab ↗</a>\n     </div>\n     <div class=\"resid\">\n       <b>Residual · ambiguous-section.</b> A bare Mustache section <code>{t(\"{{#widget}}\")}</code>","raw_url":"https://github.com/wstein/flatbars/raw/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fpages%2Flint.astro","sha":"2952305348d11be5c0c724a10f3bb85c949c2eab","status":"modified"},{"additions":7,"blob_url":"https://github.com/wstein/flatbars/blob/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fstyles%2Fopen-in-lab.css","changes":7,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/tutorials%2Fsrc%2Fstyles%2Fopen-in-lab.css?ref=82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd","deletions":0,"filename":"tutorials/src/styles/open-in-lab.css","patch":"@@ -208,6 +208,13 @@\n .oil .oil-js .oil-out-pre { max-height: 22rem; }\n .oil .oil-js-note { font: 400 11px/1.4 var(--font-mono); color: var(--fg-faint); text-transform: none; letter-spacing: 0; }\n \n+/* Live `flatbars lint` pane (opt-in): the same dark screen as the output. The\n+   findings are FIXED colours — amber for a warning, green for the clean line —\n+   because the pane is always dark in both themes (a theme token would vanish in\n+   light mode, the bug that prompted this pane). */\n+.oil .oil-lint .oil-lint-warn { color: #fbbf24; }\n+.oil .oil-lint .oil-lint-ok { color: #86efac; }\n+\n /* ── Lab affordance: DOCK (header toolbar) — quiet, labelled, always visible ── */\n .oil .oil-lab {\n   text-decoration: none;","raw_url":"https://github.com/wstein/flatbars/raw/82c2b50f4b48b2eed9033e1cfd0c52bc2b710fbd/tutorials%2Fsrc%2Fstyles%2Fopen-in-lab.css","sha":"b67f83a8804ddf78fd81cc78ff1b285db2082059","status":"modified"}]}