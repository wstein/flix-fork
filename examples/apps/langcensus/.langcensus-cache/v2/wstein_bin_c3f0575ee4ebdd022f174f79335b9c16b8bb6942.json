{"files":[{"additions":44,"blob_url":"https://github.com/wstein/bin/blob/c3f0575ee4ebdd022f174f79335b9c16b8bb6942/2links","changes":101,"contents_url":"https://api.github.com/repos/wstein/bin/contents/2links?ref=c3f0575ee4ebdd022f174f79335b9c16b8bb6942","deletions":57,"filename":"2links","patch":"@@ -7,9 +7,10 @@\n \n ## SPECIFICATION\n \n-2links is a command-line tool that extracts, normalizes, and deduplicates URLs from\n-text input. It canonicalizes URLs to a standard form and removes duplicates that point\n-to the same resource, preferring HTTPS over HTTP and sorting output alphabetically.\n+2links is a universal command-line tool that extracts, normalizes, and deduplicates URLs\n+from any UTF8 text, regardless of format (HTML, Markdown, code, YAML, JSON, etc.).\n+It canonicalizes URLs to a standard form and removes duplicates that point to the same\n+resource, preferring HTTPS over HTTP and sorting output alphabetically.\n \n ## SUPPORTED SCHEMES\n \n@@ -19,13 +20,13 @@ to the same resource, preferring HTTPS over HTTP and sorting output alphabetical\n ## CONTRACT\n \n ### Input\n-- Plain text with embedded URLs (any supported scheme)\n-- Optional HTML with href=\"...\" attributes (with --html flag)\n+- Any UTF8 text containing URLs (HTML, Markdown, code, YAML, JSON, etc.)\n - Accepts from stdin or file argument\n - Max URL length: 2048 characters (standard)\n+- Format-agnostic: works with mixed content\n \n ### Processing Pipeline\n-1. **Extraction:** Regex scan for plain text URLs + optional HTML href parsing\n+1. **Extraction:** Regex scan for scheme://host:port/path pattern (format-agnostic)\n 2. **Validation:** URI.parse() with strict scheme whitelist\n 3. **Canonicalization:** Normalize scheme/host/path, strip trailing slashes\n 4. **Deduplication:** Group by scheme-agnostic key, prefer https\n@@ -73,31 +74,33 @@ Array of objects with:\n \n ### Basic Examples\n \n-  # Extract from file\n-  ./2links urls.txt\n+  # Extract from text file\n+  ./2links input.txt\n \n-  # Extract from stdin\n+  # Extract from any format (HTML, JSON, code, etc.)\n+  ./2links document.html\n+  ./2links config.json\n+  ./2links source.py\n+\n+  # Extract from stdin (pipe any text)\n   cat page.html | ./2links\n+  grep -r \"http\" . | ./2links\n \n-  # Extract from file with stats\n+  # With statistics\n   ./2links input.txt --verbose\n \n   # JSON output with variations\n   ./2links urls.txt --format json --variations\n \n-  # Parse HTML href attributes\n-  ./2links page.html --html\n-\n-  # Extract and filter by domain\n-  ./2links urls.txt --filter github.com\n+  # Filter and exclude internal\n+  ./2links urls.txt --filter github.com --no-internal\n \n-  # Exclude internal IPs\n-  ./2links mixed_urls.txt --no-internal\n+  # Save results to file\n+  ./2links mixed.txt --output results.txt --no-internal\n \n ### Command-Line Options\n \n   -f, --format FORMAT         Output format: text|json (default: text)\n-  -H, --html                  Parse HTML href=\"...\" attributes\n   -F, --filter DOMAIN         Filter URLs by domain (supports partial match)\n   -o, --output FILE           Write output to file instead of stdout\n   -v, --verbose               Show statistics, warnings, and counts\n@@ -150,26 +153,28 @@ Array of objects with:\n   Output:\n     https://github.com/user/repo\n \n-### Example 2: HTML Parsing\n+### Example 2: Format-Agnostic Processing\n \n-  Input:\n-    <a href=\"https://example.com\">Example</a>\n-    Visit https://example.com for details\n+  Input (mixed formats):\n+    HTML: <a href=\"https://example.com\">Example</a>\n+    JSON: {\"url\": \"https://example.com\"}\n+    Markdown: [Link](https://example.com)\n+    Code: URL = \"https://example.com\"\n \n   Command:\n-    $ ./2links page.html --html --format json --variations\n+    $ ./2links mixed_content.txt --format json --variations\n \n   Output:\n     [\n       {\n         \"canonical_url\": \"https://example.com\",\n         \"domain\": \"example.com\",\n-        \"count\": 2,\n+        \"count\": 4,\n         \"variations\": [\"https://example.com\"]\n       }\n     ]\n \n-### Example 3: Filtering and Stats\n+### Example 3: Filtering and Excluding Internal\n \n   Command:\n     $ ./2links urls.txt --filter github --verbose --no-internal\n@@ -192,13 +197,11 @@ Array of objects with:\n - ✅ --output flag for file output\n - ✅ --filter flag for domain/scheme filtering\n - ✅ --no-internal flag to exclude private IPs\n-- Future: --base-url for relative URL resolution\n \n ### Phase 6 (Planned)\n-- Full Nokogiri HTML parsing (not just href)\n-- Markdown link extraction\n-- robots.txt / sitemap.xml support\n-- Bulk file processing\n+- Relative URL resolution with --base-url\n+- Performance optimizations\n+- Additional output formats (CSV, YAML)\n \n ### Phase 7+ (Long-term)\n - Web crawler mode\n@@ -220,28 +223,22 @@ require 'optparse'\n require 'json'\n \n class URLExtractor\n-  # Regex pattern to match URLs in text\n-  # Matches http(s), ftp, and other common schemes\n-  # Stops at quotes, angle brackets, and whitespace\n+  # Regex pattern to match URLs in any text format\n+  # Matches: scheme://host[:port]/path\n+  # Format-agnostic: works with HTML, JSON, Markdown, code, etc.\n+  # Greedy but lets URI.parse validation filter invalid matches\n   URL_PATTERN = %r{\n-    (?:https?|ftp)://\n-    [^\\s\"'<>]+\n+    (?:https?|ftp)://[^\\s\"'<>]+\n   }x\n \n-  # Regex pattern to match href attributes in HTML\n-  HREF_PATTERN = %r{\n-    href\\s*=\\s*[\"]([^\"]*)[\"]\n-  }xi\n-\n   # Track warnings and stats\n   attr_reader :warnings, :stats\n \n-  def initialize(text, parse_html: false, exclude_internal: false)\n+  def initialize(text, exclude_internal: false)\n     @text = text\n     @urls = []\n     @dedup_map = {} # maps dedup key (scheme-agnostic) to {canonical:, variations:, schemes:, count:}\n     @warnings = []\n-    @parse_html = parse_html\n     @exclude_internal = exclude_internal\n     @stats = {\n       urls_found: 0,\n@@ -254,15 +251,8 @@ class URLExtractor\n   end\n \n   def extract\n-    # Extract from HTML href attributes if enabled\n-    if @parse_html\n-      @text.scan(HREF_PATTERN) do |href|\n-        # Only process if it looks like a URL\n-        process_url(href) if href =~ /\\A(?:https?|ftp):\\/\\//\n-      end\n-    end\n-\n-    # Find all URL matches in text using general pattern\n+    # Extract all URLs from text (format-agnostic)\n+    # Works with HTML, JSON, Markdown, code, YAML, etc.\n     @text.scan(URL_PATTERN) do |url_string|\n       process_url(url_string)\n     end\n@@ -356,6 +346,9 @@ class URLExtractor\n   def process_url(url_string)\n     @stats[:urls_found] += 1\n \n+    # Clean trailing punctuation from URL text\n+    url_string = url_string.sub(/[)\\]}\\.,;:!?]+\\z/, '')\n+\n     parsed = canonical_form(url_string)\n     if parsed.nil?\n       @stats[:urls_invalid] += 1\n@@ -427,7 +420,6 @@ class CLI\n       format: 'text',\n       verbose: false,\n       show_variations: false,\n-      parse_html: false,\n       output_file: nil,\n       filter: nil,\n       exclude_internal: false\n@@ -449,10 +441,6 @@ class CLI\n         @options[:filter] = domain\n       end\n \n-      opts.on('-H', '--html', 'Parse HTML href attributes (in addition to plain text URLs)') do\n-        @options[:parse_html] = true\n-      end\n-\n       opts.on('-o', '--output FILE', 'Write output to file instead of stdout') do |file|\n         @options[:output_file] = file\n       end\n@@ -493,7 +481,6 @@ class CLI\n \n     extractor = URLExtractor.new(\n       input,\n-      parse_html: @options[:parse_html],\n       exclude_internal: @options[:exclude_internal]\n     )\n     extractor.extract.deduplicate","raw_url":"https://github.com/wstein/bin/raw/c3f0575ee4ebdd022f174f79335b9c16b8bb6942/2links","sha":"252e04c75ce58d79f251ff77c879dfc69b7168a4","status":"modified"}]}