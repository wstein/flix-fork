{"files":[{"additions":56,"blob_url":"https://github.com/wstein/gramaire/blob/3e8db77b115d8372c4491981ff69ecb3ea256b97/docs%2Ffmt-output-contract.md","changes":79,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/docs%2Ffmt-output-contract.md?ref=3e8db77b115d8372c4491981ff69ecb3ea256b97","deletions":23,"filename":"docs/fmt-output-contract.md","patch":"@@ -23,51 +23,82 @@ into foreign repositories, and a file that is clean only under our own\n \n ## Canonical document structure\n \n-A formatted file is exactly this sequence, in order:\n-\n-1. **One H1** naming the grammar: `# <GrammarName>` (MD041, MD025).\n+**Every heading in this section is a STYLE convention `fmt` emits and checks,\n+never a LANGUAGE requirement — the compiler ignores headings entirely (see the\n+[language specification](../site/src/content/docs/specs/grammar-format.mdx)\n+§1). A grammar's real structure is the concatenated content of its\n+` ```gramaire ` fences, each self-identifying by content shape (\"case is law\");\n+`gramaire`/`gramaire tokens`/`gramaire settings`/`gramaire precedence` are gone —\n+there is exactly one fence tag now.** A formatted file is exactly this\n+sequence, in order:\n+\n+1. **One H1** naming the grammar: `# <GrammarName>` (MD041, MD025). Purely a\n+   caption — the grammar's real name is the required `%name <name>` directive\n+   inside the `## General settings` fence (item 3 below); `fmt` keeps the two\n+   in sync but the compiler reads only `%name`.\n 2. Optional intro prose (one or more paragraphs).\n-3. **One section per nonterminal**, each consisting of:\n+3. An **optional `## General settings`** section containing one bare\n+   ` ```gramaire ` fence whose lines are all `%name`/`%lang` directives\n+   (a Settings-role fence, per the language spec's \"case is law\" rule) —\n+   present whenever the grammar declares `%lang`; the `%name` directive\n+   itself may also live in a settings fence with no heading at all, but\n+   `fmt`'s canonical layout always gives it one.\n+4. An **optional `## Tokens`** section containing one bare ` ```gramaire `\n+   fence whose lines are all token-class definitions (a Tokens-role fence).\n+   Present only when the grammar declares named token classes.\n+5. **One section per nonterminal**, each consisting of:\n    - an H2 heading `## <Nonterminal>`, unique across the file (MD024);\n    - optional prose describing the rule;\n-   - exactly one `gramaire` payload fence holding that rule's productions;\n+   - exactly one bare ` ```gramaire ` fence holding that rule's productions;\n    - an optional linked railroad image.\n-4. An **optional `## Precedence`** section containing one `gramaire precedence`\n-   fence. It is present only when the grammar declares operator precedence;\n-   a grammar with no `%left` / `%right` / `%nonassoc` declarations omits the\n+6. An **optional `## Precedence`** section containing one bare ` ```gramaire `\n+   fence whose lines are all `%left`/`%right`/`%nonassoc` declarations\n+   (a Precedence-role fence). It is present only when the grammar declares\n+   operator precedence; a grammar with no such declarations omits the\n    section entirely rather than emitting an empty fence.\n-5. An **`## Error messages`** section containing one `gramaire errors` fence.\n-6. A **`## Generated tables`** section: a short caption line, then the\n+7. An **optional `## Error messages`** section containing one plain\n+   ` ```text ` fence of curated, state-keyed prose messages. This is not a\n+   `gramaire`-tagged fence at all (curated errors are prose, not grammar\n+   notation) and is skipped by the compiler like any non-`gramaire` fence.\n+8. A **`## Generated tables`** section: a short caption line, then the\n    FIRST/FOLLOW pipe table and the conflict summary.\n-7. A single trailing newline (MD047).\n+9. A single trailing newline (MD047).\n \n Section order is fixed so that `fmt` is a pure function of the grammar:\n the same grammar always serializes to byte-identical Markdown, which is\n what makes the drift hash and reproducible builds work.\n \n-**Heading layers (ADR D29).** Only the H1 and H2 layers are structural.\n-An H2 is **a nonterminal or a reserved section** (`Precedence`,\n-`Error messages`, `Generated tables`) — not a pure bijection — and that is\n-the whole of what the structure gate and the per-section drift hash check.\n+**Heading layers (ADR D29).** Only the H1 and H2 layers are structural to\n+`fmt`'s own style gate — never to the compiler. An H2 is **a nonterminal or a\n+reserved section name** (`General settings`, `Tokens`, `Precedence`,\n+`Error messages`, `Generated tables`) — not a pure bijection — and that is the\n+whole of what the structure gate and the per-section drift hash check.\n Headings at `###` and deeper are **presentational grouping** (\"Expressions\",\n \"Statements\", or a subsection): the gate ignores them for structure, the\n drift hash never covers them, and authors may place them freely (the GitHub\n table of contents still nests them). They remain ordinary Markdown and are\n linted as such — in particular `###` must follow the usual heading-increment\n rule (MD001). One file is always exactly one grammar: one start symbol, one\n namespace; reuse across grammars is a future cross-file `import` (see\n-`spec/import-rfc.md`), never in-file sub-grammars or multiple H1s (ADR D30).\n+`spec/import-rfc.md`), never in-file sub-grammars or multiple `%name`\n+directives (ADR D30).\n \n ## Block specifications\n \n-### Payload fences\n+### The payload fence\n+\n+Every role — productions, settings, tokens, precedence — lives in a fence\n+whose info string is **exactly** `gramaire` (bare, no suffix):\n \n-Productions, precedence, and curated errors live in fenced blocks whose\n-info string begins with `gramaire`:\n+- ` ```gramaire ` — self-identified by content shape: a nonterminal's\n+  productions (including `{% ... %}` semantic actions) by default; Settings\n+  when every line is a `%name`/`%lang` directive; Tokens when every line is an\n+  unindented `ALLCAPS : …` definition; Precedence when every line is a\n+  `%left`/`%right`/`%nonassoc` declaration.\n \n-- ` ```gramaire ` — productions, including `{% ... %}` semantic actions.\n-- ` ```gramaire precedence ` — `%left` / `%right` / `%nonassoc` declarations.\n-- ` ```gramaire errors ` — Menhir-style state-keyed messages.\n+Curated per-state error messages are no longer a `gramaire`-tagged fence — use\n+a plain ` ```text ` fence, which `fmt`/the compiler both skip like any other\n+non-`gramaire` fence.\n \n Every fence carries a non-empty info string, which satisfies MD040 and is\n _why_ Gramaire fences pass a rule that bare ` ``` ` fences fail. GFM treats\n@@ -186,7 +217,9 @@ backticks. `fmt` computes this; authors never reason about it.\n Two artifact classes are derived, not authored: railroad diagrams and the\n generated-tables section. `fmt` records the diagram mode and each derived\n artifact — for sidecar diagrams, the path plus a SHA-256 of the rule it was\n-built from; for the tables, a SHA-256 of the whole grammar — in a sidecar\n+built from; for the tables, a SHA-256 of the whole grammar (every classified\n+fence — Settings, Tokens, Precedence, and each Rule — folded in, so editing a\n+`%lang`/`%name` line is drift-visible too, not just a rule body) — in a sidecar\n **`<file>.gram.lock`**. In mermaid mode there are no sidecar files: the\n diagrams live in the document and their freshness is guaranteed by `fmt`\n idempotence (re-running makes no change), while grammar edits are still caught","raw_url":"https://github.com/wstein/gramaire/raw/3e8db77b115d8372c4491981ff69ecb3ea256b97/docs%2Ffmt-output-contract.md","sha":"fdb7f3f3fa0a1c6f9dce70ef099ad1e3a505f561","status":"modified"},{"additions":37,"blob_url":"https://github.com/wstein/gramaire/blob/3e8db77b115d8372c4491981ff69ecb3ea256b97/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","changes":52,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx?ref=3e8db77b115d8372c4491981ff69ecb3ea256b97","deletions":15,"filename":"site/src/content/docs/specs/grammar-format.mdx","patch":"@@ -20,23 +20,45 @@ compiler emits. Keywords MUST / SHOULD / MAY per RFC 2119.\n ## 1. Document envelope\n \n A Gramaire grammar is hosted in a GitHub-Flavored Markdown document. The compiler\n-ignores prose, list markers, and images; it extracts the grammar by scanning ATX\n-headings and fenced code blocks whose info string begins with `gramaire`.\n-\n-- **Grammar name.** The single `# H1`. There MUST be exactly one.\n-- **Nonterminal rules.** Every non-reserved `## H2` names a nonterminal; the\n-  `lr` block beneath it holds that rule's productions.\n-- **Reserved `## H2` sections.** `General settings` (a `gramaire settings` block\n-  of document-level directives — currently `%lang <host>`, which declares the\n-  language of inline `{% … %}` actions), `Precedence` (an `lr precedence` block),\n-  `Error messages` (an `lr errors` block), and `Generated tables` (the emitted\n-  FIRST/FOLLOW table). They appear in that order when present.\n-- **Presentational headings.** `###` and deeper headings carry no structural\n-  meaning; they are free grouping the analyzer skips. Only the H1 and H2 layers\n-  are structural.\n+ignores **every** heading, prose paragraph, list marker, and image — they are\n+pure presentation and carry no grammar semantics — and extracts the grammar\n+from the concatenated content of every fenced code block whose info string is\n+**exactly** `gramaire` (bare, no suffix).\n+\n+- **Single fence tag, self-identifying content (\"case is law\").** There is\n+  exactly one fence tag, ` ```gramaire `. A fence's role is never named by the\n+  fence itself; it is inferred from the SHAPE of its own non-blank lines: every\n+  line a `%name`/`%lang` directive → **Settings**; every line a\n+  `%left`/`%right`/`%nonassoc` declaration → **Precedence**; every line an\n+  unindented `ALLCAPS : …` token definition → **Tokens**; anything else → a\n+  nonterminal's **productions**. This is unambiguous by construction: no valid\n+  production can share a token definition's one-line shape, since a rule's `:`\n+  MUST follow a newline (`IDENT NL :`, §2) while a token definition's `:` is on\n+  the same line as its name. A suffixed opener (` ```gramaire tokens `,\n+  ` ```gramaire settings `, ` ```gramaire precedence `, ` ```gramaire errors `) is\n+  a removed notation and MUST be rejected with a located, actionable error\n+  (`gramaire fmt --migrate` merges it into a bare fence).\n+- **Grammar name.** The required `%name <name>` directive, declared in a\n+  Settings-role fence. A document with no `%name` is a hard error — there is no\n+  fallback to the `# H1` or to the file's path, so a `.gram.md`/`.gram` stays\n+  fully self-describing independent of how it was loaded (a file on disk, pasted\n+  into the Lab, piped over stdin).\n+- **Nonterminal rules.** A Rule-role fence's own head (`Name` on its own line,\n+  then `:` and its alternatives) names the nonterminal — never a heading.\n+- **Presentational headings.** Every heading level, including the `# H1` and\n+  `## H2` layers, is free grouping the analyzer entirely ignores.\n+  `gramaire fmt`'s canonical output still uses `## General settings` /\n+  `## Tokens` / `## <Nonterminal>` / `## Precedence` / `## Error messages` /\n+  `## Generated tables` as a readability convention — see the\n+  [fmt output contract](../../../../../docs/fmt-output-contract.md) — but that\n+  is a STYLE the formatter emits and checks, never a LANGUAGE requirement the\n+  compiler enforces.\n+- **Curated error messages are prose, not grammar notation.** Put them in a\n+  plain ` ```text ` fence (skipped like any non-`gramaire` fence) if you want\n+  them to survive `gramaire fmt`; there is no dedicated notation for them.\n - **One grammar per file.** A file defines exactly one start symbol and one\n   nonterminal namespace. Reuse across files is a future cross-file `import`,\n-  never in-file sub-grammars or multiple H1s.\n+  never in-file sub-grammars or multiple `%name` directives.\n \n ## 2. Lexical grammar of an `lr` block\n ","raw_url":"https://github.com/wstein/gramaire/raw/3e8db77b115d8372c4491981ff69ecb3ea256b97/site%2Fsrc%2Fcontent%2Fdocs%2Fspecs%2Fgrammar-format.mdx","sha":"2d5f039bd808d4e2747dfa899b97990f6dfb4584","status":"modified"},{"additions":35,"blob_url":"https://github.com/wstein/gramaire/blob/3e8db77b115d8372c4491981ff69ecb3ea256b97/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","changes":51,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx?ref=3e8db77b115d8372c4491981ff69ecb3ea256b97","deletions":16,"filename":"site/src/content/docs/tutorials/intro.mdx","patch":"@@ -32,36 +32,53 @@ is no separate \"tutorial mode.\"\n \n ## 1. The shape of a `.gram.md` file\n \n-A grammar file is a canonical Markdown document with a fixed structure:\n+A grammar file is a Markdown document, but **headings, prose, and images carry\n+no grammar semantics at all** — they're free presentation the compiler\n+ignores completely. The grammar itself lives entirely in fenced code blocks\n+tagged `gramaire` (one tag, no variants), each self-identifying its role by\n+the shape of its own lines:\n \n <Steps>\n \n-1. **One `# H1`** — the grammar's name.\n+1. **A required `%name <name>` directive** — the grammar's name. It lives\n+   inside a `gramaire` fence (conventionally under a `## General settings`\n+   heading); there's no fallback to a heading or a file name, so a `.gram.md`\n+   stays self-describing on its own.\n \n-2. **One `## H2` per nonterminal** — the heading names the rule; a `gramaire`\n-   fenced block under it holds its productions.\n+2. **One `gramaire` fence per nonterminal** — the fence's own head (`Name`\n+   on its own line, then `:` and its alternatives) names the rule; a `## H2`\n+   heading above it is just a reader-friendly label, never load-bearing.\n \n-3. **Reserved `## H2` sections** — `General settings`, `Precedence`, `Error\n-messages`, and `Generated tables`, in that order, when present.\n+3. **Optional `gramaire` fences for tokens and precedence** — a fence whose\n+   every line is an ALL-CAPS `NAME : …` definition declares token classes; a\n+   fence whose every line is `%left`/`%right`/`%nonassoc` declares operator\n+   precedence. `gramaire fmt`'s canonical layout groups these under\n+   `## Tokens` / `## Precedence` headings, but that's a formatting\n+   convention, not a language rule.\n \n-4. **Everything else is prose** — paragraphs, images, and `###`+ headings are\n-   free presentational grouping the compiler ignores.\n+4. **Everything else is prose** — paragraphs, images, and headings at every\n+   level are free presentational grouping the compiler ignores.\n \n </Steps>\n \n-One file is exactly one grammar: one start symbol, one namespace. Productions\n-live in fenced blocks whose info string is `gramaire`; everything outside them is\n-documentation that travels with the grammar.\n+One file is exactly one grammar: one start symbol, one namespace.\n \n ## 2. Your first rule\n \n-Create `calc.gram.md` and give it a title and a first nonterminal:\n+Create `calc.gram.md`, name it with the required `%name` directive, and add a\n+first nonterminal:\n \n ````md\n # Calc\n \n A small arithmetic grammar.\n \n+## General settings\n+\n+```gramaire\n+%name Calc\n+```\n+\n ## Expr\n \n An expression is a sum or difference of terms.\n@@ -114,7 +131,7 @@ ALL-CAPS name bound to a `/regex/`; mark throwaway classes (whitespace) `%skip`:\n ````md\n ## Tokens\n \n-```gramaire tokens\n+```gramaire\n NUMBER : /[0-9]+/\n WS     : /[ \\t\\r\\n]+/   %skip\n ```\n@@ -186,7 +203,7 @@ section to resolve it — earlier declarations bind more loosely:\n ````md\n ## Precedence\n \n-```gramaire precedence\n+```gramaire\n %left '+' '-'\n %left '*' '/'\n ```\n@@ -234,12 +251,14 @@ verdict there is the same verdict the CLI would give a specific input.\n \n A recognizer throws its work away. To keep it — to build a number, or an AST —\n attach a host-language **action** between `{%` and `%}`. Declare the action\n-language once, in a reserved `## General settings` section:\n+language once, by adding `%lang` to the `%name` fence you already have from\n+§2:\n \n ````md\n ## General settings\n \n-```gramaire settings\n+```gramaire\n+%name Calc\n %lang javascript\n ```\n ````","raw_url":"https://github.com/wstein/gramaire/raw/3e8db77b115d8372c4491981ff69ecb3ea256b97/site%2Fsrc%2Fcontent%2Fdocs%2Ftutorials%2Fintro.mdx","sha":"4c878c04da580787c3de54d91b0c397642e71fab","status":"modified"},{"additions":8,"blob_url":"https://github.com/wstein/gramaire/blob/3e8db77b115d8372c4491981ff69ecb3ea256b97/spec%2Fincremental-spec.md","changes":13,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/spec%2Fincremental-spec.md?ref=3e8db77b115d8372c4491981ff69ecb3ea256b97","deletions":5,"filename":"spec/incremental-spec.md","patch":"@@ -303,8 +303,11 @@ plan ADR that resolves it, so the spec and plan agree.\n    tie-break of R10, so it stays deterministic (R12); a descriptor declares which\n    mode its expected error tree assumes, so C3 holds per mode. Repair is promoted\n    to a default only after the descriptor corpus measures its quality.\n-3. **Error-message text — Core-owned, localization deferred (ADR D17).** Message\n-   text lives in the grammar's `gramaire errors` block as one canonical set, keyed by\n-   the D6 hybrid item-set signature. It is not a per-target profile (errors are\n-   input-facing prose, not host code) and not per-locale in v0; localization is\n-   an additive runtime/LSP catalog keyed by the same ids, with no consumer yet.\n+3. **Error-message text — Core-owned, localization deferred (ADR D17).** Curated,\n+   state-keyed message text is authored as plain prose (a ```text fence, not a\n+   grammar-notation construct — the prior `gramaire errors` fence had no\n+   consumer and was removed) and, when a real consumer lands, would be keyed by\n+   the D6 hybrid item-set signature as one canonical set. It is not a\n+   per-target profile (errors are input-facing prose, not host code) and not\n+   per-locale in v0; localization is an additive runtime/LSP catalog keyed by\n+   the same ids, with no consumer yet.","raw_url":"https://github.com/wstein/gramaire/raw/3e8db77b115d8372c4491981ff69ecb3ea256b97/spec%2Fincremental-spec.md","sha":"a7d9ea63d947b0f31e58b8fd094ecf56bea011ad","status":"modified"},{"additions":29,"blob_url":"https://github.com/wstein/gramaire/blob/3e8db77b115d8372c4491981ff69ecb3ea256b97/spec%2Flexer-spec.md","changes":53,"contents_url":"https://api.github.com/repos/wstein/gramaire/contents/spec%2Flexer-spec.md?ref=3e8db77b115d8372c4491981ff69ecb3ea256b97","deletions":24,"filename":"spec/lexer-spec.md","patch":"@@ -1,4 +1,4 @@\n-# Lexer specification — `gramaire tokens`\n+# Lexer specification — the Tokens-role `gramaire` fence\n \n Status: **draft**, tracks `irVersion: 0`. Defines how a `.gram.md` grammar\n specifies its own lexis, so a grammar is self-contained and every backend can\n@@ -22,13 +22,14 @@ self-contained, the \"tables-only\" backend and generic interpreter can't produce\n a working parser from the IR, and `Gramaire.Lexer` sits _outside_ the self-host\n loop. Defining the lexer in the grammar fixes all three.\n \n-## 2. The `gramaire tokens` block\n+## 2. The Tokens-role `gramaire` fence\n \n-Lexis is declared in a fenced block whose info string is `gramaire tokens`. Each line\n-defines one **named token class**:\n+Lexis is declared in a bare ` ```gramaire ` fence — the same, only fence tag the\n+whole notation has — whose role is inferred from its own content shape (\"case\n+is law\", grammar-format spec §1): a fence classifies as **Tokens** when every\n+one of its non-blank lines defines a named token class:\n \n ```text\n-gramaire tokens\n NAME : <definition> [ modifiers ]\n ```\n \n@@ -40,11 +41,16 @@ NAME : <definition> [ modifiers ]\n   (§6); a `/regex/` may also carry a glued `i` case-insensitivity flag (`/…/i`).\n \n A grammar MUST place all its named classes here; an ALL-CAPS symbol used in a\n-production but absent from `gramaire tokens` is an error (\"token class `X` used but\n-never defined\") unless the block is in `%external` mode (§6).\n+production but absent from a Tokens-role fence is an error (\"token class `X`\n+used but never defined\") unless the block is in `%external` mode (§6).\n \n-Like `gramaire precedence` and `gramaire errors`, the `gramaire tokens` block is a\n-hand-parsed sidecar notation, not itself a `gramaire` grammar.\n+Like the Settings- and Precedence-role fences, a Tokens-role fence is a\n+hand-parsed sidecar notation, not itself a `gramaire` grammar — but unlike\n+those two, it is never ambiguous with production content: a token\n+definition's ALL-CAPS-name-before-`:` shape can never overlap a valid\n+production, since the `lr` notation requires a newline between a rule's name\n+and its `:` (`IDENT NL :`) while a token definition's `:` is on the same line\n+as its name.\n \n ## 3. The regular sublanguage\n \n@@ -83,10 +89,10 @@ not match differently), so it does not weaken this guarantee.\n \n The effective token set a grammar lexes is the union of:\n \n-1. **Named classes** — defined in `gramaire tokens` (§2).\n+1. **Named classes** — defined in the Tokens-role `gramaire` fence (§2).\n 2. **Implicit literals** — every terminal literal that appears in a production\n    (`'{'`, `','`, `'true'`, and in `gramaire` itself `':'` / `'|'`) is a token defined\n-   by its exact spelling. These need **no** `gramaire tokens` entry; the productions\n+   by its exact spelling. These need **no** Tokens-role fence entry; the productions\n    define them. A literal may be written in either of two interchangeable\n    delimiters — `'x'` or `\"x\"` — all identical (ADR D34); the author picks\n    whichever needs no escaping (`'\"'`, `\"'\"`). The chosen delimiter is escaped\n@@ -95,8 +101,8 @@ The effective token set a grammar lexes is the union of:\n    the whole quoted lexeme; the consumer unquotes and unescapes to the spelling.\n \n All of these are merged into **one** scanner DFA. A grammar therefore never needs\n-to repeat its punctuation/keyword literals in `gramaire tokens`; it declares only the\n-open-ended classes.\n+to repeat its punctuation/keyword literals in its Tokens-role fence; it declares\n+only the open-ended classes.\n \n ## 5. Matching semantics\n \n@@ -188,7 +194,7 @@ reference token stream byte-for-byte.\n \n ## 8. Self-host\n \n-`grammar/lr.gram.md` carries its own `gramaire tokens` block (§10), and the scanner\n+`grammar/lr.gram.md` carries its own Tokens-role `gramaire` fence (§10), and the scanner\n built from it is the **production** lexer for `gramaire` grammar source: `Gramaire.Lr`\n scans with `lrScanItems` (that block plus the implicit `` `:` `` / `` `|` ``\n literals), trimming each `ACTION` body in the consumer (M5). `Gramaire.Lexer` is\n@@ -204,10 +210,11 @@ and the whole grammar corpus are themselves an end-to-end check on it.\n \n ## 9. fmt and structure\n \n-- A grammar's lexis lives in a single reserved `## Tokens` H2 section holding the\n-  `gramaire tokens` block. It SHOULD appear **before the first nonterminal section**\n-  (alphabet before grammar); the structure gate treats `Tokens` as a reserved\n-  section like `Precedence` (fmt-output-contract amendment).\n+- A grammar's lexis lives in a single, reserved `## Tokens` heading holding a\n+  Tokens-role `gramaire` fence — purely a STYLE convention `fmt` emits and\n+  checks (fmt-output-contract), never a language requirement (headings carry\n+  no grammar semantics). It SHOULD appear **before the first nonterminal\n+  section** (alphabet before grammar).\n - fmt MUST align the `:` column within the block and preserve declaration order\n   (it is significant — M2). The `*.gram.lock` hashes the normalized token\n   definitions so a pattern change is drift-visible.\n@@ -221,8 +228,7 @@ its keep/drop via `%external`, while capture gives it a `\\n` text); `WS` is\n skipped (extras); operator tokens use the string-literal form; `` `:` `` and\n `` `|` `` stay implicit literals from the productions.\n \n-```text\n-lr tokens\n+```gramaire\n WS       : /[ \\t]+/                       %skip\n NL       : /(\\r?\\n)(?:[ \\t]*\\r?\\n)*/      %external(layout)\n ATTR     : /#\\[([A-Za-z_][A-Za-z0-9_]*)\\]/\n@@ -245,8 +251,7 @@ punctuation and the `true`/`false`/`null` keywords are implicit literals from th\n productions, so they are not repeated here. `STRING` and `NUMBER` carry the whole\n match as their text (no capture), and use non-capturing groups for structure.\n \n-```text\n-lr tokens\n+```gramaire\n STRING : /\"(?:[^\"\\\\]|\\\\.)*\"/\n NUMBER : /-?(?:0|[1-9][0-9]*)(?:\\.[0-9]+)?(?:[eE][-+]?[0-9]+)?/\n WS     : /[ \\t\\r\\n]+/    %skip\n@@ -268,8 +273,8 @@ tie-breaks needed.\n - **L4 (self-host lexer).** The generated `gramaire` lexer reproduces the bootstrap\n   lexer's token stream on `grammar/lr.gram.md` (§8) — terminals now, text once\n   capture lands.\n-- **L5 (json self-contained).** `json.gram.md` plus its `gramaire tokens` block parses a\n-  JSON corpus with no hand-written scanner.\n+- **L5 (json self-contained).** `json.gram.md` plus its Tokens-role `gramaire`\n+  fence parses a JSON corpus with no hand-written scanner.\n \n ## 13. Resolved questions\n ","raw_url":"https://github.com/wstein/gramaire/raw/3e8db77b115d8372c4491981ff69ecb3ea256b97/spec%2Flexer-spec.md","sha":"cf4e0b9b39b1f9a95a1c993126566ffbf17cb49b","status":"modified"}]}