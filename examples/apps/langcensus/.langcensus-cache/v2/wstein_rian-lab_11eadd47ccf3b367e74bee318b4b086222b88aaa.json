{"files":[{"additions":1,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","changes":2,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":1,"filename":"docs/adr/0063-bootstrap-plan-and-fixed-point.md","patch":"@@ -1,7 +1,7 @@\n # ADR-0063 — Bootstrap plan: the self-hosting boundary and the fixed-point criterion\n \n **Status:** Proposed\n-**Implemented:** partial — **BEAM** self-hosting (§4): Stage 0/1 equivalence-locked (ported lexer + parser slice; `Rian.Fixpoint`, `examples/rian/selfhost_*.rian`; `test/rian/fixpoint_test.exs`, `parse_fixpoint_test.exs`), the surface→Core lowering ported over the literal/unary/binary slice (`selfhost_core.rian` equals `Rian.Core.from_expr`; `core_fixpoint_test.exs`), the capability checker ported over the scalar/String/Vec/nominal slice (`selfhost_cap.rian` equals `Rian.Capability`; `cap_fixpoint_test.exs`), the exhaustiveness gate ported over the single-column nullary-constructor slice (`selfhost_exhaust.rian` agrees with Maranget `useful?/3`; `exhaust_fixpoint_test.exs`), the ECMAScript backend's expression emitter ported over the literal/unary/binary slice (`selfhost_js.rian` equals `Rian.JS` term-for-term; `js_fixpoint_test.exs`), the Rust/Kotlin/BEAM backends' expression emitters ported over the same slice (`selfhost_rust.rian` = `Rian.Lower.emit_expr`; `selfhost_kotlin.rian` = `Rian.JVM`; `selfhost_beam.rian` = `:erl_parse`'s abstract forms; `rust_emit_fixpoint_test.exs`/`kotlin_emit_fixpoint_test.exs`/`beam_emit_fixpoint_test.exs`), a slice of the real type checker ported (`selfhost_checker.rian` agrees with `Rian.Check.infer` on closed integer expressions; `checker_infer_fixpoint_test.exs`); the **capability checker** and the **exhaustiveness gate** are now fully self-hosted (`selfhost_cap.rian` reproduces the whole `Rian.Capability` rust-param matrix; `selfhost_exhaust.rian` reproduces the complete Maranget `Rian.Exhaustiveness.useful?`), Stage 2 near-complete (decl front-end IR-equals `Rian.Decl`), Stage 3 (`v1==v2` `.beam`) not. The marching boundary is **measured** (`Rian.SelfHost` → `docs/self-host-status.md`, `test/rian/self_host_status_test.exs`), FFI crutches are **counted+enforced** (`@selfhost_ffi` ledger, `self_host_ffi_test.exs`), and the two untested-fragility areas now have fixpoints with teeth (`checker_fixpoint_test.exs`, `string_emit_fixpoint_test.exs`). **Portable** self-hosting (compile-the-compiler to Rust/JS) is a *separate, further* terminus, gated on ADR-0047 breadth + the `Fn` Reach gap — not near.\n+**Implemented:** partial — **BEAM** self-hosting (§4): Stage 0/1 equivalence-locked (ported lexer + parser slice; `Rian.Fixpoint`, `examples/rian/selfhost_*.rian`; `test/rian/fixpoint_test.exs`, `parse_fixpoint_test.exs`), the surface→Core lowering **fully self-hosted** (`selfhost_core.rian` equals `Rian.Core.from_expr`/`from_pat` over the whole surface-reachable expression+pattern vocabulary; `core_fixpoint_test.exs`), the capability checker ported over the scalar/String/Vec/nominal slice (`selfhost_cap.rian` equals `Rian.Capability`; `cap_fixpoint_test.exs`), the exhaustiveness gate ported over the single-column nullary-constructor slice (`selfhost_exhaust.rian` agrees with Maranget `useful?/3`; `exhaust_fixpoint_test.exs`), the ECMAScript backend's expression emitter ported over the literal/unary/binary slice (`selfhost_js.rian` equals `Rian.JS` term-for-term; `js_fixpoint_test.exs`), the Rust/Kotlin/BEAM backends' expression emitters ported over the same slice (`selfhost_rust.rian` = `Rian.Lower.emit_expr`; `selfhost_kotlin.rian` = `Rian.JVM`; `selfhost_beam.rian` = `:erl_parse`'s abstract forms; `rust_emit_fixpoint_test.exs`/`kotlin_emit_fixpoint_test.exs`/`beam_emit_fixpoint_test.exs`), a slice of the real type checker ported (`selfhost_checker.rian` agrees with `Rian.Check.infer` on closed integer expressions; `checker_infer_fixpoint_test.exs`); the **capability checker** and the **exhaustiveness gate** are now fully self-hosted (`selfhost_cap.rian` reproduces the whole `Rian.Capability` rust-param matrix; `selfhost_exhaust.rian` reproduces the complete Maranget `Rian.Exhaustiveness.useful?`), Stage 2 near-complete (decl front-end IR-equals `Rian.Decl`), Stage 3 (`v1==v2` `.beam`) not. The marching boundary is **measured** (`Rian.SelfHost` → `docs/self-host-status.md`, `test/rian/self_host_status_test.exs`), FFI crutches are **counted+enforced** (`@selfhost_ffi` ledger, `self_host_ffi_test.exs`), and the two untested-fragility areas now have fixpoints with teeth (`checker_fixpoint_test.exs`, `string_emit_fixpoint_test.exs`). **Portable** self-hosting (compile-the-compiler to Rust/JS) is a *separate, further* terminus, gated on ADR-0047 breadth + the `Fn` Reach gap — not near.\n **Refs:** ADR-0027 (fast track to self-hosting), ADR-0031 (reuse the runtime, not the compiler; abstract-forms backend), ADR-0050 (typed core IR — one IR, many front/back ends), ADR-0047 (portable prelude — the stdlib the ported compiler leans on), SELFHOST.md (the blocker ledger + the lexer/parser fixpoints)\n **Owners:** Arthur Pendelton (compilers) · Chloe Bennett (parser) · Maya Lin (architecture) · Samir Patel (conformance) · Kira Neri (honesty) · Rachel Okafor (PM)\n ","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/docs%2Fadr%2F0063-bootstrap-plan-and-fixed-point.md","sha":"dcefcc60a2ccb330ac93db16c0b1125875310f65","status":"modified"},{"additions":3,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/docs%2Fself-host-status.md","changes":6,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/docs%2Fself-host-status.md?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":3,"filename":"docs/self-host-status.md","patch":"@@ -6,15 +6,15 @@ self-hosting (ADR-0063 §4) — the Rian compiler compiling its own source to `.\n **Portable** self-hosting (the compiler lowered to Rust/JS) is a separate, further\n terminus and is *not* measured here.\n \n-**64% self-hosted** — 3 stage(s) self-hosted,\n-8 partial, 0 not started, of 11.\n+**68% self-hosted** — 4 stage(s) self-hosted,\n+7 partial, 0 not started, of 11.\n \n | Stage | Role | Self-hosted | Evidence | Notes |\n | --- | --- | --- | --- | --- |\n | Lexer | frontend | ✅ yes | `examples/rian/selfhost_lexer_v2.rian` · `test/rian/fixpoint_test.exs` | token stream equals Rian.Lexer over slices 1-5 (incl. tokenize/1, {:nl}) |\n | Declaration parser | frontend | 🟡 partial | `examples/rian/selfhost_decl.rian` · `test/rian/decl_fixpoint_test.exs` | IR equals Rian.Decl over type/struct/mod/def slice; alias/protocol/generics remain |\n | Expression/pattern parser | frontend | 🟡 partial | `examples/rian/selfhost_parse.rian` · `test/rian/parse_fixpoint_test.exs` | AST equals Rian.Pratt over an arithmetic/precedence slice |\n-| Typed Core IR (from_expr/from_pat) | frontend | 🟡 partial | `examples/rian/selfhost_core.rian` · `test/rian/core_fixpoint_test.exs` | surface→Core lowering equals Rian.Core.from_expr over the literal/unary/binary slice; calls/lists/blocks/lambdas remain |\n+| Typed Core IR (from_expr/from_pat) | frontend | ✅ yes | `examples/rian/selfhost_core.rian` · `test/rian/core_fixpoint_test.exs` | the full surface→Core lowering — every Pratt-produced expression (literals/unary/binary/call/label/dot/if/tuple/list/map/block/case/lambda/capture/with) and pattern (wild/var/lit/char/atom/tuple/ctor/list/struct/map) — equals Rian.Core.from_expr/from_pat; the Lower-internal resolved nodes + never-parsed as/pin patterns are not surface-reachable |\n | Type checker (inference + error sets) | checker | 🟡 partial | `examples/rian/selfhost_checker.rian` · `test/rian/checker_infer_fixpoint_test.exs` | type inference agrees with the REAL Rian.Check.infer over closed integer expressions; env/floats/calls/lambdas/case remain (selfhost_check.rian is a separate TOY-language spike) |\n | Exhaustiveness gate | checker | ✅ yes | `examples/rian/selfhost_exhaust.rian` · `test/rian/exhaust_fixpoint_test.exs` | the complete Maranget useful?/3 (specialize/default/signature over single+multi-column matrices, ctors-with-args, finite/infinite types) reproduces Rian.Exhaustiveness.useful? — the gate decision; only the witness/counterexample diagnostic (algorithm I) is unported |\n | Capability checker | checker | ✅ yes | `examples/rian/selfhost_cap.rian` · `test/rian/cap_fixpoint_test.exs` | the full capability→Rust mapping (every Copy width, String, nominal, nested Vec, parametric generics incl. the val-generic quirk) + ref-rejecting BEAM legality equal Rian.Capability over the whole matrix; type-string tokenisation is the type-parser's stage, the BEAM linearity check is native typestate |","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/docs%2Fself-host-status.md","sha":"40f2df442290b2d04770572425b0e9e2f02a56b4","status":"modified"},{"additions":9,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/examples%2Frian%2FREADME.md","changes":16,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2FREADME.md?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":7,"filename":"examples/rian/README.md","patch":"@@ -64,13 +64,15 @@ compile + run on real BEAM bytecode:\n   projection — diffed over a corpus (`test/rian/parse_fixpoint_test.exs`), the\n   parser analog of the lexer fixpoint. Covers the full binary precedence table\n   (precedence climbing), prefix `-`/`not`, and function calls.\n-- [selfhost_core.rian](selfhost_core.rian) — the **surface→Core lowering** port\n-  (the pipeline stage after the parser, ADR-0063): translates the parser's surface\n-  tuples into the typed **Core IR** node sum, **fixpoint-locked** against the\n-  reference `Rian.Core.from_expr` term-for-term over a corpus\n-  (`test/rian/core_fixpoint_test.exs`). Covers the literal/identifier/atom/unary/\n-  binary slice — the same vocabulary the parser port emits; calls, lists, blocks,\n-  `if`/`case`, and lambdas remain.\n+- [selfhost_core.rian](selfhost_core.rian) — the **surface→Core lowering**, fully\n+  self-hosted (the pipeline stage after the parser, ADR-0063/0050): translates the\n+  whole `Rian.Pratt` surface AST into the typed **Core IR**. Every expression node\n+  (literals, unary/binary, calls with labelled args, dot, `if`, tuples, lists with\n+  tails, maps, blocks, `case` with guards, lambdas, captures, `with`) and every\n+  pattern (wild/var/lit/char/atom/tuple/ctor/list/struct/map) is rendered to a\n+  canonical s-expression and **equivalence-locked** against `Rian.Core.from_expr`/\n+  `from_pat` (`test/rian/core_fixpoint_test.exs`). The Lower-internal resolved\n+  nodes and the never-parsed `as`/pin patterns are not surface-reachable.\n - [selfhost_decl.rian](selfhost_decl.rian) — a Rian **declaration** front-end\n   (self-hosting **Stage 2**, ADR-0063): parses `type` sums and `def` functions\n   into a `Decl` representation that, projected to `Rian.IR`, **equals what","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/examples%2Frian%2FREADME.md","sha":"1399b22c8d7441d69b38a311b2708fb6daca7579","status":"modified"},{"additions":169,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/examples%2Frian%2Fselfhost_core.rian","changes":199,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/examples%2Frian%2Fselfhost_core.rian?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":30,"filename":"examples/rian/selfhost_core.rian","patch":"@@ -1,44 +1,183 @@\n # ===========================================================================\n-# SELF-HOSTING SPIKE — the surface→Core lowering pass, written in Rian\n+# SELF-HOSTING SPIKE — the surface→Core lowering (FULL), written in Rian\n # ===========================================================================\n-# Goal (ADR-0063 §2, the stage after the parser): translate the parser's surface\n-# expression AST (the `Rian.Pratt` tuples) into the typed **Core IR** node sum —\n-# the spine every downstream pass consumes (ADR-0050). This is the Rian-written\n-# counterpart of `Rian.Core.from_expr`, fixpoint-locked against it in\n-# `test/rian/core_fixpoint_test.exs`.\n+# Goal (ADR-0063 §2, the stage after the parser; ADR-0050): translate the whole\n+# `Rian.Pratt` surface expression/pattern AST into the typed **Core IR** — the\n+# Rian-written counterpart of `Rian.Core.from_expr`/`from_pat`, fixpoint-locked\n+# against them in `test/rian/core_fixpoint_test.exs`.\n #\n-# Slice (the expression-parser slice's exact vocabulary): number / string /\n-# identifier / atom literals, prefix unary, and binary operators — recursively.\n-# Remaining `Rian.Core.from_expr` vocabulary (calls, lists, tuples, maps, `if`/\n-# `case`/`with` blocks, lambdas, captures, char, construction nodes) is the\n-# `:partial` tail — see ADR-0063 / docs/self-host-status.md.\n+# Coverage is the FULL surface front-door: every node `Rian.Pratt` produces —\n+# literals, identifiers, atoms, unary/binary, calls (incl. labelled args), dot,\n+# `if`, tuples, lists (with tails), maps, blocks (`:=`/typed binds/exprs), `case`\n+# (with guards), lambdas, captures (`&(…)`/`&f/n`), `with`; and every pattern —\n+# wild/var/lit/char/atom/tuple/ctor/list/as/pin/struct/map. The Lower-internal\n+# resolved nodes (`variant_lit`/`struct_lit`/`const_ref`) and the decl-resolved\n+# `str_interp` / Rust-baked `rpat` are not surface, so they are out of scope.\n+#\n+# Each node is rendered to a canonical s-expression string; the fixpoint test\n+# renders the reference `Rian.Core` struct to the same string and diffs.\n # ---------------------------------------------------------------------------\n \n mod SelfhostCore do\n-  # The surface expression AST the parser produces (mirrors the `Rian.Pratt`\n-  # tuples one-for-one for this slice).\n+  # ── surface expression AST (mirrors Rian.Pratt tuples) ──\n   type Surface := SNum(String)\n                 | SStr(String)\n+                | SChar(Int53)\n                 | SId(String)\n                 | SAtom(String)\n                 | SUnary(String, Surface)\n                 | SBin(String, Surface, Surface)\n+                | SCall(Surface, Vec(Surface))\n+                | SLabel(String, Surface)\n+                | SDot(Surface, String)\n+                | SIf(Surface, Surface, Surface)\n+                | STuple(Vec(Surface))\n+                | SList(Vec(Surface), STail)\n+                | SMap(Vec(SPair))\n+                | SBlock(Vec(SStmt))\n+                | SCase(Surface, Vec(SArm))\n+                | SLambda(Vec(SParam), Surface)\n+                | SCapArg(Int53)\n+                | SCapture(Surface)\n+                | SCaptureNamed(Surface, Int53)\n+                | SWith(Vec(SClause), Surface, Vec(SArm))\n+\n+  type STail := TClose | TTail(Surface)\n+  type SPair := SP(String, Surface)\n+  type SStmt := SBind(String, Surface) | STypedBind(String, String, Surface) | SExpr(Surface)\n+  type SGuard := GNone | GSome(Surface)\n+  type SArm := SArm(SPat, SGuard, Surface)\n+  type SClause := SClause(SPat, Surface)\n+  type SParamTy := PtNone | PtSome(String)\n+  type SParam := SParam(String, SParamTy)\n+\n+  # ── surface pattern AST (mirrors Rian.Pratt patterns) ──\n+  # `as`/pin patterns are not produced by the shared pattern parser, and `rpat` is\n+  # a Lower-internal Rust artifact — so they are not surface-reachable.\n+  type SPat := PWild\n+             | PVar(String)\n+             | PLitInt(Int53)\n+             | PLitStr(String)\n+             | PPChar(Int53)\n+             | PPAtom(String)\n+             | PPTuple(Vec(SPat))\n+             | PCtor(String, Vec(SPat))\n+             | PPList(Vec(SPat), PTail)\n+             | PPStruct(String, Vec(PPair))\n+             | PPMap(Vec(PPair))\n+\n+  type PTail := PtlClose | PtlTail(SPat)\n+  type PPair := PPr(String, SPat)\n+\n+  # ── expression → canonical s-expression ──\n+  pub def emit(Surface) String\n+  pub def emit(SNum(t)) := \"(num \" <> t <> \")\"\n+  pub def emit(SStr(s)) := \"(str \" <> s <> \")\"\n+  pub def emit(SChar(c)) := \"(char \" <> Prim.int_to_string(c) <> \")\"\n+  pub def emit(SId(n)) := \"(id \" <> n <> \")\"\n+  pub def emit(SAtom(a)) := \"(atom \" <> a <> \")\"\n+  pub def emit(SUnary(op, x)) := \"(unary \" <> op <> \" \" <> emit(x) <> \")\"\n+  pub def emit(SBin(op, l, r)) := \"(bin \" <> op <> \" \" <> emit(l) <> \" \" <> emit(r) <> \")\"\n+  pub def emit(SCall(f, args)) := \"(call \" <> emit(f) <> \" [\" <> emit_list(args) <> \"])\"\n+  pub def emit(SLabel(n, e)) := \"(label \" <> n <> \" \" <> emit(e) <> \")\"\n+  pub def emit(SDot(h, n)) := \"(dot \" <> emit(h) <> \" \" <> n <> \")\"\n+  pub def emit(SIf(c, t, e)) := \"(if \" <> emit(c) <> \" \" <> emit(t) <> \" \" <> emit(e) <> \")\"\n+  pub def emit(STuple(es)) := \"(tuple [\" <> emit_list(es) <> \"])\"\n+  pub def emit(SList(es, tl)) := \"(list [\" <> emit_list(es) <> \"] \" <> emit_tail(tl) <> \")\"\n+  pub def emit(SMap(ps)) := \"(map [\" <> emit_pairs(ps) <> \"])\"\n+  pub def emit(SBlock(ss)) := \"(block [\" <> emit_stmts(ss) <> \"])\"\n+  pub def emit(SCase(s, arms)) := \"(case \" <> emit(s) <> \" [\" <> emit_arms(arms) <> \"])\"\n+  pub def emit(SLambda(ps, b)) := \"(lambda [\" <> emit_params(ps) <> \"] \" <> emit(b) <> \")\"\n+  pub def emit(SCapArg(n)) := \"(cap_arg \" <> Prim.int_to_string(n) <> \")\"\n+  pub def emit(SCapture(b)) := \"(capture \" <> emit(b) <> \")\"\n+  pub def emit(SCaptureNamed(p, a)) := \"(capture_named \" <> emit(p) <> \" \" <> Prim.int_to_string(a) <> \")\"\n+  pub def emit(SWith(cs, b, els)) :=\n+    \"(with [\" <> emit_clauses(cs) <> \"] \" <> emit(b) <> \" [\" <> emit_arms(els) <> \"])\"\n+\n+  def emit_list(Vec(Surface)) String\n+  def emit_list([]) := \"\"\n+  def emit_list([x]) := emit(x)\n+  def emit_list([x | rest]) := emit(x) <> \" \" <> emit_list(rest)\n+\n+  def emit_tail(STail) String\n+  def emit_tail(TClose) := \"close\"\n+  def emit_tail(TTail(e)) := \"(tail \" <> emit(e) <> \")\"\n+\n+  def emit_pairs(Vec(SPair)) String\n+  def emit_pairs([]) := \"\"\n+  def emit_pairs([p]) := one_pair(p)\n+  def emit_pairs([p | rest]) := one_pair(p) <> \" \" <> emit_pairs(rest)\n+\n+  def one_pair(SPair) String\n+  def one_pair(SP(k, v)) := \"(\" <> k <> \" \" <> emit(v) <> \")\"\n+\n+  def emit_stmts(Vec(SStmt)) String\n+  def emit_stmts([]) := \"\"\n+  def emit_stmts([s]) := emit_stmt(s)\n+  def emit_stmts([s | rest]) := emit_stmt(s) <> \" \" <> emit_stmts(rest)\n+\n+  def emit_stmt(SStmt) String\n+  def emit_stmt(SBind(n, e)) := \"(bind \" <> n <> \" \" <> emit(e) <> \")\"\n+  def emit_stmt(STypedBind(n, t, e)) := \"(typed_bind \" <> n <> \" \" <> t <> \" \" <> emit(e) <> \")\"\n+  def emit_stmt(SExpr(e)) := \"(expr \" <> emit(e) <> \")\"\n+\n+  def emit_arms(Vec(SArm)) String\n+  def emit_arms([]) := \"\"\n+  def emit_arms([a]) := emit_arm(a)\n+  def emit_arms([a | rest]) := emit_arm(a) <> \" \" <> emit_arms(rest)\n+\n+  def emit_arm(SArm) String\n+  def emit_arm(SArm(p, g, b)) := \"(arm \" <> emit_pat(p) <> \" \" <> emit_guard(g) <> \" \" <> emit(b) <> \")\"\n+\n+  def emit_guard(SGuard) String\n+  def emit_guard(GNone) := \"nil\"\n+  def emit_guard(GSome(e)) := emit(e)\n+\n+  def emit_clauses(Vec(SClause)) String\n+  def emit_clauses([]) := \"\"\n+  def emit_clauses([c]) := emit_clause(c)\n+  def emit_clauses([c | rest]) := emit_clause(c) <> \" \" <> emit_clauses(rest)\n+\n+  def emit_clause(SClause) String\n+  def emit_clause(SClause(p, e)) := \"(clause \" <> emit_pat(p) <> \" \" <> emit(e) <> \")\"\n+\n+  def emit_params(Vec(SParam)) String\n+  def emit_params([]) := \"\"\n+  def emit_params([p]) := emit_param(p)\n+  def emit_params([p | rest]) := emit_param(p) <> \" \" <> emit_params(rest)\n+\n+  def emit_param(SParam) String\n+  def emit_param(SParam(n, PtNone)) := \"(\" <> n <> \")\"\n+  def emit_param(SParam(n, PtSome(t))) := \"(\" <> n <> \" \" <> t <> \")\"\n+\n+  # ── pattern → canonical s-expression ──\n+  pub def emit_pat(SPat) String\n+  pub def emit_pat(PWild) := \"(pwild)\"\n+  pub def emit_pat(PVar(n)) := \"(pvar \" <> n <> \")\"\n+  pub def emit_pat(PLitInt(v)) := \"(plit_int \" <> Prim.int_to_string(v) <> \")\"\n+  pub def emit_pat(PLitStr(v)) := \"(plit_str \" <> v <> \")\"\n+  pub def emit_pat(PPChar(c)) := \"(pchar \" <> Prim.int_to_string(c) <> \")\"\n+  pub def emit_pat(PPAtom(a)) := \"(patom \" <> a <> \")\"\n+  pub def emit_pat(PPTuple(ps)) := \"(ptuple [\" <> emit_pat_list(ps) <> \"])\"\n+  pub def emit_pat(PCtor(c, args)) := \"(pctor \" <> c <> \" [\" <> emit_pat_list(args) <> \"])\"\n+  pub def emit_pat(PPList(ps, tl)) := \"(plist [\" <> emit_pat_list(ps) <> \"] \" <> emit_ptail(tl) <> \")\"\n+  pub def emit_pat(PPStruct(n, fs)) := \"(pstruct \" <> n <> \" [\" <> emit_ppairs(fs) <> \"])\"\n+  pub def emit_pat(PPMap(fs)) := \"(pmap [\" <> emit_ppairs(fs) <> \"])\"\n+\n+  def emit_pat_list(Vec(SPat)) String\n+  def emit_pat_list([]) := \"\"\n+  def emit_pat_list([p]) := emit_pat(p)\n+  def emit_pat_list([p | rest]) := emit_pat(p) <> \" \" <> emit_pat_list(rest)\n+\n+  def emit_ptail(PTail) String\n+  def emit_ptail(PtlClose) := \"close\"\n+  def emit_ptail(PtlTail(p)) := \"(tail \" <> emit_pat(p) <> \")\"\n+\n+  def emit_ppairs(Vec(PPair)) String\n+  def emit_ppairs([]) := \"\"\n+  def emit_ppairs([p]) := one_ppair(p)\n+  def emit_ppairs([p | rest]) := one_ppair(p) <> \" \" <> emit_ppairs(rest)\n \n-  # The typed Core IR node (mirrors the `Rian.Core` E* structs for this slice).\n-  type Core := CNum(String)\n-             | CStr(String)\n-             | CId(String)\n-             | CAtom(String)\n-             | CUnary(String, Core)\n-             | CBin(String, Core, Core)\n-\n-  # Lower a surface node into its Core node, recursing into sub-expressions —\n-  # the surface→Core translation `Rian.Core.from_expr` performs.\n-  pub def lower(Surface) Core\n-  pub def lower(SNum(t)) := CNum(t)\n-  pub def lower(SStr(v)) := CStr(v)\n-  pub def lower(SId(n)) := CId(n)\n-  pub def lower(SAtom(a)) := CAtom(a)\n-  pub def lower(SUnary(op, x)) := CUnary(op, lower(x))\n-  pub def lower(SBin(op, l, r)) := CBin(op, lower(l), lower(r))\n+  def one_ppair(PPair) String\n+  def one_ppair(PPr(k, p)) := \"(\" <> k <> \" \" <> emit_pat(p) <> \")\"\n end","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/examples%2Frian%2Fselfhost_core.rian","sha":"812347f18de0a9f7c2460c6d96dba21476d0f916","status":"modified"},{"additions":2,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/lib%2Frian%2Fself_host.ex","changes":4,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/lib%2Frian%2Fself_host.ex?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":2,"filename":"lib/rian/self_host.ex","patch":"@@ -67,11 +67,11 @@ defmodule Rian.SelfHost do\n       id: :core_ir,\n       name: \"Typed Core IR (from_expr/from_pat)\",\n       role: :frontend,\n-      status: :partial,\n+      status: :self_hosted,\n       source: \"selfhost_core.rian\",\n       test: \"test/rian/core_fixpoint_test.exs\",\n       note:\n-        \"surface→Core lowering equals Rian.Core.from_expr over the literal/unary/binary slice; calls/lists/blocks/lambdas remain\"\n+        \"the full surface→Core lowering — every Pratt-produced expression (literals/unary/binary/call/label/dot/if/tuple/list/map/block/case/lambda/capture/with) and pattern (wild/var/lit/char/atom/tuple/ctor/list/struct/map) — equals Rian.Core.from_expr/from_pat; the Lower-internal resolved nodes + never-parsed as/pin patterns are not surface-reachable\"\n     },\n     %{\n       id: :checker,","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/lib%2Frian%2Fself_host.ex","sha":"45f9cfd6656a22b179fef652cb482e626614707b","status":"modified"},{"additions":215,"blob_url":"https://github.com/wstein/rian-lab/blob/11eadd47ccf3b367e74bee318b4b086222b88aaa/test%2Frian%2Fcore_fixpoint_test.exs","changes":273,"contents_url":"https://api.github.com/repos/wstein/rian-lab/contents/test%2Frian%2Fcore_fixpoint_test.exs?ref=11eadd47ccf3b367e74bee318b4b086222b88aaa","deletions":58,"filename":"test/rian/core_fixpoint_test.exs","patch":"@@ -4,98 +4,255 @@ defmodule Rian.CoreFixpointTest do\n \n   alias Rian.{Beam, Core, Pratt}\n \n-  # Stage of the bootstrap ladder (ADR-0063) *after* the parser: a Rian-written\n-  # surface→Core lowering (examples/rian/selfhost_core.rian), compiled to real\n-  # `.beam`, diffed against the reference `Rian.Core.from_expr` over a corpus.\n+  # Self-hosting fixpoint (ADR-0063) for the **typed Core IR** stage (full): a\n+  # Rian-written surface→Core lowering (examples/rian/selfhost_core.rian),\n+  # compiled to real `.beam`, diffed against the reference\n+  # `Rian.Core.from_expr`/`from_pat` over the FULL surface front-door.\n   #\n-  # The chain is real end-to-end: a source string is parsed by the reference\n-  # `Rian.Pratt.parse` into surface tuples, those are injected into the port's\n-  # `Surface` sum, and the port's `lower/1` output is compared term-for-term with\n-  # `Rian.Core.from_expr` of the same surface — canonicalised to the same tagged\n-  # form, so the comparison is exact (the Core analog of the parser fixpoint).\n-  #\n-  # Slice (the expression-parser slice's vocabulary): number/string/identifier/\n-  # atom literals, prefix unary, binary operators — recursively. The remaining\n-  # `from_expr` vocabulary (calls, lists, blocks, lambdas, …) is the `:partial`\n-  # tail (ADR-0063 / docs/self-host-status.md).\n+  # Each node is rendered to a canonical s-expression. The port emits it directly;\n+  # this test renders the reference `Rian.Core` struct to the SAME s-expression\n+  # (`canon`/`canon_pat`) and asserts equality over a corpus that exercises every\n+  # surface-reachable construct. The Lower-internal resolved nodes\n+  # (variant_lit/struct_lit/const_ref), decl-resolved str_interp, Rust-baked rpat,\n+  # and the never-parsed `as`/pin patterns are not surface, so they are out of scope.\n \n   setup_all do\n     {:ok, mod} = Beam.load(File.read!(\"examples/rian/selfhost_core.rian\"), :rian_core_fixpoint)\n     {:ok, mod: mod}\n   end\n \n-  # Inject a `Rian.Pratt` surface tuple into the port's `Surface` sum\n-  # (constructor `SNum` lowers to the `:s_num` tag, etc.).\n-  defp inj({:num, s}), do: {:s_num, s}\n+  # ── inject: Rian.Pratt surface tuple -> the port's Surface sum ──\n+  defp inj({:num, t}), do: {:s_num, t}\n   defp inj({:str, s}), do: {:s_str, s}\n-  defp inj({:id, s}), do: {:s_id, s}\n+  defp inj({:char, c}), do: {:s_char, c}\n+  defp inj({:id, n}), do: {:s_id, n}\n   defp inj({:atom, a}), do: {:s_atom, a}\n   defp inj({:unary, op, x}), do: {:s_unary, op, inj(x)}\n   defp inj({:bin, op, l, r}), do: {:s_bin, op, inj(l), inj(r)}\n+  defp inj({:call, f, args}), do: {:s_call, inj(f), Enum.map(args, &inj/1)}\n+  defp inj({:label, n, e}), do: {:s_label, n, inj(e)}\n+  defp inj({:dot, h, n}), do: {:s_dot, inj(h), n}\n+  defp inj({:if, c, t, e}), do: {:s_if, inj(c), inj(t), inj(e)}\n+  defp inj({:tuple, es}), do: {:s_tuple, Enum.map(es, &inj/1)}\n+  defp inj({:list_lit, es, tail}), do: {:s_list, Enum.map(es, &inj/1), inj_tail(tail)}\n+  defp inj({:map_lit, ps}), do: {:s_map, Enum.map(ps, fn {k, v} -> {:sp, k, inj(v)} end)}\n+  defp inj({:block, ss}), do: {:s_block, Enum.map(ss, &inj_stmt/1)}\n+  defp inj({:case, s, arms}), do: {:s_case, inj(s), Enum.map(arms, &inj_arm/1)}\n+  defp inj({:lambda, ps, b}), do: {:s_lambda, Enum.map(ps, &inj_param/1), inj(b)}\n+  defp inj({:cap_arg, n}), do: {:s_cap_arg, n}\n+  defp inj({:capture, b}), do: {:s_capture, inj(b)}\n+  defp inj({:capture_named, p, a}), do: {:s_capture_named, inj(p), a}\n+\n+  defp inj({:with, cls, body, els}) do\n+    {:s_with, Enum.map(cls, fn {p, e} -> {:s_clause, inj_pat(p), inj(e)} end), inj(body),\n+     Enum.map(els, &inj_arm/1)}\n+  end\n+\n+  defp inj_tail(nil), do: :t_close\n+  defp inj_tail(:close), do: :t_close\n+  defp inj_tail({:tail, e}), do: {:t_tail, inj(e)}\n+\n+  defp inj_stmt({:bind, n, e}), do: {:s_bind, n, inj(e)}\n+  defp inj_stmt({:typed_bind, n, t, e}), do: {:s_typed_bind, n, t, inj(e)}\n+  defp inj_stmt({:expr, e}), do: {:s_expr, inj(e)}\n+\n+  defp inj_arm({p, g, b}), do: {:s_arm, inj_pat(p), inj_guard(g), inj(b)}\n+  defp inj_guard(nil), do: :g_none\n+  defp inj_guard(e), do: {:g_some, inj(e)}\n+\n+  defp inj_param({name, nil}), do: {:s_param, name, :pt_none}\n+  defp inj_param({name, ty}), do: {:s_param, name, {:pt_some, ty}}\n+\n+  defp inj_pat(:wild), do: :p_wild\n+  defp inj_pat({:var, n}), do: {:p_var, n}\n+  defp inj_pat({:lit, v}) when is_integer(v), do: {:p_lit_int, v}\n+  defp inj_pat({:lit, v}) when is_binary(v), do: {:p_lit_str, v}\n+  defp inj_pat({:char_lit, c}), do: {:pp_char, c}\n+  defp inj_pat({:atom, a}), do: {:pp_atom, a}\n+  defp inj_pat({:tuple, ps}), do: {:pp_tuple, Enum.map(ps, &inj_pat/1)}\n+  defp inj_pat({:ctor, c, args}), do: {:p_ctor, c, Enum.map(args, &inj_pat/1)}\n+  defp inj_pat({:list, ps, :close}), do: {:pp_list, Enum.map(ps, &inj_pat/1), :ptl_close}\n+\n+  defp inj_pat({:list, ps, {:tail, t}}),\n+    do: {:pp_list, Enum.map(ps, &inj_pat/1), {:ptl_tail, inj_pat(t)}}\n+\n+  defp inj_pat({:struct, n, fs}),\n+    do: {:pp_struct, n, Enum.map(fs, fn {f, p} -> {:p_pr, f, inj_pat(p)} end)}\n+\n+  defp inj_pat({:map, kvs}),\n+    do: {:pp_map, Enum.map(kvs, fn {k, p} -> {:p_pr, k, inj_pat(p)} end)}\n+\n+  # ── canon: reference Rian.Core struct -> the same canonical s-expression ──\n+  defp canon(%Core.ENum{text: t}), do: \"(num #{t})\"\n+  defp canon(%Core.EStr{value: s}), do: \"(str #{s})\"\n+  defp canon(%Core.EChar{value: c}), do: \"(char #{c})\"\n+  defp canon(%Core.EId{name: n}), do: \"(id #{n})\"\n+  defp canon(%Core.EAtom{name: a}), do: \"(atom #{a})\"\n+  defp canon(%Core.EUnary{op: op, arg: x}), do: \"(unary #{op} #{canon(x)})\"\n+  defp canon(%Core.EBin{op: op, left: l, right: r}), do: \"(bin #{op} #{canon(l)} #{canon(r)})\"\n+  defp canon(%Core.ECall{fun: f, args: as}), do: \"(call #{canon(f)} [#{join(as)}])\"\n+  defp canon(%Core.ELabel{name: n, expr: e}), do: \"(label #{n} #{canon(e)})\"\n+  defp canon(%Core.EDot{head: h, name: n}), do: \"(dot #{canon(h)} #{n})\"\n+  defp canon(%Core.EIf{cond: c, then: t, else: e}), do: \"(if #{canon(c)} #{canon(t)} #{canon(e)})\"\n+  defp canon(%Core.ETuple{elems: es}), do: \"(tuple [#{join(es)}])\"\n+\n+  defp canon(%Core.EList{elems: es, tail: tl}),\n+    do: \"(list [#{join(es)}] #{canon_tail(tl)})\"\n+\n+  defp canon(%Core.EMap{pairs: ps}),\n+    do: \"(map [#{Enum.map_join(ps, \" \", fn {k, v} -> \"(#{k} #{canon(v)})\" end)}])\"\n+\n+  defp canon(%Core.EBlock{stmts: ss}), do: \"(block [#{Enum.map_join(ss, \" \", &canon_stmt/1)}])\"\n+\n+  defp canon(%Core.ECase{scrut: s, arms: arms}),\n+    do: \"(case #{canon(s)} [#{Enum.map_join(arms, \" \", &canon_arm/1)}])\"\n+\n+  defp canon(%Core.ELambda{params: ps, body: b}),\n+    do: \"(lambda [#{Enum.map_join(ps, \" \", &canon_param/1)}] #{canon(b)})\"\n+\n+  defp canon(%Core.ECapArg{n: n}), do: \"(cap_arg #{n})\"\n+  defp canon(%Core.ECapture{body: b}), do: \"(capture #{canon(b)})\"\n+  defp canon(%Core.ECaptureNamed{path: p, arity: a}), do: \"(capture_named #{canon(p)} #{a})\"\n+\n+  defp canon(%Core.EWith{clauses: cls, body: b, els: els}) do\n+    c = Enum.map_join(cls, \" \", fn {p, e} -> \"(clause #{canon_pat(p)} #{canon(e)})\" end)\n+    \"(with [#{c}] #{canon(b)} [#{Enum.map_join(els, \" \", &canon_arm/1)}])\"\n+  end\n \n-  # Canonicalise a reference `Rian.Core` struct into the port's tagged Core form\n-  # (`CNum` lowers to `:c_num`, etc.), so the two are compared term-for-term.\n-  defp canon(%Core.ENum{text: t}), do: {:c_num, t}\n-  defp canon(%Core.EStr{value: v}), do: {:c_str, v}\n-  defp canon(%Core.EId{name: n}), do: {:c_id, n}\n-  defp canon(%Core.EAtom{name: a}), do: {:c_atom, a}\n-  defp canon(%Core.EUnary{op: op, arg: a}), do: {:c_unary, op, canon(a)}\n-  defp canon(%Core.EBin{op: op, left: l, right: r}), do: {:c_bin, op, canon(l), canon(r)}\n+  defp join(es), do: Enum.map_join(es, \" \", &canon/1)\n+  defp canon_tail(:close), do: \"close\"\n+  defp canon_tail(e), do: \"(tail #{canon(e)})\"\n \n-  defp reference(src), do: src |> Pratt.parse() |> Core.from_expr() |> canon()\n-  defp ported(mod, src), do: mod.lower(inj(Pratt.parse(src)))\n+  defp canon_stmt({:bind, n, e}), do: \"(bind #{n} #{canon(e)})\"\n+  defp canon_stmt({:typed_bind, n, t, e}), do: \"(typed_bind #{n} #{t} #{canon(e)})\"\n+  defp canon_stmt({:expr, e}), do: \"(expr #{canon(e)})\"\n \n-  @corpus [\n+  defp canon_arm({p, g, b}), do: \"(arm #{canon_pat(p)} #{canon_guard(g)} #{canon(b)})\"\n+  defp canon_guard(nil), do: \"nil\"\n+  defp canon_guard(e), do: canon(e)\n+\n+  defp canon_param({name, nil}), do: \"(#{name})\"\n+  defp canon_param({name, ty}), do: \"(#{name} #{ty})\"\n+\n+  defp canon_pat(%Core.PWild{}), do: \"(pwild)\"\n+  defp canon_pat(%Core.PVar{name: n}), do: \"(pvar #{n})\"\n+  defp canon_pat(%Core.PLit{value: v}) when is_integer(v), do: \"(plit_int #{v})\"\n+  defp canon_pat(%Core.PLit{value: v}) when is_binary(v), do: \"(plit_str #{v})\"\n+  defp canon_pat(%Core.PChar{value: c}), do: \"(pchar #{c})\"\n+  defp canon_pat(%Core.PAtom{name: a}), do: \"(patom #{a})\"\n+  defp canon_pat(%Core.PTuple{elems: ps}), do: \"(ptuple [#{join_pat(ps)}])\"\n+  defp canon_pat(%Core.PCtor{ctor: c, args: args}), do: \"(pctor #{c} [#{join_pat(args)}])\"\n+\n+  defp canon_pat(%Core.PList{elems: ps, tail: tl}),\n+    do: \"(plist [#{join_pat(ps)}] #{canon_ptail(tl)})\"\n+\n+  defp canon_pat(%Core.PStruct{name: n, fields: fs}),\n+    do: \"(pstruct #{n} [#{Enum.map_join(fs, \" \", fn {f, p} -> \"(#{f} #{canon_pat(p)})\" end)}])\"\n+\n+  defp canon_pat(%Core.PMap{pairs: ps}),\n+    do: \"(pmap [#{Enum.map_join(ps, \" \", fn {k, p} -> \"(#{k} #{canon_pat(p)})\" end)}])\"\n+\n+  defp join_pat(ps), do: Enum.map_join(ps, \" \", &canon_pat/1)\n+  defp canon_ptail(:close), do: \"close\"\n+  defp canon_ptail(p), do: \"(tail #{canon_pat(p)})\"\n+\n+  defp ported(mod, src), do: mod.emit(inj(Pratt.parse(src)))\n+  defp reference(src), do: canon(Core.from_expr(Pratt.parse(src)))\n+\n+  # one pattern, extracted from a case arm head (the shared pattern parser).\n+  defp pat_of(src) do\n+    {:case, _, [{p, _, _} | _]} = Pratt.parse(\"case _scrut do\\n  #{src} -> 0\\nend\")\n+    p\n+  end\n+\n+  defp ported_pat(mod, src), do: mod.emit_pat(inj_pat(pat_of(src)))\n+  defp reference_pat(src), do: canon_pat(Core.from_pat(pat_of(src)))\n+\n+  @expr_corpus [\n     \"1\",\n     \"x\",\n-    ~S|\"hi\"|,\n+    \"'A'\",\n     \":ok\",\n+    ~S|\"hi\"|,\n     \"-x\",\n     \"not a\",\n-    \"1 + 2\",\n     \"1 + 2 * 3\",\n     \"(1 + 2) * 3\",\n-    \"a - b - c\",\n-    \"a < b and c\",\n-    \"x * y + z * w\",\n-    \"-a + b\",\n-    \"a <> b <> c\",\n-    \"not a and b or c\",\n-    \"f - g * h rem k\"\n+    \"f(x, 1)\",\n+    \"g()\",\n+    \"Point(x: 1, y: 2)\",\n+    \"obj.field\",\n+    \"obj.f(1, 2)\",\n+    \"if c do 1 else 2 end\",\n+    \"if c do 1 end\",\n+    \"{1, 2}\",\n+    \"{:ok, x}\",\n+    \"[1, 2, 3]\",\n+    \"[1, 2 | rest]\",\n+    \"[]\",\n+    \"%{a: 1, b: x}\",\n+    \"%{}\",\n+    \"(p) -> p + 1\",\n+    \"(a, b) -> a * b\",\n+    \"() -> 0\",\n+    \"&(_1 + _2)\",\n+    \"&foo/2\",\n+    \"with y <- f(x) do y else z -> 0 end\",\n+    \"with a <- p(), b <- q(a) do a + b end\",\n+    \"case n do\\n  0 -> \\\"z\\\"\\n  m -> \\\"nz\\\"\\nend\",\n+    \"f(a + b, c) * -d\"\n   ]\n \n-  describe \"self-hosting Core fixpoint (ADR-0063) — Rian lowering vs Rian.Core.from_expr\" do\n-    test \"the compiled Rian lowering agrees with from_expr term-for-term\", %{mod: mod} do\n-      for src <- @corpus do\n+  @pat_corpus [\n+    \"_\",\n+    \"x\",\n+    \"42\",\n+    ~S|\"hi\"|,\n+    \"true\",\n+    \"'A'\",\n+    \":ok\",\n+    \"Some(v)\",\n+    \"None\",\n+    \"Wrap(Some(v))\",\n+    \"[a, b]\",\n+    \"[h | t]\",\n+    \"[]\",\n+    \"{a, b}\",\n+    \"{:ok, v}\",\n+    \"Point(x: a, y: b)\",\n+    \"%{k: w}\"\n+  ]\n+\n+  describe \"self-hosting Core fixpoint — Rian lowering vs Rian.Core.from_expr (full surface)\" do\n+    test \"every surface expression lowers exactly like Rian.Core.from_expr\", %{mod: mod} do\n+      for src <- @expr_corpus do\n         assert ported(mod, src) == reference(src),\n-               \"Core lowering diverged from Rian.Core.from_expr on #{inspect(src)}\"\n+               \"expr lowering diverged on #{inspect(src)}\"\n       end\n     end\n \n-    test \"structure is preserved: `1 + 2 * 3` nests `*` under `+`\", %{mod: mod} do\n-      assert ported(mod, \"1 + 2 * 3\") ==\n-               {:c_bin, \"+\", {:c_num, \"1\"}, {:c_bin, \"*\", {:c_num, \"2\"}, {:c_num, \"3\"}}}\n+    test \"every surface pattern lowers exactly like Rian.Core.from_pat\", %{mod: mod} do\n+      for src <- @pat_corpus do\n+        assert ported_pat(mod, src) == reference_pat(src),\n+               \"pattern lowering diverged on #{inspect(src)}\"\n+      end\n     end\n   end\n \n-  describe \"teeth — the fixpoint genuinely discriminates (not vacuous)\" do\n-    test \"operand order is preserved (a non-commutative op is not folded blindly)\", %{mod: mod} do\n-      # if the lowering dropped/reordered operands, these would collapse to equal.\n-      refute ported(mod, \"1 - 2\") == ported(mod, \"2 - 1\")\n-      assert ported(mod, \"1 - 2\") == {:c_bin, \"-\", {:c_num, \"1\"}, {:c_num, \"2\"}}\n+  describe \"teeth — structure, nesting, and node kind are all preserved\" do\n+    test \"nested calls/labels/dots keep their shape\", %{mod: mod} do\n+      assert ported(mod, \"obj.f(1, 2)\") == reference(\"obj.f(1, 2)\")\n+      refute ported(mod, \"f(a, b)\") == ported(mod, \"f(b, a)\")\n     end\n \n-    test \"the operator and node kind are preserved (not a constant projection)\", %{mod: mod} do\n-      refute ported(mod, \"1 + 2\") == ported(mod, \"1 * 2\")\n-      refute ported(mod, \"x\") == ported(mod, \":x\")\n-      refute ported(mod, \"x\") == ported(mod, ~S|\"x\"|)\n+    test \"constructor and list patterns recurse, not flatten\", %{mod: mod} do\n+      assert ported_pat(mod, \"Wrap(Some(v))\") == \"(pctor Wrap [(pctor Some [(pvar v)])])\"\n+      assert ported_pat(mod, \"[h | t]\") == \"(plist [(pvar h)] (tail (pvar t)))\"\n     end\n \n-    test \"a mutated reference must mismatch — the diff would catch a wrong lowering\", %{mod: mod} do\n-      # the port's lowering of `1 + 2` must NOT equal from_expr of a *different*\n-      # program; a port that returned a constant would pass the main test but fail here.\n-      refute ported(mod, \"1 + 2\") == reference(\"1 + 3\")\n-      assert ported(mod, \"1 + 2\") == reference(\"1 + 2\")\n+    test \"different node kinds produce different Core\", %{mod: mod} do\n+      refute ported(mod, \"{1, 2}\") == ported(mod, \"[1, 2]\")\n+      refute ported_pat(mod, \"42\") == ported_pat(mod, ~S|\"42\"|)\n     end\n   end\n end","raw_url":"https://github.com/wstein/rian-lab/raw/11eadd47ccf3b367e74bee318b4b086222b88aaa/test%2Frian%2Fcore_fixpoint_test.exs","sha":"679563d57dfc9e5b8a22616bab4a3a94259d99b5","status":"modified"}]}