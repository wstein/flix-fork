{"files":[{"additions":7,"blob_url":"https://github.com/wstein/flatbars/blob/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2FREADME.md","changes":9,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Fi18n-data%2FREADME.md?ref=ff28f9240452282fdaa412f8fd260d5d81c85beb","deletions":2,"filename":"trussbars/examples/i18n-data/README.md","patch":"@@ -14,10 +14,15 @@ The pattern, in two sentences:\n    dynamic catalog lookup happens *inside Rust*.\n \n ```sh\n-cargo run --release   # render the receipt in en/de/fr/pl (catalog + order loaded from YAML)\n-cargo test            # AOT renders + a VM-parity check (same template + data → byte-identical)\n+cargo run --release                          # render en/de/fr/pl from the embedded catalog.yaml\n+cargo run --release -- --catalog other.yaml  # fetch the catalog from disk at runtime instead\n+cargo test                                   # AOT renders + a VM-parity check (byte-identical)\n ```\n \n+`--catalog <path>` makes the \"swap the source\" claim literal — the messages come from that\n+file (`std::fs::read_to_string` + serde) instead of the embedded default, and nothing else\n+(template or helpers) changes.\n+\n ## The data ([`catalog.yaml`](catalog.yaml) + [`data.yaml`](data.yaml))\n \n The catalog and the order are authored as **YAML and decoded through serde** — not Rust","raw_url":"https://github.com/wstein/flatbars/raw/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2FREADME.md","sha":"fb86f41cd0d8bb6b215dccdf63a6f8424a0d609d","status":"modified"},{"additions":11,"blob_url":"https://github.com/wstein/flatbars/blob/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Flib.rs","changes":13,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Flib.rs?ref=ff28f9240452282fdaa412f8fd260d5d81c85beb","deletions":2,"filename":"trussbars/examples/i18n-data/src/lib.rs","patch":"@@ -138,13 +138,22 @@ pub fn load_order() -> Order {\n     serde_yaml::from_str(include_str!(\"../data.yaml\")).expect(\"data.yaml is valid YAML\")\n }\n \n-/// Compose the render context for `locale` from the separately-fetched catalog + order.\n+/// Compose the render context for `locale` from the embedded catalog + order.\n #[must_use]\n pub fn page(locale: &str) -> Page {\n+    page_with_catalog(locale, load_catalog())\n+}\n+\n+/// Compose the render context from an explicitly-supplied `catalog` (e.g. one fetched from\n+/// a file at runtime — see the bin's `--catalog`) plus the embedded order. This is the\n+/// \"swap the source\" seam: the template and helpers are identical regardless of where the\n+/// catalog came from.\n+#[must_use]\n+pub fn page_with_catalog(locale: &str, catalog: Catalog) -> Page {\n     let order = load_order();\n     Page {\n         locale: locale.to_string(),\n-        catalog: load_catalog(),\n+        catalog,\n         customer: order.customer,\n         total: order.total,\n         count: order.count,","raw_url":"https://github.com/wstein/flatbars/raw/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Flib.rs","sha":"127c6446a27990ba89850be2cdbb7f8e265c2b6e","status":"modified"},{"additions":97,"blob_url":"https://github.com/wstein/flatbars/blob/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Fmain.rs","changes":100,"contents_url":"https://api.github.com/repos/wstein/flatbars/contents/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Fmain.rs?ref=ff28f9240452282fdaa412f8fd260d5d81c85beb","deletions":3,"filename":"trussbars/examples/i18n-data/src/main.rs","patch":"@@ -4,11 +4,105 @@\n //! not hard-coded; the declared helpers (`t`/`plural`/`number`/`date`) look messages up in\n //! the catalog by key. The same template renders identically through the VM — see the\n //! `vm_render_matches_aot` test in the library.\n+//!\n+//! ```text\n+//! cargo run --release                          # embedded catalog.yaml\n+//! cargo run --release -- --catalog other.yaml  # a catalog fetched from disk at runtime\n+//! ```\n+//!\n+//! `--catalog <path>` makes the \"swap the source\" claim literal: the messages come from\n+//! that file instead of the embedded default, and nothing else (template or helpers)\n+//! changes.\n+\n+use std::process::ExitCode;\n+\n+use i18n_data::{Catalog, page, page_with_catalog, render_receipt};\n+\n+const LOCALES: [&str; 4] = [\"en\", \"de\", \"fr\", \"pl\"];\n+\n+fn main() -> ExitCode {\n+    let catalog = match parse_args(std::env::args().skip(1)) {\n+        Ok(None) => None, // embedded default\n+        Ok(Some(path)) => match load_catalog_file(&path) {\n+            Ok(catalog) => Some(catalog),\n+            Err(reason) => {\n+                eprintln!(\"i18n-data: {reason}\");\n+                return ExitCode::FAILURE;\n+            }\n+        },\n+        Err(reason) => {\n+            eprintln!(\"i18n-data: {reason}\\nusage: i18n-data [--catalog <path>]\");\n+            return ExitCode::FAILURE;\n+        }\n+    };\n \n-fn main() {\n-    for locale in [\"en\", \"de\", \"fr\", \"pl\"] {\n+    for locale in LOCALES {\n         println!(\"───── {locale} ─────\");\n-        print!(\"{}\", i18n_data::render_receipt(&i18n_data::page(locale)));\n+        let page = match &catalog {\n+            Some(catalog) => page_with_catalog(locale, catalog.clone()),\n+            None => page(locale),\n+        };\n+        print!(\"{}\", render_receipt(&page));\n         println!();\n     }\n+    ExitCode::SUCCESS\n+}\n+\n+/// Parse `--catalog <path>` out of the args (argv[0] already skipped). `None` = use the\n+/// embedded catalog. Errors on a `--catalog` missing its path or any unknown argument.\n+fn parse_args(mut args: impl Iterator<Item = String>) -> Result<Option<String>, String> {\n+    let mut catalog = None;\n+    while let Some(arg) = args.next() {\n+        match arg.as_str() {\n+            \"--catalog\" => {\n+                let path = args\n+                    .next()\n+                    .ok_or_else(|| \"--catalog needs a <path>\".to_string())?;\n+                catalog = Some(path);\n+            }\n+            other => return Err(format!(\"unknown argument `{other}`\")),\n+        }\n+    }\n+    Ok(catalog)\n+}\n+\n+/// Fetch a catalog from a YAML file at runtime — the genuine \"swap the source\" path.\n+fn load_catalog_file(path: &str) -> Result<Catalog, String> {\n+    let text = std::fs::read_to_string(path).map_err(|e| format!(\"reading {path}: {e}\"))?;\n+    serde_yaml::from_str(&text).map_err(|e| format!(\"parsing {path}: {e}\"))\n+}\n+\n+#[cfg(test)]\n+mod tests {\n+    use super::parse_args;\n+\n+    fn args(xs: &[&str]) -> impl Iterator<Item = String> {\n+        xs.iter()\n+            .map(|s| (*s).to_string())\n+            .collect::<Vec<_>>()\n+            .into_iter()\n+    }\n+\n+    #[test]\n+    fn no_args_uses_the_embedded_catalog() {\n+        assert_eq!(parse_args(args(&[])).unwrap(), None);\n+    }\n+\n+    #[test]\n+    fn catalog_flag_takes_a_path() {\n+        assert_eq!(\n+            parse_args(args(&[\"--catalog\", \"x.yaml\"])).unwrap(),\n+            Some(\"x.yaml\".to_string())\n+        );\n+    }\n+\n+    #[test]\n+    fn catalog_flag_without_a_path_errors() {\n+        assert!(parse_args(args(&[\"--catalog\"])).is_err());\n+    }\n+\n+    #[test]\n+    fn unknown_argument_errors() {\n+        assert!(parse_args(args(&[\"--nope\"])).is_err());\n+    }\n }","raw_url":"https://github.com/wstein/flatbars/raw/ff28f9240452282fdaa412f8fd260d5d81c85beb/trussbars%2Fexamples%2Fi18n-data%2Fsrc%2Fmain.rs","sha":"cea3263b3d575951be54081fc9c236895d561715","status":"modified"}]}